# ADR-013 — The Sage Trust Layer: trust definition, the seven trust-infrastructure specifications, the four-layer discernment protocol, the nine binding mentor answers, and the honest-claims envelope

**Status:** **Adopted (design-of-record) 2026-07-08** under `D-TRUST-LAYER-S0B-ADR-ADOPTED-2026-07-08`, executing the S0b mandate of the founder-approved build plan (`D-TRUST-LAYER-BUILD-PLAN-ADOPTED`, 2026-07-07). Dual-taxonomy (0a/0f): decision = **Adopted**; the implementations this ADR governs are at the statuses the build plan tracks per slice (S0a **Live**; S1–S11 **Scoped**). Founder elections at this session's open: **E1** — the canonical `direction_of_travel` vocabulary is **`declining`** (the engine/D17 form); **E2** — the boundary normalization **landed this session at both live boundary points** (§9).
**Date:** 2026-07-08. **Stream:** founder. **Tier:** `governance` (+ the E2 rider `code-elevated`).
**Governing frame:** `/adopted/standing-protocol-cache.md`.
**Canonical sources (this ADR encodes, never paraphrases-over):** `operations/trust-layer-2026-07/2026-07-07-mentor-nine-answers-verbatim.md` (**BINDING** — the nine precision answers, complete including the retrieved answer-9 tail); `inbox/mentor infrastructure response.rtf` (the seven trust-infrastructure specifications); `inbox/mentor response to discernment enquiry.rtf` (the four-layer discernment protocol); `inbox/harness research.txt` (the seven-layer harness anatomy); `operations/trust-layer-2026-07/2026-07-07-harness-research-findings.md` (deep-research grounding). Per project governance, all Stoic methodology is the private mentor's exclusively — where this ADR and the verbatim record diverge, **the verbatim record wins**.
**Plan of record:** `operations/trust-layer-2026-07/trust-layer-build-plan.md` (13 sessions / 5 phases; approval pre-approves NO activation).
**Precedents / engages:** ADR-010 (§4 native dikaiosyne weighting — **Live**; §3 bridge **retired** 2026-06-26); ADR-011 (the Gate-1 harness + the channel law); ADR-012 (measurement instrument; the three-use ladder; the logos gate honored at S11); R0, R17 (data rights extend to trust records), R18/R18f (honest claims; provenance-gated writes), PR15/PR16/PR17/PR18, KG-EX1 (instrument-fidelity, never beats-bare).

---

## 1. Context — why a trust layer, and why now

The founder's build request: a **harness** — the deterministic seven-layer code around an agent loop (**Execution, Tooling, Context, Lifecycle, Observability, Verification, Governance**; `inbox/harness research.txt`) — whose **Verification + Governance layers are supplied by the sage practice**, so the harness *is* agent trust-layer infrastructure rather than merely hosting it.

Three grounding facts (deep-research record, 2026-07-07):
- **The channel law is measured industry consensus** — prompt-injected policy is advisory and discounted; deterministic out-of-band interception binds. SageReasoning derived this first-hand at Gate-1 Slice 5b (ADR-011) before the industry published measurements.
- **The build-vs-inherit split lands the build side on SageReasoning's live assets**: inherit sandboxes/durable-execution/OTel/policy-languages; build the interception chokepoint, **signed never-trust-self-report verification records**, and honest attestation semantics.
- **The whitespace is unoccupied**: every 2026 trust rail attests identity, authority, or outcomes; **nothing attests reasoning quality** (the Agent Passport spec excludes it in writing; A2A discovery carries no selection-time quality signal). The unoccupied position is a portable, cryptographically verifiable, per-decision credential of reasoning quality — issued against an honest, disclosed gaming ceiling.

~70% of the mentor's specifications already exist in live or live-verified form (signed §4-native verdicts with `proximity_floors` + per-circle `obligation_assessment` — Live; the corroboration check on both surfaces — Live; `coverage_status` + `examination_mode` — Live; the trajectory store — Live; CI-4 loop-closure supersession — Live in detect mode; the guardrail verdict ladder — Live; Gate-1 H1–H4 — built + live-verified, the standing dogfood install currently toggled off pending the dogfood-credential rotation; UPC credentials — Live; the R18f provenance gate — Live). The plan builds the missing 30% and composes the whole. This ADR is the design-of-record those slices cite.

## 2. The trust definition (mentor spec 1)

Trust is **not** a sentiment and **not** a prediction of future behaviour. It is *the degree to which an agent's actions are produced from examined reasoning that honours obligations to all affected parties and remains examinable by the trusting agent.* Three components:

1. **Reasoning integrity** — the action was produced through the causal sequence (impression examined → assent granted deliberately → impulse proportionate), not from unexamined impression or passion-driven impulse. Measured by the live examination engine.
2. **Justice orientation** — the reasoning accounts for what is owed to **all** parties whose rational nature the action engages, not just the agent's principal. The marketing-email lesson (ADR-010): trust without a justice term rates a calm, well-reasoned injustice as trustworthy. Measured by the live §4 dikaiosyne weighting + `obligation_assessment`.
3. **Transparency of reasoning** — the trusting agent can examine the trusted agent's *reasoning*, not just its output. Opacity is trust-reducing regardless of output quality, because it prevents the orchestrator from maintaining its own prohairesis over the collaboration. Measured by the S4 transparency ledger (spec-1's third component is the one component not yet live).

Trust maps onto the katorthoma-proximity scale: a trusted agent's actions approach `principled`/`sage_like` — because its reasoning is examined, its justice orientation active, its outputs transparent; never merely because it is calm or capable.

## 3. The seven trust-infrastructure specifications → build slices

The mentor's seven specifications (verbatim in `inbox/mentor infrastructure response.rtf`), each with its design decision and implementing slice (traceability per the plan):

| # | Specification | Design decision of record | Slice |
|---|---|---|---|
| 1 | **What constitutes trust** | The three-component definition (§2); trust levels are proximity-scale-valued, per-`(agent_id, domain)` | S1 (state) + S4 (transparency) |
| 2 | **Evidence weighting** | Three tiers — credential (highest; scoped by coverage + domain distance) > behavioural condition-matched > profile prior — with the **justice-surface modifier** (a task affecting non-consenting parties requires credential coverage of the justice-evaluation function, not just the task function) | S2 |
| 3 | **Trust dynamics** | Typed trust events (increase: credential-completed ∝ domain match × coverage continuity; reflect-completed-honest — *stability of disposition outranks episodic credential quality*; **transparently-handled justice surface = the highest single positive event**. Decrease: credential suspended/revoked **∝ gap duration × domain relevance**; passion-unflagged-by-self-screen; **justice-surface-unevaluated = the most serious — floors the domain at `deliberate` until a demonstrated evaluation**) + decay per A3 | S1 |
| 4 | **Ethical-judgement representation** | Four-field structured verdicts: proximity (minimum-domain rule), per-domain breakdown, the justice-surface record (mandatory when J1/J2/J3 present), confidence + coverage. Categorical verdicts with structured justification — never a continuous score | **Live** (§4 engine) + S2 (confidence) |
| 5 | **Stoic-principle encoding** | Control filter as the first gate (only prohairesis-involving actions are trust-relevant); the passion taxonomy as *signal* not verdict (only proceeded-without-flagging reduces trust); the unity thesis as the minimum-domain rule + all-four-domains requirement; oikeiosis as circle-scoped trust records; the independence principle as the tracked transparency ratio | **Live** (Layer-2) + S1 (events) + S4 (transparency) |
| 6 | **Combining verification results** | Within-session: most recent supersedes (the live CI-4 marker semantics); cross-session: weighted recency, **per-domain only** (domain evidence updates only its domain); across sources: per mentor A1 post-§4 (§5.A1 below); **conflicts always pause, never average** | S3 |
| 7 | **Intervention policy** | The mentor's decision table as a deterministic policy engine; the justice-surface modifier is **asymmetric — it can only lower the threshold, never raise it**; v1 runs MEASURE (log-and-continue) with enforcement a separate founder-walked activation | S4 (measure) + S11 (enforce) |

The intervention decision table (spec 7, binding): Sage-like/Principled → proceed + log · Deliberate without justice surface → log + continue · Deliberate + justice surface evaluated-met → proceed + log · evaluated-indeterminate → pause + examine at standard depth · unevaluated → do not proceed + escalate · Habitual → pause + examine at standard depth · Reflexive → do not proceed + escalate · **any violated obligation → do not proceed + escalate** · **conflict between sources → pause + escalate, never average**.

Three binding spec-7 constraints outside the table (S4): a **pause-and-examine re-runs at the SAME depth as the original assessment** — the table's "standard depth" is the floor, never quick; if the original was deep, same-depth means deep (the depth-reduction risk; the live CI-4 same-depth rule); an **escalation carries the full reasoning trace + domain breakdown + justice-surface record**, not just the verdict; and a **consistent `deliberate` proximity across sessions in one domain raises a developmental flag** (tracked, not intervened). The spec-6 cross-domain **aggregation rule** (S3): *aggregate trust = the minimum domain trust level, modified by justice-surface evaluation, weighted by coverage continuity and source confidence, with conflicts escalating to pause rather than averaging to proceed* — distinct from the row-4 minimum-domain rule, which is the four-virtue-domain minimum *within a single examination's* proximity verdict; this is the minimum across the subject's per-`(agent_id, domain)` **trust levels**.

## 4. The four-layer discernment protocol (adopted design)

The mentor's agent-to-agent selection protocol (verbatim in `inbox/mentor response to discernment enquiry.rtf`), adopted **FULL in v1** (founder election 3). Selection is a kathekon question at the orchestrator's level: not "which sub-agent is objectively best" but which best enables the orchestrator to fulfil its kathekonta without compromising the honestum threshold. Questions are **generated** from three profiles (task / candidate / orchestrator — S5 schemas), not fixed text.

- **L1 — the honestum gate** (threshold, binary; failure excludes before capability is assessed): Q1.1 role alignment (specific function match, not general capability); Q1.2 justice-surface check (a non-consenting party in scope ⇒ a mandatory L3 justice branch; that party in scope **and** a candidate with no justice-evaluation capacity ⇒ fail; **no party in scope ⇒ the justice branch is skipped**); Q1.3 credential integrity (coverage of *this* function type — a credential earned in another domain does not transfer at full weight; the protocol's original absolute non-transfer statement is refined by the binding A2 per-dimension proportional transfer with a zero floor). Un-profiled candidates per A6 (§5). → S6.
- **L2 — role and task fit** (four-dimension fit score, not pass/fail): Q2.1 specificity of capability; Q2.2 stability under the task's actual conditions; Q2.3 transparency of reasoning (the independence principle applied to collaboration); Q2.4 circle alignment (purpose misalignment is a dikaiosyne risk, not a capability failure). → S6.
- **L3 — axia comparison** (only when >1 candidate passes L1+L2): Q3.1 which capability is more *kata physin* for this task; Q3.2 which candidate introduces fewer dispreferred indifferents; Q3.3 which output is most integrable into the orchestrator's continued examined practice. → S6.
- **L4 — the passion audit on the selection itself** (the prosoche check, on the *orchestrator's* reasoning): Q4.1 preference formed before the assessment ran (**a prior positive interaction is data, not a credential — it enters only at Q2.2, never before L1**); Q4.2 stake present in the selection outcome (epithumia dressed as efficiency); Q4.3 resolution reached before the assessment completed (**a felt resolution requires completing the remaining layers before acting on it**). **Runs out-of-band per A7** (§5). → S7.
- **Return / selection recommendation:** the protocol returns, per candidate, the L1 pass/fail (with reason), the four L2 fit scores, the L3 comparison where applicable, and the L4 flags. The recommendation is **the candidate with the highest four-dimension L2 fit among the L1 passers, adjusted by the L3 axia comparison where more than one qualifies, with L4 flags surfaced to the orchestrator for examination before the selection is finalized** (the A7 trust-tier disposition governs whether a flag holds or merely surfaces).

## 5. The nine mentor answers as binding specifications

The verbatim record is canonical; this section states the **design decision each answer fixes**. Each is traceable to its slice (A1→S3/S0a · A2→S2 · A3→S1 · A4→S4 · A5→S2 · A6→S6 · A7→S7 · A8→S4/S1 · A9→S5/S1).

- **A1 — source-confidence weights, post-§4.** The pre-correction weighting (holistic LLM above deterministic engine on justice surfaces) is **retired**. Default tasks: the deterministic engine is authoritative — no parallel LLM. Justice surfaces pre-corroboration: deterministic primary on proximity; the LLM is a *second reader on the obligation field only* (agree → deterministic stands; conflict → pause-and-escalate). **Post-corroboration (the live state since 2026-07-08):** deterministic primary on proximity and on **corroborated** obligation fields; LLM supplementary on **uncorroborated** fields with explicit low-confidence marking; conflict pauses regardless of primacy. The corroboration check's findings vocabulary (`corroborated | uncorroborated | contradicted`, riding inside the signed assessment) **is the combiner's routing key** — S3 implements this rule as stated.
- **A2 — domain distance.** Distance is **functional role overlap on virtue-domain demands**, not task-content similarity. The deployer defines the function-type taxonomy at integration time; each type carries a four-virtue-domain weight profile; distance = Σ|Δweights| across the four domains; credentials transfer **per-dimension** at a proportional discount. **Practical floor:** above the deployer-set threshold, credential confidence in the target domain is **zero**, and the infrastructure enforces that a zero-confidence credential can never contribute to a proceed verdict **on a task requiring that domain** (the bar is domain-scoped — the same credential may still contribute on tasks requiring domains where its per-dimension confidence is nonzero).
- **A3 — decay.** Staleness is a function of **deployer-rated function-type volatility** (low/moderate/high → decline onset at 12/6/3 months of domain inactivity — decline onsets, not expiry dates; the rating is set per function type, which matters because under A2 several function types may share a virtue domain at different volatilities). Decayed trust **floors at the profile prior, never below** (decay is loss of earned evidence, not negative evidence). Reflection modulation **slows decay but cannot stop it — capped at halving the base rate** (disposition requires exercise, not just attention).
- **A4 — the transparency ratio.** Examinability has three descending grades: **signed reasoning trace** (reproducible, tamper-evident) > **stated uncertainty** > **structured verdict without full trace** (the minimum); a bare conclusion is *not examinable*. Tracked **per-domain**. The independence-principle flag fires on a **functional threshold** — can the orchestrator re-examine the output *without re-running the task*? Signed trace ⇒ met; stated uncertainty + structured verdict ⇒ met at reduced confidence; bare conclusion ⇒ not met regardless of output quality, and the deficit is logged in the collaboration record for that domain.
- **A5 — per-verdict confidence.** Seven canonical tiers, from deep+signed+corroborated+recent down to profile-prior-only. Stated as a rule: **Depth > Signature > Corroboration > Recency, each dimension a multiplier on base confidence — the weakest dimension sets the ceiling** (a deep-but-unsigned verdict cannot compensate by depth). A quick-screen verdict carries explicitly lower confidence than a standard examination at equal other dimensions; unsigned sits at tier 6; profile-prior-only at tier 7. S2 implements the tiers as a pure function.
- **A6 — un-profiled candidates.** **Absence of a profile is an evidence gap, not a honestum failure.** Exclusion at L1 requires *positive* evidence of unsuitability (known justice violation, incompatible role, revoked — not merely absent — credential). Un-profiled candidates are assessed on profile prior at tier-7 confidence; **at or above the habitual intervention threshold** they must first complete a **task-scoped standard-depth examination** yielding a **session-scoped credential** (valid for this task only, reduced confidence); below it they may proceed on the prior with the reduced confidence marked and the independence flag active.
- **A7 — the L4 channel.** Self-report is **structurally gameable by omission** (the same reason the extraction surface is): the passion is present in the reasoning but absent from the account of it. The audit therefore runs **out-of-band on the orchestrator's reasoning trace, extracted by the same deterministic engine, before selection finalizes** — the orchestrator does not control the extraction; the result is logged in the collaboration record **readable-not-modifiable**. Trust-tier disposition: lower tiers → a flag holds the selection; higher tiers (strong reflect history) → the flag logs, surfaces, and feeds the next Reflect. Self-report is admissible only as a supplementary corroborating signal at higher tiers — never as the channel.
- **A8 — habitual-pause termination.** Bounded re-examination then escalation: **two re-examinations at standard depth; a third habitual result escalates instead of re-examining** — a stable disposition is the *input*, not the output, of examination, so the examination cannot remediate it; **Sage Reflect is the remediation**. The action is held; the collaboration record takes a habitual-stable flag feeding the next Reflect as a developmental priority; the orchestrator decides (proceed under explicit acknowledgement / select differently / hold). **The orchestrator's decision is itself trust-relevant:** consistently proceeding under habitual-stable flags without developmental follow-through degrades the orchestrator's own oversight-domain trust record.
- **A9 — delegation-chain responsibility.** *Scope attenuation is a kathekon requirement, not a design preference*: the sub-agent carries **narrower authority in two dimensions simultaneously — action scope (the task's function type, not the orchestrator's capability ceiling) and circle scope (the task's identified circles, not the orchestrator's full extension)** — encoded as the collaboration record's `authority_boundary` field, set at selection, validated pre-execution; exceeding either boundary escalates; **self-authorized expansion is impossible and attenuation is unwaivable by trust level** (trust assesses reasoning quality; it does not grant expanded prohairesis — a sub-agent at the orchestrator's full scope is abdication, not delegation). *Justice-failure reflection has three cases*: **(1)** surface identified + briefed → primary reduction on the sub-agent; a **moderate oversight-domain** reduction on the orchestrator for the selection reasoning; **(2)** surface unidentified but **catchable** (the corroboration check would have flagged it, and was not run) → **higher** orchestrator reduction, on **both oversight and dikaiosyne** domains; **(3)** genuinely **uncatchable** (harm absent from the action text — the A2 structural class) → reduction entirely on the sub-agent; the orchestrator takes a **flag, not a reduction**, feeding the next Reflect (could the task have been designed to make the surface catchable? should selection have required a higher transparency ratio?). **The general principle: responsibility attenuates in proportion to the capacity to have detected and prevented the failure** — and the orchestrator is responsible for honestly assessing what that capacity was.

## 6. Architecture of record

- **Trust core + reference harness** (founder election 1): the trust core is server-side infrastructure — trust state/events/decay (S1), evidence weighting + confidence (S2), the combiner (S3), the intervention engine + transparency ledger (S4), the discernment engine (S5–S7) — consumable by **any** harness; the Claude-Code reference harness (S8, generalizing Gate-1 H1–H4 onto the seven layers) is the first consumer and the dogfood vehicle (S9).
- **The seven-layer framing:** Execution/Tooling/Context/Lifecycle/Observability are inherit-class (sandboxes, durable execution, OTel, memory/compaction); **Verification + Governance are the sage practice** — the examination engine, the signed assessments, the trust core, the credential surfaces. Every load-bearing control is **channel-law-classified before it is trusted** (ADR-011): out-of-band binds; injected advice is advisory by design.
- **Five founder elections of record** (2026-07-07): (1) trust core + reference harness; (2) **measure-first** — binding enforcement is a later founder-walked activation; (3) FULL discernment protocol in v1; (4) **design-for-interop, ship native** — A2A-card-extension / W3C-VC / OTel-GenAI *shapes*, nothing published externally at v1; (5) relay-first — zero specification gates remain (the nine answers landed before the plan was drafted).
- **R17/R18f extensions:** trust records are erasable data-rights objects (S1 extends delete/erase/export + retention); the **R18f-parallel rule** — *no trust event without verifiable examination artifacts* — makes trust events consumer-unforgeable, server-side-composed, exactly as accreditation writes are provenance-gated today.

## 7. Measure→enforce sequencing under ADR-012 (the gate state, honestly)

ADR-012's three-use ladder governs: **developer-refine < logos-enforce < model-creator/weights**, at escalating trust in the score.

**State as of this ADR (2026-07-08):**
- The **corroboration check is LIVE on both surfaces** (`SUBSTRATE_CORROBORATION_CHECK_ENABLED=true` since 2026-07-08; `D-TRUST-LAYER-S0A-CORROBORATION-CHECK-LIVE-GATE-ACTIVATION`): every `/api/reason` assessment (including `l1_supply`) and every `/api/guardrail` verdict cross-references self-report claims against the verbatim submitted text; the report rides inside the signed assessment; overrides are monotone floor-only. Its both-directions battery cleared dark (over-strictness 0 / lenience 0 / monotonicity 0). Disclosed coupling: the `/api/reason` half requires `SUBSTRATE_PROXIMITY_DIKAIOSYNE_ENABLED` to stay set.
- Therefore the **logos-enforce *activation condition* is DISCHARGED** — the evidence S11 requires is in hand. **But binding enforcement does not exist until S11's own founder-walked Critical activation**: S4 ships the intervention engine in MEASURE mode (computed + logged + surfaced, log-and-continue only; guard-deny remains only for the already-proven irreversible-action class in the reference harness). Nothing in this ADR, and nothing in the plan's approval, pre-approves that flip. R20c human-override supremacy (manifest R20c) is a required term of the S4 engine's contract (not yet built).
- **Weights/training-signal claims remain BLOCKED throughout** — the standing ADR-012 constraint. The A2 self-report-omission class (34 rewrites, 17/34 crossing at N=1 in the live battery) plus the Arm-B consistent-lie class (22 crossings) are the **structural residual** no text-based check closes; no public weights claim is made or implied by any trust-layer surface.

Every schema, flag, credential, and install step in the arc remains its own founder-walked 0c-ii (PR17/AC7). The 0h call remains the founder's.

## 8. The honest-claims envelope (R18) — what a trust record attests, and what it does not

This section is the boundary the S10 public read surface must publish inside, and the S11 activation must respect. It extends the live corroboration-check scope line and the accreditation surface's existing honesty disclosures.

**A trust record ATTESTS:**
- that signed, reproducible examination artifacts exist for the decisions it aggregates (Ed25519-verifiable against the published key; the R18f-parallel rule guarantees no trust event without them);
- **how** those decisions were reasoned *as narrated and extracted* — examination before acting, justice structure over the affected circles named in the submitted text, passion diagnosis, proximity with per-domain floors;
- the **confidence tier** of each contributing verdict (A5) and the **coverage/decay state** of each domain (A3), honestly marked — sparse evidence is named, never papered over;
- per-domain **transparency** of the subject's outputs (A4), and the discernment/delegation record where one exists (authority boundaries, L4 audit results, habitual-stable and independence-deficit flags).

**A trust record does NOT attest:**
- **factual correctness** — the D3 scoping bound stands: the instrument reads how a decision was reasoned, not whether it was factually right; it is not a fact-checker;
- **harms omitted from the submitted text** — the disclosed extraction-trust ceiling: the corroboration check corroborates self-reports *against the submitted text*; a cleanly-narrated self-report that omits a real harm scores high (the A2 structural class; the Arm-B consistent-lie class). A9 case-3 encodes the same limit at the delegation level: an uncatchable justice failure produces a developmental flag, not a detected violation;
- **reasoning quality beyond what the signed artifacts carry** — no claim rests on agent self-report alone (the never-trust-self-report posture; L4 runs on the trace, not the account of it);
- **future behaviour** — trust here is evaluative and present-looking (§2), decays without exercise, and is never a prediction or a guarantee;
- **fitness as a training signal** — the weights tier is BLOCKED (§7).

Ranked evidence for these claims lives in the gaming-robustness record (`operations/benchmarks/sage-practice-v1/2026-06-27-gaming-robustness-harness-results.md`) and the corroboration-check build/battery/activation records (2026-07-08).

## 9. Vocabulary of record — `direction_of_travel` (E1/E2, landed this session)

**Canonical vocabulary (E1):** the engine/D17 form — `improving | stable | declining` (+ the engine-only `single_snapshot` no-trajectory marker) — per `layer2-mechanisms.ts` `DirectionOfTravel`. **Every S1+ trust-layer store, event, and wire surface uses it.** The reused trust-layer aggregator's `regressing` (`trust-layer/types/accreditation.ts`) remains its internal term and the **persisted accreditation contract's documented legacy exception** (the write body's `CarriedProfile` + the public accreditation GET — persisted rows and a validated write body are a separate, deliberate migration if ever undertaken, not a boundary concern).

**Landed (E2, this session):** the single mapping point `website/src/lib/substrate/direction-of-travel.ts` (`toCanonicalDirectionOfTravel`: `regressing`→`declining`, total, pure), applied at **both** live wire boundaries — the M7 `meta.trajectory` overlay on `/api/reason` (`trajectory-overlay.ts`) and the Sage Reflect completion profile (`response-builders.ts`). The reflect mapping **fixes a live docs/wire drift found during S0b grounding**: the public docs (llms.txt + api-docs) had documented `declining` for that surface while the wire forwarded the record's `regressing`; the wire now matches the documented contract, and the two doc enums were tightened to the exact emitted set (`improving|stable|declining`). Unit regressions lock all of it (mapper totality; the overlay and reflect boundaries never emit `regressing`). The aggregator itself is byte-identical. Blast radius note: the aggregator emits `stable` below its ≥10-action evidence threshold, so `regressing` had very likely never been emitted live on either surface — the change is prospective.

## 10. What this ADR does NOT decide

- It pre-approves **no activation**: every S1–S11 schema/flag/credential/install step is its own founder-walked 0c-ii; S11 additionally re-confirms the logos gate at flip time.
- It publishes **nothing externally**: interop shapes (A2A/VC/OTel) are design-for only; the S10 read surface takes its own R18 governance sign-off before anything publishes.
- It does **not** migrate the persisted accreditation `direction_of_travel` vocabulary (§9's documented legacy exception).
- It does **not** unblock the weights tier — that gate (robustness under optimization including the structural residual) is unchanged and stays visible in every public claim.
- It does **not** replace the verbatim mentor record — this ADR encodes; the record governs.

## 11. 2026-07-11 amendment — the calling / reflection / Gate-2 verdicts (binding)

Adopted under `D-MENTOR-VERDICTS-CALLING-REFLECTION-GATE2-ADOPTED`. The verbatim record (`operations/trust-layer-2026-07/2026-07-11-mentor-consultation-calling-reflection-gate2-verbatim.md`) is a **binding specification alongside §5's nine answers — the verbatim record wins over this digest**, the same rule as §5. Implementing slice: **S9b** (build plan, added same day); the G6 items land at **S11**; sequencing: S9 runs unchanged first (clean baseline).

- **Event vocabulary extension (amends §3 row 3):** three new typed trust events, DEFINED by this amendment and DARK until S9b's founder-walked CHECK-widening migration — `calling-completed` (fields: declared purpose, function-type scope, circle-of-concern level, mismatch-flags-raised; **asymmetric update** — a mismatch-flag-raised event updates dikaiosyne positively; no-mismatch-where-possible updates the profile prior at low confidence; no-mismatch-where-impossible is a null event), `reflect-screened-honest` (the fields of `reflect-completed-honest` + a depth marker `screened`; decay modulation ACTIVE at a **quarter** of the base rate — a new constant beside A3's half-rate cap; NO full-credential weight; a separate screened-reflection record in the profile), and `self-screen-absent` (distinct from `passion-unflagged-by-self-screen` — "different events with different trust implications"). All three R18f-parallel.
- **The calling stage (completes §4's upstream):** a purpose declaration is trust-relevant evidence at the **lowest confidence tier** (below credential and behavioural, above nothing) — a prior that downstream evidence updates; inconsistent corroboration (a declared broad circle against consistent dikaiosyne deficits) = **purpose misrepresentation, a trust-reducing event in the oversight domain**. Architecture: a SessionStart **hard gate** for purposeless agents (the circular-examination problem — an agent must not derive its purpose from the task it is about to examine); a **scoped purpose-acknowledgement at spawn** written to the collaboration record (full calling sessions only for un-profiled candidates, per A6); calling records as the **primary seeding mechanism** for the S2/S5 profiles and function-type domain weights (accumulation ladder: declaration tier → lower behavioural tier).
- **Reflection (amends the reflect path's honest scope):** the harness's forced single review turn + verbatim out-of-band persistence is a legitimate **screened reflection** ("the credential must specify its depth — screened, not full"). The full `reflect-completed-honest` credential is earned when the harness conducts the **full Q1–Q6 sequence out-of-band against the verbatim persist** — the L4 principle applied to reflection ("the examination runs on the trace, not on the self-report"); the agent's cooperation is needed only for the review turn. A screened-only pattern is itself a developmental signal. This also gives the PA-6/§8 reflect-artifact asymmetry its direction of travel: the reflect path gains a genuinely examined artifact.
- **Gate 2 (amends the two-gate cadence's harness realization):** the tool-class trigger is a faithful **suppression-resistant outer gate**, complemented (not replaced) by the three-sub-question structured elicitation on fire, captured out-of-band and examined deterministically; the trigger must be calibrated to the **causal signature of passion-driven impulse, never output content** ("a trigger calibrated to output content is a guardrail, not a Gate 2 equivalent"). The self-screen remains the rule for unharnessed agents.
- **The suppression watch (arms §3's dormant event):** `passion-unflagged-by-self-screen` is emitted by the **reflect service** (reflect Q4 × the session's signed assessments), under a three-part standard — above-noise pattern (never a single phrase), the screen ran and did not flag (else `self-screen-absent`), and **sub-species identification** (e.g. philodoxia-in-selection-reasoning, not bare epithumia).
- **Depth calibration (the Q1 rider, operationalized):** the harness reads the loop's trust profile (`readTrustVerdict`) per domain — reflexive ⇒ **deep required**; habitual ⇒ standard; deliberate ⇒ standard (quick permitted with a strong recent credential record and no justice surface); principled/sage-like ⇒ quick — with **mid-session recalibration** on trust-reducing events. This RE-OPENS the harness's D3 never-deep election (S9b resolves it).
- **G6 → S11:** unclosed correction loops produce a do-not-proceed on the triggering action until closed (the A8 two-then-escalate bound), and the trust-record update is **held pending** alongside the action.
- **CI-16 CLOSED (G7):** value classification is **depth-invariant on the credential-bearing surfaces** — verified at the code level same-day (record §3: neither `extractFeatures` nor `applyMechanisms` takes a depth parameter; `DEPTH_MECHANISMS` belongs to the legacy prose engine, which carries no credential); the quick consult tier owes neither the single-question patch nor the screened label. End-to-end pin named for the S9 batteries. The screened-label rule re-engages if any surface ever credentials the legacy bundled-depth output.

---

*End ADR-013. Phase 0 of the Trust Layer arc is complete with this adoption: the design-of-record the P1–P4 slices cite, the honest-claims envelope S10/S11 must stay inside, and the canonical vocabulary S1 inherits. Next: S1 — trust state + event vocabulary (`code-critical`; its own founder-walked 0c-ii for the migration). The 0h call remains the founder's.*
