Governing frame: /adopted/session-opening-protocol.md

I am opening a new session in the tech stream. The previous session (26 April 2026) completed ADR-PE-01 Session 5 (live `mentor_interactions` loader, PR1 single-endpoint proof on `/api/mentor/ring/proof`). Founder selected Framing A at session open with all Q-Loader-* defaults: hub-scoped per (user, hub) per ADR §1.4; per-consumer-request cadence; last 90 days + limit 100; keep 2A-skip on absence on existing live consumers (reflect, founder-hub) until loader is Verified live; defer backfill (natural per_request cadence will overwrite on next consumer call); single-endpoint proof target = the proof endpoint. Critical Change Protocol (0c-ii) was executed in full pre-deploy with eleven worst cases (A through K) accepted by founder approval. Five-probe verification completed in-session via consolidated Console snippet on `/founder-hub`: L1/L2 (cache hit, both labels, no bypass) returned `pattern_source: 'persisted'`, `interactions_source: null`; L3/L4 (FORCE RECOMPUTE, both labels, `bypass_pattern_cache: true`) returned `pattern_source: 'recomputed'`, `interactions_source: 'live_loader'` — the decisive verification that the loader was actually invoked, returned data, was synthesised through pattern-engine successfully, and the live-recomputed result was persisted; Re-L1 confirmed round-trip end-to-end on real data. TypeScript reached `tsc --noEmit` exit 0; the push completed via GitHub Desktop; Vercel reached Ready; smoke check on `/founder-hub` rendered normally. Session 5 reached Verified status. The session close is at `/operations/handoffs/tech/2026-04-26-ADR-PE-01-S5-loader-close.md`. One decision-log entry was Adopted at the close: `D-PE-01-S5-LOADER-VERIFIED`. Six decision-log entries (`D-PE-01-S1-1A-VERIFIED`, `D-PE-01-S2-2A-VERIFIED`, `D-PE-01-S3-3A-VERIFIED`, `D-PE-01-S35-3.5α-VERIFIED`, `D-PE-01-S4-4B-VERIFIED`, `D-PE-01-S5-LOADER-VERIFIED`) all currently carry "TBD per founder share" in the commit-hash slot. The §8 single-endpoint-proof sequence has reached the architectural completion of ADR-PE-01: the live `mentor_interactions` loader is Verified live on the proof endpoint, hub-scoped per (user, hub), per-consumer-request, last 90 days + limit 100, with the `bypass_pattern_cache` flag as the deterministic verification mechanism (also a permanent operational tool — force-recompute on demand). Reflect and founder-hub remain on 2A-skip on absence pending per-consumer 2A-recompute switching. The founder's `pattern_analyses['founder-mentor']` and `['private-mentor']` entries on the encrypted profile blob are now live-recomputed (overwritten by Probes L3/L4 from the prior session — worst case J accepted). This session continues from there.

I use GitHub Desktop to push commits. If any code lands this session, give me explicit GitHub Desktop instructions (open GitHub Desktop → confirm Current Repository = `sagereasoning` → confirm Current Branch = `main` → review the Changes pane → fill Summary + Description → click Commit to main → click Push origin → wait for spinner) per commit, in the same pattern the prior tech-stream sessions used.

The verification probe pattern uses a Console snippet on a deployed page; the working auth-cookie discovery for this project is `decodeURIComponent(cookies['sb-access-token'])` reading the raw JWT directly from the cookie value (no JSON envelope, no chunked patterns). Use that snippet pattern from the start. This pattern has now been used three times successfully without rediscovery (Sessions 3.5, 4, 5 — Session 5 being the consolidated five-probe snippet). PR8's third-recurrence rule supports promotion to canonical at this session open. Recommendation: promote it at session open as a small standalone item (or batch with whatever framing is picked).

Before touching any code, complete the session opening protocol:

* Tier declaration: this is at minimum a documentation/cleanup session; it becomes a code/tech session if Framing A or Framing C-with-code is picked. Read tier 1, 2, 3, 6 every-session sources + KG governance scan; add 8, 9 (code state) if a framing requiring code is picked.
* Read the most recent handoff in `/operations/handoffs/tech/`: `2026-04-26-ADR-PE-01-S5-loader-close.md`. Read end-to-end. The "Decisions Made", "Status Changes", "Open Questions", "Next Session Should", and "Process-Rule Citations" sections are the load-bearing parts.
* Read the adopted ADR in full: `/compliance/ADR-PE-01-pattern-analysis-storage.md`. Specifically §1.2 (loader (c) — now Verified live on the proof endpoint per Session 5), §1.4 (hub-scoping), §3, §6 (R17 footprint), §7 (Coordination with the loader — particularly §7.3 read cadence per consumer, newly relevant if this session takes on per-consumer 2A-recompute switching), §8 (Single-endpoint proof sequence — Sessions 1, 2, 3, 3.5, 4, 5 all Verified; live-consumer rollout via per-consumer 2A-recompute switch is the natural next step), §9 (Risks accepted), §10 (Rollback), and §12 (Open items O1–O5; O3 — `last_pattern_compute_at` plain column — becomes more relevant if cadence work or freshness queries surface during a per-consumer switch).
* Scan `/operations/knowledge-gaps.md` for any entry touching live-consumer integration with the loader (KG3 hub-label end-to-end contract — engages on every new reader/writer; KG7 JSONB shape — now mooted on the loader path because the defensive parse landed in Session 5 but re-engages on any new writer to a JSONB column; KG1 rule 2 awaited writes — engages if a per-consumer 2A-recompute switch introduces a new conditional write surface or changes the existing awaited writes; KG6 composition order — engages if any change alters prompt assembly).
* Confirm P0 hold-point status — should still be active. Pattern-engine integration sits inside the assessment set; the loader work completed Session 5; live-consumer rollout would be inside the same assessment.
* Confirm model selection (PR4). The likely next work items are pure DB-loader-routing work (no LLM added by switching reflect or founder-hub from 2A-skip to 2A-recompute) or cleanup work (no LLM at all). Confirm at session open. If any framing changes prompt composition, PR4 re-engages.

Step 1 — Pick the next framing.

The §8 single-endpoint-proof sequence has reached the architectural completion of ADR-PE-01 on a single endpoint. The remaining ADR-PE-01 work items vary widely in scope and risk. Three candidate framings — surface all three with brief reasoning. I pick.

* Framing A — Per-consumer 2A-recompute switch on a live consumer. Critical risk under PR6 + R17. The natural next §8 rollout step: with the loader Verified live on the proof endpoint, the existing 2A-skip on absence at reflect and founder-hub can be switched to 2A-recompute on a per-consumer basis. The first per-consumer switch is itself a PR1 single-endpoint proof of the loader-on-a-live-consumer pattern (the proof endpoint is admin-only / founder-only fixture-shaped; reflect and founder-hub are real-user-facing flows even though founder-only in practice). Sub-questions for the plan walk listed below. Founder picks reflect or founder-hub first.

* Framing B — Cleanup pass + Console-snippet promotion. Standard risk. Batch the small carry-forward items into a single session: (i) Console-snippet auth-cookie discovery promotion to canonical under PR8's third-recurrence rule; (ii) commit-hash recording across the six `D-PE-01-*-VERIFIED` decision-log entries (founder shares from GitHub Desktop History tab; AI amends inline); (iii) O-PE-01-B blob-size measurement (one-off Supabase probe of `length(encrypted_profile)` against the 50 KB threshold from ADR §9); (iv) cosmetic `27 April 2026` → `26 April 2026` comment-date fix in `/website/src/app/api/mentor/private/reflect/route.ts` (carry-forward from Session 3 close); (v) cosmetic proof-endpoint top-of-file JSDoc bullet stale ("Uses a hand-constructed fixture profile (not the live profile store)" — inaccurate since 25 April 2026 and now doubly inaccurate post-Session-5). Smaller scope, finishes the trailing items, frees the next architectural session from carry-forward overhead. Sub-questions listed below.

* Framing C — Step out of ADR-PE-01. The hold-point assessment is still active per P0 0h. Pattern-engine integration was inside the assessment set; with the integration now architecturally complete on a single endpoint, the assessment can use the wired-and-loader-backed pattern data to test what works on real founder data. Or — alternative — step into a different priority entirely (P2 ethical safeguards is the next priority per project instructions; P2 starts with R20a vulnerable user detection and R17a bulk profiling prevention, both Critical and both pre-dating ADR-PE-01 in priority order). Sub-question for the plan walk depends on which alternative. AI signal: I'd push back gently on starting P2 work while ADR-PE-01 carry-forward items (commit-hash recording, blob-size measurement, Console-snippet promotion) remain open — Framing B handles those in one session. But if you want P2, I'll execute it.

Do not assume which framing I am taking. Ask me directly: "Framing A, B, or C?" Wait for my answer.

Also surface, at the same time as the framing question, the appropriate plan-walk questions for each framing:

For Framing A (per-consumer 2A-recompute switch), surface these sub-questions and ask me to answer alongside the framing pick:

* Q-Consumer — reflect or founder-hub first? Reflect is the smaller scope (hardcoded `'private-mentor'` literal at the writer; smaller PR1 surface). Founder-hub is the higher-traffic consumer in principle (though founder-only in practice via FOUNDER_USER_ID). Default recommendation: **reflect first** — smaller scope, mirrors Session 3's wiring pattern most closely.
* Q-Empty-Recompute-Posture — what happens if the live loader returns `[]` (no rows in the 90-day window)? Three options: (i) skip persistence (don't write the empty PatternAnalysis to the blob — preserves any existing fixture-derived entry); (ii) persist the empty PatternAnalysis (overwrites any existing entry; cache pollution risk per ADR §6.3 worst case G language); (iii) keep 2A-skip semantics on the empty case specifically (recompute fires but doesn't replace cache when result is empty). Default recommendation: **(i) skip persistence on empty input** — preserves any existing useful entry and matches the conservative posture the founder picked at the loader's Q-Recompute-Read-Precedence.
* Q-Cadence-Adjustment — keep per_request, or switch to throttled (e.g., 6 hours) for the live consumer? Default recommendation: **keep per_request** — continuity with Sessions 1/2/3/3.5/4/5; revisit if cost / latency picture changes.
* Q-Verification-Pattern — same probe pattern as Session 5 (Console snippet on the deployed page)? The bypass flag does not exist on reflect or founder-hub yet — verification needs a different mechanism. Three options: (a) propagate `bypass_pattern_cache` to reflect / founder-hub as well (analogous request body parameter); (b) verify by deleting the relevant `pattern_analyses[label]` entry on the founder's blob to force cache miss (one-off Supabase SQL — the analogue of bypass without code change); (c) verify by adding a brand-new hub label specifically for testing (analogous to Session 3.5's seeding pattern but inverted). Default recommendation: **(a) — propagate the bypass flag to reflect / founder-hub** — symmetric with the proof endpoint; offers permanent operational value.
* Q-Bypass-Long-Term-Posture (O-PE-01-G) — concurrent with Q-Verification-Pattern: do we keep the bypass flag long-term across all consumers (admin diagnostic / force-recompute on demand), retire it once rollout is complete, or generalise it (e.g., a single header `X-Bypass-Pattern-Cache: true` rather than a per-route body field)? Default recommendation: **keep on each consumer** — small surface, default false, opt-in only; permanent operational value.

For Framing B (cleanup pass + Console-snippet promotion), surface these sub-questions:

* Q-Cleanup-Scope — all five items, or narrower? Default recommendation: **all five** — they are independent and small, and the next architectural session benefits from a clean carry-forward.
* Q-Console-Snippet-Promotion-Format — promote to a new canonical-pattern entry in `/operations/knowledge-gaps.md` (under a new slot or a dedicated "Canonical Patterns" sub-section), or to a new file `/adopted/canonical-patterns.md`, or to the existing `/operations/verification-framework.md`? Default recommendation: **add a new file `/adopted/canonical-patterns.md`** — keeps the auth-cookie pattern findable separately from KGs and verification framework; separation of concerns.
* Q-Blob-Size-Probe-Pattern — the blob-size probe needs Supabase access. Two options: (i) a SQL query through Supabase's web UI (founder runs once, reports back); (ii) a temporary diagnostic API route. Default recommendation: **(i)** — simpler, no code change needed.
* Q-Commit-Hash-Recording-Mechanic — founder shares all six hashes inline at session open in chat; AI amends six entries in `/operations/decision-log.md` in a single edit pass. Confirm.
* Q-Cosmetic-Date-Fix-Risk-Classification — Standard risk under 0d-ii (cosmetic, comment-only, not behavioural, not safety-critical, not user-facing). Confirm.
* Q-JSDoc-Bullet-Fix-Risk-Classification — Standard risk under 0d-ii (cosmetic, comment-only, JSDoc accuracy). Confirm.

For Framing C (step out), surface this sub-question:

* Q-Alternative — which alternative? Hold-point assessment work (use wired-and-loader-backed pattern data to test what works on real founder data per P0 0h Assessments 1-5)? Or step into a different priority entirely (P2 ethical safeguards — R20a vulnerable user detection, R17a bulk profiling prevention, R17b application-level encryption wiring, R17c genuine deletion endpoint, R19c/d limitations page + mirror principle, R20b/d independence + relationship asymmetry guidance)? Or something else (e.g., O-PE-01-F MENTOR_CONTEXT_V2 decoupling decision, O-PE-01-G `bypass_pattern_cache` long-term posture decision)? Founder names the alternative; AI then surfaces the plan-walk questions for that alternative.

Step 2 — Critical Change Protocol (0c-ii) requirements.

The Critical / Standard classification depends on which framing is picked.

Framing A (per-consumer 2A-recompute switch on reflect or founder-hub): Critical under PR6 (third or fourth `saveMentorProfile()` consumer added to the recompute path; encryption-pipeline blast radius engaged, plus the new live read of `mentor_interactions` on the chosen consumer). All five steps of the Critical Change Protocol must appear in the conversation before I deploy anything:

1. What is changing — plain language. No jargon. Name the specific files (the chosen consumer's route file, possibly a small loader-import + Q-Verification-Pattern's bypass flag if (a) is selected).
2. What could break — the specific worst case. Live-consumer regression on real reflect / founder-hub traffic. Empty-recompute cache pollution if Q-Empty-Recompute-Posture is (ii). KG3 hub-label drift on the new consumer's call site. Read amplification on real founder traffic (the loader now fires on every cache miss, not just admin proof traffic).
3. What happens to existing sessions — does this affect users currently signed in? Existing pattern_analyses entries (live-recomputed by Session 5 Probes L3/L4) would be overwritten on next cache miss once the loader is wired in on the consumer; the user-visible difference is whether the pattern context is recompute-fresh on cache miss or stays absent under 2A-skip.
4. Rollback plan — exact GitHub Desktop steps. The consumer's revert should leave the live-recomputed entries intact (they are still readable by the cache-hit branch which hasn't changed).
5. Verification step — what URL I visit, what I expect to see, what to do if the result is different. Likely: a probe that exercises the consumer with bypass=true (per Q-Verification-Pattern (a)), confirms `pattern_source: 'recomputed'` (cache miss → recompute from real data → save), then a re-run that confirms `pattern_source: 'persisted'` (cache hit on the freshly recomputed entry). Use the canonical `decodeURIComponent(cookies['sb-access-token'])` cookie pattern from the start.

Framing B (cleanup pass + Console-snippet promotion): Standard risk for items (ii)-(v); item (iii) blob-size probe is read-only (Standard). Item (i) Console-snippet promotion is a documentation change (Standard). The full Critical Change Protocol does not apply, but PR2 verification immediate still applies for any code change, and any code change still goes through founder approval before deploy. Most of Framing B is documentation; only the JSDoc bullet fix and the comment-date fix touch code.

Framing C (step out): Risk classification depends on the alternative picked. P2 ethical safeguards work is Critical for several items (R17a, R17b, R17c, R20a) per the project instructions — full Critical Change Protocol applies for any of those. Hold-point assessment work is generally Standard or Elevated (not modifying production code). O-PE-01-F or O-PE-01-G decisions are documentation (Standard).

Cannot answer one of those? Stop and signal "I need your input" or "This is a limitation."

PR3 applies — pattern-engine call (when invoked via cache-miss recompute on a live consumer) is synchronous, no fire-and-forget. PR4 applies — confirmed at session open. KG1 rule 2 applies if any new conditional write or new write surface is added (Framing A's per-consumer switch adds a new `await saveMentorProfile()` call site on the chosen consumer — KG1 rule 2 engaged). KG3 applies — any new reader/writer of hub-scoped data must use the canonical mapper or its allowlist equivalent. KG6 applies — any change altering prompt assembly must respect the user-message zone for per-request data.

Step 3 — Acceptance criteria.

This session ends successfully if at least one of these is true:

* The chosen framing's first implementation step reaches Verified status: TypeScript clean (`npx tsc --noEmit` exit 0 in `/website` if code is involved); the chosen verification probe passes (per-framing probe shape, designed in the CCP step 5); I push the commit(s) via GitHub Desktop; Vercel reaches Ready (if applicable); I confirm the round-trip works.
* The session ends earlier with a clear "I'm done for now" signal from me. Stabilise and close. Do not propose additional fixes.
* The session reveals a blocker that requires a smaller scoping change before the chosen work can land. In that case, surface the blocker with the AI signal "I need your input" and let me decide whether to proceed in this session or close and reopen with a narrower scope.

Step 4 — Session close.

Per the protocol Part C, produce a handoff at session end in the required-minimum format plus the relevant extensions (Verification Method Used / Risk Classification Record / PR5 Knowledge-Gap Carry-Forward / Founder Verification / Decision Log Entries — Adopted / Process-Rule Citations / Orchestration Reminder). Save to `/operations/handoffs/tech/[YYYY-MM-DD]-[short-description]-close.md`. Name the protocol explicitly as the governing frame at close.

If the session involved code and I pushed via GitHub Desktop, the handoff's "Founder Verification" section names the pushed commit hashes and the live-probe / verification result.

If the session involved any framing-A per-consumer switch that completes the live-consumer integration of the loader, the handoff should also note the new state of `pattern_analyses` entries on the founder's blob (which were last overwritten by Session 5 Probes L3/L4 to live-recomputed values).

Carry-forward open items (do not address unless I bring them up):

* Decision-log entries to confirm at session open: `D-PE-01-S1-1A-VERIFIED`, `D-PE-01-S2-2A-VERIFIED`, `D-PE-01-S3-3A-VERIFIED`, `D-PE-01-S35-3.5α-VERIFIED`, `D-PE-01-S4-4B-VERIFIED`, and `D-PE-01-S5-LOADER-VERIFIED` are all Adopted as of the 2026-04-26 closes. Commit hashes for all six pushes are placeholder ("TBD per founder share from GitHub Desktop History tab"); if I share the hashes at session open, AI amends all six entries inline. Framing B item (ii) is the natural place to do this if not done at session open.
* Cosmetic comment-date fix carried forward from Session 3: `/website/src/app/api/mentor/private/reflect/route.ts` carries comment blocks dated "27 April 2026" rather than "26 April 2026". Standard-risk one-line fix; can be batched with any future change to that file. Tiered as Efficiency & stewardship per PR9; absorbed into ongoing work. Framing A would naturally batch this if reflect is the chosen Q-Consumer for the per-consumer switch. Framing B item (iv) is the explicit fix path.
* Cosmetic JSDoc bullet stale (NEW from Session 5 close): `/website/src/app/api/mentor/ring/proof/route.ts` top-of-file bullet "Uses a hand-constructed fixture profile (not the live profile store)" became inaccurate at ADR-Ring-2-01 Session 1 (25 April 2026) and is doubly inaccurate post-Session-5 (the recompute branch now uses live data via the loader). Tiered as Efficiency & stewardship per PR9. Framing B item (v) is the explicit fix path.
* O-S5-A — `/private-mentor` chat thread persistence (pre-existing UX gap).
* O-S5-B — write-side verification of ADR-Ring-2-01 4b (deferred until natural triggering).
* O-S5-D — static fallback canonical-rewrite revisit.
* O-PE-01-A (read amplification) — every profile read decrypts the full blob even when pattern data is not needed. Now also concretely measurable on the proof endpoint's recompute branch (two new queries per cache-miss / bypass invocation: profile_id lookup + mentor_interactions read). Will scale materially under Framing A's per-consumer 2A-recompute switch. Revisit if profile reads on either consumer become a measurable hot path.
* O-PE-01-B (blob-size monitoring) — versions now incremented by three writers (proof endpoint, reflect endpoint, founder-hub endpoint) plus the live-recomputed L3/L4/Re-L1 from Session 5. Trigger condition for revisit is blob > 50 KB or encrypt/decrypt p95 > 50 ms. Not blocking; a quiet measurement when convenient. Framing B item (iii) is the explicit measurement path.
* O-PE-01-C (optional `last_pattern_compute_at` plain column) — adding a plain timestamp column would let freshness queries skip decryption. More relevant if Framing A introduces freshness-driven cadence (e.g., throttled with a conditional via Q-Cadence-Adjustment).
* O-PE-01-D (write cadence) — resolved for Sessions 1, 2, 3, 3.5, 4, 5 (per_request). Revisit in Framing A's plan walk if Q-Cadence-Adjustment surfaces concerns.
* O-PE-01-E (backfill of existing profiles) — partially resolved for the founder's profile via Session 5 Probes L3/L4 overwrite. Carries forward unchanged for any other practitioners (none currently).
* O-PE-01-F — pattern-read decoupling from `useProjection` / MENTOR_CONTEXT_V2 env var. Defer until the V2 feature flag retirement question lands on the table; not blocking.
* O-PE-01-G (NEW from Session 5) — `bypass_pattern_cache` long-term posture. Keep, retire post-rollout, or generalise. Framing A's Q-Bypass-Long-Term-Posture is the natural place to address this if Framing A is picked. Otherwise defer until natural triggering.
* PR5 candidate for promotion — Console-snippet auth-cookie discovery (`decodeURIComponent(cookies['sb-access-token'])` reading raw JWT directly). Now used three times successfully without rediscovery (Sessions 3.5, 4, 5). PR8's third-recurrence rule supports promotion. Framing B item (i) is the explicit promotion path. Can also be done as a small standalone item at session open before any framing work begins.
* PR5 candidate (1 of 3) — two-phase staging within a single founder session, separated by a stop-point gate. Demonstrated successfully in the 2026-04-26 Sessions 3.5 + 4 combined session. Promotion trigger: two more occurrences. Counter unchanged at 1 of 3 from Session 5.
* PR5 candidate (1 of 3) — diagnostic-via-pipeline_meta spread-conditional pattern. First introduced at Session 4 in `/api/founder/hub/route.ts`. Counter unchanged at 1 of 3 from Session 5.
* PR5 candidate (1 of 3) — sub-decision-after-framing-acceptance pattern. Demonstrated at Sessions 3.5 + 4 close. Counter unchanged at 1 of 3 from Session 5.
* PR5 candidate (1 of 3, NEW from Session 5) — bypass-flag-as-verification-mechanism (an opt-in request-body flag specifically as the deterministic mechanism for exercising a code path that natural traffic would not reach — e.g., cache-warm labels under per_request + 2A read precedence). Promotion trigger: similar opt-in verification flag earns its place in two more sessions.

Do not write any code until I have picked a framing (Step 1) AND named the relevant scope/cadence/alternative sub-question(s) for that framing AND (if Framing A) named all five Q-Consumer / Q-Empty-Recompute-Posture / Q-Cadence-Adjustment / Q-Verification-Pattern / Q-Bypass-Long-Term-Posture sub-decisions AND the Critical Change Protocol (Step 2) is satisfied with my explicit "OK / go ahead" naming the listed risks (where Critical risk applies).

If I am only doing the plan walk and decide not to proceed to code in this session, that is a complete and successful session. Do not push for code.
