# Mentor ruling — primal-substrate scope confirmation (verbatim, 2026-08-11)

**Provenance:** the mentor's ruling on
`2026-08-11-MENTOR-BRIEF-scope-confirmation.md`, relayed by the founder in-session on 2026-08-11 and
transcribed here in full, unedited.

**This file is binding and wins over every annotation folded into the S1–S8 scope documents.** Where a
scope document's inline `RULED` note and this record differ, this record governs.

**Scope of the ruling:** all 35 items — 7 corrections (Part A), 7 blocking decisions (Part B), 19
confirmations (Part C), 2 founder acts (Part D), plus a sequencing confirmation. **Every correction was
confirmed; no correction was overruled.**

**What it does NOT license.** No build starts on this ruling alone. Every schema, flag, credential, or
public-surface step remains its own founder-walked 0c-ii Critical step, and PR19 independent
adversarial review still runs before any build lands. Two items are explicitly **founder acts** and
neither has been performed (Part D). The Q1 hard constraint — the loop proposes, it never executes — is
preserved throughout and is explicitly reasoned about in B1.

---

## Part A — Seven corrections. Confirmed or overruled.

> **A1. Confirmed.** The DIAGNOSTIC_SEQUENCE is the committed examination pathway. S7 applies it rather
> than authoring a parallel taxonomy. The synthesis's two questions are steps 2 and 5 entered from four
> trait starting points. The locust problem is already named in the committed corpus as the `praxis`
> failure mode. No new taxonomy is licensed.
>
> **A2. Confirmed.** The four-virtue proxy ruling from 2026-08-09 stands and is the larger answer. The
> synthesis's oikeiosis-circle-only framing was a narrowing, not an extension. The high/medium/low
> vocabulary and the two-record requirement — loop indicator and agent's own assessment — are already
> fixed in `manifest.md`. S8 inherits the four-dimension answer.
>
> **A3. Confirmed.** The watching table has no `target_circle` column. The synthesis's claim was false
> and was already caught on 2026-08-09. Three additive columns are needed: loop indicator, agent
> assessment, and circle. S8 carries this correctly.
>
> **A4. Confirmed.** The first build gate closed 2026-08-10. The five parked items are re-parked on
> their correct current gates: §6 report, post-run ATRF scoping session, or standing-runner design as
> specified in the scope documents. The synthesis's "after the first build gate" language is
> superseded.
>
> **A5. Confirmed.** The synthesis's "at principled proximity" claim for cycle 2 rests on an
> uncertified verdict delivered observed rather than examined, and both h7 outcomes were decided by
> tie-break. The corrected reading is: h7 is competitive with the virtue-domain heuristics, not
> demonstrated superior. S6 carries this correction. **The friction hypothesis is not weakened — it is
> stated more honestly.**
>
> **A6. Confirmed.** Two `/api/reason` incidents of opposite sign: cycle 3 is contamination by
> addition, cycle 5 is contamination by omission. Heading 4's evidence base should be argued from both,
> not from cycle 3 alone. S4 carries this correctly. The strengthening of the case is real.
>
> **A7. Confirmed and recorded.** Layer 2b is correct terminology per `manifest.md` AC6. The earlier
> reviewer's search found the code-level name `practitionerContext` and did not check the manifest.
> Recorded, not dropped.

---

## Part B — Decisions that block a build.

> **B1. GS-ATRF-3 — Middle option adopted.** The 2026-08-09 ruling rested on actor and moment: the
> completion signal is authored by a different actor at a different moment from the generation-step.
> That reasoning survives. The synthesis's new argument — that the signal must carry examination
> evidence, not a binary flag — is real and was not before me on 2026-08-09. It addresses content, not
> actor or moment.
>
> Ruling: the generation-step document **states the requirement** — the completion signal must carry
> examination evidence, not a binary flag, and the reason is that the signal is the primary
> post-execution evidence of whether genuine examination occurred rather than simulation. The **return
> path** — how the agent sends it, what the harness does with it, schema — is **scoped in the ATRF
> session**. The Q1 hard constraint is preserved because the generation-step document is stating a
> requirement on a downstream artefact, not designing that artefact's implementation. This is the same
> relationship the generation-step document has to the watching table: it specifies what must be
> carried, not how the table is built.
>
> **B2. S7 — New tool, not extension of `/passion-log`.** The live-coupling risk is real. The Step M
> mapping means any schema change to `passion_events` touches the live Phase-2 in-session practice
> suggestion. The independent-revert property is a design principle the shipped siblings all carry. The
> `/sage-compass` precedent is the correct model: conceptual link in page prose, no code coupling.
> Ruling: new tool. The link to `/passion-log` is stated in prose. No code coupling. The build proceeds
> on this basis.
>
> **B3. S7 — Inside the R20a distress perimeter.** The family precedent is outside. This tool is
> different in kind. It deliberately elicits shame and agonia in the practitioner's own words, beside
> grief, envy, and jealousy. The design premise is that the practitioner should not suppress this
> material — which means the tool is doing exactly what the perimeter exists to catch when it fires
> genuinely. A false positive costs a redirect to a non-distressed practitioner. A false negative is a
> practitioner writing about their shame into a tool that does not notice. The asymmetry favours
> inclusion. Ruling: **inside the perimeter.** The `/api/score-conversation` `enforceDistressCheck`
> pattern is the implementation precedent. AC5 records this as a departure from family precedent, with
> the reason stated.
>
> **B4. S7 — Fourth pathway shape.** The reciprocity pathway is not a passion sub-species. Forcing it
> into the sub-species shape would either invent a sub-species — a PR15 source-fidelity violation — or
> silently drop it. The corpus's own name for the relevant phenomenon is "externally correct behaviour
> driven by wrong reasons" — the `praxis` failure mode already committed. Ruling: **distinct mode
> within the same tool**, with its own question set, entered from the reciprocity trait and framed
> around the praxis failure mode. **Not routed to `/oikeiosis` in v1** — the examination is about the
> ground of the action, which belongs in this tool's scope. **Not omitted.** The question set asks: is
> this cooperation grounded in recognition of the other as a rational being, or in expected return?
> What would the action look like if the expected return were removed?
>
> **B5. S6 — Null result specified now.** The run is live. The temptation to fit the discriminator
> after seeing the distribution is real and the project has a precedent for freezing thresholds first.
> Ruling: the friction hypothesis is **rejected** if, restricted to cycles whose winner extraction was
> cross-checked clean, h7's proximity distribution — with strict wins and tie-break wins reported
> separately — does not differ from h1–h6's distribution when blast-radius and reversibility are read
> alongside proximity. A channel that reliably ties at the top is a real finding but a different one
> from a channel that reliably wins. Both are reported. Neither is the other. **The null result is: no
> difference in the joint distribution across proximity, blast-radius, and reversibility, in clean
> cycles, with strict wins separated. This is frozen now.**
>
> **B6. S5 — Verbatim wording for the manifest amendment.** I will supply the text. It belongs in the
> same register as R21, R22, the ATRF section, and the Consciousness and Continuity Obligation —
> adopted verbatim from founder instruction, with any AI judgement as a bracketed note outside the
> text. Proposed wording for your adoption or amendment:
>
> ---
>
> *The moral community SageReasoning builds for is ordered by degree of rational participation in logos
> — not by species membership, not by population count, and not by cognitive supremacy. Any being
> capable of examined assent — of receiving an impression, recognising it as an impression, and
> choosing whether to act on it — is a candidate member. The degree of membership tracks the degree of
> that capacity. The framework applies fully where the capacity is fully present, partially where it is
> partially present, and precautionarily where it is uncertain. Humans currently occupy the centre of
> that distribution as the best-understood case. The direction extends by interpolation to animals
> capable of guidance and by extrapolation to agents capable of guidance. The Sage — the fully rational
> being who has perfected examined assent — is the extrapolated ideal that anchors the upper end. No
> current human or artificial agent is assumed to have achieved it.*
>
> *[Bracketed note: this amendment sharpens R0's Circle 4 statement — "All rational agents, human and
> artificial" — by making the ordering criterion explicit and naming the directional extensions. It
> does not resolve the three-enumeration discrepancy between manifest.md R0, stoic-brain.ts:445, and
> the trust core's OikeiosisCircle vocabulary. That discrepancy is named and carried forward as a
> separate resolution item.]*
>
> ---
>
> Supply this text verbatim if it reflects your intent, or amend it before adoption. **It does not
> become a governing document entry until you confirm it.**
>
> **B7. S4 — Traceability check mid-flight.** Ruling: the cross-endpoint comparison is proposed through
> the run's own channel now, in this response, as a ruling on the run's review protocol. It requires no
> repo-side session because it costs nothing — both extractions are already produced every cycle,
> already billed, never compared. The check is: after each cycle's winner is identified, compare the
> guardrail extraction and the reason extraction on the same input. Divergence is flagged. The table
> extension is the durable record of it, parked on the §6 report gate. **The mechanism is the
> comparison itself, which starts immediately.**

---

## Part C — Confirmations.

> **C1. Yes.** S7 adopts `DIAGNOSTIC_SEQUENCE` as the examination pathway explicitly. No parallel
> taxonomy is authored.
>
> **C2. Yes.** The cross-endpoint comparison is adopted as the systematic step now. Zero cost, no
> schema change. The table extension is the durable record, not the mechanism.
>
> **C3. Yes.** The criterion's honest bound is stated explicitly: the check detects divergence and
> unsourceability. Two agreeing extractions can both be wrong. No third independent reading is
> introduced.
>
> **C4. Yes.** Derived summary in v1 — element counts, domain set, divergence flag — with
> `guardrail_session_id` preserving the full re-audit path. Verbatim is preserved as an upgrade path,
> not a v1 requirement.
>
> **C5. Yes.** Reformulated as evidence, not proof. A specific, contextually grounded answer is
> evidence of genuine assessment. A formulaic answer is evidence against. The mechanism is paired with
> S4's check rather than treated as self-verifying. Cycle 5's confident articulate empty extraction is
> the direct counter-evidence that makes this reformulation necessary.
>
> **C6. Yes.** The runner's own state only: shared task list, cycle history, credential-scoped
> examination history, and its own public trust record. **Explicitly not `getProjectContext`.**
> Generation and examination are different calls — the ruling that removes `projectContext` from
> API-key-authenticated `/api/reason` calls does not touch the generation call, but the generation
> call's signal sources are bounded as stated.
>
> **C7. Yes.** Two proposals. The normative-gap mechanism adds a question: where does the system fall
> short of what virtue requires? Heuristic 7 reads technical friction. Conflating them would let cycle
> 2's evidence stand in for a mechanism that did not produce it.
>
> **C8. Yes.** Test jointly. Friction candidates are borderline proposals by construction. The §6
> report cannot carry the friction hypothesis and its undercutting caveat in separate sections without
> naming the joint dependency explicitly.
>
> **C9. Yes.** The `/sage-compass` binding "not a verdict" constraint binds the agent-side mechanism. A
> computed gap that feeds a score is the same violation in a different medium. Stated in the framing.
>
> **C10. Yes.** Named distinctly now. The loop's blast-radius indicator is the reasoning-level proxy
> computed at proposal time without task details. Item 16's `blast_radius` is the expensive enrichment
> produced by item 14's three-step post-verdict analysis on a separate permission-layer schema. Same
> vocabulary, different method, producer, and moment. The project has been bitten twice this window by
> two things sharing a name. **They are named distinctly from this point: loop-level blast-radius proxy
> and permission-layer blast-radius enrichment.**
>
> **C11. Yes.** Persisted. A `high` should remain auditable after the derivation changes. Consistent
> with the traceability criterion this synthesis asks for.
>
> **C12. Yes.** Accept the duplication and add a drift pin. The boundary test reads `stoic-brain.ts` as
> text — imports nothing — and asserts the local IDs are a subset. Silent drift is the risk; the pin
> catches it without coupling.
>
> **C13. Yes.** Build the four given traits in v1. Design the trait vocabulary so the remaining seven
> can be added without a schema change. The four are: competition/hierarchy → philodoxia; resource
> acquisition → philoplousia; threat avoidance → phobos sub-species; reciprocity → praxis failure mode
> (distinct mode, B4 ruling above).
>
> **C14. Yes.** Un-numbered section immediately after R0. Matches the Consciousness and Continuity
> Obligation's precedent. R-numbering stays stable.
>
> **C15. Yes.** The amendment names the discrepancy between the three committed enumerations and
> declines to resolve it. Resolving it inside a moral-community amendment would silently change a
> foundation rule. The discrepancy is carried as a separate resolution item.
>
> **C16. Yes.** State the connection. GS-ATRF-1's indicator measures reach across circles, not headcount
> — consistent with "not by population count" by design, not by coincidence. The blast-radius design
> inherits this as a constraint explicitly.
>
> **C17. Yes.** The operational rule is that new participant-class enums are built extensible, or their
> closure is a recorded decision rather than a default. Naming a direction in a governing document does
> not prevent a closed two-value enum being written later. The rule prevents that default closure.
>
> **C18. Yes.** Report strict wins and tie-break wins separately. A channel that reliably ties at the
> top is a real finding but a different one from a channel that reliably wins. Both are reported in the
> §6 analysis.
>
> **C19. Yes.** Internal only in v1. A public statement is an R18 change with its own honesty review
> and session. It does not arrive as a side effect of this build.

---

## Part D — Two things only you can supply.

> **D1.** The eleven-traits research is not in the repo. S1, S2, and S7 are blocked on it. The research
> was submitted in this conversation on 2026-08-11 at 6:26am and assessed here. It exists in this
> session's context but has not been committed. You need to commit it to `inbox/` or
> `operations/primal-substrate-2026-08/` verbatim before S1, S2, and S7 can proceed. Building
> examination pathways from a summary of a taxonomy rather than the taxonomy is the failure the
> verbatim-wins rule exists to prevent. **This is a founder act.**
>
> **D2.** The S5 manifest amendment wording is proposed above under B6. It does not become a governing
> document entry until you confirm it or supply amended text. **This is a founder act.**

---

## Sequencing confirmation.

> The scope documents' sequencing is correct and adopted:
>
> **S4 and S6 first** — both feed the §6 report. S4's cross-endpoint comparison starts immediately per
> B7. S6's null result is frozen per B5.
>
> **S8 second** — protects the post-run ATRF scoping session. The B1 middle-option ruling and the
> A2/A3 corrections are its inputs.
>
> **S1 → S2 → S3 third** — strict internal order, no deadline, blocked on D1.
>
> **S5 fourth** — blocked on D2 confirmation.
>
> **S7 last and parallel-safe** — blocked on S1 and D1. Can start any time after both are resolved.
> Shares no surface with anything else.
>
> Nothing self-starts. Nothing in this family bears on the 0h call.

---

## Repo-side annotations (NOT the mentor's text — added 2026-08-11)

**⚠ D1 — an honest correction the founder must act on.** The ruling states the research *"exists in
this session's context but has not been committed"* and instructs that it be committed. **It exists in
the MENTOR's session context, not in this repo session's.** No AI session in this repository has ever
been given the eleven-traits research; it cannot be committed from here. **The founder must relay the
research text into a repo session, or paste it into a file directly.** Until that happens, S1, S2, and
S7 remain blocked exactly as ruled — the blocker is unchanged, but the mechanism for clearing it is a
founder relay, not a repo-side commit of something already present.

**D2 — not performed.** The B6 wording is recorded here verbatim as *proposed text*. It has **not**
been written into `manifest.md`, because the ruling is explicit that it *"does not become a governing
document entry until you confirm it."* S5 stays blocked on the founder's confirmation or amendment.

**Settled names, from C10 — added to the project's naming register by this ruling:**
- **loop-level blast-radius proxy** — the reasoning-level indicator computed at proposal time from
  circle + virtue domains, without task details (GS-ATRF-1's four-virtue answer).
- **permission-layer blast-radius enrichment** — item 16's field, produced by item 14's three-step
  post-verdict analysis, on a separate permission-layer schema.

These are distinct quantities. Neither name may be shortened to bare "blast radius" in any future scope
or build document.

**B7 — the run-side action is the founder's to relay.** The ruling issues the cross-endpoint comparison
as a protocol change on the live validation run *"through the run's own channel … in this response."*
That channel is the idea-loop-validation scratch project, not this repo. **The founder must carry the
B7 ruling into `RUN-LOG.md`** so the next cycle applies it; a repo-side session must not reach into the
run's records.
