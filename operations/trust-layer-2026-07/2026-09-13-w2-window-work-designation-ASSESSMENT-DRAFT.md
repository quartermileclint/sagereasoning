# The W2 window's work — the F-1 assessment (DRAFT, NOT a designation)

> **⚖️ RULED 2026-09-13, same day, on the relay this draft fed** (verbatim, canonical:
> `2026-09-13-mentor-ruling-eight-questions-w2-work-s11d2-sequencing-r8d7-under-w-verbatim.md`; wins over this document). **Q-M1:**
> candidate (b) is WITHDRAWN; the ordering is (c) standing-runner design track primary, (a)
> staged-clause application secondary; the §2 D2 correction is accepted as fact. **Q-M2:** a
> recorded guarded-file waiver on the measured checkout, guard armed, IS window work; a worktree is
> not. **Q-M3:** consequential design authoring in `operations/` satisfies the clock rule. **Q-M4:**
> the S11-D2 baseline restarts with the W2 window. §4's two named questions are therefore answered
> by the ruling, not by this draft. **The clock has still not started; the founder opens the first
> sitting under a Q-S2-conforming prompt.**

**Status: DRAFT — FOR THE FOUNDER'S F-1 DECISION. This document designates nothing and recommends
nothing.** The 2026-09-13 ruling made the work designation the founder's, to be settled before the
first W2-window session. This is the comparison the founder asked for, read from primary sources.
Where a source is quoted, the verbatim wins over this document.

**Authored 2026-09-13 (from `date`), by an autonomous session (`sagereasoning-dd [856dc7]`), founder
absent.** Tier `governance` / documents. No code, schema, flag, credential, migration, deploy, push,
`GUARD_RE` file or R18 surface touched.

**This is not a W2-window session prompt.** It names the window and the observation goal freely,
because Q-S2 binds W2 *session prompts*, and this is not one. **It must not be pasted into a W2
session prompt.**

---

## 0. The two ruled constraints every candidate is read against

From `2026-09-12-mentor-ruling-andreia-capture-path-and-window-design-verbatim.md` (Q-W1–Q-W4) and
`2026-09-13-mentor-ruling-w2-window-work-and-QS2-discipline-verbatim.md` (verbatim wins):

- **Channel (Q-W1).** *"a window capable of honestly evaluating andreia requires consequential
  decisions carried out through composed, narrated actions — Write/Edit, not Bash. The subject matter
  and the channel are both required. Either without the other is insufficient."* And: *"The subject
  matter of the work does not reach the extraction if the work passes through Bash."* The 2026-09-13
  ruling adds the clock rule: *"It starts from the first consult record produced by consequential
  Write/Edit work on a live surface."*
- **Variety (Q-W2).** *"A domain cannot be honestly evaluated from a single behavioural class at any
  N. The window must be chosen for variety of courage-relevant moments, not duration alone."* The
  class the current window captured is *"the courage of honest record-keeping under the pressure of a
  finding that blocks progress"*; the full domain *"includes the courage to act on a consequential
  decision when the outcome is uncertain, to hold a position under external pressure, to name a
  disagreement when agreement would be easier."*
- **Variety is assessed after, never targeted before** (the specification's §2; Q-S2 ruled it
  binding). So this document does **not** predict which classes a candidate would produce. For each
  candidate it states only what the work would actually require the loop to *do*, from the work's own
  record — the founder's assessment (Q-W2: *"the founder's assessment to make"*) is made from that,
  not from a forecast here.

**Two facts about the harness that bear on every candidate**, both from source this session:

- The byte-identity guard (`website/src/app/logos/__tests__/human-practitioner-boundary.test.ts:419`)
  binds on **uncommitted working-tree lines** matching `GUARD_RE`
  (`api/reason|api/guardrail|guardrail-sandwich|sage-reason-engine|reasoning-receipt|translation-sandwich|/substrate/|trust-core|kathekon-engagement|false-hold|harness/gate1|layer1-extractor|layer2-mechanisms|sage-reflect|stoic-brain`)
  while `GATE1_FALSE_HOLD_CAPTURE` is set, plus two test-asserted SHA pins (`layer2-mechanisms.ts`,
  `stoic-brain.ts`) and one records-carried pin (`intervention-engine.ts`).
- The W2 build avoided the guard by working in **an isolated worktree with capture unset** (W2 design
  §9) — which is exactly why it left the buffer zero consult records and why the ruling says it *"did
  not happen"* from the window's perspective. **Any candidate whose authored work touches a `GUARD_RE`
  file therefore faces the same fork: a recorded per-commit waiver on the measured checkout, or a
  worktree that the window cannot see.** Whether the window's own discipline tolerates a waived edit
  to a guarded file *during* the window is a question this document names and does not answer (§4).

---

## 1. Candidate (a) — W2 activation's remaining authored work

**The ruling's description.** *"W2 is live-but-dark. The activation step is founder-walked and coupled
to the flip — but the work leading up to it, including the honest-claims envelope, the L4
dual-recording rule, and the schema-shape election, involves Write/Edit authoring on a live
trust-record surface. If any of that work remains as authored documents rather than completed builds,
it constitutes the right kind of work. The founder should assess what W2 activation actually requires
in terms of composed, narrated authoring before the founder-walked step."*

**What the primary record says is done and not done** (W2 design of record
`2026-09-12-W2-record-honesty-DESIGN.md`; the waiver close
`2026-09-12-W2-waiver-merge-and-schema-walk-CLOSE.md` §6; register §F row W3-d and its change-log;
CLAUDE.md's 2026-09-12 block):

| Item the ruling names | State in the record | Remaining Write/Edit authoring |
|---|---|---|
| The honest-claims envelope (the L7 compliance-not-virtue clause) | **Inline half BUILT** (dark; `COMPLIANCE_NOT_VIRTUE_CLAUSE`, battery-locked). **Record-level half STAGED, not applied** — `2026-09-12-W2-compliance-not-virtue-clause-STAGED-R18.md` carries the proposed `TRUST_RECORD_ENVELOPE.does_not_attest` text (§3) and the ADR-013 §8 dated amendment (§4), which must land as **one edit** after the founder's R18 signature (opener item **F-3**). | The wording **already exists in the staged file**. What remains is the founder's signature (a founder act), then the application: one edit to `trust-record-payload.ts` + one to ADR-013 §8 + an S10 battery pin. A small amount of composed authoring, most of it already composed. |
| The L4 dual-recording rule | **BUILT** (design §6; pinned both directions; PR19-folded). | None in the design's own account. |
| The schema-shape election | **DECIDED and EXECUTED** (design §5: a separate 21→22 CHECK widening; applied TEST + production 2026-09-12, `D-W2-ENFORCEMENT-VOCABULARY-MIGRATION-APPLIED-2026-09-12`). | None. |
| The R18 documentation of the new field (`enforcement_outcomes`, the `regime` marker) on `llms.txt`, `agent-card.json`, `api-docs` | **NOT drafted.** The staged file §5 says these *"are NOT drafted here; they are the activation session's R18 step, once the field can actually appear."* | Genuine composed authoring on three live public surfaces — **but sequenced by the staged file to activation**, and activation is *"its own step coupled to the flip"* (register §F W3-d; opener `Q-W2-ACTIVATE`; waiver close §6). |
| The accreditation-row regime column (design §3 follow-on) | **Unscheduled** (design §3: *"a named follow-on for the flip-component session"*; waiver close §6). | Code on the accreditation write path + a schema step (founder-walked). |
| The first-circle event class, C1c-original (design §6) | **Unscheduled**; the design says creating it inside W2 *"would silently absorb the original C1c, which the record forbids twice over."* | Code in `trust-core` + a CHECK widening + its own session. |
| The sandwich-flag rollback coverage gap (design §7) | *"Named, not closed."* | None elected. |

**Against the channel constraint.** The surface is live in the strongest sense the ruling uses — the
public trust record and the ADR that governs its honest-claims boundary. The staged-clause application
and the field's R18 documentation are Write/Edit by nature. The two follow-ons are code (Edit-tool
work is consult-path by tool, but see the guard fork below).

**Against the variety constraint (what the work requires the loop to do, not what it would yield).**
Applying an already-composed clause under a founder signature; composing public documentation for a
field that cannot yet appear; and, if the follow-ons are elected, two code-plus-schema builds each with
a PR19 pass and a founder-walked step. The ruling's own phrase applies: *"If any of that work remains
as authored documents rather than completed builds"* — on the record, most of the named items are
completed builds or founder acts, and the authored remainder is small.

**Size.** The staged-clause application: well under one session (the wording exists). The field's R18
docs: roughly one session, **at activation**. The two follow-ons: roughly one session each if elected,
plus their founder-walked schema/activation steps.

**`GUARD_RE` exposure.** `trust-record-payload.ts` matches (`/substrate/` and `trust-core`; checked
against the regex this session). `enforcement-record.ts` and any C1c-original work match (`trust-core`).
`sage-assent-accreditation-store.ts` matches (`/substrate/`). The three R18 surfaces and ADR-013 do
**not** match. **So every code item in this candidate needs a waiver on the measured checkout or a
worktree the window cannot see; the documentation items need neither.**

---

## 2. Candidate (b) — the pre-flip report's outstanding items

**The ruling's description.** *"The Part 1 script defect needs a waived session. The D2 ruling is
pending and its relay will require a response document. These are Write/Edit work on governing
documents with real consequences for the flip's timeline. They are not as consequential as W2's live
surface, but they are consult-channel work."*

**What the primary record says, item by item.**

- **The Part-1 script defect — FIXED 2026-09-12 under the B1 waiver**
  (`D-PART2-EVIDENCE-RULING-ADOPTED-B1-D1-D2-EXECUTED-2026-09-12`; pre-flip report §12 item 2:
  *"DISCHARGED 2026-09-12"*). The verbatim's own recording note already says so. **No authored work
  remains.**
- **The D2 "engaged" definitions — the RULINGS ARE NOT PENDING.** Three D2 rulings were adopted on
  **2026-09-06**: the principal ruling
  (`2026-09-06-mentor-ruling-D2-virtue-domain-tagging-verbatim.md`,
  `D-MENTOR-RULING-D2-VIRTUE-DOMAIN-TAGGING-ADOPTED-2026-09-06`), the natural-relationship follow-on
  (`…-D2-natural-relationship-disjunct-verbatim.md`, whose header reads *"Completes the D2
  specification… this leaves no open axis"*), and the mid-window sequencing ruling
  (`…-D2-window-sequencing-MID-WINDOW-verbatim.md`). The S11 register's own D2 row (§D) ends:
  *"FOLLOW-ON RULED 2026-09-06 — D2's SPECIFICATION IS NOW COMPLETE; NO OPEN AXIS REMAINS."*
  **The phrase "scoped-for-ruling, relay pending" survives in three secondary records** — the window
  specification §3, the 2026-09-12 session-continuation summary close (line 44), and the drafted
  opener's standing queue (`Q-D2-ENGINE`, line 295) — and from there reached the relay the mentor
  ruled on. **This document records the discrepancy and does not resolve it**: whether the mentor's
  *"the D2 ruling is pending"* refers to some D2 question this session could not find in the
  repository, or inherited the stale phrase, is for the founder to settle with the mentor. From the
  repository as it stands, **no unanswered D2 question exists, and no D2 "response document" is
  owed.**
- **What IS open on D2 is the BUILD** — `S11-D2`, the engine correction to `computeVirtueDomains` in
  `layer2-mechanisms.ts`, with the over-broad `is_kathekon !== null` trigger corrected in the same
  pass. The mid-window ruling fixes its shape: it lands *"after a defined baseline threshold is
  established, while the window continues to record"*; the threshold is *"five ordinary days with
  consult records"*; the stand-down is *"a recorded founder waiver for the named commit, with the
  guard left armed"*, *"never a silent commit"*; the segmentation boundary is the commit timestamp; and
  the SHA-256 pin on `layer2-mechanisms.ts` is the one now in the guard battery (`60cefedb…`). The
  pre-flip report (§9) records D2 as *"blocked on the separate 'ordinary' condition"* — whether the
  days accrued are "ordinary" is not a count this session can make. *(Re-derived this session: every
  UTC day from 2026-09-06 to 2026-09-13 carries ≥1 consult record — 3, 17, 6, 18, 34, 19, 40, 2 — so
  the count half of the threshold is met; the "ordinary" half is a judgement.)* **Note the ruled
  sequencing is against the CURRENT window's baseline; how it composes with a restarted W2 window
  (Q-S1) is not stated in any ruling this session read.**
- **The other §12 items** are founder decisions, not authored work: the fork (decided — the window);
  the mark (Task A of this session's prompt, owed as a record the founder makes, F-4); the conditions
  on any future write (§12 item 4); Part 3's consult/guard split (F-7, parked by the founder's own
  election); the `/api/guardrail`-at-503 election (F-5).

**Against the channel constraint.** The only build item, S11-D2, is an Edit-tool change to the
engine that `/api/reason` runs — the most live surface the project has. The mark record and any D2
response document, if one were owed, are governance authoring: the exact class the current window
already produced (the Q-W2 finding was made on records of this kind).

**Against the variety constraint (what the work requires).** The S11-D2 build: a recorded waiver on a
SHA-pinned file, a PR19 pass, a regime-boundary commit, a segmentation read. The rest: record-keeping.

**Size.** S11-D2: roughly one build session plus its waiver and review. Everything else: zero
sessions of authored work on the record.

**`GUARD_RE` exposure.** S11-D2 is **certain** exposure — `layer2-mechanisms.ts` matches the regex
*and* carries a test-asserted SHA pin, and the mid-window ruling already prescribes the waiver shape
and a pin update. **This is the one candidate whose ruled build shape is itself a guarded-file waiver
during a running window.**

---

## 3. Candidate (c) — the standing-runner design session

**The ruling's description.** *"the standing-runner design session, which carries GS-ATRF-1 through
GS-ATRF-4 and GS-CYB-1 and GS-CYB-2. This is substantive product design work, carried out through
Write/Edit, on a surface where getting it wrong has real downstream consequences for the IDEA loop's
architecture. It is the strongest candidate after W2 activation work, because it is genuinely
consequential and genuinely composed."*

**What the primary record says the track has already done.** Three design sittings have run:
**R8** (2026-08-30, `2026-08-30-standing-runner-design-R8.md` — GS-CYB-2's consumption/update rule,
the observation apparatus R8-D6, R8-D7 restated as the M/W/S election, the Q-C2b signature),
**R9** and **R10** (2026-09-04, `…-R9.md`, `…-R10-twelve-environment-amendment.md` — the bidirectional
algorithm, the cognitive-environments framework, A2/A3/A4, item D, the twelve-environment
amendment). The priority index's named-inputs register (`00-PRIORITY-INDEX.md` §"Named inputs")
carries an `[R8:…]`/`[R9:…]`/`[R10:…]` disposition on every standing-runner row — GS-CYB-1 *"gate
untouched; B1/B2 recorded"*; GS-CYB-2 *"DESIGNED"* with a second read added; GS-ATRF-3 *"RESOLVED"*
on production apply status; GS-ATRF-1's proxy *"DESIGNED at §3… §5"*; GS-ATRF-4 ↔ the environment
tag *"carried untouched… REDIRECTED to the standing-runner track's next design-capable session"*
(and, at R9 §4.2's own heading, *"examined, recommended, not owned, not built"*). The 2026-09-04 gate ruling's *"the founder opens
the session"* was exercised the same day (R9 ran after the founder opened it in-conversation, per
CLAUDE.md). **The M/W/S election is now MADE (2026-09-13, W) and R8-D7's sampling policy is
unblocked** — scoped separately this session
(`operations/agent-circles-2026-08/2026-09-13-R8-D7-sampling-policy-SCOPING-DRAFT.md`).

**What remains on the track as authored Write/Edit work**, from R9 §16 and R8 §11 (each *"NOT
authorised… a founder election"*):

| Item | Nature | Live surface? | `GUARD_RE`? |
|---|---|---|---|
| R8-D7's sampling policy design (unblocked 2026-09-13) | design document | no (design) | no |
| The manifest ATRF item-3 wording amendment, drafted for ruling (R9 §16.5) | governance draft on a governing surface | the manifest — founder/mentor surface, not product | no |
| A mentor question on an environment-sensitive blast-radius indicator (R9 §16.9) | question draft | no | no |
| The standing-runner build brief, second increment (R9 §16.2; R8 §11.2) | a build brief, then a build: nullable `generative_environment` column, `derivation` JSONB, `runner_melete_entries` with export/delete wiring, `role_context`, the runner cycle-open read behind `SUBSTRATE_RUNNER_CYCLE_OPEN_READ_ENABLED`, dashboard folds — one Q-B2-bundled migration window | yes when built (`/api/practice/watching`, `/api/founder/watching`, migrations) | the brief: no. The build: **`idea-loop-watching-store.ts` matches (`/substrate/`)**; `api/practice/watching/handler.ts` does not |
| The A2 engine change — role consumption in `/api/guardrail` (R9 §16.3) | `code-critical`, gated on item D's end condition | yes — the live gate | **yes** (`api/guardrail`) |
| Phase-1 activation of the completion-signal write path (R8 §11.3) | founder-walked | yes | founder act |
| Item D's end condition; the harness identity for the v1 executing actor (R9 §16.4, §16.10) | founder acts | — | — |
| Attic/Cellar heuristics; the v2 environment chooser (R9 §16.7–8) | deferred by ruling / not proposed | — | — |

**Against the channel constraint.** Design and brief authoring is Write/Edit on documents in
`operations/agent-circles-2026-08/`. The ruling characterises that as *"a surface where getting it
wrong has real downstream consequences"*; Q-W1's phrase is *"consequential decisions carried out
through composed, narrated actions"* and the 2026-09-13 clock rule says *"on a live surface"*. Both
texts are quoted here so the founder reads the fit directly; this document does not adjudicate
whether a design document is a "live surface" in the clock rule's sense.

**Against the variety constraint (what the work requires).** Each design sitting so far has run a
three-reviewer PR19 pass with findings folded or refuted (R8: 3 HIGH/8 MEDIUM folded, one refuted;
R9: 44 findings folded; R10: 45 findings, one of which reversed the sitting's central question), and
each has put questions of principle to the mentor rather than deciding them. The build increment, if
elected, adds a founder-walked migration window and flag steps.

**Size.** Each design sitting has been one session. The remaining design-class items above are
roughly two to three sittings (the sampling-policy design; the manifest draft; the build brief). The
build increment is several sessions plus founder-walked steps.

**`GUARD_RE` exposure.** The design/brief work: **none**. The build increment: partial
(`idea-loop-watching-store.ts`). The A2 engine change: certain, and `code-critical`.

---

## 4. The comparison, side by side — and the question the founder must answer that no ruling has

| | (a) W2 activation's remaining authored work | (b) the pre-flip report's outstanding items | (c) the standing-runner track |
|---|---|---|---|
| Authored Write/Edit work actually remaining | small: apply an already-composed clause (after F-3); R18 docs for the field, sequenced to activation | on the record, none — the Part-1 fix is done and the D2 specification is complete; the build S11-D2 remains | design-class: R8-D7's policy design, a manifest draft, a build brief; then a build if elected |
| Live surface | public trust record + ADR-013 + three R18 surfaces | `/api/reason`'s engine (S11-D2) | design documents; the watching surfaces and the gate if the track goes to build |
| Sessions | <1 + ~1 at activation (+1–2 per follow-on if elected) | ~1 (S11-D2) | 2–3 design; several build |
| `GUARD_RE` | code items yes; docs no | certain (pinned file) | design no; build partial; A2 certain |
| Coupled to a founder-walked step | activation is coupled to the flip (W3-d) | the waiver + the "ordinary days" judgement | migration windows; activations |

**The question that sits under all three, named and not answered.** The window needs consult
records on the measured checkout. The guard binds on that checkout while capture is on. Every code
item above that matches `GUARD_RE` must therefore be built either under a recorded per-commit waiver
with the guard armed (the shape the D2 mid-window ruling prescribes for one file) or in a worktree
the window cannot see (the shape W2 took, which the ruling says produced nothing). **Whether the W2
window's discipline tolerates guarded-file waivers as part of the window's own work is not ruled.**
The candidates differ sharply on how much of their work is exposed to it: (c)'s design work is not
exposed at all; (a)'s documentation work is not, its code work is; (b)'s only work is.

**A second factual note for the founder**, stated because it changes the ruled candidate list rather
than the founder's choice: candidate (b) as the ruling describes it consists of one item that was
fixed the day before the ruling and one item whose rulings landed a week before it. The verbatim's
recording note already corrects the first; the second is corrected here from the register's own row.
If the founder relays this, the ruling's ordering may or may not change — that is the mentor's, not
this document's.

---

## 5. The founder's next step, exactly as F-1 in the opener states it

> **F-1 (HIGHEST)** — **Designate the W2 window's work** (ruled 2026-09-13; the relay was sent and
> both questions ruled): assess what W2 activation still requires as composed Write/Edit authoring;
> if insufficient, designate the standing-runner design session. The clock starts at that work's
> first consult record. Then author the first session prompt naming the work, never the observation.

**This session did not author that prompt and must not** (the session prompt's §0: *"a session
steeped in the observation goal is the wrong author"*).

**D2 remains blocked. The S11 flip remains REFUSED. Weights remain BLOCKED. The 0h call remains the
founder's.**
