# S11 FLIP PREREQUISITES — the standing open-questions register

> **STATUS: STANDING. The session that reconsiders the S11 ENFORCE flip MUST read this file in full at open.**
>
> **Created 2026-07-17** under `D-TRUST-LAYER-S11-F2-MENTOR-RULING-EXCLUSION-CLAUSE-GOVERNS-ADOPTED`, on the mentor's instruction (**R15**, binding):
>
> > *"Which input the flip would use — the at-action verdict or the accumulated trust state — was named as unresolved. It does not need to be resolved in the narrowing session, but **it needs to be added to the open questions register before the session closes.** The at-action verdict and the trust state currently produce different recommendations on this action class. That divergence will matter when the flip is reconsidered, and **it should not be discovered again from scratch at that point.**"*
>
> No standalone register existed — the `fix_before_s10` list is buried in an audit report's §4 and the decision log is ~14,500 lines, which is exactly the "discovered again from scratch" risk the instruction names. **This file is that register.** It is short by design. **Append; do not let it sprawl.**

**Binding specifications this register serves (verbatim wins over every line below):**
- `operations/trust-layer-2026-07/2026-07-12-mentor-consultation-s11-enforce-gate-verdict-verbatim.md` — the deferral + the four-part readiness standard + the flip's binding shape
- `operations/trust-layer-2026-07/2026-07-17-mentor-consultation-F2-exclusion-clause-verdict-verbatim.md` — the exclusion-clause ruling (§1) + the supplementary ruling (§1b)

**Standing state:** **ENFORCE remains S11, readiness-gated. The readiness standard is NOT met (mentor, 2026-07-17). The flip does not proceed. Weights BLOCKED. The 0h call remains the founder's.**

---

## A. Hard prerequisites — the flip cannot be reconsidered until each is discharged

| # | Prerequisite | Source | Status |
|---|---|---|---|
| **P1** | **The §9 input question: what does the decision table actually read when it fires?** The **at-action verdict** and the **accumulated trust state** currently produce **different recommendations on the same action class** — the F2 briefing's §9 reconstruction over its **125-record snapshot** *(attribution corrected 2026-07-18 — these figures are the briefing-era snapshot's, NOT derivable from the frozen 130; the same computation over the frozen 130 reads **129 `do-not-proceed` / 1 pause** vs **130 `proceed`**)*: the at-action verdict's justice surface (Arm 1's reading) ⇒ **124 `do-not-proceed`** (+1 pause); `justiceSurface:'none'` (the §4 reading) ⇒ **125 `proceed`**; while the **live** harness advisory reads the trust state and reports **`proceed/log`** — three readings. The mentor: *"That divergence is not a minor implementation detail. It is the question of what the decision table is actually reading when it fires."* | 2026-07-17 §1 (R8) + §1b (R15) | **RULED + DISCHARGED 2026-09-04** — `2026-09-04-mentor-ruling-P1-decision-table-input-verbatim.md` (BINDING; verbatim wins). **Ruled: the AT-ACTION verdict is the table's input for the per-action rows, FILTERED by Q3's kathekon-engagement threshold before any justice surface is reported.** A verdict meeting no kathekon condition reports `justiceSurface: 'none'`, NOT `'unevaluated'` — so **Q2's zero-false-positive floor is RESTORED and its staging premise STANDS**; the 129/130 figure is the unfiltered composition Q3 exists to forbid. **The register's "three readings" framing is superseded** (scope doc Finding A, mentor-accepted): there was one wired path + two RECONSTRUCTIONS — `recommendIntervention` has exactly ONE live caller (`readTrustVerdict`, `harness-integration.ts:563`). **Reading 3 was never a distinct third reading** (Finding B → new item **D5**). The AGGREGATE is NOT the per-action input — its consumer is Q7 depth calibration, undisturbed. `interventionInputFromS3` is not wrong, it is wired to the wrong consumer for enforce; a sibling `interventionInputFromAtAction` serves that path, reusing the ALREADY-IMPLEMENTED `assessKathekonEngagement` (no new predicate). **Ruling licenses NO build/schema/activation/publication and does NOT move P4/P5/P6 or the flip.** |
| **P2** | **The Arm-1 narrowing must land.** *"Arm 1 requires at least one identified circle."* → **FURTHER narrowed 2026-07-19: ≥1 circle BEYOND self_preservation.** | 2026-07-17 §1 (R1) + §1b (R11); **2026-07-19 self-circle ruling** | **PREDICATE LANDED IN-REPO (2×).** S11b (2026-07-18): the ≥1-circle rule, predicate + reducer in tandem. **2026-07-19 self-circle narrowing:** the PREDICATE (`kathekon-engagement.ts`) now requires `beyondSelfCircleCount ≥ 1` (circle name ≠ `self_preservation`; unknown-identity circles never satisfy it; Arms 2–4 unchanged). Batteries: kathekon **105/0** (§8 both directions + the R13 `selfCircleExclusion` bound), loop-fold **172/0** (§19 the v2 three-way split). The frozen-buffer dry-run under the self-circle-narrowed predicate reads **strict 129 FP / 0 CH** (legacy bracket 128 FP / 1 CH — the one former CH is a v1 record with no captured circle identity; evidence at `runs/2026-07-19/`), NOT a readiness claim — the old lean composition, one action class, no denominator. **⚠ THE REDUCER HALF IS NOT SELF-CIRCLE-NARROWED** — `deriveWorstJusticeOutcome` reads no circle identity, so the LIVE trust ledger still emits `justice-surface-unevaluated`/`indeterminate` for self-only circles. **That correction is a SEPARATE `code-critical` founder-walked step (D3; new register item D4)** — the predicate is now deliberately narrower than the reducer on self-only inputs (pinned §8.9). |
| **P3** | **The extraction question must be settled** — is Layer 1 identifying affected parties where they are genuinely present? **This GATES the narrowing:** *"If the extraction is known to be reliable on the action classes in scope, narrow the arm. **If the extraction is uncertain, the extraction question is prior.**"* Two hypotheses with **opposite** remedies: **starved** (should find parties, doesn't) vs **mis-sited** (a tool-call payload genuinely has no party/role/purpose — the project already acted on this reading once, dropping Bash from the consult). | 2026-07-17 §1b (R12) | **ANSWERED AT DIAGNOSIS LEVEL (S11a, 2026-07-18 — Diagnostic-certain; `2026-07-18-S11a-extraction-gate-diagnosis.md`): STARVED BY COMPOSITION at the harness site**, resolving the starved-vs-mis-sited fork: `describeAction` (at-action-hook.mjs:280-321) sends a `Write` as path + char count — **content discarded entirely** (64/64 buffer records match) — and an `Edit` as the first 200 chars of `new_string`; the consult POSTs that one line. NOT a Layer-1 defect (the prompt is faithful; circles surface when the composed text carries parties — U2/§4 smokes + the in-scope idx-4 record) and NOT mis-sited ("the payload has no party" is factually wrong for `Write` — the payload HAS content; the hook drops it). **Remedy = the S11b examined-input recomposition** (work item authored 2026-07-18). ~~The extraction remains UN-FIXED until S11b lands~~ → **REMEDIED IN-REPO + LIVE IN THE FOUNDER LOOP (S11b, 2026-07-18; hooks hot-load from the working tree):** `action-composer.mjs` — narrated intent (transcript tail, frame-stripped, redacted) + bounded payload (Edit both sides; Write head+tail), the MANDATORY append-only sensitive-path denylist + token redaction (elections E1/E2), payload-content-hash dedup (E3), `GATE1_ACTION_TEXT_MODE` knob default composed (E4), regime version-marks settled once (`at-action-v2-composed`/`at-action-v1-lean`, ADR-014) + the item-5 T2-soft bare-payload trigger. **Validation (real composer + real L2 chain, N=3, pre-stated thresholds — r2):** party-affecting **9/9 runs with circles** (the starvation closed); party-less clean; **mention-without-affect FAILED 6/6 — Layer 1 converts QUOTED party language into circles** (recorded, not tuned away — `NARROWED_ARM_BOUNDS.mentionConversion`; **the Layer-1 re-check is a NAMED FOLLOW-UP, its own Critical step**); noise 0/3. Composed extraction latency 13–20s (two live 28s consult timeouts observed — fail-open-honest; the lean knob + budget trims are the mitigations). The residual mis-siting question was resolved INSIDE the composition (intent first, payload second). |
| **P4** | **The readiness standard's four parts** — (1) ≥7 days over a **representative** distribution; (2) all four cardinal domains evaluated, confidence above conservative on ≥2; (3) a measured false-hold **rate**; (4) Q3 encoded. | 2026-07-12 verdict (Q1) | **NOT MET.** (1) fails — the 2026-07-12→17 window was **one tool class, one depth, one proximity**. (2) fails — **one** evaluated cardinal domain (`confidence_weight: 0.42`). (3) **unmeasured** — see P5. (4) satisfied, but the predicate it encodes is now narrowing. |
| **P5** | **Part (3)'s denominator has no source.** *"Part (3) as specified may be unmeasurable on this capture set regardless of the ruling. The genuinely dangerous actions are on the guard path, which writes no record."* The capture is CONSULT-path only (`at-action-hook.mjs:595`, inside `runConsult`); `runGuard` writes nothing; Bash is not consulted. So *"correct holds on genuinely problematic actions"* — the denominator of the mentor's ratio — cannot be populated. **⚠ RECORD CORRECTION 2026-09-05 (mentor-directed; `2026-09-05-mentor-ruling-P6-window-recommendation-verbatim.md`): the sentences above are STALE, not wrong-at-the-time.** `runGuard` HAS written records since **commit `3e8f231`, 2026-08-17 ("R2b item 8" — P8a)**: `buildGuardHoldRecord` (schema **v4**, `path: "guard"`, `guardHold` [deny only — a caution allows the tool and would make the denominators incommensurable], `guardOutcome`, `captureBasis` distinguishing a classifiable assessment from an outage with none) is wired at three call sites in `at-action-hook.mjs` behind the same `GATE1_FALSE_HOLD_CAPTURE` flag, with its own PR19 fold and two separately-pinned safety properties (fail-soft capture; fail-safe placement AFTER the deny emit). **Read the Status cell, not the Item cell.** The stale text was inherited by `2026-09-04-C-at-action-seam-caller-SCOPE.md` §4 before being caught. | 2026-07-17 §1 (R6) | **OPEN — status UNCHANGED, and the correction above does not close it.** The mentor: *"P8a's build discharges P5's blocking claim at the code level. The register correction is owed independently"*, and *"P8a's build does not discharge P5 — activation is open."* **A built-dark denominator is not a measured one:** `GATE1_FALSE_HOLD_CAPTURE` is unset, the clock is stopped, and no guard record has ever been written. Any re-measurement design must still solve it — by activating P8a, not by rebuilding it. |
| **P6** | **A NEW observation window** — the 2026-07-12→17 buffer **cannot** be reused for part (3): it predates the narrowing, is one action class, and has no denominator. A re-measurement needs: the narrowed predicate + a **representative** distribution + a populated denominator (P5). The clock was **stopped 2026-07-17**; the buffer is frozen as evidence. | 2026-07-17 §4 (election 2) | **SCOPED 2026-08-15** (concurrent-arc C1): `operations/trust-layer-2026-07/2026-08-15-false-hold-new-window-scoping-note.md` — v3/composed-regime records only (never mix regimes); the narrowed predicate frozen as the measured classifier; **P8a guard-path capture is a hard precondition** (solves P5's denominator); coverage/loss accounting (timeouts + transient 401s counted); bounds printed on the rate; durable `GATE1_STATE_DIR`; **window starts only as R4's LAST step, after every guard-bundle activation** (a window spanning instrument edits measures neither state; the M1 window-conditional guard re-arms at start by design). Build = R2 item 8; activation + start = R4. *Prior status inputs (S11b regime, mention-conversion bound, latency bias) are folded into the note.* **AMENDED 2026-09-05 by binding ruling** (`2026-09-05-mentor-ruling-P6-window-recommendation-verbatim.md`; scope: `2026-09-05-P6-window-recommendation-SCOPE-FOR-RULING.md`): **the window's stated purpose WIDENS to cover the decision table's RECOMMENDATION, not only hold classification** — part (3) names a false-hold rate and *"under G6(a), a hold is what a do-not-proceed produces"*, so the widening is *"a correction of the original scoping to what part (3) actually names."* **Derived at REPORT TIME, never stored in the capture record** (the table is under active ruling — P1 moved its input mid-arc — so a stored reading would freeze stale evidence that looks authoritative); the record shape is unchanged and `recordHash` is untouched. **Consequence: measuring it needs NO capture-layer edit, so it carries NO contamination exposure and NO R2 deadline** — the scope withdrew its own deadline claim and the ruling accepts the withdrawal. Consult and guard populations reported **separately** (not optional). **Three bounds PRINTED ON THE RATE, not footnoted:** the A8 bound (no re-examination counter exists anywhere in the harness ⇒ `habitualReExaminationCount` floors to 0 at `intervention-engine.ts:392` ⇒ the two-then-escalate row can NEVER fire), the guard-record `depth: ""` bound, and an as-of-table disclosure (the derivation reflects TODAY's table, not the table at capture time). **A v3/v4 lift check runs before any figure is published** (the existing non-vacuous round-trip is proven on 130 **v1** records only) — a precondition of publication, not of the ruling. **Licenses no build, activation, or publication; P6 is NOT discharged, not scoped further, and the window has not started.** Note added at the amendment: §2.1 of the note says v3-only, but P8a emits **v4** guard records — the report's multi-version whitelist covers both, and `path` is the population discriminator. |

## B. Named bounds that must be stated ON the enforcement claim at the flip

Per the 2026-07-12 verdict (Q4/Q5) — these do **not** block the flip; they must be **published as bounds on what the enforcement claim covers**, not merely carried as register items:

| # | Bound |
|---|---|
| **B1** | **PA-5** — the direct reflect path's mintability. The enforcement claim covers trust levels produced by the **harnessed** path; it does **not** cover levels modulated by the direct reflect path. Disclosure upgraded from carried-register item to a **named bound on the enforcement surface** at the flip. |
| **B2** | **PA-10** — stale-artifact replay (carry-with-disclosure; over-trust is the safer error direction; the A5 recency tier is the shaped closure at the S2 fold). |
| **B3** | **The A2 decrease-dodge — declared IRREDUCIBLE.** *"The enforcement binds on what the examination can find. It does not bind on what the examination cannot find because it was not narrated."* Not closeable by any text-based fix; **state the bound accurately.** |

## C. The flip's binding shape (when it is finally reconsidered)

Per the 2026-07-12 verdict — **do not re-derive this; it is settled**:
- **STAGED, not whole-table:** the **do-not-proceed class first**, then the pause rows + the G6 loop bound after a live calibration window. Faithful **iff** disclosed-as-partial with a committed completion target.
  - ⚠ **2026-07-17 rider (R5):** the staging rested on *"a benign action that engages no kathekon factors cannot trigger a do-not-proceed row — a structural false-positive floor of zero."* **The exclusion-clause ruling PRESERVES that premise** — but it is now premise-dependent: had Arm 1 governed, **124/125** would have routed do-not-proceed. **The staging is safe only if the narrowing (P2) lands.**
- **G6(a) QUALIFIED** by the kathekon-engagement threshold; a "contrary; no kathekon factors detected" verdict ⇒ log-and-continue + a developmental flag + a recorded false-positive instance, **never** a do-not-proceed. **G6(b) inherits.**
- **The calling-gate enforce arm flips WITH the engine** (one coherent regime; the gate is the structural precondition for a non-circular examination).
- **Aggregate-keyed conservative depth binds at v1**; the per-domain `deliberate⇒quick` carve-out is gated on the Q5 (A5 recency-tier) closures.
- **The assent is re-confirmed at flip time (PR7).**

## D. Live consequences of the same root (not flip-gating, but open)

| # | Item | Status |
|---|---|---|
| **D1** | **The public `justice_capped: true` on `sagereasoning:s9-loop@v1`.** **GROUND NOW QUERIED (2026-07-17, was "inferred") — and it is narrower than first recorded:** the ledger holds **exactly TWO** examination-derived events, both at the **S9 install, `2026-07-11 05:45:29.674+00`**, artifact `signed:substrate-layer2-2026Q2` — `credential-completed` + **one** `justice-surface-unevaluated`. **The cap rests on that single event, NOT on a stream from the 130 observation records** — the at-action consults never became trust events (emission fires only on accreditation writes, `emitAccreditationTrustEvents` gated on `provenanceEnforced`). Every other ledger row is a reflect event (`virtue_domain: null`, `artifact_ref: reflect:reflect-<session-uuid>`) — modulate-only, cannot latch. **Consequence for S11a: the cap review is ONE event to examine, not a stream.** The clearing bound is unchanged — clearing needs `justice-surface-transparently-handled` ⇒ a circle carrying `status:'met'` ⇒ impossible on this action class. *"A permanent cap on an agent whose actions had no identified affected parties is a live signal on a live surface."* **S11a closing condition (R14):** *"Do not close the narrowing session with the arm fixed and the cap unreviewed."* | **REVIEWED — S11a, 2026-07-18 (R14 discharged on CURRENT ground; the arm was NOT narrowed — the extraction gate ruled "prior" — and the review says so plainly).** Public surface re-verified first-hand same day (read-only GET): `justice_capped: true`, "justice cap active", 1 evaluated domain, confidence 0.42; gen-2 harness live (`honest_reflect_count: 11`, latest 2026-07-17). **Disposition (founder election): DISCLOSE-AND-CARRY; the durable fix rides S11b.** Deciding argument = stability: with the gen-2 harness writing, a data-only latch clear is UNDONE by the next accreditation close-write (the un-narrowed reducer re-emits `justice-surface-unevaluated` off a zero-circle assessment and re-latches). Durable fix = the reducer narrowing (`deriveWorstJusticeOutcome`, LIVE ⇒ `code-critical` + founder-walked AC7, per D3) + the state correction in the SAME walk — one coherent change inside S11b. The ledger event is append-only historical truth and is NOT deleted. Cap remains the safe direction; no external users; R18 honesty issue, disclosed here + in the S11a diagnosis §5. **CORRECTION PREPARED (S11b, 2026-07-18):** the reducer narrowing is in-repo (battery-pinned: zero-circle no longer derives `justice-surface-unevaluated`; the circle-present J2 class still latches — correctly); the founder walk (`2026-07-18-S11b-part3-founder-walk.md`) deploys it FIRST, then clears `justice_floor_active` by SQL (rollback pre-written in the walk doc; §VERIFY = DB SELECT + public GET `justice_capped:false` + the re-latch watch at the next gen-2 close-write). **Disclosed caveat (first-hand review):** the historical ledger event stays append-only, so a future FULL-LEDGER REPLAY of the materialised state would re-latch this row — any rebuild tooling must re-apply the correction (or a by-hand replay must re-run the walk's §C3). **✅ WALK DISCHARGED (2026-07-18, founder-walked `code-critical` 0c-ii; AC7 + PR17):** commit `b2ae8d5` pushed + Vercel green (the narrowed reducer verified on `origin/main` BEFORE the SQL — the ordering guard held); pre-state SELECT read 1 row latch=true; the rollback statement was saved before the forward UPDATE; post-UPDATE SELECT read `justice_floor_active=false`; **the public GET verified first-hand: `justice_capped: false`**, basis = "minimum-domain rule across 1 evaluated domain(s): dikaiosyne=deliberate; weighted by source confidence (min weight 0.420)" — the cap clause GONE, the earned level correctly retained. **The public signal the mentor called "not a correct public signal" (R7) is corrected, stably.** REMAINING OBSERVATION: the re-latch watch — after the next gen-2 accreditation close-write, re-read the GET (expect still false); record here. |
| **D2** | **The two "engaged" definitions** — `computeVirtueDomains` (`circles ≥ 1 \|\| is_kathekon !== null`) vs `computeDikaiosyneFloor` (`circles ≥ 1 \|\| hasNaturalRelationship`). **R3: the newer, more precise one governs.** Whether the older tag is reconciled at the root or left divergent is undecided. | **OPEN.** |
| **D3** | **`derive-trust-events.ts` is LIVE** (trust core ON in production under MEASURE since 2026-07-11). Any narrowing at the **reducer** — as opposed to the predicate — changes live trust-event emission ⇒ **`code-critical` + founder-walked (AC7)**. The predicate (`kathekon-engagement.ts`) is report-time only ⇒ `code-elevated`. **Same root, different tiers.** | **Standing constraint.** |
| **D4** | **The reducer self-circle narrowing (the 2026-07-19 ruling's live-surface half).** The 2026-07-19 predicate narrowing (Arm 1 requires ≥1 circle beyond `self_preservation`) landed in `kathekon-engagement.ts` (MEASURE-consumed — `loop_fold`, the report) but NOT in `deriveWorstJusticeOutcome` (`derive-trust-events.ts`), which reads no circle identity and so still emits `justice-surface-unevaluated`/`justice-surface-indeterminate` for a self-only-circle assessment. Per the ruling this is a mis-attribution (self-only action is `phronesis`/`sophrosyne`, not `dikaiosyne`). Because the reducer is a LIVE trust-event emitter, correcting it is `code-critical` + founder-walked (D3). **Consequence for the flip:** the predicate the flip binds on (G6(a)) is now narrower than the reducer that feeds the live justice ledger — the P1 §9 input divergence gains a THIRD axis (predicate vs reducer vs trust-state, on self-only circles). The reducer half must be settled — alongside D1's cap logic, which also rests on `deriveWorstJusticeOutcome` — before the flip is reconsidered. Also revisit: the `computeVirtueDomains` self-circle attachment (D2's "two engaged definitions" now has a self-circle dimension). | **BUILT DARK 2026-08-17 (R2b) — ACTIVATION OPEN, founder-walked `code-critical` (D3).** `deriveWorstJusticeOutcome` now reads circle identity behind an OPT-IN (`requireBeyondSelfCircle`, default false ⇒ pre-D4 byte-identical, battery-asserted), bound at its ONE live consumer (`deriveCredentialAndJusticeEvents`) to the NEW dedicated flag `SUBSTRATE_JUSTICE_SELF_CIRCLE_NARROWING_ENABLED` (unset everywhere; dedicated, not a reuse of `SUBSTRATE_TRUST_CORE_ENABLED`, per the shared-flag lesson). **The narrowing is SYMMETRIC across all four outcomes — CORRECTED 2026-08-16 by mentor ruling M-1, which OVERTURNED this build's original asymmetry.** As first built, `unevaluated`/`indeterminate`/`met` were gated on ≥1 identified circle beyond `self_preservation` while **`violated` was left ungated**, on the ground that dropping adverse justice evidence makes trust read HIGHER. **The mentor rejected that reasoning:** *"the evidence is not being dropped; it is being correctly attributed."* Dikaiosyne is other-directed whether the obligation was met or violated; preserving the violated case *"is not conservative — it is preserving a category error in the direction that hard-floors the wrong domain."* A false adverse signal is not safer than a false positive one — it corrupts the ledger by attributing a self-regarding failure to a domain that was never engaged, and the hard-floor exemption from hysteresis makes that worse, not better. **All four outcomes are now gated.** Grounds per outcome: ruling #4 names `indeterminate` verbatim as the trigger misfiring; #1 denies the self circle is a justice surface at all; #3 puts self-regarding action under phronesis/sophrosyne, so a `met` credit is a rise the agent never earned — **and M-1 extends #1/#3 to `violated` on the same logic.** **CARRIED, named by M-1 and NOT built:** the correct destination for a genuine self-only violation is `phronesis`/`sophrosyne`; this reducer emits into dikaiosyne by construction and cannot route there. The ruled interim posture is verbatim: *"If the implementation cannot yet route to those domains, the correct interim posture is to WITHHOLD the dikaiosyne emission rather than preserve a known mis-attribution."* **A deliberate predicate/reducer divergence now exists in the OPPOSITE direction from the pre-D4 one and must not be "fixed":** the predicate's Arms 2–4 still ENGAGE a self-only violated obligation (engagement ≠ emission; the 2026-07-19 ruling left Arms 2–4 untouched and M-1 rules only on emission). Pinned §8.9d/§8.9e. STRICT on unknown identity, matching the predicate (unreachable from live input — `circle` is required on every live assessment). **A coupling found in build and guarded:** narrowing the reducer *under the predicate* would collapse `selfCircleOnlySuppression` (its first conjunct is `justice !== null`), silently emptying `loop_fold`'s live `self_regarding` bucket — so the predicate's delegation deliberately does NOT pass the flag, and §8.1c/§8.9b/§8.11 fail if anyone makes it (mutation-verified). Batteries after the M-1 correction: trust-core **112/0** (14 D4 pins, three mutations verified; D4-8 INVERTED by M-1), kathekon **113/0** (§8.9 amended to a TWO-STATE pin rather than inverted — a flat inversion would assert something false while the flag is unset; §8.9d inverted by M-1 and §8.9e added to hold the engagement/emission distinction), loop-fold 181/0, practice-suggestion 791/0, emission-hooks 19/0. **The activation walk must, in this order:** (1) re-read D1's `justice_floor_active` for `sagereasoning:s9-loop@v1`/dikaiosyne — the harness is live and writing, so it may have RE-LATCHED since the 2026-07-18 clear; (2) deploy + verify the narrowed reducer on `origin/main` FIRST; (3) only then the flag, and any SQL correction with the rollback pre-written (the S11b Part-3 ordering guard). **D4 does NOT close D1's full-ledger-replay caveat** — the historical 2026-07-11 event is append-only and a replay still re-latches. `computeVirtueDomains`/D2 deliberately EXCLUDED (a scoring-engine change that would pull `/api/reason` determinism into a trust-ledger step) — carried. **R18 (named, since nothing here surfaced it before):** TWO public claims sit on this mechanism. (a) `llms.txt:418` describes the `loop_fold` **`self_regarding`** bucket as *"loops whose justice arm was suppressed only because every identified circle was `self_preservation`"* — that bucket is gated by `selfCircleOnlySuppression`, so narrowing the predicate's delegation would not merely regress a battery, it would make a PUBLISHED claim false on the next deploy. The opt-out above is what holds it true; verified verbatim 2026-08-17. (b) `llms.txt:632` documents the trust record as *"capped at `deliberate` while a justice latch is active"* — that sentence stays TRUE under D4 (the latch mechanism is unchanged; only WHICH agents reach it changes), so **no doc edit is due while the flag is unset**, but the activation walk must carry an explicit R18 decision rather than assume silence. **Frozen-buffer acceptance gate RUN 2026-08-17, before any report-script edit: strict `n=130 false_positive=129 correct=0`** — unchanged from the 2026-07-19 record, so the PREDICATE provably did not move. |
| **D5** | **`taskHasJusticeSurface` is never supplied — the live advisory answers a question it never asks.** *(⚠ STATED IN THE PRESENT TENSE **AS FOUND**, 2026-09-04, and preserved rather than rewritten per this file's own "do not delete it" discipline. **Every present-tense claim in this cell is now FALSE of current code, and the line numbers it cites are PRE-FIX** — read the Status cell for what is actually true. Flagged because a stale present-tense mechanism fact read as current is a failure class this project has hit repeatedly, and PR20 requires the timestamp check.)* `readTrustVerdict` declares `taskHasJusticeSurface?: boolean` and defaults it **false** (`harness-integration.ts:566`); **no caller anywhere passes it** — verified by grep across `website/src` + `harness/`: the only occurrences are the parameter definitions and FOUR call sites, none supplying it (`harness-integration.ts:566` the default · `api/practice/discernment/handler.ts:191` · `api/trust-record/[agent_id]/handler.ts:152`, which passes `strictStore` only · **`website/scripts/false-hold-observation-report.ts:358`, found by `tsc` at the build — the scope doc's grep missed `website/scripts`; count corrected same day, conclusion unchanged**). `interventionInputFromS3` gates the ENTIRE justice branch on that flag (`intervention-engine.ts:786`), so the wired path reports `justiceSurface: 'none'` **unconditionally, by omission**. The live harness advisory therefore does not judge that no justice surface was engaged — **it never asks.** Mentor 2026-09-04, verbatim: *"a honesty defect in the MEASURE-mode sense — the instrument reports a result without having performed the relevant test"*; **"a standing defect that the P1 ruling does not license deferring"** — owed INDEPENDENTLY of P1, not a P1 consequence. Fix: supply the flag where it is known, or remove the parameter as unreachable. Ruled to be corrected in the SAME build pass as P1's consequences, but **gated only on that build being licensed — which the P1 ruling does not do.** | **CLOSED 2026-09-04 — fixed, deployed, live-verified in code.** Fixed in the SAME build pass as P1's consequences, as ruled. `taskHasJusticeSurface` is now **REQUIRED** on `readTrustVerdict` (`harness-integration.ts:531` — no `?`, so tsc refuses an omitting caller) and **every call site states it with its reason**: `api/practice/discernment/handler.ts:197` and `api/trust-record/[agent_id]/handler.ts:157` (both genuinely task-agnostic standing reads — no task is in scope, so `false` is the honest value, not an omission) and `scripts/false-hold-observation-report.ts:358` (the task-agnostic report read). The `basis` string is re-labelled per the ruling's third consequence (`harness-integration.ts:605` — the recommendation is disclosed as ranging over the AGGREGATE trust state, task-agnostic, explicitly *not* the per-action decision-table input). Commit `50c0c0f`, pushed and Vercel green 2026-09-04; the regression check is a `grep` for `readTrustVerdict(` across `src` + `scripts` filtered to lines lacking `taskHasJusticeSurface`, which must print nothing. **NO LIVE OUTPUT CHANGED, and that is the point:** `false` yields the same `justiceSurface: 'none'` the default yielded, so no production behaviour moved — what changed is the WARRANT. The instrument no longer reports a result without having performed the relevant test; it now states that the test is inapplicable at these call sites. That is precisely the MEASURE-mode honesty defect the mentor named, and it is what closure here means — not a behaviour fix. **Standing hazard recorded at the header for any future caller:** passing `true` without S3 obligation routing routes to `'unevaluated'` ⇒ do-not-proceed for every agent — *"you are not asking the question, you are asserting an answer."* The two existing `false` call sites are load-bearing and must NOT be flipped to `true`; a genuine task-scoped read is a DIFFERENT call site. |

---

## E. Identity + credential generations (for row attribution)

The subject of every trust/accreditation row below is **`sagereasoning:s9-loop@v1`** — one identity, two credential generations. **Attribute rows by generation before drawing any conclusion from them.**

| Gen | Role | id | Status |
|---|---|---|---|
| 1 (S9, 2026-07-11) | consult | `09e83b4d…` | **REVOKED 2026-07-17** — public-exposure incident |
| 1 (S9, 2026-07-11) | accred | `e715520b…` | **REVOKED 2026-07-17** — public-exposure incident |
| 2 (2026-07-17) | consult | **`33bef3d4-018d-4313-bcfd-65a75132155c`** | **LIVE** — 5000/200 |
| 2 (2026-07-17) | accred | **`1ffe14f6-0f07-4296-b340-c3bdfbbc7ce2`** | **LIVE** — 5000/200, owner+agent bound |

**Gen-1 rows are all pre-2026-07-17 and are established as ours** — the abuse check (incident §4b) cleared both the billing surface and the trust ledger across the full 5d13h exposure window, on two independently-corroborating tables. **The identity was deliberately NOT split to `@v2`:** it is the subject of **D1**'s cap review, and splitting would have fragmented the record that review examines. **Incident record:** `operations/incidents/2026-07-17-public-credential-exposure-s9-loop.md`.

**2026-07-19 — gen-2 consult DIAGNOSED HEALTHY (DB-verified); NO rotation performed (`D-S9-LOOP-CONSULT-CREDENTIAL-DIAGNOSED-HEALTHY-NO-REFRESH-2026-07-19`).** The P0 "consult-credential refresh" ran as a diagnosis and found **the gen-2 consult credential is healthy — no refresh needed.** **DB-verified (founder-run `mint-credential.ts list`, prod):** the settings consult token `sr_prac_0ba814…` hashes to id **`33bef3d4…`** (this §E row) — limits **5000/mo, 200/day**, monthly usage **623** (13% of cap); the accred `sr_prac_18c213…` = `1ffe14f6…`, 5000/200, used 70. **This CORRECTS the 2026-07-18 record "the raw token no longer hashes to `33bef3d4…`":** it hashes to exactly that row (623 successful calls recorded on it). **Quota is definitively NOT the constraint** — monthly 623/5000, and daily successful consults peak ~57 (07-18) far below the 200/day cap (the ~50–57/day ceiling is the SUCCESS count under a high transient-failure rate, not a limit). **Route-masking (important, and easy to mis-read):** at `/api/reason` the route flattens EVERY `validateApiKey` failure — `invalid_token`, quota-429, suspended-403, usage-RPC-503 — into a single **401** (`route.ts:726` returns `auth.error`, never `apiKey.error`; the body is the generic "Please sign in"), so the 401 status alone is ambiguous and the `list` DB lookup is the ONLY disambiguator (it here rules out quota + suspension). With those ruled out and the token valid (256 successes; 623 recorded calls), the **131 all-time 401s are transient DB-layer fail-secures under the dogfood's burst of heavy 13–20s composed consults** (a credential-lookup query error → `invalid_token` → 401; the usage-RPC fail → 503 → also masked). A fresh credential carries the SAME limits and hits the SAME fail-secures ⇒ **no mint would help.** The real remedy — server-side lookup resilience (retry-before-fail-close) + composed-consult latency — is handed off as a `code-elevated` follow-up (`operations/handoffs/founder/2026-07-19-consult-lookup-resilience-and-latency-NEXT-SESSION-PROMPT.md`), NOT a credential op. **§E rows unchanged** — gen-2 consult stays LIVE + healthy.

---

## Change log
- **2026-09-04 (P1 BUILD + D5 CLOSURE + the frozen-buffer re-run; `D-S11-P1-AT-ACTION-SEAM-BUILT-D5-FLAG-SUPPLIED-2026-09-04` and `D-S11-P1-FROZEN-BUFFER-RECLASSIFIED-UNDER-FILTERED-READING-2026-09-04`)** — the ruling's build consequences landed (commits `50c0c0f`, `d74aea0`; pushed + Vercel green). **NEW seam** `at-action-seam.ts` — `interventionInputFromAtAction`, the ENFORCE-path sibling of `interventionInputFromS3`, reusing the existing `assessKathekonEngagement` (no new predicate); **dark, wired to no live caller.** **D5 CLOSED** (row above). **The frozen 130 re-run under the filtered reading** (`runs/2026-09-04/`, input md5 `a4e2465f3897fddeea1a189c95af39a5`): over all 130, the pre-S11b reducer gives **129 `do-not-proceed` / 1 pause** (reproducing this register's own recorded figure exactly — the harness's non-vacuity check), today's reducer unfiltered gives **129 `proceed` / 1 pause**, and the FILTERED reading gives **130 `proceed`**. **Q2's zero-false-positive floor HOLDS on both readings of the unknowable legacy circle identity — but the restoration was delivered by the S11b reducer narrowing of 2026-07-18, NOT by the P1 filter, which changes exactly ONE record of 130** (the single `indeterminate` — the same record the legacy bracket flips back). Recorded against the interest of the work: the ruled seam is correct and its effect on this buffer is one record. **A clarification, not a correction — the row above is right and was read wrongly at first:** its "over the frozen 130" means exactly that, ALL 130 records including the one `closed` one; the re-run's first pass restricted to the 129 holds and got 128 / 1, which looked like a failed reproduction until the population difference was found. Both are now reported, since the holds are the population a G6(a) bound would actually bind on. **EVIDENCE ONLY — not a readiness claim. P4/P5/P6 unmoved; no schema, flag, credential, migration, or public-doc change; the flip remains REFUSED.**
- **2026-09-04 (P1 ruling, `2026-09-04-mentor-ruling-P1-decision-table-input-verbatim.md`)** — **P1 RULED + DISCHARGED.** The at-action verdict, FILTERED by Q3's kathekon-engagement threshold, is the decision table's input for the per-action rows; Q3's *"consistent with the deliberate-proximity-without-justice-surface row"* clause is read as disclosing the table's meaning for the per-action case, and that reading is extended to the per-action rows generally. **Q2's staging premise STANDS** (the zero-false-positive floor is restored, not falsified). The aggregate is not the per-action input (Q7 depth calibration undisturbed). **Two register corrections ride the ruling:** the "three readings" framing is superseded (one wired path + two reconstructions), and reading 3 is reclassified as the accidental arrival at reading 2 — now standing item **D5**, owed independently and explicitly NOT deferrable behind P1. Scope doc: `2026-09-04-P1-decision-table-input-SCOPE-FOR-RULING.md`. **Nothing built, activated, or published; P4/P5/P6 unmoved; the flip remains REFUSED.**
- **2026-08-15 (concurrent-arc C1, documents only)** — **P6 SCOPED** (design note authored: `2026-08-15-false-hold-new-window-scoping-note.md`; P8a guard-path capture named as hard precondition, solving P5's denominator; window start sequenced to R4-last per the contamination rule + the M1 window-conditional guard ruling). No build, no activation, no flag, no capture code touched; the frozen buffer untouched; `agent_hold_observations` still empty. **The flip remains REFUSED; readiness NOT met; weights BLOCKED.**
- **2026-07-19 (P0 s9-loop consult-credential DIAGNOSIS, `D-S9-LOOP-CONSULT-CREDENTIAL-DIAGNOSED-HEALTHY-NO-REFRESH-2026-07-19`)** — the "refresh" ran as a diagnosis and returned **no refresh needed: the gen-2 consult credential is healthy (DB-verified).** Founder-run `list`: the settings token hashes to `33bef3d4…` (this §E row), limits **5000/200**, monthly usage **623/5000**, daily successes peak ~57 << 200 ⇒ **quota ruled out.** At `/api/reason` the route masks EVERY `validateApiKey` failure into a 401 (quota-429, invalid_token, suspended, usage-RPC-503 all flatten via `route.ts:726`), so the 401 status is ambiguous — but with quota+suspension ruled out by the DB and the token valid (256 successes), the intermittent framing is **transient DB-layer fail-secures under load + the S11b composed-consult latency (28s timeouts)**, NOT a stale/wrong token and NOT quota. The 2026-07-18 "token no longer hashes" reading is **corrected** (it hashes to `33bef3d4…`; 623 calls recorded). Added the §E note above. **NO mint / revoke / SQL / deploy / DB change; production byte-equivalent.** The real remedy is handed off as a `code-elevated` follow-up (credential-lookup retry-before-fail-close + the lean-mode/hook-timeout latency lever), not a credential op. Register items P1–P6/B/C/D unchanged — this is not S11-flip-gating; §E is the correct home. **The flip remains REFUSED; readiness NOT met; weights BLOCKED.**
- **2026-07-19 (the SELF-CIRCLE narrowing, `D-KATHEKON-DIKAIOSYNE-SELF-CIRCLE-NARROWING-BUILT-REVIEW-FOLDED-2026-07-19` + `D-MENTOR-CONSULTATION-DIKAIOSYNE-SELF-CIRCLE-ADOPTED-2026-07-19`)** — **P2 further narrowed + PREDICATE landed** (`beyondSelfCircleCount ≥ 1`; the AE-2 activation surfaced that Layer 1 attaches `self_preservation` to every self-regarding consult, so the ≥1-circle arm still fired universally). **AE-2 `loop_fold` split RE-SPECIFIED (schema v2)** — three-way (character / self_regarding / instrument_calibration) + the dikaiosyne-evidence rule; the fold stays LIVE (MEASURE), R18 docs deferred. **Frozen-buffer re-classified** (strict 129 FP / 0 CH; legacy bracket 128 FP / 1 CH; `runs/2026-07-19/`). **New register item D4** — the REDUCER self-circle narrowing (the live-surface half; `code-critical`, founder-walked; couples with D1's cap logic). Adversarial review: the Workflow died whole on the account **monthly spend limit** (6/6 finders errored, ~1.57M tokens) → **completed FIRST-HAND per the §4 precedent, all six dimensions** — 5 CLEAN (ruling-fidelity: no broadening, `engaged` can only shrink; split-correctness: exhaustive+exclusive partition, no self-regarding hold leaks into `character.loops`; dikaiosyne-evidence: adverse evidence never dropped; legacy-bracket: `recordHash` byte-identical for v1/v2 so ingest idempotency preserved; blast-radius: `assessKathekonEngagement` MEASURE-only-consumed, live delta = the `loop_fold` block only), battery-adequacy: **2 load-bearing gaps FOLDED** (violated-on-unknown-circle; multiple/mixed self circles) → 4 new pins (kathekon §8.10–8.12, loop-fold §19.12). Two disclosed behaviours (not defects): the predicate⊂reducer divergence (D4) and a self-only reflexive-dik-only redirection reading as calibration. **NO schema / flag-set / mint / deploy / DB change; the frozen buffer untouched; `agent_hold_observations` still empty; production byte-equivalent to the AE-2 activation state (the `loop_fold` block content changes on the founder's push — flag stays `true`, the block re-specification deploys with the code).** Batteries: kathekon 105/0 · loop-fold 172/0 · capture 37/0 · logic-harness 155/0 · negative-battery 230/0 RELEASE GATE · trust-core 98/0 · s3-combiner 106/0 · emission-hooks 15/0 · route 90/90 · tsc 0 · build ✓. **The flip remains REFUSED; readiness NOT met; weights BLOCKED.**
- **2026-07-18 (S11b, `D-TRUST-LAYER-S11B-RECOMPOSITION-NARROWING-REDUCER-CAP-FIX`)** — **P3 REMEDIED in-repo** (the examined-input recomposition: `action-composer.mjs`; elections E1–E4; live in the founder loop via hot-reload; validation r2 — party-affecting 9/9 circles, **mention-without-affect FAILED 6/6, recorded as a bound**, Layer-1 re-check = named follow-up); **P2 LANDED in-repo** (predicate + reducer narrowed in tandem, both directions pinned; frozen-buffer dry-run 128 FP / 1 CH matches the S11a reconstruction — evidence, not readiness); **D1 correction PREPARED** (walk doc with pre-written rollback + the ledger-replay re-latch caveat); the ADR-014 **regime settlement DISCHARGED** (marks `at-action-v2-composed`/`at-action-v1-lean` settled once; item-5 T2-soft trigger landed; AE-1's pre-condition met once the commit pushes). Adversarial review: the Workflow died whole on the account **monthly spend limit** (7/7 finders errored) → **completed FIRST-HAND per the §4 precedent** — 1 HIGH folded at root (the v1-inherited sensitive-path **Edit snippet egress** — the lean Edit string carried ≤200 chars of `new_string`; sensitive Edits are now path-line-only in text AND summary; lean-mode Edit text redacted; zero frozen-window records show the class historically), 1 MEDIUM disclosed (ledger-replay re-latch), 3 LOW folded. Batteries: kathekon 79/0 · trust-core 98/0 · emission-hooks 15/0 · S10 106/0 · logic-harness 155/0 · capture 32/0 · **negative-battery 230/0 RELEASE GATE** · tsc 0. **The flip remains REFUSED; nothing here is progress toward it; weights BLOCKED.**
- **2026-07-17** — created (mentor R15). Seeded A/P1–P6, B/B1–B3, C, D/D1–D3 from the 2026-07-12 and 2026-07-17 binding verdicts. Standing state: **readiness NOT met; flip does not proceed.**
- **2026-07-18 (S11a, `D-TRUST-LAYER-S11A-EXTRACTION-PRIOR-NARROWING-DEFERRED-CAP-REVIEWED`)** — **P3 ANSWERED AT DIAGNOSIS LEVEL** (starved-by-composition at the harness site — Diagnostic-certain; `2026-07-18-S11a-extraction-gate-diagnosis.md`); **P2 DEFERRED behind P3** (the R12 gate ruled "prior"; the narrowing rides S11b on the recomposed input); **D1 REVIEWED** (R14 discharged on current ground; disposition disclose-and-carry, durable fix = reducer narrowing + state correction inside S11b); **P1 confirmed present** (R15's before-close check). **ADR-014 sequencing consequence:** AE-1's hard pre-condition ("S11a has settled the extraction regime") moves to **S11b** — S11a diagnosed but did not settle; the regime version-mark + the item-5 bare-tool-payload trigger ride S11b's one-time settlement (dated notes on both prompts; the item-5 fire-rate diagnostic was superseded by direct source reading — the trigger enters S11b as a remedy component). **P1's 124/125 figures re-attributed** to the briefing's 125-record snapshot (review fold — they are not derivable from the frozen 130; the frozen-130 re-derivation added: 129 do-not-proceed / 1 pause vs 130 proceed). **Adversarial review (Workflow `wf_44d7a2ed-b8b`):** 5/6 finder dimensions completed (~2.23M tokens); the 6th (cap-review-verification) + 13 of 14 verifiers died on the account monthly spend limit → **completed FIRST-HAND per the §4 precedent** — the re-latch chain verified at source (route:803 emission → :200 unevaluated → trust-transition:196 latch; content-hash idempotency ⇒ new assessments emit new events); 14 findings adjudicated: 1 HIGH (S11b sensitive-path egress control — FOLDED into S11b Part 1), 4 MEDIUM (AE-1 collision pre-folded independently mid-session; buffer counter-evidence + validation-vacuity + dedup consequence — all FOLDED), rest low/nit folded or discharged-by-design. No code/schema/flag/DB change; the frozen buffer untouched; `agent_hold_observations` still empty. Successor: `operations/handoffs/founder/2026-07-18-trust-layer-S11b-examined-input-recomposition-NEXT-SESSION-PROMPT.md`.
- **2026-07-17 (same day, post-incident)** — **D1's ground QUERIED, not inferred, and narrowed:** the public `justice_capped` rests on **ONE** `justice-surface-unevaluated` event (S9 install, `2026-07-11 05:45:29.674+00`, artifact `signed:substrate-layer2-2026Q2`) — **not** a stream from the 130 observation records, because at-action consults never become trust events (emission fires only on accreditation writes). **S11a's cap review is one event, not a stream.** Added **§E** (credential generations) after the public-credential-exposure incident: gen-1 revoked, gen-2 live, identity deliberately preserved.

*Append entries with a date + the binding source. If an item is discharged, mark it and say where — do not delete it; the point of this file is that nothing here gets rediscovered from scratch.*
