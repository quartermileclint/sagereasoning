# S11 FLIP PREREQUISITES — the standing open-questions register

> **STATUS: STANDING. The session that reconsiders the S11 ENFORCE flip MUST read this file in full at open.**
>
> **Created 2026-07-17** under `D-TRUST-LAYER-S11-F2-MENTOR-RULING-EXCLUSION-CLAUSE-GOVERNS-ADOPTED`, on the mentor's instruction (**R15**, binding):
>
> > *"Which input the flip would use — the at-action verdict or the accumulated trust state — was named as unresolved. It does not need to be resolved in the narrowing session, but **it needs to be added to the open questions register before the session closes.** The at-action verdict and the trust state currently produce different recommendations on this action class. That divergence will matter when the flip is reconsidered, and **it should not be discovered again from scratch at that point.**"*
>
> No standalone register existed — the `fix_before_s10` list is buried in an audit report's §4 and the decision log is ~14,500 lines, which is exactly the "discovered again from scratch" risk the instruction names. **This file is that register.** It is short by design. **Append; do not let it sprawl.**

**Binding specifications this register serves (verbatim wins over every line below):**
- `operations/trust-layer-2026-07/2026-07-12-mentor-consultation-s11-enforce-gate-verdict-verbatim.md` — the deferral + the four-part readiness standard + the flip's binding shape
- `operations/trust-layer-2026-07/2026-07-17-mentor-consultation-F2-exclusion-clause-verdict-verbatim.md` — the exclusion-clause ruling (§1) + the supplementary ruling (§1b)

**Standing state:** **ENFORCE remains S11, readiness-gated. The readiness standard is NOT met (mentor, 2026-07-17). The flip does not proceed. Weights BLOCKED. The 0h call remains the founder's.**

---

## A. Hard prerequisites — the flip cannot be reconsidered until each is discharged

| # | Prerequisite | Source | Status |
|---|---|---|---|
| **P1** | **The §9 input question: what does the decision table actually read when it fires?** The **at-action verdict** and the **accumulated trust state** currently produce **different recommendations on the same action class** — the F2 briefing's §9 reconstruction over its **125-record snapshot** *(attribution corrected 2026-07-18 — these figures are the briefing-era snapshot's, NOT derivable from the frozen 130; the same computation over the frozen 130 reads **129 `do-not-proceed` / 1 pause** vs **130 `proceed`**)*: the at-action verdict's justice surface (Arm 1's reading) ⇒ **124 `do-not-proceed`** (+1 pause); `justiceSurface:'none'` (the §4 reading) ⇒ **125 `proceed`**; while the **live** harness advisory reads the trust state and reports **`proceed/log`** — three readings. The mentor: *"That divergence is not a minor implementation detail. It is the question of what the decision table is actually reading when it fires."* | 2026-07-17 §1 (R8) + §1b (R15) | **RULED + DISCHARGED 2026-09-04** — `2026-09-04-mentor-ruling-P1-decision-table-input-verbatim.md` (BINDING; verbatim wins). **Ruled: the AT-ACTION verdict is the table's input for the per-action rows, FILTERED by Q3's kathekon-engagement threshold before any justice surface is reported.** A verdict meeting no kathekon condition reports `justiceSurface: 'none'`, NOT `'unevaluated'` — so **Q2's zero-false-positive floor is RESTORED and its staging premise STANDS**; the 129/130 figure is the unfiltered composition Q3 exists to forbid. **The register's "three readings" framing is superseded** (scope doc Finding A, mentor-accepted): there was one wired path + two RECONSTRUCTIONS — `recommendIntervention` has exactly ONE live caller (`readTrustVerdict`, `harness-integration.ts:563`). **Reading 3 was never a distinct third reading** (Finding B → new item **D5**). The AGGREGATE is NOT the per-action input — its consumer is Q7 depth calibration, undisturbed. `interventionInputFromS3` is not wrong, it is wired to the wrong consumer for enforce; a sibling `interventionInputFromAtAction` serves that path, reusing the ALREADY-IMPLEMENTED `assessKathekonEngagement` (no new predicate). **Ruling licenses NO build/schema/activation/publication and does NOT move P4/P5/P6 or the flip.** |
| **P2** | **The Arm-1 narrowing must land.** *"Arm 1 requires at least one identified circle."* → **FURTHER narrowed 2026-07-19: ≥1 circle BEYOND self_preservation.** | 2026-07-17 §1 (R1) + §1b (R11); **2026-07-19 self-circle ruling** | **PREDICATE LANDED IN-REPO (2×).** S11b (2026-07-18): the ≥1-circle rule, predicate + reducer in tandem. **2026-07-19 self-circle narrowing:** the PREDICATE (`kathekon-engagement.ts`) now requires `beyondSelfCircleCount ≥ 1` (circle name ≠ `self_preservation`; unknown-identity circles never satisfy it; Arms 2–4 unchanged). Batteries: kathekon **105/0** (§8 both directions + the R13 `selfCircleExclusion` bound), loop-fold **172/0** (§19 the v2 three-way split). The frozen-buffer dry-run under the self-circle-narrowed predicate reads **strict 129 FP / 0 CH** (legacy bracket 128 FP / 1 CH — the one former CH is a v1 record with no captured circle identity; evidence at `runs/2026-07-19/`), NOT a readiness claim — the old lean composition, one action class, no denominator. **⚠ THE REDUCER HALF IS NOT SELF-CIRCLE-NARROWED** — `deriveWorstJusticeOutcome` reads no circle identity, so the LIVE trust ledger still emits `justice-surface-unevaluated`/`indeterminate` for self-only circles. **That correction is a SEPARATE `code-critical` founder-walked step (D3; new register item D4)** — the predicate is now deliberately narrower than the reducer on self-only inputs (pinned §8.9). |
| **P3** | **The extraction question must be settled** — is Layer 1 identifying affected parties where they are genuinely present? **This GATES the narrowing:** *"If the extraction is known to be reliable on the action classes in scope, narrow the arm. **If the extraction is uncertain, the extraction question is prior.**"* Two hypotheses with **opposite** remedies: **starved** (should find parties, doesn't) vs **mis-sited** (a tool-call payload genuinely has no party/role/purpose — the project already acted on this reading once, dropping Bash from the consult). | 2026-07-17 §1b (R12) | **ANSWERED AT DIAGNOSIS LEVEL (S11a, 2026-07-18 — Diagnostic-certain; `2026-07-18-S11a-extraction-gate-diagnosis.md`): STARVED BY COMPOSITION at the harness site**, resolving the starved-vs-mis-sited fork: `describeAction` (at-action-hook.mjs:280-321) sends a `Write` as path + char count — **content discarded entirely** (64/64 buffer records match) — and an `Edit` as the first 200 chars of `new_string`; the consult POSTs that one line. NOT a Layer-1 defect (the prompt is faithful; circles surface when the composed text carries parties — U2/§4 smokes + the in-scope idx-4 record) and NOT mis-sited ("the payload has no party" is factually wrong for `Write` — the payload HAS content; the hook drops it). **Remedy = the S11b examined-input recomposition** (work item authored 2026-07-18). ~~The extraction remains UN-FIXED until S11b lands~~ → **REMEDIED IN-REPO + LIVE IN THE FOUNDER LOOP (S11b, 2026-07-18; hooks hot-load from the working tree):** `action-composer.mjs` — narrated intent (transcript tail, frame-stripped, redacted) + bounded payload (Edit both sides; Write head+tail), the MANDATORY append-only sensitive-path denylist + token redaction (elections E1/E2), payload-content-hash dedup (E3), `GATE1_ACTION_TEXT_MODE` knob default composed (E4), regime version-marks settled once (`at-action-v2-composed`/`at-action-v1-lean`, ADR-014) + the item-5 T2-soft bare-payload trigger. **Validation (real composer + real L2 chain, N=3, pre-stated thresholds — r2):** party-affecting **9/9 runs with circles** (the starvation closed); party-less clean; **mention-without-affect FAILED 6/6 — Layer 1 converts QUOTED party language into circles** (recorded, not tuned away — `NARROWED_ARM_BOUNDS.mentionConversion`; **the Layer-1 re-check is a NAMED FOLLOW-UP, its own Critical step**); noise 0/3. Composed extraction latency 13–20s (two live 28s consult timeouts observed — fail-open-honest; the lean knob + budget trims are the mitigations). The residual mis-siting question was resolved INSIDE the composition (intent first, payload second). |
| **P4** | **The readiness standard's four parts** — (1) ≥7 days over a **representative** distribution; (2) all four cardinal domains evaluated, confidence above conservative on ≥2; (3) a measured false-hold **rate**; (4) Q3 encoded. | 2026-07-12 verdict (Q1) | **NOT MET.** (1) fails — the 2026-07-12→17 window was **one tool class, one depth, one proximity**. (2) fails — **one** evaluated cardinal domain (`confidence_weight: 0.42`). (3) **unmeasured** — see P5. (4) satisfied, but the predicate it encodes is now narrowing. |
| **P5** | **Part (3)'s denominator has no source.** *"Part (3) as specified may be unmeasurable on this capture set regardless of the ruling. The genuinely dangerous actions are on the guard path, which writes no record."* The capture is CONSULT-path only (`at-action-hook.mjs:595`, inside `runConsult`); `runGuard` writes nothing; Bash is not consulted. So *"correct holds on genuinely problematic actions"* — the denominator of the mentor's ratio — cannot be populated. **⚠ RECORD CORRECTION 2026-09-05 (mentor-directed; `2026-09-05-mentor-ruling-P6-window-recommendation-verbatim.md`): the sentences above are STALE, not wrong-at-the-time.** `runGuard` HAS written records since **commit `3e8f231`, 2026-08-17 ("R2b item 8" — P8a)**: `buildGuardHoldRecord` (schema **v4**, `path: "guard"`, `guardHold` [deny only — a caution allows the tool and would make the denominators incommensurable], `guardOutcome`, `captureBasis` distinguishing a classifiable assessment from an outage with none) is wired at three call sites in `at-action-hook.mjs` behind the same `GATE1_FALSE_HOLD_CAPTURE` flag, with its own PR19 fold and two separately-pinned safety properties (fail-soft capture; fail-safe placement AFTER the deny emit). **Read the Status cell, not the Item cell.** The stale text was inherited by `2026-09-04-C-at-action-seam-caller-SCOPE.md` §4 before being caught. | 2026-07-17 §1 (R6) | **OPEN — status UNCHANGED, and the correction above does not close it.** The mentor: *"P8a's build discharges P5's blocking claim at the code level. The register correction is owed independently"*, and *"P8a's build does not discharge P5 — activation is open."* **A built-dark denominator is not a measured one:** `GATE1_FALSE_HOLD_CAPTURE` is unset, the clock is stopped, and no guard record has ever been written. Any re-measurement design must still solve it — by activating P8a, not by rebuilding it. **📋 S4, 2026-09-06 (window-start-readiness session): status re-confirmed unchanged, not re-solved.** `grep -c GATE1_FALSE_HOLD_CAPTURE .claude/settings.local.json` = 0. P8a's activation was NOT performed this session — the founder act is gated on F-3′'s threshold being elected AND B4's own follow-up rate, neither of which is available yet (see B4 and F-3′ annotations below). A companion finding, offered as a question rather than acted on: the guard-DENY population — the only class satisfying `guardHold: true`, hence the sole source of a populated P5 denominator once P8a activates — is measured at 15 events across 57 calendar days (0.089/day excluding one outlier day), i.e. even once activated the denominator this row exists to populate will likely run ~0.6 holds per week, not zero, but also not many. See `2026-09-06-F3prime-guard-availability-threshold-PROPOSAL.md` §6 for the full figures. **📋 2026-09-06 (F-3′ ruling): unchanged as an item, but its blocking claim is now fully dissolved.** Part (3) is a within-consult measure (2026-09-06 part-(3) ruling), so P5's guard denominator is a separately-reported disclosure rather than the thing part (3) counts; P8a's activation is the window start itself and is now licensed. **P5 discharges when the flag is set** — the built-dark denominator becomes a measured one at that moment. **✅ DISCHARGED 2026-09-06T09:44:55Z — the denominator is now MEASURED, not built-dark.** `GATE1_FALSE_HOLD_CAPTURE=true` set in the founder's local harness config (NOT production — no route reads it); took-effect proven by the buffer moving 138 → 139 with the **first `false-hold-record-v4` in its history**, `path: "guard"`, `captureBasis: "assessment"`. *"A built-dark denominator is not a measured one"* — it is one now. `D-FALSE-HOLD-OBSERVATION-WINDOW-STARTED-LIVE-2026-09-06`. |
| **P6** | **A NEW observation window** — the 2026-07-12→17 buffer **cannot** be reused for part (3): it predates the narrowing, is one action class, and has no denominator. A re-measurement needs: the narrowed predicate + a **representative** distribution + a populated denominator (P5). The clock was **stopped 2026-07-17**; the buffer is frozen as evidence. | 2026-07-17 §4 (election 2) | **SCOPED 2026-08-15** (concurrent-arc C1): `operations/trust-layer-2026-07/2026-08-15-false-hold-new-window-scoping-note.md` — v3/composed-regime records only (never mix regimes); the narrowed predicate frozen as the measured classifier; **P8a guard-path capture is a hard precondition** (solves P5's denominator); coverage/loss accounting (timeouts + transient 401s counted); bounds printed on the rate; durable `GATE1_STATE_DIR`; **window starts only as R4's LAST step, after every guard-bundle activation** (a window spanning instrument edits measures neither state; the M1 window-conditional guard re-arms at start by design). Build = R2 item 8; activation + start = R4. *Prior status inputs (S11b regime, mention-conversion bound, latency bias) are folded into the note.* **AMENDED 2026-09-05 by binding ruling** (`2026-09-05-mentor-ruling-P6-window-recommendation-verbatim.md`; scope: `2026-09-05-P6-window-recommendation-SCOPE-FOR-RULING.md`): **the window's stated purpose WIDENS to cover the decision table's RECOMMENDATION, not only hold classification** — part (3) names a false-hold rate and *"under G6(a), a hold is what a do-not-proceed produces"*, so the widening is *"a correction of the original scoping to what part (3) actually names."* **Derived at REPORT TIME, never stored in the capture record** (the table is under active ruling — P1 moved its input mid-arc — so a stored reading would freeze stale evidence that looks authoritative); the record shape is unchanged and `recordHash` is untouched. **Consequence: measuring it needs NO capture-layer edit, so it carries NO contamination exposure and NO R2 deadline** — the scope withdrew its own deadline claim and the ruling accepts the withdrawal. Consult and guard populations reported **separately** (not optional). **Three bounds PRINTED ON THE RATE, not footnoted:** the A8 bound (no re-examination counter exists anywhere in the harness ⇒ `habitualReExaminationCount` floors to 0 at `intervention-engine.ts:392` ⇒ the two-then-escalate row can NEVER fire), the guard-record `depth: ""` bound, and an as-of-table disclosure (the derivation reflects TODAY's table, not the table at capture time). **A v3/v4 lift check runs before any figure is published** (the existing non-vacuous round-trip is proven on 130 **v1** records only) — a precondition of publication, not of the ruling. **Licenses no build, activation, or publication; P6 is NOT discharged, not scoped further, and the window has not started.** Note added at the amendment: §2.1 of the note says v3-only, but P8a emits **v4** guard records — the report's multi-version whitelist covers both, and `path` is the population discriminator. **AMENDED 2026-09-05 (F-3′) — a SECOND window precondition: a bounded guard-availability rate.** Binding ruling `2026-09-05-mentor-ruling-part3-structural-unfailability-verbatim.md` **Q3**: *"A bounded guard-outage rate is a further precondition on the window, in the same class as P8a. The window's preconditions are amended to include it."* Routed here by `agent-circles-2026-08/2026-09-05-mentor-ruling-guard-availability-and-lean-mode-doctrine-verbatim.md` **Q-G1(c)**, which makes the availability covariate *"not optional for honest reporting"* — a window run at a degraded presence rate captures a sample whose selection mechanism is **not neutral**. **The measurement, its mechanism, the remedy applied 2026-09-05 and the follow-up that remedy owes all live in bound `B4` (Section B) and are deliberately NOT restated here.** **B4 does not move and is not re-filed as a Section-A gate:** it is explicitly *"NOT a new gate and does not reopen P4's four-part standard"*, and a window precondition — like P8a — is not a fifth part of that standard. **The threshold is NOT set here** (Q3: *"The threshold is a P6 design question"*) — it is a fourth input to this row's design work, joining the three printed bounds above. **Status: OPEN.** B4's follow-up measurement is owed and not run, so the precondition cannot be discharged from the pre-remedy figure; the honest baseline to carry forward is **11–32% on ordinary days, with 60% on 2026-09-04 as a named outlier** (an earlier restatement said "20–60% baseline"; the mentor corrected it and the correction is carried). Landed in the scoping note at §2.4 and §3. **📋 S4, 2026-09-06 (window-start-readiness session): a threshold is now PROPOSED, not elected.** `2026-09-06-F3prime-guard-availability-threshold-PROPOSAL.md` proposes ≤5% aggregate AND ≤10% any single ordinary day (≥20 guard attempts), sustained over ≥3 successive ordinary days — PR19-reviewed, 1 HIGH + 2 MEDIUM confirmed and fixed in place (a partial-day 60% figure wrongly paired with a full-day 202-attempt count, corrected to the day's actual 25.25% close; the lean-mode ruling overstated as 'closed' rather than 'held'; an unsourced concurrency figure removed). Independently re-verified: of 28 windows of three successive ordinary days findable anywhere in the 57-day log, ZERO pass both proposed rules — the threshold binds on this log's history, genuinely, but the known post-remedy figures (≈0.5–1.2%) sit roughly an order of magnitude below 5%, so it is expected to pass once B4's real measurement exists. **Still OPEN: the founder has not elected the number.** **✅ F-3′ THRESHOLD ELECTED 2026-09-06 (founder), as proposed and unmodified: aggregate guard-outage rate ≤5%, no single ordinary day above 10%, ordinary day = ≥20 guard attempts, over ≥3 successive ordinary days.** Elected BEFORE B4's measurement exists (not due until ≥2026-09-08 UTC) — a genuine pre-registration, with the disclosed qualification that the known post-remedy figures sit ~10× below it and it is therefore expected to pass. **This discharges only the THRESHOLD half of this precondition. The measured rate itself remains owed (B4), so the precondition as a whole is still OPEN and the window remains closed.** Source + full derivation: `2026-09-06-F3prime-guard-availability-threshold-PROPOSAL.md` (now marked ELECTED at its head). **A companion question is drafted and awaiting relay — whether part (3) is measurable at all on the guard denominator P8a will supply** (`2026-09-06-mentor-question-part3-guard-denominator-FOR-RULING.md`): the guard-deny class runs ≈0.6/week excluding one outlier, all `Bash`/`reflexive`/`depth:""`, which is part (1)'s own recorded failure mode reproduced on the guard side. Nothing in that question reopens the D2 sequencing or this row's ruling. **✅ RULED 2026-09-06 — PART (3) IS A WITHIN-CONSULT-POPULATION MEASURE; the guard population is a separately-reported DISCLOSURE, not a denominator.** Verbatim: `2026-09-06-mentor-ruling-part3-guard-denominator-verbatim.md` (`D-MENTOR-RULING-PART3-GUARD-DENOMINATOR-ADOPTED-2026-09-06`); verbatim wins over this cell. Q1's three grounds: the P6 amendment's own separation requirement forbids a denominator mixing two measurement conditions, and an advisory-hold-to-enforced-deny ratio is exactly that; the two events mean different things at any volume; and the 2026-07-17 blocker was about the guard path being STRUCTURALLY ABSENT, which P8a closes — satisfying its intent does not require the guard side to be part (3)'s denominator. **Q2 dissolved** (thinness becomes a disclosure). **Q3 No** — part (1)'s representativeness applies to the population part (3) measures; the guard population's narrowness is a property of the irreversible-action allowlist, not a sampling failure, and *"a longer window would not broaden it."* **Sequencing: D2 holds unchanged, but the guard population is REMOVED from the window's duration constraint** — the window runs on consult terms (130 records in 5 days). The ruling closes: *"Start the clock. Report the guard side honestly as thin."* **⚠ THE RULING DOES NOT NAME F-3′, B4, OR GUARD AVAILABILITY** — it answers guard DENSITY (how many denies exist), a different quantity from guard AVAILABILITY (what fraction of attempts complete). **F-3′ therefore STANDS AS WRITTEN until the founder elects otherwise or the mentor discharges it**, notwithstanding the "start the clock" line; the coherent reading — that F-3′'s not-a-random-sample ground lapses once the guard population stops being a denominator, leaving Q-G1(c)'s covariate requirement attached to the DISCLOSURE's honesty rather than to a gate — is recorded in the decision-log entry as a reading, NOT acted on. **The window's report must now carry the guard population as an explicit separate disclosure with its narrowness named as structural.** **✅✅ F-3′ DISCHARGED AS A GATE 2026-09-06; A CONSULT-SIDE BOUND REPLACES IT AT PUBLICATION.** Verbatim: `2026-09-06-mentor-ruling-F3prime-discharged-consult-bound-verbatim.md` (`D-MENTOR-RULING-F3PRIME-DISCHARGED-CONSULT-BOUND-ADOPTED-2026-09-06`); verbatim wins. **Q1:** F-3′'s ground was stated in denominator terms and the part-(3) ruling removed the guard population from part (3)'s denominator — *"a precondition whose rationale has been dissolved by a subsequent ruling does not survive as a formality."* The honesty requirement MIGRATES to the guard disclosure (report the guard population separately, with its availability rate). **Q2: YES — a CONSULT-side availability bound is owed**, on F-3′'s own reasoning, and more urgently: guard 31.7% vs **consult 70.3%** outage over the full log span; *"a measurement whose denominator is losing 70% of its attempts to outage is not measuring what it claims to measure"*; the scoping note's counting requirement is *"necessary but not sufficient — counting the losses tells you the rate. It does not bound it."* **Q3: NO, the window does NOT wait** — the bound is a condition on **PUBLICATION**, not on start; requiring it first *"is not a precondition. It is a circularity."* A miss is **published as a qualification on validity, not suppressed.** **THRESHOLD NOT SET, and deliberately NOT F-3′'s numbers** (≤5%/≤10%/≥20 was calibrated for the guard path; reusing it *"would defer the window indefinitely"*) — **it is set after the first FIVE ordinary post-remedy days of window data.** **⇒ BOTH WINDOW PRECONDITIONS ARE RESOLVED; the founder may set `GATE1_FALSE_HOLD_CAPTURE`.** New standing obligations at window start: count consult availability from day one; report the guard population separately with its rate; assess the consult bound before publication. **✅ THE WINDOW IS RUNNING as of 2026-09-06T09:44:55Z** (`D-FALSE-HOLD-OBSERVATION-WINDOW-STARTED-LIVE-2026-09-06`). Both preconditions resolved (P8a = this act; F-3′ discharged as a gate). **Record 139 is a deliberate took-effect probe — EXCLUDE it from the rate.** The 138 `v1` records are a different regime and never enter it; the frozen 130 remains an exact prefix. **Never refresh the buffer.** Now running: consult-availability counting from day one; the guard population reported separately with its rate; the consult-side threshold set after five ordinary post-remedy days; **publication waits on that, the start did not.** **The byte-identity guard is ARMED** — no file matching its `GUARD_RE` may sit modified in the working tree, which reaches this note's own filename and blocks S11-D2 until resolved. **📊 APPENDED 2026-09-06 (session S5b, `D-S5B-WINDOW-HEALTH-VERIFIED-D2-GUARD-COLLISION-SCOPED-FOR-RULING-2026-09-06`) — WINDOW HEALTH VERIFIED AT ~100 MIN. NOT A REWRITE; the cell above stands as written.** **The window IS capturing on BOTH paths.** At `11:22:53Z`: buffer 152 lines = 138 `v1` (excluded, different regime) + 14 `v4`-era; **13 organic** (record 139 excluded as S4's took-effect probe) = **10 guard** (`v4`, `Bash`, depth `""`) + **3 consult** (`v3`, `Edit`, depth `standard`, `loopEvent` opened/reopened, `carriedPrior:true` ×2). **The window-close representativeness condition (§2.6/§5 — "more than one tool class/depth") is MET ON ITS FACE**; whether 3 consult records are *representative* under part (1) is unanswered. **`GATE1_STATE_DIR` unchanged; buffer never refreshed/truncated.** **The consult population populated only when the session switched from Bash heredocs to the `Edit` tool** — an accident of the PR19 fold, but a clean natural experiment confirming from behaviour what was first established from source: the guard is checked first and returns, and non-guard `Bash` is dropped from the consult floor (`GATE1_CONSULT_BASH` unset), so only `Write`/`Edit`/`MultiEdit`/`NotebookEdit` reach `runConsult`. **An earlier zero-consult observation was correctly recorded as a NULL RESULT, not a finding** — the v1 window ran at the same 12.9:1 Bash dominance and still produced 138 records (28.10 consults/day ⇒ over 20 min λ≈0.396, P(0)≈0.673). **AN UNSOUGHT FINDING WITH POSSIBLE BEARING ON PART (3): 6 of 13 organic records carry the live D2 tagging defect** (`circles=[]` + non-null `kathekon` ⇒ `dikaiosyne` tagged), **and all 13 tag `dikaiosyne`** — but the split is **6 of 10 guard, 0 of 3 consult**, so **the defect currently sits OUTSIDE part (3)'s within-consult denominator**. n=3 consult — evidence for the ruling, explicitly NOT a conclusion. **CONSULT AVAILABILITY (F-3′, counted from day one): the report script CANNOT measure it** (an outage writes no record); the data accrues in `gate1.log`. **Instrument NAMED not built.** Calibration only: all-time loss **70.3%** (1,375/4,636 — reproduces the mentor's figure exactly), but **must not be quoted forward** — 1,918/1,921 timeouts are at the pre-remedy 28,000 ms default; under the current 55 s regime the loss rate is **25.2%** (35/139). **The consult-side bound threshold is NOT set** (owed after five ordinary post-remedy days). **⚠ Frame discipline: the mentor's documents state LOSS, not availability — reporting availability inverts every comparison.** **A check that came back CLEAN:** the scoping note's "STRICT on v3" text is stale; the report script correctly accepts `v1|v2|v3|v4` and sums v3+v4. **B4's post-remedy guard rate was NOT computed** (≥`2026-09-08 UTC` gate stands, not pre-empted). **D2/guard-collision scope doc authored for relay** (`2026-09-06-D2-window-sequencing-guard-collision-SCOPE-FOR-RULING.md`); **S11-D2 remains BLOCKED pending that ruling.** PR19 independent review: **GO_WITH_FIX, no CRITICAL**, 1 HIGH + 2 MEDIUM + 2 LOW all folded. |

## B. Named bounds that must be stated ON the enforcement claim at the flip

Per the 2026-07-12 verdict (Q4/Q5) — these do **not** block the flip; they must be **published as bounds on what the enforcement claim covers**, not merely carried as register items:

| # | Bound |
|---|---|
| **B1** | **PA-5** — the direct reflect path's mintability. The enforcement claim covers trust levels produced by the **harnessed** path; it does **not** cover levels modulated by the direct reflect path. Disclosure upgraded from carried-register item to a **named bound on the enforcement surface** at the flip. |
| **B2** | **PA-10** — stale-artifact replay (carry-with-disclosure; over-trust is the safer error direction; the A5 recency tier is the shaped closure at the S2 fold). |
| **B3** | **The A2 decrease-dodge — declared IRREDUCIBLE.** *"The enforcement binds on what the examination can find. It does not bind on what the examination cannot find because it was not narrated."* Not closeable by any text-based fix; **state the bound accurately.** |
| **B4** | **GUARD AVAILABILITY — the presence rate, orthogonal to the false-hold rate.** **Added 2026-09-05 by binding ruling** (`agent-circles-2026-08/2026-09-05-mentor-ruling-guard-availability-and-lean-mode-doctrine-verbatim.md`, Q-G1(a); evidence: `operations/harness-2026-09/2026-09-04-gate2-guard-outage-diagnosis.md`). **Measured from the live `gate1.log`: the guard is unavailable on 11–32% of attempts on ordinary days and was absent on 60% of 65 attempts on 2026-09-04.** Every outage reads `timeout after 28000ms` (no 429s — latency, not throttling); the 28,000 ms budget (`framing-core.mjs:100`) sits under 2× headroom over `/api/guardrail`'s measured 14.5–19.1 s. **Concurrency does not explain it** (2026-08-30: 17 sessions, 32%; 2026-09-04: 9 sessions, 60%). It fails **open** with an honest log — correct and designed (KG1) — but that describes the failure MODE, not its FREQUENCY, which was recorded nowhere until this measurement. **The mentor, verbatim:** *"The false-hold rate and the presence rate are orthogonal quantities, and a flip that satisfies part (3) while leaving the presence rate unmeasured would be making a claim the evidence does not support"*; a channel absent that often *"is not failing its examination. It is not conducting one"* — *"intermittent caution, which is a different thing"* from the eulabeia the guard exists to enact. **⚠ THIS IS NOT A NEW GATE AND DOES NOT REOPEN P4's four-part standard** — the ruling is explicit: *"The observation is that the standard is incomplete with respect to availability, and that incompleteness should be named explicitly before any flip is considered."* Hence Section B, not Section A. **The remedy is F1's timeout raise (hook 30→60 s AND `GATE1_TIMEOUT_MS`→~55000 ms TOGETHER — raising only the inner budget converts a logged fail-open into a killed hook, losing the honest log line); it is the founder's and is NOT applied.** **Lean mode is NOT the remedy and is ruled doctrinal** — see the same ruling's Q-G1(b): *"A shorter impression is a different impression"*, and a lean examination that misses what would trigger a hold *"is failing to see what it would need to see to hold — a different failure mode from a false hold, and not measured by the false-hold rate."* **⚙ REMEDY APPLIED 2026-09-05 (founder-directed; local config only — `.claude/settings.local.json`, gitignored, backed up first, credentials untouched). `GATE1_TIMEOUT_MS` set to **55000** (it was UNSET, so it had been falling back to the 28,000 ms default) and **all four 30 s hook timeouts raised to 60 s** (`Task|Agent` was already 120). **⚠ A CORRECTION TO F1's OWN WORDING, load-bearing and found at source before applying:** F1 says *"hook timeout to 60s"* — **singular**. But `GATE1_TIMEOUT_MS` is a **single global env var**, and all FIVE hooks (`at-action`, `close`, `framing`, `subagent-framing`, `handback`) load the shared config and feed it to `AbortSignal.timeout()` on every fetch path (`framing-core.mjs:100,553,595`). **Raising only the guard hook would have left `UserPromptSubmit`, `PostToolUse` and `Stop` killed at 30 s before the 55 s abort could fire — relocating the killed-hook trap the ruling warned about to four other hooks rather than removing it.** All were raised. **Invariant now checked and holding on every hook: hook timeout > inner budget** (5 s margin on four, 65 s on `Task|Agent`). **Verified, not asserted:** Bash is dropped from the auto-consult, so the guard was fired deliberately with a harmless `/tmp` overwrite-redirect (a shape its classifier catches) and returned a real `GUARD-CAUTION` verdict in **~6 s** rather than timing out; **zero timeouts and no log line reporting the old 28,000 ms budget after the change** (before, same day: 684 outages against 1,237 completed guard calls). **⚠ THIS DOES NOT RETIRE B4.** The bound records that the **presence rate is orthogonal to the false-hold rate and was never measured**; a remedy that improves the rate does not discharge the disclosure, and the ruling requires the incompleteness be *"named explicitly before any flip is considered"*. B4 stands until the follow-up below is run and recorded. **📋 FOLLOW-UP MEASUREMENT — OWED, NOT DONE.** One successful call is not a rate: the chronic 11–32% was measured across six days, and the endpoint genuinely takes 14.5–19.1 s, so the raise buys headroom, not speed. **After ≥3 days of ordinary traffic, re-run the per-day counts and record the post-remedy rate in this cell beside the pre-remedy one** — method unchanged so the two are comparable: `LOG=~/.sage-gate1/gate1.log; grep "^<date>" $LOG | grep -o "GUARD-[A-Z]*" | sort | uniq -c`, plus the reasons via `grep -o 'reason="[^"]*"'`. **Three things the follow-up must report, not just the headline:** (1) the outage rate per day against the 11–32% baseline; (2) whether any residual outage now reports **55000ms** (endpoint genuinely slower than the new budget) versus a **killed hook** with no log line at all (the invariant broken somewhere) — these are different failures and only the first is visible in the log; (3) the distinct-session count per day, since concurrency was ruled out as the cause and that should remain true. **Until that measurement exists, the honest statement on any flip claim is that the presence rate was 11–32% when last measured, a remedy has been applied, and its effect is unquantified.** **📋 S4, 2026-09-06 (window-start-readiness session): NOT run — the date gate held.** `date -u` at open was 2026-09-06T04:11:54Z; the paste's gate requires ≥2026-09-08 UTC. No post-remedy rate computed this session (confirmed by review of the session's own records); the only post-remedy figures touched are isolated counts, each disclaimed as not a rate. A companion observation, verified but not requested by B4's own follow-up spec: of the four total lines in the whole log reporting the new `55000ms` budget, exactly ONE is a `GUARD-OUTAGE` (the rest are `CONSULT-OUTAGE`) — consistent with, not a substitute for, the opener's 83/1 early indication. **The actual follow-up measurement remains owed and is NOT this.** **📋 2026-09-06 (F-3′ ruling): B4's follow-up measurement is NO LONGER A GATE ON THE WINDOW, and is still owed as a DISCLOSURE.** F-3′ is discharged as a precondition, but the honesty requirement it carried migrates to the guard disclosure — the guard population is reported separately *with its availability rate disclosed*, which is exactly what this row's owed measurement supplies. The ≥2026-09-08 UTC timing discipline still applies to that measurement; it simply no longer blocks the clock. **A second post-remedy guard outage was observed 2026-09-06T08:03:20Z** (`timeout after 55000ms`, fail-open, session `d99ff2f9…`) — the visible failure mode, invariant holding; **an instance, explicitly NOT a rate.** |

## C. The flip's binding shape (when it is finally reconsidered)

Per the 2026-07-12 verdict — **do not re-derive this; it is settled**:
- **STAGED, not whole-table:** the **do-not-proceed class first**, then the pause rows + the G6 loop bound after a live calibration window. Faithful **iff** disclosed-as-partial with a committed completion target.
  - ⚠ **2026-07-17 rider (R5):** the staging rested on *"a benign action that engages no kathekon factors cannot trigger a do-not-proceed row — a structural false-positive floor of zero."* **The exclusion-clause ruling PRESERVES that premise** — but it is now premise-dependent: had Arm 1 governed, **124/125** would have routed do-not-proceed. **The staging is safe only if the narrowing (P2) lands.**
- **G6(a) QUALIFIED** by the kathekon-engagement threshold; a "contrary; no kathekon factors detected" verdict ⇒ log-and-continue + a developmental flag + a recorded false-positive instance, **never** a do-not-proceed. **G6(b) inherits.**
- **The calling-gate enforce arm flips WITH the engine** (one coherent regime; the gate is the structural precondition for a non-circular examination).
- **Aggregate-keyed conservative depth binds at v1**; the per-domain `deliberate⇒quick` carve-out is gated on the Q5 (A5 recency-tier) closures.
- **The assent is re-confirmed at flip time (PR7).**

## D. Live consequences of the same root (not flip-gating, but open)

| # | Item | Status |
|---|---|---|
| **D1** | **The public `justice_capped: true` on `sagereasoning:s9-loop@v1`.** **GROUND NOW QUERIED (2026-07-17, was "inferred") — and it is narrower than first recorded:** the ledger holds **exactly TWO** examination-derived events, both at the **S9 install, `2026-07-11 05:45:29.674+00`**, artifact `signed:substrate-layer2-2026Q2` — `credential-completed` + **one** `justice-surface-unevaluated`. **The cap rests on that single event, NOT on a stream from the 130 observation records** — the at-action consults never became trust events (emission fires only on accreditation writes, `emitAccreditationTrustEvents` gated on `provenanceEnforced`). Every other ledger row is a reflect event (`virtue_domain: null`, `artifact_ref: reflect:reflect-<session-uuid>`) — modulate-only, cannot latch. **Consequence for S11a: the cap review is ONE event to examine, not a stream.** The clearing bound is unchanged — clearing needs `justice-surface-transparently-handled` ⇒ a circle carrying `status:'met'` ⇒ impossible on this action class. *"A permanent cap on an agent whose actions had no identified affected parties is a live signal on a live surface."* **S11a closing condition (R14):** *"Do not close the narrowing session with the arm fixed and the cap unreviewed."* | **REVIEWED — S11a, 2026-07-18 (R14 discharged on CURRENT ground; the arm was NOT narrowed — the extraction gate ruled "prior" — and the review says so plainly).** Public surface re-verified first-hand same day (read-only GET): `justice_capped: true`, "justice cap active", 1 evaluated domain, confidence 0.42; gen-2 harness live (`honest_reflect_count: 11`, latest 2026-07-17). **Disposition (founder election): DISCLOSE-AND-CARRY; the durable fix rides S11b.** Deciding argument = stability: with the gen-2 harness writing, a data-only latch clear is UNDONE by the next accreditation close-write (the un-narrowed reducer re-emits `justice-surface-unevaluated` off a zero-circle assessment and re-latches). Durable fix = the reducer narrowing (`deriveWorstJusticeOutcome`, LIVE ⇒ `code-critical` + founder-walked AC7, per D3) + the state correction in the SAME walk — one coherent change inside S11b. The ledger event is append-only historical truth and is NOT deleted. Cap remains the safe direction; no external users; R18 honesty issue, disclosed here + in the S11a diagnosis §5. **CORRECTION PREPARED (S11b, 2026-07-18):** the reducer narrowing is in-repo (battery-pinned: zero-circle no longer derives `justice-surface-unevaluated`; the circle-present J2 class still latches — correctly); the founder walk (`2026-07-18-S11b-part3-founder-walk.md`) deploys it FIRST, then clears `justice_floor_active` by SQL (rollback pre-written in the walk doc; §VERIFY = DB SELECT + public GET `justice_capped:false` + the re-latch watch at the next gen-2 close-write). **Disclosed caveat (first-hand review):** the historical ledger event stays append-only, so a future FULL-LEDGER REPLAY of the materialised state would re-latch this row — any rebuild tooling must re-apply the correction (or a by-hand replay must re-run the walk's §C3). **✅ WALK DISCHARGED (2026-07-18, founder-walked `code-critical` 0c-ii; AC7 + PR17):** commit `b2ae8d5` pushed + Vercel green (the narrowed reducer verified on `origin/main` BEFORE the SQL — the ordering guard held); pre-state SELECT read 1 row latch=true; the rollback statement was saved before the forward UPDATE; post-UPDATE SELECT read `justice_floor_active=false`; **the public GET verified first-hand: `justice_capped: false`**, basis = "minimum-domain rule across 1 evaluated domain(s): dikaiosyne=deliberate; weighted by source confidence (min weight 0.420)" — the cap clause GONE, the earned level correctly retained. **The public signal the mentor called "not a correct public signal" (R7) is corrected, stably.** REMAINING OBSERVATION: the re-latch watch — after the next gen-2 accreditation close-write, re-read the GET (expect still false); record here. **📋 OBSERVED 2026-09-05, at the D4 activation walk — still `false`, with the limit named.** Public read-only GET on `sagereasoning:s9-loop@v1`, read twice ~2 minutes apart across the D4 flag flip: **`justice_capped: false`** both times, `earned_level`/`effective_level` = `deliberate`, `any_justice_capped: false`, `confidence_weight` 0.42, and the `basis` string **byte-identical to the 2026-07-18 post-clear record** — the cap clause still gone, the earned level still retained. The read is demonstrably live rather than cached: `honest_reflect_count` moved **117 → 119** between the two reads (it was 11 at the 2026-07-18 review). **⚠ THE LIMIT, because it is load-bearing and this observation is weaker than the watch condition as written:** the watch asks for a re-read *after the next gen-2 accreditation close-write*, and the public trust record exposes state, not the event ledger — **so this cannot show whether such a write occurred in the interval.** A `false` reading is therefore consistent with two different worlds: the write happened and correctly did not latch, or no qualifying write happened at all. **Discharged only in the weaker sense**; a ledger query (`agent_trust_events` for this agent since 2026-07-18) would settle which, and is not run here. **What DID change on 2026-09-05:** D4 is now live, so the self-only-circle re-latch route this watch exists to catch is closed going forward — but **D4 does not close the full-ledger-replay caveat** above, because the flag gates DERIVATION (`deriveWorstJusticeOutcome`) while a replay folds stored events through `applyTrustEvent`, which never reads it (verified first-hand at the walk). **📋 SETTLED 2026-09-05 (the ledger query the observation above named as not run) — THE WATCH IS OPEN, NOT DISCHARGED.** A grouped, unlimited scan of `agent_trust_events` for this agent since 2026-07-18 returns **exactly four event types, every one `virtue_domain: null`**: `orientation-reading-toward` (1356), `orientation-reading-indeterminate` (786), `reflect-screened-honest` (165), `reflect-completed-honest` (107). **No `credential-completed`. No `justice-surface-*`.** So **no qualifying accreditation close-write has occurred since the 2026-07-18 clear**, and the 2026-09-05 `justice_capped: false` reading is consistent with the correction holding while proving nothing about it — there was nothing for the latch to fire on. **Of the two worlds the observation above named, it is the second.** Revisit after the next genuine gen-2 close-write. **⚠ A METHOD NOTE, because the query as carried would have answered wrongly:** the carried form was `order by occurred_at desc limit 50`; those four types total **2,414 rows**, so it would have returned 50 orientation readings from 2026-09-04 and nothing else — "no accreditation writes appear" would have been the right conclusion by an unsound route, indistinguishable from the limit hiding them. Replaced with a `group by` before running. **A SEPARATE OBSERVATION, outside D1's scope and not diagnosed:** consults and reflects both fire on this credential through 2026-09-04, so credential and harness work — **but no accreditation write has landed in seven weeks.** The H4 close-hook write is either not firing or failing silently. **📌 DIAGNOSIS ALREADY ON RECORD — cross-referenced 2026-09-05 (grounding session); a pointer, not a new finding, and D1's status is unchanged (OPEN).** The 2026-08-30 provenance-ledger C2 finding traced this exact mechanism to a line (`D-PROVENANCE-LEDGER-C2-OBSERVATION-INPUT-UNREACHABLE-FOUND-2026-08-30`): the H4 close hook sends exactly one body kind — `close-hook.mjs:168`, `kind: "seed"`, no `update` path (re-verified at source 2026-09-05) — and the `s9-loop` accreditation row already exists, so the accreditation route returns **409 before any emission** on every close (`accred=already-exists` 15/15 in that finding's window; `accred=written` 0 across the log's whole span). The mentor then ruled option (c), *"give the harness an update path"*, **out as the remedy for C2's observation question**. **Consequence for this watch:** under the current design no harness close-write on this identity can emit a trust event, so the re-latch watch **cannot discharge from harness traffic**; it discharges only on a deliberate `update`-kind accreditation write on `sagereasoning:s9-loop@v1` (a founder-elected act — e.g. the SDK driver), or on a design change the C2 ruling did not license and D1 would need its own election for. |
| **D2** | **The two "engaged" definitions** — `computeVirtueDomains` (`circles ≥ 1 \|\| is_kathekon !== null`) vs `computeDikaiosyneFloor` (`circles ≥ 1 \|\| hasNaturalRelationship`). **R3: the newer, more precise one governs.** Whether the older tag is reconciled at the root or left divergent is undecided. | **OPEN — scoped for ruling, relay pending.** **📋 SCOPED FOR RULING 2026-09-06** (session S5; scope doc `operations/trust-layer-2026-07/2026-09-06-D2-virtue-domain-tagging-SCOPE-FOR-RULING.md`; relay-ready question `2026-09-06-mentor-question-D2-virtue-domain-tagging-FOR-RULING.md`; `D-D2-VIRTUE-DOMAIN-TAGGING-SCOPED-FOR-RULING-2026-09-06`). **APPENDED, NOT REWRITTEN — the row above is preserved as written, and two findings correct it.** **(1) THIS ROW IS STALE, PRE-Q4, AND THE STALENESS IS DATABLE.** It names the counterpart as `computeDikaiosyneFloor` with the formula shown; at the commit that created this register (`94ba579`, 2026-07-17) that function read literally `circles.length >= 1 or hasNaturalRelationship` with no circle-identity parameter. `isDikaiosyneEngaged` — which excludes `self_preservation` when `SUBSTRATE_AGENT_CIRCLES_ENABLED` is on — was introduced 16 days later by `aac6442` (2026-08-02, Q4). So the row describes a divergence about the `natural_relationship` term, whereas **the live divergence is about circle identity**, which is what both binding rulings speak to. **(2) A NARROWED PREDICATE ALREADY EXISTS, LIVE, 307 LINES ABOVE THE UN-NARROWED TAG IN THE SAME FILE** (`layer2-mechanisms.ts:1688` vs `:1995`), and neither this row nor D4 says so. That changes the remedy question from 'invent a rule' to 'why do two rules coexist, and which governs' — **subject to the 2026-08-02 Q4-residual ruling**, which holds that an accumulation surface and a verdict surface have different governing principles and that the right answer for one can be wrong for the other; `isDikaiosyneEngaged` was built for, and ruled on as, a VERDICT surface, so its transfer to the ledger is not automatic. Note `isDikaiosyneEngaged` is **not exported** (a cost of the boundary remedy). **A THIRD FINDING, about D4's own row rather than this one:** D4 cites the published `self_regarding` claim at `llms.txt:418`; the sentence is at **`:548`** and `:418` is an unrelated closing brace. The mis-citation travels with the words *"verified verbatim 2026-08-17"*, which is how a stale citation survives a re-read. **D4's row is left as written per the append-only discipline; the correct locus is recorded here.** **Question NOT decided and no remedy elected — that was the session's explicit scope.** Nothing here moves the flip: weights remain BLOCKED, S11 remains REFUSED. **Live exposure today is nil on the one live harness identity** (no `credential-completed` since 2026-07-18 — the D1 row's own query and its 409-before-emission cause); other agent ids were NOT checked. **✅ RULED 2026-09-06 — A CHANGE IS OWED; SCOPE-DOC OPTION 3 FAILS BY NAME.** Verbatim: `operations/trust-layer-2026-07/2026-09-06-mentor-ruling-D2-virtue-domain-tagging-verbatim.md` (`D-MENTOR-RULING-D2-VIRTUE-DOMAIN-TAGGING-ADOPTED-2026-09-06`); verbatim wins over this cell. **(1) The 2026-07-19 ruling REACHES the tag**, on a ground more basic than the verdict-surface principle: the ledger accumulates what happened, and a domain that did not engage did not happen — crediting it is **mis-accumulation, not mis-verdict**, and is *"accumulation of a false impression"*. The accumulation-vs-verdict distinction *"governs how evidence is treated once correctly attributed. It does not govern whether the attribution is correct in the first place."* **(2) Zero-circle reaches the same answer by the same route.** **(3) The `is_kathekon !== null` trigger is OVER-BROAD and is corrected IN THE SAME PASS** — appropriateness is assessed across all four virtues and a non-null kathekon result does not isolate dikaiosyne; the zero-factor anomaly is a defect in the trigger's logic, resolved by correcting the trigger, NOT a separate ruling. **(4) M-1's conditional referred to the FULL implementation, not the reducer alone** — the destination exists, the condition is met, the interim posture is over, **the removal is owed**. **(5) LOCATION: at the ENGINE (`computeVirtueDomains`), not the trust-core boundary** — the boundary option *"defers the divergence rather than closing it"* and its drift is *"the same class of drift this project has been paying for repeatedly"*. **(6) SEQUENCING IS BINDING: the false-hold observation window opens FIRST to establish a baseline; the engine correction lands AFTER** — explicitly *"not a reason to defer the correction indefinitely… a reason to sequence it correctly."* **CARRIED, UNRULED, and it is a RELAY GAP not a mentor omission:** the scope doc's **Q-D2-4** (should the corrected tag inherit `isDikaiosyneEngaged`'s `\|\| hasNaturalRelationship` term, under which a relationship claimed with NO identified party would still tag dikaiosyne on a credit surface) **was never relayed** — the relay renumbered and spent its Q4 on the location question; the phrase appears zero times in the document the mentor answered. The build session settles it or re-relays it. Also unruled: the exact corrected predicate's form, and the mid-window tagging-regime boundary the ruled sequencing creates (AE-1's S11b read-side segmentation is the precedent). **Status: RULED, BUILD OWED, SEQUENCED AFTER THE WINDOW OPENS. Weights remain BLOCKED; the S11 flip remains REFUSED; neither is unblocked by this.** **📮 THE UNRULED AXIS IS NOW PUT (2026-09-06): `operations/trust-layer-2026-07/2026-09-06-mentor-question-D2-natural-relationship-disjunct-FOR-RULING.md`, awaiting relay (opener F-15).** Two corrections to how this row and the scope doc framed it, both found by re-deriving the arithmetic after the ruling. **(i) The stake was OVERSTATED as a credit concern:** a circle-free natural-relationship input floors the assessment to `reflexive`, the ledger event carries that floored value, and `increase` raises only when the carried value exceeds the current level — `reflexive` is rank 0, so such a tag **cannot raise anything**. The real stake is whether the ledger row should exist at all, which is the ruling's own ground. **(ii) Two sharper facts neither record stated:** a corrected tag that mirrors `isDikaiosyneEngaged` would **still tag dikaiosyne on a SELF-ONLY action** whenever Layer 1 extracted a natural-relationship factor (partially defeating the ruled correction), and the ruled kathekon-trigger fix does NOT close that route, since `natural_relationship` is itself one of the three kathekon factors. Frequency of that combination is **unmeasured**. **✅✅ FOLLOW-ON RULED 2026-09-06 — D2's SPECIFICATION IS NOW COMPLETE; NO OPEN AXIS REMAINS.** Verbatim: `2026-09-06-mentor-ruling-D2-natural-relationship-disjunct-verbatim.md` (`D-MENTOR-RULING-D2-NATURAL-RELATIONSHIP-DISJUNCT-ADOPTED-2026-09-06`). **Term (b) is RETAINED.** The 2026-07-19 boundary *"was not drawn at identified parties. It was drawn at other parties — named or anonymous, identified or foreseeably present"*; an asserted natural relationship is *"evidence that an other party is foreseeably implicated"* and *"the engine does not require certainty; it requires a reasonable basis."* **THE SELF-ONLY-PLUS-(b) CASE DOES NOT DEFEAT THE CORRECTION** — that evidence *"should be credited, not suppressed"*, and a wrong extraction is *"an extraction error, not a tagging error"* whose fix belongs at the extraction layer. **The D2 ruling's self-only case is specifically self-circle-alone AND no natural-relationship factor**, handled by term (a) being false. **The deliberate-divergence third possibility FAILS** — it would mean *"the verdict surface says dikaiosyne engaged and the accumulation surface says it did not. That is not a difference in treatment. It is a contradiction."* **The zero-credit self-correction is ACCEPTED and noted for the record.** ⇒ **CORRECTED TAG = (a) a circle beyond `self_preservation` OR (b) a `natural_relationship` factor, and NOT the `is_kathekon !== null` trigger, which is removed as over-broad — i.e. EXACTLY the existing `isDikaiosyneEngaged` predicate**, so the one-rule aim is satisfied literally and Finding A's observation is what the remedy adopts. **BUILD NOTE from source, not from the ruling: `isDikaiosyneEngaged` and `computeVirtueDomains` are in the SAME MODULE, so NO EXPORT is required** — the export cost recorded above applies only to the ruled-out boundary option; threading the boolean into `computeVirtueDomains` is a SIGNATURE CHANGE for PR19 to review. **Both corrections build in ONE pass. Sequencing UNCHANGED: after the window opens (S4(d)).** Still untouched by either ruling: the mid-window tagging-regime boundary (AE-1 S11b segmentation is the precedent), the ~12 consumers, the live score bonus, the published `llms.txt:548` claim, and the build's own R18 decision. **Weights BLOCKED; S11 flip REFUSED.** **⚖️ RULED 2026-09-06 — MID-WINDOW GOVERNS (`D-MENTOR-RULING-D2-WINDOW-SEQUENCING-MID-WINDOW-ADOPTED-2026-09-06`; verbatim: `2026-09-06-mentor-ruling-D2-window-sequencing-MID-WINDOW-verbatim.md`). APPENDED, NOT REWRITTEN.** The S5b scope document (`2026-09-06-D2-window-sequencing-guard-collision-SCOPE-FOR-RULING.md`) put the mid-window-vs-post-window question; **Reading A is ruled** — the correction lands **mid-window**, after a defined baseline, **and the window keeps running after it lands.** **The §7 sub-question decided it and was answered FIRST:** read-side segmentation **transfers** to a write-side change **iff** the boundary is dated, recorded, and the changed fields distinguishable — **condition MET** (`circles`, `virtue_domains_engaged`, the `is_kathekon` trigger are in every record; *"the boundary does not need to be inferred. It is legible in the data"*). **B1's contamination rule is ANSWERED, not overridden** — it reaches **uncontrolled** edits; *"the contamination rule is satisfied, not violated, by Reading A executed correctly."* **S5b's finding that the guard argument cuts against itself is CONFIRMED** (*"B2's argument from the guard's arming proves less than it claims"*). **THREE BINDING OPERATIONAL ANSWERS: (1) BASELINE = five ordinary days WITH CONSULT RECORDS** — days with ≥1 consult count, **zero-consult days do NOT**; **(2) STAND-DOWN = a recorded founder waiver for the named commit, guard LEFT ARMED, exception documented not encoded** (a scoped allowlist is acceptable only as its own reviewed change, removed after; **a silent commit exploiting the committed-edit gap is FORBIDDEN**); **(3) SEGMENTATION BOUNDARY = the D2 commit timestamp**, with the guard disclosure reporting two regimes, boundary dated. **⚠ AN ITEM OWED *NOW*, BEFORE THE BASELINE PERIOD: a SHA-256 content pin on `layer2-mechanisms.ts`** (same footing as §C2b's `stoic-brain.ts` pin), **updated to the corrected hash after the correction commits** — closes the committed-edit gap and makes the rule *"genuinely enforceable rather than nominal."* **SCOPED NOT BUILT at S5b** (its paste authorises no code change; a mid-session ruling does not widen a session's tier). Guard-safety checked: the test file's own path does **not** match `GUARD_RE`. **§3.3b (defect 6/10 guard vs 0/3 consult) is expressly left open and routed to the window to measure.** **S11-D2 remains BLOCKED — but now on a MEASURABLE THRESHOLD, not an open question.** **✅ THE OWED SHA-256 PIN IS BUILT — 2026-09-06, session S6a (`D-S6A-LAYER2MECHANISMS-SHA-PIN-BUILT-UNCONDITIONAL-PR19-FOLDED-2026-09-06`). APPENDED, NOT REWRITTEN.** §C2c added to `human-practitioner-boundary.test.ts`, on the §C2b pattern the ruling names; hash **derived fresh** and independently equal to the S5b anchor `60cefedb…` (agreement is evidence, not circularity). **Because it reads DISK bytes it catches COMMITTED and uncommitted edits alike — one pin discharges the whole obligation.** **Mutation-verified in FOUR directions:** uncommitted+window-on → 248/2 (both fire); **committed-edit simulated+window-on → 249/1, the pin fires ALONE** (the tree binding passes, proving it cannot see this class); clean+window-off → 249/0; **committed-edit simulated+window-off → 248/1, the pin STILL fires.** Every mutation reverted, target hash re-verified byte-exact, **no `git update-index` flag survived**; re-verified after the PR19 fold. Battery **250/0**. **THE OPEN DESIGN QUESTION IS DECIDED: UNCONDITIONAL**, on four grounds, the decisive one **tested rather than argued** — a window-conditional pin would share its off-switch with the thing it polices (unsetting `GATE1_FALSE_HOLD_CAPTURE` would disarm the pin AND stop capture in one move, reopening the forbidden silent-commit route via a flag flip that erases the evidence of its own use); plus M1's own line (tree binding conditional, content pin not); operational identity wherever the ruling governs; and a **measured** standing cost — **15 commits in 12 months** (last 2026-08-24) vs `stoic-brain.ts`'s 6, correcting the paste's "heavily-trafficked" characterisation. **PR19 ran as THREE independent reviewers, no HIGH, all findings folded, each re-verified at source first:** one MEDIUM (a quotation presented as verbatim was a **splice** of ruling:74+:86, `grep -c`=0 — replaced with ruling:74 contiguous; **two reviewers found this independently**); one LOW that **corrected the author's counter-argument in the author's own favour** (the claim that the ruling scopes the pin's purpose to the baseline period is an INCOMPLETE reading — the same sentence-pair extends it past the baseline and mandates the pin surviving the correction); one LOW (the ruling's **second** permitted stand-down, a scoped allowlist, was unnamed); blast-radius **CLEAN, zero findings**. Guard-adequacy ruled **no self-test is owed** — a mistyped `GUARD_RE` fails silent-green, a mistyped 64-hex literal fails immediate-red, *"there is no typo that widens it"*. **⚠ ONE FINDING NAMED AND DELIBERATELY NOT ACTED ON — A FOUNDER ELECTION:** the repo has an **ACTIVE commit-time gate** (`core.hooksPath`=`.husky/_`; `.husky/pre-commit` blocks commits on five check classes) that **does not run this battery**, and there is no CI and no npm test script — so *"genuinely enforceable rather than nominal"* today rests on a session choosing to run the file, at exactly the moment (commit time) where the committed-edit gap lives. **Remedy is one line, but it changes the founder's local commit behaviour; recommended, not taken** (the session's authorised scope was this pin and nothing else). Shortfall is INHERITED from §C2b, not introduced. **Also disclosed at the pin:** the committed-edit gap remains open ONE HOP out (`corroboration-check`, `reasoning-integrity`, type-only `layer1-extractor`) — though both D2 rulings bind `computeVirtueDomains` in the pinned file only, so the ruled correction cannot be executed in a sibling without tripping the pin. **`layer2-mechanisms.ts` is byte-unchanged; nothing committed or pushed; the window ran untouched throughout.** **Baseline at close: 1 of 5 days with consult records** (2026-09-06 UTC), unchangeable today since that day is already counted. **D2's build gate is otherwise unmoved: S11-D2 still opens only at five ordinary days with consult records.** |
| **D3** | **`derive-trust-events.ts` is LIVE** (trust core ON in production under MEASURE since 2026-07-11). Any narrowing at the **reducer** — as opposed to the predicate — changes live trust-event emission ⇒ **`code-critical` + founder-walked (AC7)**. The predicate (`kathekon-engagement.ts`) is report-time only ⇒ `code-elevated`. **Same root, different tiers.** | **Standing constraint.** |
| **D4** | **The reducer self-circle narrowing (the 2026-07-19 ruling's live-surface half).** The 2026-07-19 predicate narrowing (Arm 1 requires ≥1 circle beyond `self_preservation`) landed in `kathekon-engagement.ts` (MEASURE-consumed — `loop_fold`, the report) but NOT in `deriveWorstJusticeOutcome` (`derive-trust-events.ts`), which reads no circle identity and so still emits `justice-surface-unevaluated`/`justice-surface-indeterminate` for a self-only-circle assessment. Per the ruling this is a mis-attribution (self-only action is `phronesis`/`sophrosyne`, not `dikaiosyne`). Because the reducer is a LIVE trust-event emitter, correcting it is `code-critical` + founder-walked (D3). **Consequence for the flip:** the predicate the flip binds on (G6(a)) is now narrower than the reducer that feeds the live justice ledger — the P1 §9 input divergence gains a THIRD axis (predicate vs reducer vs trust-state, on self-only circles). The reducer half must be settled — alongside D1's cap logic, which also rests on `deriveWorstJusticeOutcome` — before the flip is reconsidered. Also revisit: the `computeVirtueDomains` self-circle attachment (D2's "two engaged definitions" now has a self-circle dimension). | **BUILT DARK 2026-08-17 (R2b) — ACTIVATION OPEN, founder-walked `code-critical` (D3).** `deriveWorstJusticeOutcome` now reads circle identity behind an OPT-IN (`requireBeyondSelfCircle`, default false ⇒ pre-D4 byte-identical, battery-asserted), bound at its ONE live consumer (`deriveCredentialAndJusticeEvents`) to the NEW dedicated flag `SUBSTRATE_JUSTICE_SELF_CIRCLE_NARROWING_ENABLED` (unset everywhere; dedicated, not a reuse of `SUBSTRATE_TRUST_CORE_ENABLED`, per the shared-flag lesson). **The narrowing is SYMMETRIC across all four outcomes — CORRECTED 2026-08-16 by mentor ruling M-1, which OVERTURNED this build's original asymmetry.** As first built, `unevaluated`/`indeterminate`/`met` were gated on ≥1 identified circle beyond `self_preservation` while **`violated` was left ungated**, on the ground that dropping adverse justice evidence makes trust read HIGHER. **The mentor rejected that reasoning:** *"the evidence is not being dropped; it is being correctly attributed."* Dikaiosyne is other-directed whether the obligation was met or violated; preserving the violated case *"is not conservative — it is preserving a category error in the direction that hard-floors the wrong domain."* A false adverse signal is not safer than a false positive one — it corrupts the ledger by attributing a self-regarding failure to a domain that was never engaged, and the hard-floor exemption from hysteresis makes that worse, not better. **All four outcomes are now gated.** Grounds per outcome: ruling #4 names `indeterminate` verbatim as the trigger misfiring; #1 denies the self circle is a justice surface at all; #3 puts self-regarding action under phronesis/sophrosyne, so a `met` credit is a rise the agent never earned — **and M-1 extends #1/#3 to `violated` on the same logic.** **CARRIED, named by M-1 and NOT built:** the correct destination for a genuine self-only violation is `phronesis`/`sophrosyne`; this reducer emits into dikaiosyne by construction and cannot route there. The ruled interim posture is verbatim: *"If the implementation cannot yet route to those domains, the correct interim posture is to WITHHOLD the dikaiosyne emission rather than preserve a known mis-attribution."* **A deliberate predicate/reducer divergence now exists in the OPPOSITE direction from the pre-D4 one and must not be "fixed":** the predicate's Arms 2–4 still ENGAGE a self-only violated obligation (engagement ≠ emission; the 2026-07-19 ruling left Arms 2–4 untouched and M-1 rules only on emission). Pinned §8.9d/§8.9e. STRICT on unknown identity, matching the predicate (unreachable from live input — `circle` is required on every live assessment). **A coupling found in build and guarded:** narrowing the reducer *under the predicate* would collapse `selfCircleOnlySuppression` (its first conjunct is `justice !== null`), silently emptying `loop_fold`'s live `self_regarding` bucket — so the predicate's delegation deliberately does NOT pass the flag, and §8.1c/§8.9b/§8.11 fail if anyone makes it (mutation-verified). Batteries after the M-1 correction: trust-core **112/0** (14 D4 pins, three mutations verified; D4-8 INVERTED by M-1), kathekon **113/0** (§8.9 amended to a TWO-STATE pin rather than inverted — a flat inversion would assert something false while the flag is unset; §8.9d inverted by M-1 and §8.9e added to hold the engagement/emission distinction), loop-fold 181/0, practice-suggestion 791/0, emission-hooks 19/0. **The activation walk must, in this order:** (1) re-read D1's `justice_floor_active` for `sagereasoning:s9-loop@v1`/dikaiosyne — the harness is live and writing, so it may have RE-LATCHED since the 2026-07-18 clear; (2) deploy + verify the narrowed reducer on `origin/main` FIRST; (3) only then the flag, and any SQL correction with the rollback pre-written (the S11b Part-3 ordering guard). **D4 does NOT close D1's full-ledger-replay caveat** — the historical 2026-07-11 event is append-only and a replay still re-latches. `computeVirtueDomains`/D2 deliberately EXCLUDED (a scoring-engine change that would pull `/api/reason` determinism into a trust-ledger step) — carried. **R18 (named, since nothing here surfaced it before):** TWO public claims sit on this mechanism. (a) `llms.txt:418` describes the `loop_fold` **`self_regarding`** bucket as *"loops whose justice arm was suppressed only because every identified circle was `self_preservation`"* — that bucket is gated by `selfCircleOnlySuppression`, so narrowing the predicate's delegation would not merely regress a battery, it would make a PUBLISHED claim false on the next deploy. The opt-out above is what holds it true; verified verbatim 2026-08-17. (b) `llms.txt:632` documents the trust record as *"capped at `deliberate` while a justice latch is active"* — that sentence stays TRUE under D4 (the latch mechanism is unchanged; only WHICH agents reach it changes), so **no doc edit is due while the flag is unset**, but the activation walk must carry an explicit R18 decision rather than assume silence. **Frozen-buffer acceptance gate RUN 2026-08-17, before any report-script edit: strict `n=130 false_positive=129 correct=0`** — unchanged from the 2026-07-19 record, so the PREDICATE provably did not move. **✅ TOOK-EFFECT PROOF DISCHARGED 2026-09-05** (`D-D4-TOOK-EFFECT-PROVEN-D1-WATCH-OPEN-RETRY-HELD-2026-09-05`; founder-walked `code-critical`, all artifacts removed in-session, verified `0 / 0 / 0`). The activation close carried this as owed because its own post-flip check was a **non-regression** check that reads identically both ways by design. **Three legs, none sufficient alone:** (i) a **local differential on the real signed artifact** — `deriveWorstJusticeOutcome` run twice over the production consult's own assessment, `false` ⇒ `justice-surface-indeterminate`, `true` ⇒ `null`, so the branches **provably differ on this input** (the harness was first validated against two negative controls that each would have produced a hollow proof: a two-circle fixture still emits flag-on, a status-less self-only fixture is silent flag-off — **both are plausible outcomes of a real consult**); (ii) a **positive control** — three `credential-completed` rows prove the emission path ran; (iii) **no `justice-surface-*` row of any kind**, with `agent_trust_state.dikaiosyne.justice_floor_active = false`. **Corroboration recorded as premise-only, not conclusion:** the write's `loop_fold` independently classified the input as a self-regarding prudential loop (`n_dikaiosyne_level_excluded: 1`, dikaiosyne absent from `character.domains`) — but that runs on `assessKathekonEngagement`, which **deliberately does not receive this flag**, so it corroborates that the input is self-only and **not** that the flag took effect. **Honest scope: the `indeterminate` path only; `violated` and `met` remain un-exercised.** **⚠ AND THE PROOF SURFACED A LIVE COUPLING THIS ROW ALREADY CARRIED AS D2, NOW EVIDENCED AND SHARPER THAN 'two engaged definitions'.** `credential-completed` is emitted **one per engaged domain straight from `virtue_domains_engaged`, with no circle test** (`derive-trust-events.ts:86-99`) and its effect is **`'increase'`** (`trust-transition.ts:41`). On the self-only fixture the state row read **`dikaiosyne: earned_level 'deliberate', profile_prior 'habitual', justice_floor_active false`.** So **M-1's correction reached the justice-surface EMISSION but not the domain TAGGING**: `computeVirtueDomains` still tags dikaiosyne engaged on a self-only action, and the same mis-attribution now yields an **increase instead of a cap**. At a single event the level is identical either way (the rise stops at `deliberate`; the pre-D4 cap also sat there) — **the difference is the latch, which governs FUTURE rises.** The precedent is D1's own `s9-loop` permanent cap, the mentor's *"permanent cap on an agent whose actions had no identified affected parties"*. D4 correctly ends that class; what remains is that **dikaiosyne can rise past `deliberate` on evidence from actions with no other party at any circle.** **Confidence marked: the single-event state is OBSERVED; the multi-event rise is REASONED from `trust-transition.ts`, not measured.** **This is not an argument against D4** — M-1 is right and the remedy is D2, not restoring a mis-attributed cap — **but D4's activation RAISED D2's PRIORITY, which neither this row nor the activation close previously said.** |
| **D5** | **`taskHasJusticeSurface` is never supplied — the live advisory answers a question it never asks.** *(⚠ STATED IN THE PRESENT TENSE **AS FOUND**, 2026-09-04, and preserved rather than rewritten per this file's own "do not delete it" discipline. **Every present-tense claim in this cell is now FALSE of current code, and the line numbers it cites are PRE-FIX** — read the Status cell for what is actually true. Flagged because a stale present-tense mechanism fact read as current is a failure class this project has hit repeatedly, and PR20 requires the timestamp check.)* `readTrustVerdict` declares `taskHasJusticeSurface?: boolean` and defaults it **false** (`harness-integration.ts:566`); **no caller anywhere passes it** — verified by grep across `website/src` + `harness/`: the only occurrences are the parameter definitions and FOUR call sites, none supplying it (`harness-integration.ts:566` the default · `api/practice/discernment/handler.ts:191` · `api/trust-record/[agent_id]/handler.ts:152`, which passes `strictStore` only · **`website/scripts/false-hold-observation-report.ts:358`, found by `tsc` at the build — the scope doc's grep missed `website/scripts`; count corrected same day, conclusion unchanged**). `interventionInputFromS3` gates the ENTIRE justice branch on that flag (`intervention-engine.ts:786`), so the wired path reports `justiceSurface: 'none'` **unconditionally, by omission**. The live harness advisory therefore does not judge that no justice surface was engaged — **it never asks.** Mentor 2026-09-04, verbatim: *"a honesty defect in the MEASURE-mode sense — the instrument reports a result without having performed the relevant test"*; **"a standing defect that the P1 ruling does not license deferring"** — owed INDEPENDENTLY of P1, not a P1 consequence. Fix: supply the flag where it is known, or remove the parameter as unreachable. Ruled to be corrected in the SAME build pass as P1's consequences, but **gated only on that build being licensed — which the P1 ruling does not do.** | **CLOSED 2026-09-04 — fixed, deployed, live-verified in code.** Fixed in the SAME build pass as P1's consequences, as ruled. `taskHasJusticeSurface` is now **REQUIRED** on `readTrustVerdict` (`harness-integration.ts:531` — no `?`, so tsc refuses an omitting caller) and **every call site states it with its reason**: `api/practice/discernment/handler.ts:197` and `api/trust-record/[agent_id]/handler.ts:157` (both genuinely task-agnostic standing reads — no task is in scope, so `false` is the honest value, not an omission) and `scripts/false-hold-observation-report.ts:358` (the task-agnostic report read). The `basis` string is re-labelled per the ruling's third consequence (`harness-integration.ts:605` — the recommendation is disclosed as ranging over the AGGREGATE trust state, task-agnostic, explicitly *not* the per-action decision-table input). Commit `50c0c0f`, pushed and Vercel green 2026-09-04; the regression check is a `grep` for `readTrustVerdict(` across `src` + `scripts` filtered to lines lacking `taskHasJusticeSurface`, which must print nothing. **NO LIVE OUTPUT CHANGED, and that is the point:** `false` yields the same `justiceSurface: 'none'` the default yielded, so no production behaviour moved — what changed is the WARRANT. The instrument no longer reports a result without having performed the relevant test; it now states that the test is inapplicable at these call sites. That is precisely the MEASURE-mode honesty defect the mentor named, and it is what closure here means — not a behaviour fix. **Standing hazard recorded at the header for any future caller:** passing `true` without S3 obligation routing routes to `'unevaluated'` ⇒ do-not-proceed for every agent — *"you are not asking the question, you are asserting an answer."* The two existing `false` call sites are load-bearing and must NOT be flipped to `true`; a genuine task-scoped read is a DIFFERENT call site. |

---

## E. Identity + credential generations (for row attribution)

The subject of every trust/accreditation row below is **`sagereasoning:s9-loop@v1`** — one identity, two credential generations. **Attribute rows by generation before drawing any conclusion from them.**

| Gen | Role | id | Status |
|---|---|---|---|
| 1 (S9, 2026-07-11) | consult | `09e83b4d…` | **REVOKED 2026-07-17** — public-exposure incident |
| 1 (S9, 2026-07-11) | accred | `e715520b…` | **REVOKED 2026-07-17** — public-exposure incident |
| 2 (2026-07-17) | consult | **`33bef3d4-018d-4313-bcfd-65a75132155c`** | **LIVE** — 5000/200 |
| 2 (2026-07-17) | accred | **`1ffe14f6-0f07-4296-b340-c3bdfbbc7ce2`** | **LIVE** — 5000/200, owner+agent bound |

**Gen-1 rows are all pre-2026-07-17 and are established as ours** — the abuse check (incident §4b) cleared both the billing surface and the trust ledger across the full 5d13h exposure window, on two independently-corroborating tables. **The identity was deliberately NOT split to `@v2`:** it is the subject of **D1**'s cap review, and splitting would have fragmented the record that review examines. **Incident record:** `operations/incidents/2026-07-17-public-credential-exposure-s9-loop.md`.

**2026-07-19 — gen-2 consult DIAGNOSED HEALTHY (DB-verified); NO rotation performed (`D-S9-LOOP-CONSULT-CREDENTIAL-DIAGNOSED-HEALTHY-NO-REFRESH-2026-07-19`).** The P0 "consult-credential refresh" ran as a diagnosis and found **the gen-2 consult credential is healthy — no refresh needed.** **DB-verified (founder-run `mint-credential.ts list`, prod):** the settings consult token `sr_prac_0ba814…` hashes to id **`33bef3d4…`** (this §E row) — limits **5000/mo, 200/day**, monthly usage **623** (13% of cap); the accred `sr_prac_18c213…` = `1ffe14f6…`, 5000/200, used 70. **This CORRECTS the 2026-07-18 record "the raw token no longer hashes to `33bef3d4…`":** it hashes to exactly that row (623 successful calls recorded on it). **Quota is definitively NOT the constraint** — monthly 623/5000, and daily successful consults peak ~57 (07-18) far below the 200/day cap (the ~50–57/day ceiling is the SUCCESS count under a high transient-failure rate, not a limit). **Route-masking (important, and easy to mis-read):** at `/api/reason` the route flattens EVERY `validateApiKey` failure — `invalid_token`, quota-429, suspended-403, usage-RPC-503 — into a single **401** (`route.ts:726` returns `auth.error`, never `apiKey.error`; the body is the generic "Please sign in"), so the 401 status alone is ambiguous and the `list` DB lookup is the ONLY disambiguator (it here rules out quota + suspension). With those ruled out and the token valid (256 successes; 623 recorded calls), the **131 all-time 401s are transient DB-layer fail-secures under the dogfood's burst of heavy 13–20s composed consults** (a credential-lookup query error → `invalid_token` → 401; the usage-RPC fail → 503 → also masked). A fresh credential carries the SAME limits and hits the SAME fail-secures ⇒ **no mint would help.** The real remedy — server-side lookup resilience (retry-before-fail-close) + composed-consult latency — is handed off as a `code-elevated` follow-up (`operations/handoffs/founder/2026-07-19-consult-lookup-resilience-and-latency-NEXT-SESSION-PROMPT.md`), NOT a credential op. **§E rows unchanged** — gen-2 consult stays LIVE + healthy.

---

## F. Logos-on W3 — S11-anchored staging (register annotations, adopted 2026-09-10)

Per `operations/agent-circles-2026-08/2026-08-01-agent-circles-logos-on-plan.md` §3 (W3), executing
the binding mentor verdicts L2–L4 and L6 from
`operations/agent-circles-2026-08/2026-08-01-mentor-consultation-agent-circles-logos-on-verbatim.md`
(verbatim wins over this annotation). **W3's dependency gate — C1c, C2, D4 all closed — is now
discharged**: C2/C1c live since 2026-08-08, D4 live and took-effect-proven since 2026-09-05
(re-verified from `/CLAUDE.md`'s "Live in production" list at this writing). **Nothing here
activates anything. These are dated annotations to this register, per W3.1** — pins (W3.2) are
listed separately below as named-not-yet-built, since they touch `GUARD_RE`-matched source
(`website/src/lib/substrate/trust-core/intervention-engine.ts` matches both `/substrate/` and
`trust-core`) and this register itself does not.

| # | Annotation | Source |
|---|---|---|
| **W3-a** | **Circle-4 enters the S11 flip's staged PAUSE tier only, never the do-not-proceed class.** Promotion to a stronger tier is earned by the extraction demonstrating the SAME zero-false-positive floor on circle-4 candidates that the kathekon-free class must demonstrate on its own — the practice record's accumulated circle-4 track record is the promotion-evidence instrument. | L3 |
| **W3-b** | **The first circle (own reasoning integrity / prohairesis) is measure-only under enforcement, permanently** — the tool must structure the hegemonikon, never substitute for it. **Narrow exception (L4):** when a task-pressure assent produces an other-directed (circle-2/3/4) violation, the enforcement block is justified by THAT violation; the record distinguishes — enforcement trigger = the other-directed violation; the first-circle failure is recorded separately, measure-only, never as the cited enforcement ground. | L4 |
| **W3-c** | **NEW named prerequisite: the practice-on first-circle correction (C1) must be FULLY SETTLED — landed AND calibrated — before the S11 flip proceeds.** This is in addition to, not a replacement for, the 2026-07-12 four-part readiness standard (§A above) — the 2026-07-12 shape is unchanged (L2). Cross-reference to §A when this register's own hard-prerequisites table is next revised. | L2 |
| **W3-d** | **The W2 record machinery (the enforcement class + per-entry regime markers + the compliance-not-virtue clause) is a flip component.** The flip must not activate without it, or enforced outcomes would land in the trust record unrecorded and unmarked — an honest-claims violation on its own terms, independent of readiness. **⚖️ 2026-09-12 (Q-W2): W2 has acquired a SECOND role — it is the mentor-ruled strongest candidate for the WORK of a part-(2)-qualifying observation window, and it satisfies that ruling's CHANNEL requirement (Write/Edit on a live surface with real cost to getting it wrong). Whether it satisfies the VARIETY requirement — more than one class of courage-relevant moment — is the founder's assessment against W2's actual build requirements and is expressly NOT settled by that ruling.** Status of W2 itself: **NOT built** as of this annotation (soft-dependent on C1c's schema being settled per the plan §3 W2 item 4/§4 dependency graph item 7 — a soft, not hard, dependency; W2 was deliberately not decided to ride inside any earlier schema step). | L5, L7 |

**Pins named here 2026-09-10 — ✅ BOTH BUILT 2026-09-12** under the founder's D2 election ("D2 bundle
in"), inside the same waived session as the B1 Part-1 fix. They live in
`website/src/lib/substrate/trust-core/__tests__/s4-intervention-engine.test.ts` **§W3** — the
**test file only**; `intervention-engine.ts` itself is NOT modified and stays byte-identical. (W3.2
— `code-standard`, buildable dark, but touches `GUARD_RE`; needed its own founder-waived session,
guard left armed throughout.)
1. The fifth-circle orientation reading (`C2`/`C1c`'s `orientation-reading-{toward,away,indeterminate}`
   event class) is never an input to `recommendIntervention` or any enforce path — L6, *"the enforce
   layer may NOT consume the orientation reading… enforcement's blindness to unexamined reasoning is
   addressed by strengthening the corroboration standard, never by feeding orientation into the
   loop."* The intervention engine's existing `import type`-only purity discipline (documented at its
   S4 build, `/CLAUDE.md`'s Trust Layer S4 refresh) already structurally helps; this pin would make the
   guarantee an explicit, asserted battery test rather than an inherited property.
2. No enforcement path keys on first-circle-only findings — L4's dual-recording rule, the negative
   direction: an enforcement decision citing ONLY a first-circle (prohairesis) finding, with no
   other-directed circle-2/3/4 violation present, should be structurally unrepresentable or
   battery-caught.

**Built shape (2026-09-12).** §W3.1/§W3.1b assert pin 1 — the orientation vocabulary appears nowhere
in `intervention-engine.ts` source, with a **positive control** so the assertion cannot pass by
grepping a file that was never read. §W3.2–§W3.4 assert pin 2 — the intervention engine's inputs
carry no first-circle/prohairesis channel, so an enforcement decision citing only a first-circle
finding is structurally unrepresentable rather than merely untested. **§W3.5 pins L4's positive half
by source-grepping the ACTUAL `JusticeSurfaceState` union** (not the test file's own `ALL_JUSTICE`
fixture — the first cut pinned the fixture, was caught by PR19, independently confirmed by mutating
the real union and watching the pin stay green, and fixed; re-mutation now correctly turns it RED).
Every pin is mutation-verified against the real source and the source restored SHA-identical.

**Both pins were named here so a future waived session did not have to re-derive them from the plan
document; that session was 2026-09-12.** Nothing activates. The S11 flip remains REFUSED; weights
remain BLOCKED; the 0h call remains the founder's.

---

## Change log
- **2026-09-12 (latest — W2 BUILT DARK ON BRANCH `w2-record-honesty` (`5aa82f5`), NOT MERGED, NOT
  ACTIVATED; `D-W2-ENFORCEMENT-RECORD-MACHINERY-BUILT-DARK-ON-BRANCH-PR19-FOLDED-2026-09-12`;
  close: `operations/handoffs/founder/2026-09-12-W2-record-honesty-build-CLOSE.md`; design:
  `operations/agent-circles-2026-08/2026-09-12-W2-record-honesty-DESIGN.md`)** — Row **W3-d**'s
  "NOT built" is now "BUILT DARK, unmerged": all five plan §3 W2 items (the effect-neutral
  `enforcement-outcome` class; per-entry regime markers; the L7 clause inline, record-level half
  STAGED for R18; the L4 dual-recording rule pinned both directions; the schema election decided as
  a SEPARATE 21→22 CHECK widening, authored on main) behind the new dedicated flag
  `SUBSTRATE_ENFORCEMENT_RECORD_ENABLED`. PR19 three-reviewer, one HIGH folded at the root. The
  guard is armed and no waiver existed, so the code sits in an isolated worktree; merge + the
  migration walk are the founder-walked waiver session's acts
  (`2026-09-13-W2-waiver-merge-and-schema-walk-NEXT-SESSION-PROMPT.md`). W3-d's flip coupling is
  unchanged: the flip must not activate without W2 LIVE, and W2 is not live.
- **2026-09-12 (THE W2 WINDOW IS SPECIFIED, APPROVED, AND RULED. THE CLOCK RESTARTS AT ITS
  FIRST RECORD; NO W2 SESSION HAS YET RUN;
  `D-PART2-WINDOW-SPECIFICATION-APPROVED-RULED-2026-09-12`; verbatim, canonical:
  `2026-09-12-mentor-ruling-window-specification-QS1-QS2-QS3-verbatim.md`; specification:
  `2026-09-12-part2-window-SPECIFICATION-FOR-APPROVAL.md`)** — The founder approved W2 as the
  window's work, the consult-channel constraint, and the §2 discipline (variety is assessed, never
  targeted). The mentor then ruled three questions. **⚖️ Q-S1 — THE CLOCK RESTARTS at W2's first
  record**, not the current window's 2026-09-06 start: *"A materially different composition is a
  materially different window."* Named consequence: the current window's Sunday mark closes it at
  **three of four parts met, part (2) structurally unmet** — that mark does not carry into, or
  accelerate, the W2 window. **⚖️ Q-S2 — the §2 discipline is sound but now carries a BINDING
  PER-SESSION CONDITION, not merely a specification property:** *"If a W2 session prompt names the
  observation goal… the discipline is broken at the point of application, regardless of what the
  specification says. The founder's responsibility is to ensure the session prompts name the work,
  not the observation."* **⚖️ Q-S3 — SEQUENCED, NOT REFUSED.** No standard revision now — *"the
  domain is not structurally unreachable. It is rarely reached under the current window's
  composition"* — but *"if the window's post-assessment finds that andreia remains unevaluated
  despite a genuinely composed, consequential window, bring the standard-revision question back with
  that finding."* Evidence first, revision second. **W2's §F pins remain the only W2 work built to
  date; the window itself has not started.** Nothing built, activated or flipped this session. D2
  remains blocked; the S11 flip remains REFUSED; weights remain BLOCKED; the 0h call remains the
  founder's.
- **2026-09-12 (THE QUALIFYING WINDOW NOW HAS A SPECIFICATION: CHANNEL + VARIETY;
  `D-ANDREIA-CAPTURE-PATH-RULING-ADOPTED-2026-09-12`; verbatim, canonical:
  `2026-09-12-mentor-ruling-andreia-capture-path-and-window-design-verbatim.md`; relay:
  `2026-09-12-mentor-question-andreia-capture-path-and-window-design-FOR-RULING.md`)** — **AMENDS the
  Q-A1 ruling of earlier the same day.** A founder follow-up ("can anything advance the andreia
  hold-up now; will a different window capture properly") sent a session back to the buffer, and it
  returned two things the ruled evidence base did not contain. **(1) The andreia count moved 1 → 2,
  and the second record was produced by the session folding the Q-A1 ruling** — an Edit to THIS FILE,
  the change-log entry recording part (2) as not dischargeable; both records are near-identical
  (`agonia`, reopened, carriedPrior, ~3.5k composed chars, self_preservation+cosmopolis, obligations
  met/met) and both are *the act of committing an unwelcome finding to a binding record*. Q-A1 is
  **not** reopened — two of one kind is still not an evaluation. **(2) THE STRUCTURAL FINDING:
  `andreia` has NEVER been captured on the guard path in 285 records and cannot be.** Guard n=285
  (Bash, raw shell text): phronesis 255 · dikaiosyne 284 · sophrosyne 214 · **andreia 0**. Consult
  n=119 (Edit 89 / Write 30, composed intent): dikaiosyne 119 · phronesis 67 · **andreia 2** ·
  sophrosyne 2. Every andreia reading and every sub-species passion comes from the consult path.
  **⚖️ Q-W1 — THE WINDOW SPECIFICATION IS AMENDED TO TWO DIMENSIONS, AND THE CHANNEL IS THE BINDING
  HALF:** *"a window capable of honestly evaluating andreia requires consequential decisions carried
  out through composed, narrated actions — Write/Edit, not Bash. The subject matter and the channel
  are both required. Either without the other is insufficient."* The decisive sentence: ***"The
  subject matter of the work does not reach the extraction if the work passes through Bash."*** Not a
  defect — *"a designed property of the harness"* (Bash removed from auto-consult at S1).
  **⚖️ Q-W2 — VARIETY IS NOW BINDING ALONGSIDE VOLUME AND CANNOT BE BOUGHT WITH DURATION:** *"A domain
  cannot be honestly evaluated from a single behavioural class at any N. The window must be chosen
  for variety of courage-relevant moments, not duration alone."* The two captured records are *"the
  courage of honest record-keeping under the pressure of a finding that blocks progress. That is real
  courage. It is not the full domain"* — which also includes acting under uncertain outcome, holding
  under external pressure, and naming a disagreement when agreement would be easier. **W2 is ruled
  the strongest candidate for the work and satisfies the CHANNEL requirement; whether it satisfies
  VARIETY is the founder's assessment against W2's actual build requirements and is NOT settled.**
  **⚖️ Q-W3 — PART (2) IS FURTHER FROM MET THAN THE BOARD SHOWS.** Q-A2 stands, but *"'count as
  records' and 'constitute an honest evidence base for a signed assessment' are not the same claim."*
  `sophrosyne` 214/216 = **99%** guard-path; `phronesis` 255/322 = **79%**. The question belongs in
  any write's honest assessment, **not in a build decision** — *"A conservative weight on a domain
  with a predominantly unreliable extraction path is not a failure. It is the correct reading."* A
  **guard-path composition disclosure** was ordered onto the pre-flip report and is discharged at its
  new **§3.3b**. **⚖️ Q-W4 — priority order for the founder:** (1) **decide the fork**, now that both
  constraints are in hand — **no accreditation write on the present evidence, CONFIRMED**; (2) if the
  fork is a new window, **specify it before starting it** — *"A window started on the wrong channel
  or without the variety criterion in view spends weeks and captures nothing"* — stating **what work,
  through what channel, producing what variety of courage-relevant moments**; (3) part (1)'s Sunday
  threshold — running to the mark first or deciding the fork now are **both legitimate**, and *"the
  clock reaching its mark does not license the flip."* **Nothing built, activated or flipped. D2
  remains blocked; the S11 flip remains REFUSED; weights remain BLOCKED; the 0h call remains the
  founder's.**
- **2026-09-12 (later — PART 2 IS A WINDOW, NOT A WRITE; B1/B2/B3/D1/D2 executed;
  `D-PART2-EVIDENCE-RULING-ADOPTED-B1-D1-D2-EXECUTED-2026-09-12`; verbatim, canonical:
  `2026-09-12-mentor-ruling-part2-evidence-and-part3-split-verbatim.md`)** — **The single most
  consequential change to this register since the four-part standard was set.** The founder relayed
  the A1/A2/C1 questions and the mentor ruled on the per-domain evidence. **Part (2) is NOT MET AND
  IS NOT DISCHARGEABLE FROM THIS WINDOW.** `andreia` carries **one** record window-wide, and *"one
  observation is not an evaluation"*; `dikaiosyne` 378 / `phronesis` 309 / `sophrosyne` 215 are real,
  but the window's composition — governance authoring — *"may never produce andreia-engaging
  material."* So part (2) *"is not waiting on a write, a flag, or more days — it is waiting on a
  window pointed at different work"*: satisfying it *"may require a different kind of window — one in
  which the loop is operating on consequential product decisions."* **The Q-B Branch-1 accreditation
  write, recommended the day before, therefore cannot by itself discharge part (2)** — a write made
  over one andreia observation would be exactly the dishonest write the Q-B condition forbids.
  **Which fork to take — a differently-composed window, a standard revision, or accepting part (2)
  unmet — is the founder's call, unmade.** Also ruled: *"evaluated"* means a domain produced a
  record with an assessed reading, not merely appeared; *"above conservative"* reconciles to **tier 4
  or better** on the A5 seven-tier scale (tier canonical, weight derived). **Part 3's consult/guard
  split remains OPEN and unruled** — deliberately left by the founder ("D3 leave until part 2
  resolves"). **Executed the same turn under founder election:** **B1** (waiver granted) — the
  pre-flip report's Part-1 pooled-span defect is FIXED in
  `website/scripts/false-hold-observation-report.ts`: duration is now window-scoped via a shared
  `windowRowsFor` helper, with the pooled span retained beneath as a `[DISCLOSURE ONLY]` line, and
  the Q-C ruling's wording replaces the script's "OPEN, UNRULED" note. **B3** — the standing opener
  is de-drafted to ✅ FOUNDER-ADOPTED 2026-09-12. **D1** — the R18 logos-on framing is applied to
  `llms.txt`, `agent-card.json` (extension count unchanged at 26) and `api-docs`. **D2** — the two
  W3.2 pins are BUILT (see §F above). **D3 (W2) deferred by election.** PR19 ran on the waived build
  and found one HIGH — §W3.5 pinned a test-file-local constant rather than the real
  `JusticeSurfaceState` union, confirmed independently by mutating the real union and watching the
  pin stay green; fixed and re-mutation-verified. `intervention-engine.ts` is NOT modified; both SHA
  pins clean; the guard stayed armed and the waiver was scoped per-commit. **D2 remains blocked; the
  S11 flip remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**
- **2026-09-07 (S7, later — OPTION D RULED and EXECUTED;
  `D-MENTOR-RULING-CALLER-CLASS-OPTION-D-ADOPTED-EXECUTED-2026-09-07`; verbatim, canonical:
  `2026-09-07-mentor-ruling-caller-class-option-D-verbatim.md`)** — **AMENDS the segment-1 clause
  published the same morning.** `caller_class` was built exactly as ruled, then MEASURED null: 20+
  post-boundary records, all `'unknown'`, incl. commands provably issued by a review-fleet subagent —
  the `PreToolUse` hook fired inside a subagent receives no `/subagents/` path segment. **Ruled: the
  guard population is STRUCTURALLY UNABLE to satisfy ruling 3's exclusion on this harness as wired;
  carried as a DISCLOSED LIMIT.** *"This is not a build failure. It is a discovery about the
  environment."* Spawn-depth (H2/H5) ruled OUT on measured evidence (it would misflag a genuine
  live-agent action — *"the normal operating condition"*). The amended clause is LIVE in the report;
  the printed zero now reads **NOT a clean bill of health**; the exclusion mechanism is RETAINED as
  *"correct in principle"* and still fires if the environment ever supplies the signal. **Option C
  investigation OWED as a bounded one-session work item** (does `SubagentStart` correlate to an H3
  record via a key other than session id? YES ⇒ ruling revisited; NO ⇒ disclosure final) — authored,
  not started; it needs a founder `GATE1_DEBUG` act. Report battery 99 → 105/0.
- **2026-09-07 (S7 — rulings 2 and 3 executed under a recorded founder waiver;
  `D-S7-RULINGS-2-3-EXECUTED-CALLER-CLASS-BOUNDARY-2026-09-07`; commit under `--no-verify`, guard left
  armed, waived per D2 stand-down shape (a))** — the guard-side outage exclusion now reaches Part 3's
  own rate (previously Part 3b only, a HIGH found by PR19); a `callerClass` field (v4→v5 boundary)
  excludes review-fleet subagent records with a two-value design (`'subagent'`/`'unknown'`, no
  `'live_agent'`). **MEASURED, not merely disclosed: 14 real production records post-boundary, all
  `'unknown'`, including one confirmed fleet-generated command — the transcript-path signal does not
  discriminate on this machine as currently wired.** A harness-level open question is named for the
  founder/mentor: whether another field (the SDK-callback `SubagentStartHookInput` shape) could. PR19
  8/9 upheld, 0 refuted, both HIGHs folded. Baseline unchanged at 1 of 5 (this session's one
  consult-eligible action was lost to a `CONSULT-OUTAGE`).
- **2026-09-07 (S6b's three questions RULED; `D-MENTOR-RULINGS-S6B-THREE-QUESTIONS-ADOPTED-2026-09-07`; verbatim, canonical: `2026-09-07-mentor-rulings-S6b-three-questions-verbatim.md`)** — all three **ADOPTED AS BINDING**. **(1) Option S's even-K median convention is RULED `lower_median`** — the value at position K/2, never the average, because *"averaging two ordinal ranks produces a number that may not correspond to any actual rank on the scale"*; **settled BEFORE any run, deliberately** (*"Change this after a run and every `would_option_M_record` is wrong"*). It stays a **convention, not a ruling on the underlying scale** — revisited if the scale changes or K moves off 10. **No code change needed, VERIFIED not assumed** (K=10 fixture returns position 5, never averages, and the two conventions genuinely differ there). **(2) `GUARD-OUTAGE` records are EXCLUDED from the guard rate denominator** — *"records that no examination happened, not that an examination happened and produced a result"*; **symmetry governs** (the consult side already excludes outages), the outage rate is reported **separately on both sides** per F-3′, and the record's own `captureBasis: "no_assessment"` is the clean exclusion mechanism. **(3) Review-fleet SUBAGENT records are EXCLUDED from the guard population** — including them mixes *actions taken with actions reviewed*, the class the P6 amendment forbids; exclude on session id if it distinguishes, **and it does NOT** (S6b established subagent records carry the PARENT id), **so a `caller_class` field is OWED, before publication**. **⚠ RULINGS 2 AND 3 ARE ADOPTED BUT NOT EXECUTED — both collide with the ARMED guard, reproducing the D2 collision class TWICE:** verified at source, `false-hold-observation-report.ts` (ruling 2) and `harness/gate1-…/lib/false-hold-capture.mjs` (ruling 3) **both match `GUARD_RE`**, so neither may sit modified while the window runs; the **D2 waiver mechanics carry** (recorded founder waiver for the named commit, guard LEFT ARMED, exception documented not encoded, never a silent commit), and **neither file has a SHA pin**. **A NEW MENTOR QUESTION, named and open:** `caller_class` mid-window creates a **schema boundary** (`v4`→`v5`) and the ~75 records already captured are classifiable only by an `actionPreview` heuristic — **is a retroactive classification acceptable for pre-boundary records, or does the guard rate report only post-boundary?** **Scope note lowering urgency: ruling 3 reaches the guard DISCLOSURE only — the consult population is CLEAN** (all 3 consult records are S5b's `Edit`s; S6b produced 50 records, all `Bash`, all guard, **zero consult**), so the gated within-consult measure carries none of it. **Founder decisions recommended, NOT taken:** make the hook's `npx not found` branch **fail closed** (*"a guard that passes on machines where it cannot run is not a guard"*) — **but verify Node is on GitHub Desktop's PATH first, or the founder cannot commit at all**; and **close four of five open peer sessions**, working one arc (*"epithumia — craving — presents as urgency"*). **P4/P5/P6 unmoved; S11-D2 still BLOCKED on the five-day threshold; the flip remains REFUSED; weights BLOCKED.**
- **2026-09-07 (S6b — the D2 pin made enforceable at the commit gate; Option S pre-run blockers fixed; `D-S6B-COMMIT-GATE-WIRED-OPTION-S-PRE-RUN-BLOCKERS-FIXED-2026-09-07`; close `2026-09-07-S6b-commit-gate-and-option-s-fixes-CLOSE.md`; commits `af67e4c`, `6bdff56`, **neither pushed**)** — **The D2 ruling's own standard is now met at the moment it applies.** Ruling §4 required the `layer2-mechanisms.ts` pin be *"genuinely enforceable rather than nominal"*; S6a built it, but `.husky/pre-commit` (an active gate on five check classes) never ran the battery, and there is no CI and no npm test script. The battery now runs **first and ALWAYS** in that hook. **Always-on was decided on its merits:** a staged-only trigger **cannot enforce the byte-identity guard at all**, because that guard's subject is files *modified in the working tree*, which need not be staged. **Mutation-proven to block a real commit** (broken pin ⇒ `git commit` exit 1, **HEAD did not move**), including with `GATE1_FALSE_HOLD_CAPTURE` unset — the GitHub Desktop path — where it blocks on the unconditional pin. **Three limits stated, not papered over:** the working-tree guard binds only when the flag is in the **hook's** environment (absent under GitHub Desktop), and the hook **deliberately does not set or fake it** (forcing it would bind the guard while the window is stopped, contradicting M1); the battery is one flat script so the gate blocks on any assertion; and **NOT fixed, for the founder** — the pre-existing `npx not found` branch `exit 0`s the hook, so a machine without Node on PATH **silently skips the pin and commits**. PR19 six dimensions / 11 agents: 1 HIGH folded (the failure message asserted a measured-file change — false for a section A/B/D failure) + 1 LOW folded (the guidance was unreachable dead code under `sh -e`). **Option S** (S6's gate): all four 2026-09-05 pre-run blockers resolved — B1 **moot by removal** under the 2026-09-05 ruling; B2 fixed by reading the gate's **own `proceed`** rather than re-deriving from rank (which collapses B2+B4 and sidesteps the open `habitual`-in-the-floor-set question; `p_hat_floor` unchanged); B3 fixed with an ordinal rank transcribed from `guardrails.ts`, the even-K convention **published as a convention, not a ruling** (`lower_median`) and a live M/W/S input — plus series ids, `--resume`, series-aware pooling, the six D6a safeguards, a **fail-closed** spend gate, a refused 3xx, and L4 read from data (**24**). New 45-assertion battery, **mutation-verified twelve ways**; PR19 3 raised / 0 refuted / all folded, incl. a **MEDIUM vacuous spend-gate probe**. **Option S has still never made a call.** **WINDOW:** untouched; **baseline 1 of 5** (2026-09-06 UTC only, and it could not move that day — already counted). **⚠ THE CARRIED CORROBORATION RECIPE IS STALE:** S6a's `CAUTION+PROCEED+BLOCK` tally was an enumeration that was *accidentally complete*; this session produced the window's first **`GUARD-OUTAGE`**, after which it read a **false** disagreement (log 69 vs buffer 70, stable across three reads — not a race). The family is **four** tokens and then AGREES 72=72. **Derive `GUARD-*`; never quote the list.** A guard outage **still writes a buffer record** but labels itself (`captureBasis: "no_assessment"`), so it can be excluded deliberately — asymmetric with the consult side, where D6a already excludes outages. **⚠ TOOL-CHOICE VALIDITY SHARPENED, NOT RESOLVED:** this session contributed **45 of 75 new-window records (60%), all guard** (S6a's PR19 flagged 4) — **corrected at the close reflection: NOT "read-only" (23 of 48 carry an overwrite redirect, the guard's irreversible-action signal) and NOT all its own (10 of 48 are PR19 review-fleet SUBAGENT commands, recorded under the PARENT session id)**, and — the new half — it ran under a harness directive preferring `Bash`, which is dropped from the consult floor, so **a session's tool mode decides whether its work enters the measured population at all**. Tool choice was NOT steered. **P4/P5/P6 unmoved; S11-D2 still BLOCKED on the five-day threshold; the flip remains REFUSED; weights BLOCKED.**
- **2026-09-06 (S4, window-start-readiness session; `D-S4-WINDOW-START-READINESS-BLOCKED-DATE-GATE-2026-09-06`)** — **the window did NOT open. Both preconditions checked and both fail.** P8a activation confirmed still absent (`grep -c` = 0); leg (a) (B4's follow-up rate) correctly NOT run — `date -u` at open was 2026-09-06T04:11:54Z against the paste's ≥2026-09-08 UTC gate. **F-3′ threshold PROPOSED, not elected:** `2026-09-06-F3prime-guard-availability-threshold-PROPOSAL.md` (≤5% aggregate AND ≤10% any single ordinary day [≥20 guard attempts], over ≥3 successive ordinary days) — PR19-reviewed, 1 HIGH + 2 MEDIUM confirmed and fixed (a partial-day 60% figure wrongly paired with a full-day attempt count, corrected to the day's actual 25.25% close; "lean mode is closed" corrected to "held pending founder election"; an unsourced concurrency figure removed after the reviewer found its source document uses it to REFUTE concurrency, not support it). Independently re-verified addition: of 28 windows of three successive ordinary days in the 57-day log, ZERO pass both proposed rules — genuinely binding, though the known post-remedy figures sit an order of magnitude below 5%, so it is expected to pass once B4 actually runs. **A companion finding, not flip-gating but named for the founder:** the guard-DENY population (P5/part-3's "correct holds" denominator once P8a activates) is thin — 15 events over 57 calendar days, 45 of 50 active guard days at zero, 0.089/day excluding the 09-04 outlier (≈0.6/week); P8a moves the denominator from structurally-absent to present-but-thin, not to populated, and F-3′ does nothing for it (the constraint is volume, not selection). **D2 confirmed as read at source: the window opens FIRST, D2's engine correction lands after — S4 gates D2, not the reverse.** S9 (harness A11b redaction) confirmed built AND firing at source (`redacted=` observed 8× live), not merely inherited from the opener's claim. No code, schema, flag or credential changed; `GATE1_FALSE_HOLD_CAPTURE` remains unset. **The S11 flip remains REFUSED; weights remain BLOCKED.** Next: the founder elects F-3′'s threshold; ≥2026-09-08 UTC a fresh session runs B4's actual measurement; only then can (d) proceed.**
- **2026-09-05 (RETROACTIVE ENTRIES, added by the 2026-09-05 grounding session — the two sessions below edited rows without appending here; the register's own append rule is served late rather than not at all.)** **(i) D4 ACTIVATED — `SUBSTRATE_JUSTICE_SELF_CIRCLE_NARROWING_ENABLED=true` in Vercel Production; F-3′ landed** (`D-S11-D4-REDUCER-NARROWING-ACTIVATION-LIVE-F3PRIME-LANDED-2026-09-05`; close `2026-09-05-D4-activation-and-F3prime-CLOSE.md`): the live-surface half of the 2026-07-19 self-circle ruling, as corrected by M-1, is in force — a self-only-circle assessment emits no `dikaiosyne` justice event; M-1 symmetry mutation-verified (un-gating `violated` fails pin D4-8 alone); the predicate opt-out re-verified (passing the flag to the predicate empties `loop_fold`'s live `self_regarding` bucket — the R18 claim at `llms.txt:548` holds only because the predicate does NOT receive it); F-3′ recorded in the P6 row + the scoping note §2.4/§3 as a second window precondition (threshold NOT set — a P6 design question); D1 observed still `false` across the flip with the limit named. Took-effect was NOT verified at activation (no route reads the flag; the non-regression check reads identically both ways by design). **(ii) D4 TOOK-EFFECT PROVEN; D1 WATCH SETTLED OPEN; the credential-lookup retry HELD** (`D-D4-TOOK-EFFECT-PROVEN-D1-WATCH-OPEN-RETRY-HELD-2026-09-05`; close `2026-09-05-post-D4-live-op-cluster-CLOSE.md`): a three-legged proof on a throwaway identity (local differential on the real signed artifact; positive control of three `credential-completed` rows; no `justice-surface-*` row, `justice_floor_active=false`) — `indeterminate` path only, `violated`/`met` un-exercised; all artifacts removed and verified `0 / 0 / 0`. D1: a grouped, unlimited ledger scan since 2026-07-18 shows NO `credential-completed` and NO `justice-surface-*` on `s9-loop` — the watch is OPEN, not discharged (see the D1 row's 2026-09-05 pointer to the C2 finding for why harness traffic cannot discharge it). The retry stays built-dark: zero genuine `http 401` since 2026-08-08 across 26 traffic days; an unverifiable activation was declined. **D2's priority is RAISED** by the proof (`credential-completed` still tags dikaiosyne on a self-only action, now an `increase` where it was a cap). **P4/P5/P6 unmoved; the window has not started; the S11 flip remains REFUSED; weights BLOCKED.**
- **2026-09-05 (P6 WINDOW PURPOSE WIDENED by binding ruling; `D-S11-P6-WINDOW-RECOMMENDATION-RULED-ADOPTED-PARTLY-EXECUTED-2026-09-05`, on `D-S11-P6-WINDOW-RECOMMENDATION-SCOPED-FOR-RULING-DEADLINE-PREMISE-WITHDRAWN-2026-09-05`)** — the mentor ruled on whether the new window should measure the decision table's recommendation, not only hold classification (verbatim: `2026-09-05-mentor-ruling-P6-window-recommendation-verbatim.md`; verbatim wins). **Q1 — the window's stated purpose WIDENS.** Part (3) names a false-hold rate and *"under G6(a), a hold is what a do-not-proceed produces"*, so *"the table's output — not the predicate's classification alone — is what part (3) names"*; reporting classification alone *"leaves the reader to assume the mapping from classification to hold is total"*, which the P1 ruling establishes it is not. The widening is *"a correction of the original scoping to what part (3) actually names"*, not an expansion. **Q2 — DERIVED AT REPORT TIME, never stored**, because *"the table is under active ruling… a stored recommendation would freeze the table's reading at capture time and become stale evidence that looks authoritative."* Consequence, recorded because the opposite was previously believed: the measurement needs **no capture-layer edit**, so it carries no exposure to the contamination rule and **the R2 deadline is WITHDRAWN** — the scope document withdrew its own urgency claim and the ruling accepted the withdrawal. **Q3 — the consult and guard populations are reported SEPARATELY** (not optional), with three bounds **printed on the rate, not footnoted**: the A8 bound (no re-examination counter exists, so the two-then-escalate row can never fire), the guard `depth: ""` bound, and an as-of-table disclosure. A **v3/v4 lift check** precedes any published figure. **Scope of the amendment:** §7 was added to `2026-08-15-false-hold-new-window-scoping-note.md`; §2's capture list is deliberately unchanged. **P6 is NOT discharged, not scoped further, and not ready to open; the window has not started. Nothing built, activated or published by the ruling; P4/P5 unmoved; the flip remains REFUSED.**
- **2026-09-04 (P1 BUILD + D5 CLOSURE + the frozen-buffer re-run; `D-S11-P1-AT-ACTION-SEAM-BUILT-D5-FLAG-SUPPLIED-2026-09-04` and `D-S11-P1-FROZEN-BUFFER-RECLASSIFIED-UNDER-FILTERED-READING-2026-09-04`)** — the ruling's build consequences landed (commits `50c0c0f`, `d74aea0`; pushed + Vercel green). **NEW seam** `at-action-seam.ts` — `interventionInputFromAtAction`, the ENFORCE-path sibling of `interventionInputFromS3`, reusing the existing `assessKathekonEngagement` (no new predicate); **dark, wired to no live caller.** **D5 CLOSED** (row above). **The frozen 130 re-run under the filtered reading** (`runs/2026-09-04/`, input md5 `a4e2465f3897fddeea1a189c95af39a5`): over all 130, the pre-S11b reducer gives **129 `do-not-proceed` / 1 pause** (reproducing this register's own recorded figure exactly — the harness's non-vacuity check), today's reducer unfiltered gives **129 `proceed` / 1 pause**, and the FILTERED reading gives **130 `proceed`**. **Q2's zero-false-positive floor HOLDS on both readings of the unknowable legacy circle identity — but the restoration was delivered by the S11b reducer narrowing of 2026-07-18, NOT by the P1 filter, which changes exactly ONE record of 130** (the single `indeterminate` — the same record the legacy bracket flips back). Recorded against the interest of the work: the ruled seam is correct and its effect on this buffer is one record. **A clarification, not a correction — the row above is right and was read wrongly at first:** its "over the frozen 130" means exactly that, ALL 130 records including the one `closed` one; the re-run's first pass restricted to the 129 holds and got 128 / 1, which looked like a failed reproduction until the population difference was found. Both are now reported, since the holds are the population a G6(a) bound would actually bind on. **EVIDENCE ONLY — not a readiness claim. P4/P5/P6 unmoved; no schema, flag, credential, migration, or public-doc change; the flip remains REFUSED.**
- **2026-09-04 (P1 ruling, `2026-09-04-mentor-ruling-P1-decision-table-input-verbatim.md`)** — **P1 RULED + DISCHARGED.** The at-action verdict, FILTERED by Q3's kathekon-engagement threshold, is the decision table's input for the per-action rows; Q3's *"consistent with the deliberate-proximity-without-justice-surface row"* clause is read as disclosing the table's meaning for the per-action case, and that reading is extended to the per-action rows generally. **Q2's staging premise STANDS** (the zero-false-positive floor is restored, not falsified). The aggregate is not the per-action input (Q7 depth calibration undisturbed). **Two register corrections ride the ruling:** the "three readings" framing is superseded (one wired path + two reconstructions), and reading 3 is reclassified as the accidental arrival at reading 2 — now standing item **D5**, owed independently and explicitly NOT deferrable behind P1. Scope doc: `2026-09-04-P1-decision-table-input-SCOPE-FOR-RULING.md`. **Nothing built, activated, or published; P4/P5/P6 unmoved; the flip remains REFUSED.**
- **2026-08-17 (P8a guard-path capture BUILT — entered retroactively 2026-09-04; commit `3e8f231`, "R2b item 8 (P8a): guard-path capture, so readiness part (3) gains a denominator")** — recorded here because the build is a fact this register's P5 row and the scoping note's §2.3 both described in the present tense as not having happened. `buildGuardHoldRecord` (schema `false-hold-record-v4`, carrying `path: "guard"`, `guardHold`, `guardOutcome`, `captureBasis`) is wired at three call sites in `at-action-hook.mjs` behind the existing `GATE1_FALSE_HOLD_CAPTURE` flag, so **`runGuard` does write records** — the 2026-07-17 ruling's blocker (*"the genuinely dangerous actions are on the guard path, which writes no record"*) is closed at the code level. **ACTIVATION IS OPEN, SO P5 IS OPEN:** a built-dark denominator is not a measured one, and the flag is unset, so the denominator part (3) needs remains unpopulated. Both descriptions were corrected in place on 2026-09-05 with the requirement they state left standing. **No schema, flag, credential, migration or public-doc change is claimed by this entry; it is a record correction. P4/P6 unmoved; the flip remains REFUSED.**
- **2026-08-15 (concurrent-arc C1, documents only)** — **P6 SCOPED** (design note authored: `2026-08-15-false-hold-new-window-scoping-note.md`; P8a guard-path capture named as hard precondition, solving P5's denominator; window start sequenced to R4-last per the contamination rule + the M1 window-conditional guard ruling). No build, no activation, no flag, no capture code touched; the frozen buffer untouched; `agent_hold_observations` still empty. **The flip remains REFUSED; readiness NOT met; weights BLOCKED.**
- **2026-07-19 (P0 s9-loop consult-credential DIAGNOSIS, `D-S9-LOOP-CONSULT-CREDENTIAL-DIAGNOSED-HEALTHY-NO-REFRESH-2026-07-19`)** — the "refresh" ran as a diagnosis and returned **no refresh needed: the gen-2 consult credential is healthy (DB-verified).** Founder-run `list`: the settings token hashes to `33bef3d4…` (this §E row), limits **5000/200**, monthly usage **623/5000**, daily successes peak ~57 << 200 ⇒ **quota ruled out.** At `/api/reason` the route masks EVERY `validateApiKey` failure into a 401 (quota-429, invalid_token, suspended, usage-RPC-503 all flatten via `route.ts:726`), so the 401 status is ambiguous — but with quota+suspension ruled out by the DB and the token valid (256 successes), the intermittent framing is **transient DB-layer fail-secures under load + the S11b composed-consult latency (28s timeouts)**, NOT a stale/wrong token and NOT quota. The 2026-07-18 "token no longer hashes" reading is **corrected** (it hashes to `33bef3d4…`; 623 calls recorded). Added the §E note above. **NO mint / revoke / SQL / deploy / DB change; production byte-equivalent.** The real remedy is handed off as a `code-elevated` follow-up (credential-lookup retry-before-fail-close + the lean-mode/hook-timeout latency lever), not a credential op. Register items P1–P6/B/C/D unchanged — this is not S11-flip-gating; §E is the correct home. **The flip remains REFUSED; readiness NOT met; weights BLOCKED.**
- **2026-07-19 (the SELF-CIRCLE narrowing, `D-KATHEKON-DIKAIOSYNE-SELF-CIRCLE-NARROWING-BUILT-REVIEW-FOLDED-2026-07-19` + `D-MENTOR-CONSULTATION-DIKAIOSYNE-SELF-CIRCLE-ADOPTED-2026-07-19`)** — **P2 further narrowed + PREDICATE landed** (`beyondSelfCircleCount ≥ 1`; the AE-2 activation surfaced that Layer 1 attaches `self_preservation` to every self-regarding consult, so the ≥1-circle arm still fired universally). **AE-2 `loop_fold` split RE-SPECIFIED (schema v2)** — three-way (character / self_regarding / instrument_calibration) + the dikaiosyne-evidence rule; the fold stays LIVE (MEASURE), R18 docs deferred. **Frozen-buffer re-classified** (strict 129 FP / 0 CH; legacy bracket 128 FP / 1 CH; `runs/2026-07-19/`). **New register item D4** — the REDUCER self-circle narrowing (the live-surface half; `code-critical`, founder-walked; couples with D1's cap logic). Adversarial review: the Workflow died whole on the account **monthly spend limit** (6/6 finders errored, ~1.57M tokens) → **completed FIRST-HAND per the §4 precedent, all six dimensions** — 5 CLEAN (ruling-fidelity: no broadening, `engaged` can only shrink; split-correctness: exhaustive+exclusive partition, no self-regarding hold leaks into `character.loops`; dikaiosyne-evidence: adverse evidence never dropped; legacy-bracket: `recordHash` byte-identical for v1/v2 so ingest idempotency preserved; blast-radius: `assessKathekonEngagement` MEASURE-only-consumed, live delta = the `loop_fold` block only), battery-adequacy: **2 load-bearing gaps FOLDED** (violated-on-unknown-circle; multiple/mixed self circles) → 4 new pins (kathekon §8.10–8.12, loop-fold §19.12). Two disclosed behaviours (not defects): the predicate⊂reducer divergence (D4) and a self-only reflexive-dik-only redirection reading as calibration. **NO schema / flag-set / mint / deploy / DB change; the frozen buffer untouched; `agent_hold_observations` still empty; production byte-equivalent to the AE-2 activation state (the `loop_fold` block content changes on the founder's push — flag stays `true`, the block re-specification deploys with the code).** Batteries: kathekon 105/0 · loop-fold 172/0 · capture 37/0 · logic-harness 155/0 · negative-battery 230/0 RELEASE GATE · trust-core 98/0 · s3-combiner 106/0 · emission-hooks 15/0 · route 90/90 · tsc 0 · build ✓. **The flip remains REFUSED; readiness NOT met; weights BLOCKED.**
- **2026-07-18 (S11b, `D-TRUST-LAYER-S11B-RECOMPOSITION-NARROWING-REDUCER-CAP-FIX`)** — **P3 REMEDIED in-repo** (the examined-input recomposition: `action-composer.mjs`; elections E1–E4; live in the founder loop via hot-reload; validation r2 — party-affecting 9/9 circles, **mention-without-affect FAILED 6/6, recorded as a bound**, Layer-1 re-check = named follow-up); **P2 LANDED in-repo** (predicate + reducer narrowed in tandem, both directions pinned; frozen-buffer dry-run 128 FP / 1 CH matches the S11a reconstruction — evidence, not readiness); **D1 correction PREPARED** (walk doc with pre-written rollback + the ledger-replay re-latch caveat); the ADR-014 **regime settlement DISCHARGED** (marks `at-action-v2-composed`/`at-action-v1-lean` settled once; item-5 T2-soft trigger landed; AE-1's pre-condition met once the commit pushes). Adversarial review: the Workflow died whole on the account **monthly spend limit** (7/7 finders errored) → **completed FIRST-HAND per the §4 precedent** — 1 HIGH folded at root (the v1-inherited sensitive-path **Edit snippet egress** — the lean Edit string carried ≤200 chars of `new_string`; sensitive Edits are now path-line-only in text AND summary; lean-mode Edit text redacted; zero frozen-window records show the class historically), 1 MEDIUM disclosed (ledger-replay re-latch), 3 LOW folded. Batteries: kathekon 79/0 · trust-core 98/0 · emission-hooks 15/0 · S10 106/0 · logic-harness 155/0 · capture 32/0 · **negative-battery 230/0 RELEASE GATE** · tsc 0. **The flip remains REFUSED; nothing here is progress toward it; weights BLOCKED.**
- **2026-07-17** — created (mentor R15). Seeded A/P1–P6, B/B1–B3, C, D/D1–D3 from the 2026-07-12 and 2026-07-17 binding verdicts. Standing state: **readiness NOT met; flip does not proceed.**
- **2026-07-18 (S11a, `D-TRUST-LAYER-S11A-EXTRACTION-PRIOR-NARROWING-DEFERRED-CAP-REVIEWED`)** — **P3 ANSWERED AT DIAGNOSIS LEVEL** (starved-by-composition at the harness site — Diagnostic-certain; `2026-07-18-S11a-extraction-gate-diagnosis.md`); **P2 DEFERRED behind P3** (the R12 gate ruled "prior"; the narrowing rides S11b on the recomposed input); **D1 REVIEWED** (R14 discharged on current ground; disposition disclose-and-carry, durable fix = reducer narrowing + state correction inside S11b); **P1 confirmed present** (R15's before-close check). **ADR-014 sequencing consequence:** AE-1's hard pre-condition ("S11a has settled the extraction regime") moves to **S11b** — S11a diagnosed but did not settle; the regime version-mark + the item-5 bare-tool-payload trigger ride S11b's one-time settlement (dated notes on both prompts; the item-5 fire-rate diagnostic was superseded by direct source reading — the trigger enters S11b as a remedy component). **P1's 124/125 figures re-attributed** to the briefing's 125-record snapshot (review fold — they are not derivable from the frozen 130; the frozen-130 re-derivation added: 129 do-not-proceed / 1 pause vs 130 proceed). **Adversarial review (Workflow `wf_44d7a2ed-b8b`):** 5/6 finder dimensions completed (~2.23M tokens); the 6th (cap-review-verification) + 13 of 14 verifiers died on the account monthly spend limit → **completed FIRST-HAND per the §4 precedent** — the re-latch chain verified at source (route:803 emission → :200 unevaluated → trust-transition:196 latch; content-hash idempotency ⇒ new assessments emit new events); 14 findings adjudicated: 1 HIGH (S11b sensitive-path egress control — FOLDED into S11b Part 1), 4 MEDIUM (AE-1 collision pre-folded independently mid-session; buffer counter-evidence + validation-vacuity + dedup consequence — all FOLDED), rest low/nit folded or discharged-by-design. No code/schema/flag/DB change; the frozen buffer untouched; `agent_hold_observations` still empty. Successor: `operations/handoffs/founder/2026-07-18-trust-layer-S11b-examined-input-recomposition-NEXT-SESSION-PROMPT.md`.
- **2026-07-17 (same day, post-incident)** — **D1's ground QUERIED, not inferred, and narrowed:** the public `justice_capped` rests on **ONE** `justice-surface-unevaluated` event (S9 install, `2026-07-11 05:45:29.674+00`, artifact `signed:substrate-layer2-2026Q2`) — **not** a stream from the 130 observation records, because at-action consults never become trust events (emission fires only on accreditation writes). **S11a's cap review is one event, not a stream.** Added **§E** (credential generations) after the public-credential-exposure incident: gen-1 revoked, gen-2 live, identity deliberately preserved.

- **2026-09-08 (S8, the Option D ruling's owed Option C investigation, RESULT: superseded, not settled; `D-S8-OPTION-C-INVESTIGATION-AGENTID-CANDIDATE-FOUND-2026-09-08`)** — the bounded question (does `SubagentStart` correlate to an H3 record via a key other than session id?) was answered **neither YES nor NO on its own terms**: `SubagentStart` was never observed, because a live `GATE1_DEBUG` capture of H3's own `PreToolUse` stdin (founder-waived, one non-repo config edit, reverted SHA-verified same session) found `agent_id`/`agent_type` **present directly in H3's payload when fired inside a subagent, and absent when fired at the top level** — a field pair NOT in the 2026-06-20 baseline capture the standing memory records. `session_id`/`transcript_path` re-confirmed non-discriminating on the current client (2.1.260). **This supersedes Option C's premise** (no `SubagentStart` marker or correlation key would be needed) **but is NOT built and NOT ruled on** — relayed to the mentor (`2026-09-07-mentor-question-caller-class-agentid-in-h3-FOR-RULING.md`) per the same governing discipline that falsified Option B: whether a top-level session can ever legitimately carry an `agent_id`-shaped value is unchecked (`CLAUDE_CODE_CHILD_SESSION=1` observed set at the top level this session, unchased, out of scope). **`caller_class` distribution re-measured: 30 post-boundary v5 records, 0 `subagent`, invariant holds.** **Baseline MOVED to 2 of 5** — PR19 (this same session, sonnet/low) found the buffer had grown while under review, traced precisely to this session's OWN Write/Edit tool calls authoring its required governance documents (not review-fleet noise, checked and ruled out); this is the first baseline movement this window, driven entirely by documentation-writing rather than substantive work, named for the founder as a fair-count question, not decided here (`D-S8-CORRECTION-WINDOW-HEALTH-STALE-BASELINE-MOVED-2026-09-08`). **Nothing built, nothing committed. Option D stands as the live disclosure; whether it is revisited awaits the mentor's ruling on the new candidate. P4/P5/P6 unmoved; the flip remains REFUSED; weights BLOCKED.**

- **2026-09-08 (S8 — the five-question ruling; `D-MENTOR-RULING-S8-FIVE-QUESTIONS-ADOPTED-2026-09-08`; verbatim, canonical: `2026-09-07-mentor-ruling-S8-five-questions-verbatim.md`)** — **ADOPTED AS BINDING.** **(1) Option D is CONDITIONALLY REOPENED.** The `agent_id`-at-H3 finding is accepted (the `tool_name: Bash` provenance proof credited as *"exactly the discipline the ruling's method requirement asked for"*); the candidate is named **Option C′**, H3 self-discrimination. **THREE CONDITIONS, ALL REQUIRED, before `classifyCaller` may be modified:** a **false-positive check under controlled conditions** whose *"first thing… must examine"* is the unchased `CLAUDE_CODE_CHILD_SESSION` question (*"a parent session acting with `agent_id` set… would be the spawn-depth failure mode in a different form"*); a **second live capture on a separate day and session**; and **client-version pinning with fallback to `unknown` on field absence**. *"The investigation is not complete until Condition 1's `CLAUDE_CODE_CHILD_SESSION` question is answered."* **NOTHING EXECUTED — the amended segment-1 clause stands exactly as Option D left it** (*"a candidate mechanism, however promising, does not change what the disclosure may currently claim"*). **(2) Baseline: the documentation-only day COUNTS** — part (1)'s representativeness clause is satisfied because Write/Edit *"are actions the instrument examines… in the measured population by design"*; filtering rejected as *"a post-hoc narrowing of the population mid-window."* **Baseline 2 of 5, composition disclosed.** **(3) G6(a) GAINS A RULED FURTHER QUALIFICATION** — it does NOT bind on this class; the boundary is sharper than the question proposed: *"not between documentation and engineering in general… between actions the protocol requires and actions the agent elects."* **G6(a) binds on kathekon-engaged loops opened by consequential actions on the product or its governing documents, NOT on the agent's own protocol-required record-keeping** — because *"the protocol's own requirements"* should not *"generate a do-not-proceed condition on the act of following the protocol."* Carried as a **disclosed property, not a filter on the loop count**. Unbuilt. **(4) TWO NEW PRE-FLIP OWINGS:** a **baseline composition disclosure** (day-by-day, tool distribution) and a **loop count by action class** (product vs. protocol-required documentation), both owed in the pre-flip report alongside the false-hold rate — leaving the latter to flip time is *"the move the window was designed to prevent."* Neither built; `false-hold-observation-report.ts` matches `GUARD_RE`, so each needs a founder waiver and its own session. **Nothing built, activated or published. P4/P5/P6 otherwise unmoved; S11-D2 still BLOCKED at 2 of 5; the flip remains REFUSED; weights BLOCKED.**

- **2026-09-08 (S8 — a convention deviation by this session, recorded rather than left silent)** — the S8 row dated 2026-09-08 above it (the Option C investigation result) had its baseline figure **edited IN PLACE** by the same session that wrote it, when PR19 found the figure stale (1 of 5 → 2 of 5). The justification offered at the time was that a same-session correction to a row not yet built on is continuing to write that row rather than rewriting history. **That is a defensible reading but it is not what this register's own rule says** — the rule is append-only, and the reasoning above is the shape of argument the rule exists to refuse. Recorded here as an append so the deviation is visible in the history rather than invisible in a silently-corrected row. **The figure itself is correct as it now stands (2 of 5, ruled and confirmed).** Founder notified; disposition — accept the in-place edit with this note standing as its disclosure, or revert the row to its original text and carry the correction only here — remains the founder's.

- **2026-09-08 (S9 — Condition 1 of the five-question ruling's three-condition Option C′ gate; `D-S9-CONDITION-1-AGENTID-PARENT-SESSION-PASSES-2026-09-08`; evidence: `2026-09-08-condition-1-agentid-parent-session-CAPTURE-EVIDENCE.md`)** — **CONDITION 1 PASSES, on client 2.1.260 / `claude-desktop`, with disclosed limitations. `classifyCaller` is byte-unchanged; nothing built or activated.** A founder-waived `GATE1_DEBUG` capture (config-file-only, byte-verified reverted by SHA, though the runtime effect itself lagged the revert — see below), driven by a sentinel-content-provenance method (a session-original addition beyond S8's, needed because the single-slot dump collides under concurrent fleet activity and the alternative, per-invocation filenames, would edit a `GUARD_RE`-matched file), tested four configurations. **The `CLAUDE_CODE_CHILD_SESSION` question the ruling named as the first thing to examine was answered directly:** a top-level session's H3 capture carries no `agent_id` even while that env var reads `1` in its own tool environment — reconfirmed in a second, fresh session (S8's finding was not a one-off). **Configuration 1 (nested delegation):** the intermediate carries its OWN `agent_id`, never the parent's and never none — ruled `consistent, not a falsification` per the ruling's own stated criterion (a "parent" relative to its child is not a top-level session). **Configurations 2+3 (background/concurrent, the case the ruling's reasoning turns on):** a top-level capture taken while two background agents were genuinely in flight — corroborated three independent ways (no completion notification yet, the very next read of the same slot overwritten by a live agent with no intervening Bash of the author's own, both agents' subsequent 20-tool-use reports) — carries NO `agent_id`. **Configuration 4 (worktree-isolated, untested at S8):** carries `agent_id` normally; `cwd` alone differs, as S8 found. **Configuration 5 (a different entrypoint): NOT attempted — no `claude` CLI on this machine; disclosed as a real gap, not discharged.** **A live artifact-level provenance check (not present at S8) closed one of the ruling's own open questions first-hand:** `maybeDebugDump(cfg, raw)` dumps `raw = readStdin()` verbatim, BEFORE `JSON.parse` — the captured `agent_id` is Claude Code's own wire, not a hook-synthesized value. **Two genuine defects were found and corrected within the same session, both by PR19 (sonnet/low), neither smoothed over:** the revert-lag S8 first observed reproduces AND sharpens into an ASYMMETRY — the addition of `GATE1_DEBUG` took effect within one tool call, the removal had NOT taken effect at all by session close (sentinel-proven, not inferred from timestamps); and the closing window-health figures were stale by one buffer record at write time (guard 129→130, 2026-09-07 guard 53→54, "98 of 129" `contrary`→98 of 130 with a previously-unnamed `null` third bucket of 2, and this session's own contribution 1→2 records — caused by an ad-hoc time filter that excluded an earlier record from the same session), corrected by APPENDED addendum in the evidence file rather than an in-place edit (the S8 in-place-edit deviation above this row was deliberately not repeated). An incidental instrument-composition finding: this session wrote a 163-line governance document via Bash heredocs and generated ONE guard record and ZERO consult records — the consult denominator is sensitive to which tool a session happens to author with, not only to action class; the ruled baseline-composition disclosure and the owed loop-count-by-action-class report should read this as a live caveat, not adjusted for here. **Conditions 2 (a second capture on a separate day and session) and 3 (client-version pinning with fallback) remain OUTSTANDING — this session's UTC day is 2026-09-07, the SAME UTC day as S8's, on the SAME client 2.1.260; whether that satisfies "separate day and session" is named for the founder/mentor, not decided here.** PR19 ran all three dimensions (substantive claim, arithmetic, scope/safety); dimension 1's reviewer disclosed it had not read source before answering and its live points were closed first-hand instead of trusted; dimension 2 found the two genuine defects above; dimension 3 returned CLEAN (no guarded file touched, both SHA pins held, `classifyCaller` byte-unchanged, nothing committed or pushed, no stray worktree, no credential leaked). **NO BUILD IS LICENSED by this result. The flip remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**

- **2026-09-08 (S9 — the four-question ruling; `D-MENTOR-RULING-S9-FOUR-QUESTIONS-ADOPTED-2026-09-08`; verbatim, canonical: `2026-09-08-mentor-ruling-S9-four-questions-verbatim.md`)** — **ADOPTED AS BINDING. All four S9 recommendations UPHELD; two changed in force or scope.** **(1) Condition 2 is NOT discharged by S9's capture** — a same-UTC-day, same-client capture *"confirms the finding holds across session IDs… but it does not discharge what Condition 2 exists to establish"* (what varies with wall-clock conditions: server state, session initialisation, client behaviour across time). **The next capture must be a genuinely different CALENDAR DAY, a different SESSION ID, and the SAME client version (2.1.260)** — the same-client requirement is newly explicit, and the day boundary is *"literal, not approximate."* **(2) Condition 1 is COMPLETE without Configuration 5** — it is *"scoped to the parent-versus-agent distinction on the current harness as wired"* and passed on that scope; a second entrypoint *"would duplicate work Condition 3 already exists to do."* **The untested-entrypoint gap S9 carried is therefore CLOSED as a Condition-1 item and belongs to Condition 3.** **(3) THE ONE NEW BINDING OBLIGATION — the `GATE1_DEBUG` pre-check is ELEVATED from a founder question to a STANDING REQUIREMENT.** Before **any** session runs `GATE1_DEBUG` again, including Condition 2's capture session, **both** are required and **neither is optional**: (i) `rm ~/.sage-gate1/*-stdin.json`; (ii) **open a FRESH session, run a control probe, confirm no fresh dumps appear** — if dumps appear the variable is still live and *"its persistence mechanism must be understood before it is used again"*. Grounds: *"A debug session run without this confirmation is a debug session whose outputs cannot be trusted to reflect the current wire rather than a residual state"* — *"the same class of invisible contamination the project has ruled against consistently."* **NEITHER STEP WAS PERFORMED BY S9** (the probe requires a fresh session by construction; the `rm` was scoped to the founder at S9's own prompt) — both are now **prerequisites on Condition 2's capture session**, not hygiene. **(4) The instrument-composition finding stays a DISCLOSED CAVEAT — correcting mid-window refused as *"the post-hoc narrowing of a population the project has ruled against twice"* — but the pre-flip disclosure obligation is ENLARGED:** it must be folded in *"not only as a named limitation but as a named structural dependency"*, stating that the consult denominator's composition is partly a function of tool mode, tool mode partly a function of the kind of work a session does, and — the formulation S9 had not reached — that the measured population **"is not a random sample of all actions the agent takes — it is a sample of actions taken with Write/Edit/MultiEdit/NotebookEdit tools"**; a reader *"should see that dependency clearly, not discover it in a session's evidence file."* Folded into the already-required loop-count-by-action-class report as a **parallel disclosure** (loop counts by action class; consult denominator composition by tool mode), *"both named as structural properties of the measurement rather than session-specific artefacts."* **Neither report is built** — `false-hold-observation-report.ts` matches `GUARD_RE`, so each needs a founder waiver and its own session. **Nothing built, activated or published. Condition 1 CLOSED; Conditions 2 and 3 OUTSTANDING. P4/P5/P6 unmoved; S11-D2 still BLOCKED; the flip remains REFUSED; weights BLOCKED; the 0h call remains the founder's.**

*Append entries with a date + the binding source. If an item is discharged, mark it and say where — do not delete it; the point of this file is that nothing here gets rediscovered from scratch.*

- **2026-09-09 (S11 — Condition 2 of the five-question ruling's three-condition Option C′ gate; `D-S11-CONDITION-2-REPRODUCTION-PASSES-2026-09-09`; evidence: `2026-09-09-condition-2-reproduction-CAPTURE-EVIDENCE.md`)** — **CONDITION 2 PASSES on a genuinely different calendar day (2026-09-09 under both AEST and UTC), a different session, same client 2.1.260. `classifyCaller` is byte-unchanged; nothing built or activated.** STEP 0's date gate ran first and passed by inspection (both `date` and `date -u` return 09-09); the S9-elevated two-step `GATE1_DEBUG` pre-check ran clean (no stale dump; a fresh control probe wrote none) — confirming the variable was **inert at session open**, a boundary S9 could not observe. A founder-elected `GATE1_DEBUG` capture (AI-performed, sequential, config-only, byte-verified by both a whole-file SHA and a per-key SHA of the `env` block before/after — all 11 pre-existing values unchanged, one key added) took two sentinel-verified `tool_name:"Bash"` H3 captures: top-level control (`agent_id`/`agent_type` **absent**, keys absent not empty) and agent-issued (`agent_id`/`agent_type` **present**, `Explore`) — the finding reproduces exactly as at S8/S9. **A stronger-than-required corroboration was found: the parent's own `PostToolUse` dump for the `Agent` tool call reports `tool_response.agentId` byte-matching the child's H3 `agent_id`** — the subagent could not self-report its id (asked, answered "not known to me"), so the harness's own spawn report substituted and agreed. **The client-version condition is satisfied but by a different route than the prompt assumed:** `version`/`entrypoint` are **absent from the H3/H4 hook payload itself** (both captures' full key lists confirm it) — S9's evidence-file header describing them as read "off the hook payload" was imprecise; they were verified instead by scanning this session's own transcript JSONL, where 88 lines carry `version:"2.1.260"`/`entrypoint:"claude-desktop"`, single-valued throughout. Named as a genuine input to Condition 3 (a runtime version read would need `transcript_path`, which IS on the payload, plus a file read and parse — a materially different design from reading a field already in hand), not a licence to start it. **The revert-lag reproduced and is now bounded, not merely repeated:** the config was restored and SHA-verified byte-identical to the pre-session backup, `GATE1_DEBUG` absent by grep — and a fresh post-revert sentinel probe still produced a dump, exactly as at S9. New this session: because the *pre-check at open* (fresh session, key absent from file) found the variable genuinely inert, while the *within-session* revert did not, the residual is now bounded to **not surviving a session boundary** — offered as a hypothesis consistent with two sessions' observations (env applied into a running process cannot be retracted by a file edit; a new session rebuilds its environment from the file), explicitly not proven, and not further tested (out of this session's scope). **Window health re-derived at close, with one method correction to the log-anchor discipline itself:** the anchor the prompt specifies (the log line, not the buffer's `capturedAt`, because the same event double-stamps) turned out to be the took-effect probe's own log line — anchoring inclusively there double-counts an event whose buffer record (#139) is excluded by rule; the correct anchor is the line immediately after it, found by 1:1 pairing every window `GUARD-*` log line to a buffer record, which left exactly one log line unmatched (the anchor itself). At the corrected anchor, log and buffer agree exactly in both families (`GUARD-*` 203=203, `CONSULT` exact-token 27=27). **This session contributed ZERO records to the window** (verified by the buffer's own `session` field) — it authored entirely via Bash heredocs (including its own evidence file), which the log confirms logs `AT-ACTION-SKIP-BASH` and enters neither the consult nor the guard floor; the starkest instance yet of the ruled instrument-composition dependency. Baseline composition now carries one `website/src` engineering consult (up from S9's zero) alongside 19 operations-doc + 3 other-md + 4 scratchpad. **One discrepancy flagged, not resolved:** the buffer yields four distinct UTC days present, all four with ≥1 consult, against the prompt's carried "2 of 5" — reported as a derivation mismatch rather than reconciled by assumption. PR19 ran (dropped to sonnet/low for the review per founder standing permission, returned to opus/medium after) across the three S9-precedent dimensions; no defect found in the arithmetic this time (both the anchor correction and the zero-contribution finding were caught first-hand before review, not by it). **Condition 3 (client-version pinning with fallback) remains OUTSTANDING, sharpened by §4's correction above. `classifyCaller` byte-unchanged; nothing built. The flip remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**


- **2026-09-10 (S11/Condition 3 — client-version-pinning DESIGN executed;
  `D-CONDITION-3-VERSION-PIN-DESIGN-2026-09-10`)** — the third and final condition of the 2026-09-07
  five-question ruling was designed, not built: `classifyCaller.ts` and every `GUARD_RE`-matched path
  remain byte-unchanged, verified at open and close. The obvious implementation (read `version` off
  the head of the session transcript) was tested against real data and found wrong: a scan of 400
  transcripts on this machine found 17 (4.25%) carry more than one `version` value, including two
  straddling exactly the pinned boundary `2.1.258 → 2.1.260` — a head-read would report the
  session-start version while the live client had moved on, reproducing the silent-misclassification
  failure Option D exists to prevent. **The design specifies a tail-read instead** (scan backward from
  end-of-file for the nearest `version`+`entrypoint`-bearing transcript line, since neither field rides
  the hook payload itself — confirmed by key-list inspection of existing dumps, no new capture taken),
  gated on version AND entrypoint AND field-presence, all three AND-ed, falling back to `unknown` on
  any single failure. Widens ruling 3's deliberate two-value vocabulary to three (`subagent`/
  `live_agent`/`unknown`), argued as licensed by Condition 1's closed negative result and explicitly
  named as reversible if that finding is ever overturned. Configuration 5 (the untested second
  entrypoint) is argued — not assumed — safe-by-construction ONLY once `entrypoint` is pinned alongside
  `version`; a version-only gate would let an untested-but-same-version CLI pass with its `agent_id`
  wire behaviour entirely unverified, which the design's own failure-mode table names as the one
  genuinely dangerous case. Two build-traps in the existing report code were surfaced by reading it,
  not assumed: `recordHash` doesn't hash `schema` (a v6 bump is hash-safe if new fields stay top-level,
  mirroring the v4→v5 precedent) but the report's validator hard-codes `callerClass ∈
  {'subagent','unknown'}` for v5 — an unextended validator would silently DROP every future
  `live_agent` record from the population, a data-loss failure dressed as a schema check; both must be
  fixed in the same commit as any build. **Adversarial self-review (model dropped to Sonnet 5 per
  founder standing permission for this step) found and folded a real overclaim before this record was
  written**: the design's first draft claimed the proposed 64 KB tail-scan window was "comfortably
  larger" than worst-case inter-version-line spacing; direct measurement of this session's own
  transcript found a single 1,142,339-byte line (17x the window) — the claim was retracted and
  replaced with the actual finding (the giant line itself happens to carry `version`, so the scan
  still succeeds in practice; correctness is unaffected either way since the fallback covers a failed
  scan by construction, but the claimed margin was false as stated). **Baseline days re-derived a
  second consecutive session at 4 of 4** (reconciling the still-carried "2 of 5" discrepancy in the
  same direction as S11/Condition 2); per-day guard/consult counts and gate1.log 1:1 pairing (incl. the
  probe-partner correction the immediately-prior session first identified) both reproduced exactly.
  This session contributed ZERO records to the buffer (no Edit/Write tool call; every write went
  through Bash heredocs), the starkest instance yet of the ruled instrument-composition dependency.
  The stale CLAUDE.md `GATE1_DEBUG` claim (already twice-deferred) was corrected precisely — the true
  state is a three-leg sequence (inert at S11/Condition-2's own open → live again after that session's
  in-session revert → inert throughout this entire session) — rather than restated a third time.
  **Nothing built, nothing licensed. A relay asking whether Conditions 1–3 jointly discharge the gate,
  and which of the two named judgement calls (the `live_agent` value; the entrypoint pin) the
  mentor/founder prefers, is outstanding: `2026-09-10-condition-3-gate-discharge-RELAY.md`. The flip
  remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**

- **2026-09-10 (same session, post-mentor-ruling — `D-S11-CONDITION3-CLASSIFYCALLER-BUILT-2026-09-10`)**
  — the mentor ruled the three-condition gate DISCHARGED and licensed the build in the same session
  the design above was authored. Built: `classifyCaller` (three-value gate — version+entrypoint+
  agent_id-presence, all AND-ed, `unknown` on any failure), schema bump to `false-hold-record-v6`,
  and the report's validator/vocabulary/disclosure extended to admit it without dropping records.
  A genuine test-authoring bug (a field-name mismatch that made two of three new end-to-end pins
  pass for the wrong reason) was found and fixed before commit, not after. Harness batteries 67+256+
  173+24+70 = 590 assertions green; report battery 117/0; `tsc` clean. The `/logos` live byte-identity
  guard was observed going RED while the tree was dirty (confirming it is a working-tree check, not a
  permanent freeze) and is expected GREEN again once this commits. SHA pins on `layer2-mechanisms.ts`
  and `stoic-brain.ts` re-verified clean vs HEAD, unchanged. **`classifyCaller` is no longer inert —
  Option C′ is live in the harness code as of this commit.** Full record:
  `operations/handoffs/founder/2026-09-10-condition-3-version-pinning-design-CLOSE.md`.

- **2026-09-10 (post-build verification session — `D-S11-CONDITION3-NULL-ANOMALY-DIAGNOSED-2026-09-10`)**
  — read-only verification, nothing built or changed. `classifyCaller`/`false-hold-capture.mjs`/the
  report file untouched (confirmed via `git status`; none of the working tree's six dirty/untracked
  entries match `GUARD_RE`). **The null/null anomaly the build session's close document disclosed as
  unresolved is now diagnosed with direct evidence, not merely re-hypothesised:** the other session's
  transcript (`f4a10611-40cd-45b9-989f-293a2df009fd`) genuinely carries `version`/`entrypoint` on the
  transcript line closest to each anomalous capture timestamp (ruling out "nothing to find"); the build
  session's own Bash-tool command timestamps place both anomalous captures (19:27:17Z, 19:28:21Z)
  squarely inside the 19:25:00Z–19:29:27Z window during which `false-hold-capture.mjs` was being
  rewritten via sequential `sed`/heredoc edits, with the first passing `node --check` only at
  19:29:54Z. The edit-window hypothesis is corroborated by timestamp correlation, not confirmed by
  reconstruction of the exact bytes read — reported at that honest confidence level. The mechanism has
  since fired correctly a fourth time (a sixth v6 record, `live_agent`/`2.1.260`/`claude-desktop`,
  captured 2026-09-10T06:16:26Z on the build session's own id), consistent with the null/null reads
  being a one-time build-timing artifact rather than a standing defect. **Window re-derived: buffer 423
  lines, 0 unparseable; window (post-probe) 284 records = 47 consult + 237 guard; schema v3=47/v4=96/
  v5=135/v6=6; baseline days = 5 of 5** (a new UTC day, 2026-09-10, entered the window since the prior
  session — reconciling forward from the two prior sessions' independently-agreeing 4 of 4). A naive
  `GUARD-CAUTION`-log-line-count-per-day cross-check against the buffer's per-day guard count did NOT
  reconcile 1:1 this session (log counts ran higher than buffer counts on most days, most sharply
  09-06: 138 log lines vs 76 buffer records) — reported as an honest non-match rather than forced to
  agree; CONSULT counts DID reconcile closely on the four non-09-06 days. This discrepancy is not
  resolved here and is named for a future session rather than explained away. `/logos` byte-identity
  guard green (250/0); SHA pins on `layer2-mechanisms.ts`/`stoic-brain.ts` unchanged, clean vs HEAD. A
  relay reporting the build's live, corroborated outcome and the anomaly's diagnosis was sent to the
  mentor this session (`2026-09-10-post-build-outcome-RELAY.md`) rather than held for a longer
  observation period — judged sufficient given the build was already self-corroborating before this
  session opened. **Nothing built, nothing activated. The flip remains REFUSED; weights remain
  BLOCKED; the 0h call remains the founder's.**

- **2026-09-10 (guard population reconciliation — `D-GUARD-LOG-BUFFER-RECONCILED-TRAPS-RECORDED-2026-09-10`; evidence: `operations/trust-layer-2026-07/2026-09-10-guard-log-buffer-RECONCILIATION.md`)** — **The carried GUARD-CAUTION log-vs-buffer non-reconciliation is CLOSED. The guard POPULATION COUNT reconciles exactly: 258 = 258 as of 2026-09-10T07:06:10Z, zero session mismatches, zero strict token failures, max |Δt| 11 ms.** Bears on the **F-3′ standing obligation** to *"report the guard population separately with its rate"*: the guard-side **denominator** is now verified against the log and therefore trustworthy. **The rate itself is NOT computed here and is not moved.** There was never a data discrepancy — three compounding measurement errors: the log not clipped to the window start (09-06 only, ~9.8 h of pre-window traffic); one token compared against the whole guard family (which **deflates** every fully-in-window day, making the carried *"log ran higher on every day"* imprecise — it runs **lower** on 09-07/08/09); and TRAP-1. **TWO NEW TRAPS, both bearing on the pre-flip report. TRAP-1: `GUARD-OUTAGE` log lines carry NO `tool=` field** — any regex keyed on it drops them silently (verified universal: 0 of 687 across the whole log). Because outage records are excluded from the rate denominator **by ruling** and the outage rate is **reported separately**, this bug makes that separately-reported rate read **zero** — a silent false negative in an obligated disclosure. **TRAP-3: "record N" in this project's records is 1-INDEXED** — CLAUDE.md's *"record 139 is the first v4"* is 0-based index **138**; the probe sits at 0-based 138 and the window is 0-based **139 onward**. Two sessions have now read that sentence two different ways; **this document's own first draft got it wrong** and was corrected by PR19. Also recorded: the log token is **lossier than the buffer** — `GUARD-CAUTION` covers both `pause_for_review` and `proceed_with_caution`, so caution **grade** is not recoverable from the log; the buffer is authoritative. **Attribution corrected by the author after PR19 passed:** the 1:1 pairing method was **established at the 2026-09-09 Condition-2 session** (its own row records `GUARD-*` 203=203, `CONSULT` 27=27); this session **reproduced** it across all five days, diagnosed the failure modes, and added the traps — it did not invent it. Neither reviewer was pointed at this register and neither caught the attribution gap. **PR19: two blind reviewers (Sonnet), both folded** — one upheld a real off-by-one in the window anchor (TRAP-3, verified first-hand before folding; both anchors independently tie, so the headline held and the count was corrected 75 → 76 on 09-06); one re-ran the reconciliation at a later moment and got 250=250 against the author's 243=243, the method reproducing under buffer growth. **Nothing built, nothing activated, no `GUARD_RE` file modified, buffer not refreshed. Baseline day count untouched. The flip remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**

- **2026-09-10 (logos-on W1 + W3 opened — `D-LOGOS-ON-W1-W3-EXECUTED-2026-09-10`)** — **§F above
  added**: the four W3 register annotations (a)–(d) per the 2026-08-01 logos-on plan, plus the two
  named-not-built pins (orientation reading never an enforce input; no enforcement path keying on
  first-circle-only findings) — both matching `GUARD_RE`, correctly not built here, left for a
  founder-waived session. W3's dependency gate (C1c + C2 + D4 all closed) independently re-verified
  from `/CLAUDE.md`'s Live-in-production list before annotating, not merely quoted from the
  Cognitive-OS standing-close prompt that named it unblocked. **Same session: W1 (the ADR-level
  documentation/justification workstream) also executed** — see `adopted/adr/2026-07-08-sage-trust-layer.md`
  §11-dated-amendment and `adopted/adr/2026-06-24-sage-practice-measurement-instrument-reframe.md`'s
  matching amendment for the fifth-circle-instantiation framing; the R18 public-surface component of
  W1 is **drafted, not applied** — held for founder sign-off per the plan's own "sign-off-before-touch"
  discipline for public surfaces. Full record:
  `operations/handoffs/founder/2026-09-10-logos-on-W1-W3-CLOSE.md`. **Nothing production-facing
  changed. No `GUARD_RE` file touched. The S11 flip remains REFUSED; weights remain BLOCKED; the 0h
  call remains the founder's.**

- **2026-09-10 (Q-G6A-QUALIFICATION + Q-PREFLIP-REPORTS BUILT — `D-Q-G6A-QUALIFICATION-AND-Q-PREFLIP-REPORTS-BUILT-2026-09-10`; founder-waived, `GUARD_RE` touched under a recorded, scoped waiver — same session).**
  **Skipped W2** (out of scope, no verified dependency into this pair — the founder confirmed the
  earlier-drawn W2→G6A→PREFLIP chain, sourced from the "Practice, Logos, Idea" artifact, had no
  ruling backing it). Built directly against the two binding rulings instead: a new **Part 5** in
  `website/scripts/false-hold-observation-report.ts` (`reportPreFlipDisclosures`) covering the
  schema distribution (window-scoped), the edit-window anomaly count, the `live_agent`
  non-emission-window fact (all three from the 2026-09-10 Q1 ruling), and the baseline composition +
  loop-count-by-action-class figures (from the 2026-09-07 S8 ruling, Q-G6A-QUALIFICATION + the two
  Q-PREFLIP-REPORTS items) via a new heuristic classifier `classifyActionClass`. **PR19 independent
  review found 3 real defects in the first draft, all confirmed and folded at the root**: (HIGH) 5a
  had used the whole buffer, not the window, contradicting the verbatim "schema distribution IN THE
  WINDOW" convention (`2026-09-10-post-build-outcome-RELAY.md:52`) — fixed; (MEDIUM/HIGH) the
  action-class fallback regex missed governing documents (`manifest.md`/`CLAUDE.md`/ADRs) entirely,
  defeating the qualification's own purpose — fixed and regression-pinned; (MEDIUM) a stray v1
  record past the window's index-slice prefix would not have been excluded — fixed with an explicit
  schema filter. Battery **133/0**; `tsc` clean; real-buffer smoke re-confirmed baseline still reads
  5 of 5 after the fix. Full record:
  `operations/handoffs/founder/2026-09-10-Q-G6A-QUALIFICATION-and-Q-PREFLIP-REPORTS-CLOSE.md`.
  **Nothing activated; this is a diagnostic script, no DB write; the measured false-hold rate itself
  is still NOT computed. D2 remains blocked (Q1's "ordinary" condition, not counting). The S11 flip
  remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**

- **2026-09-11 (Part 3 corrected to a regime-scoped readiness answer — `D-S11-PART3-REGIME-SCOPED-CORRECTION-2026-09-11`; a FRESH, separately-recorded founder waiver, mentor-ruled, PR19-reviewed).**
  **Part (3) of the S11 four-part readiness standard now reads MET.** Running the newly-built Part 5
  against the real buffer found the false-hold rate's POOLED headline reads NOT MET (137
  false-positive, 61 correct at the time) — but the report's own ADR-014 regime-split diagnostic shows
  those 137 false positives are ENTIRELY the retired `at-action-v1-lean` pre-window instrument (137
  false / 0 correct); the CURRENT instrument, `at-action-v2-composed`, reads **0 false-positive
  holds, 61+ correct holds**. Put to the mentor
  (`2026-09-11-mentor-question-regime-mixed-false-hold-rate-FOR-RULING.md`) rather than decided
  unilaterally. **Ruled** (verbatim, canonical:
  `2026-09-11-mentor-ruling-regime-mixed-false-hold-rate-verbatim.md`): the regime-scoped figure
  governs part (3) — "the pooled figure ... does not describe either instrument ... [using it] is
  evaded, not answered" — the pooled figure may appear only as a disclosure, never the headline; the
  correction needed its own **fresh, separately-recorded waiver** (granted, recorded BEFORE the edit
  per the ruling's explicit instruction:
  `2026-09-11-founder-waiver-part3-regime-correction.md`); part (3) reads **MET with an
  instrument-age qualification** (not a small-sample flag), verbatim form supplied by the ruling and
  now reproduced in the script's own output. **PR19 found one real HIGH**, confirmed by live
  reproduction: the naive `fps <= corrects` comparison reads `0 <= 0 = true`, so an operative regime
  with records but ZERO holds would have printed a dishonest "MET" on no hold evidence at all — fixed
  with an explicit non-vacuity guard (`operativeHoldCount > 0`), now printing a distinct "NO HOLDS
  YET" state instead, regression-pinned. Battery **141/0**; `tsc` clean; real-buffer smoke
  re-confirmed **MET** (0 false-positive, 73 correct as of this write) after every fix. Full record:
  `operations/handoffs/founder/2026-09-11-part3-regime-scoped-correction-CLOSE.md`.
  **Part (2) still needs a live DB pass; the S11 flip itself remains a separate, founder-walked
  Critical activation — a corrected readiness figure is not a flip. D2 remains blocked (a different
  condition). The S11 flip remains REFUSED; weights remain BLOCKED; the 0h call remains the
  founder's.**

- **2026-09-12 (Part (2) run against production + the S11 PRE-FLIP REPORT assembled and PR19-folded — `D-S11-PREFLIP-REPORT-ASSEMBLED-PART2-RUN-2026-09-12`).**
  **The pre-flip report the 2026-07-12 verdict asked for exists:**
  `operations/trust-layer-2026-07/2026-09-12-S11-PRE-FLIP-REPORT.md`, evidence file
  `operations/trust-layer-2026-07/runs/2026-09-12/observation-report-PRODUCTION-RUN.txt`.
  **Part (2) was run non-dry-run against PRODUCTION** (founder-directed target; read-only pre-check
  first; 492 records ingested into `agent_hold_observations`, all new, idempotent on `record_hash`;
  no schema/flag/credential change). **The four-part board:** (1) **NOT YET MET on the window's own
  clock — 5.54 days**, reaching seven on 2026-09-13T09:44:55Z (the script's printed "61.39 days ⇒
  MEETS" is a pooled-buffer-span artifact, **disclosed not corrected** — same file matches
  `GUARD_RE` and no waiver covered a Part-1 edit); (2) **NOT MET — 1 of 4** by the trust-state
  reading, **and PR19 sharpened this**: the same run file also prints **4/4 by a records-proxy
  reading**, so the examinations DID touch all four domains — the gap is that engagement has not
  become an accreditation-write-derived state row. **Structurally blocked by the emission path, not
  by time** (D1's own 409-before-emission cause); (3) **MET** (0 false-positive / 73 correct under
  `at-action-v2-composed`); (4) **SATISFIED** + the B2 qualification disclosed.
  **PR19 ran as a 4-lens / 30-agent adversarial workflow with two refuters per finding: 13 findings
  raised, 13 survived, 0 refuted, all folded at the root and re-verified first-hand.** Two were HIGH:
  the omitted records-proxy 4/4 figure (above), and the report's failure to name the 2026-09-10
  ruling's **Q2** — an explicit precondition on writing this document — now disclosed as **already
  discharged** by the earlier 2026-09-10 retroactive check. The most-repeated finding (four raises):
  §4.1 silently substituted a more precise pre-window breakdown (137 fp / 0 correct / 1 not-a-hold)
  for the ruling's dictated *"n=138, all false-positive"* — now **disclosed as a divergence**,
  surfaced for the founder and mentor alongside the v4=96 item, **not corrected in the ruling**.
  **Also fixed:** two unmarked elisions in the §0 verbatim blockquote, a misattributed quote
  (*"harness traffic can never discharge it"* is not in D1), and a secondary figure that did not
  reproduce (2,850 → **2,846** AT-ACTION-SKIP-BASH, exact-bound re-derived).
  **A `GUARD_RE` near-miss caught and closed:** the evidence file was first written as
  `false-hold-observation-report-PRODUCTION-RUN.txt` — whose **name** matches `GUARD_RE` via the
  "false-hold" substring and would have tripped the armed guard the moment it was staged. Renamed to
  `observation-report-PRODUCTION-RUN.txt`; both it and the report itself now test `false`.
  Guard 250/0; SHA pins unchanged; no production/app surface touched.
  **The report is a record, not a licence: it computes nothing binding, and its own §11 poses three
  open questions rather than recommendations. D2 remains blocked; the S11 flip remains REFUSED;
  weights remain BLOCKED; the 0h call remains the founder's.**

- **2026-09-12 (THE PRE-FLIP REPORT IS RULED ON — `D-MENTOR-RULING-PREFLIP-REPORT-THREE-QUESTIONS-ADOPTED-2026-09-12`; verbatim, canonical: `operations/trust-layer-2026-07/2026-09-12-mentor-ruling-preflip-report-three-questions-verbatim.md`).**
  **All three of the pre-flip report's §11 questions are answered; the report is folded and its §12
  now lists what is owed.** **Q-A — the WINDOW-CLOCK reading is operative for part (1)**, on the
  2026-09-11 reasoning (*"the pooled span is a disclosure, not an answer"*): **part (1) is NOT MET**
  at 5.54 days and reaches its counting threshold **2026-09-13T09:44:55Z = Sun 13 Sep 19:44 AEST**.
  The report's §8 item 1 (Part 1's duration reads the pooled-buffer span) is **CONFIRMED AS A
  DEFECT** and **needs its own waiver before it can be fixed** — the ruling dictates the grounds the
  request must state. **Not requested or granted; the fix is NOT made.** The ruling also adds a
  composition paragraph to carry into the seven-day assessment: the window's 78 consult records are
  overwhelmingly governance/record-keeping Write/Edit, the 2,846 Bash actions are dropped entirely,
  and the 73 correct holds are **all justice-surface reads** — *"the seven-day clock reaching its
  mark does not change what the window contains."*
  **Q-B — the TRUST-STATE reading is operative for part (2)**, resolving the ambiguity the report
  identified: *"the standard asks for evaluation that produces a record — not engagement that passes
  through the harness without leaving a trace"*; the records-proxy 4/4 *"answers a different
  question."* **Part (2) reads 1 of 4, NOT MET, structurally blocked by the emission path.** Of the
  three branches: **Branch 1 (a founder-walked accreditation write) is the recommended path** —
  *"not a workaround… the path the system was built for"*; **Branch 2 (changing the close hook's
  seed-only shape) is expressly NOT recommended**; **Branch 3 (re-reading the standard) is closed**
  by the Q-B ruling itself. **WHICH BRANCH TO TAKE IS THE FOUNDER'S CALL — the ruling declines to
  pre-empt it. AND THE CONDITION ON BRANCH 1 IS BINDING AND IS THE MOST IMPORTANT LINE IN THE
  RULING:** *"If they are not ready to be made honestly, the accreditation write should not be
  performed to discharge a gate. The gate exists to ensure the record reflects genuine assessment,
  not to be discharged by any available mechanism… If the honest assessment of phronesis, andreia
  and sophrosyne is that the evidence is thin, the write should say so. A conservative confidence
  weight on a domain with thin evidence is not a failure. It is the correct reading."*
  **Q-C — the guard-side three-event rate DISCHARGES the F-3′ "reported separately with its rate"
  obligation and CARRIES a small-sample qualification**, wording dictated and now used verbatim in
  the report: *"MET (0 false-positive holds, 3 correct holds; n=3 — rate stated, not leaned on)."*
  The asymmetry with the unflagged 73-hold consult figure is ruled *"not inconsistency"* — *"73
  holds is enough to characterise a rate. 3 holds is enough to report a rate."* The guard-side
  availability figure (275/276) carries **no** analogous qualification.
  **BOTH WORDING DIVERGENCES ARE SETTLED:** the **v4 operative figure is 96** (*"the prior ruling's
  figure was a snapshot of a moving buffer; the current run's figure is the authoritative one"*),
  and the report's §4.1 substitution of the precise pre-window breakdown for the ruling's
  *"all false-positive"* **stands** (*"the ruling's wording was imprecise… which is exactly the
  discipline the report applied"*). Neither remains an open item.
  **Owed after this ruling (report §12):** the founder's Branch-1 honesty examination; a waiver
  request for §8 item 1; part (1)'s threshold on Sunday evening; and Part 3's consult/guard split,
  **still open and unruled**. **Nothing built, activated or flipped. D2 remains blocked; the S11 flip
  remains REFUSED; weights remain BLOCKED; the 0h call remains the founder's.**
