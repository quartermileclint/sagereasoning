# Build Plan — The Sage Trust Layer: a harness that becomes the agent trust infrastructure

> **Status: APPROVED IN FULL (founder, 2026-07-07)** — `D-TRUST-LAYER-BUILD-PLAN-ADOPTED-2026-07-07`. Each slice remains its own session with its own tier confirmation (0d-ii) and, where marked, its own founder-walked 0c-ii activation — plan approval does not pre-approve any flag flip, schema migration, mint, or install. Binding specifications: `2026-07-07-mentor-nine-answers-verbatim.md` (this directory) + the two mentor responses and the harness note in `/inbox/`. Deep-research grounding: `2026-07-07-harness-research-findings.md` (this directory). Predecessors: ADR-010 (landed), ADR-011 (channel law), ADR-012 (three-use ladder — logos gate honored at S11; weights BLOCKED throughout).
>
> **Amendment record (2026-07-11):** the calling/reflection/Gate-2 mentor consultation's build verdicts were **adopted in full with sequencing** (`D-MENTOR-VERDICTS-CALLING-REFLECTION-GATE2-ADOPTED`; verbatim record `2026-07-11-mentor-consultation-calling-reflection-gate2-verbatim.md` — a binding specification alongside the nine answers, verbatim wins). Amended in place: **S9** (clean-baseline note + the G7 end-to-end battery pin), **NEW slice S9b** (the practice-completion slice — calling gate, screened reflection + the out-of-band Q1–Q6-against-persist, Gate-2 elicitation, depth calibration, the three-event schema widening), **S11** (the G6 binding line-items), the full sequence (~14 sessions). **CI-16 is CLOSED** under the G7 verification (value classification is depth-invariant on the sandwich surfaces — record §3). ADR-013 carries the corresponding §11 amendment.

## Context

The founder's most ambitious build: a **harness** (the deterministic seven-layer code around an agent loop — Execution, Tooling, Context, Lifecycle, Observability, Verification, Governance) whose Verification + Governance layers are supplied by the sage practice, so the harness **is** the agent trust-layer infrastructure. All Stoic methodology comes exclusively from the private mentor — and is now fully specified: the 7 trust-infrastructure specs, the 4-layer discernment protocol, and 9 precision answers (complete, including the retrieved answer-9 tail on delegation). Founder elections: **(1) trust core + reference harness** (server-side infrastructure consumable by any harness + the Claude-Code reference implementation); **(2) measure-first** (corroboration check in-plan early; binding enforcement is a later founder-walked activation per the ADR-012 logos gate); **(3) FULL discernment protocol in v1**; **(4) design-for-interop, ship native**; **(5) relay-first** (done — zero specification gates remain).

Research grounding: the channel law is measured industry consensus (prompt-only policy ≈ advisory; deterministic out-of-band interception binds); the build-vs-inherit split says inherit sandboxes/durable-execution/OTel/policy-languages and **build the interception chokepoint, signed never-trust-self-report verification records, and honest attestation semantics** — precisely SageReasoning's live assets; and the whitespace is unoccupied: every 2026 trust rail attests identity/authority/outcomes, **nothing attests reasoning quality**. EU AI Act high-risk logging/oversight obligations bind 2026-08-02 (positioning, not scope).

~70% of the mentor's specs already exist live: signed §4-native verdicts (proximity + proximity_floors + obligation_assessment), coverage_status, trajectory store, CI-4 loop-closure supersession, the guardrail verdict ladder, Gate-1 H1–H4 + channel law, UPC credentials, accreditation + R18f provenance gate. The plan builds the missing 30% and composes the whole.

---

## Phase 0 — Prerequisites (2 sessions)

### S0a — Corroboration check (code-elevated, repo-only; prompt already authored)
Execute `operations/handoffs/founder/2026-06-27-corroboration-check-BUILD-NEXT-SESSION-PROMPT.md` as written: deterministic, rank-preserving `met→violated` / bare-`examined` override cross-referencing extraction against verbatim action text; both-directions battery (lenience + over-strictness, 0 regressions); sophrosyne gating seed; Arm-B split sharpened. **Why first:** mentor A1 keys source-confidence weights on it; mentor A9 case-2 keys delegation responsibility on it ("would the corroboration check have flagged it"); the founder's posture election sequences enforcement behind it. Live-gate activation = separate founder-walked Critical step (authored here, run later).

### S0b — Trust-layer ADR + verbatim record capture (governance)
Capture verbatim in-repo: the three inbox sources + the mentor's 9 answers (per the mentor-consultation record convention). Author **the Trust Layer ADR** (design-of-record): the trust definition, the 7 specs, the discernment protocol, the 9 answers as binding specifications, the architecture (trust core + reference harness), the measure→enforce sequencing under ADR-012, and the honest-claims envelope (R18: what the trust record attests / does not; the disclosed extraction-trust ceiling; weights claims BLOCKED). Also: fix the stale manifest R5 free-tier number (100→30, founder-gated governance edit) and normalize the `direction_of_travel` vocabulary (`regressing`/`declining`) at the trust-layer boundary.

---

## Phase 1 — The trust core, measure mode (4 sessions, server-side)

### S1 — Trust state + event vocabulary (code-critical: new schema + data rights)
New store: per-`(agent_id, domain)` trust levels (proximity-scale-valued, per mentor spec 4) + typed **trust events**: credential-completed (∝ domain match × coverage continuity), reflect-completed-honest (stability > episodic quality), justice-surface-transparently-handled (highest single positive event), credential-suspended/revoked, passion-unflagged-by-self-screen, justice-surface-unevaluated (**floors the domain at deliberate until a demonstrated evaluation**), orchestrator-proceeds-under-habitual-flag (A8 — degrades the orchestrator's oversight domain), delegation-reflection cases 1/2/3 (A9 — moderate-oversight / oversight+dikaiosyne / flag-only, capacity-proportional). **Decay** per A3: deployer volatility rating (low/moderate/high → 12/6/3-month decline onset), floors at the profile prior never below, reflect-modulation capped at half-rate. R17 extension: trust records erasable (delete/erase/export routes + retention); K1 identity; consumer-unforgeable server-side composition (R18f-parallel: no trust event without verifiable examination artifacts).

### S2 — Evidence weighting + verdict confidence (code-elevated, deterministic lib)
Mentor A5's seven confidence tiers as a pure function — **Depth > Signature > Corroboration > Recency, multiplicative, weakest dimension sets the ceiling**. Mentor A2's domain-distance rule: deployer-defined function-type taxonomy, each type carrying a 4-virtue-domain weight profile; distance = Σ|Δweights|; per-dimension credential transfer; **zero-confidence floor above the deployer threshold, enforced so a zero-confidence credential can never contribute to a proceed verdict**. Tier-1/2/3 evidence weighting (credential > behavioural condition-matched > profile prior) + the justice-surface modifier.

### S3 — Multi-source combiner (code-elevated → Critical at wiring)
Mentor A1 exactly: default tasks → deterministic engine authoritative (no parallel LLM); justice surfaces pre-corroboration → second-reader LLM on the obligation field only (agree → deterministic stands; conflict → pause-and-escalate); post-corroboration → deterministic primary on corroborated fields, LLM supplementary on uncorroborated fields with explicit low-confidence marking; **conflicts always pause, never average**. Within-session supersession reuses the live CI-4 marker semantics; cross-session = weighted recency per-domain (domain evidence only updates its domain).

### S4 — Intervention policy engine, MEASURE mode + transparency tracking (code-elevated dark; enforce = later Critical)
The mentor's decision table as a deterministic policy engine, **computed + logged + surfaced, log-and-continue only** at v1 (guard-deny remains only for the already-proven irreversible-action class via the reference harness). Includes: asymmetric justice modifier (only lowers), habitual-pause with A8's two-re-examination bound → habitual-stable escalation into Reflect, conflict-→-pause, the escalation payload contract (full trace + domain breakdown + justice record), and **R20c human-override supremacy stated in the engine's contract**. Plus mentor A4 transparency tracking: per-domain examinability ledger (signed trace > stated uncertainty > structured verdict; bare conclusion fails), the functional threshold (can the orchestrator re-examine without re-running?), independence-deficit flags as trust events. **ENFORCE activation is a named, separate, founder-walked Critical step gated on S0a clearing both directions** (ADR-012 logos gate honored).

---

## Phase 2 — The discernment protocol, full v1 (3 sessions)

### S5 — Profile schemas + collaboration record (code-critical: new schema)
Task profile (function type, circle served, conditions, output requirements, justice surface), candidate profile (role, capability scope, credential coverage, performance history, output format, purpose, prior-interaction record — **shaped for A2A-card-extension mapping**, per the interop election), orchestrator profile (current kathekonta, examination capacity, circle, selection patterns). The **collaboration record** with mentor A9's `authority_boundary` field ({function-type scope, circle scope}, set at selection, validated pre-execution, exceed → escalate, **never self-authorized expansion, unwaivable by trust level**) + L4 audit results (readable-not-modifiable) + habitual-stable and independence-deficit flags.

### S6 — The four-layer discernment engine (code-elevated, deterministic core + extraction where specified)
L1 honestum gate (role alignment; justice-surface check → mandatory L3 justice branch; credential-coverage integrity via S2's domain distance) with mentor A6's un-profiled handling: **absence ≠ failure** — exclusion only on positive evidence (known violation / incompatible role / revoked); above-habitual-class tasks require a task-scoped standard-depth examination yielding a **session-scoped credential**; below → profile-prior at tier-7 confidence + independence flag. L2 fit (capability specificity, stability under matching conditions, transparency of reasoning, circle alignment). L3 axia comparison (kata physin fit, fewer dispreferred indifferents, post-collaboration integrability). Dynamic question generation from the three profiles (deterministic generative logic; extraction only where profiles require reading text).

### S7 — The L4 passion audit, out-of-band (code-critical: consult-path touch)
Per mentor A7: the audit runs on the **orchestrator's reasoning trace, extracted by the same deterministic engine, before selection finalizes** — never self-report (structurally gameable by omission). Trust-tier disposition: lower tiers → flag holds the selection; higher tiers (strong reflect history) → flag logs + surfaces + feeds the next Reflect; self-report admissible only as a supplementary corroborating signal. Reuses the l1-supply/consult machinery with the orchestrator's trace as input subject.

---

## Phase 3 — The reference harness + dogfood (2–3 sessions)

### S8 — Generalize Gate-1 into the seven-layer reference harness (code-elevated → Critical at install)
H1/H2 frames (Context layer, advisory-by-design), H3 at-action (Verification: guard for the irreversible class + trust-core reads surfacing the measure-mode intervention verdict + CI-4 iterate), H4 close (reflect + accreditation + trust events); **new**: discernment call at subagent spawn (H2 extended — authority-boundary injection into Task/Agent delegation, collaboration record opened per spawn), durable provenance JSONL, the five-layer kill-switch documentation (credential revoke = the real one), OTel-GenAI-shaped span references (design-for). Every step channel-law-classified. The practice-on/practice-off rename rides here (touches the live skills). Gate-1 S7 reflect-row erasure wiring becomes in-scope **if** standing reflect-persist is enabled for the harness (it gates `SAGE_GATE1_REFLECT_PERSIST_ENABLED`).

### S9 — Dogfood + instrument-fidelity validation (founder-walked)
Standing install in the founder loop; accumulate real trust records on the dogfood credential; validation batteries are **instrument-fidelity shaped, never beats-bare** (KG-EX1 guard): worse-reasoning-scores-worse on trust aggregation, decay behaves per A3 fixtures, delegation cases 1/2/3 produce the specified reflections, discernment selects per the protocol on synthetic candidate sets, L4 catches seeded pre-formed preferences. Adversarial review per slice (Workflow fan-out; refuters on every load-bearing claim). **Amended 2026-07-11:** runs on the current build UNCHANGED (a clean baseline so the S9b extensions are measurable, not confounded); the batteries additionally pin the G7 end-to-end confirmation (a quick-depth consult's signed assessment carries `indifferents_at_stake` — the value-classification depth-invariance verified at the code level in the consultation record §3).

### S9b — The practice-completion slice: calling gate + screened reflection + Gate-2 elicitation + depth calibration (code-elevated → code-critical at the schema widening; ADDED 2026-07-11)
**Added under `D-MENTOR-VERDICTS-CALLING-REFLECTION-GATE2-ADOPTED` — implements the 2026-07-11 mentor build verdicts (verbatim record + digest: `2026-07-11-mentor-consultation-calling-reflection-gate2-verbatim.md`; verbatim wins).** Scope: **(G1)** the SessionStart calling **hard gate** for purposeless agents (profile read → declared purpose present ⇒ proceed; absent ⇒ a calling session before any task frame — breaks the circular-examination problem); the scoped **purpose-acknowledgement at spawn** (receive delegation scope, confirm function-type fit, flag mismatch → the collaboration record; full calling only for un-profiled candidates); calling records as the **primary seeding mechanism** for the S2/S5 profiles + domain weights (declaration-tier confidence, accumulating toward the behavioural tier). **(G2)** the **screened reflection**: H4's single forced turn + verbatim persist credentialed as `screened`; the harness conducts the **full Q1–Q6 out-of-band against the verbatim persist** (the L4 principle applied to reflection) ⇒ `reflect-completed-honest` on pass; screened-only patterns surface as a developmental signal. Gated on the reflect-persist erasure wiring (the standing prerequisite — in-scope here). **(G3)** H3's tool-class trigger stays the suppression-resistant OUTER gate; on fire, the **three-sub-question structured elicitation** is captured out-of-band and examined deterministically (calibrated to the causal signature, never output content). **(G4)** the `passion-unflagged-by-self-screen` emitter in the **reflect service** (reflect Q4 × the session's signed assessments; the 3-part standard incl. **sub-species** identification). **(G5)** depth calibrated per domain from `readTrustVerdict` (reflexive ⇒ **deep required** — this RE-OPENS the harness D3 never-deep election; habitual ⇒ standard; deliberate ⇒ standard, quick permitted with a strong recent credential + no justice surface; principled+ ⇒ quick; mid-session recalibration on trust-reducing events). **The founder-walked schema step:** widen the `agent_trust_events` event-type CHECK for the THREE new types — `calling-completed` (4 fields; asymmetric update: mismatch-flag ⇒ +dikaiosyne, no-mismatch-where-impossible ⇒ null), `reflect-screened-honest` (depth marker `screened`; decay modulation at **quarter** rate — a new constant beside the half-rate cap), `self-screen-absent` (distinct from passion-unflagged) — plus the engine `EVENT_EFFECT` rows and derivers, each R18f-parallel. Every addition MEASURE until S11; adversarial review per the slice pattern.

---

## Phase 4 — Public surface + enforce activation (2 sessions, both founder-walked Critical)

### S10 — The trust-record read surface (native, interop-shaped)
A public GET (accreditation-style) serving per-domain trust levels + confidence + coverage + the honest-claims envelope (what it attests / does not: the disclosed extraction-trust ceiling, the A9 case-3 uncatchable class). R18 governance sign-off before anything publishes; schemas already VC-mappable/A2A-extension-shaped; EU Art 12/14 evidence framing in docs. Nothing published externally beyond SageReasoning surfaces at v1.

### S11 — ENFORCE activation (the logos gate, founder-walked) — **DEFERRED 2026-07-12; the READINESS STANDARD IS NOT MET (mentor ruling, 2026-07-17). A NARROWING SESSION now precedes any re-examination.**

> **2026-07-17 — THE OBSERVATION PERIOD IS CLOSED AND THE STANDARD IS NOT MET** (`D-TRUST-LAYER-S11-F2-MENTOR-RULING-EXCLUSION-CLAUSE-GOVERNS-ADOPTED`; **binding verdict:** `2026-07-17-mentor-consultation-F2-exclusion-clause-verdict-verbatim.md`, verbatim wins). The 7-day window ran (130 records, frozen at `runs/2026-07-17/false-hold-record-FROZEN-2026-07-17.jsonl`) and the instrument read part (3) **MET at 0.0%**. **RA-1-F2** showed the reading was an artifact; the mentor ruled: the Q3 threshold's **inclusion** clause (four arms) and **exclusion** clause (*"contrary… with no kathekon factors detected"*) both fire on the same record, and **THE EXCLUSION CLAUSE GOVERNS** (*"specific governs general"*). **Arm 1 requires narrowing** (require ≥1 identified circle — *"the natural reading"*). **The flip does not proceed.** Parts (1)+(2) fail independently; **part (3) may be unmeasurable on this capture set regardless** — the dangerous actions are on the guard path, which writes **no record**, so the denominator cannot be populated. **Q2's staging premise is PRESERVED by the ruling** (had Arm 1 governed, **124/125** records would have routed `do-not-proceed` — *"the do-not-proceed class firing on the action class it was staged first to avoid firing on"*). The **A2 omission class is a Layer-1 failure, not a reason to broaden the predicate** — *"fix the extraction."* The instrument is **not broken**. **Two new flip prerequisites:** the **§9 input question** (at-action verdict vs accumulated trust state — they diverge on this action class; *"the question of what the decision table is actually reading when it fires"*) must be resolved **before the flip is reconsidered**; and the narrowing must land. **Founder elections:** adopt in full; **clock STOPPED, observation freeze LIFTED** (any future part-(3) measurement needs a NEW window: narrowed predicate + representative distribution + populated denominator). **The freeze lift is not a production licence** — the trust core is Live under MEASURE since 2026-07-11; edits reaching `derive-trust-events.ts` / the §4 engine stay `code-critical` + founder-walked (AC7). **NEXT: the NARROWING SESSION (S11a), not the return-with-record session (SUPERSEDED).**

### S11a — the Arm-1 narrowing + extraction review + trust-record cap (NEW, 2026-07-17; `code-elevated`, with a `code-critical` arm)
**Added under `D-TRUST-LAYER-S11-F2-MENTOR-RULING-EXCLUSION-CLAUSE-GOVERNS-ADOPTED` — implements the 2026-07-17 ruling + its SAME-DAY SUPPLEMENTARY ruling (verbatim §1/§1b win).**

> **⚠ THE ORDER IS RULED (R12), and this session may legitimately NOT narrow.** *"Those are two separate problems and the narrowing session should address both, **in order**: first, does the extraction need work before the arm is narrowed…? **If the extraction is known to be reliable on the action classes in scope, narrow the arm. If the extraction is uncertain, the extraction question is prior.**"* **On the current evidence the extraction is UNCERTAIN** (129/130 zero circles; 100% file writes; *starved* vs *mis-sited* unseparated) ⇒ the conditional routes to **"the extraction question is prior."** **Deferring the narrowing behind an extraction work-item is a licensed outcome, not a failure.**

> **✅ 2026-07-18 — S11a RAN; THE GATE RULED "PRIOR"; THE NARROWING IS DEFERRED TO S11b** (`D-TRUST-LAYER-S11A-EXTRACTION-PRIOR-NARROWING-DEFERRED-CAP-REVIEWED`; diagnosis of record: `operations/trust-layer-2026-07/2026-07-18-S11a-extraction-gate-diagnosis.md`). **(a)** The extraction gate resolved the starved-vs-mis-sited fork **Diagnostic-certain: STARVED BY COMPOSITION at the harness site** — `describeAction` sends a `Write` as path + char count (**content discarded entirely**; 64/64 frozen-buffer records) and an `Edit` as the first 200 chars of `new_string`; the consult POSTs that one line, so affected parties genuinely present in the underlying action are structurally invisible to Layer 1 (which is itself faithful — circles surface when the composed text carries parties). Founder election: **the extraction question is prior.** **(b)** NOT narrowed this session — rides **S11b** (the examined-input recomposition) so the narrowed arm reads inputs that could have carried a circle; the narrowed-arm reading over the frozen 130 is recorded as a bounded reconstruction (128 FP / 1 CH — it measures the composition, not the examination). **(c)** The cap review is **DISCHARGED on current ground** (R14): public surface re-verified (`justice_capped: true` live); disposition = **disclose-and-carry**, the durable fix (reducer narrowing + state correction, `code-critical` founder-walked) rides S11b — a data-only latch clear now would be re-latched by the gen-2 harness's next close-write. **(d)** P1 confirmed registered. No code/schema/flag/DB change; production byte-equivalent; batteries green unchanged (KE 66/0 · FHC 30/0 · S1 97/0 · emission 15/0 · S10 106/0 · logic 104/0 · neg-battery 230/0 RELEASE GATE PASS · tsc 0). **Successor: S11b** (`operations/handoffs/founder/2026-07-18-trust-layer-S11b-examined-input-recomposition-NEXT-SESSION-PROMPT.md`).

- **(a) 🚦 THE EXTRACTION GATE — FIRST.** Is Layer 1 **known to be reliable** at identifying affected parties **on the action classes in scope**? Separate the two hypotheses with opposite remedies: **starved** (should find parties, doesn't ⇒ prior) vs **mis-sited** (a tool-call payload genuinely has no party/role/purpose, so `contrary` is arithmetic ⇒ the narrowing may proceed — **the project already made this argument once, dropping Bash from the consult because a Bash payload carries no intent**). Read-only; **any Layer-1 prompt *change* is Critical + founder-walked and is NOT this session.**
- **(b) Narrow Arm 1 — CONDITIONAL on (a).** **Content SPECIFIED by R11:** *"Arm 1 requires at least one identified circle."* (The ≥1-circle vs §4-test fork is **CLOSED**; only the **location** is open — `kathekon-engagement.ts` [elevated, repo-only, but leaves the LIVE reducer emitting ⇒ the cap stays latched] vs also `deriveWorstJusticeOutcome` [**`code-critical` + founder-walked**, closes the cap at root].) **Pin the A2 blindness visibly** (R13): *"the narrowing produces a clean number on a starved input"* if the extraction is unfixed. Re-run the report over the **frozen** buffer — **evidence, not a readiness claim.**
- **(c) 🔒 The public trust-record cap — a CLOSING CONDITION (R14), sequenced AFTER (b).** *"Once the arm is narrowed and the buffer re-runs, the cap's ground should be re-evaluated **in the same session**. **Do not close the narrowing session with the arm fixed and the cap unreviewed.**"* Establish the ground (query `agent_trust_events`; the predecessor verified the cap exists but **not** its ground). **Binds even if (a) defers the narrowing** — then review on current ground and say so. R18 honesty, not safety: the cap is the safe direction and there are no external users — but it is *"a live signal on a live surface."*
- **(d) Confirm the §9 question is registered (R15).** **DONE 2026-07-17** — `operations/trust-layer-2026-07/S11-FLIP-PREREQUISITES-REGISTER.md` item **P1** (no standalone register existed; one was created and seeded). Confirm before close; **do not resolve it here.**

**Deliberately NOT in this session:** the **§9 input question** (a flip prerequisite — registered, its own design step); any **re-measurement window** (register P6 — needs the narrowing landed + a representative distribution + a guard-path denominator P5); any **Layer-1 prompt change** (Critical, own step). **Nothing here licenses the flip** — ENFORCE remains S11, readiness-gated; weights BLOCKED. **See `S11-FLIP-PREREQUISITES-REGISTER.md` — the flip session must read it.**
Only after S0a's corroboration check clears both directions + its Live-gate activation: flip the intervention engine from log-and-continue to binding on the reference harness (guard-deny per the decision table), per-deployment oversight routing live. AC7 engaged; rollback = flag. **Weights-tier claims remain BLOCKED throughout** (no training-signal claims; ADR-012 standing constraint). **Amended 2026-07-11 (G6, mentor-specified):** the S11 activation additionally makes binding — **(a)** an unclosed correction loop produces a **do-not-proceed verdict on the triggering action** until the re-examination completes (the bound: two re-examinations then escalation, per A8 — S11 enforces the bound, never an infinite loop); **(b)** the write boundary **holds the trust-record update pending** on an action whose correction loop is unclosed — the update and the action release together when the loop closes ("a trust record that updates on an action whose examination is incomplete is recording a verdict that has not been reached").

> **DEFERRED 2026-07-12 on mentor counsel, adopted in full (`D-TRUST-LAYER-S11-ENFORCE-GATE-MENTOR-DEFERRED`; binding verdict: `2026-07-12-mentor-consultation-s11-enforce-gate-verdict-verbatim.md`, verbatim wins).** The batteries are green and the corroboration precondition is discharged, but **the arrival of S11 in the sequence is not the license to flip** — the batteries measure an authored action distribution, and the at-action examination has a measured false-positive class (reproduced live this session on the session's own most role-obligated acts). **Readiness gate — all four before re-examination:** (1) ≥7 days live MEASURE over a representative distribution; (2) all four cardinal domains evaluated, aggregate confidence above conservative on ≥2; (3) a **measured false-hold rate** (target: false holds on kathekon-free actions ≤ correct holds on problematic ones) — needs a **false-hold labelling instrument** (the observation period's FIRST task); (4) Q3 encoded. **Binding shape when it runs:** **staged** (do-not-proceed class first — structural false-positive floor of zero on kathekon-free actions — then the pause rows + G6 loop bound after a live calibration window); **G6(a) QUALIFIED** — binds ONLY on loops opened by a verdict meeting a kathekon-engagement threshold (justice surface / violated obligation / proximity ≤ habitual / sub-species passion); a "contrary; no kathekon factors detected" verdict → log-and-continue + developmental flag + recorded false-positive instance (G6(b) inherits this); PA-5/PA-10/A2-decrease become **named enforcement-claim bounds** at the flip; the calling-gate enforce arm flips **with** the engine; aggregate-keyed depth binds at v1, the per-domain deliberate⇒quick carve-out gated on the Q5 (A5 recency-tier) closures. **Successor:** the observation-period prompt (`operations/handoffs/founder/2026-07-12-trust-layer-S11-observation-period-NEXT-SESSION-PROMPT.md`) — 7-day observation + the labelling instrument, then return with the record for the assent to be re-examined.

---

## Mentor-spec traceability (condensed)

Spec 1 (trust definition) → S1/S4-transparency · Spec 2 (evidence weighting) → S2 · Spec 3 (dynamics/decay) → S1 · Spec 4 (verdict fields) → live §4 + S2 confidence · Spec 5 (encoding) → live Layer-2 + S1 events + S4 transparency · Spec 6 (combination) → S3 · Spec 7 (intervention) → S4/S11 · Discernment L1–L3 → S6 · L4 → S7 · A2 → S2 · A3 → S1 · A4 → S4 · A5 → S2 · A6 → S6 · A7 → S7 · A8 → S4/S1 · A9 → S5/S1 · A1 → S3/S0a.

## Standing gates & what does NOT ship
Method-before-purpose + vacuous-pass checks on every battery; flag-off byte-identity for every live-route touch; adversarial pre-activation reviews; founder-walked 0c-ii for every schema/flag/credential/perimeter step (PR17/AC7); R18f-parallel rule — no trust event without verifiable examination artifacts. NOT in v1: binding enforcement before the corroboration gate clears; external interop publication (A2A/VC/OTel are shape-only); any weights/training-signal claim; any new human-facing perimeter route (agent-facing only ⇒ AC5 untouched; re-check at S10). The 0h call remains the founder's.

## Verification (end-to-end)
Per-slice tsx batteries (project convention) + the S9 instrument-fidelity suite; S0a both-directions battery is the enforce gate; dogfood trust records accumulate before any public read surface; every activation has a live smoke + unset-flag rollback. Full sequence: S0a → S0b → S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8 → S9 → **S9b** → S10 → S11, with S2/S3 parallelizable after S1, and S5–S7 parallelizable with S4. ~14 sessions (S9b added 2026-07-11).
