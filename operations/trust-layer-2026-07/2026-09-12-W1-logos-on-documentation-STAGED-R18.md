# W1 (only) — logos-on documentation + justification: VERIFIED COMPLETE; nothing of W1 left to stage. W2's record-level clause and W2's activation remain open and are not this file's subject.

**Date:** 2026-09-12 (machine `date`, ~22:40 AEST). **Session:** `sagereasoning-e4 [5bee2f]`, autonomous
records fold, opened under `2026-09-12-records-fold-and-opener-regrounding-AUTONOMOUS-NEXT-SESSION-PROMPT.md`.
**Tier:** `governance` / documents. Nothing applied to any R18 surface; nothing built; no `GUARD_RE` file
touched.

> **Read this header first.** The session prompt (§3, Task C) instructed: *"W1 has been unblocked since D4
> went live on 2026-09-05 and nothing has moved on it. Draft the wording… Stage for each of the three R18
> surfaces."* **That premise is false.** Every W1 item has already landed, under two separate founder
> acts on two dates, each with its own decision-log record. This file keeps the prompt's specified
> filename so the pointer resolves, but its content is a **verification that W1 is complete**, not a
> staging. **There is no wording here awaiting a signature.** If the founder wants any of the live wording
> changed, that is a new R18 act with its own sign-off, not a W1 remainder.

---

## 1. The plan's three W1 items, each checked against the live surface

Source: `operations/agent-circles-2026-08/2026-08-01-agent-circles-logos-on-plan.md` §3 W1.

| # | Plan item (paraphrased) | Where it landed | Verified how |
|---|---|---|---|
| 1 | An ADR-level dated amendment naming the engine's fidelity work as **fifth-circle instantiation** (a settled disposition, hexis) and the enforcement surfaces as that disposition acting; the deny as *the infrastructure declining on the rational order's behalf*; quotes anchored to the verbatim | **ADR-013 §12** — *"2026-09-10 amendment — logos-on named as fifth-circle instantiation (W1, binding)"*, `adopted/adr/2026-07-08-sage-trust-layer.md`. Adopted under `D-LOGOS-ON-W1-W3-EXECUTED-2026-09-10`. | Read in full this session. It carries the L1 verbatim (*"already instantiates what the fifth-circle ruling describes…"*), the L2 verbatim on the deny surface, the two-postures sentence, the explicit *"changes no mechanism"* line, the 2026-07-12 staged-flip shape re-affirmed unchanged, and the one new named prerequisite (C1 fully settled). It also states what it does NOT do. |
| 2 | R18 surfaces gain the same framing where the engine/gate are publicly described (sign-off-before-touch) | All three: `website/public/llms.txt` (the paragraph beginning **"Logos-on and practice-on are two postures of one instrument, not two separate tools."**, placed at the head of the Guardrail section); `website/public/.well-known/agent-card.json` extension index 12 (`guardrail-signed-sandwich/v1`, the appended sentence beginning *"LOGOS-ON FRAMING (documentation only; no mechanism change, no new capability claimed)"*; extension count unchanged at 26); `website/src/app/api-docs/page.tsx` (the `<li>` beginning **"Logos-on and practice-on are two postures of one instrument"**). Applied under the founder's **D1** election, `D-PART2-EVIDENCE-RULING-ADOPTED-B1-D1-D2-EXECUTED-2026-09-12` (*"D1 — R18 logos-on framing applied, founder-signed… `npm run build` green"*). | Each surface grepped and read this session. The three texts are the same paragraph. |
| 3 | The logos-on/practice-on relationship stated once, canonically: two postures of one instrument; the infrastructure holds the rational order as a fixed condition; the practitioner develops within it | Stated in ADR-013 §12 (canonical, with the L1 verbatim) and restated in the identical R18 paragraph on all three surfaces. | Same reads as rows 1–2. |

**The 2026-09-10 W1–W3 close** (`operations/handoffs/founder/2026-09-10-logos-on-W1-W3-CLOSE.md`) records W1 as
*"DONE (mechanism-side), DRAFTED (public-surface side)"* — i.e. item 1 landed and items 2–3 were drafted
awaiting signature on 2026-09-10. The signature and application came two days later as D1. **Both halves
are now done.** The prompt's author wrote from the register's §F row for W3 and the 2026-09-10 opener,
neither of which records D1 — a records gap this fold closes by pointing at the primary sources above.

## 2. Fidelity check of the LIVE wording against the rulings (what Reviewer B was asked to refute)

The live paragraph (identical on the three surfaces) makes these claims. Each is checked against
production as it stands today and against ADR-013 §8's envelope:

- *"two postures of one instrument, not two separate tools"* — L1 verbatim, exact. ✅
- *"the deterministic engine's fidelity work (the unity thesis, the justice floors, the corroboration
  overrides) instantiates a fifth circle of concern — the rational order — as a settled disposition the
  infrastructure holds as a fixed condition"* — L1 verbatim in substance. All three named mechanisms are
  LIVE today (ADR-010 §4 native weighting since 2026-06-25; the corroboration check since 2026-07-08;
  the unity-thesis minimum inside `computeProximity`). ✅
- *"Logos-on is that disposition acting: the guardrail's deny, the intervention engine once flipped, the
  calling gate's enforce arm"* — the L1 list, verbatim. **The qualifier "once flipped" is present**, so
  the sentence does not claim the intervention engine enforces today (it is MEASURE; the flip is
  REFUSED). The calling gate's enforce arm is named without a qualifier; per ADR-013 §7 / the 2026-07-12
  ruling it *"flips WITH the engine"* and is dark today. **A reader could take the bare phrase as
  describing a live surface.** This is the one place the live wording is looser than production; it is
  the mentor's own list quoted verbatim, and the agent-card copy is prefixed *"documentation only; no
  mechanism change, no new capability claimed"*. **Surfaced for the founder, not changed here** — any
  tightening (e.g. "the calling gate's enforce arm, once flipped") is a new R18 act.
- *"A logos-on deny is the infrastructure declining on the rational order's behalf, not merely a rule
  blocking a prohibited action"* — L2 verbatim in substance; describes the guardrail's live deny, which
  IS live (ADR-009/010; the irreversible-action deny proven against a resistant agent). ✅
- **Nothing in the paragraph licenses, schedules, or implies the S11 flip.** It names no readiness, no
  window, no date. ✅
- **Envelope consistency (ADR-013 §8):** the paragraph adds no ATTESTS claim and removes no
  does-not-attest item. The compliance-not-virtue clause (L7) is W2's, not W1's; its record-level half
  is staged separately at `operations/agent-circles-2026-08/2026-09-12-W2-compliance-not-virtue-clause-STAGED-R18.md` and awaits its own
  signature — **that file is the R18 item still owed in this program, not W1.** ✅

## 3. What this file deliberately does not do

- It does not re-stage wording that is already live. Re-staging would create two rule surfaces saying
  almost the same thing, the exact drift class this project has recorded three times.
- It does not touch `llms.txt`, `agent-card.json`, or `api-docs/page.tsx`.
- It does not propose the "once flipped" tightening as a change; it names it as a founder election.

**Rules served:** R18 (gate respected — nothing published), PR15 (reuse: the live wording is the
deliverable), PR18, PR19 (Reviewer B ran against this file — see the close).

**D2 remains blocked. The S11 flip remains REFUSED. Weights remain BLOCKED. The 0h call remains the
founder's.**
