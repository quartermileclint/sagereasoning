# Public-honesty batch — R18 sign-off package (2026-08-17)

**Status: DRAFT — AWAITING FOUNDER SIGNATURE. Nothing here has been applied.**
**R18:** founder sign-off on exact wording is required **before** any public surface changes.
**Covers three items**, all founder-directed this session: the `/limitations` crisis disclosure
(M-5(a) Option A, previously signed), the `ops-hub` monitoring copy, and the `transparency`
support@ promise.

---

## 0. R18 re-derivation — what changed since the Option A draft

Every cited line was re-read first-hand at application time. **Two findings:**

**No drift in the cited text.** `ops-hub:638`, `ops-hub:918` and `transparency:171-173` are verbatim
as recorded in the 2026-08-17 package.

**⚠ Option A as signed contains a factual error, and this package does NOT apply it as drafted.**
The drafted sentence reads:

> "That happens automatically, **every time**, and it is the protection this system actually provides."

**That is false.** Verified first-hand at application time:

- `HUMAN_FACING_POST_ROUTES` now holds **20 routes** (the five score routes, `/api/reason`,
  `/reflect`, `private/reflect`, `journal-feed`, `journal`, `score-conversation`, both Stoa routes,
  `impulse`, and this session's six).
- **Six human practice routes have NO distress check**, confirmed by direct grep for
  `enforceDistressCheck`: `premeditatio`, `hupexairesis`, `oikeiosis`, `view-from-above`, `morning`,
  `sage-compass`. They carry `SupportFooter` as their crisis exit, by recorded family precedent.
  Whether they should join the perimeter is a standing, unresolved AC5 question.

Publishing "every time" would place a **new false claim on the page whose purpose is honesty**.
It would also repeat the exact error the mentor ruled on for this same page (M4, 2026-08-15): a
single blanket formulation that is true of one surface and not another. `/view-from-above` is the
grief-facing tool, which makes the inaccuracy sharpest precisely where it matters most.

**This is returned to the founder rather than silently adapted, per the original package's own
footer instruction.** Two corrected variants are offered below.

---

## 1. `/limitations` — the crisis disclosure (Option A, corrected)

Insert as a new `<h2>` section immediately after **"We are not therapists"** (currently `page.tsx:36`),
before the mentor-amended per-surface section. Register is practitioner-facing, matching the page.

### Variant A1 — recommended. Honest without a route inventory.

> ### What happens if you are in crisis
>
> If what you write shows acute distress, SageReasoning stops the philosophical response and shows
> you crisis resources immediately, in the session. That happens automatically on the tools that
> evaluate what you write — the scoring tools, the journal, and the reflection and mentor tools.
> Some of the shorter practice exercises do not run that check; they carry crisis contact details in
> the page footer instead.
>
> **What does not happen, anywhere: nobody is notified, and no one follows up afterwards.** There is
> no queue, no on-call reviewer, and no monitoring of your session after you close it. The crisis
> resources we show you are the whole of it — they are not an introduction to a human who will make
> contact.
>
> This is a real limitation of a system built and run by one person, and we would rather state it
> plainly than let the presence of a safety feature imply a safety net that is not there. If you need
> a person, please use one of the numbers shown — they are staffed, and we are not.

**Why this variant:** keeps every sentence of the signed Option A that was accurate, repairs only the
false clause, and moves "nobody is notified" to *"anywhere"* — which is unconditionally true and is
the substantive disclosure the ruling was about.

### Variant A2 — names the exercises explicitly.

Same as A1, but the second sentence reads:

> That happens automatically on the tools that evaluate what you write. The shorter practice
> exercises — preparing for the day, the view from above, the reserve clause, the circle of concern,
> and the sage compass — do not run that check, and carry crisis contact details in the page footer
> instead.

**Trade-off:** maximally transparent, but tells a practitioner exactly which pages will not catch
them, and will need editing whenever the perimeter changes.

### Variant A3 — close the gap instead of disclosing it.

Bring the six practice routes inside the perimeter, making the original "every time" true, and
publish Option A unmodified. **Not recommended as part of this batch** — it is a Critical AC5
perimeter change on six more routes, it is the standing question the mentor has not ruled on, and it
should not ride along inside a copy-editing item.

---

## 2. `ops-hub/page.tsx:638` — the monitoring commitment

**Current (live, public):**

> All alerts are monitored by Sage Ops. Critical alerts require acknowledgment within 2 hours.

**Why it is false:** no Sage Ops monitoring cadence exists. Go-live item **#11 is OPEN** — the
founder confirmed directly that neither `support@` nor `zeus@` is watched on a regular cadence. The
human-escalation owner (#12) is the founder personally. There is no acknowledgment SLA in force.

**Proposed replacement:**

> Alerts are generated automatically and reviewed by the founder when this hub is opened. There is no
> monitoring rota and no acknowledgment guarantee.

## 3. `ops-hub/page.tsx:918` — the review-queue implication

**Current (live, public):**

> Pipeline processing is automated. Items flagged for review are queued above.

**Why it is misleading rather than flatly false:** the list above it is real, but "queued for review"
implies a review workflow that will act on the queue. None exists.

**Proposed replacement:**

> Pipeline processing is automated. Items needing attention are listed above; there is no separate
> review process behind this view.

## 4. `transparency/page.tsx:171-173` — the support@ promise

**Current (live, public), under the heading "Human oversight":**

> If you have concerns about any AI-generated output, you can always contact a human
> at support@sagereasoning.com.

**Why it is false:** the founder has confirmed `support@` is not watched on any regular cadence
(go-live #11, OPEN). *"You can always contact a human"* promises availability that is not provided.

**Proposed replacement:**

> If you have concerns about any AI-generated output, you can write to support@sagereasoning.com.
> SageReasoning is built and run by one person, so this address is not monitored continuously and a
> reply may take time.

**Note:** this softens the promise to match reality. The alternative — staffing the channel so the
original sentence becomes true — is the option that resolves it in the practitioner's favour and
remains open to the founder. It is recorded as a go-live #11 dependency either way.

---

## 5. Signature

**`/limitations` crisis disclosure:**
- [ ] **Variant A1** — corrected Option A, no route inventory *(AI recommendation)*
- [ ] **Variant A2** — corrected Option A, exercises named
- [ ] **Variant A3** — close the perimeter gap first, publish Option A unmodified *(separate Critical session)*
- [ ] Amended wording (write below)

**`ops-hub:638`:**  - [ ] as proposed   - [ ] amended: ______________________
**`ops-hub:918`:**  - [ ] as proposed   - [ ] amended: ______________________
**`transparency:171-173`:**  - [ ] as proposed   - [ ] amended: ______________________  - [ ] staff the channel instead

**Founder signature / date:** ______________________

*Nothing is applied until the relevant box is ticked. If any cited line has drifted by the time of
application, the applying session returns here rather than adapting the wording itself.*
