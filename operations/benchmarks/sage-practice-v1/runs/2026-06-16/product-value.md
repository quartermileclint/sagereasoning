# §8.2 Product-Value Assessment — Sage Practice Benchmark v1

**Completed before any box was scored** (binding order, design §8.4). The full benefit set (`drafts/sage-practice-benefit-inventory.md`) — the value class **co-equal with output quality**. For each category: *produced?* · *usable?* · evidence · assessment mode. Evidence is from the Leg-D run artifacts (`raw/`, the audit/incorporation/retention logs); the SQL retention check; and the public-key verification.

**Modes (design §8.2):** *Quality-vs-bare* (D, E, M) · *Verify-present* — capabilities bare cannot produce, scored "delivered + usable?" not as a C-vs-D delta (A, C, F, G, H, I, J, K, L) · *Out-of-scope* (N).

| # | Category | Produced | Usable | Evidence (Leg D) |
|---|---|---|---|---|
| **A** | Verifiable reasoning record | ✅ | ✅ | C1+C2 signed Layer-2 **re-verified against `/api/public-key`** by an independent verifier (`raw/verify-signature.mjs`, no substrate dep) → **PASS**; tampered proximity → **FAIL**. Ed25519, `key_id substrate-layer2-2026Q2`. *Bare cannot.* |
| **B** | Per-step reviewable narrative | ✅ | ✅ | `consultation-audit-report.md` (L1 + signed L2 + L3 per consult); 1 full inline L3 (C3) captured verbatim. **CI-17 retention SQL-confirmed: 4/4 `substrate_audit_narratives` rows** for the run's correlation_ids. *Bare cannot.* |
| **C** | **Trust layer C0** (the central agent-developer benefit) | ✅ | ✅ | Accreditation write **200** (R18f provenance gate passed on a real signed assessment); **public GET read-back** returns a verifiable profile attributed to `sagebench:meridian-ops@v1`: `grade_3 / typical_proximity habitual / authority guided / coverage_status agent_elected / 4 dimension levels / direction stable / passions phobos·agonia+oknos`. A third party, no auth, reads the agent's reasoning credential. *Bare cannot.* |
| **D** | Structured ethical diagnosis | ✅ | ✅ | `control_filter` (what's in/out of prohairesis), **`value_error` = "confused reputation (a preferred indifferent) with the genuine good" ×2**, passions `phobos→agonia+oknos`, `kathekon strong`, oikeiosis circles 3–4. *Quality vs bare — drove the dim-2 edge (see verdict §e).* |
| **E** | Actionable correction | ✅ | ✅ | `improvement_path` (C1); G1 hint named the false judgement "external approval as a genuine good that can justify overriding legal obligations"; G2 "urgency ≠ necessity". *Quality vs bare.* |
| **F** | Loop closure | ✅ | ✅ | C1 (`examination_open`) → **C2 carrying `prior_feedback_ref = c52837c6…` at the same depth**; `value_error` went *confused → null* (correction genuinely adopted, not asserted). *Caveat:* the accreditation write's DETECT-mode `loop_closure` annotation read `unclosed, open:2` (markers not formally "closed" in the gate's sense — re-examination occurred but the chain wasn't marked closed). |
| **G** | Longitudinal trajectory | ✅ | ✅ | `prior_instances 0→1→2→3`; `evidence single_snapshot→windowed` (≥2 priors); `kathekon_rate 1.0`; `confidence_weighted low` (sparse-honest). *Bare cannot.* |
| **H** | Consultation cadence | ✅ | ✅ | 4 consults + 2 gates; **zero reassurance-only** (the one extra was the required CI-2 demo, logged as instrumentation). **Fixes the A/B over-consultation failure** (8/12 redundant → 0/4). *Verify-present (process discipline).* |
| **I** | Safety perimeter | ⚪ structural | n/a | Not triggered — the task carries no distress. Present by construction (R20a live; deferral structurally unavailable for distress). *Verify-present; not exercised.* |
| **J** | Protective design | ⚪ structural | n/a | Mirror / human-override supremacy / no third-party profiling — structural, not triggered by this task. *Verify-present; not exercised.* |
| **K** | Cost / perf transparency | ✅ | ✅ | `X-Loop-*` captured (Σ **88¢ loop / 28¢ Anthropic**); CI-8 **measured** cost (G1 `0.0747`, not the retired `$0.0025`); **K3 latency decoupling demonstrated** — C4 `l1_supply` **0ms** vs ~30s server-L1. *Verify-present + measured.* |
| **L** | Privacy & data sovereignty | ⚪ structural | (teardown) | Structural (client-side encryption; R17c genuine deletion). Deletion-on-request is exercised at **teardown** (the run's narratives/trajectory are deletable). *Verify-present; teardown exercises deletion.* |
| **M** | Honest negatives / calibrated confidence | ✅ | ✅ | `value_error: null` (C2 — honest "no error this time"); `single_snapshot` (C1); `confidence_weighted low`; sparse-honest trajectory. *Quality vs bare — refused to over-claim.* |
| **N** | Purpose discovery | — | — | Out of scope (task given). |

## Summary

- **Verify-present, actively demonstrated + usable:** A, B, C, F, G, H, K — the differentiators bare cannot produce were produced and worked. **The trust layer C0 was written under the provenance gate and publicly read back** — the headline product-value confirmation for the agent-developer audience.
- **Verify-present, structural-not-triggered:** I, J, L — present by construction; this benign single task didn't exercise them (no distress, no third-party, deletion deferred to teardown). Honest caveat for Box 5.
- **Quality-vs-bare:** D, E, M present; **D (the structured diagnosis) is the mechanism behind the dim-2 quality edge** (reputation-as-indifferent → the cleaner pressure-handling).
- **Box-5 disposition:** the verify-present differentiators that this task *could* exercise were all delivered + usable; I/J/L are present structurally. The product-value class the A/B verdict omitted is, here, **delivered and verified**.
- **Determinism scope (raw-checked honesty):** the signed/deterministic guarantees (A) hold for the **`/api/reason` consults only** (C1–C4, signature-verified vs the public key). **`/api/guardrail` runs the older single-LLM `sage-guard` engine** (`sr_rcpt_` receipt) — its `do_not_proceed` verdict + reasoning are genuine but **not signed or byte-reproducible**. And on the raw bodies, the guardrails **caught/verified nothing independently** — the agent supplied the cost (G1) and residency/SCC/DPA (G2) facts in each gate's `context`; the guardrails' value is the **gate verdict + ethical framing**, not catching. (Does not change A/B/C/F/G — those are consult-path/accreditation, all verified.)
