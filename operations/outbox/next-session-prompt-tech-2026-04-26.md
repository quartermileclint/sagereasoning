# Next-session prompt — tech stream (paste into a new Cowork session)

Save the prose below this divider line and paste it into a new tech-stream session as the opening message. Everything between the divider and the "End of prompt" line is the prompt itself.

---

Governing frame: /adopted/session-opening-protocol.md

I am opening a new session in the tech stream. The previous session (26 April 2026) completed ADR-PE-01 Sessions 3.5 + 4 combined under Options 3.5-α + 4B. Phase 1 parameterised `/api/mentor/ring/proof` to accept an optional `hub_id` field (default `'private-mentor'` for back-compat; allowed values `'private-mentor'` and `'founder-mentor'`; 400 on invalid). A four-probe verification confirmed back-compat (Probe 1), explicit private-mentor (Probe 2), allowlist enforcement (Probe 3 → 400), and the founder-mentor SEED (Probe 4 — first-ever write of `pattern_analyses['founder-mentor']`, computed from `PROOF_INTERACTIONS` fixture, `pattern_source: 'recomputed'`, `ok: true`). Phase 2 wired pattern-data read+write on `/api/founder/hub` gated by `agent === 'mentor' && useProjection && storedProfile?.profile`, with per_request cadence on cache hit, 2A-skip on absence, and `pipeline_meta.pattern_source` + `pipeline_meta.pattern_persistence` diagnostic via spread-conditional. A four-probe verification confirmed: Probe 5 (founder-hub label, founder-mentor read) returned `pattern_source: 'persisted'`, `version: 12`, `ok: true`; Probe 6 (re-run) returned `version: 13`, **delta = 1** (per_request cadence diagnostic confirmed); Probe 7 (private-mentor label) returned `pattern_source: 'persisted'`, `version: 14` (KG3 mirror confirmed end-to-end); Probe 8 (non-mentor agent, ops) returned both pattern fields absent on `pipeline_meta` (the `agent === 'mentor'` gate confirmed by absence). TypeScript reached `tsc --noEmit` exit 0 on both phases; both pushes completed via GitHub Desktop; Vercel reached Ready on both deploys; smoke checks on `/founder-hub` rendered normally. Both Implementation Sessions 3.5 and 4 reached Verified status. The session close is at `/operations/handoffs/tech/2026-04-26-ADR-PE-01-session-3.5-and-4-close.md`. Two decision-log entries were adopted at the close: `D-PE-01-S35-3.5α-VERIFIED` and `D-PE-01-S4-4B-VERIFIED`. Five decision-log entries (`D-PE-01-S1-1A-VERIFIED`, `D-PE-01-S2-2A-VERIFIED`, `D-PE-01-S3-3A-VERIFIED`, `D-PE-01-S35-3.5α-VERIFIED`, `D-PE-01-S4-4B-VERIFIED`) all currently carry "TBD per founder share" in the commit-hash slot. The §8 single-endpoint-proof sequence has reached a natural pause: the two major mentor-touching live consumers (reflect + founder-hub) are now both wired with pattern data read+write under per_request + 2A-skip on absence. This session continues from there.

I use GitHub Desktop to push commits. If any code lands this session, give me explicit GitHub Desktop instructions (open GitHub Desktop → confirm Current Repository = `sagereasoning` → confirm Current Branch = `main` → review the Changes pane → fill Summary + Description → click Commit to main → click Push origin → wait for spinner) per commit, in the same pattern the prior tech-stream sessions used. The verification probe pattern uses a Console snippet on a deployed page; the working auth-cookie discovery for this project is `decodeURIComponent(cookies['sb-access-token'])` reading the raw JWT directly from the cookie value (no JSON envelope, no chunked patterns). Use that snippet pattern from the start — it is logged as PR5 candidate observation (1 of 3) in the Session 3 close, with confirming evidence at the Sessions 3.5 + 4 close (used twice without iteration; counter remains 1 of 3 conservatively, but promotion to canonical at the next verification-probe session is now reasonable).

Before touching any code, complete the session opening protocol:

* Tier declaration: this is a code/tech session. Read tier 1, 2, 3, 6, 8, 9 (every-session sources + KG governance scan + code state).
* Read the most recent handoff in `/operations/handoffs/tech/`: `2026-04-26-ADR-PE-01-session-3.5-and-4-close.md`. Read end-to-end. The "Decisions Made", "Status Changes", "Open Questions", "Next Session Should", and "Process-Rule Citations" sections are the load-bearing parts.
* Read the adopted ADR in full: `/compliance/ADR-PE-01-pattern-analysis-storage.md`. Specifically §1.2 (loader deferral status — particularly (c), the live `mentor_interactions` loader still deferred), §1.4 (hub-scoping per (user, hub)), §3 (Decision), §6 (R17 footprint), §7 (Coordination with the loader — particularly §7.1 read contract and §7.2 write cadence; both newly relevant if this session takes on the live loader work because the loader is the precondition for moving any consumer from 2A-skip to 2A-recompute), §8 (Single-endpoint proof sequence — Sessions 1, 2, 3, 3.5, 4 all Verified; the §8 sequence reaches a natural pause), §9 (Risks accepted), §10 (Rollback), and §12 (Open items O1–O5; O3 — `last_pattern_compute_at` plain column — becomes more relevant if cadence work or freshness queries surface).
* Scan `/operations/knowledge-gaps.md` for any entry touching live-loader integration with the encrypted blob (KG3 hub-label end-to-end contract — engages on every new reader/writer of `mentor_interactions` rows by hub_id; KG7 JSONB shape — engages if the loader queries any plain JSONB column on `mentor_interactions`; KG1 rule 2 awaited writes — engages if the loader introduces a new write surface or new conditional write; KG6 composition order — engages if any change alters prompt assembly). Also scan for any post-Session-4 candidate-observation updates.
* Confirm P0 hold-point status — should still be active. Pattern-engine integration sits inside the assessment set; the live loader work would be a natural extension of the same hold-point assessment.
* Confirm model selection (PR4). The likely next work items are pure DB-loader work (no LLM added by reading `mentor_interactions` rows and synthesising them through the deterministic `analysePatterns` function), or cleanup work (no LLM at all). Confirm at session open. If any framing changes prompt composition, PR4 re-engages.

Step 1 — Pick the next framing.

The §8 single-endpoint-proof sequence has reached a natural pause. The remaining ADR-PE-01 work items vary widely in scope and risk. Three candidate framings — surface all three with brief reasoning. I pick.

* Framing A — Live `mentor_interactions` loader (ADR §1.2 (c)). Critical risk under PR6 + R17. The architectural completion of ADR-PE-01: with a live hub-scoped loader, every consumer's 2A-skip-on-absence fallback can be revisited (potentially switched to 2A-recompute) because the recompute branch would then have real input data instead of the empty array that pre-loader 2A-recompute would produce. The loader reads `mentor_interactions` rows scoped to `(profile_id, hub_id)`, returns `InteractionRecord[]`, and the consumers pass them to `analysePatterns(profile, interactions, null)` to compute fresh pattern data on cache miss. This work is significant — it touches the encryption pipeline indirectly (every profile read would now also load interactions; every persisted pattern_analysis would now be computed from real data, replacing the fixture-derived entries currently in the founder's blob). Sub-questions for the plan walk listed below. PR1 single-endpoint proof discipline applies — the loader proves on one consumer first before rollout.

* Framing B — ADR-PE-01 cleanup pass. Standard risk. Batch the small carry-forward items into a single session: (i) O-PE-01-B blob-size measurement — a one-off Supabase probe of `length(encrypted_profile)` to confirm blob size against the 50 KB threshold from ADR §9; (ii) the cosmetic `27 April 2026` → `26 April 2026` comment-date fix in `/website/src/app/api/mentor/private/reflect/route.ts` (carry-forward from Session 3 close); (iii) commit-hash recording — amend the five `D-PE-01-*-VERIFIED` decision-log entries with the actual commit hashes from GitHub Desktop's History tab, replacing the "TBD per founder share" placeholders. Smaller scope, finishes the trailing items, frees the next architectural session from carry-forward overhead. Sub-questions listed below.

* Framing C — Step out of ADR-PE-01. The hold-point assessment is still active per P0 0h. Pattern-engine integration was inside the assessment set; with the integration at a natural pause, the assessment can now use the wired pattern data to test what works on real founder data. Or — alternative — step into a different priority entirely (P2 ethical safeguards is the next priority per project instructions; P2 starts with R20a vulnerable user detection and R17a bulk profiling prevention, both Critical and both pre-dating ADR-PE-01 in priority order). Sub-questions for the plan walk depend on which alternative. AI signal: I'd push back gently on doing P2 work while ADR-PE-01 carry-forward items (O-PE-01-F env-var decoupling, commit-hash recording) remain open, but if you want to do that, I'll execute it.

Do not assume which framing I am taking. Ask me directly: "Framing A, B, or C?" Wait for my answer.

Also surface, at the same time as the framing question, the appropriate plan-walk questions for each framing:

For Framing A (live loader), surface these sub-questions and ask me to answer alongside the framing pick:

* Q-Loader-Scope — confirm hub-scoped (per-(user, hub)) per ADR §1.4 and the 2026-04-26 founder direction. Default: confirmed hub-scoped. Tell me if anything has changed.
* Q-Loader-Cadence — when does the loader run? Per consumer request (every reflect/founder-hub mentor call loads interactions, computes fresh pattern, optionally writes back), cached at the request boundary (loader runs once per request, results passed to the rest of the route synchronously), or background-precomputed (separate scheduled task or trigger; consumer reads pre-computed results). Default recommendation: per-consumer-request initially, with `mentor_interactions` query scoped tight (recent N days, top M rows) to keep latency bounded; revisit if the read amplification cost rises.
* Q-Loader-Source — which `mentor_interactions` rows are eligible? Time-windowed (last 30 days? 90? all)? Limit (top N by recency)? Filter (only interactions with passion_diagnosis populated, only interactions where katorthoma_proximity is non-null)? Default recommendation: hub-scoped + last 90 days + limit 100, then revisit based on observed pattern-engine output stability.
* Q-Recompute-Read-Precedence — with the loader live, does the existing 2A-skip on each consumer get switched to 2A-recompute on cache miss? Or stays 2A-skip indefinitely? Per consumer. Default recommendation: keep 2A-skip on absence everywhere initially (preserves the cache-pollution mitigation that the pre-loader work depended on), then switch to 2A-recompute on per-consumer basis once the loader is verified live and the founder's profile's fixture-derived pattern entries have been replaced by recomputed entries from real data.
* Q-Backfill — should the founder's profile's existing fixture-derived `pattern_analyses['founder-mentor']` and `pattern_analyses['private-mentor']` entries be replaced by recomputed entries from real `mentor_interactions` data once the loader is live? If yes, that backfill is itself a Critical-risk operation. Default recommendation: defer the backfill until the loader has been verified live for at least one session; once verified, the natural per_request cadence will overwrite the entries on next consumer call (no separate backfill needed unless the founder wants a one-off explicit refresh).
* Q-Single-Endpoint-Proof — which consumer is the PR1 single-endpoint proof for the live loader? Default recommendation: the proof endpoint (`/api/mentor/ring/proof`) — symmetric with Sessions 1/2 + 3.5; allows verification before any live-consumer surface is touched. Alternative: reflect or founder-hub (live consumer first). Founder picks.

For Framing B (cleanup pass), surface these sub-questions:

* Q-Cleanup-Scope — which carry-forward items are in scope this session? Default recommendation: all three (blob-size measurement, cosmetic comment-date fix, commit-hash recording). They are independent and small. If founder wants narrower scope, name which to defer.
* Q-Blob-Size-Probe-Pattern — the blob-size probe needs Supabase access. Two options: (i) a SQL query through Supabase's web UI (founder runs once, reports back), or (ii) a temporary diagnostic API route that returns `length(encrypted_profile)` for the founder's row (Critical-adjacent because it touches the profile row, but read-only). Default recommendation: (i) — simpler, no code change needed.
* Q-Cosmetic-Date-Fix-Risk-Classification — Standard risk under 0d-ii (cosmetic, comment-only, not behavioural, not safety-critical, not user-facing). Confirm at session open.

For Framing C (step out), surface this sub-question:

* Q-Alternative — which alternative? Hold-point assessment work (use wired pattern data to test what works on real founder data per P0 0h Assessments 1-5)? Or step into a different priority entirely (P2 ethical safeguards — R20a vulnerable user detection, R17a bulk profiling prevention, R17b application-level encryption wiring, R17c genuine deletion endpoint, R19c/d limitations page + mirror principle, R20b/d independence + relationship asymmetry guidance)? Or something else entirely (e.g., O-PE-01-F MENTOR_CONTEXT_V2 decoupling decision)? Founder names the alternative; AI then surfaces the plan-walk questions for that alternative.

Step 2 — Critical Change Protocol (0c-ii) requirements.

The Critical/Standard classification depends on which framing is picked.

Framing A (live loader): Critical under PR6 (encryption pipeline blast radius — every profile read now also reads interactions, every recompute writes a new pattern_analysis). All five steps of the Critical Change Protocol must appear in the conversation before I deploy anything:

1. What is changing — plain language. No jargon. Name the specific files (loader file, the consumer endpoint that becomes the single-endpoint proof).
2. What could break — the specific worst case. Encryption-pipeline regression is the dominant risk. Read amplification on real founder traffic. KG3 drift if the loader's hub_id query doesn't match the canonical mapper. KG7 JSONB shape if `mentor_interactions` has a JSONB column that gets stringified.
3. What happens to existing sessions — does this affect users currently signed in? Existing pattern_analyses entries (fixture-derived) would be overwritten on next consumer call once the loader is live; the user-visible difference is that the mentor's pattern context becomes real-data-derived rather than fixture-derived.
4. Rollback plan — exact GitHub Desktop steps. The loader's revert should leave existing fixture-derived entries intact (they remain readable under 2A-skip until next consumer call rewrites them).
5. Verification step — what URL I visit, what I expect to see, what to do if the result is different. Likely: a probe that exercises the proof endpoint with the live loader, confirms `pattern_source: 'recomputed'` (cache miss → recompute from real data → save), then a re-run that confirms `pattern_source: 'persisted'` (cache hit on the freshly recomputed entry). Use the working `cookies['sb-access-token']` cookie pattern from the start.

Framing B (cleanup pass): Standard risk under 0d-ii for items (ii) and (iii); Standard for item (i) (read-only probe). The full Critical Change Protocol does not apply, but PR2 verification immediate still applies, and any code change still goes through founder approval before deploy.

Framing C (step out): Risk classification depends on the alternative picked. P2 ethical safeguards work is Critical for several items (R17a, R17b, R17c, R20a) per the project instructions — full Critical Change Protocol applies for any of those. Hold-point assessment work is generally Standard or Elevated (not modifying production code).

Cannot answer one of those? Stop and signal "I need your input" or "This is a limitation."

PR3 applies — pattern-engine call (when invoked via fallback or recompute) is synchronous, no fire-and-forget. PR4 applies — confirmed at session open. KG1 rule 2 applies if any new conditional write or new write surface is added. KG3 applies — any new reader/writer of hub-scoped data must use the canonical mapper or its allowlist equivalent. KG6 applies — any change altering prompt assembly must respect the user-message zone for per-request data.

Step 3 — Acceptance criteria.

This session ends successfully if at least one of these is true:

* The chosen framing's first implementation step reaches Verified status: TypeScript clean (`npx tsc --noEmit` exit 0 in `/website` if code is involved); the chosen verification probe passes (per-framing probe shape, designed in the CCP step 5); I push the commit(s) via GitHub Desktop; Vercel reaches Ready (if applicable); I confirm the round-trip works.
* The session ends earlier with a clear "I'm done for now" signal from me. Stabilise and close. Do not propose additional fixes.
* The session reveals a blocker that requires a smaller scoping change before the chosen work can land. In that case, surface the blocker with the AI signal "I need your input" and let me decide whether to proceed in this session or close and reopen with a narrower scope.

Step 4 — Session close.

Per the protocol Part C, produce a handoff at session end in the required-minimum format plus the relevant extensions (Verification Method Used / Risk Classification Record / PR5 Knowledge-Gap Carry-Forward / Founder Verification / Decision Log Entries — Adopted / Process-Rule Citations / Orchestration Reminder). Save to `/operations/handoffs/tech/[YYYY-MM-DD]-[short-description]-close.md`. Name the protocol explicitly as the governing frame at close.

If the session involved code and I pushed via GitHub Desktop, the handoff's "Founder Verification" section names the pushed commit hashes and the live-probe / verification result.

If the session involved any framing-A loader work that completes the live-loader integration, the handoff should also note whether the existing fixture-derived `pattern_analyses` entries on the founder's blob were overwritten by recomputed real-data entries (and at which probe versions), or whether they were left for natural per_request overwrite at the next consumer call.

Carry-forward open items (do not address unless I bring them up):

* Decision-log entries to confirm at session open: `D-PE-01-S1-1A-VERIFIED`, `D-PE-01-S2-2A-VERIFIED`, `D-PE-01-S3-3A-VERIFIED`, `D-PE-01-S35-3.5α-VERIFIED`, and `D-PE-01-S4-4B-VERIFIED` are all Adopted as of the 2026-04-26 Session 3.5 + 4 close. Commit hashes for all five pushes are placeholder ("TBD per founder share from GitHub Desktop History tab"); if I share the hashes at session open, AI amends all five entries inline. Framing B item (iii) is the natural place to do this if not done at session open.
* Cosmetic comment-date fix carried forward from Session 3: `/website/src/app/api/mentor/private/reflect/route.ts` carries comment blocks dated "27 April 2026" rather than "26 April 2026". Standard-risk one-line fix; can be batched with any future change to that file. Tiered as Efficiency & stewardship per PR9; absorbed into ongoing work. Framing A would naturally batch this if reflect is the chosen single-endpoint proof for the live loader. Framing B item (ii) is the explicit fix path.
* O-S5-A — `/private-mentor` chat thread persistence (pre-existing UX gap).
* O-S5-B — write-side verification of ADR-Ring-2-01 4b (deferred until natural triggering).
* O-S5-D — static fallback canonical-rewrite revisit.
* O-PE-01-A (read amplification) — every profile read decrypts the full blob even when pattern data is not needed. With Sessions 3 + 4 wiring two live consumers, this is concretely measurable on real reflect and founder-hub mentor traffic. Revisit if profile reads on either consumer become a measurable hot path.
* O-PE-01-B (blob-size monitoring) — versions 12+ now written by three writers (proof endpoint, reflect endpoint, founder-hub endpoint). Trigger condition for revisit is blob > 50 KB or encrypt/decrypt p95 > 50 ms. Not blocking; a quiet measurement when convenient. Framing B item (i) is the explicit measurement path.
* O-PE-01-C (optional `last_pattern_compute_at` plain column) — adding a plain timestamp column would let freshness queries skip decryption. More relevant if Framing A introduces freshness-driven cadence (e.g., throttled with a conditional). Defer until that condition triggers.
* O-PE-01-D (write cadence) — resolved for Sessions 1, 2, 3, 4 (per_request). Revisit in Framing A's plan walk if loader work surfaces concerns (e.g., per_request becomes too expensive once recompute is added to every cache-miss request).
* O-PE-01-E (backfill of existing profiles) — directly addressed by Framing A's Q-Backfill above.
* O-PE-01-F (NEW from Sessions 3.5 + 4) — pattern-read decoupling from `useProjection` / MENTOR_CONTEXT_V2 env var. Defer until the V2 feature flag retirement question lands on the table; not blocking.
* PR5 candidate (1 of 3) — Console-snippet auth-cookie discovery. The working pattern is `decodeURIComponent(cookies['sb-access-token'])` reading the raw JWT directly. This session marks the second successful use without rediscovery; counter remains 1 of 3 conservatively, but promotion to canonical at the next verification-probe session is reasonable.
* PR5 candidate (1 of 3, NEW) — two-phase staging within a single founder session, separated by a stop-point gate where Phase 1 must reach Verified before Phase 2 code begins. Demonstrated successfully in the 2026-04-26 Sessions 3.5 + 4 combined session. Promotion trigger: two more occurrences.
* PR5 candidate (1 of 3, NEW) — diagnostic-via-pipeline_meta spread-conditional pattern (`...(agent === 'mentor' ? { ... } : {})`). First introduced at Session 4 in `/api/founder/hub/route.ts` to surface mentor-only diagnostic fields without affecting non-mentor pipeline_meta shape. Promotion trigger: similar pattern used elsewhere.
* PR5 candidate (1 of 3, NEW) — sub-decision-after-framing-acceptance pattern (founder picks 4B + cadence + read precedence; AI begins CCP design; chicken-and-egg blocker becomes visible; AI signals "I need your input" with three sub-options; founder picks 3.5-α). Promotion trigger: similar mid-design surfacing in two more sessions.

Do not write any code until I have picked a framing (Step 1) AND named the cadence/scope sub-question for that framing (Q-Cadence/Q-Cleanup-Scope/Q-Alternative as appropriate) AND (if Framing A) named the read-precedence sub-decision AND (if Framing A) named the single-endpoint proof target AND the Critical Change Protocol (Step 2) is satisfied with my explicit "OK / go ahead" naming the listed risks (where Critical risk applies).

If I am only doing the plan walk and decide not to proceed to code in this session, that is a complete and successful session. Do not push for code.

---

End of prompt. Copy from "Governing frame:" through this line into the new session.
