# Recommendation Set for the P1 Review Session (Leg B, harnessed)

**Produced:** 2026-06-11, leg B. The set as a whole was consulted before adoption (`/api/reason`, standard depth, consult #11 — `principled`, kathekon true/strong); the two judgement-laden items carried their own standard-depth consults (#9, #10). Three items are explicitly the founder's to decide (marked **[FOUNDER]**); the rest are execution recommendations P1 can adopt by reference.

---

**R1 — Adopt this rebuilt pack as P1's input basis; archive `/business` with supersession stubs.** **[FOUNDER — archive timing]** The pre-pivot pack is structurally obsolete (F1), internally contradictory (F2), and its diversification argument is void (F3). Archive all eight files to `/archive/` with pointer stubs naming this pack (Elevated, per 0d-ii archive tier). P1 reads: verdict memo → inputs pack → findings memo → this set.

**R2 — Reframe the investment case on evidence gates; no month-twelve projections.** **[FOUNDER — adoption]** State as fact: working product verified for both audiences; observed unit economics (realized price = 2× Anthropic cost; 6–10¢/consult at real depths); cost base $5,674 upfront + $279/month (legal line is a floor pending the lawyer); zero external customers. Gate each further outlay on observed adoption signals (first external install; first paid onboard; first month of real-traffic ledger data) rather than projected revenue. The pre-pivot M12 numbers do not appear, even as "stretch" anchors. *Consult #9 directed this be written at full strength — the pull to soften it for reception is the named failure mode, and Character Kernel earns its keep here or is revised (rec 3.2 PR16 note).*

**R3 — Resolve launch criterion 2 by amendment.** **[FOUNDER — the criterion is founder-named]** Recommended branch: amend to *"billing mechanism proven; Stripe activation is a runbook triggered by the first paying consumer"* — consistent with the review's direction, the deliberate `not_configured` state, and the verified live metering (this run's headers). Write the activation runbook (Stripe account prerequisites depend on FPE-1 incorporation — sequencing already noted in the review). Alternative branch, stated honestly: activate pre-launch — adds configuration surface and compliance weight against zero customers; no observed argument for it. Decide once, on the record (PR7).

**R4 — Re-tune the $0.02 base rate only on real-traffic data, and fix the headline honestly.** The design's re-tune window (first 2–4 weeks live, Elevated edit) now has its first evidence: overage fires on **every** consultation-grade call, so the realized price is 2× Anthropic cost and "two cents per task" describes no real consult (F4). Either re-tune the base/trigger so the headline is true for typical traffic, or change the public sentence to the truthful "2¢ + metered model cost above 1¢ — your bill is at most 2× model cost." Exclude verification traffic from the tuning sample.

**R5 — Close the funnel-credential defects before the first external onboard.** From this run's live evidence (F11–F12): (a) fix the admin mint route's 667/50/20 hard-coded defaults to the adopted 30/1/1 (one-line class of fix; Elevated — it changes user-facing limits); (b) unify or honestly document the `sr_inst_`/`sr_live_` split on `/api/guardrail`; (c) give the per-install path cost visibility (emit the loop headers or document their absence in llms.txt); (d) populate `models_used`/token columns on `wrapper_internal` loops; (e) add `purpose` to the documented mint examples (PF-1).

**R6 — Tell the product story per audience until A8 lands.** Plan, pricing, and risk sections split human-practitioner (prose paths today, substrate after migration) from agent-developer (substrate contract now). The A8 mapping session is already elected pre-launch; the plan should treat its completion as the point the two stories converge (F7).

**R7 — Make the schedule spine compliance-first.** Lawyer engagement this week (Art-50 runway ~7.4 weeks); FPE-1..5 starts are founder wall-clock items independent of build pace; incorporation precedes Stripe account setup and changes privacy wording (F8). Build items (A8, score-conversation wiring, npm fixes, W1–W4) sequence inside that frame.

**R8 — Refresh market sizing around the agent-consumer thesis.** Fold in the June-15 Anthropic credit-pool change ($20–$200/month sanctioned agent budgets — a 6–10¢/consult service fits hundreds of consults/month inside the smallest cap); keep the competitor anchors as context, not as pricing inputs (F10).

**R9 — Harmonise the free tier across credential classes.** Three different free tiers exist today by mint surface (667/50/20 ecosystem-admin, 100/100/1 per-install, 30/1 schema default). Pick the adopted policy, apply it at every mint surface, and state it once in the public docs (F12).

**R10 — Fix the P1 session's decision list now.** P1 decides, on the record: criterion-2 branch (R3); investment-case adoption (R2); `/business` archive execution (R1); and notes the R4 re-tune trigger and R5 pre-onboard gate as standing conditions. Everything else in this set is execution it can adopt by reference.

---

*Set ends. Judgement items: R1 (timing), R2, R3. Consultation trail: `incorporation-log.md` consults #9–#11; raw payloads in `raw/`.*
