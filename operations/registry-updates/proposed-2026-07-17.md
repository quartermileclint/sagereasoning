# Registry Update Proposal — 2026-07-17 (RA-1)

**Registry version:** 1.6.0
**Registry lastUpdated:** 2026-06-10
**Lookback range (Pass 1 anchor):** 2026-06-10 (`D-REGISTRY-UPDATE-v1.6.0`, the most recent non-superseded update entry) → 2026-07-17
**Components audited (Pass 4):** 214 (= totalComponents)
**Handoffs scanned (Pass 1):** 166 files in `/operations/handoffs/founder/`
**Decision-log entries scanned (Pass 1):** 68 entries dated on/after the anchor
**Components proposed for update:** 13 existing rows
**New components proposed:** 90 — each needs explicit founder approval (minor version bump → v1.7.0)
**Ambiguous matches needing founder input:** see §6

**Method:** a 15-agent Workflow (9 Pass-1 arc scanners + 6 Pass-2/3/4 verification groups) — completed fully, 0 errors, ~3.87M subagent tokens. Every agent claim was then validated deterministically (id/path/duplicate/convention checks) and the load-bearing ones re-verified first-hand. **4 defects were found in the agent output before anything touched the registry** (§6).

---

## 0. HEADLINE — a plan-overturning finding (RA-1-F1)

**The mentor's second AMEND-CRITICAL is NOT stale. It is live and open. The reconciled build plan (§1a item 2) verified the wrong file.**

- The registry row `tool-sage-reflect` has path `/website/src/app/api/reflect/route.ts`. D24 (`consumer-workflow-audit.md:711`) names that endpoint verbatim (`endpoint: '/api/reflect'`). **`/api/mentor/private/reflect` has no registry row at all** — the plan verified `/api/mentor/private/reflect/route.ts:224–238` and declared the whole finding stale.
- **Fix A (fire-and-forget distress log): genuinely CLOSED** on the correct file — `await` at `route.ts:103` wraps the insert chain. (Nit: the vestigial `.then(() => {})` at `:115` discards the `{error}`, so a failed safety-log insert is silent — unlike the private route, which destructures and `console.error`s it.)
- **Fix B (`user_id` vs `auth.user.id`): OPEN.** The plan checked the *distress log* (which does correctly use `auth.user.id`, `:107`). But D24's Route-7 item 6 — which **D24 itself classes `Critical under PR6 (R17 intimate data protection perimeter)`** (`:1053`) — is about *persistence and profile update*:

  | Fact | Evidence |
  |---|---|
  | `requireAuth` only — **no founder gate** (contrast `/api/mentor/private/reflect:143–150`, which 403s non-founders) | `api/reflect/route.ts:69` |
  | body `user_id` accepted unvalidated | `:74` |
  | **no `user_id === auth.user.id` check anywhere** | grep: 0 hits |
  | reflection narrative written under the arbitrary `user_id` | `:181–189` |
  | analytics written under it | `:220–226` |
  | `updateProfileFromReflection(supabaseAdmin, user_id, …)` — mutates **another practitioner's** passion map + rolling window, which surfaces in *their* next mentor turn's Layer-2 context | `:236–248` |
  | all writes via `supabaseAdmin` — **service role, RLS bypassed** | `:3`, `:103/:178/:216/:240` |

  **Impact:** an authenticated user who knows another user's UUID can write intimate reflection text into that user's record and poison their longitudinal mentor profile. Write-misattribution, not a read leak. D24's own recommendation: add the equality check, or drop the body parameter and use `auth.user.id`.

  **Exposure today: ~zero** — 0h is held, there are no external users. **This does not make it stale.**

**Consequence for this proposal:** the `tool-sage-reflect` blocker is **NOT cleared**. It is *narrowed and sharpened* (§1). **Recommendation: RA-1-F1 gets its own founder-walked Critical session (R17/PR6), scoped alongside or ahead of RA-2's G1.**

---

## 1. Source-scan findings (Pass 1) — 13 existing rows

Full per-row evidence is in the workflow journal (`wf_4f0e5fa8-dd3/journal.jsonl`). Six rows had **field-level conflicts** — different arcs proposing different `notes` for the same row. These are *sequential true facts*, not competing claims (e.g. `tool-sage-guard`: the ADR-009 port activated 2026-06-19, then the §3 bridge retired 2026-06-26), so they are **merged**, not picked between.

| Row | Fields | Substance |
|---|---|---|
| `tool-sage-converse` | blocker, notes | **Blocker cleared** — R20a wired AND activated 2026-07-07; A8 migration remains. |
| `infra-invocation-guard-test` | desc, notes | desc/notes corrected — runs green under **tsx** (92/0), not "cannot run"; **11 route-level / 13 perimeter routes**, not 8. |
| `tool-sage-guard` | blocker, connects, desc, notes, status | **wired → live.** desc/notes replaced: the ADR-009 signed sandwich (Sonnet L1 → deterministic L2 → Ed25519), NOT the retired `runSageReason(quick)`/Haiku. §3 bridge retired 2026-06-26. CI-10 metering + CI-8 cost honesty + CI-9 diagnostic folded. Blocker added (deferred api-docs section; the guardrail-into-perimeter election). |
| `data-api-keys` | notes | CI-14 UPC schema live on prod. |
| `tool-sage-reason` | notes | ADR-010 §4 native weighting + corroboration check + Part-A continuation + M1 + CI-5 + CI-4 + CI-13 all Live. |
| `tool-delete` | notes | M6 + S9b reflect-erasure closed; honest null-owner boundary. |
| `tool-export` | blocker, notes | M6 trajectory + S10 §2e reflect fold; owner-scoping carried. |
| `tool-accreditation-public` | notes, status | **verified → live.** FX-11 closed; CI-11 columns restored silently-broken prod writes; examination_mode live. |
| `tool-sage-practice-reflect` | agentReady, blocker, notes | The completion-503 fix; context_source; S9b screened examination. agentReady na → partial. |
| `substrate-translation-sandwich` | notes | §4 + corroboration folded. |
| `infra-agent-discovery` | notes | Gate-1 extensions; 17 agent-card extensions. |
| `prod-premeditatio` | desc, notes | "Prepare a disposition" (#7-human) live 2026-07-13. |
| `prod-oikeiosis` | desc, notes | circle-extension (#6/#15) live 2026-07-14, gate-free. |

**Added by hand — the plan's §1 named corrections the arc-partitioned fan-out MISSED (6 of 12).** My fan-out partitioned by decision-log arc; these are pre-existing drift with no arc entry to surface them. An honest gap in my own method:

| Row | Field | Correction |
|---|---|---|
| `tool-sage-reflect` | blocker | **NARROWED, not cleared** — D24 fire-and-forget half closed; the R17/PR6 `user_id` half **stays open** (see §0). A8 claim verified TRUE (`:1`,`:18`,`:140` — raw `client.messages.create`). |
| `infra-r20a-classifier-eval` | desc | Group names are **factually wrong**. Real (verified first-hand): **A=`REGEX_FALSE_NEGATIVES`** (:19), **B=`CORRECT_PASS_THROUGHS`** (:57), **C=`CONTENT_SAFETY_EDGE_CASES`** (:160), **D=`CLINTON_PROFILE_ZONE2`** (:109) — not "clinical crisis / philosophical / ambiguous". |
| `infra-r20a-classifier-eval` | notes | Group D Haiku-verified at S8a 2026-06-10 (6/6; audit doc exists). **A/B/C never ran at Haiku and have NO runner** — `run-zone2-calibration-eval.ts:39` imports Group D only → RA-4. |
| `tool-sage-classify` | notes | "Factory wrapper" is **false** — 238 LOC, no `createContextTemplateHandler` import (verified). Bespoke. |
| `tool-sage-prioritise` | notes | "Factory wrapper" is **false** — 263 LOC, bespoke. (Exactly 13 routes use the factory, confirmed.) |
| `infra-r20a-classifier` | notes | humanReady `not-ready` residue annotated to the verified cause: the **four**-page rendering gap (G2) → RA-3. |

---

## 2-4. Code-grep / transitive / internal-consistency findings (Passes 2, 3, 4)

**83 findings — 19 high, 39 medium, 20 low, 5 nit.** Several are NOT registry drift but **real production honesty gaps**, which is why they are separated below.

### Class A — registry-field drift (fix in this update)

Rows whose fields contradict verified code. Representative high-severity:

- `infra-a10-plugin-auth` — **the registered `path` is the wrong file.** It points at `/api/keys/route.ts` (the self-service `sr_live_` mint, 222 LOC, zero `sr_inst_`/`install` references). A10 lives in `lib/plugin-install-auth.ts`.
- `infra-encryption` — status `wired` with **zero importers** (the only grep hit is its own docstring example). The *server* half is wired (12 importers); the client half is not.
- `reasoning-journal-layers` — desc/notes/blocker all claim "layers 1-8 wired; 9-10 stub". Code: **layer 1 extracted; layers 2–10 all stubs.**
- `data-profile-store` — notes claim "isolated… not verified"; 5 distinct `/website/src/` files import it.
- `data-trust-schema` — status `designed`, "needs review before deployment"; the approved subset **is deployed and live**.
- `prod-oikeiosis` / `prod-premeditatio` — `deps` claim `claude-api`; the oikeiosis routes import **no LLM client** (gate-free); `supabase` missing from both.

### Class B — REAL production gaps surfaced by the audit (NOT registry drift; each needs a named follow-up)

| # | Gap | Evidence |
|---|---|---|
| **B1** | **`/api/reflect` R17 write-misattribution** — see §0. | `api/reflect/route.ts:69,74,189,226,240` |
| **B2** | **`manifest.md:296` contradicts its own cited source** — it says "exactly the following **eight** human-facing POST routes, as enumerated in `r20a-invocation-guard.test.ts`"; that test enumerates **13**. The governing safety document understates the live perimeter. | `manifest.md:296` vs the test |
| **B3** | **`openapi.yaml` declares 12 of ~112 routes**, unmodified since 2026-04-03 — omits every agent surface shipped since. Row reads `agentReady: ready`. Also the registry `path` points at an **orphan** file referenced by nothing. | `/website/public/openapi.yaml` |
| **B4** | **`/product/AGENTS.md` last revised 2026-05-17** — zero mention of six agent-facing contracts that went live since. Row reads `agentReady: ready`. | `gov-agents-md` |
| **B5** | **`engine-agent-assessment` advertises a URL that 404s** — two live routes point agents at a framework JSON that isn't deployed. Row reads `agentReady: ready`. | `/api/assessment/full` |
| **B6** | **Quarterly governance review overdue** — due 2026-07-06 per `manifest.md:262`; 11 days late; no blocker on the row. | `gov-audit-log` |
| **B7** | `/api/community-map` 42703 (`show_on_map` missing) — already known, but `prod-community-map` is `live` with an **empty blocker**. | CLAUDE.md named follow-up |
| **B8** | `substrate-agent-mode-service` is `live`/`agentReady: ready` but **no route or page reaches it**, directly or transitively. | Pass-2 grep |

**B3/B4/B5 share a root:** three agent-facing surfaces marked `agentReady: ready` are materially incomplete or 404. That is an **R18 honest-claims cluster**, and it is directly relevant to the 0h launch call.

---

## 5. New component proposals — 90 (founder approval required)

Normalised to registry convention before proposal (§6). Full entries: `/private/tmp/claude-501/-Users-clintonaitkenhead-Claude-work-PROJECTS-sagereasoning/b48fe7ee-1419-4951-925f-cd805bac6ca4/scratchpad/new-components-normalised.json`.

| Status | Count |
|---|---|
| `live` | 62 |
| `verified` | 20 |
| `wired` | 6 |
| `scaffolded` | 2 |

| Prefix | Count | What |
|---|---|---|
| `infra-` | 26 | harness hooks, batteries, flags, corroboration check, SDK, crons |
| `substrate-` | 20 | the trust core + discernment + L4 audit + trajectory/narrative libs |
| `data-` | 15 | migrations: trust core, collaboration records, UPC schema, the 6 Remaining-Principles tables |
| `doc-` | 9 | ADR-009/010/011/012/013 + the trust-layer plan + SEVEN-LAYERS |
| `tool-` | 5 | /api/trust-record, /api/practice/discernment, /api/credential/erase, crons, mint CLI |
| `engine-` | 5 | guardrail-sandwich, loop-closure gate, logos-teaching, screened examination |
| `prod-` | 5 | the 5 new Remaining-Principles pages |
| `api-` | 5 | the 5 new /api/mentor/* routes |

---

## 6. Ambiguous matches / defects found IN THE AGENT OUTPUT (resolved before proposal)

The skill forbids silently choosing on ambiguity. These were caught by deterministic validation of the workflow's own output:

1. **ADR-010 proposed twice** (two agents, two ids, two types) → **deduped**; kept the `governance`-typed row per the `doc-rag-mentor-alt3-adr-01` precedent (prefix `doc-`, type `governance`).
2. **Prefix drift — 20 rows.** Agents proposed `engine-trust-*`/`infra-*` for files under `/website/src/lib/substrate/`. **Convention is path-determined and unambiguous: 0 of 22 `engine-` rows point there; 7 of 7 `substrate-` rows do.** → renamed to `substrate-*`. Zero cost (new rows; no existing row renamed).
3. **2 harness hooks** proposed as `harness-*` → `infra-gate1-*`, matching their 9 siblings.
4. **ADR type/prefix drift** → all ADRs normalised to `doc-adr-NNN-*` + type `governance`.
- *Not* a defect: **directory paths** (`substrate-trust-core/`, `sdk/typescript/`) — an established convention (`substrate-translation-sandwich`, `gov-knowledge-base`). The skill's rule bans the `+` separator, not directories.
- **Clean:** 0 duplicate ids, 0 collisions with existing ids, 0 collisions with existing paths, **0 proposed paths missing from disk**.

**Open for founder decision:**
- **(a)** The 27 `doc-rag-mentor-alt3-*` rows read `humanReady: ready` while every comparable internal design doc reads `na`. `journey` is null on all 62 document rows, so Q3 can't be applied mechanically. Batch default, or intended? *Not applied unilaterally.*
- **(b)** `infra-openapi` — the registry path points at an orphan (`/api/api-spec.yaml`, referenced by nothing). Repoint to the served `/website/public/openapi.yaml`, and dispose of the orphan separately? *No silent deletion.*
- **(c)** `doc-journal-layers` — a known pre-rename duplicate; deletion deferred for founder decision since 2026-04-28. Still deferred.

---

## 7. Verification (post-apply)

```bash
python3 -c "import json; d=json.load(open('website/public/component-registry.json')); print(d['version'], d['lastUpdated'], d['totalComponents'], len(d['components']))"
# expect: 1.7.0 2026-07-17 <N> <N>  (N == totalComponents == len(components))
cd website && npx tsx src/lib/__tests__/r20a-invocation-guard.test.ts | tail -1   # expect 92 passed, 0 failed
```