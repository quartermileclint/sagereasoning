# Registry Update Proposal — 2026-04-28

**Registry lastUpdated:** 2026-04-18
**Lookback range:** 2026-04-18 → 2026-04-28 (10 days)
**Handoffs scanned:** 47 close handoffs across founder/, tech/, ops/, support/, growth/ streams; decision-log.md entries from 2026-04-18 onwards
**Components proposed for update:** 6
**Components flagged ambiguous (founder decision):** 3
**Components left unchanged:** 13

**Skill version note:** This is the first invocation of `sage-registry-update` (PR1 single-endpoint proof). Treat outputs as both a registry update and a design test of the skill.

---

## APPLIED OUTCOMES (recorded post-apply, 2026-04-28)

**Founder approval:** "Apply 1, 2, 3, 5, 6; for A1 pick (a); for A2 pick (c); for A3 leave pending."

**Founder correction during review:** Surfaced that mentor-ledger functionality is live (founder's recall correct). AI investigation found the website re-implemented the logging via `/website/src/lib/logging/mentor-observation-logger.ts` rather than wiring through `/sage-mentor/mentor-ledger.ts`. Founder selected option (i) — add a 7th edit refining the `engine-mentor-ledger` blocker to reflect that distinction.

| Item | Founder decision | Status |
|------|------------------|--------|
| Edit 1 — agent-support status `designed` → `wired` | Approved | **APPLIED** |
| Edit 2 — engine-pattern-engine status + blocker | Approved | **APPLIED** |
| Edit 3 — engine-ring-wrapper status + blocker | Approved | **APPLIED** |
| Edit 4 — engine-profile-store | **Deferred** by founder (not in approved list) | **NOT APPLIED** — revisit alongside engine-mentor-ledger journey question |
| Edit 5 — agent-private-mentor blocker | Approved | **APPLIED** |
| Edit 6 — infra-resend blocker | Approved | **APPLIED** |
| Edit 7 (new) — engine-mentor-ledger blocker | Founder option (i) | **APPLIED** |
| A1 — engine-trust-layer | Founder option (a) — verify and refine | **INVESTIGATED + APPLIED** (refined blocker; type-only vs runtime distinction; bridge has zero callers) |
| A2 — new helper components | Founder option (c) — add only mentor-interactions-loader | **APPLIED** (engine-mentor-interactions-loader added; status `verified`; minor version bump) |
| A3 — agent-sage-ops cost_health_snapshots | Founder: leave pending | **NOT APPLIED** — revisit when P7 begins |

**Registry transitions:** `version` 1.2.0 → 1.3.0 (minor: new component); `lastUpdated` 2026-04-18 → 2026-04-28; `totalComponents` 163 → 164; `statusSummary` recomputed and corrected (previously had `scaffolded:1` undercounted, `wired:127`/`designed:5` overcounted; new self-consistent counts: `wired:124`, `designed:5`, `scaffolded:1`, `verified:32`, `live:2`).

**Pre-edit backup:** `/archive/component-registry/component-registry.json.backup-2026-04-28-0908`.

**Decision-log entry:** D-REGISTRY-UPDATE-1.3.0 (2026-04-28).

---

---

## Headline summary

The lookback covers a major architectural milestone: the full **ADR-Ring-2-01** shape-adapter migration (Sessions 1–5, 3a–3e, 4a–4c) **and** the full **ADR-PE-01** pattern-engine integration (Sessions 1–6 + cleanup pass) all reached **Verified** between 2026-04-25 and 2026-04-26. The mentor pipeline — ring-wrapper, pattern-engine, profile loader, mentor_interactions loader — is now wired end-to-end into two live consumers (`/api/mentor/private/reflect` and `/api/founder/hub`).

Several "Isolated" blockers on Sage Mentor components are therefore **partially stale**. The proposals below refine those blockers conservatively — they no longer say "isolated" where evidence shows live integration, but they preserve specific outstanding items (e.g., per-consumer 2A-recompute switch on founder-hub remains deferred per ADR-PE-01 §8 next step).

The remaining 13 components show no integration evidence and are left unchanged.

---

## Note on registry currency before this update

The registry's `lastUpdated` field reads 2026-04-18, but the file already contains journey-field changes from D-D1-1 through D-D1-12 (2026-04-25) and the D-D3-1 component rename (2026-04-22 — `doc-journal-layers` → `reasoning-journal-layers`). Those edits landed without a `lastUpdated` bump.

This proposal does not re-edit those fields (they're already current). It does bump `lastUpdated` to 2026-04-28 on apply, which has the side effect of making the registry's own self-report match its actual content.

---

## Proposed Edits

### 1. agent-support — Support Agent

**Current state:**
- status: `designed`
- blocker: "No run-loop caller exists. processInboxItem has zero external callers. Next: dedicated 3-session scope (ADR > build caller > mount + test per PR1). See: support-wiring-fix-close.md + support-wiring-mount-close.md"

**Proposed change:**
- status: `designed` → **`wired`**
- blocker: leave unchanged

**Evidence:**
- Source: `/operations/handoffs/support/support-wiring-fix-close.md` (line 5)
- Quoted text: *"Both channels reached 0a status **Wired** + verified at the unit level. Live integration (mounting on the support run-loop with the real Supabase client + real `detectDistressTwoStage` classifier) is the next session's work."*
- Quoted text (table line 21): *"Channel 1 — Support distress pre-processing | Scaffolded (not invoked) | **Wired** (unit-verified; integration-verified next session)"*
- Reasoning: Status field correction. Both channels were promoted to Wired at unit level on 2026-04-20. The blocker text remains accurate (no live run-loop caller yet) — only the status word was conservative. The ADR > build caller > mount sequence in the blocker is still the path to Verified.

---

### 2. engine-pattern-engine — Pattern Recognition Engine

**Current state:**
- status: `wired`
- blocker: "Isolated. Part of Sage Mentor. Not integrated."

**Proposed change:**
- status: `wired` → **`verified`**
- blocker: replace with: *"Live integration Verified end-to-end. Pattern-engine output persists in encrypted profile blob (ADR-PE-01) and runs on `/api/mentor/private/reflect` via per-consumer 2A-recompute (D-PE-01-S6, 2026-04-26) and on `/api/founder/hub` via 2A-skip on absence (D-PE-01-S4, 2026-04-26). Live `mentor_interactions` loader Verified at D-PE-01-S5 (2026-04-26). Outstanding: founder-hub per-consumer 2A-recompute switch is the natural ADR-PE-01 §8 next step (deferred)."*

**Evidence:**
- Source: `/operations/handoffs/tech/2026-04-25-pattern-engine-proof-close.md` (line 19)
- Quoted text: *"`sage-mentor/pattern-engine.ts` — was **Live-as-module / isolated** (zero imports from `website/src`); now **Wired-as-module → Verified** via the bridge into the mentor-ring proof endpoint (post-deploy live-probe confirmed deterministic analysis runs end-to-end on production)."*
- Source: `/operations/decision-log.md` D-PE-01-S6-REFLECT-RECOMPUTE-VERIFIED (2026-04-26)
- Quoted text: *"the decisive verification that the loader was actually invoked on a live consumer, returned 51 real `mentor_interactions` rows, was synthesised through pattern-engine successfully, and the live-recomputed result was persisted"*
- Reasoning: Six decision-log entries (D-PE-01-S1 through S6) all Verified on 2026-04-26 with live probes. The "Isolated. Not integrated" blocker is now factually incorrect.

---

### 3. engine-ring-wrapper — Ring Wrapper (BEFORE/AFTER)

**Current state:**
- status: `wired`
- blocker: "Isolated. Part of Sage Mentor subsystem; not integrated into website."

**Proposed change:**
- status: `wired` → **`verified`**
- blocker: replace with: *"Verified across three proof endpoints (mentor-ring, support-agent, founder-hub-ring) per D-RING-1 (2026-04-25). Canonical-profile shape-adapter migration (D-RING-2 / ADR-Ring-2-01 Sessions 1, 2, 3a–3e, 4a–4c) reached Verified on 2026-04-26 — `/api/founder/hub`, `/api/mentor/private/reflect`, `/api/mentor-baseline-response`, `/api/mentor-profile`, `/api/mentor/private/baseline-response`, `setFounderFacts`, `appendFounderFactsNote`, and `saveMentorProfile` all consume canonical `MentorProfile`. Transitional `ProfileForSignals` retired (Session 4c)."*

**Evidence:**
- Source: `/operations/handoffs/tech/2026-04-25-ring-wrapper-three-flow-proofs-close.md` (line 18)
- Quoted text: *"`sage-mentor/ring-wrapper.ts`: Wired-as-module → **Verified** (proven end-to-end on production across three flows with real LLM calls)"*
- Source: `/operations/decision-log.md` D-RING-2-S4C (2026-04-26)
- Quoted text: *"both callers (`/api/founder/hub` and `/api/mentor/private/reflect`) now pass canonical `MentorProfile`. Tightening the function signature to canonical-only makes the contract explicit and removes the structural-type indirection."*
- Reasoning: Both proof-endpoint Verification (D-RING-1) and live-endpoint canonical migration (D-RING-2 chain) complete. "Not integrated into website" is no longer accurate.

---

### 4. engine-profile-store — Profile Store (sage-mentor's `/sage-mentor/profile-store.ts`)

**Current state:**
- status: `wired`
- blocker: "Isolated. Part of Sage Mentor. Not integrated."

**Proposed change:**
- status: leave at `wired` (caution flag — see Reasoning below)
- blocker: replace with: *"Type contract integrated: canonical `MentorProfile` exported from `/sage-mentor/` and consumed by website's `engine-mentor-profile-store` (`saveMentorProfile`/`loadMentorProfileCanonical`) per ADR-Ring-2-01 Session 4b (D-RING-2-S4B, 2026-04-26). Sage Mentor's own `/sage-mentor/profile-store.ts` runtime functions (`saveProfile`, `loadProfile` etc.) are not invoked from website — the website uses its own profile-store with the canonical type. Consider whether this component should be marked deprecated-runtime (type-source-only) given the website's `engine-mentor-profile-store` is the live runtime."*

**Evidence:**
- Source: `/operations/decision-log.md` D-RING-2-S4B (2026-04-26)
- Quoted text: *"`saveMentorProfile()` parameter type migrated from `MentorProfileData` to canonical `MentorProfile`."*
- Source: Grep of `/website/src/` imports — `import type { MentorProfile } from '../../../sage-mentor'` appears in 8+ files; no runtime imports from `/sage-mentor/profile-store` other than via `sage-mentor-bridge.ts`.
- Reasoning: This is a nuanced case. The Sage Mentor `profile-store.ts` runtime is still not directly invoked by website routes. But its **type** (`MentorProfile`) is now canonical across the website. Two registry components exist: `engine-profile-store` (this one, `/sage-mentor/`) and `engine-mentor-profile-store` (website's, integrated). The blocker should reflect the type-vs-runtime distinction.
- **Status caution:** I'm not promoting status to verified because the runtime functions still aren't invoked. Founder may prefer to either (a) leave status at wired with the refined blocker, (b) add a new `journey: deprecated-runtime` value à la D-D1-4 for engine-llm-bridge, or (c) deprecate this component entirely. **Flagging for founder decision below.**

---

### 5. agent-private-mentor — Private Mentor (Intimate Data Boundary)

**Current state:**
- status: `wired`
- blocker: "Isolated. Part of Sage Mentor subsystem. Journal interpreter layers 9-10 incomplete (25 TODOs)."

**Proposed change:**
- status: leave at `wired`
- blocker: replace with: *"`/api/mentor/private/reflect` (the Private Mentor's primary write endpoint) Verified end-to-end on 2026-04-26: canonical-profile loader (D-RING-2-S4A), pattern-engine integration (D-PE-01-S3, D-PE-01-S6 per-consumer 2A-recompute), live `mentor_interactions` loader (D-PE-01-S5). Distress classifier wrapper untouched per AC5. Outstanding: Journal interpreter layers 9-10 stub only (25 TODOs); `/private-mentor` page chat thread does not load past reflections from `mentor_interactions` on page mount (UX limitation logged as O-S5-A in 2026-04-26 Session 4 close)."*

**Evidence:**
- Source: `/operations/decision-log.md` D-RING-2-S4A (2026-04-26)
- Quoted text: *"`/website/src/app/api/mentor/private/reflect/route.ts` (R20a perimeter, AC5 — Critical) switched from `loadMentorProfile()` to `loadMentorProfileCanonical()`. The route's distress-classifier wrapper... was untouched by the migration."*
- Source: D-PE-01-S6-REFLECT-RECOMPUTE-VERIFIED — three-probe verification of reflect with live loader
- Reasoning: "Isolated. Part of Sage Mentor subsystem. Not integrated" is no longer accurate for the Private Mentor's main pipeline. Layers 9-10 incomplete remains true and is preserved. Status promotion held at `wired` because the journal-interpreter layers are part of this component's surface and remain incomplete.

---

### 6. infra-resend — Email (Resend)

**Current state:**
- status: `wired`
- blocker: "Isolated. Part of Sage Mentor. Not integrated into website."

**Proposed change:**
- status: leave at `wired`
- blocker: replace with: *"Isolated. `/sage-mentor/send-notification.ts` exists with raw `fetch` API; zero callers in `website/src` (read-and-report close, 2026-04-26). Four blockers identified before integration: (a) trigger pipeline (cron / scheduled-function / in-request), (b) audit-trail table for sent notifications, (c) deep-link signing for clickable links in emails, (d) Resend retention verification. ADR-Resend-01 deferred pending P2 ethical-safeguards work (R17 / CR-025). Wiring without (a)–(d) would produce code that exists but is never called (AC4 anti-pattern)."*

**Evidence:**
- Source: `/operations/handoffs/tech/2026-04-26-infra-resend-read-and-report-close.md`
- Quoted text: *"Wiring `sendNotification` into `proactive-scheduler` without (1)–(4) would produce code that exists but is never called — the AC4 anti-pattern. The single-endpoint-proof discipline (PR1) cannot run without an endpoint that actually gets called. This is the same redirect shape as the 2026-04-26 mentor-ledger-wiring → ADR-PE-01 sequence."*
- Reasoning: The blocker text is accurate but vague. The read-and-report close gives specific, named architectural blockers. Refining preserves the same gating but documents the work shape concretely.

---

## Ambiguous Matches Requiring Founder Decision

### A1. engine-trust-layer — "Zero imports from website/src" claim is partially falsified

**Source:** Grep of `/website/src/` for `from '../../../trust-layer'`
**Quoted evidence:**
- `/website/src/lib/mentor-profile-adapter.ts:55` — `} from '../../../trust-layer/types/accreditation'`
- `/website/src/lib/trust-layer-bridge.ts:20` — `} from '../../../trust-layer'`

**The ambiguity:** The current blocker reads *"Architecturally isolated. Zero imports from website/src. All 14 modules wired to each other but none consumed by the live app."* Two imports now exist. They may be type-only (compile-away) or runtime. I have not opened the files to verify which.

**Founder decision needed:** Three options:
- **(a) Verify and refine the blocker.** Read both files, determine type-vs-runtime, update blocker text accordingly.
- **(b) Refine to acknowledge type-imports.** Change "Zero imports" to "Zero runtime imports" if both are type-only.
- **(c) Leave unchanged and revisit when Trust Layer becomes a P3 priority.** The component is still architecturally isolated in any meaningful sense; refining the blocker text without changing the underlying integration doesn't help.

---

### A2. New helper components mentioned in handoffs — should they be added to the registry?

Three new files created during the lookback that are currently NOT in the registry:

1. **`/website/src/lib/mentor-profile-adapter.ts`** — created in shape-adapter Session 1 (2026-04-25). Pure synchronous adapter from `MentorProfileData` (legacy) to `MentorProfile` (canonical). Has tests at `/website/src/lib/__tests__/mentor-profile-adapter.test.ts`.
2. **`/website/src/lib/sage-mentor-ring-bridge.ts`** — created in pattern-engine proof session (2026-04-25). Re-exports pattern-engine functions and types from sage-mentor for the ring proof endpoint's use.
3. **`/website/src/lib/mentor-interactions-loader.ts`** — created in ADR-PE-01 Session 5 (2026-04-26). Loads pattern-engine input from the live `mentor_interactions` table, with KG7 defensive parse and worst-case-D fallback.

**The ambiguity:** Are these distinct components warranting registry entries, or are they "wiring code" / bridge files that don't need their own component IDs? The skill rules say "No silent additions" — this needs founder decision.

**Founder decision needed:** Three options:
- **(a) Add all three as new components.** Probably under `engine-` prefix. This raises totalComponents from 163 → 166 and triggers a minor version bump (1.2.0 → 1.3.0).
- **(b) Add none.** Treat as wiring-code that the existing components depend on but don't warrant their own entries.
- **(c) Add only `mentor-interactions-loader.ts`.** It's the most architecturally significant of the three (the "live loader" closing ADR-PE-01's deferred item). The other two are pure adapter/bridge plumbing.

---

### A3. agent-sage-ops — `cost_health_snapshots` table migration claim

**Source:** Current blocker reads *"Supabase migration not run (cost_health_snapshots table). Ops activation scoped for P7."*
**Quoted evidence:** None found in the lookback. Searched all close handoffs from 2026-04-18 to 2026-04-28 — no mention of `cost_health_snapshots`.

**The ambiguity:** No evidence either way. The blocker may still be accurate (P7 hasn't started) or the migration may have been quietly run. The Sage Ops sessions on 2026-04-21 (live-data-wiring) and 2026-04-22 (D3 registry hygiene) and 2026-04-24 (D1 journey second pass) all worked **on** Sage Ops without addressing this specific item.

**Founder decision needed:** Should the AI verify by running a Supabase query to check the table's existence, or leave the blocker as written pending P7?

---

## No-Change Findings

The following 13 components had their blockers reviewed against scanned handoffs and no evidence justifies a change:

- `agent-session-bridge` — no mentions in lookback; remains "Isolated. Part of Sage Mentor subsystem. Not integrated."
- `engine-accred-card` — no mentions in lookback; remains "Isolated. Complete internally but not integrated."
- `engine-authority-mgr` — no mentions in lookback; remains "Isolated. Part of Sage Mentor / Trust Layer. Not integrated."
- `engine-embedding` — no mentions in lookback; "Embedding API not yet configured" remains accurate.
- `engine-llm-bridge` — already marked `journey: deprecated` per D-D1-4 (2026-04-25); blocker text stating "candidate for deprecation rather than integration" already aligns with that decision.
- `engine-mentor-ledger` — wiring brief was redirected to ADR-PE-01 (D-PE-LEDGER-WIRING-REDIRECTED, 2026-04-26) but mentor-ledger itself was not integrated; the redirect produced an ADR for pattern-analysis storage (which is now Verified end-to-end), not for ledger persistence. Ledger remains isolated.
- `engine-proactive` — confirmed isolated by 2026-04-26 infra-resend read-and-report close (line: *"Confirmed unwired. Pure types + functions; no callers in `website/src`; no email send anywhere in the module."*).
- `engine-progression` — no mentions in lookback.
- `infra-stripe` — no mentions in lookback; P4 deferral remains accurate.
- `reasoning-journal-layers` — D-D3-1 (2026-04-22) renamed and cleaned the description; current blocker text "Layers 9-10 stub only (25 TODOs). Spec exists but implementation incomplete" matches the post-rename state.
- `reasoning-sanitise` — no mentions in lookback.
- `agent-private-mentor` (proposed-edit #5 covers refinement, not "no change" — listed in proposals).
- `agent-sage-ops` flagged as ambiguous — see A3.

---

## Apply mechanics if approved

Per the skill, on approval the following side effects occur (Step 6 onward):
- Pre-edit backup at `/archive/component-registry/component-registry.json.backup-2026-04-28-HHMM`
- `lastUpdated` → `2026-04-28`
- `version`: `1.2.0` → `1.2.1` (patch bump — field-only updates to existing components, no new/removed components — assuming founder rejects A2 option (a) and (c); if A2 (a) approved, minor bump to `1.3.0`; if A2 (c) approved, minor bump to `1.3.0`)
- `statusSummary`: counts recomputed (status changes affect: `wired -2`, `verified +2`, `designed -1` if all four status-changes approved)
- `totalComponents`: stays at 163 unless A2 is approved
- Decision-log entry written
- Git command bundle provided to founder for terminal paste

---

## Founder review prompt

Please reply with one of:
- **"Apply all edits 1–6, decide A1=(b), A2=(b), A3=leave, proceed"** (one example — substitute your choices)
- **"Apply only 1, 2, 3, 6"** (or any subset)
- **"Reject and rescope"** (if the proposal needs rework)
- Per-edit instructions (e.g., "Apply 1 and 6, defer the rest pending verification")

For ambiguous items A1–A3, either pick an option or instruct the AI to investigate further before proposing again.
