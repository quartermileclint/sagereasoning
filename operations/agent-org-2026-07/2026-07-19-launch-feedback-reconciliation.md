# Launch-feedback reconciliation — 28 agent-supplied items vs verified production state

**Created 2026-07-19** under `D-LAUNCH-FEEDBACK-RECONCILIATION-FOLDED-INTO-AO-PLAN-2026-07-19`. **Source input for Agent-Org P1** (the gap-map session) and the new Go-Live session (§3).

The founder relayed 28 launch-readiness items authored by the Tech/Growth/Support/Ops role-agents. Each was classified against the ACTUAL codebase (routes/libs/migrations/flags — not CLAUDE.md claims) by a finder, then INDEPENDENTLY adversarially re-verified by a second agent. Workflow `wf_3e81a945-4b8`: 13 agents, 0 errors, ~3.18M tokens, 246 tool calls. **Every classification CONFIRMED on re-verify — zero overturned** (the re-verify sharpened evidence, flipped no verdicts).

**Classification key:** LIVE = built + serving now · BUILT_OUT_OF_SCOPE = built but deliberately dark by a recorded decision · PARTIAL = some exists, real work remains · GAP = genuinely unbuilt · ORG_DECISION = founder ownership/vendor decision, not a build · MISCHARACTERIZED = the item's premise is factually wrong.

**Counts:** LIVE 4 · BUILT_OUT_OF_SCOPE 1 · PARTIAL 12 · GAP 6 · ORG_DECISION 4 · MISCHARACTERIZED 1 = 28. Already-done (no build): items [1, 2, 3, 4, 19, 24]. Of the 5 items called TIER-1 pre-launch blockers, **only 1 (#5) is a genuine gap.**

## Headline

> Of the five items your role-agents flagged as TIER1 pre-launch blockers, only one (#5, error monitoring) is a genuine gap — and even that is an observability should-have, not a broken-site blocker: #1 R20 safety, #2 R17 encryption, and #3 API-key auth are already LIVE and need only a ~5-minute prod confirmation, and #4 Layer-3 context is mischaracterized (already wired live across 20 routes). Nothing in core product, safety, or security is a real launch blocker; the genuine remaining work is small and clusters in two places — solo-founder observability builds (error store, real health probes, human-route graceful degradation, rate-limit logging) and business-ops ownership calls (who watches support@, incident/rollback owner, email vendor). Your single highest-value quick fix is #28: the privacy page currently tells users to use Export/Delete dashboard buttons that are disabled placeholders — the backend is fully built, so it's a ~1-hour wire-up that closes a live GDPR-honesty defect that the feedback buried as a PARTIAL. Recommended path: run one go-live checklist session to verify the LIVE items and knock out the ~8 small gate builds, route the ownership/vendor decisions into the Agent-Org P1 gap-map, and defer the scale/nice-to-have items. The feedback being this stale about your own build is itself the finding — direct evidence for the Agent-Org context-provisioning work.

## Per-item verified table

| # | Item | Class | Disposition | Conf | True state (verified) |
|---|---|---|---|---|---|
| 1 | Vulnerable User Safety Infrastructure (R20) | LIVE | fold_into_go_live_checklist | high | Fully built and serving. The distress pre-screen fires before the Stoic engine on every human route with no flag gate (compile-time SafetyGate discipline), halts the session at moderate/acute with a crisis-resource redirect, and logs the event (metadata) not the content. Two nuances unchanged: 'regi |
| 2 | Encryption Verification (R17) | LIVE | fold_into_go_live_checklist | high | AES-256-GCM is defined and actively wired to all practitioner intimate data at rest via a single key variable, MENTOR_ENCRYPTION_KEY (the item's premise 'ENCRYPTION_KEY' names the wrong variable). Because getEncryptionKey() throws when the key is absent, any successful encrypted write in prod proves |
| 3 | API Key Validation on External Endpoints (Sage Assent, skill | LIVE | verify_only | high | Every agent-facing / LLM-calling external endpoint is auth-protected and reachable-only-with-a-credential; the control is in place. Two minor imprecisions in the prior evidence, neither weakening the conclusion: (1) sage-classify and sage-prioritise use requireAuth (user-JWT-only), not the context-t |
| 4 | Layer 3 Context Architecture Completion (project-context inj | MISCHARACTERIZED | already_done_no_action | high | The item conflates two different 'Layer 3's. The reasoning-prompt three-layer context (L1 Stoic Brain / L2 Practitioner / L3 Project Context injection) is wired and live across all human-tool/skill/mentor/agent routes — its premise ('in progress but not verified live') is factually wrong. The substr |
| 5 | Error Monitoring — console.error only; MVP = Supabase errors | GAP | fold_into_go_live_checklist | high | Accurate. Production route errors go to console.error → ephemeral Vercel function logs only; there is no queryable/alertable error store, and the OTel path exports to console, not a backend (production export backend deferred to PR7). An errors-table MVP with server-side inserts is genuinely unbuilt |
| 6 | Health Check Endpoint (/api/health validating DB + Anthropic | PARTIAL | fold_into_go_live_checklist | high | The endpoint is built and public but does exactly what the item warns against: it reports 'healthy'/'connected' on config presence, so a live DB outage or an invalid/expired Anthropic key (env vars still set) would still report healthy. It is a config-presence check mislabeled as a health check. |
| 7 | Token Usage Tracking Pipeline — per-endpoint token usage to  | PARTIAL | fold_into_go_live_checklist | high | The premise understates what exists — R5 (Anthropic cost as the largest variable expense) is already tracked at the loop grain on the paid agent surfaces AND actively alerted via the revenue:cost detector delivered through the daily observability cron to Slack. Genuinely incomplete: token-COUNT capt |
| 8 | Rate Limit Visibility — rate limiting implemented but invisi | GAP | fold_into_go_live_checklist | high | Accurate. Rate limiting exists (IP in-memory + API-key quota) but throttle events are never logged, so in production a 429 cannot be distinguished from a bug and throttle rates are unobservable. The IP limiter is entirely invisible and resets on every cold start; quota throttles are inferrable only  |
| 9 | Supabase RLS Policy Audit (document which tables have RLS +  | PARTIAL | fold_into_go_live_checklist | medium | RLS is genuinely built and looks sound — every PII/user-scoped and security-critical table checked has RLS (owner-scoped for human tables, service-role-only + REVOKE for substrate/agent tables). What is missing is the item's actual ask: a consolidated, per-table audit that positively confirms every  |
| 10 | Graceful Degradation for Anthropic Outages — fallback respon | PARTIAL | fold_into_go_live_checklist | high | Half-true and half-unmet. The safety perimeter (reason substrate + guardrail gate) already degrades conservatively/honestly by design (KG1). The human tool routes, mentor routes, and /api/reason's outer catch-all return raw 500s on an LLM outage — the item's specific ask ('honest message not a 500'  |
| 11 | Support Ticket Intake (MVP email intake + categorisation; be | PARTIAL | fold_into_go_live_checklist | high | The email-intake MVP surface (mailto links) is live everywhere, but it dead-ends at an inbox whose monitoring is explicitly unconfirmed, with no categorisation pipeline wired to user reports and no in-app form. The support-agent schema + triage brain are designed and driven only by offline founder t |
| 12 | Human Escalation Decision (who handles human support at laun | ORG_DECISION | fold_into_AO_P1_gapmap | high | A founder ownership/vendor decision, not a build item. Nothing in code presupposes an answer; the escalation brain gives the agent instructions but there is no human-handoff mechanism or vendor wired in. 'Who handles it' belongs in the Agent-Org P1 gap-map. |
| 13 | Auth Recovery Flow Verification (Supabase self-service passw | PARTIAL | fold_into_go_live_checklist | medium | Account recovery is functionally reachable via the magic-link (a forgotten-password user can still sign in by email), but self-service PASSWORD RESET specifically is not built in the UI. Whether Supabase's recovery/OTP email templates are enabled and tested is dashboard config the repo cannot show — |
| 14 | Analytics Setup with Conversion Tracking | PARTIAL | fold_into_go_live_checklist | high | A first-party event-tracking pipeline with genuine conversion/funnel events (sign_up -> sign_in -> baseline_completed -> dashboard_view -> score_action) and an admin metrics dashboard is built and serving; the item premise that analytics is absent is wrong. Genuinely missing is reporting sophisticat |
| 15 | Email Platform Selection & Setup | ORG_DECISION | fold_into_AO_P1_gapmap | high | No email platform is selected, no email is sent anywhere, and no reminder/delivery cron exists. The weekly-reflection prompt exists only as an in-app form the user must visit; the email retention loop the item describes is unbuilt. Selecting a vendor is a founder decision; the delivery integration ( |
| 16 | Launch Readiness Checklist (go/no-go) | GAP | new_named_session | high | There is no documented go/no-go readiness checklist spanning positioning/content/discovery (Growth), safety infra (Support), and Tier-1 verified (Tech). Many underlying pieces ARE verifiable/live, so the checklist would largely assemble already-verified state into one go/no-go artifact — but that ar |
| 17 | Caching Persistence: in-memory cache doesn't survive cold st | GAP | defer_post_launch | high | Premise is factually correct: the cache is a per-instance in-memory Map that does not survive Vercel cold starts, and no Upstash/Supabase-backed persistent cache exists. Genuine gap, but a TIER3 at-scale optimization — with the launch held (0h) and low traffic the per-instance LRU is adequate, so it |
| 18 | Practitioner Session Continuity: session-level context accum | PARTIAL | fold_into_AO_P1_gapmap | medium | Partly built, partly mischaracterized. Cross-instance reasoning-history surfacing IS live (meta.trajectory) and Reflect accumulates cross-session, so 'reasoning history informs later work' is already partly served. But the literal ask — a server-side within-session context that feeds the reasoning e |
| 19 | Agent Developer Onboarding Infrastructure: API key issuance  | LIVE | verify_only | high | Already built and wired end-to-end — none of the three named components is missing. Self-service /api/keys mint (auth-guarded, 5-key cap, free-tier defaults), the developer docs surfaces, and atomic per-key monthly/daily quota enforcement (429 on /api/reason) are all present. The only deferred adjac |
| 20 | Feedback & Quality Signal Pipeline: lightweight thumbs up/do | GAP | defer_post_launch | high | Genuinely not built — no thumbs up/down (or any user quality-signal) capture on reasoning responses, and no Supabase table backing one. Nothing else in the code covers it. TIER3 item, not a launch blocker, but a natural first input for the Agent-Organization Evidence Program (which needs response-qu |
| 21 | Deployment Rollback Protocol (written decision rules for whe | GAP | fold_into_AO_P1_gapmap | high | The raw material (per-flag/per-deploy rollback recipes) is abundant, but the deliverable — a written protocol with decision rules and an incident/rollback runbook — does not exist. Ownership of incident response is unassigned (AO plan line 45). |
| 22 | Database Migration Strategy (schema changes currently ad hoc | PARTIAL | fold_into_AO_P1_gapmap | high | The premise is accurate about the MANAGEMENT layer: application is manual via the Supabase SQL Editor, ordering/applied-state is untracked, and two file locations coexist. Per-file hygiene is strong; the missing piece is a formalized, tracked migration system/strategy. |
| 23 | Knowledge Base (searchable help centre — framework explanati | PARTIAL | defer_post_launch | high | The item's premise ('only text in the Support brief') is nearly right — the KB is internal-agent text — but understates that several standalone explanatory pages already serve framework + scope content. The specific deliverable (a searchable help centre / how-tos hub) is genuinely absent. |
| 24 | Dependency Detection (R20b) | BUILT_OUT_OF_SCOPE | defer_post_launch | high | Both the detection lib and the coaching consumer (including the usage-data feed) are built and wired; it runs the moment the flag is set true. It is dark by a recorded off-perimeter decision (reviewed at S6), diagnostic-only and non-blocking. |
| 25 | Content Production Workflow | ORG_DECISION | fold_into_AO_P1_gapmap | high | Channel cadence guidance exists as Growth-brain context, but the operational workflow the item asks for (founder drafts -> Growth reviews -> founder publishes, on a defined cadence, with named owners) is not defined anywhere. This is primarily a process/ownership definition for the Growth agent, not |
| 26 | Competitive Intelligence Feed | PARTIAL | fold_into_AO_P1_gapmap | high | Competitor monitoring partly exists as one topic inside the broader weekly environmental scan (last refreshed 2026-07-13), backed by JSON + Supabase table + lib. It is NOT a dedicated structured competitive feed (per-competitor positioning/pricing/publishing), and NOT automated — cadence depends on  |
| 27 | Community Health Metrics & Support Analytics Dashboard — 30- | ORG_DECISION | fold_into_AO_P1_gapmap | medium | The support-analytics half presupposes a support/ticketing operation (tool/vendor, process, defined SLAs) that does not exist — a founder ownership/tooling/vendor decision, not primarily a build. It is also premature pre-launch (0h held, ~nil traffic — resolution times/SLA compliance cannot be measu |
| 28 | User-Facing Data Export & Deletion | PARTIAL | fold_into_go_live_checklist | high | The compliance-critical backend (genuine deletion, encrypted-data export/decryption, Art-15 access disclosure, audit logging, rate-limiting) is fully built and live. Missing is the user-facing self-service: the two dashboard buttons are disabled placeholders, so a practitioner can exercise these rig |

## Genuine remaining work (ranked) + routing

- **#28 User-facing data export & deletion (wire 2 disabled buttons + fix privacy copy)** — _new_go_live_checklist_session_. LIVE defect: the privacy page tells users to use Export/Delete dashboard buttons that are disabled '(coming soon)' placeholders — an Art-12 facilitate-the-rights / honest-disclosure contradiction AND an unmet self-service right. The compliance backend (genuine deletion, encrypted export, Art-15 access, audit logging) is fully built and live. Highest ROI on the list. **Effort:** ~1-2 hours: wire the two buttons to the existing /api/user/export + /api/user/delete endpoints (Bearer JWT from the sb-*-auth-token localStorage key; requireAuth is Bearer-only), add a confirm modal, and correct the privacy page copy.
- **#11 Support inbox monitoring (support@sagereasoning.com)** — _into_AO_P1_gapmap_. The mailto intake is live everywhere but dead-ends at an inbox whose monitoring is explicitly unconfirmed (terms/page.tsx carries a live 'Pre-launch: confirm support@ is configured and monitored' comment). A vulnerable-user product must have a reachable, watched support channel at launch. This is an ownership assignment, not a build. **Effort:** Org decision (minutes to assign an owner/rota); the go-live checklist confirms it. The optional in-app support_interactions form (schema exists, unwired) is a post-launch build.
- **#16 Launch go/no-go readiness checklist (the doc itself)** — _new_named_session_. No consolidated cross-functional go/no-go artifact exists; the operative gate is the single undocumented founder '0h call'. This audit's per-item findings plus the CLAUDE.md Live/inert lists ARE its raw inputs, so it largely assembles already-verified state. **Effort:** This is the go-live session's own output; ~1 session.
- **#5 Error monitoring MVP** — _new_go_live_checklist_session_. The only genuine gap among the 5 'TIER1 blockers'. A solo founder is otherwise blind to prod errors beyond ephemeral Vercel function logs. Operationally important the moment real users arrive. **Effort:** Small — errors Supabase table + shared insert helper in route catch blocks, OR wire instrumentation.ts/OTel to a real backend. Half a session.
- **#10 Graceful degradation on human/mentor routes during Anthropic outages** — _new_go_live_checklist_session_. The safety perimeter (/api/reason substrate + /api/guardrail) already degrades conservatively by design, but the human score routes, mentor routes, and /api/reason's OUTER catch-all return raw 500s on an LLM outage. At launch, any Anthropic hiccup shows users an ugly 500 instead of an honest 'temporarily unavailable'. **Effort:** Small-medium — honest 503 and/or transient retry-with-backoff at the human-route catch blocks + the /api/reason outer catch, distinguishing outage from generic 500.
- **#6 Health check with real DB + Anthropic reachability probes** — _new_go_live_checklist_session_. /api/health checks env-var PRESENCE only and mislabels config as 'connected'. A live DB outage or an invalid/expired Anthropic key (env still set) would still report 'healthy' — a false green light for the founder's own monitoring. **Effort:** Small — add a lightweight DB SELECT/RPC and a cheap Anthropic ping, each with a timeout; degrade status on failure. Rename 'connected' labels.
- **#13 Auth recovery / password reset** — _new_go_live_checklist_session_. No dedicated password-reset UI exists (auth page has signin/signup/magic only). Magic-link works as a functional recovery path, so users aren't locked out, but self-service password reset is unbuilt and the Supabase recovery/OTP email templates are unverifiable from the repo (medium confidence). **Effort:** Small — founder decides magic-link-suffices vs build a reset UI, then verifies/tests the Supabase Auth dashboard recovery/OTP templates end-to-end (dashboard config, not code).
- **#8 Rate-limit / throttle event logging** — _new_go_live_checklist_session_. Rate limiting exists (IP in-memory + API-key quota) but throttle events are never persisted, so a production 429 can't be distinguished from a bug and throttle rates are unobservable. The IP limiter is invisible and resets on cold start. **Effort:** Small — persist throttle events (category, ip/key, endpoint, timestamp) at the 429 return points in security.ts.
- **#9 RLS audit deliverable + clear 3 no-RLS tables** — _new_go_live_checklist_session_. RLS is broadly and soundly applied (37 files, 97 policies; every PII/security-critical table protected), but the item's actual ask — a positive per-table inventory — doesn't exist, and a CREATE-TABLE-vs-RLS cross-check surfaced 3 data-bearing tables (cost_health_snapshots, translation_sandwich_comparisons, translation_sandwich_cost_tracker) with no RLS and no REVOKE. PII-free, but the residual concern is default PostgREST anon/authenticated grants, unverifiable from the repo (medium confidence). **Effort:** Small-medium — query prod pg_tables/pg_policies for the inventory (applied state, not migration files) and REVOKE/lock or positively clear the 3 tables.
- **#21 Rollback / incident-response protocol + named owner** — _into_AO_P1_gapmap_. Per-flag/per-deploy rollback recipes are abundant (every CLAUDE.md 'Rollback:' line) but there is no decision-rule doc for WHEN to trigger a deploy rollback, and incident-response ownership is unassigned — the AO plan explicitly names the 2026-07-17 credential-exposure incident as having had no owner. **Effort:** Small doc (trigger criteria + Vercel-rollback/flag-unset steps) + an org owner assignment.
- **#14 Analytics funnel reporting + confirm analytics_events prod table** — _new_go_live_checklist_session_. A first-party event pipeline with real conversion/funnel events + an admin metrics dashboard is already live (the 'analytics absent' premise is wrong). Genuinely missing: computed funnel conversion-rates / retention cohorts / drop-off (dashboard is counts-only). Also: the analytics_events CREATE TABLE isn't in any in-repo migration (only ALTERed) — near-certainly exists in prod but confirm it. **Effort:** Small to confirm the prod table now; medium for funnel/retention reporting later (defer).
- **#7 Token-count capture (R5 completeness)** — _new_go_live_checklist_session_. Premise understated: R5 cost tracking at the loop grain AND the revenue:cost detector (delivered daily to Slack) are already LIVE on the paid agent surfaces. Genuinely uneven: /api/reason writes 0 tokens (captures cost-in-cents by accepted PR7 scope) and the 6 human score routes carry no token/cost tracking. Low priority because cost-level R5 monitoring already works. **Effort:** Small-medium and optional pre-launch — capture real token counts on /api/reason and the human score routes if in scope.
- **#12 Human escalation owner (founder / contractor / vendor)** — _into_AO_P1_gapmap_. Escalation exists only as LLM guidance in the support brain; no route routes a request to a human and no vendor (Intercom-class) is wired. Nothing in code presupposes an answer — 'who handles it' is a founder ownership/vendor decision. **Effort:** Org decision.
- **#15 Email platform selection & setup** — _into_AO_P1_gapmap_. Zero email infrastructure: no vendor, no mail dependency, no send anywhere, no reminder/delivery cron. The weekly-reflection loop is in-app only. Vendor selection is the org decision; delivery integration (subscriber capture + scheduled send + content) is a build tail behind it. **Effort:** Org decision now; build tail post-launch.
- **#22 Database migration strategy (management layer)** — _into_AO_P1_gapmap_. 51+ flat migration files at two locations, no config.toml/CLI project, no tracked applied-state/ordering table. Per-file hygiene is strong (idempotent, reversible, VERIFY sections, 'migration BEFORE flag'); the missing piece is a formalized, tracked apply mechanism. A tooling/ops decision, not a code defect. **Effort:** Org/tooling decision — adopt Supabase CLI migrations or document a consolidated manual strategy.
- **#26 Competitive intelligence feed cadence** — _into_AO_P1_gapmap_. Partly mischaracterized: competitor monitoring exists as one topic inside the weekly environmental scan (last refreshed 2026-07-13, JSON + Supabase + lib), but the scan runs manually/in-session (CLAUDE.md flags it stale) and there is no dedicated per-competitor structured feed. Not a launch blocker. **Effort:** Org — Growth agent decides automation vs manual cadence, and whether a dedicated feed is worth building.
- **#25 Content production workflow** — _into_AO_P1_gapmap_. Channel cadence guidance exists as Growth-brain context (blog 2-3/mo, social 3-5/wk, email weekly) but the operational draft->review->publish process with named owners is undefined. A process/ownership definition, not a code build. **Effort:** Org process artifact, owned by the Growth agent.
- **#18 Practitioner session continuity** — _into_AO_P1_gapmap_. Partly mischaracterized: cross-instance meta.trajectory surfacing (read-and-describe) and cross-session Reflect are already live. The literal ask — a server-side within-session context that FEEDS the reasoning engine — is unbuilt and would conflict with the deliberate stateless-per-instance signed scoring the accreditation/trust model depends on. A design question, not a launch build (medium confidence it's even wanted). **Effort:** Design decision for the founder; no launch work.
- **#27 Community health metrics & support-analytics dashboard** — _into_AO_P1_gapmap_. The support-analytics half presupposes a support/ticketing operation + defined SLAs that don't exist (a vendor/ownership decision) and is premature pre-launch with ~nil traffic — resolution times/SLA compliance can't be measured before there are tickets. The community-engagement half has partial primitives (analytics_events + admin/metrics) but no 30-day dashboard (medium confidence). **Effort:** Org (decide support tooling + SLAs first); the community dashboard is a buildable-later slice.
- **#23 Knowledge base / searchable help centre** — _defer_post_launch_. Internal-agent KB text exists, and several standalone explanatory pages already serve framework + scope content (/methodology, /limitations, /transparency, /terms, /api-docs). The specific deliverable — a searchable help hub with how-tos — is genuinely absent. TIER-3, not a launch blocker. **Effort:** Medium — can seed from existing KNOWLEDGE_BASE_CONTEXT Q&A + methodology/limitations content. Post-launch.
- **#20 Feedback / quality-signal pipeline (thumbs up/down)** — _defer_post_launch_. No user quality-signal capture on reasoning responses exists. TIER-3 and not a launch blocker, but a natural FIRST input to the Agent-Organization Evidence Program, which needs response-quality signals — scope it there, not as a raw launch task. **Effort:** Small — endpoint + Supabase table + client control; post-launch, framed as an Evidence Program input.
- **#17 Distributed cache persistence (Upstash/Supabase-backed)** — _defer_post_launch_. The cache is a per-instance in-memory LRU Map that doesn't survive Vercel cold starts. Correct premise, but a TIER-3 at-scale optimization — with the launch held and low traffic the per-instance LRU is adequate. Partly a vendor/architecture eval. **Effort:** Medium — only justified once traffic makes cross-instance prompt-dedup pay for itself.

## Verify-in-prod (cannot be checked from the repo)

- **#1 R20 safety flags** — Confirm the 5 R20a flags are true in Vercel production (SUBSTRATE_CALLING_R20A_ENABLED, SUBSTRATE_REFLECT_R20A_ENABLED, SUBSTRATE_R20A_AUDIENCE_RENDERING_ENABLED, SUBSTRATE_R20A_GATE_ENABLED, SUBSTRATE_SCORE_CONVERSATION_R20A_ENABLED). The unconditional two-stage floor works flag-off regardless.
- **#2 Encryption key set** — Confirm /api/health returns mentor_encryption: active in production (proves MENTOR_ENCRYPTION_KEY is set — the only piece not repo-verifiable).
- **#3 Free-demo cost surface** — Confirm the /api/evaluate unauthenticated demo (5/min, 500-char, Haiku) is an acceptable pre-launch cost/abuse surface. Nothing to build.
- **#19 Developer onboarding infra deployed** — Code wiring (self-service /api/keys mint, docs surfaces, atomic per-key quota RPC, /api/reason call site) re-verified end-to-end. /api/keys carries no feature flag, so confirm the route + analytics_events-adjacent tables are present in the deployed prod (a live HTTP probe, not a code check).

## Mischaracterizations (premise factually wrong)

- #4 Layer 3: premise 'in progress but not verified live' is factually wrong — the prompt-layer three-layer context is wired live across 20 routes; the 503-gated substrate Layer-3 is a deliberate out-of-scope deferral, not an incomplete blocker.
- #2 Encryption: premise names the wrong env var (ENCRYPTION_KEY) — the live, actively-wired key is MENTOR_ENCRYPTION_KEY. The one src hit for 'ENCRYPTION_KEY' is an inert documentation string.
- #7 Token tracking: premise understates reality — R5 cost tracking at the loop grain AND the revenue:cost (2.0x) detector delivering daily to Slack are already LIVE; only token-COUNT precision is uneven.
- #14 Analytics: premise 'analytics absent' is wrong — a first-party event pipeline with real conversion/funnel events (sign_up->sign_in->baseline->dashboard->score) and an admin metrics dashboard is built and serving; only computed funnel/retention reporting is missing.
- #24 R20b dependency detection: premise 'needs usage data first' is wrong — both the detection lib and the coaching consumer (including the usage-data feed) are built and wired; it runs the instant R20B_INDEPENDENCE_COACHING_ENABLED is set. Dark by a recorded off-perimeter decision.
- #26 Competitive intel: premise 'no monitoring' is partly wrong — competitor monitoring exists as one topic in the weekly environmental scan (last refreshed 2026-07-13); it is just not automated or a dedicated per-competitor feed.
- #18 Session continuity: premise 'reasoning history doesn't inform later responses' is partly wrong — cross-instance meta.trajectory surfacing and cross-session Reflect are live; only a stateful within-session engine input is unbuilt (and by design, to protect stateless signed scoring).

## Meta-findings (the most valuable output — feed P1/P4/P5)

- CONTEXT-PROVISIONING (Agent-Org P1/P4): the launch feedback was substantially stale about the current build. Of the 5 items called TIER1 pre-launch blockers, 3 are already LIVE (R20 safety, R17 encryption, API-key auth), a 4th is mischaracterized/already-wired (Layer-3), and only 1 (error monitoring) is a genuine gap. Multiple PARTIALs also understated live infrastructure (#7 R5 cost alerting, #14 analytics pipeline, #24 R20b feed, #26 competitive scan, #18 trajectory surfacing). The role-agents generated readiness feedback WITHOUT the authoritative Live-state ledger (the CLAUDE.md Live/inert lists) — direct evidence for the agent context-provisioning / situational-awareness gap. Fix: make a mandatory 'load current build-state' step a precondition of any readiness-review workflow.
- INVERTED PRIORITIZATION (situational awareness): the feedback flagged already-built safety/security infrastructure as blockers while burying the one live user-facing defect (#28 — the privacy page directs users to disabled Export/Delete buttons, a GDPR-honesty contradiction) as a mid-tier PARTIAL. An unprovisioned agent over-weighted plausible-sounding categories and under-weighted a concrete, verifiable on-page contradiction.
- GAP TOPOLOGY validates the Agent-Org direction: the genuine remaining work clusters in OBSERVABILITY (error store #5, health probes #6, rate-limit visibility #8, token counts #7) and BUSINESS-OPS OWNERSHIP (support monitoring #11, escalation owner #12, email vendor #15, rollback/incident owner #21, migration tooling #22) — the 'running a company' scaffolding a solo founder hasn't stood up. The core product, safety, and security substrate is genuinely built. The missing layer is org ownership + ops, not engineering.
- VERIFICATION OVER CONSTRUCTION: the dominant remaining action is confirmation, not building. 4 of the 5 'blockers' need only a founder prod check, and most PARTIALs are last-mile wire-ups (2 buttons, one table, one probe, one log line), not new systems. This is a launch that is much closer than the raw 28-item list implies.
- The stale feedback is itself a reusable artifact for Agent-Org process quality (P5): it demonstrates that an unprovisioned role-agent will confidently assert a fully-built, live-in-production system is a missing blocker. That is the strongest available argument for a standing situational-awareness contract in the agent-org — provision current state first, then assess.

## Routing summary

- **Go-Live session (verify + small gate-builds; produces item #16):** [1, 2, 3, 5, 6, 7, 8, 9, 10, 13, 14, 19, 28]
- **Into AO P1 gap-map (org/vendor decisions):** [11, 12, 15, 18, 21, 22, 25, 26, 27]
- **Defer post-launch (§9 pool):** [17, 20, 23, 24] — #20 tagged as the first Evidence-Program (Track B) input; #24 already dark-by-decision.
- **Declined:** none

---
*Full per-agent verification detail: `wf_3e81a945-4b8` journal (session subagents dir). This artifact is the durable record; the workflow transcript is ephemeral.*
