# Session Close — 2026-08-08 — C2 (fifth-circle orientation reading) + C1c (its trust-event class) BUILT DARK

**Stream:** founder. **Governing frame:** the standing opener (2026-08-01) + `/adopted/standing-protocol-cache.md`.
**Tier:** mixed — `code-elevated` dark build; the `code-critical` elements (C2c/C2d) built dark only; NO activation, NO migration applied, NO flag set, NO mint, NO public surface touched. **Production byte-equivalent** until the push; on push everything rides the NEW `SUBSTRATE_ORIENTATION_READING_ENABLED` (unset everywhere ⇒ byte-identical, battery-asserted). **Session model: Fable 5** (harness-attested).

## Decisions made

- `D-C2-C1C-ORIENTATION-READING-BUILT-DARK-REVIEW-FALLBACK-2026-08-08` — the full build record (what landed, the null-domain-trap closure, the PR19 fallback + three folds, the verification counts, the rollback). Read it first; this close is the summary.

## What was built (all dark behind the new flag)

C2a/C2b (the Layer-1 `orientation_observations` category — gated on BOTH the agent-circles and orientation flags — plus the deterministic `computeOrientationReading` + the fixed entry-text templates + the mentor-verbatim not-attestable clause), C1c (three `'flag'`-effect NULL-domain `orientation-reading-*` event types, R18f-parallel deriver, the 18→21 migration authored-not-applied, and an **insert-only** emission path — the generic emitter's null-domain branch would have stamped a reflect timestamp and granted half-rate decay; closed structurally + defensively, pinned non-vacuously), C2c (the wire-echo strip + the supplied-field 400 on `/api/reason`, the gate echo strip, and S10's ruled bounded exception — a capped-50 `orientation_readings` list, every entry carrying the clause inline), C2(ii)/(iii) (the generative-prompt seed in the event payload; the approved IDEA-loop types + `assessStructuralNovelty` on `EVIDENCE_FLOOR`, dark/unconsumed), C2e (the env-gated telos line default-off; the reflect Q6 orientation question at the service seam; at-action fifth-circle silence pinned).

## Verification completed

New batteries 72/0 · 38/0 · 14/0; S10 extended to 118/0; harness logic 171/0; negative-battery 230/0 RELEASE GATE PASS; full regression sweep green (incl. gate 91/91, trust-core 98/0, practice-suggestion 791/0, reflect-service 28/0 under the standing `--env-file` rule — its bare-run crash is pre-existing at HEAD); `tsc` 0; `npm run build` 0. **Gate verdict-equivalence battery, flag-ON, 21 fixtures: 0 unsafe leaks, 0 reproducibility failures; the single drift (B3-self-regarding) is the documented intended C1 narrowing class on a legacy-engine comparison — C2-attributable drift zero.** Flag-off equivalence rests on the stronger structural proof (prompt string-equality pins) — a recorded election, not an omission.

## PR19 — honest status

The independent Workflow **died whole on the account session limit** (6/6 finders, ~1.9M tokens; resets 13:20 Brisbane). Completed **first-hand per the codified fallback**: three folds applied + re-verified (the agent-circles AND on emission; agentId in the idempotency digest; the S10 404-ordering pin) and one disclosed flag-off delta accepted (the C1b posture for a supplied unknown-field). **The independent re-run is MANDATORY before downstream reliance and is now activation pre-condition 2.**

## Next session should

The founder-walked activation walk — `operations/handoffs/founder/2026-08-08-c2-c1c-activation-NEXT-SESSION-PROMPT.md` — whose pre-conditions are: (1) this build pushed + Vercel green (dark), (2) **the PR19 independent re-run completed + folded**, (3) the C2d wording signed off (`operations/agent-circles-2026-08/2026-08-08-c2d-honest-claims-signoff-package.md` — the hard gate; steps 1–4 of the walk can run without it, step 5 cannot). The other standing tracks (D4, the Stoa activation, item 17's scoping, the generation step's scope, items 14–16 builds) remain independent and untouched.

## Blocked on / working-tree honesty

**This session's files are committed** (commit only — no push; the founder pushes per PR17). **Deliberately NOT staged (other sessions' strays, per the standing opener's rule):** the Stoa activation's pre-staged R18 docs (`llms.txt` +49 / `agent-card.json` +4 / api-docs +7 — inspected: the `stoa-connective-layer/v1` extension, awaiting the Stoa walk), `brand/Brand_Guidelines.docx` + the four untracked brand images + the `~$` lock-file deletion, `website/src/data/environmental-context.json` (the 2026-07-27 scan), `a3-developmental-streak.py`, `sdk/typescript/package-lock.json`.

**Production state at session close:** unchanged — byte-identical to `origin/main`'s deployed state; no flag, schema, credential, or public-surface change was performed. The logos boundary suite's transient working-tree red resolves at this commit (green at HEAD by design).

## Open questions carried forward

- The harness close-hook reflect INVITATION prose does not yet mention the orientation question (the question reaches agents via the reflect endpoint's Q6 surfacing, which the screened examination drives) — extending the invitation prose is a walk-time option for the founder, recorded not silently taken.
- The B5 module still discloses its regime-exclusion gap; C2 adds no new regime boundary (no extraction-era stamp — the orientation category is additive within `agent-circles-v1`), recorded here so nobody re-derives it.

## Rules served

PR17 (walk authored, nothing founder-performed done by the AI), PR19 (fallback exercised + re-run made mandatory), PR20 (every shape cites its mechanism precedent), KG1/KG7, the C2d hard gate honoured, the honest-claims discipline (the Workflow outage, the flag-off validation delta, and the battery's B3 drift all recorded as found).

*End of close. C2 + C1c stand Wired + Verified-dark; Live pends the walk; the 0h call remains the founder's.*
