# Session Close — 2026-08-03 — Stoa ST3: the human surface `/stoa` (dark; the twelfth R20a route)

**Stream:** founder.
**Governing frame:** /adopted/standing-protocol-cache.md; the Stoa build plan §2 (all thirty) + §3 ST3 + §4; the binding verbatim record (Q1/Q2a/Q3a–e/Q4/Q5a/Q7/Q8/Q9/Q14).
**Tier:** `code-elevated` as a whole; **`code-critical` for the declaration route's R20a perimeter addition** (0c-ii; the full-form sections below). **Date:** 2026-08-03. **Model:** Fable 5 (`claude-fable-5`); the distress classifier is Haiku via the shared module (cache AC1 row) — zero other LLM calls in the built surface.
**Honesty notes:** (1) every Gate-1/Gate-2 hook attempt 401'd and guard probes 429'd all session (the known transient fail-secure class) — unframed/unguarded throughout, proceeded deliberately; the close-time reflect invitation was engaged genuinely mid-session. (2) The session ran non-interactively: the AC5 recorded decision executed is the one the founder-commissioned ST3 prompt specified verbatim — recorded as **prompt-specified**, re-openable at the ST5 walk before anything activates.

## What happened

The Stoa is now visible to humans, dark: browse (recency served verbatim, tag+search as filters), declare/tend/withdraw with per-entry visibility, the passive shelf (declared-content-only, own view, filter-never-rank), the gentle 180-day "is this still yours?", the canonical self-description + published ethic as exported values, and the honest dark state (browser-verified rendering). The declaration route (`POST`/`PATCH /api/mentor/stoa`) is the **twelfth R20a route-level perimeter member** — the AC5 pattern awaited before any store write, human-audience rendering, mild rides additively; the browse route stays outside (no free text). Everything 503s behind `SUBSTRATE_STOA_ENABLED` (UNSET everywhere; ST5 activates).

**PR19 (four parallel independent reviewers — the disclosed Workflow-equivalent) earned its keep:** the headline MEDIUM was **tags** — a fourth human-free-text field outside the distress composition, with a tags-only PATCH that skipped the classifier entirely while still writing acute-tier language for public display — folded (tags join the subject comma-joined; live acute-tag catch + no-bridge control pinned). Two HIGHs from the adequacy reviewer were both **live-proven defeats**: the route layer's imports were guarded by nothing (a trust-core import passed every battery), and the shelf-purity pin was token-shallow (a `credentialRef`-keyed branch ran green) — folded with a serving-surface forbidden-import scan and a Proxy access-set pin that makes the Q2a ruling *observable* (the matcher may read exactly `{tags, whatISeek, whatIBring}`). Also folded: merged-entry gating (cross-request distress assembly closed), the mild-escalation reuse, the record-anchored copy pin (which caught a one-byte apostrophe divergence from the verbatim record), the exact tag-vocabulary snapshot, the near-empty framing no longer misfiring on filtered lists, and the own-view projection. **This session's own mutation testing found a perimeter-wide defect before the reviewers did:** the shared r20a-invocation-guard's call checks were comment-satisfiable — fixed at the root (comment-stripping; strictly tightening; all 12 routes green; the mutation now trips it). One reviewer claim refuted with citation (the store trims tags before persist). Eleven live mutations, all bit. Full disposition: `D-STOA-ST3-HUMAN-SURFACE-BUILT-DARK-2026-08-03`.

## Decisions Made
- `D-STOA-ST3-HUMAN-SURFACE-BUILT-DARK-2026-08-03` appended (the AC5 recorded decision + the full PR19 disposition).

## Status Changes
| Item | Old | New |
|---|---|---|
| ST3 (human surface) | Scoped | **Verified (build-level; boundary 83/0 · route battery 42/0 · store 65/0 · guard 102/0 · 11 mutations bit · tsc 0 · build ✓ · dark state browser-verified · PR19 folded) — dark, awaiting ST5** |
| R20a perimeter | 11 route-level + 2 substrate-gate | **12 route-level + 2 substrate-gate = 14** (the Stoa declaration route, flag-gated dark) |
| r20a-invocation-guard | raw-source call checks | **comment-stripped call checks (strictly tightening; perimeter-wide)** |

## Verification Method Used (Critical element)
The AC5 pattern + ordering pinned by the new per-route battery (INV-1..16, awaited-regex hardened) and the extended guard registry; flag-off = structural 503 before any work on all five handlers (no separate byte-identity claim needed — the routes are new); fail posture traced (stage-1 floor always runs; stage-2 failures escalate or fail-open-with-alert, never below the floor); audience rendering human-only (RH pins); subject composition seam/cap/tags proven against the live regexes incl. the negative control; the gate-bypass, tags-drop, and comment-satisfiability mutations all demonstrated red then restored green.

## Risk Classification Record (Critical element)
R20a perimeter addition = Critical per 0d-ii/AC5. What changes on push: nothing live (dark). What could break: the guard-suite fold touches the shared safety test — verified strictly tightening with all 12 routes green. Rollback: `git revert` the session commit (registry + guard fold ride the same revert). AC7 NOT engaged (no flag, no schema, no mint, no deploy). Founder approval: the perimeter addition was specified in the founder-authored session prompt; the ST5 walk is the founder's live confirmation point before activation.

## Blocked On
**Production state at session close (PR18, as of 2026-08-03):** production is **byte-equivalent on push except inert additions** — the three new surfaces register but 503/dark-state (`SUBSTRATE_STOA_ENABLED` UNSET everywhere), and the shared guard-suite fold is test-only. `stoa_entries` remains applied-inert on TEST + PROD (ST2). No flag, schema, credential, or deploy action was taken this session.

**Files remaining uncommitted (this session's — stage ONLY these; the tree carries other sessions' strays, untouched):** see the Founder Verification block.

## Open Questions
- The row-level reactivation guard (recency-cycling residual) — touches ST2's "a re-declaration IS a new declaration" adjudication; flag to the mentor if contested (ST4/ST5).
- ST5 checklist additions: Supabase **anonymous sign-ins OFF** check; the q-filter pagination bound (disclosed in-route).
- Renewal-never-reorders: uncontested this session — carried settled.

## Next Session Should
**ST4** — the agent surface + R18 staging (`code-elevated`, dark; the §5-item-ii credential-capability election at open). **Prompt: `operations/handoffs/founder/2026-08-03-stoa-ST4-agent-surface-NEXT-SESSION-PROMPT.md`.** Pre-condition: this commit pushed, Vercel green with the confirmed hash.

## Founder Verification
```bash
cd "/Users/clintonaitkenhead/Claude-work/PROJECTS/sagereasoning" && git add website/src/lib/stoa/stoa-copy.ts website/src/lib/stoa/stoa-tags.ts website/src/lib/stoa/stoa-shelf.ts website/src/lib/stoa/stoa-r20a.ts website/src/lib/stoa/stoa-presentation.ts website/src/lib/stoa/stoa-store.ts website/src/lib/stoa/__tests__/stoa-boundary.test.ts website/src/app/api/stoa/entries/route.ts website/src/app/api/mentor/stoa/route.ts website/src/app/api/mentor/stoa/__tests__/r20a-invocation.test.ts website/src/app/stoa/page.tsx website/src/app/stoa/layout.tsx website/src/lib/__tests__/r20a-invocation-guard.test.ts operations/decision-log.md operations/connective-layer-2026-08/2026-08-02-stoa-build-plan.md operations/handoffs/founder/2026-08-03-stoa-ST3-human-surface-CLOSE.md operations/handoffs/founder/2026-08-03-stoa-ST4-agent-surface-NEXT-SESSION-PROMPT.md
```
```bash
cd "/Users/clintonaitkenhead/Claude-work/PROJECTS/sagereasoning" && git commit -m "Stoa ST3: the human surface /stoa dark - twelfth R20a route, PR19 folds incl. tags-in-subject + merged gating + the guard-suite comment-strip fix"
```
Then push via GitHub Desktop; wait for Vercel green and confirm the deployed hash. (Everything deploys dark; the batteries in the decision-log verification block re-run clean at any time.)

## Cross-references
- `operations/handoffs/founder/2026-08-03-stoa-ST2-entry-model-CLOSE.md` (predecessor)
- `operations/connective-layer-2026-08/2026-08-02-stoa-build-plan.md` (§3 ST3 status updated)
- `operations/connective-layer-2026-08/2026-08-02-mentor-consultation-connective-layer-verbatim.md` (binding)
- `D-STOA-ST3-HUMAN-SURFACE-BUILT-DARK-2026-08-03`
- `operations/handoffs/founder/2026-08-03-stoa-ST4-agent-surface-NEXT-SESSION-PROMPT.md`

*End of session close. The colonnade's human face exists dark — browse, declare, tend, withdraw under the fourteen rulings, with the distress perimeter on the one route that takes a person's words, and batteries that have all been proven able to go red.*
