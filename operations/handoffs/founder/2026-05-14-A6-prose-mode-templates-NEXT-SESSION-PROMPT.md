# Next-Session Prompt — A6: Layer 3 `prose_mode` Per-Mode Templates

**Stream:** founder.
**Tier:** `code-standard` (changes to Layer 3 service prose generation; no auth/encryption/perimeter; new internal templates only). Session-as-a-whole **Standard** under 0d-ii. Lean template form per cache.
**Governing frame:** `/adopted/standing-protocol-cache.md` (full governance via the cache). Deliverable-of-the-day: per-mode prose templates filled in for `clinical` / `terse` / `standard` / `educational` modes in `/website/src/lib/substrate/layer3-service.ts` (closes the A5.5 parameter-plumbing-only scope).
**Predecessor session close:** `/operations/handoffs/founder/2026-05-14-agent-card-a2a-reshape-close.md` (agent-card.json A2A v1 reshape Verified; closed the deferred gap from D-AGENT-CARD-CURRENCY-CHECK).
**Predecessor decision-log entries:** `D-AGENT-CARD-A2A-V1-RESHAPE-2026-05-14` (closed the A2A v1 alignment gap); `D-ANTHROPIC-NATIVE-POSTURE-2026-05-14` (umbrella; CLAUDE.md + skills install + amended PR15); `D-A9-J6-COST-MONITORING-WIRED-2026-05-14`; `D-A5-LAYER3-SCAFFOLDED-VERIFIED-2026-05-12` (A5 close that A5.5 plumbed parameters through — A6 fills in templates).
**Risk classification:** **Standard** under 0d-ii (additive prose-template content; no existing user-facing behaviour changes for already-set defaults; new templates only invoked when a non-default `prose_mode` is requested). **Critical Change Protocol NOT engaged.** AC7 + PR6 not engaged (not safety-critical; R20a injection logic unchanged). AC5 perimeter unchanged. PR1 applies on the single Layer 3 service module.

## Why this session matters

Layer 3 is the substrate's prose generation layer. A5 scaffolded the service; A5.5 plumbed the `prose_mode` parameter through the call chain (`clinical / terse / standard / educational`). The templates the parameter selects between are not yet filled in — `standard` is the default and is what every call currently produces regardless of `prose_mode` value.

A6 closes that gap by authoring the four per-mode templates. The work is bounded, well-scoped, and self-contained inside `layer3-service.ts`. It produces visible, valuable substrate behaviour: consumers can now request a clinical tone for therapeutic contexts, terse for high-throughput API calls, educational for learning surfaces, or the existing standard for general use.

This lands BEFORE A10 (Critical; per-agent credentials + token-format ADR) only if the founder elects it. A10 / A11a / A8 remain available either way.

**F3 fold-in (per `/operations/agentic-commerce-findings-downstream-order.md`):** A6 references A5; note in session-open + inline in the decision-log entry that A5's `Layer3Response` shape is structurally a substrate-consultation-mandate producer (R3 + R19c + R19d + R20a + R18a + R18e injections + AC9/AC10/AC11 projections = AP2-style mandate-output shape). The prose generated by A6's templates is one of the mandate's projection fields.

## Pre-conditions

1. **Agent-card A2A v1 reshape commit pushed and Vercel green.** Founder confirms `git log --oneline -3 origin/main` shows the reshape commit on top.
2. **Live agent-card.json serves A2A v1 canonical shape.** Founder ran post-deploy checks 9+10 from the reshape close; both PASS.
3. **A1–A7 + A9 + J6 still Verified.** No regression since the reshape session.
4. **Founder commits to a 120-180 min bounded session.** Standard tier focused work.
5. **Production state unchanged.** `SUBSTRATE_LAYER3_ENABLED` UNSET; `SUBSTRATE_R20A_GATE_ENABLED` UNSET; `/api/reason` byte-identical; `/api/substrate/layer3` returns 503; `/api/public-key` serves Ed25519 steady-state. **A6 is a code change to the Layer 3 service module; the env-flag remains UNSET so the new templates do not affect production until a separate activation session.**
6. **Cowork project-instructions panel paste-synced** against `/adopted/project-instructions-snapshot.md` (amended PR15 with OPERATIONAL DISCIPLINE). Unchanged from the agent-card reshape session.

## Part A — Open under the protocol

Read in order:

1. **`/adopted/standing-protocol-cache.md`** (~3 min) — confirms tier; model selection (N/A — A6 uses existing Sonnet path per AC1; no new model selection); status vocabulary; signals; risk classification; lean Standard template form.
2. **`/adopted/build-sessions-protocol-cache.md`** (~3 min) — build-arc-specific cache for substrate-build sessions.
3. **`/operations/handoffs/founder/2026-05-14-agent-card-a2a-reshape-close.md`** (~5 min) — predecessor close; confirms steady state preserved.
4. **`/operations/decision-log.md` last 3 entries** (~3 min) — `D-AGENT-CARD-A2A-V1-RESHAPE-2026-05-14` (this session's predecessor); `D-AGENT-CARD-CURRENCY-CHECK-2026-05-14` (superseded); `D-ANTHROPIC-NATIVE-POSTURE-2026-05-14` (umbrella).
5. **`/adopted/project-instructions-snapshot.md` §PR15 (amended)** (~3 min) — confirms the OPERATIONAL DISCIPLINE. **PR15 consultation:** `.claude/skills/anthropic/skill-creator/SKILL.md` is available if any prose-template-as-skill design discussion arises. The four per-mode templates are SageReasoning-authored (per staging plan §A6 "SageReasoning-authored, not community-extensible") so they live inside `layer3-service.ts`, not as separate Anthropic skills. The skill-creator consultation is for design-pattern reference only.
6. **`/website/src/lib/substrate/layer3-service.ts`** (~10 min) — the deliverable-of-the-day's primary file. Read in full to locate the current template structure, the `ProseMode` enum, the `DEFAULT_PROSE_MODE` constant, the call sites at lines 564 + 609 + 621, and any existing template-selection branching. **F3 fold-in:** note the `Layer3Response` shape is structurally a substrate-consultation-mandate producer (R3 + R19c + R19d + R20a + R18a + R18e injections + AC9/AC10/AC11 projections = AP2-style mandate output).
7. **`/adopted/substrate-plugin-staging-plan.md` §A6 row + Stage 1 dependencies** (~3 min) — confirms A6 depends on A5; "Standard; 1 hr; Scoped" (slightly underestimated — bring 2-3hr expectation).
8. **`/manifest.md` §R3 + §R19c + §R19d + §R20a + §AC1 + §AC2** (~5 min, targeted) — R3 (control filter injection) + R19c (mirror principle) + R19d (limitations transparency) + R20a (distress-aware redirection text) all appear in every Layer 3 response and the templates must preserve these. AC1 Sonnet selection per cache row (Layer 3 translation = Sonnet/DeepModel — confirmed; A6 does not change model selection). AC2 latency budget (must be preserved across all modes).
9. **`/operations/agentic-commerce-findings-downstream-order.md` §F3** (~2 min) — the F3 fold-in spec.

**Confirm at session open** (state explicitly, briefly):
- Tier: `code-standard`; session-as-a-whole **Standard**
- Hold-point status: P0 0h active
- Model selection: **Sonnet** (DeepModel) per cache AC1 row "Layer 3 translation (alt-3)"; unchanged from A5
- Status vocabulary: implementation `Scoped → Designed → Scaffolded → Wired → Verified → Live`; decision `Adopted / Under review / Superseded`
- Signals + risk classification: Standard; **Critical Change Protocol NOT engaged**; AC7 + PR6 not engaged; AC5 perimeter unchanged
- PR1 single-endpoint proof: applies on the single Layer 3 service module
- PR2 build-to-wire-verification immediate: applies (the new templates must be invoked in `generateLayer3Response` to count as wired)
- PR4 (model selection): Sonnet confirmed at session-opening checkpoint
- PR10 PEV loop: applies in lean Standard form
- PR11 authoritative-current-sources: scan `/inbox/` for any material since 2026-05-14
- PR12 negative-finding discipline: when checking what tone-shaping language already exists in `layer3-service.ts`, run multiple greps before concluding "the templates don't exist"
- PR13 consider-implications: 5-question assessment after the implementation step
- PR15: no bespoke election — the templates are SageReasoning-authored Stoic-philosophy-grounded prose; no Anthropic-canonical primitive replaces this. Justification recorded inline.
- PR16 (positioning + dogfood): per-mode templates strengthen Character-Kernel-as-tone-controllable positioning (R18a); dogfood relevance HIGH — the founder may consult `/api/reason` to evaluate the templates against real prompts in a follow-up session.

## Part B — Procedure

### Step 1 — Survey the current template structure (Design; ~15-20 min)

Read `layer3-service.ts` in full. Identify:
- The `ProseMode` enum + `PROSE_MODES` array + `DEFAULT_PROSE_MODE` constant
- The current single-template implementation (likely a single function or inline string template constructing the Layer 3 response prose)
- Where R3 + R19c + R19d + R20a injections occur in the prose
- The call sites that pass `prose_mode` through (lines 564 + 609 + 621 per pre-session grep)
- The existing tests in `/website/src/lib/substrate/__tests__/layer3-service.test.ts` (the 28-test file that PASSES at the reshape close)

Output (in-chat, ~10 lines): a concise summary of what's there + what needs to change. State: "Diagnostic-certain — root cause identified" if the single-template-with-no-branching pattern is confirmed; "Diagnostic-uncertain — pattern level" if the structure is more complex than expected.

### Step 2 — Design per-mode template differentiation (Design + founder approval gate; ~15-20 min)

Before any code edit, produce a short design spec (~30-40 lines, in-chat) covering:

**Per-mode tone differentiation** — exactly how the four modes differ:
- **`clinical`** — therapeutic-context-appropriate tone. Removes second-person address ("you should"). Uses observational framing ("the assessment notes ..."). Preserves all four mandatory injections (R3/R19c/R19d/R20a) verbatim.
- **`terse`** — high-throughput API tone. Removes connective tissue, examples, encouragement. Single-sentence-per-section structure. Preserves the four mandatory injections at the end as a compact block.
- **`standard`** — the current default. Conversational but bounded. The existing template. **Keep byte-identical to today's output** when `prose_mode: 'standard'` (this is the regression-guard for existing consumers).
- **`educational`** — explanatory-context tone. Adds "this is what this means" framing around assessment outputs. Slightly longer than standard. Preserves all four mandatory injections + adds Stoic-source pointer line when relevant.

**What must NOT vary across modes** — the four mandatory injection texts (R3 / R19c / R19d / R20a) are constants; PR6 not engaged but treat the R20a injection text as immutable per its source-of-truth file. The `Layer3Response` JSON shape is invariant across modes — only the prose field varies.

**Founder approval gate:** surface the design spec + four mode-differentiation summaries in chat. The founder elects whether the design matches their intent before any code is written. Particular questions to surface:
- Confirm the four-mode tone targets above match the founder's intent for clinical/terse/standard/educational
- Confirm "standard" stays byte-identical to today's output (regression-guard for existing consumers)
- Confirm the four mandatory injections (R3/R19c/R19d/R20a) are preserved verbatim across all four modes

### Step 3 — Implement the per-mode templates (Build; ~45-60 min)

Apply the approved design. Inside `layer3-service.ts`:

1. Introduce a `proseTemplate(mode: ProseMode, fields: ProseTemplateInput): string` function (or a `Record<ProseMode, TemplateFn>` map) that branches on the `proseMode` value
2. Each branch produces the per-mode prose, with the four mandatory injections inserted verbatim from their existing constant sources (R3 / R19c / R19d / R20a — DO NOT inline; reference the existing constants)
3. Replace the current single-template call site with a call into `proseTemplate(proseMode, fields)`
4. PR1 single-endpoint proof applies — this IS the single Layer 3 service module; no other endpoint is reshaped
5. PR2 build-to-wire-verification immediate — the new function must be invoked at the existing call sites for the change to count as wired (verification step 3 confirms this)

If the design naturally produces a small new file (e.g., `/website/src/lib/substrate/prose-templates.ts`), that is acceptable; the implementation may be split across two files (the dispatcher in `layer3-service.ts` + the templates themselves in `prose-templates.ts`).

### Step 4 — Verify

Verification expectations:

```bash
cd "/Users/clintonaitkenhead/Claude-work/PROJECTS/sagereasoning/website"

# 1. TypeScript compile (must be clean — new function signature + new dispatch)
npx tsc --noEmit -p tsconfig.json
# Expected: clean compile (EXITCODE=0)

# 2. A5 regression check — the 28-test suite for layer3-service
npx tsx src/lib/substrate/__tests__/layer3-service.test.ts
# Expected: 28 pass / 0 fail. The 'standard' mode must produce byte-identical output
# to today's; if any test fails, the implementation has drifted from the design's
# regression-guard. Investigate before proceeding.

# 3. A7 regression check (R20a gate; defensive — A6 should not touch R20a logic)
npx tsx src/lib/substrate/__tests__/r20a-gate.test.ts
# Expected: 33/33 pass

# 4. Per-mode behavioural test (NEW; written this session)
# A small in-session test that exercises proseTemplate() with each of the four modes,
# confirming each produces non-empty distinct output and each contains all four
# mandatory injection substrings (R3 + R19c + R19d + R20a).
# Test location candidate: src/lib/substrate/__tests__/prose-mode-templates.test.ts
npx tsx src/lib/substrate/__tests__/prose-mode-templates.test.ts
# Expected: 4 modes × 4 mandatory injections = 16 substring checks + 4 distinctness
# checks = 20 PASS / 0 FAIL (target shape; the actual test count depends on the
# design's chosen structure)

# 5. Substrate steady state (production unchanged)
cd ..
curl -sS https://www.sagereasoning.com/api/public-key | python3 -c "
import json, sys
d = json.load(sys.stdin)
ok = d.get('previous') is None and d.get('rotation_overlap_until') is None and d.get('algorithm') == 'Ed25519'
print('PASS' if ok else 'FAIL')
"
# Expected: PASS

curl -sS -o /dev/null -w "%{http_code}\n" -X POST https://www.sagereasoning.com/api/substrate/layer3
# Expected: 503 (SUBSTRATE_LAYER3_ENABLED still UNSET; A6 does not activate the endpoint)

# 6. (Post-deploy, optional) After Vercel redeploys, the deployed code includes the
# new templates but the env-flag remains UNSET, so /api/substrate/layer3 continues
# to return 503. No behaviour change visible to live consumers until a separate
# activation session.
```

If any of checks 1-4 fails, do NOT push. Engage rollback (revert the template additions; re-run check 1-2). Investigate. If check 5 fails, the substrate has an unrelated regression — investigate before push.

### Step 5 — Append decision-log entry (lean form)

Append `D-A6-PROSE-MODE-TEMPLATES-WIRED-VERIFIED-YYYY-MM-DD` to `/operations/decision-log.md`. Lean form per cache §"Lean decision-log entry":
- Decision: A6 templates wired + verified; A5.5 parameter-plumbing-only scope closed
- Reasoning: closes the prose_mode templates gap; per-mode tone differentiation enables clinical/terse/educational consumption paths; standard mode kept byte-identical for existing-consumer regression-guard
- **F3 fold-in (inline):** note the `Layer3Response` shape is structurally a substrate-consultation-mandate producer (R3 + R19c + R19d + R20a + R18a + R18e injections + AC9/AC10/AC11 projections = AP2-style mandate-output shape); A6's templates produce one of the mandate's projection fields
- Files touched: `layer3-service.ts` + (if split) `prose-templates.ts` + `prose-mode-templates.test.ts` (NEW)
- Risk classification: Standard (additive; existing standard-mode output byte-identical)
- Rollback path: `git revert` of the A6 commit; standard-mode output is the regression-guard so consumers see no change after revert
- Verification step: the 6 checks from Step 4 above
- Rules served: 0a (status — A6 Scoped → Verified), 0d-ii (Standard), 0f (this entry), PR1 (single-endpoint proof — this IS the Layer 3 module), PR2 (build-to-wire-verification immediate — proseTemplate invoked at existing call sites + new test exercises it), PR4 (model selection — Sonnet confirmed unchanged), PR10 (PEV loop in lean Standard form), PR11 (authoritative-current-sources — `/inbox/` scanned; no material changes), PR15 (no bespoke election — SageReasoning-authored per staging plan §A6; skill-creator consulted as design-pattern reference only), PR16 (positioning + dogfood — strengthens Character-Kernel-as-tone-controllable positioning; substrate consultation offer noted as optional follow-up)

### Step 6 — Session close (lean form)

Path: `/operations/handoffs/founder/YYYY-MM-DD-A6-prose-mode-templates-close.md`. Lean template per cache §"Lean session close":
- **Decisions Made** — `D-A6-PROSE-MODE-TEMPLATES-WIRED-VERIFIED-YYYY-MM-DD` appended; one-sentence summary
- **Status Changes** — table covering: A6 (Scoped → Verified); `layer3-service.ts` (wired with proseTemplate dispatcher); production state (unchanged — env-flag remains UNSET)
- **Next Session Should** — A10 (Critical; ~3-4hr) per F4 fold-in OR A11a (Standard; ~1hr) OR A8 (Standard; ~1-2hr) per founder election; A6 activation timing remains an open question (when the env-flag is set, the new templates become live for consumers)
- **Blocked On** — uncommitted files + Vercel green confirmation
- **Open Questions** — A6 production activation timing (env-flag SET decision); per-mode tone calibration against real-consumer feedback (no current consumer; revisit when first paid agent consumer onboards)
- **Founder Verification** — commit command + the 6 verification commands + Vercel green confirmation
- **Cross-references** — predecessor close (`2026-05-14-agent-card-a2a-reshape-close.md`); this session's decision-log entry; touched files

## Part C — Anticipated session shape

| Phase | Estimate |
|---|---|
| Cache + predecessor close + layer3-service.ts read (Part A) | 25-30 min |
| Step 1 — Survey current template structure | 15-20 min |
| Step 2 — Design spec + founder approval gate | 15-20 min |
| Step 3 — Implement per-mode templates | 45-60 min |
| Step 4 — Verify (6 checks; ~5 min each — TS compile most of the time) | 15-20 min |
| Step 5 — Decision-log entry | 10-15 min |
| Step 6 — Session close | 10-15 min |
| **Total** | **~135-180 min (~2.25-3 hr)** |

If founder elects a tighter scope (~90-120 min), drop Step 3's "split into prose-templates.ts" optionality (keep everything in `layer3-service.ts`) and drop the new `prose-mode-templates.test.ts` separate file (fold per-mode assertions into the existing 28-test suite as additional cases).

## Rollback path

`git revert <a6-commit>` and push via GitHub Desktop. The new `proseTemplate` dispatcher disappears; the prior single-template implementation returns. **Standard-mode output is byte-identical between pre-A6 and post-A6**, so consumers see no behaviour change after revert. Vercel redeploys on revert-push as usual. **No data loss; no user impact** beyond removing the clinical / terse / educational mode capabilities (which were never live before this session anyway since the env-flag remains UNSET — A6 lands at code level but does not activate the endpoint).

## Forecast

Successful session produces:

- **`/website/src/lib/substrate/layer3-service.ts`** — wired with `proseTemplate(mode, fields)` dispatcher invoking per-mode templates
- (optional) **`/website/src/lib/substrate/prose-templates.ts`** — separate file with the four per-mode template functions, if the design splits the dispatcher from the templates
- **`/website/src/lib/substrate/__tests__/prose-mode-templates.test.ts`** (NEW) — exercises each mode + confirms the four mandatory injections + confirms distinct output per mode
- **Standard mode byte-identical** to pre-A6 output — the regression-guard for existing consumers
- 1 decision-log entry appended; lean Standard form
- F3 fold-in noted inline in the decision-log entry + at session-open

**Stage 1 status after this session:** existing critical chain A1→A2→A3→A4→A5→A7 + cost monitoring (A9 + J6) + Anthropic-native posture + agent-card.json A2A v1 alignment + **A6 prose_mode templates** complete. Stage 1 remaining: A8 (V3 endpoint mapping) / A10-A19 (Stage 1 expansion; A10 token-format ADR is the highest-leverage Critical item next).

**Next session after this:** A10 per-agent credentials kickoff (Critical; ~3-4hr; F4 fold-in for AC10 / AP2 mandate alignment) OR A11a endpoint-auth audits (Standard; ~1hr) OR A8 V3 endpoint mapping (Standard; ~1-2hr) — per founder election. Subsequent sessions consult `.claude/skills/anthropic/` + agentic-commerce-findings tracker at session-open per amended PR15.

End of prompt.
