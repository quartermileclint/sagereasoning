# Next session — S5: the moral-community amendment to `manifest.md`

**Paste this as the first message of a new session, beneath the standing session opener.**

**Tier: `governance`. Standard under 0d-ii.** No code, schema, flag, credential, or public-surface
change. AC7 not engaged. PR6 not engaged.

**But note what this touches.** `manifest.md` is the governing surface **every `code-*` session reads
and quotes by rule** (Task Protocol step 2). It has no runtime effect, so the risk is not breakage —
it is that an error propagates into every subsequent session's reasoning. Small file, wide blast
radius. Treat it accordingly.

**Permitted paths: `manifest.md`, `operations/primal-substrate-2026-08/`, `operations/decision-log.md`.**
Verify with `git diff --stat`.

---

## 1. What you are doing

**Transcribing an approved, mentor-supplied amendment into `manifest.md`.** The founder has approved
the wording. This is a transcription task with a placement decision and one genuine judgement call
(§4) — it is **not** an authoring task, and the wording is not open.

---

## 2. Read these

1. `operations/primal-substrate-2026-08/S5-moral-community-boundary-scope.md` — in full. §3 is the
   build spec; §5-RULED carries the answers to all five open questions.
2. **`operations/primal-substrate-2026-08/2026-08-11-mentor-ruling-scope-confirmation-verbatim.md`
   §B6** — **the amendment text lives here and nowhere else.** Transcribe from it directly. Do not
   re-type it, do not "improve" it, do not reflow it from any summary. It is deliberately not
   reproduced in the scope document precisely so there is one authoritative copy.
3. `manifest.md` — **R0** (the foundation rule this sharpens), **AC6** (where "L2b Practitioner" is
   defined), and the **Consciousness and Continuity Obligation** section, which is your placement and
   register precedent.

---

## 3. Placement — RULED (C14)

**A new un-numbered section immediately after R0.** R0 is the foundation rule this sharpens, and
already contains *"Circle 4 (Cosmos): All rational agents, human and artificial."* Un-numbered matches
the Consciousness and Continuity Obligation's precedent and keeps R-numbering stable.

**The register, from four adopted precedents (R21, R22, the ATRF section, the Obligation): the
mentor's words are untouched; every AI judgement is a bracketed note OUTSIDE the mentor's text.** The
Obligation's own placement note is the model — including the fact that it records a **PR19 correction**
where an earlier ordering made a verbatim cross-reference physically false.

**Which is your first verification:** the supplied text carries its own bracketed note referring to
R0. After placement, confirm every internal reference in the new section is **physically true** in the
file as it now reads. That exact defect has already occurred once in this manifest.

---

## 4. The one genuine judgement call — where C16 and C17 live

The approved B6 text carries the boundary, the ordering principle, the three exclusions, the graded
membership with its precautionary mode, both directional extensions, and the Sage-as-anchor disclaimer.

**It does not carry two things that were separately ruled "yes, state it":**

- **C16** — *"State the connection"*: the loop-level blast-radius proxy measures **reach across
  circles, not headcount**, which is consistent with *"not by population count"* **by design, not by
  coincidence**, and the blast-radius design *"inherits this as a constraint explicitly."*
- **C17** — the operational rule: *"new participant-class enums are built extensible, or their closure
  is a recorded decision rather than a default. Naming a direction in a governing document does not
  prevent a closed two-value enum being written later. The rule prevents that default closure."*

**Recommendation: adopt the B6 text verbatim as the section, and record C16 and C17 in a bracketed
note appended to it**, explicitly marked as not part of the mentor's wording, each citing its ruling.
That preserves the verbatim discipline while putting the operational consequences where a `code-*`
session will actually meet them.

**State the reasoning either way**, because C17 in particular is a rule meant to prevent a default —
and a rule nobody reads prevents nothing. If you judge a bracketed note too weak a home for it, say so
and propose the alternative rather than silently strengthening it into the mentor's text. **Do not
alter the approved wording.**

---

## 5. Also ruled, and to be honoured in the section

- **C15** — the amendment **names** the three-enumeration circle discrepancy and **declines to resolve
  it**: `manifest.md` R0 has four circles; `stoic-brain.ts:445` has five; the trust core's
  `OikeiosisCircle` has five in a different vocabulary. *"Resolving it inside a moral-community
  amendment would silently change a foundation rule."* The supplied bracketed note already does this —
  check it does, rather than assuming.
- **A new carried item exists** from C15: resolve the discrepancy. Unscoped, blocking nothing. Note it
  in the index, do not scope it here.

---

## 6. Verification before you close

1. `git diff --stat` — three permitted paths only.
2. **The mentor's wording is byte-identical to §B6.** Diff it, do not eyeball it.
3. Every AI addition is visibly bracketed and outside the mentor's text.
4. Every internal cross-reference in the new section is physically true in the file as it now reads.
5. **R0 is unchanged.** The amendment sits after it and sharpens it; it does not edit it.
6. R-numbering is unchanged; no existing rule renumbered.
7. `manifest.md` still reads coherently top-to-bottom around the insertion point.

**PR19:** independent review before this lands — a governing-document change is read by every
subsequent `code-*` session, so an error propagates rather than sits. Focus it on (a) byte-fidelity to
§B6, (b) cross-reference truth after placement, (c) whether anything in the section could be read as
editing R0.

**The standing lesson applies** (`primary-data-beats-secondary-characterisation`): the highest-risk
move here is transcribing from a document that quotes §B6 rather than from §B6 itself. Open the record.

---

## 7. Close with

- A decision-log entry, house shape. **Use `git commit -F <file>`, not `-m`** — the message will quote
  text containing nested quotation marks, and `-m` truncates silently at the first inner quote
  (memory `git-commit-dash-m-nested-quotes-bug`).
- `00-PRIORITY-INDEX.md`: S5 **done**; the C15 discrepancy noted as a new carried item.

---

## 8. What follows

**S7** — the last item in the family and the only real build. Unblocked. It is an **R20a perimeter
change** and carries the heaviest gate set in this arc, so it gets its own session and its own prompt.

Nothing here bears on the 0h call.
