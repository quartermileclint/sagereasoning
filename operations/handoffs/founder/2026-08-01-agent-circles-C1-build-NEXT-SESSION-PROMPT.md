# Next-Session Prompt — Agent Circles: the First-Circle Correction build (practice-on C0 + C1 + C3)

ultracode

**Paste this whole file as the FIRST message of a new session.** Target model: **Claude Opus 5** — confirm the session genuinely runs on it and **state model + effort at open** (the P2-erratum discipline: silent model substitution must never go unlogged; the leg-B S3 confound was caught only because the model field was mandatory). Default effort `high`; use lower effort for mechanical battery sweeps and `xhigh` only for the hardest verify/judge stages — effort is the cost lever, not delegation volume.

**Stream:** founder (substrate / trust layer / agent circles). **Tier: `code-critical` arc** (a shared Layer-1 extraction change that reaches the LIVE `/api/guardrail` gate; a trust-event schema CHECK widening; new trust-event derivations). AC7 + PR6 + PR17 engage at every founder-walked step; **PR19 independent adversarial review is mandatory** before the build is treated as verified. **Nothing in this prompt pre-approves any deploy, schema application, flag, or mint.**

---

## How to work (calibration for this model + harness)

- **Scope:** deliver what this prompt asks, at the scope intended. Make routine judgment calls yourself and record them as numbered build decisions (BD-*) inside the verdicts' bounds; check in (AskUserQuestion) only where different readings would produce materially different work. If something here seems mistaken against the binding records, say so in a sentence and follow the records — **the verbatim mentor records win over this prompt.**
- **Narration:** before the first tool call, one sentence on what you're doing; brief updates only at load-bearing findings or direction changes; lead every summary with the outcome.
- **Delegation:** use the Workflow tool for the PR19 review (fresh contexts, given the artifact only — never this session's own conclusions) and for genuinely independent parallel sweeps (e.g., the battery matrix). Do not spawn agents for work finishable in a handful of tool calls, and do not add verification passes beyond the named gates below — the batteries, the mutation cycle, and PR19 are the verification surface; nothing more is wanted.
- **Written deliverables:** match length to need; no filler sections; the close and decision-log entry follow the house template exactly.
- **The harness:** you are running inside the practice instrument you are modifying. Frames/suggestions are ADVISORY (channel law); expect transient 401s/28s timeouts, fail-open-honest; the logos boundary suite will run **red-while-uncommitted** on this work (you are editing the measured set — that is the designed transient state; NEVER weaken, scope, or bypass the guard); verify any claimed hook event server-side, never from narration.

## Read first (in this order; ~20 min)

1. `operations/handoffs/founder/STANDING-SESSION-OPENER-grounded-foundations.md` — Parts A–E in full (protocol, current state, harness context).
2. **The two binding verbatim records, IN FULL** — `operations/agent-circles-2026-08/2026-08-01-mentor-consultation-agent-circles-practice-on-verbatim.md` (nine answers + the placement follow-up) and `…-logos-on-verbatim.md` (L1–L7). **These govern; every summary loses to them.**
3. **The plan this session executes, IN FULL:** `operations/agent-circles-2026-08/2026-08-01-agent-circles-practice-on-build-plan.md` (§1 the settled verdicts; §2 grounding; §3 phases C0/C1/C3; §4 boundaries; §5 risk; §6 verification; §7 elections). Plus the logos-on plan's **§3 W2** only (the schema co-ride election this session decides).
4. Code (targeted): `website/src/lib/translation-sandwich/layer1-extractor.ts` (the LAYER1_SYSTEM_PROMPT + validators — the surface C1a/C1b/C3 change), `layer2-mechanisms.ts` (`assessOikeiosis` → `computeProximity` region; the additive-fields idiom), `corroboration-check.ts` (the verbatim-evidence-span idiom C1b reuses), `website/src/lib/substrate/trust-core/kathekon-engagement.ts` (Arm 1 — byte-unchanged this session, pins added), `emission-hooks.ts` + `trust-transition.ts` (`EVENT_EFFECT`; the derivation discipline), the S9b CHECK-widening migration (the precedent C1c's migration follows), `agent-assessment-history-store.ts` (`assignRegimeEra` — C0.3's version marker), `session-decline-signal.ts` (C1f's rider target), and the §4/route-2a activation records for how an unconditional extractor-prompt change is landed and batteried (CLAUDE.md's 2026-06-25 refreshes).

## The task

Execute practice-on **C0 (groundwork) + C1 (the first-circle correction) + C3 (the circle-4 obligation class)** per the plan — C1+C3 share ONE extraction change (one regime boundary; the plan's recorded election — revisit only if your review finds the combined change unverifiable, and say so rather than silently splitting). In outline (the plan's §3 governs the detail):

1. **C0.2** — `practitioner_type` (server-composed, additive; `'agent'` on credential-bearing consults; never caller-supplied). **C0.3** — the `agent-circles-v1` extraction-era label wired through the existing regime machinery, minted at deploy.
2. **C1a** — the Layer-1 prompt re-grounds first-circle engagement: fires ONLY when the agent's own reasoning integrity is directly implicated (task-pressure assent · disclosure-of-limits · confidence-representation); `self_preservation` stops attaching as background. Unconditional prompt (shared with the gate) — the route-2a precedent governs.
3. **C1b** — additive Layer-1 fields for the three-element task-pressure standard (tension-identified · instruction-as-operative-reason · independent-divergence, each with verbatim evidence spans) + the examined-refusal demonstration; Layer-2 requires the CONJUNCTION and routes the domain by causal locus (unexamined → phronesis; recognised-pressure assent → sophrosyne). Pin both directions (each element alone must NOT read as the class).
4. **C1c** — the failure/demonstration trust-event classes: derivations R18f-parallel; the `agent_trust_events` CHECK-widening migration AUTHORED (founder-walked to apply — never applied by you); the PA-6 no-oversight-increase audit re-run in the same change. **Decide the W2 co-ride election** (logos-on's enforcement-class + regime-marker **schema surface** — its exact shape is a build-time election, logos-on plan §6.3 — riding the same migration) and record it. **D4 (`derive-trust-events.ts`) stays untouched.**
5. **C1d** — kathekon Arm 1 byte-unchanged; pins that first-circle-only engagement under the NEW semantics still routes self-regarding/prudential.
6. **C1e** — the calibration probe (pre/post first-circle attachment rate on a fixed consult set; the AE-2 probe precedent) — authored and TEST-runnable; its live run gates C2 and is founder-walked.
7. **C1f** — B5's session-decline module gains the regime-boundary exclusion its own header names as missing.
8. **C3** — the circle-4 extraction teaching (the mentor's test, verbatim from the record's Q8: *"an action that will be received by another agent as input to that agent's examination, where the acting agent knows the input is incomplete or distorted, is a candidate circle-4 violation"* — the phantasia-corruption test; the summariser case as the anchor fixture). Observe the plan's circle-number↔identifier mapping rule (mentor-4 → `cosmopolis`; mentor-3 → the `local_community`/`political_community` band; mentor-5 → NO extractable identifier, ever). Fixtures BOTH directions — catches (summariser omission · context-poisoning · induced false assent) AND protective cases that must NEVER floor (honest disclosure of incompleteness · a summary labelled partial · a disclosed redaction).
9. **Gates (the verification surface — these, the mutation cycle, and PR19; nothing beyond):** every named battery green; `tsc` 0 + `npm run build` 0; **the guardrail verdict-equivalence battery BOTH directions at BOTH thresholds (strict AND the live `deliberate` default)** — the gate binds in production, and C3 makes a new justice surface visible to it, so over-strictness fixtures are as load-bearing as catches; regime-band tests across `agent-circles-v1`; the PR19 Workflow review with a dedicated dimension on C3's protective-context direction (the corroboration-tripwire lesson) and one on C1b's conjunction non-vacuity.

## Boundaries (do NOT)

- No C2 work (the orientation reading is gated on C1e's live calibration — a later session). No S11 / enforce / logos-on W1–W3 building — **sole exception:** if the C1c co-ride election lands yes, the authored migration may carry W2's *schema surface only* (never W2's derivations, entries, or machinery). No orientation reading anywhere.
- No deploy, no schema APPLY, no flag set, no mint, no push — author + verify; the founder walks every live step (commit-and-push BEFORE any flag, always).
- `derive-trust-events.ts` untouched (register D4). `stoic-brain.ts` untouched. The logos guard untouched.
- No re-processing or re-interpretation of pre-adoption records (Q9a — forward-only; pinned).
- MEASURE only; weights BLOCKED; the disclosed ENFORCE consequence (the gate can newly block on circle-4 `violated`) is named to the founder at the walk, never silently shipped.

## Exit

Built + batteries green + PR19 folded ⇒ write the close, the decision-log entry, the founder-walk checklist (migration → push/deploy with the pre-deploy gate batteries → calibration probe → smokes → R18 wording signed off BEFORE any public surface changes; TEST before prod throughout — **plan §C4 governs the full checklist**, this parenthetical is not it), and commit for the founder's push. **Stopping at a phase boundary (e.g., C0+C1a/b only, C1c/C3 next session) is a legitimate outcome — say so plainly rather than rushing the gate surface.** Success = the first-circle correction faithful to the verbatim records, verified to the named gates, with every founder-walked step staged and nothing activated.

End of prompt.
