# Session Close — 2026-08-09 — `watching` (per-cycle record table): scoped, adversarially reviewed, offered for relay

**Stream:** founder.
**Governing frame:** `operations/handoffs/founder/2026-08-09-watching-per-cycle-record-table-scoping-NEXT-SESSION-PROMPT.md` + `/adopted/standing-protocol-cache.md`.
**Tier:** `governance` / design (explore-scope) — Standard risk throughout. AC7 not engaged. Production byte-equivalent (no code, schema, flag, or credential change; the only production writes were the harness's own organic consult traffic, including three Gate-2 at-action 28000ms timeouts — the known fail-open-honest class, organic, disclosed; the third fired on this close file’s own write, after the initial count of two was drafted).
**Date:** 2026-08-09.

## Decisions Made

- `D-WATCHING-PER-CYCLE-RECORD-TABLE-SCOPED-2026-08-09` appended — the `watching` per-cycle record table scope document offered for mentor relay.

## What this session did

1. **Opened under the protocol** — all three pre-conditions held: the `fresh` records commit at HEAD, pushed, Vercel green (founder-confirmed); the session's own opening prompt FRAMED by the hook; the founder confirmed no new mentor guidance supersedes the Q11 sequence or any ruling.
2. **Consolidated what Q5/Q6/Q7 settled** (scope §1, cited to the verbatim record) and **proposed** the realisation (scope §2, every item marked PROPOSAL): a **two-table shape** (`idea_loop_cycles` + `idea_loop_candidates`) resolving the cycle-row-vs-Q7-per-candidate tension; a **runner-submitted `POST /api/practice/watching`** write — the only shape consistent with SageReasoning's per-cycle statelessness (the runner is the sole holder of a cycle's full state), with the ingest-script alternative named and set aside; a **`FOUNDER_USER_ID`-gated `GET /api/founder/watching`** + dashboard page on the re-confirmed house pattern (grep first-hand: zero push/websocket/SSE anywhere in `website/src`); a **runner-composed-and-disclosed honesty posture** with per-candidate `guardrail_session_id` traceability against the server's own signed assessments; `loopId`/`sessionId` kept separate in the row with the trust-event composition explicitly left to the generation-step document; full **R17 retention/data-rights wiring** (the `collaboration_records` standing-surface precedent); dark behind **`SUBSTRATE_WATCHING_ENABLED`**. Three open questions for the mentor (QW-A/QW-B/QW-C below).
3. **Re-verified the carried question first-hand, per the ruling's own instruction:** the `dependency_unavailable`/null-cycle fallback-counter distinction is **genuinely NOT settled** — the brief's §1.3 fallback rule counts *null* cycles specifically; line 51 says only "honestly distinct"; Q6 ruled only the seventh `cycleOutcome` value. Raised as **QW-A** with three options (counts / resets / passes transparently; AI recommends transparent) and a perversity note for the mentor: counting dependency outages toward a trigger whose action is friction-detection-**only** mode would steer the loop toward the exact mechanism whose dependency failed. Drafting also surfaced an **adjacent under-specification the corpus never states** — when the *cycle-level* outcome reads `dependency_unavailable` at all, given Phase 1 has the cycle continue on the other heuristics — raised as QW-A(ii) with a proposed reading (the friction-only-mode case), not assumed.
4. **PR20 mechanism facts verified first-hand before drafting:** `cycleOutcome` carries exactly six values at HEAD (`idea-loop-types.ts:135-141` — the ruled seventh stays a named code follow-up); the no-push dashboard pattern; the two non-interchangeable admin gates (`FOUNDER_USER_ID` vs `ADMIN_EMAILS`); the 6e §A write-class set `{accreditation_write, calling, reflect}` hard-coded in the CHECK + `WRITE_CLASS_CAPABILITIES`; the X-Loop-Cost-Cents/integer-cents cost mechanism; the two retention precedents correctly characterised (standing vs temporary-instrument).
5. **Adversarial review (PR19), paused both sides at the founder's direction** so model settings could be switched (review ran on Sonnet 5; folds applied after the founder switched back to Fable 5): 3 dimensions → per-finding adversarial verification, 12 agents, 0 errors, ~3.0M tokens — **9 raised, 8 confirmed, 1 refuted; the PR20 mechanism-fact dimension returned 0 findings** (every code/corpus fact claim traced exactly). **The substantive confirmed pattern was in the author's own citation practice:** the draft's §1 quotes labelled "verbatim" were stitched, reordered, or spliced paraphrases (F1–F4) — corrected to the exact contiguous ruling spans, each fold disclosed inline rather than silently repaired. Also folded: F6 (a Q3 over-attribution — "founder-owned" is not in Q3, which rules only that the credential is *dedicated*; ownership routed to the runner scoping session); boundary-F1 (the cycle-level timeout-token respelling of a ruled value, previously an unlabelled proposal, now raised explicitly as **QW-C**); boundary-F2 (this document's question labels renamed **`QW-*`** to prevent collision with the ruled `fresh` scope's own Q-A/Q-B). F7 confirmed the QW-A re-derivation faithful (a clean check). The refuted finding misread the brief §4 actor-category header as a per-item joint-execution claim. Disclosed: the automated safety-classifier was unavailable for 2 verify agents; every finding and verdict was read first-hand before folding.

## Status Changes

| Item | Old | New |
|---|---|---|
| Per-cycle record table (`watching`) — SCOPING | next in the ruled sequence, not started | **SCOPED — offered for mentor relay (PR19 review run, all 8 confirmed findings folded)** |
| `watching` — BUILDING | blocked | still blocked — the first build gate sits after the generation-step scope document |
| `dependency_unavailable`/fallback-counter distinction | re-verified NOT settled (predecessor) | **raised as QW-A for the mentor** (with the newly-surfaced QW-A(ii) cycle-level semantics) |
| Generation-step scope document | queued after `watching` | **still queued — does not open until the mentor rules on this scope** (Q11) |

## Next Session Should

Wait for the mentor's ruling on this scope (relay via the founder). On ruling: fold the rulings into the scope document (the `fresh` session's RULED-annotation pattern), update the architecture-map mirror, and author the generation-step scope document's next-session prompt (the sequence's next item). If the ruling arrives before this session's records are pushed, the fold may ride the same commit (the `fresh` precedent). The two registered defects, C1c-original, D4, the Stoa activation, W1–W3, B6, and the permission-layer items remain independently electable and untouched.

## Blocked On

**Files new/modified this session (uncommitted, for the founder's push — one records commit):**
- `operations/agent-circles-2026-08/2026-08-09-watching-per-cycle-record-table-scope.md` (new; revised through the PR19 fold)
- `operations/decision-log.md` (one entry appended)
- `operations/handoffs/founder/2026-08-09-watching-per-cycle-record-table-scoping-CLOSE.md` (this file)

Pre-existing untracked strays from prior sessions remain unchanged and deliberately unstaged.

**Production state at session close (2026-08-09):** no code, schema, flag, or credential change. AC7 not engaged. C2/C1c remain LIVE (MEASURE); the two registered defects remain open, untouched per the prompt's scope. The Q1 hard constraint stands in every document: the loop proposes; it never executes.

## Open Questions

For the mentor, in the scope document §3: **QW-A** (the fallback-counter distinction + cycle-level `dependency_unavailable` semantics), **QW-B** (the write path's capability class), **QW-C** (the cycle-level timeout token's spelling). None resolved by default.

## Founder Verification

```bash
cd "/Users/clintonaitkenhead/Claude-work/PROJECTS/sagereasoning" && git add operations/agent-circles-2026-08/2026-08-09-watching-per-cycle-record-table-scope.md operations/decision-log.md operations/handoffs/founder/2026-08-09-watching-per-cycle-record-table-scoping-CLOSE.md && git status --short
```

Review the staged list (three paths, all documents — no website/ or code file), then commit and push via GitHub Desktop. No Vercel behaviour change is expected (no code touched). The scope is ready for relay to the mentor.

## Rules served

PR15 (settled corpus cited, not re-derived); PR19 (full independent Workflow, both stages, run under the founder's model election with both pauses honoured; a real citation-fidelity defect class in the author's own draft caught, folded, disclosed — the fallback was not needed); PR20 (every mechanism fact read first-hand from the code before drafting; the review's dedicated fact dimension independently returned zero findings; the fallback-counter carry-forward re-verified against the brief text, not trusted from the pointer); PR17/PR18 (this close); the Q1 hard constraint carried; the scoping-not-building boundary intact end-to-end (no code file created or edited; the code-adjacent acts were *reading* `idea-loop-types.ts`, `practice-credential.ts`, the 6e migration, `loop-cost-tracker.ts`, the founder-hub route/page, and the two retention-precedent migrations to verify facts).

## Cross-references

- `operations/handoffs/founder/2026-08-09-fresh-novelty-endpoint-scoping-CLOSE.md` (predecessor)
- `operations/handoffs/founder/2026-08-09-watching-per-cycle-record-table-scoping-NEXT-SESSION-PROMPT.md` (governing prompt)
- `D-WATCHING-PER-CYCLE-RECORD-TABLE-SCOPED-2026-08-09`
- `operations/agent-circles-2026-08/2026-08-09-watching-per-cycle-record-table-scope.md` (the deliverable)
- `operations/agent-circles-2026-08/2026-08-09-mentor-consultation-fresh-endpoint-scope-rulings-verbatim.md` (the charter carry-forward — verbatim wins)
- `operations/agent-circles-2026-08/2026-08-08-autonomous-loop-design-brief.md` (Q5/Q6/Q7 + §1.3/§1.4)

*End of close. The `watching` scope is drafted, adversarially reviewed with all eight confirmed findings folded (the review's headline catch was the author's own verbatim-labelling discipline, now corrected span-for-span), and offered for relay; the carried fallback-counter question reaches the mentor as a genuine open question with a newly-surfaced adjacent semantic; the build boundary is intact — the first build gate sits two ruled steps away.*
