# Next-Session Prompt — Remaining Principles: morning-preparation tool (#8)

**For the founder. Paste as the first message of a fresh session.** This is the **next window-safe generative build** after #9 view-from-above (Live 2026-07-14). It builds **one net-new human-practitioner tool** — the **morning-preparation tool (#8)**, the *morning pole* of the daily practice that completes the cycle alongside the evening Sage Reflect. Like its predecessors (#7-human, #10-human, #9+#13) it never touches `/api/reason` or the substrate, so it is safe to build and deploy whether or not the 7-day observation window is still running.

**Open under:** `operations/handoffs/founder/STANDING-SESSION-OPENER-grounded-foundations.md` (Parts A–F).
**Stream:** founder. **Tier:** `code-elevated` + **`schema`** (a net-new page + `/api/mentor/*` route + a new user-scoped table → a founder-walked migration, PR17/AC7). Not `code-critical` unless the R20a decision below is taken (it should not be, this session).
**Governing plan:** `operations/trust-layer-2026-07/2026-07-13-remaining-stoic-principles-build-plan.md` — §3 (row #8), §4 (Bucket A, item 5 + the per-item guardrails), §5 (deploy posture), §8 (the sequence). **Binding deploy verdict:** mentor **D6** (`…decision-points-verdict-verbatim.md`) — human-surface tools ship during the window as standalone, boundary-verified-**before** PRs.
**Governing context:** if the **7-day false-hold observation window** is still running (`GATE1_FALSE_HOLD_CAPTURE=true`, `SUBSTRATE_TRUST_CORE_ENABLED` MEASURE), the byte-identity deploy gate below is mandatory. If it has ended, keep the byte-identity check as hygiene. **S11 (ENFORCE) remains DEFERRED, readiness-gated; weights BLOCKED.**

## Why this session exists

The mentor's remaining-principles survey named the **morning examination** (#8 — the Stoic daily orientation of the *hegemonikon* before the day's impressions arrive, distinct from the evening Senecan review the **live Sage Reflect** already encodes) as a net-new human-surface tool that **completes the daily practice cycle**. Marcus opens the *Meditations* with it: what kind of people will I meet today, what virtues will I need, what impressions should I expect and how should I receive them. The instrument currently has only the *evening* pole (Sage Reflect); this is the *morning* pole — the tool that makes the practitioner surface a full daily companion rather than a retrospective review.

**First task at open — ground the design in the mentor's own words.** The plan's one-line description is a starting hypothesis, NOT the design. Before building, read the survey's principle-8 section in `inbox/Mentor answer to remaining principles question.rtf` (search `### 8` — headed "The evening examination — the Senecan review"; the morning tool is in its *"What is not yet encoded"* / *"Encodable"* paragraphs) and confirm the mentor's design: it is **SHORTER than the full reflect sequence**; **three questions** — (1) *what roles are active today and what kathekonta (appropriate actions) do they generate*; (2) *what impressions am I likely to encounter, and which are most likely to produce hasty assent*; (3) *what virtue response do I want to have prepared* — producing a **daily orientation record**. The mentor's framing *"the morning declares the intention, the evening assesses whether it held"* is a **CONCEPTUAL daily-cycle pairing, NOT a code coupling this session** (see constraint 1). Design from the mentor text, not from this paragraph.

## Scope — one net-new standalone tool (no folded component, no agent half)

A net-new practitioner page (e.g. `website/src/app/<name>/page.tsx` + `<name>/layout.tsx`) + a net-new `website/src/app/api/mentor/<name>/route.ts` + a new user-scoped table, modelled on the **freshest net-new templates — `/view-from-above` and `/hupexairesis`** (route + page + layout + migration + boundary test), with the *"produces a disposition/orientation record"* shape closest to **`/premeditatio`'s "Prepare a disposition"** mode. Likely shape (confirm against the mentor text): a short daily form carrying the three orientation questions → a stored daily record. A plausible schema — `roles_active` (today's active roles + the kathekonta they generate), `expected_impressions` (likely impressions + which risk hasty assent), `prepared_virtue_response` (the virtue response you want prepared) — but the **exact fields are the design phase's to settle from the mentor's three questions**.

Unlike #9+#13, #8 ships **no folded component and no agent half** — it is wholly human-surface and standalone.

**A quality/insight gate is OPTIONAL here.** Unlike hupexairesis (separation) and view-from-above (calibration), morning-prep is *declarative* — there may be no clear failure mode to classify. Decide at open whether it warrants a gate (e.g. a light classification of whether `prepared_virtue_response` names a *concrete disposition* rather than a vague intention — the `/premeditatio` "prepared disposition" precedent) or ships **gate-free** (the `/oikeiosis` lean precedent). Any gate follows the family pattern exactly: **LLM classification-only** (no LLM-authored commentary; deterministic pre-authored messages), `MODEL_FAST`, cached, **fail-open**.

**Route/table naming is a scoping decision** — the plan notes no `/morning` route exists. Candidates: `/morning` (clean, recommended), `/morning-prep`, `/morning-preparation`. Decide at open.

## Hard constraints (window-safe — from plan §4/§5 + D6)

1. **No instrument touch.** The new page/route/table must not edit or import `/api/reason`, the substrate libs (`layer1-extractor`, `layer2-mechanisms`/`computeProximity`, `kathekon-engagement`, `trust-core/*`), the Gate-1 hooks, or the reflect/calling routes/`sage-reflect`. New code lives only under `website/src/app/<name>`, `website/src/app/api/mentor/<name>`, a new `website/supabase-<name>-migration.sql`, and the small additive data-rights + welcome-link edits (below). Reuse `@/lib/security`, `@/lib/model-config`, `@/lib/sage-reason-engine` (`getClient` — verified NOT to drag in the substrate engine), `@/lib/supabase`, `@/lib/auth-fetch` — the safe shared libs the family already uses.
   - **#8-SPECIFIC TRAP — do NOT import or couple to `sage-reflect` (the evening engine).** `sage-reflect` is a **forbidden module** (it feeds the measured reflect trust events on `sagereasoning:s9-loop@v1`; it is on the boundary blocklist). The mentor's *"the evening examination can reference the morning record"* is a **conceptual daily-cycle relationship + a possible FUTURE separate integration — NOT this session.** The morning-prep tool stands alone; any "revisit this tonight in Sage Reflect" cue is **UI text, not a code coupling**. A code link to the reflect engine would breach the boundary AND perturb the observation window.
2. **Clean file boundary — battery-verified BEFORE shipping, not after** (D6; the S10-ENV-1 lesson). Copy `website/src/app/api/mentor/view-from-above/__tests__/human-practitioner-boundary.test.ts` for the new tool (self-contained; guard **route + page + layout**). It asserts none of the new files import `@/lib/substrate`, `trust-core`, `kathekon-engagement`, `layer1-extractor`, `layer2-mechanisms`, `translation-sandwich`, `sage-reflect`, `stoic-brain`, the Gate-1 hooks, or `assessKathekon`. Run it (`npx tsx …`) and confirm green before the deploy.
3. **Deploy gate + ORDER (founder-walked, PR17/AC7).** **Migration BEFORE code** — the new route reads/writes the new table, so the table must exist on Production before the route serves traffic (the `build-dark-migrate-later-breaks-writes` class). Before deploy, verify `/api/reason` + the frozen capture set are byte-unchanged (`git status --short | grep -iE "api/reason|translation-sandwich|/substrate/|trust-core|kathekon-engagement|false-hold|harness/gate1"` → NONE) and `npm run build` ✓ with the new routes registered.
4. **Data-rights for the new PII table** (R17c/R17i; the S1/S9b + view-from-above precedent). Wire the new table into the three data-rights surfaces — `website/src/lib/user-data-gathering.ts` (access), `website/src/app/api/user/export/route.ts` (export), `website/src/app/api/user/delete/route.ts` (delete + it rides `...tablesToDelete`) — as one additive, missing-table-benign line each. **Note: the `/api/user/delete` edit is 0d-ii Critical** (data deletion) — the one Critical-classified edit; walk it as such.
5. **R20a perimeter — match precedent, do NOT expand.** The `/api/mentor/*` practitioner tools sit OUTSIDE the distress perimeter (premeditatio/oikeiosis/passion-log/hupexairesis/view-from-above all do), and importing the substrate R20a gate would itself breach constraint 1. Match that (no distress check; keep `SupportFooter`). **Note this tool is LESS grief-facing than view-from-above** — it is forward-looking daily orientation, so the standing "should the family join the perimeter?" question is *less sharp* here, not more. Still **surface — do not decide** — the standing perimeter question; any change is its own Critical/AC5 session, never a same-session add.
6. The **agent-instrument principles are OUT of scope** — #8 has no agent half; it is wholly human-surface. Do not touch the enforce predicate, the reflect engine, or any flag.

## Procedure

1. **Reads** — the standing opener; plan §3(#8)/§4/§5/§8 + §4 per-item guardrails; mentor D6; the survey's principle-8 section (`inbox/Mentor answer to remaining principles question.rtf`, `### 8`); the freshest templates (`view-from-above` + `hupexairesis` route+page+layout+migration+boundary test; `premeditatio` for the "prepared disposition" gate + daily-record contrast; `oikeiosis` as the leanest gate-free `/api/mentor` route). Confirm the frozen-capture-set hash is still pinned if the window is running.
2. **Design** — ground #8's shape in the mentor text (the three questions; shorter than reflect; daily orientation record; the evening-reference is conceptual only); decide the route/table name; scope the fields + whether a (fail-open, classification-only) gate is warranted. Surface the design briefly before building.
3. **Build** — migration (new user-scoped table: uuid PK, `user_id` FK→auth.users ON DELETE CASCADE, per-verb RLS `auth.uid()=user_id` + service-role FOR ALL, `idx_<t>_user_created`, idempotent + reversible) → route (`requireAuth` + service-role client + `.eq('user_id', userId)` on every query; `RATE_LIMITS.scoring`; `TEXT_LIMITS.medium`; optional fail-open classification-only gate; consider adding the revise/edit-in-place `PATCH` from the #7/#9/#10 pattern up front) → page (+ layout with `SupportFooter`) → welcome `MORE_TO_EXPLORE` link → data-rights (constraint 4) → the boundary test.
4. **Verify** — the boundary test (green), `tsc --noEmit` (0), `npm run build` (✓, new routes registered), the byte-identity git-guard (NONE). Then an **adversarial review** (a Workflow: dimensions → find → verify each; if it dies on token expiry/limit, complete first-hand per the §4 precedent). Fold any confirmed findings. *(Reference: the view-from-above review `wf_e0b7d327-b29` — 6 dimensions, 0 confirmed defects — as the shape.)*
5. **Founder-walked deploy** — migration TEST→prod (+ §VERIFY: columns, RLS true, the policies, any CHECK) → push the code (**stage the build files explicitly; do NOT `git add .`** — keep unrelated tree changes like `environmental-context.json` out of the build commit) → live smoke. **Records + close** — a decision-log entry + a session close (lean form) + name the next window-safe successor + refresh the `CLAUDE.md` "Live in production" human-practitioner-tools family bullet to add the new tool.

## What this session does NOT do

It does not touch the instrument, the enforce predicate, or the reflect engine (no `sage-reflect` coupling). It builds no folded component and no agent half (there is none for #8). It does not flip any enforce flag — **S11 stays DEFERRED**. It does not build the other window-safe tools; it does not do the D2 justice-arm narrowing (that is `code-elevated`, report-side, and precedes the return-with-record session).

## Rollback

`git revert` the tool's PR (standalone, additive human surface; no shared runtime imports). The new table is `DROP TABLE`-able (founder-walked). `/api/reason` and the frozen capture set are untouched, so the observation record is unaffected.

## Forecast

Success = the morning-preparation tool live and verified measurement-neutral (byte-identity + clean file boundary proven *before* shipping, with **no `sage-reflect` coupling**), the **daily practice cycle complete** (morning + evening poles), the window still clean. **Queued behind it (window-safe, founder's tempo):** #6/#15 oikeiosis circle-extension + cosmopolitan check (enhance the live `/oikeiosis`); #14 sage-compass (net-new); #12 logos teaching module (net-new static page, read-only `stoic-brain.ts` import only). **Then, before the return-with-record session:** the D2 justice-arm narrowing. **The agent halves (#7/#10) + the kathekon pair (#4/#11) wait for the flip.** *Hold the window clean. Build as capacity allows. The assent will be examined then.*

End of prompt.
