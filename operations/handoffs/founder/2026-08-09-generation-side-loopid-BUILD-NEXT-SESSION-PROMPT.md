# Next-Session Prompt — Build the generation-side `loop_id` field on `/api/reason` (third and last of the founder-approved build order)

**Stream:** founder.
**Tier:** `code-elevated` — an additive, flag-gated change to an EXISTING LIVE route (`/api/reason`). No schema migration is needed (the field rides the orientation event's existing free-form JSONB `payload` column — confirmed first-hand at scoping, see Part A read 5). Elevated because it touches a production request path already serving traffic, per the standing risk table ("changes to existing user-facing functionality" = Elevated) — **not Critical**, because nothing here is auth/session/encryption/R20a/data-deletion, and the flag itself starts unset (the eventual flag-flip activation, later, is its own founder-walked `code-critical` step, exactly the B5/M1/AE-1 precedent). Confirm this classification at open against the cache rather than trusting this header.

## THE FOUNDER-APPROVED BUILD ORDER (read this first — every session in this arc should)

**Approved 2026-08-09** (founder election, following the AI's recommendation put to the founder — see the honesty note below, carried unchanged across all three build sessions):

1. **`fresh`** — the novelty-check endpoint. **DONE 2026-08-09.** Built dark, batteries green (61/0 + 20/0), PR19 GO with zero findings. `D-FRESH-ENDPOINT-BUILT-DARK-REVIEW-CLEAN-2026-08-09` + `operations/handoffs/founder/2026-08-09-fresh-endpoint-build-CLOSE.md`. Activation not yet taken.
2. **`watching`** — the per-cycle record table + dashboard. **DONE 2026-08-09.** Routes built dark (batteries 70/0 + 20/0 + 23/0, PR19 GO_WITH_FIX, two nits folded across a first-hand pass and a fully-completed independent re-run). Because this build touched schema + a new write-class capability, its migration and the `watching_write` capability CHECK-widening were **walked live founder-side to both TEST and PRODUCTION** the same session (both tables and the widened `api_keys` CHECKs are Live; `SUBSTRATE_WATCHING_ENABLED` itself stays unset — routes still dark). No credential was minted — `watching_write` provisioning stays with the runner scoping session. `D-WATCHING-BUILT-AND-CAPABILITY-LIVE-2026-08-09` + `operations/handoffs/founder/2026-08-09-watching-build-CLOSE.md`. Route activation not yet taken.
3. **The generation-side runner code** — in THIS repo, only the additive `loop_id` field on the live `/api/reason` route (QG-C, ruled). The heuristic templates, cycle logic, per-heuristic prompts, and configuration defaults are all **runner-owned and external** to this repo by the architecture ruling (§1 item 1 of the generation-step scope) — nothing about them is built here, ever. **DONE 2026-08-09.** Built dark, battery green (55/0), PR19 GO_WITH_FIX with one low finding folded (a "never read" wording imprecision — a destructuring assignment still reads the property; only inspection/validation is flag-gated). Two load-bearing claims (the deriver's stamp; the route's byte-identity gate) proven by live mutation, not asserted. `D-LOOP-ID-FIELD-BUILT-DARK-REVIEW-FOLDED-2026-08-09` + `operations/handoffs/founder/2026-08-09-loop-id-field-build-CLOSE.md`. **This closes the three-item build-gate sequence.** Activation not yet taken.

**Honesty note, carried forward unchanged across all three sessions:** this order is a **founder election**, not itself a separate mentor ruling — the AI recommended it and the founder approved it in the conversation that produced the original `fresh` prompt. The generation-step scope's own ruling states explicitly that *"the first build gate is its own election — which of `fresh`/`watching`/the generation-side runner code builds first is not pre-decided"* (§4). No mentor confirmation of this specific ORDER has arrived as of this prompt's authoring — **confirm at this session's open whether that has changed.**

**Update this section (or leave a pointer) at the close of this session.** After this build, the three-item build-gate order is exhausted — the natural next arc item is the **runner scoping session** (credential mint, `watching_write` provisioning, the dedicated K1 identity, the `ORIENTATION_DELIVERY_TIMEOUT_MS` revisit trigger, the `frictionAssessment` PM-tool mapping decision — all four are confirmed carry-forwards assigned there, per the generation-step ruling's §2.9), which this session does NOT touch or pre-empt.

---

**Governing frame:** `/adopted/standing-protocol-cache.md` (full governance via the cache; deliverable-of-the-day named below).
**Predecessor session close:** `operations/handoffs/founder/2026-08-09-watching-build-CLOSE.md`.
**Predecessor decision-log entries:** `D-WATCHING-BUILT-AND-CAPABILITY-LIVE-2026-08-09` (the build this session follows); `D-GENERATION-STEP-SCOPE-RULED-2026-08-09` (the ruled scope this session builds to, in the one narrow slice this repo owns); `D-FRESH-ENDPOINT-BUILT-DARK-REVIEW-CLEAN-2026-08-09` (the sibling build whose precedent this session should mirror in shape).

## Why this session matters

This closes the founder-approved three-item build-gate sequence. It is the smallest of the three in surface area — one additive optional field on one existing route — but it is the first of the three that touches a **live, already-serving** endpoint rather than standing up a brand-new dark route. The discipline that matters here is not novelty (there's a well-worn precedent: B5's `session_marker` field, built 2026-07-30, is *the exact same shape* — an optional declared request field, validated flag-on, 400 on malformed input, silently ignored flag-off, stamped into a downstream event payload). The discipline is **fidelity to that precedent** and **proof, not assertion, of flag-off byte-identity** on a route this many other sessions have already touched and re-verified byte-identical dozens of times.

## Pre-conditions

1. Confirm at open: no new mentor guidance has superseded anything in the ruled corpus (the brief, `fresh`, `watching`, the generation-step scope, or the build order above) — ask the founder before writing code.
2. Confirm the session's hook framed. If it does not (the known 28s-timeout transient class), proceed unframed and disclose it at close, per the `fresh`/`watching` sessions' precedent — do not block on it.
3. **Confirm `/api/reason`'s current live state** before editing: read the route file in full (or at minimum the sections named in Part A read 5 below) and re-verify first-hand that every existing flag-gated feature this session's edit sits alongside (B5's `session_marker`, the trajectory-delta overlay, the practice-suggestion block, the orientation-reading emission) is exactly as described here — CLAUDE.md's own "Live in production" list is the fastest cross-check, but the code is authoritative (PR20).
4. Decide the new flag's name at open, before writing code. `SUBSTRATE_LOOP_ID_FIELD_ENABLED` is the natural analogue to the existing flag-naming convention (`SUBSTRATE_SESSION_DECLINE_SIGNAL_ENABLED`, `SUBSTRATE_TRAJECTORY_DELTA_ENABLED`, etc.) — confirm or pick something clearer, but pick it explicitly and use it consistently; do not default silently.

## Part A — Open under the protocol

Read in order:
1. `/adopted/standing-protocol-cache.md` (~3 min — confirms tier, model selection, risk class, signals).
2. `operations/handoffs/founder/2026-08-09-watching-build-CLOSE.md` (~3 min — the immediate predecessor).
3. **The ruled generation-step scope document IN FULL** — `operations/agent-circles-2026-08/2026-08-09-generation-step-scope.md`. Read the whole thing even though most of it (the heuristic templates, the phantasia mechanism, the configuration defaults, the friction threshold) is **runner-owned and out of this repo's scope** — the ONE slice this session builds is §2.7 + §2.11 dimension 6 + the QG-C ruling in §3, and reading it in context (especially §1 items 1, 9, 10 on why `loopId`/`sessionId` are deliberately independent, and §2.8's six-step cycle composition, which names exactly where in a runner's cycle this field gets used) is what keeps the build from drifting into scope the runner owns.
4. The verbatim ruling record — `operations/agent-circles-2026-08/2026-08-09-mentor-consultation-generation-step-scope-rulings-verbatim.md` — verbatim wins over the scope document's own annotations if either is ambiguous. The QG-C ruling is the operative paragraph.
5. The code, read first-hand (PR20) — **this is the load-bearing precedent-fidelity step**:
   - `website/src/app/api/reason/route.ts` — specifically: the B5 `session_marker` validation block (`grep -n "session_marker"` — the flag check, the 400-on-malformed shape with `isBillable: false`, the additive spread into the M6 trajectory write around line 1830); the orientation-reading emission call (`emitOrientationReadingTrustEvent`, imported from `@/lib/substrate/trust-core/emission-hooks`, called from within the route — find the exact call site and its argument object first-hand, do not assume this prompt's excerpt is current); the existing flag-check imports near the top of the file (`isOrientationReadingEnabled`, `isAgentCirclesEnabled`, `isTrajectoryDeltaEnabled`, `isSessionDeclineSignalEnabled` or its actual name) as the pattern to match for the new flag.
   - `website/src/lib/substrate/trust-core/emission-hooks.ts` — `emitOrientationReadingTrustEvent`'s full signature (`OrientationReadingEmissionInput`) and body: confirm it is a fire-and-forget-safe async fn that never throws to the caller (a `try/catch` wrapping the whole body), gated on `isTrustCoreEnabled() && isOrientationReadingEnabled() && isAgentCirclesEnabled()`, and that `input.layer1Source !== 'server'` short-circuits (a supplied extraction never mints a reading — irrelevant to this build but confirm it doesn't interact badly with a `loop_id` addition).
   - `website/src/lib/substrate/trust-core/types.ts` — the `OrientationReadingEmissionInput`-adjacent event-payload type (search for `generativePrompt?:` and `orientationDeliveryClass?:` — the two most recently added optional fields on this same payload, both prospective-only per their own ruled fold) and confirm the payload interface carries an index signature (`[key: string]: unknown`) or an explicit field list — this determines whether `loopId?: string` needs a typed field added or can ride the index signature (the QG-C ruling and the B5 precedent both argue for an explicit typed field, matching `sessionMarker`'s own treatment, not a silent untyped pass-through).
   - `website/src/lib/substrate/trust-core/derive-trust-events.ts` — `deriveOrientationReadingEvent` (or whatever the actual function name is at this session's open — confirm) — where the event's persisted `payload` object is actually assembled, to confirm exactly where a `loopId` field would need to be threaded from the emission call's input into the persisted row.
   - `website/src/lib/substrate/trust-core/trust-core-store.ts` — confirm `payload: event.payload` (or the current equivalent) still passes the object directly into a JSONB column with no per-field extraction — the reason no schema migration is needed for this build. **If this has changed since this prompt was authored, that changes the tier — stop and re-scope with the founder before proceeding.**
   - `website/src/app/api/reason/__tests__/` — find and read the existing B5 `session_marker` test file in full (its exact name may have changed; grep for `session_marker` or `SessionMarker` under this directory) as the battery-shape precedent this session's new tests should mirror almost line-for-line.
6. `operations/decision-log.md` — last 3 entries.

Confirm at open: tier (per the header above — `code-elevated`, re-verify against the cache's guidance for a live-route-touching Elevated session vs. the fuller Critical template; the eventual flag-activation is `code-critical`, not this build); hold-point status (P0 0h); model selection per the cache's AC1 table (this field adds NO LLM call of its own — pure request-shape validation + an additive payload field on an EXISTING orientation-reading emission that already ran; no new model-selection row applies); status vocabulary; signals/risk class.

## Part B — Procedure

### Step 1 — Add the flag + the request-field validation, exactly mirroring B5's `session_marker`

In `website/src/app/api/reason/route.ts`:
- A new `isLoopIdFieldEnabled()` (or whatever module the existing flag-checkers live in — match the house pattern; B5's checker likely lives in a small dedicated module, e.g. `session-decline-signal.ts` — find and mirror it) reading a NEW env var, UNSET everywhere ⇒ `false`.
- Parse an optional `loop_id` field off the request body. **Ruled shape (QG-C, verbatim from §2.7):** *"an optional, additive `loop_id` request field on `/api/reason`, exactly the live B5 `session_marker` precedent (optional declared field; validated flag-on, malformed → 400; ignored flag-off — byte-identical)."* Validation: a non-empty string within a sensible length cap (the `fresh`/`watching` siblings use 200 chars for ref-shaped fields — `MAX_GAPREF_CHARS`/`MAX_REF_CHARS` — use the same bound unless a reason emerges not to; document the rationale either way, the house discipline). Malformed + flag-on ⇒ a plain `400` with `isBillable: false` (pre-substrate — no LLM cost incurred), matching `session_marker`'s exact error-response shape. Flag-off ⇒ the field is **never read at all**, not merely ignored after being read — confirm this distinction holds in the code (an `if (flagEnabled && body.loop_id !== undefined) {...}` gate structure, not a bare parse-then-discard).

### Step 2 — Thread the validated value into the orientation event payload

**Ruled composition (QG-C, verbatim):** *"stamped by the server into the orientation event payload as `loopId?`... the two identifiers ride the *same event* as *separate fields* — the payload's `loopId` (runner-declared passthrough) alongside the event's own session-derived correlation identity (server-computed) — never concatenated into one token, never a composite key, both independently visible."*

- Add `loopId?: string` as an explicit typed optional field on the orientation-event payload type (`trust-core/types.ts`), documented with the same one-paragraph style as `generativePrompt?`/`orientationDeliveryClass?` immediately above it — a passthrough label the server stamps and never interprets (contrast the four timing parameters in `IdeaLoopConfiguration`, which the server structurally never sees at all — `loopId` is the deliberate, ruled exception).
- Thread the validated value from the route into `emitOrientationReadingTrustEvent`'s input object (`OrientationReadingEmissionInput`) → into `deriveOrientationReadingEvent`'s composed payload → into the persisted row, **only when both the flag is on AND the caller supplied a validated value** — mirror the exact `...(sessionDeclineEnabled && validatedSessionMarker !== undefined ? { sessionMarker: validatedSessionMarker } : {})` spread-conditional pattern the B5 code already uses at the trajectory-write call site (find it first-hand; the line number in this prompt may be stale).
- **No schema migration.** Confirm this at Step 1's read (the payload column is JSONB, additive fields need no `ALTER TABLE`) — if that confirmation fails, stop and re-scope.

### Step 3 — Battery: hit the ruled §2.11 dimension 6 exactly

The ruling's own words for what "done" looks like here (§2.11, item 6, verbatim): *"The additive `loop_id` field on `/api/reason` correctly implemented per the B5 `session_marker` precedent — optional declared field, validated flag-on, malformed → 400, ignored flag-off, byte-identical behaviour verified."* Build the battery to hit this explicitly, mirroring the B5 test file's exact shape (found at Part A read 5):

1. Flag-off: the field is present in a request body ⇒ **completely ignored** — no 400, no validation error, the response is byte-identical to the same request with the field absent (a diff-based or field-stripped comparison, not just "no crash").
2. Flag-on + absent: byte-identical to flag-off (no behavior change when the caller doesn't opt in).
3. Flag-on + valid: a 200 response; the orientation event (via an injected/spied emission dependency, the discernment/`fresh` sibling pattern — do not hit a real DB) receives `loopId` set to exactly the supplied value.
4. Flag-on + malformed (wrong type, empty string, over the length cap): a `400` with `isBillable: false`, the exact error-shape `session_marker` uses, before any LLM call is made (assert on a spy that no Anthropic-calling dependency was invoked).
5. Flag-on + valid, but the underlying orientation-reading emission itself doesn't fire this consult (e.g. `layer1Source !== 'server'`, or `isOrientationReadingEnabled()`/`isAgentCirclesEnabled()` off): confirm no error, no dangling reference — the field is simply unused, honestly.
6. Source-grep INV pins (the fresh/watching sibling pattern): the new flag-checker function exists and is read at call time (never cached); the field never rides into the signed assessment itself (it's an event-payload passthrough, not part of the examined artifact — confirm this distinction is real in the diff, not just asserted); no new LLM call was introduced (grep for the `client.messages.create`/`Anthropic` call count staying unchanged in this code path).

Plus: `tsc` clean; `npm run build` clean (the route continues to export only its existing HTTP handlers — this is an edit to an existing route.ts, not a new one, so re-confirm the Next route-export rule wasn't accidentally violated by the edit).

### Step 4 — Adversarial review (PR19)

Independent review before this build is treated as done — ruling-fidelity (the QG-C composition rule exactly: same event, separate fields, never concatenated; the B5-precedent shape exactly matched, not merely similar); PR20 compliance (every mechanism-fact claim in code comments traces to actual code — especially the "no schema change needed" claim and the "byte-identical flag-off" claim, both of which must be verified against the real diff, not assumed); boundary compliance (nothing here builds any runner-side heuristic/template/configuration-default code, touches `idea-loop-types.ts` beyond what's already landed, mints any credential, or activates the flag). **Pause before and after the review for the founder's model-settings changes**, per this arc's standing practice — confirmed working well across both prior sessions in this arc. If the launched review workflow's finder agents die on the account session limit (the documented spend-limit-outage class, recurred both prior sessions in this arc), **do not accept an empty `"GO"` as a pass** — complete the review first-hand across all dimensions by direct source inspection, disclose that honestly, and offer the founder a genuine independent re-run once the account limit resets.

### Step 5 — Records

Decision-log entry (`code-elevated`, given the live-route touch but no schema/credential/flag-activation step — note explicitly that the flag itself stays unset on this build's push, matching every dark-build precedent in this arc). Session close, closing out the three-item build-gate sequence and naming the runner scoping session as the natural next item (not pre-empted here — this session does not mint anything or decide the runner's identity/credential shape).

## What this session does NOT do

- Does not build any runner-side content — heuristic templates, the friction threshold, the `randomOffsetPercent` mechanism, the five configuration defaults, or any prompt text. All of that is runner-owned and external to this repo by the architecture ruling (§1 item 1) — **permanently**, not just for this session.
- Does not flip the new flag in production — activation is its own founder-walked `code-critical` step, at or after this build, not pre-approved here.
- Does not mint, provision, or reference minting any credential (the `watching_write` provisioning + the dedicated identity mint are the runner scoping session's, confirmed twice now not to be pulled forward).
- Does not touch `idea_loop_cycles`/`idea_loop_candidates`, the `watching` routes, or the `fresh` route beyond perhaps reading them for context.
- Does not resolve QG-A's guardrail-fail-closed handling in any server code — that ruling governs how the **runner** classifies a failed guardrail call in its own `watching` write; nothing server-side needs to change for it (confirm this reading at open rather than assuming it — if Step 1's reads surface a server-side implication of QG-A this prompt missed, stop and flag it to the founder before building).
- Does not re-open anything ruled — the brief's Q1–Q11, `fresh`'s §1/§2, `watching`'s §1/§2/§3, the generation-step scope's §1/§2/§3 (QG-A/B/C/D all ruled).
- Does not touch C1c-original, D4, the Stoa activation, W1–W3, B6, the permission-layer items 14–17, or the ATRF/Consciousness-and-Continuity Obligation additions (all independent, untouched by this arc).

## Rollback path

`git revert` the build commit — the field is flag-gated and the flag is never set in this session, so nothing live-facing changes regardless of the revert timing. No schema migration accompanies this build, so no `DROP`/inverse-migration step is needed.

## Forecast

Success = the `loop_id` field built exactly to QG-C's ruled shape (optional, additive, B5-precedent validation, stamped as a separate never-concatenated field on the orientation event payload), the ruled §2.11 dimension-6 review requirement hit explicitly in the battery, flag-off byte-identity proven (not asserted) on a route many other sessions depend on staying stable, PR19 clean or folded, and dark on push. This closes the three-item build-gate sequence founder-approved at the `fresh` prompt's authoring. **Next session: the runner scoping session** — credential + dedicated K1 identity mint, `watching_write` provisioning, the `ORIENTATION_DELIVERY_TIMEOUT_MS` revisit trigger, and the `frictionAssessment` PM-tool mapping decision (all four confirmed carry-forwards, generation-step scope §2.9) — its own prompt, not yet authored, gated on this session's completion.

End of prompt.
