# Next-Session Prompt — P5b: Growth's permissions-matrix row

**Stream:** founder (AO program — a small revision session extending P5, not a repeat of it).
**Tier:** `code-elevated` for the matrix amendment itself (same rationale as the original P5 session's own tier — v2 fold PROACT-3: a document that is the effective access-control policy for autonomous agent action is not a documents-only artifact under 0d-ii, even before any provisioning occurs). No code / flag / schema / mint / deploy / DB change this session. Production byte-equivalent. AC7 not engaged. Critical Change Protocol NOT engaged — any actual mint/install for Growth stays its own founder-walked `code-critical` step in a later P4 sub-session, exactly like Tech's and Ops's were.
**Governing frame:** `/adopted/standing-protocol-cache.md`; `operations/agent-org-2026-07/agent-org-and-evidence-build-plan.md` §3-P5, §2 (standing constraints), §7 (Rule B).
**Predecessor session close:** `operations/handoffs/founder/2026-07-21-P4-agent2-ops-calling-and-provisioning-CLOSE.md` (this session's own "Next Session Should" named this exact gap: there is no ready "P4 agent 3" session because Growth's matrix row was deliberately deferred, not drafted, at the original P5 session).
**Predecessor decision-log entries:** `D-P5-PERMISSIONS-MATRIX-TECH-OPS-ROWS-SIGNED-2026-07-21`; `D-P4-AGENT1-TECH-CALLING-AND-PROVISIONING-2026-07-21`; `D-P4-AGENT2-OPS-CALLING-AND-PROVISIONING-2026-07-21`.
**Risk classification:** `code-elevated` under 0d-ii, matching the original P5 session's own classification and reasoning — this is an access-control policy artifact, not mere documentation, even though nothing is provisioned this session.

## Why this session matters

`operations/agent-org-2026-07/P5-permissions-matrix.md` §3 Row 3 (Growth) currently reads: *"Deferred, not drafted this session… Its capability/spend/external-account needs plausibly differ from Tech/Ops's pattern… which this session did not have grounds to reason through carefully without either overfitting the Tech/Ops template or inventing needs."* P1's own gap analysis (§5) already named the reason this genuinely needs its own session rather than a copy-paste: Growth's real work (drafting blog/social content, running competitive web research) is WebSearch/WebFetch-heavy rather than the file-and-state-centric diagnostic/tracking shape Tech's and Ops's sessions both share, and P1 explicitly left open whether Growth's operating surface should even be the Claude-Code-loop + Gate-1 harness at all — *"or website runtime if the founder prefers a lighter-weight rollout for Growth specifically"* (P1 §5).

This session's whole job is to close that gap honestly: draft Growth's row from its own real remit, not by stretching Tech/Ops's template over it, and get it founder-signed. **This session does not mint anything, install anything, or draft Growth's calling document** — those stay a later, separate, founder-walked `code-critical` P4 sub-session (P4 agent 3), gated on this row being signed, exactly as Tech's and Ops's sessions were each gated on their own signed row.

## Part A — Open under the protocol

Read in order:
1. `/adopted/standing-protocol-cache.md` (~3 min) — confirm tier, model selection (N/A — no LLM calls in this session beyond ordinary reasoning), risk class, signals.
2. `operations/handoffs/founder/2026-07-21-P4-agent2-ops-calling-and-provisioning-CLOSE.md` (~5 min) — the session that surfaced this gap explicitly.
3. `operations/agent-org-2026-07/P5-permissions-matrix.md` — read in full, especially §0 (method), §2 (the hard floor), §3 Row 3 (Growth's current deferred stub — this is what gets replaced), and §6 (the sign-off record this session appends to).
4. `operations/agent-org-2026-07/P1-agent-roster-gap-analysis.md` §3 (the gap map — Growth's specific rows: "Marketing vs. Growth separation," "Content production workflow," "Competitive-intelligence cadence"), §4 recommendation 3 (the content-workflow re-scope, not a new agent), and §5 (the ranked-order table's Growth row — read the "Recommended E1" column carefully, it is explicitly unresolved).
5. `operations/agent-org-2026-07/credential-ledger.md` and `operations/agent-org-2026-07/tech-calling-v1.md` + `ops-calling-v1.md` — read as **format precedent only**, not content to reuse. Growth's row should not read like a relabelled copy of either.

Confirm at open: tier (`code-elevated`); hold-point status (P0 0h — untouched); model selection (N/A); status vocabulary; signals + risk classification.

## Part B — Procedure

### Step 1 — Ground Growth's real remit before drafting anything
Before writing a capability set or spend envelope, read (or re-read) what Growth's actual work looks like per the existing wiring-fix material (`operations/handoffs/growth/growth-wiring-fix-close.md`) and P1's own characterization (content/positioning/competitive-intel, cadence-driven, WebSearch/WebFetch-shaped). Do not assume the Tech/Ops consult+write UPC-pair template transfers unchanged — check whether Growth's actual task shape (e.g., fewer `/api/reason` consults per session if it's mostly web research, or more if every drafted piece gets examined) suggests a genuinely different spend envelope, not just a smaller or larger number picked by analogy.

### Step 2 — Resolve election E1 (operating surface) explicitly, via AskUserQuestion
P1 left this open: Claude-Code-loop + Gate-1 harness (matching Tech/Ops), or website runtime (a lighter-weight rollout, no harness install, per P1's own alternative). Present both with their real tradeoffs (the Gate-1 harness gives the evidence-dividend Track B applies to; the website runtime is lighter but forgoes that dividend for Growth specifically) and let the founder decide — do not default to the Tech/Ops pattern just because it is already twice-proven.

### Step 3 — Draft Growth's row and present for sign-off
Using the matrix's own schema (§1), draft: UPC capability set (or the website-runtime-appropriate equivalent, depending on Step 2's outcome), spend envelope (reasoned from Growth's actual call shape, not copied from Tech's 150/15 or Ops's 120/10 by default), external accounts (P1 found "none named" — confirm this still holds, or name a real one if the session's own research surfaces a genuine need), and the proactive/unattended envelope (named-candidate-only, attended-only default, per §2's hard floor — unchanged rule regardless of E1's outcome). Present via AskUserQuestion for explicit founder sign-off before treating the row as final, matching the same care the original P5 session gave Tech's and Ops's rows.

### Step 4 — Amend `P5-permissions-matrix.md` in place
Replace §3 Row 3's current deferred stub with the drafted, signed row. Update §6 (sign-off record) to add Growth's signature line, matching Tech's and Ops's existing entries.

### Step 5 — Append decision-log entry
Pattern per `/adopted/standing-protocol-cache.md` §"Lean decision-log entry" (or the fuller form the original P5 entry actually used, if more fidelity is warranted — match the level of detail P5's own entry carries, not the bare-minimum 20-line template).

### Step 6 — Session close
Pattern per the cache's §"Lean session close". Name the next step: a future, separate, founder-walked `code-critical` P4 sub-session (agent 3 — Growth) — calling document + credential mint + harness or website-runtime setup + verification — now unblocked by this row being signed, on the founder's own cadence. Support (agent 4) remains deferred, unchanged, pending the founder's own ring-vs-Gate1 decision (P1 §4.2) — not something this session touches.

## Part C — Anticipated session shape

| Phase | Estimate |
|---|---|
| Opens reads | 15–20 min |
| Step 1 — ground Growth's real remit | 15–20 min |
| Step 2 — E1 election | 10–15 min |
| Step 3 — draft + sign-off | 25–35 min |
| Step 4 — amend the matrix | 10 min |
| Decision-log + close | 20–30 min |
| **Total** | **~1.5–2 hours** (materially lighter than the original P5 session — one row, not schema + two rows + ledger stand-up + KILL-SWITCHES extension) |

## Rollback path

Documents-only session — no code / schema / flag / credential / deploy change (no mint runs here). `git revert` the records commit reverts the matrix amendment and the decision-log entry together; nothing live depends on any of it, since Growth's actual provisioning hasn't happened yet.

## Forecast

Success is Growth's matrix row genuinely grounded in its own remit (not a relabelled Tech/Ops row) and founder-signed, with E1 resolved as an explicit decision rather than an assumption — so a future P4 agent-3 (Growth) session can open with a real, satisfied dependency, exactly as Tech's and Ops's sessions did. This does not touch Support (still gated on a separate founder decision, not a matrix row), the held P2 Fable-5 repeat, or the 0h launch call.

End of prompt.
