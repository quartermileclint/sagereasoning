# Next-Session Prompt — S11b: the examined-input recomposition → the Arm-1 narrowing → the reducer/cap fix

> **Why this session exists.** S11a (2026-07-18) ran the R12 extraction gate and it ruled **"the extraction question is prior"**: the at-action examination's input is **starved by composition at the harness site** — `describeAction` sends a `Write` as *path + character count* (**the file content is discarded entirely**; 64/64 frozen-buffer records) and an `Edit` as the first 200 chars of `new_string`; the consult POSTs that one line. So affected parties genuinely present in the underlying action are **structurally invisible** to Layer 1 — which is itself faithful (circles surface when the composed text carries parties). The narrowing was therefore **deferred here**, so it lands on inputs that could actually have carried a circle. **Diagnosis of record (read §2 before any work):** `operations/trust-layer-2026-07/2026-07-18-S11a-extraction-gate-diagnosis.md`.
>
> **⚠ Sequencing consequence (ADR-014, adopted 2026-07-18 — READ THIS):** ADR-014's E7 sequencing is **"S11a first (the extraction regime settled + version-marked once; the item-5 trigger a named instrument in it) → AE-1 → AE-2 → AE-3"**, and the AE-1 prompt carries the **hard pre-condition "S11a has settled the extraction regime."** S11a did NOT settle the regime — the R12 gate ruled the extraction question prior, and the settlement moved HERE. **So S11b inherits the regime-settlement obligation, and AE-1's hard pre-condition now reads: S11b has settled the extraction regime.** AE-1 is blocked until this session lands (a dated note to that effect sits at the top of the AE-1 prompt). Two ADR-014 requirements therefore BIND on Part 1 below: **(i) the regime is settled ONCE and VERSION-MARKED** (extraction-regime markers — rows carry the regime under which they were extracted; delta computations refuse to compare across a regime boundary; ADR-014 §"Extraction-regime markers"), and **(ii) the item-5 bare-tool-payload trigger** (E5: a T2-soft/marker trigger on the at-action surface for an action text that is a bare path/diff with no party, role, or purpose — **NEVER a T1 halt there**, which would starve R18f provenance on 100% of the live distribution) **rides this same settlement** — it is the remedy for the RESIDUAL genuinely-party-less class after recomposition, and settling it separately would create the two-regime contradiction ADR-014 exists to prevent. Note: ADR-014 expected the trigger's fire-rate to be the diagnostic separating starved from mis-sited; S11a separated them by direct source reading instead (stronger, cheaper — the diagnosis §2), so the trigger enters S11b as a REMEDY component, not as the diagnostic.

**Stream:** founder (trust-layer).
**Tier:** `code-elevated` for the recomposition + the predicate narrowing — **with a `code-critical` founder-walked arm** (the reducer narrowing + the state correction; AC7 + PR17 engage there). The founder may reclassify upward at any time.
**Governing frame:** `/adopted/standing-protocol-cache.md` + `/adopted/build-sessions-protocol-cache.md`.
**BINDING SPECIFICATIONS (verbatim wins):** the 2026-07-17 F2 verdict §1 + §1b (`operations/trust-layer-2026-07/2026-07-17-mentor-consultation-F2-exclusion-clause-verdict-verbatim.md`) — R11 (the narrowing's content: **≥1 identified circle** — settled, not re-litigated), R13 (the A2 blindness must be **visible**), R14 (discharged at S11a on current ground; the cap's **durable fix** was dispositioned to ride THIS session).
**Predecessor close:** `operations/handoffs/founder/2026-07-18-trust-layer-S11a-extraction-gate-CLOSE.md`.
**Predecessor decision-log entry:** `D-TRUST-LAYER-S11A-EXTRACTION-PRIOR-NARROWING-DEFERRED-CAP-REVIEWED`.
**The register:** `operations/trust-layer-2026-07/S11-FLIP-PREREQUISITES-REGISTER.md` — P2 (deferred behind P3), P3 (answered at diagnosis level; the remedy is this session), D1 (cap disposition), D2, D3, §E (credential generations).

---

## What this session delivers (three parts, in order)

### Part 1 — the examined-input recomposition (`code-elevated`; harness-side)

Redesign what `describeAction` + the consult POST as the examined action text, so the examination reads an input that can carry affected parties. **Elections to put to the founder at open (AskUserQuestion), with a recommendation formed first-hand:**

1. **Composition content.** The candidate axes, composable:
   - **(a) Bounded payload content** — `Write`: include a bounded head (and optionally tail) of `content`; `Edit`: include bounded `old_string → new_string` (both sides — the change's direction matters for justice reading). Bounds: the composed text must clear `truncateForServer` (4800) and the server's `TEXT_LIMITS.medium` (5000) — the S9b request-cap class; truncate **safely inside** the composer, never rely on the server 400.
   - **(b) Narrated intent** — the transcript tail preceding the tool call (the H2/H5 discernment precedent already reads transcript tails; the S9 F-CONF finding: *proposed-action framings are the reliable input class*). This addresses the residual mis-siting question the diagnosis carried: even with full content, a tool payload lacks the agent's narrated intention.
   - **Recommendation to evaluate first-hand:** (a)+(b) both, bounded — intent first, payload excerpt second. But reach this with the diagnosis §2 open; do not inherit it.
2. **Egress + privacy posture.** Content inclusion widens what leaves the machine to prod `/api/reason` (retained encrypted 90d per R17b; signed assessments persist). The operator is the founder and the repo is the founder's — but the election must be conscious (the S8 operator-install disclosure precedent). The local observation record stays PII-light regardless (previews stay truncated).
   **2b. 🔴 SENSITIVE-PATH DENYLIST — MANDATORY, not elective (adversarial-review HIGH, 2026-07-18).** The hook fires on ANY `Edit`/`Write` in the founder loop — **including `.claude/settings.local.json` (which `/practice-on`//`/practice-off` themselves edit, and which carries the live `sr_prac_` tokens — the exact file class of the 2026-07-17 public-exposure incident) and `.env*`.** Under the current lean composition nothing beyond a path ever leaves the machine; under the recomposition, composed content would (a) be retained encrypted 90d server-side, (b) be **quoted VERBATIM inside SIGNED assessments** via the live corroboration check's marker spans, (c) land in the local provenance JSONL (`appendProvenance`), and (d) ride H4 accreditation writes. **The recomposition MUST ship with a sensitive-path denylist that forces the LEAN composition for matching paths** — at minimum `.env*`, `**/.claude/settings.local.json*`, key/credential/backup patterns (align with the incident's `.gitignore` glob + the F2 tripwire follow-up; guards must match the real thing, not a name resembling it — the RA-1-F3 lesson). The transcript-tail option (1b) carries the SAME widening — the tail can quote secrets the conversation displayed — and needs the same screen or a conscious founder acceptance. The review's egress dimension must check this specifically.
3. **Cost.** Bigger inputs = more Layer-1 tokens per consult on the gen-2 loop credential (5000/200). Estimate the per-consult delta at the chosen bounds before electing.
4. **Knob or hard-land.** Recommend a config knob (e.g. `GATE1_ACTION_TEXT_MODE=lean|composed` or an explicit content-length knob in `discernment.config.json`/env) defaulting to the NEW composition, with the lean form retained for comparison — the next observation window (P6) may want an A/B leg.
5. **The regime version-mark (ADR-014, binding).** The recomposition IS the extraction-regime change: stamp a regime identifier (e.g. `extraction_regime: "at-action-v2-composed"`) onto every captured observation record and every surface AE-1's delta computations will read, so no delta is ever computed across the boundary (ADR-014 §"Extraction-regime markers" — an instrument change must never masquerade as agent change). Settle it ONCE here.
6. **The item-5 bare-tool-payload trigger (ADR-014 E5) rides this settlement.** For an action text that is STILL a bare path/diff with no party/role/purpose after recomposition: the T2-soft/marker trigger form (ask, never halt) on the at-action surface. Design + land it as part of the same regime so the extraction is settled once, not twice.
7. **The dedup consequence (review MEDIUM, 2026-07-18) — elect, don't inherit.** The decision signature is **path-based** (`Edit:${fp}` / `Write:${fp}`, at-action-hook.mjs:294/:303) and the consult fires once per (session, signature) — positively verified: the TEXT change does NOT break dedup or loop identity. But it means **only the FIRST edit's content per file is ever examined**; a second, materially different — possibly party-affecting — edit to the same file is silently unexamined. Defensible under the lean composition (every edit looked identical); it partially defeats the recomposition's purpose. Elect: keep path-based dedup (disclosed, first-edit-only) vs add a content-hash component to the composed-mode signature (estimate the consult-count/cost consequence under election 3).
8. **Scope statement — the four consumers of `action.text` (review LOW, 2026-07-18).** The recomposition must state which are in/out: (i) the consult `fetchFrame` (the recomposition's target); (ii) **`fetchGuardrail`** (at-action-hook.mjs:456 — the DENY-capable channel; inert for file tools under the default `guardTools = []`, but any configured guardTool would route composed content through it); (iii) `renderGate2ElicitationBlock` (embeds `action.text.slice(0,200)` — post-recomposition this shows intent/content head instead of a path); (iv) the loop-state `adoptedCorrection` (`action.text.slice(0,160)`, persisted locally). The caps cited in 1a are verified accurate: `truncateForServer` = `MAX_SERVER_INPUT_CHARS` 4800 with marker (framing-core.mjs:43-52), under `TEXT_LIMITS.medium` 5000 (security.ts:198-202), applied on BOTH fetchFrame and fetchGuardrail.

**Validation (in-session, small — SPECIFIED against vacuous pass; review MEDIUM, 2026-07-18):** a constructed-action battery driven through the REAL composer + extraction, **N≥3 runs per fixture with an explicit pre-stated pass threshold** (the standing lesson: an N=2 run produced a FALSE-PASS in the §4 LOCUS-2 arc; raise repro runs — memory `over-strictness-check-must-be-rank-preserving`). Fixture classes, each represented: **(a)** party-affecting content (editing public-facing copy that affects users; writing a doc that names an obligation) → circles MUST surface; **(b)** genuinely party-less content (lorem scratch file) → no circles; **(c) mention-without-affect** (editing a doc that QUOTES user-facing copy or discusses parties without the action affecting them — the frozen buffer's idx-43/112/126/127 class) → the battery must show the extractor does NOT convert mere mention into circles, else the narrowed Arm 1 floods with manufactured correct_holds — **the "did we narrow it so it passes?" trap in its recomposed form**; **(d)** the noise class (the buffer's idx-104: a zero-information Write that read `moderate` — extraction variance on one-liners) → quantify, don't assume away. This is the reliability demonstration the R12 gate found missing. Update `logic-harness.mjs` + `negative-battery.mjs` for the new composition (the RELEASE GATE must pass).

### Part 2 — the Arm-1 narrowing (`code-elevated`; repo-only; only after Part 1 lands)

R11's content, verbatim: **"Arm 1 requires at least one identified circle."** In `kathekon-engagement.ts` (report-time predicate; the flip later binds on the same function):

- Regression-pin **both directions**: a `contrary` + zero-circle verdict now reads `false_positive`; a genuine circle-carrying justice surface (violated / indeterminate / unevaluated-with-circle / met) still reads `correct_hold`.
- **Keep the circle-present `unevaluated` class firing** — a circle identified whose obligation was never evaluated is the U2/J2 marketing-email class; the narrowing removes ONLY the zero-circle case.
- **Pin the A2 blindness explicitly and visibly (R13):** a test documenting what the narrowed arm does with a zero-circle record — the omission class (a harm omitted from narration → no circle → same wire signature as party-less) remains the disclosed structural residual **even after recomposition**; it must be visible in the battery and stated on every output of the narrowed arm. Also update the "belt and braces if the justice arm is ever narrowed" comment (kathekon-engagement.ts:122) — the anticipated narrowing has now happened; the OR-listed `violatedObligation` arm becomes load-bearing.
- Re-run the report `--dry-run` over the **frozen** buffer — **evidence of what the narrowed arm sees on the OLD starved input** (expect ≈128 FP / 1 CH, matching S11a's reconstruction) — recorded with the S11a bounds (it measures the old composition, not the examination). **Not a readiness claim.**

### Part 3 — 🔒 the reducer narrowing + the cap/state correction (**`code-critical`, founder-walked, AC7 + PR17**)

The S11a cap disposition (D1): disclose-and-carry ends HERE. One coherent founder-walked step:

1. **Narrow `deriveWorstJusticeOutcome`'s `unevaluated` branch** (`derive-trust-events.ts:200`): the zero-circle case (`dikaiosyneEngaged && statuses.length === 0` with **no circles at all**) no longer emits `justice-surface-unevaluated`. **Careful — the branch must still fire when circles EXIST but none carries an assessment** (that is J2, the class the whole ADR-010 arc exists for). Design decision in-session: implement as a circle-count check alongside the existing gate, or re-key the dikaiosyne-engagement gate to §4's definition (`circles >= 1 || hasNaturalRelationship`) per register **D2** — but note D2's engine half (`computeVirtueDomains`) is LIVE SCORING code on `/api/reason`; reconciling THAT is a bigger change and is **not** licensed by this prompt unless the founder explicitly elects it (it changes live assessments; own byte-identity battery + review if so).
2. **The state correction, same walk (founder-run SQL; the AI performs no Supabase op):** clear the justice latch on the `agent_trust_state` row for (`sagereasoning:s9-loop@v1`, dikaiosyne) — the ledger's single S9-install `justice-surface-unevaluated` event (2026-07-11) stays, append-only, historical truth. §VERIFY: the public GET reads `justice_capped: false` (or the latch field cleared; the earned level itself may legitimately stay `deliberate`), and the NEXT gen-2 accreditation close-write does **NOT** re-latch (the reducer narrowing makes the correction stable — the whole point of sequencing it here).
3. **Update the S1 battery pins** (trust-core 97/0 carries `unevaluated`-derivation pins that will rightly move) + the emission-hooks battery; full sweep + `tsc` + the negative-battery RELEASE GATE.
4. **Rollback, specified BEFORE the walk:** `git revert` the reducer commit (emission reverts to the old predicate); the state correction reverts by re-running the fold or founder SQL restoring the latch flag (write the exact statement down in the walk record before executing the forward step).

## Explicitly NOT in scope
- **The S11 flip** — readiness NOT met; the register governs. Weights BLOCKED. Nothing here is progress toward it.
- **The §9 input question (P1)** — its own design step, before the flip is reconsidered.
- **The new observation window (P6) + the guard-path denominator (P5)** — design AFTER this session lands; the window needs the recomposed input + the narrowed predicate to mean anything.
- **`computeVirtueDomains` / §4 engine changes** — live scoring; only by explicit founder election with its own battery + review (see Part 3.1).
- **Any Layer-1 prompt change** — the diagnosis found the prompt faithful; re-check after recomposition and, if a change is then warranted, it is its own Critical step.

## Verification (Step-8 set)
`kathekon-engagement` (will grow) · `false-hold-capture` · trust-core S1 (pins updated at Part 3) · `emission-hooks` · S10 106/0 · `logic-harness` (updated) · `negative-battery` RELEASE GATE · `tsc` 0 · `npm run build` only if any route/page changes (none expected). Adversarial review per the slice pattern (Workflow fan-out) — dimensions: composition-faithfulness (does the composed text carry what the diagnosis says it must?); truncation-safety (the 4800/5000 caps); A2-visibility (R13); reducer-narrowing correctness in BOTH directions (zero-circle silenced; circle-present J2 still fires); cap-correction stability (the re-latch test); claims-vs-code; egress-posture honesty.

## Rollback
Part 1: `git revert` (harness-local; the knob's lean mode is the in-place fallback). Part 2: `git revert` (report-time predicate only). Part 3: per its own specified-before-walk rollback (above).

## Forecast
Success = the examination reads inputs that can carry parties (demonstrated on constructed actions), the narrowed arm is live in the predicate with the A2 blindness pinned visible, the reducer no longer manufactures `justice-surface-unevaluated` from zero-circle assessments, and the public cap on `sagereasoning:s9-loop@v1` is corrected **stably**. Then: the P6 window design (with P5's denominator problem) → P1 → only then is the flip re-examinable against the four-part standard. **The 0h call remains the founder's.**

End of prompt.
