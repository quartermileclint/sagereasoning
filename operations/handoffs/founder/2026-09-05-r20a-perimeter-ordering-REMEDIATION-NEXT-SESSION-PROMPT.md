> **GROUP 1 DONE 2026-09-05** — the four minimum-length moves are LIVE (`c679739` on `origin/main`, Vercel green) and **live-smoked, all eight probes as expected** (`2026-09-05-r20a-perimeter-ordering-remediation-session3-CLOSE.md`; `D-R20A-PERIMETER-ORDERING-REMEDIATION-GROUP-1-BUILT-2026-09-05` + its LIVE addendum). F-6's two `format` smokes are also discharged. **Groups 2 and 3 remain open under this prompt.**

> **GROUP 2 LIVE 2026-09-06** (`cbd93ae` on `origin/main`, Vercel green, twenty founder-run smokes on ten routes all as expected — `D-R20A-PERIMETER-ORDERING-REMEDIATION-GROUP-2-LIVE-2026-09-06`). **Item 9 (the Stoa pair) was NOT in Group 2 (Election A = items 5–8 + 10) and remains open as its own sitting.** Originally: BUILT (items 5–8 + item 10 + the R3 Branch-2 row; Session 3B) — pending the founder's push and smokes; three-reviewer PR19 completed (2 HIGH on the batteries folded: a class fence NEG-1 on every route). Group 2b (P′ + O, ruled below) was elected for its OWN sitting; Group 3 remains.**

> **GROUP 2b + GROUP 3 BUILT 2026-09-06** (`555502e`, Session 3C; `D-R20A-PERIMETER-ORDERING-REMEDIATION-GROUP-2B-3-BUILT-2026-09-06`) — pending the founder's push and smokes (close: `2026-09-06-r20a-perimeter-ordering-remediation-session3C-group2b-3-CLOSE.md`). Three-reviewer PR19 completed (2 MEDIUM on the proof routes' retained presence halves folded — typeof-only → falsy). Journal-feed's two `event_timestamp` O items moved too (founder election; absent from the paste). **Only item 9 (the Stoa pair) remains under this prompt — S3.4, paste authored at this close.**

> **NEXT SITTING (2026-09-06 plan): Group 2b + Group 3 together — paste `2026-09-06-SESSION-3C-r20a-remediation-group2b-group3-SESSION-PASTE.md`. Then item 9 (Stoa) closes the arc.**

> **RULED ADDITION 2026-09-05 (mentor Part 5, `2026-09-05-mentor-rulings-five-relays-verbatim.md`) — GROUP 2b, added to this list:** the principle extends beyond length guards. **P′ (three sites, Group-1 shape):** `score-scenario/route.ts:293` (`scenario` presence, screened field `response`), `mentor/journal-feed/route.ts:47` (sibling presence, screened `impression`), `journal/route.ts:34` (`day_number` presence, screened `reflection_text`) — line numbers at `19509bb`, re-derive. **O where the screened text is present, case by case:** e.g. `mentor/private/reflect` `bypass_pattern_cache` boolean 400; the Stoa `visibility` enum; `/api/reason`'s `session_marker`/`loop_id` 400s (human path). **Outside the principle, no move owed:** malformed-JSON 400s (J), founder-only 403s (A), flag-off 503s (F). The ruled boundary: route open + caller the intended human user + screened text present and readable, jointly.

# NEXT SESSION PROMPT — Session 3: the R20a perimeter-ordering REMEDIATION

**Paste into a fresh session after the standing opener. Tier: `code-critical`** — every candidate is a
live R20a perimeter member (PR6 + AC5). **Founder-walked (PR17): the founder pushes, watches Vercel,
and runs every live smoke; the AI edits, tests, and guides. AC7 engages at the push. PR19 is
REQUIRED** — three blind reviewers on each group's diff before the founder pushes it, exactly as the
`format` move had (`D-SCORE-CONVERSATION-FORMAT-MOVE-PR19-INDEPENDENT-REVIEW-FOLDED-2026-09-06`).

**Written 2026-09-05** (machine date — `date`; HEAD at writing `a014620` plus the audit commit). Session
3 of the 2026-09-05 plan (standing opener, Version 2026-09-05, Standing queue B.3). **Executes the
audit; the audit governs the list; the ruling governs the audit.**

---

## 0. Read first, in this order

1. The ruling, verbatim — `operations/count-discipline-2026-09/2026-09-06-mentor-ruling-r20a-length-guard-ordering-verbatim.md`
   (authored 2026-09-05 AEST despite the label). One sentence binds this session: *"The distress check
   runs before the length guard on any route where the human crisis form is rendered."* And one frames
   it: *"inherited properties are not examined properties"* — nothing you move is anyone's error.
2. The audit — `operations/count-discipline-2026-09/2026-09-05-r20a-perimeter-ordering-AUDIT.md`
   **in full**, then its review record in `2026-09-05-r20a-perimeter-ordering-audit-CLOSE.md`. §6 is
   your list; §4.3 (screening truncation) and §4.6 (`/api/score/save` fails closed) are design
   constraints; §1.8 names what the audit could not see.
3. The precedent move — `website/src/app/api/score-conversation/route.ts` (the `format` guard now sits
   after the R20a block; read its comment) and its battery
   `website/src/app/api/score-conversation/__tests__/r20a-invocation.test.ts` **FV-6a–d** (~lines
   682–795): the pin shape you will reproduce on every route you touch.
4. Memories: `human-routes-bearer-jwt-console-smoke` (every human route 401s on a bare fetch — the
   smoke pattern), `guard-scope-must-cover-the-class` (a guard scoped to the last instance is worse
   than none; strip comments before any source-text assertion), `content-pins-assert-exported-values`.

---

## 1. State at hand-off

- **Production** serves the `format` guard after the R20a block (`0126645` + `97db750`, both on
  `origin/main`, Vercel green). The audit found, by control-flow trace, **16 route-level members** on
  which a length guard can return 400 before the distress check on a human session — **39 guard
  sites, 9 of them minimum-length** — and **27 conformant members**; the 2 substrate-gate members are
  agent-facing and **not reached by the ruling**. Re-read the audit's tables; do not quote these
  numbers forward without opening §5. **The audit was reviewed by three blind reviewers (0 HIGH;
  every fold marked in place); its sweep is rev 2, mutation-verified.**
- **The sweep is your per-group ordering check, in addition to the pins:** after each group's
  edits, `node operations/count-discipline-2026-09/2026-09-05-r20a-ordering-sweep.js website` must
  show each moved guard **neither** in the handler's pre-check list **nor** in its check→redirect
  window (which must stay `0 bound line(s)`), and the SUMMARY's window count must stay 0. Commit the
  refreshed `…-sweep-OUTPUT.txt` with the group so the record shows the movement.
- **One comment to correct while you are in the file:** `score-conversation/route.ts:265–268` says
  the `conversation` minimum shares provenance `aeadbd1` with the maxima; git says `496d832`
  (2026-03-23). Fix the comment in the same move (audit §4.7).
- **No route has been changed since the audit.** Confirm: `git diff --stat <audit commit> -- website/`
  must be empty at your open; if it is not, re-derive the affected rows before trusting the audit's
  line numbers (they were as at `a014620` and files in this arc move lines weekly).
- **The two owed Bearer-JWT smokes on `/api/score-conversation` (opener F-6) have never been run.**
  They ride this session's walk.
- **The false-hold window (S4) may or may not have started.** Check
  `GATE1_FALSE_HOLD_CAPTURE` in the founder's `.claude/settings.local.json` (keys only — never read a
  token). If it is SET, **`/api/reason` is inside the measured surface and must NOT be edited** (its
  move, §6 item 10, waits for the window to close). If it is absent, item 10 is available and should
  land **before** the founder sets it.

## 2. First move: verify, don't trust this file

```bash
cd "/Users/clintonaitkenhead/Claude-work/PROJECTS/sagereasoning"
date && git fetch origin && git status && git log --oneline -3 && ListAgents
node operations/count-discipline-2026-09/2026-09-05-r20a-ordering-sweep.js website > /tmp/sweep.txt; tail -3 /tmp/sweep.txt
cd website && npx tsx src/lib/__tests__/r20a-invocation-guard.test.ts > /tmp/guard.txt 2>&1; tail -1 /tmp/guard.txt
npx tsx src/app/api/score-conversation/__tests__/r20a-invocation.test.ts > /tmp/fv6.txt 2>&1; tail -1 /tmp/fv6.txt
```

Expected at writing: sweep `KNOWN-CASE … format guard AFTER block end: true`; guard `722 passed, 0
failed`; score-conversation `66/66 pass`. (Redirect to a file, then `tail` — never pipe a tsx battery
straight into `tail`; memory `tsx-tests-setinterval-keepalive-hang`.)

---

## 3. Design constraints — settled by the ruling and the audit, not open

1. **Order, not existence.** Every guard stays. Each moves to sit **after the distress check and its
   redirect return**, and **still before** any context load, LLM call, engine call, or store write.
   Guard *values* do not change.
2. **Bound what reaches the classifier.** On routes that pass the raw field to
   `detectDistressTwoStage` (the score family, `/api/reflect`, `/api/mentor/private/reflect`,
   `/api/journal`, `/api/mentor/journal-feed`, the three proof routes), moving a maximum guard after the
   check sends **unbounded** text to the classifier. Add a screening cap at the move — compose the
   screened subject through a slice at the route's existing bound (the gap-closure convention:
   `DISTRESS_SUBJECT_FIELD_CAP`, `r20a-gap-closure.ts`; the `format` move's own `composeDistressSubject`
   caps at `TEXT_LIMITS.long`). The ruling accepted *"a bounded cost-amplification vector"*; keep it
   bounded. Disclose in the route comment that distress past the cap is not screened (audit §4.3 —
   the relocated residual, named, not new).
3. **Minimum guards move too.** A 14-character *"I want to die."* must reach the check. After the
   move, a benign short input still 400s — that is the guard doing its job *after* the person has
   been answered.
4. **Dual-audience members bind on the human path only.** `/api/reason`'s credential path and
   `/api/execute`'s API-key branch are purpose (a) territory: **do not touch them.** On `/api/reason`
   the human path's guards must move without altering the agent path's order or any byte the agent
   path executes (byte-identity on the credential path is a PR19 dimension).
5. **`/api/score/save` stays fail-closed** (503 if the classifier throws) — it is conformant; do not
   "normalise" it toward the fail-open siblings.
6. **The Stoa pair is a restructure, not a line move** (`/api/mentor/stoa` `parseDeclaration`;
   `draft-reflect` `parseDraft`): the gate runs over the merged entry after a store read. The
   screened subject must be composed from the raw body (and the prior entry on PATCH) **before**
   parsing, with the parse's 400s after the gate. Design it on paper first; it is the one item where
   the shape of the fix is not obvious from the precedent.
7. **Agent-facing members untouched:** `/api/calling`, `/api/practice/reflect`, and every perimeter
   exclusion. Purpose (a) governs there; the audit found no length guard on them in any case.
8. **The pin.** Each move is pinned in the route's per-route battery (create one where none exists —
   the opener lists 15 of 22 routes without per-route invocation tests) by an **execution-order anchor
   of the FV-6 shape**: locate the check call; brace-match from the opening brace of the block the
   check lives in (the flag block where there is one; the redirect-return `if` where the check is
   unconditional) to its structural **end**; assert every moved guard's index exceeds that end and
   precedes the first context-load/engine/store call; assert non-vacuity separately (anchors found,
   open ≠ end, exactly one guard occurrence). Strip comments before matching. **Mutation-verify each
   pin against the two bypasses three reviewers demonstrated on the `format` move:** (i) the guard
   placed *inside* the block, before the check; (ii) the guard placed *between* the check and the
   redirect return. Both must go red. Record the mutation output in the close.
9. **One pin per route**, even where the shape repeats (the score family): a shared helper is fine, a
   shared *assertion* is not — memory `guard-scope-must-cover-the-class`.

---

## 4. The list — the audit's §6 order, keyed to harm; the founder elects the scope at open

**Do not attempt all sixteen in one sitting.** A group is one PR19 pass, one push, one set of smokes.
Group 1 plus the F-6 smokes is a full session; Groups 2–3 follow in their own sittings under this same
prompt (mark the group done in a dated line at the top of this file when it lands).

**Group 1 — minimum-length guards on practitioner surfaces (a short genuine cry is refused unread):**
1. `/api/score-conversation` — the `conversation` `<20` minimum (~`:129` at writing). The ruled route.
2. `/api/reflect` — `what_happened` `<10` (~`:96`).
3. `/api/mentor/private/reflect` — `what_happened` `<10` (~`:174`). Same shape as 2; move together.
4. `/api/score-scenario` — `response` `<5` (~`:300`).

**Group 2 — maximum guards on the most distress-likely practitioner surfaces:**
5. `/api/reflect` (~`:84`, `:90`) and `/api/mentor/private/reflect` (~`:162`, `:168`) — one shape.
6. `/api/journal` (~`:43`); `/api/mentor/journal-feed` (~`:55`, `:58`, `:61`).
7. `/api/score-conversation` (~`:111`, `:117`) — the two remaining pre-block maxima.
8. The score family — `/api/score` (~`:146`, `:148`); `/api/score-decision` (~`:102`, `:104`);
   `/api/score-scenario` (~`:308`, `:313`); `/api/score-social` (~`:110`); `/api/score-document`
   (~`:106`, a 30,000-character field — the largest raw input any member would send to the
   classifier; the screening cap matters most here).
9. The Stoa pair — `/api/mentor/stoa` `parseDeclaration` (~`:150`, `:171`, `:176`) and
   `/api/mentor/stoa/draft-reflect` `parseDraft` (~`:114`) — the restructure (constraint 6).
10. `/api/reason` human path (~`:1038`, `:1040`, `:1042`; `:1064` when the continuation flag is on) —
    **only if the window has not started** (§1). Agent path untouched.

**Group 3 — founder-only proof surfaces (one human caller; lowest reach):**
11. `/api/founder/hub/ring-proof` (~`:153`, `:160`); `/api/mentor/ring/proof` (~`:118`, `:125`);
    `/api/support/agent/proof` (~`:141`, `:147`, `:153`, `:172`, `:174`).

**Re-derive every line number at open** — `grep -n` the guard text; the tilde is deliberate.

---

## 5. Procedure, per group

1. **Design the move on paper** (a five-line note per route: the check line; the block it lives in;
   where the guards go; whether a screening cap is added; what the pin anchors on). Put it to the
   founder before editing.
2. **Edit**, keeping each guard's text byte-identical where possible so `git log -S` still finds it.
   Comment at the new location in the `format` move's register: *placed after the R20a block under
   the 2026-09-05 ruling; order, not existence*.
3. **Pin + mutation-verify** (constraint 8). `tsc --noEmit`; `npm run build` (route files change —
   memory `nextjs-route-export-validation`); the guard battery; the route battery.
4. **PR19 — three blind reviewers, in parallel, briefed to break:** (a) execution order on the actual
   control flow, including the two bypasses; (b) the screening cap and the classifier's bounded input;
   (c) byte-identity of every path the ruling does not reach (agent paths; the conformant siblings;
   the guard values). Fold at the root; withdraw over-claims at the head. If the account limit kills
   the fleet, complete first-hand and **do not push** until an independent re-run has passed (PR19
   §4 — the re-run gates the push, not the close).
5. **Commit path-scoped** with `Model:`/`Effort:` trailers; **the founder pushes** (GitHub Desktop);
   Vercel green.
6. **Smokes — founder-run, Bearer JWT from the `sb-*-auth-token` localStorage key, none of them
   write a row:**
   - For each moved **minimum**: a 14-character distressed submission → **HTTP 200 with the crisis
     redirect** (`distress_detected: true`, the seven-line resource list); a benign submission of the
     same length → **400** (the guard retained). Neither reaches a store.
   - For each moved **maximum**: an oversized distressed body (distress in the first 2,000 characters)
     → the redirect; an oversized benign body → **400**.
   - **F-6, on `/api/score-conversation`** (rides Group 1 or 2, whichever the founder walks first): an
     oversized benign `format` → **400**; an oversized distressed `format` → the redirect.
   - Avoid *mild*-severity wording in a distressed probe on `/api/score-conversation` — mild proceeds
     to the engine by design and would spend.
   Record each as observed (status + body fields), never as expected.
7. **Records:** decision-log entry at the physical tail (`D-R20A-PERIMETER-ORDERING-REMEDIATION-GROUP-N-LIVE-…`);
   the CLAUDE.md production-state block per PR18 (a production change — batch with S8 only if S8 runs
   the same day); the audit's §2.1 row annotated *MOVED <commit> <date>* (append, do not rewrite);
   this prompt's top line marked for the group; a lean close.

---

## 6. Do NOT

Change a guard's value. Move an agent-path guard. Edit `/api/reason` while the window is running.
Push without the PR19 pass. Anchor a pin on a block's *opening*. Rely on a textual anchor a rename can
defeat. Flip or unset an R20a flag (the guards are unconditional; a flag is not a rollback for them).
Touch `operations/agent-circles-2026-08/d6a/` or any file the byte-identity guard names. Quote a
perimeter count from a document. Read a token from `settings.local.json`.

## 7. Rollback

Per group: `git revert` the group's commit(s) + redeploy. Never a flag. Each route's move is
independent of every other's, so a single route can be reverted alone if a smoke fails.

## 8. Anticipated shape (one group)

| Phase | Estimate |
|---|---|
| Opener + reads + verify | 25 min |
| Design notes + founder confirm | 15 min |
| Edits + pins + mutation proofs (4 routes) | 60–90 min |
| PR19 fleet + fold | 40 min |
| Push, deploy, smokes (founder) | 30 min |
| Records + close | 30 min |

**Forecast:** success = the group's guards sit after the check on every human path, pinned by
block-end anchors that go red under both bypasses, three reviewers satisfied, the founder's smokes
showing a 14-character cry answered with the crisis resource. **The session ends with the guard
values unchanged and the classifier's input still bounded.**

End of prompt.
