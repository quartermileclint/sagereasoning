# Next-session prompt — the standing-runner design session (R8) opens

**Paste this as the task after the standing session opener.** Tier: `governance` — a design
session. It designs; it does not build. No code, schema, flag, credential, or activation is
licensed by this prompt. If the session's own design work concludes something should be built,
that becomes its own separate `code-*` prompt with its own founder election — this session does
not carry that authorization forward by default.

## What this is

The fifth and final step of the mentor's ruled Q7 sequence
(`operations/agent-circles-2026-08/2026-08-29-mentor-ruling-five-instruction-family-verbatim.md`,
Q7, verbatim wins):

> *"Fifth, open the standing-runner design session with the four frames presented together per
> F5's instruction, plus the adversarial review's findings as a named input."*

**All four prior steps are done, in order, each PR19-discharged where the ruling required it:**

1. The five instruction files adopted with verbatim capture (`D-FIVE-INSTRUCTION-FAMILY-RULED-ADOPTED-EXECUTED-2026-08-29`), and the Q11 Prerequisite Criterion entered `manifest.md` as binding governance (§"The Prerequisite Criterion", added 2026-08-29).
2. The seven-probe adversarial review run and PR19-folded (`D-ADVERSARIAL-REVIEW-CYBERNETIC-SEVEN-PROBES-RUN-PR19-FOLDED-2026-08-29`, deliverable `operations/agent-circles-2026-08/2026-08-29-ADVERSARIAL-REVIEW-cybernetic-seven-probes.md`).
3. The ATRF row of the named-input register completed and PR19-folded (`D-ATRF-REGISTER-ROW-COMPLETED-2026-08-29`, register at `operations/primal-substrate-2026-08/00-PRIORITY-INDEX.md` §"Named inputs held for not-yet-opened sessions").
4. The nine-candidate remediation-shape classification run, corrected twice (once by this session's own close-turn reflection, once by independent PR19 review), and discharged (`D-NINE-CANDIDATE-REMEDIATION-SHAPE-CLASSIFIED-2026-08-29`, `D-NINE-CANDIDATE-CLASSIFICATION-TWIN-COUNT-CORRECTED-2026-08-29`, `D-NINE-CANDIDATE-CLASSIFICATION-PR19-REVIEW-RUN-BASE-RATE-CORRECTED-2026-08-29`).

**This prompt licenses step 5 and nothing beyond it.** Read the sources below, don't re-derive
them — every one of them was itself independently reviewed, and re-deriving from summary risks
reintroducing exactly the drift those reviews caught.

## What R8 is designing

Per the register (`00-PRIORITY-INDEX.md` §"Standing-runner design," rows tagged
`**Standing-runner design**`) and the Q7/Q8/Q9 rulings, this session's design surface is:

1. **GS-CYB-1** — the proximity score as an error signal, and the candidate-weighting function.
   **The weights-BLOCKED standing constraint is a gate that PRECEDES examination of this** — do
   not design a weighting mechanism; the two-condition gate that licenses examining one at all is
   itself the open question, and it is not this session's to unlock. (`agent-circles-2026-08/2026-08-24-agent-cybernetic-control-architecture.md` §3.)
2. **GS-CYB-2** — the controlled-system model and the completion-signal return path. **Ruled
   design-with-disclosed-absence (Q8):** design the completion-signal consumption and the update
   rule now, name explicitly that no completion-signal data exists yet (the run produced none; the
   endpoint postdates the run), and phase activation on the standing runner's own first observed
   signals. A design that cannot be activated until data exists is still a design that can be
   made — do not defer the design itself, only its activation.
3. **GS-ATRF-3 as a named dependency, not a reason to re-split** — GS-ATRF-3's return-path design
   is already ruled and its endpoint (`POST/GET /api/practice/completion-signal`) is built dark
   (independently HTTP-verified). **GS-ATRF-3 first, GS-CYB-2 second**, as an ordering within this
   session, not a reason to route GS-CYB-2 elsewhere. Note: whether the `idea_loop_completion_signals`
   migration is actually applied to production is a **named, unresolved contested fact** (two prior
   sessions' records disagree, neither independently re-verifiable from a documents-only session)
   — the design should proceed on the *design* question, which is settled either way, and should
   name the contested apply-status rather than assume either answer.
4. **Q-C2b** — the discriminating-signature design for the completion signal: the causal pattern
   that distinguishes genuine examination from apparent completion. Carries the Q4.3/F-Q43
   precedent explicitly: *"detecting apparent completion is free and worthless; the Q4.3 precedent
   became useful only when narrowed to causal order."* This is distinct from Q-C2a (the
   three-question examination content, already ruled, NOT open) — do not re-open Q-C2a.
5. **The multi-scale operator design and F5's long-horizon question** — both re-anchored (not
   dissolved) to the standing runner's own longitudinal data rather than the bounded validation
   run's 20 cycles. Per Q9: *"do not propose architectures that claim to implement it before the
   standing runner's longitudinal operation has produced the trajectory data that would make such
   a claim honest. Twenty cycles is not that data."* Name these as carried orientation, not as
   something this session's data can discharge.

**Named but explicitly a register-granularity choice, not a ruling** (per the register's own
disclosure at the seven-probe review's row): the review's own Leverage Point Summary contains
three items the register deliberately did *not* split into their own rows — **election-logic
resolution** (#5), **functional-vs-structural novelty** (#6), and **persisted per-cycle proximity
delta + circle attribution** (#7). Read the review's own Leverage Point Summary for these; they are
findable there, and the register's choice not to promote them to independent rows "can be
revisited, not silently assumed" — if this session finds that choice wrong, say so rather than
inheriting it silently.

## The four inputs, presented together (per F5's own instruction — do not read them separately)

1. **F3 — the Anandkumar/neural-control frame**: `operations/agent-circles-2026-08/2026-08-27-mentor-instruction-neural-control-anandkumar-verbatim.md`
2. **F4 — the cybernetics–neural-operator convergence frame**: `operations/agent-circles-2026-08/2026-08-27-mentor-instruction-convergence-cybernetics-neural-operators-verbatim.md`
3. **F5 — the Spinoza/scientia-intuitiva capstone frame** (integrates the four-part frame as one — F5's "four" names frame *components*, not four separate instruction files, per Q6's correction of the C-7 category slip): `operations/agent-circles-2026-08/2026-08-29-mentor-instruction-structural-causal-apprehension-verbatim.md`
4. **The pre-run-research response's surviving item** (subsumed into the F3/F4/F5 row, not a
   separate input — the register explicitly folded it in because it "duplicates F4's
   convergence-instruction content in part"): the multi-scale coherence question, "whether the
   harness's multi-scale structure — cycle, session, longitudinal — produces coherent signals at
   each scale," at `2026-08-29-mentor-response-pre-run-research-verbatim.md`.

**Read F3's standing-rules block with its Q10 annotations, not its original unannotated text** —
each item in that block was annotated at adoption with its authoritative source (PR20, the
name-departures discipline, the per-flag lesson, the phased-not-carve-out principle); the block
itself is a pointer layer, not an independent rule surface. And read F3 with the Q4/Q5 corrections
applied at adoption — its internal GS-ATRF-1 inconsistency, its logos-on/Layer-3 misassignment, and
its "Layers 1/2/3 verified live" overclaim are all corrected in the adopted capture, not in the
original.

## The seven-probe adversarial review's findings (the named input from step 2)

`operations/agent-circles-2026-08/2026-08-29-ADVERSARIAL-REVIEW-cybernetic-seven-probes.md`
(governance tier, documents-only, PR19-folded). Headline, for orientation — **read the full
document, this is not a substitute**:

- The harness most closely resembles a **filtered biased walk** — bounded below by the
  guardrail/novelty gates, not mean-reverting, no restoring force proportional to measured
  deviation, because the completion-signal balancing loop is confirmed absent.
- **Resolution loss** between the guardrail's graded five-value verdict and the runner-side
  election: 4 of h7's 5 wins were resolved by a random tie-break, 0 by out-scoring a contested
  field on proximity — the loop consumes the gate's signal at coarser resolution than it is
  emitted.
- The highest-leverage stabilising change (closing the completion-signal loop) and the
  highest-risk reinforcing change (a weighting function biased by the harness's own score) are **the
  same code path** — which is exactly why the weights-BLOCKED gate precedes examining it (GS-CYB-1
  above).
- Two confirmed structural findings, not deferrals: completion-signal data does not exist;
  per-candidate oikeiosis-circle data does not exist for the historical 20 cycles and cannot be
  backfilled.
- The trust-record stock's degradation is bounded below a measured ceiling for 85.8% of recorded
  consults (3,200) and **unmeasured** for the remaining 14.2% (454 pre-stamp rows) — permanently
  unmeasurable, no ledger can repair it retroactively.

**PR19 already folded 7 constraint-compliance findings and 3 claims-vs-source findings into this
document** — most severely a systematic charitable posture that violated the review's own explicit
instruction not to treat the harness charitably, and a same-session reflexivity failure (the
mentor's own Prerequisite Criterion ruling ranked #1 leverage point with no adverse assessment,
named and removed). Trust the folded document; do not re-derive the review from the raw probes.

## This session's own classification finding (step 4, corrected twice)

`operations/agent-circles-2026-08/2026-08-29-nine-candidate-remediation-shape-classification.md`.
**Read the whole document — it carries two rounds of correction inline (§10, §11), and the
corrections change what should and shouldn't be inherited:**

- The **primary evidence** is §3's within-run divergence control: at least three of the nine
  guardrail-rejected candidates have near-verbatim or materially identical twins elsewhere in the
  same 20-cycle run that scored `deliberate`/`principled`/`sage_like` — independently confirmed a
  genuine outlier by fuzzy-match (0.944 similarity vs <0.25 for every other pair), not an
  excerpting artifact. This points at **low-frequency verdict divergence on near-identical
  inputs**.
- The **base-rate argument (§4) is corrected and weakened, not eliminated** — the first draft
  claimed remediation-shape was "essentially universal" across the 120 candidates and that 8/9
  rejected being remediation-shaped was "indistinguishable from chance." Independent review found
  the true base rate is **60–65%**, not near-universal (all 20 `fifth_circle_weighting` "publish
  the SDK" candidates are verified non-remediation-shaped — a hard floor missed in the first
  draft). At the corrected rate, P(≥8 of 9 by chance) ≈ **0.07–0.12** — a real but mild signal, not
  a decisive one. **Do not inherit the withdrawn "rules out systematic depression" claim.**
- **§5 poses an open successor question this session should engage, not assume an answer to:**
  is the divergence a genuine reproducibility defect in the examination path (Reading A), or is it
  expected variance from a probabilistic Layer-1 extraction call, in which case the real design
  question is what confidence a single `reflexive` verdict carries and whether a floor-class
  verdict should be re-examined before being treated as final (Reading B)? **The data cannot
  distinguish these, and the document explicitly declines to choose.** This is directly relevant to
  GS-CYB-1/GS-CYB-2's design surface — if Reading B is closer to the truth, "closing the
  completion-signal loop" and "improving extraction reproducibility" may be two different problems
  wearing one name.
- **A cheap, decisive experiment is named and deliberately not run** (outside a `governance`
  session's licence, consumes credential quota, writes production billing rows): re-submitting the
  c11 candidate text to `/api/guardrail` several times would settle both the submitted-payload
  assumption §3 rests on *and* the Reading A/B question at once. **Recommended as this session's
  own first evidence-gathering step**, if this session's tier allows it (check at open — if not, an
  explicit founder-elected sub-step, not a silent skip).
- h1/cycle-20's rejection is **not reversed** by any of this — out of scope per the original §6
  ruling, unchanged by either correction round.

## Standing constraints this session inherits and must not silently soften

- **The Q11 Prerequisite Criterion is now binding governance** (`manifest.md`, added 2026-08-29):
  *"a design that produces outputs resembling wisdom without building the prerequisite is not
  serving the purpose — it is bypassing it."* Its engagement condition: **any design proposal in
  this session that claims to produce practitioner-facing outputs — scores, recommendations,
  diagnoses, virtue assessments — must be evaluated against this criterion before adoption.**
  Failing designs are ruled against, not merely noted as suboptimal. This is the first design
  session to operate under it; apply it explicitly and show the application, don't just cite it.
- **Weights-BLOCKED is unchanged.** GS-CYB-1's gate precedes examination of any weighting
  mechanism. No finding in this session may pre-answer it or reason around it.
- **Q1's loop-proposes-never-executes constraint is unchanged.** No design here should propose a
  path from a generated candidate to an action-taking tool or scheduler.
- **Layer 3 naming discipline (Q5b), binding on this and every future record:** never use bare
  "Layer 3" without qualification. "The context-injection layer" or "Layer 3 context injection" for
  what is live on `/api/reason`; "the Layer 3 prose service" or "the per-consumer prose service" for
  what is not live. Get this right from the first sentence that needs it.
- **logos-on/off and practice-on/practice-on-the-skill are both domain-scoped, no rename** — see
  Q5a if either surfaces. Do not re-litigate.
- **The O-C Gate-3 design session is a parallel track with no ordering dependency on this one.**
  Don't wait for it, don't assume its outputs, don't fold its scope in.

## What "done" looks like

- A design (not a build) for GS-CYB-1's examination surface **that respects the weights-BLOCKED
  gate** — i.e., it may design *what would need to be true* for the gate to open, and design the
  measurement/observation apparatus, but does not design or propose the weighting function itself.
- A design for GS-CYB-2's completion-signal consumption and update rule, explicit about the current
  data absence, phased for activation on first real signals.
- Q-C2b's discriminating-signature design, honouring the Q4.3/F-Q43 causal-order precedent.
- Explicit engagement with the Reading A/B question from this session's own classification finding,
  and a stated position on whether it changes anything about GS-CYB-2's design.
- A stated application of the Prerequisite Criterion to every proposal this session makes, not a
  bare citation of it.
- The three un-split Leverage Point Summary items (#5, #6, #7) either explicitly addressed or
  explicitly re-deferred with reasoning — not silently dropped.
- A decision-log entry, PR19-reviewed per this project's standing practice for a
  design-consequential session, recording what was designed, what was deliberately left as
  disclosed absence, and what (if anything) is proposed as a follow-on `code-*` session — with the
  explicit note that this prompt does not pre-authorize that follow-on.

## Standing constraints (process)

- **Concurrency convention:** run `ListAgents` at open; commit path-scoped; never `git add -A`;
  run `git status` before staging (a peer session moved HEAD mid-session on 2026-08-24 — check for
  it, don't assume you're alone).
- **PR19** engages if this session's design has consequences beyond documentation (it will —
  GS-CYB-1/GS-CYB-2 are engine-adjacent design surfaces). Run independent review before treating
  any design finding as final, per the project's standing practice this window (parallel
  independent `Agent` calls when the Workflow opt-in gate is not met, disclosed as such).
- **PR20** — if any mentor consultation is drafted from this session, name the specific existing
  mechanisms the ruling will land on before relaying it; timestamp-check any present-tense fact
  against the decision log first.

## Cross-references

- `operations/agent-circles-2026-08/2026-08-29-mentor-ruling-five-instruction-family-verbatim.md` (Q1, Q2, Q6, Q7, Q8, Q9, Q10, Q11 — the ruling this whole prompt executes step 5 of)
- `operations/agent-circles-2026-08/2026-08-29-ADVERSARIAL-REVIEW-cybernetic-seven-probes.md` (step 2's PR19-folded output)
- `operations/agent-circles-2026-08/2026-08-24-agent-cybernetic-control-architecture.md` (GS-CYB-1/GS-CYB-2's origin document)
- `operations/agent-circles-2026-08/2026-08-29-nine-candidate-remediation-shape-classification.md` (step 4, twice-corrected)
- `operations/primal-substrate-2026-08/00-PRIORITY-INDEX.md` §"Named inputs held for not-yet-opened sessions" (the full register, "Standing-runner design" rows)
- `manifest.md` §"The Prerequisite Criterion" (Q11, binding governance)
- `operations/decision-log.md` — `D-FIVE-INSTRUCTION-FAMILY-RULED-ADOPTED-EXECUTED-2026-08-29`, `D-ADVERSARIAL-REVIEW-CYBERNETIC-SEVEN-PROBES-RUN-PR19-FOLDED-2026-08-29`, `D-ATRF-REGISTER-ROW-COMPLETED-2026-08-29`, `D-NINE-CANDIDATE-REMEDIATION-SHAPE-CLASSIFIED-2026-08-29`, `D-NINE-CANDIDATE-CLASSIFICATION-TWIN-COUNT-CORRECTED-2026-08-29`, `D-NINE-CANDIDATE-CLASSIFICATION-PR19-REVIEW-RUN-BASE-RATE-CORRECTED-2026-08-29`
