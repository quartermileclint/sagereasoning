# CLOSE — the Q-S2 opener collision verified, and the three pre-W2 items prepared

**Session `sagereasoning-b6 [efdaf2]`, 2026-09-13 (evening, from `date` — the authoring prompt was
filename-dated 2026-09-14; the machine clock read **Sun 13 Sep 2026 23:07 AEST** at open, and every
artifact this session wrote is dated from `date`).**
**Opened under** `operations/handoffs/founder/2026-09-14-QS2-opener-collision-and-pre-W2-items-NEXT-SESSION-PROMPT.md`,
beneath the standing opener (Version 2026-09-13, founder-adopted), addendum first.
**Tier: `governance` / documents. AC7 NOT engaged.**

> **This session was NOT W2's first sitting and was not treated as one.** W2's clock has not started.
> **Every record this session produced belongs to the CURRENT window**, whose intake remains open per
> Q2 — expected and correct.

---

## 1. What was done

| Task | Outcome |
|---|---|
| **A — the Q-S2 opener collision** | **The finding is VERIFIED, not refuted.** Relay drafted (**NOT sent**); a conforming W2-only opener drafted; its design notes split into a separate founder-facing file. Independent review run, two substantive findings folded. |
| **B — the D-3 compliance-not-virtue clause** | Sign-off package produced. **Nothing applied.** Seven staged claims verified as holding; **one documentation drift** found; **one sequencing decision** surfaced for the founder. |
| **C — the two `manifest.md` edits** | Both prepared as before/after in one file for one founder sitting. **`manifest.md` is untouched** — verified clean at close. |

---

## 2. Task A — the finding, verified first-hand

**Verified, and it is not a close call on content.** Grepped the opener myself and read Q-S2 verbatim
rather than any summary.

**The opener names both Q-S2 triggers, in its BODY** — not only in its own §H warning:
- **line 298** (the S11 readiness board's part-(2) row) names the domain, its guard/consult split, and
  — decisively — links the unmet part's discharge to *"a **differently-composed window** — now
  specified, approved and ruled (below)"*;
- **line 309** (the part-(2) window section) names the criterion verbatim.

**The operative test is the mentor's own, from the 2026-09-13 ruling question (ii):** *"The distinction
between knowing the instrument exists and knowing what the instrument is watching for is real and it is
the operative one."* The ruling then names exactly what must stay unnamed: *"that andreia specifically
is the domain the post-window assessment will focus on, or that variety across courage-relevant moment
types is the criterion."* The opener supplies both, across one page.

### 2.1 The question the prompt said it does not settle — answered

**Is a *state* table naming a domain the same act as naming the *criterion*?**

**The distinction is real, but this opener does not sit on the safe side of it.** A bare four-domain
count table would be a genuinely hard case and would read as instrument state, which question (ii)
expressly permits. **This row is not that.** It names part (2) as the unmet part, attributes the gap to
one domain, and ties the discharge to the window the sitting is running — and the next section names
the criterion outright. The chain is complete on one page.

**This is a session's reading, not law.** The scope half — whether Q-S2 reaches an opener at all — is
genuinely the mentor's, which is why the relay was drafted regardless.

### 2.2 A wrinkle the previous session did not have

**The carve-out warning is itself a breach.** Header lines 19–24 and addendum §H quote both trigger
phrases while explaining the collision. So *"read the carve-out and skip the part-(2) row"* does not
save the document — **a separate trimmed document is necessary, not merely tidier.**

### 2.3 What supports treating an opener as in scope

The mentor **applied the Q-S2 test to `CLAUDE.md`** — plainly not a "session prompt" in the literal
sense — and passed it **on content**. The same ruling's recording note goes further: *"If `CLAUDE.md`
is ever edited to name andreia or variety as the watched criterion, the ruling's premise changes."*
And the six-question ruling directs that the founder open W2 *"under a **Q-S2-conforming prompt**."*
**Stated as the ground for the reading, not as a ruling.**

---

## 3. Deliverables

| File | State |
|---|---|
| `operations/trust-layer-2026-07/2026-09-13-qs2-opener-collision-RELAY-DRAFT.md` | **DRAFTED, NOT SENT.** One question in two parts (scope; state-vs-criterion), both readings stated, evidence quoted with anchors, three routes. **No position on the breach**; a recommendation on **route** only, marked as one. |
| `operations/handoffs/founder/W2-SESSION-OPENER-qs2-conforming-DRAFT.md` | **DRAFT, not adopted.** Carries tier, constraints, production state, the work designation, the standing holds, the re-derive list, harness context. **Zero occurrences of any forbidden term — grep-verified after every edit.** |
| `operations/handoffs/founder/W2-SESSION-OPENER-qs2-conforming-DESIGN-NOTES.md` | **FOUNDER-FACING, do not paste.** The omissions, their cost, and the review findings. |
| `operations/agent-circles-2026-08/2026-09-13-compliance-not-virtue-clause-SIGNOFF-PACKAGE.md` | Task B. **Nothing applied.** |
| `operations/handoffs/founder/2026-09-13-manifest-edits-FOR-FOUNDER-ACT.md` | Task C. **`manifest.md` untouched.** |

### 3.1 A defect this session introduced and caught in its own work

The **first** cut of the conforming opener carried its omissions table as an in-document §9 — and that
table **quoted the forbidden criterion verbatim while explaining that the document omits it.** A grep
caught it. **That is the same defect the session had just verified in the standing opener, reproduced
one level up in the remedy.** Fixed by splitting the notes into a separate file. **Recorded because
the lesson generalises: an explanation of an omission tends to reconstitute it.**

---

## 4. Independent review — run, and two substantive findings folded

A fresh reviewer was run adversarially on the three Task-A files (leakage-by-inference, completeness,
accuracy, relay neutrality). **Six findings; each verified against source before acting on it.**

**Both substantive findings were real and are folded:**

1. **MEDIUM-HIGH, factual — §3's candidate sentence was INVERTED.** It read *"A third candidate was
   withdrawn by ruling."* Q-M1 withdrew candidate **(b)** — the **second** of the three the
   two-questions ruling lists — while the **third** (the standing-runner track) is the one now
   **PRIMARY**. An ordinal-counting reader would have concluded the designated work was the thing
   dropped, **exactly backward, in the section that tells a sitting what to do.** Fixed: the withdrawn
   candidate is named, not numbered.
2. **MEDIUM, leakage by inference — §9's pointer to the design notes was itself disclosive.** It told a
   sitting that material was deliberately withheld from it and that a document discussing that material
   existed. It named neither trigger, so it does not fail Q-S2's literal test — but announcing targeted
   suppression invites the inference the document exists to prevent. **§9 deleted from the pasted file.**
   **This is §3.1's defect class a second time**, which is why it is recorded rather than quietly fixed.

**Three completeness findings folded:** the buffer's two parsing traps (1-indexing; `GUARD-OUTAGE`
lines carry no `tool=` field) and buffer-over-log precedence — correctness-relevant, since the document
directs that same parsing; a per-track close-pointer index; the one-arc concurrency norm.

**One finding accepted as a caution, not a change:** the tool-mode constraint is the passage closest to
the line, because channel is the axis the composition question turns on. It stays at the instrument
level, ties channel to no domain and no criterion, and `CLAUDE.md` already discloses the property.
**Flagged for the founder rather than silently kept.**

**One NIT folded in the relay:** its closing cross-cutting note leaned toward one reading; a
counter-consideration of comparable weight was added.

**Honest limit: one reviewer, one pass.** It cannot settle whether Q-S2 reaches an opener — that is the
relay's question.

---

## 5. Task B — what holds, the drift, and the decision

**Holds (seven claims, each checked against code or the verbatim, not against the staged file's own
account):** the clause is byte-equal to mentor L7 and to the live constant; the context marker carries
all three L5 statements; `enforcement_outcomes` is the real served field name; the inline half is live
in the composer; *"the fifth-circle item above"* resolves correctly (item 8 of 11; a new item appends
as 12).

**DRIFT-1 (LOW, documentation).** The staged file says the inline half is *"built dark **on branch
`w2-record-honesty`**"*. **It was merged to `main` at `0e4ea4e`.** No consequence for the wording;
it matters so nobody hunts for a branch.

**⚠ THE DECISION, and it is genuinely the founder's.** `enforcement_outcomes` is **structurally absent
from every served payload** while `SUBSTRATE_ENFORCEMENT_RECORD_ENABLED` is unset — which it is —
while `TRUST_RECORD_ENVELOPE` is served on **every** read. So applying §3 as worded publishes a
present-tense description of a field that cannot appear.

**Why that is a live question here and not a nicety:** ADR-013 §8's own amendment history states the
discipline twice (*"a present-tense claim would publish behaviour that does not exist"*; *"the record
cannot simultaneously display a mechanism and disclaim that it exists"*) and states the ordering rule
**four times** (*"never ahead of it"*). **The honest counter-argument is also real:** the sentence is
definitional rather than existential, and **L7 obliges** the record-level clause, so deferring is not
neutral either. Three options are set out; **this session states no preference.**

**One internal inconsistency in the staged file, offered as evidence:** its §5 defers the three doc
surfaces *"once the field can actually appear"* but applies no such test to the envelope, in the same
document.

**Also prepared:** exact insertion points (envelope array lines **58–148**, append after line 147;
ADR §8 from line 101; its `2026-09-XX` placeholder needs a real date), the same-edit rule and its
reason (`S2-37` is strict reference identity and cannot detect a missing item), and **the next free
battery pin — `S2-104`, re-derived by counting.**

---

## 6. Task C — both manifest edits, prepared and unapplied

**D-4** quotes the **ruled** full wording (Q-R11-C1) against the live line 265, with a change-by-change
table. **The minimal-difference alternative is flagged as NOT the ruled wording** — the likeliest
paste error.

**D-8** documents the §AC5 contradiction precisely: the counts are **CORRECT today** (re-derived:
**43 + 2 = 45**), so **the defect is form, not staleness** — and the bolded *"does not hand-enumerate"*
sentence **hand-maintains two counts inside itself**. Three options, no preference between A and B;
**a recommendation on structure only** — pair whichever is chosen with an executable check, since this
section's own history is three consecutive demonstrations that a written instruction does not arrest
the drift. **Option C is a code change and must be its own act** (verified: `GUARD_RE` does **not**
match `r20a-invocation-guard.test.ts`, but it is still code and falls under PR19).

---

## 7. Not done, and why

- **W2's first sitting NOT opened and its prompt NOT authored** — blocked on Task A, as instructed.
- **`manifest.md` NOT edited** (D-4, D-8 — reserved to the founder). **No R18 surface touched** (D-3
  needs the signature first). **No relay sent.**
- **D-6 — `sagereasoning:option-s@v1` NOT revoked or queried.** Needs a production admin JWT; the
  founder's. **⚠ Still the only open item with a live security surface.**
- **D-7 — idle peers not closed.** `ListAgents` at open: **23 peers** (14 `sagereasoning-*`
  interactive, one of them Session R's scratch project). The founder's, through the UI.
- **`option-s/` not opened; Session R's scratch project not opened, read or listed; no Session R or S
  run; nothing written to `~/.sage-gate1/`; no report run at all** (not even `--dry-run`).
- **The peer's `environmental-context.json` left unstaged**, as every session has left it.

---

## 8. Tool-mode and record disclosure

**This session authored entirely through Bash heredocs**, under the harness's standing auto-mode
instruction. **Consequence, disclosed rather than adjusted for: its records are guard-class, not
consult-class.** The buffer read **792 lines at open** (from 646 at the previous session's close — it
is live and append-only). **The choice followed the harness instruction, not the counter.**

**The at-action guardrail fired repeatedly and the elicitation was answered genuinely each time**,
including on a read-only grep the engine read as *"contrary"* with no kathekon factors extracted — the
measured false-positive class, behaving as documented. **Two answers are worth carrying forward:** on
the conforming opener, a prior preference for a *long* document over a minimal one, formed before
examining whether length serves a sitting; and on the manifest file, a prior preference for the
executable-check option, handled by recommending it *alongside* rather than *instead of* the founder's
A/B choice, and by declining to choose between A and B at all.

---

## 9. Verified at close

| Check | Result |
|---|---|
| Byte-identity guard battery | **run — 250 passed, 0 failed** |
| SHA pins ×3 | **unchanged** — `60cefedb…` / `fa8895ec…` / `db86fccb…` |
| `GUARD_RE` | re-read; **no matching file touched** — every file written is under `operations/` |
| R20a perimeter | **43 + 2 = 45**, counted from the array bodies |
| Agent-card extensions | **26**, from the file |
| Buffer | **792 lines**; read-only, never written |
| `manifest.md` | **clean** |
| `git status` | whole; the peer's `environmental-context.json` and two peer prompt files left untouched |
| Conforming opener | **0 occurrences** of any forbidden term |

---

## 10. The founder's next steps

1. **Decide Task A's route** — use the conforming opener now, send the relay, or rule the carve-out
   unnecessary. **The relay recommends route 1 *and* 2, on the asymmetry the mentor itself applied to
   the W2 clock:** opening W2 under a breaching document is **not recoverable**; using a shorter
   document unnecessarily **is**.
2. **D-3** — decide the §3.4 sequencing question, then sign. Application is its own session.
3. **D-4 + D-8** — one sitting, from the prepared file.
4. **D-6** — **revoke `sagereasoning:option-s@v1`.** Still the only live security surface.
5. **D-7** — close idle peers **before** W2 opens; a stale peer waking inside W2 adds records nobody
   decided to add.
6. **Then D-9** — open W2's first sitting. **Your act of opening is what makes it the first**, not the
   prompt's content.

**D2 remains blocked. The S11 flip remains REFUSED. Weights remain BLOCKED. The 0h call remains the
founder's.**

---

## 11. ADDENDUM — the mentor ruled, and the mandatory second pass found what the first did not

**Appended after §10, same session. The founder relayed the brief and returned the ruling; the machine
clock rolled to 2026-09-14 00:01 AEST during this addendum's authoring. The work and the ruling are
dated 2026-09-13.** Verbatim, canonical:
`operations/trust-layer-2026-07/2026-09-13-mentor-ruling-qs2-opener-collision-four-questions-verbatim.md`;
entry `D-MENTOR-RULING-QS2-OPENER-COLLISION-FOUR-QUESTIONS-ADOPTED-SECOND-PASS-FOLDED-2026-09-13`.

**All four ruled.** Q-A1 — **Q-S2 reaches the standing opener.** Q-A2 — the state-vs-criterion
distinction is real, **a bare count table would be permitted**, this purposive row is not. Q-A3 — the
discipline governs **what a sitting is told**; the conforming draft *"is the correct path forward."*
Q-B1 — **NO**: the record-level clause **waits for activation** and must first be **recast in the
future tense**; the *"definitional not existential"* argument is *"noted and rejected."*

### 11.1 The second pass was ruled MANDATORY, ran, and changes §3's verdict

**§3.1 above recorded one defect this session caught in its own remedy. That count was wrong — it is
five.** The ruling made a second pass mandatory and specified its form; two reviewers ran.

**The blind inference test passed.** Given the document alone, the reviewer could not name the domain
or the criterion, said so rather than guessing, disclosed that `CLAUDE.md` was in its context and that
it deliberately did not use it, and could not say how to shape work toward the criterion.

**The implication hunt found a HIGH that inverts §9's "verified at close" claim.** The opener's §2
close-pointer index cited the pre-flip report **by exact subsection** and the register §F + change-log
tail — **29 and 19 trigger-term occurrences, verified first-hand.** **A document this close certified
as "0 occurrences of any forbidden term" was routing a sitting, by precise citation, into the densest
statement of the criterion in the repository.** *"The pointer is the leak."*

**And that index was added to fix a completeness finding from the FIRST pass.** Remedying completeness
created the leak.

**Five instances of one class, in one document:** the omissions table → the pointer to it → the
prohibition (which proved the criterion was virtue-domain-shaped, narrowing it to four) → the
provenance banner (which named the standing opener by filename) → the reading index. **All five fixed;
the document re-verified at 0 and every cited file re-audited.**

### 11.2 Where trimming was stopped — deliberately, and it should not be re-trimmed

`CLAUDE.md` (27) and `operations/decision-log.md` (62) are still cited. **Left on purpose.**
`CLAUDE.md` is expressly ruled within bounds; the decision log is mandatory reading every session does
anyway; and Q-A3 forecloses repository-wide remediation. **A pointer leaks when it directs a sitting to
a specific location whose content states the criterion — not when it names standard reading.**

### 11.3 What §7's "not done" list gains

- **The standing opener was ANNOTATED** (annotate-only, dated) where the ruling made its own text false
  — the carve-out and addendum §H both read *"not resolved."* **Flagged prominently because it edits a
  founder-adopted document**; nothing else in it was touched.
- **The D-3 package is banner-superseded** at §3.4/§7, with the **recast future-tense wording drafted
  at a new §8 and unapplied.**
- **A founder option opened by Q-A2:** a bare per-domain count table is **permitted**. The conforming
  opener carries none — **stricter than required**. Not taken here, on the judgement that a document
  which had just leaked at five levels was not the place to add domain figures the same day.
- **One founder item in the ruling rests on a stale premise:** F-2 (*"adopt the corrected version"*) is
  **already discharged** — recorded, not acted on.

### 11.4 Re-verified at this addendum's close

Guard battery **run**; three SHA pins; conforming opener **0 occurrences**; every file it cites
re-audited; `manifest.md` clean; no `GUARD_RE` file touched. **One harness note, disclosed:** a Gate-1
pre-decision examination returned *"no assessment in response"* on one subagent spawn and that task
proceeded unframed — recorded as an outage, not treated as a pass.

**W2's clock has not started. D2 remains blocked. The S11 flip remains REFUSED. Weights remain BLOCKED.
The 0h call remains the founder's.**
