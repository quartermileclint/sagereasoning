# Next-Session Prompt — Remaining Principles: view from above (#9) + fate-acceptance reframe (#13)

**For the founder. Paste as the first message of a fresh session.** This is the **next window-safe generative build** after the two split human halves (#7-human premeditatio + #10-human reserve-clause, now Live). It builds **one net-new human-practitioner tool** — the "view from above" calibration tool (#9) — with the **fate-acceptance reframe (#13) shipped inside it as an integrated component** (the plan pairs them; #9 anchors #13). Like its predecessors it never touches `/api/reason` or the substrate, so it is safe to build and deploy whether or not the 7-day observation window is still running.

**Open under:** `operations/handoffs/founder/STANDING-SESSION-OPENER-grounded-foundations.md` (Parts A–F).
**Stream:** founder. **Tier:** `code-elevated` + **`schema`** (a net-new page + `/api/mentor/*` route + a new user-scoped table → a founder-walked migration, PR17/AC7). Not `code-critical` unless the R20a decision below is taken (it should not be, this session).
**Governing plan:** `operations/trust-layer-2026-07/2026-07-13-remaining-stoic-principles-build-plan.md` — §3 (rows #9, #13), §4 (Bucket A, items 3–4 + the per-item guardrails), §5 (deploy posture), §8 (the sequence). **Binding deploy verdict:** mentor **D6** (`…decision-points-verdict-verbatim.md`) — human-surface tools ship during the window as standalone, boundary-verified-before PRs.
**Governing context:** if the **7-day false-hold observation window** is still running (`GATE1_FALSE_HOLD_CAPTURE=true`, `SUBSTRATE_TRUST_CORE_ENABLED` MEASURE), the byte-identity deploy gate below is mandatory. If it has ended, keep the byte-identity check as hygiene. **S11 (ENFORCE) remains DEFERRED, readiness-gated; weights BLOCKED.**

## Why this session exists

The mentor's remaining-principles survey named the **view from above** (#9 — the Stoic cosmic/temporal zoom-out that right-sizes a concern) as a net-new human-surface calibration tool, framed as a **"Zone-2 grief/catastrophising calibration"** exercise, and named **fate/providence → a fate-acceptance reframe** (#13) as a component that ships *inside* #9. Together they give the practitioner a way to meet grief and catastrophising by widening perspective and accepting what is not theirs to control — the counterpart to premeditatio (preparing for adversity) and the reserve clause (holding outcomes lightly).

**First task at open — ground the design in the mentor's own words.** The plan's one-line descriptions are a starting hypothesis, NOT the design. Before building, read the survey's principle-9 (view from above) and principle-13 (fate/providence) sections in `inbox/Mentor answer to remaining principles question.rtf` and confirm: (a) exactly what the mentor means by the "Zone-2" framing and what a "calibration" tool should DO (a guided reflection? a before/after right-sizing? a set of perspective prompts?); (b) how the fate-acceptance reframe integrates as a step within #9 rather than a separate page. Design from that, not from this paragraph.

## Scope — one net-new tool, #13 folded in as a component

A net-new practitioner page (e.g. `website/src/app/<name>/page.tsx` + `<name>/layout.tsx`) + a net-new `website/src/app/api/mentor/<name>/route.ts` + a new user-scoped table, modelled on the **freshest template — the just-built `/hupexairesis` reserve-clause tool** (route + page + layout + migration + boundary test). The likely shape (confirm against the mentor text): the practitioner names a concern that feels overwhelming → applies the view-from-above (widen to the cosmic / temporal / whole-of-humanity scale) → the fate-acceptance reframe (this event is part of the whole; what does accepting what-is-not-mine look like) → a re-calibrated reading of the concern, stored as a record. A quality/insight gate is optional (the premeditatio/hupexairesis gates are the precedent — LLM, `MODEL_FAST`, cached, **fail-open**).

**Route/table naming is a scoping decision** — the family carries both Latin/Greek names (`/premeditatio`, `/oikeiosis`, `/hupexairesis`) and plain English; "view from above" has no clean single Greek term, so `/view-from-above` (English) is the natural default, but the founder may prefer otherwise. Decide at open.

## Hard constraints (window-safe — identical to the #7/#10 build; from plan §4/§5 + D6)

1. **No instrument touch.** The new page/route/table must not edit or import `/api/reason`, the substrate libs (`layer1-extractor`, `layer2-mechanisms`/`computeProximity`, `kathekon-engagement`, `trust-core/*`), the Gate-1 hooks, or the reflect/calling routes/`sage-reflect`. New code lives only under `website/src/app/<name>`, `website/src/app/api/mentor/<name>`, a new `website/supabase-<name>-migration.sql`, and the small additive data-rights + welcome-link edits (below). Reuse `@/lib/security`, `@/lib/model-config`, `@/lib/sage-reason-engine` (`getClient` — verified NOT to drag in the substrate engine), `@/lib/supabase`, `@/lib/auth-fetch` — these are the safe shared libs the family already uses.
2. **Clean file boundary — battery-verified BEFORE shipping, not after** (D6; the S10-ENV-1 lesson). Copy `website/src/app/api/mentor/hupexairesis/__tests__/human-practitioner-boundary.test.ts` for the new tool (self-contained; guard **route + page + layout**). It asserts none of the new files import `@/lib/substrate`, `trust-core`, `kathekon-engagement`, `layer1-extractor`, `layer2-mechanisms`, `translation-sandwich`, `sage-reflect`, `stoic-brain`, the Gate-1 hooks, or `assessKathekon`. Run it (`npx tsx …`) and confirm green before the deploy.
3. **Deploy gate + ORDER (founder-walked, PR17/AC7).** **Migration BEFORE code** — the new route reads/writes the new table, so the table must exist on Production before the route serves traffic (the `build-dark-migrate-later-breaks-writes` class). Before deploy, verify `/api/reason` + the frozen capture set are byte-unchanged (`git status --short | grep -iE "api/reason|translation-sandwich|/substrate/|trust-core|kathekon-engagement|false-hold|harness/gate1"` → NONE) and `npm run build` ✓ with the new routes registered.
4. **Data-rights for the new PII table** (R17c/R17i; the S1/S9b + reserve-clause precedent). Wire the new table into the three data-rights surfaces — `website/src/lib/user-data-gathering.ts` (access), `website/src/app/api/user/export/route.ts` (export), `website/src/app/api/user/delete/route.ts` (delete + it rides `...tablesToDelete` into the compliance log) — as one additive, missing-table-benign line each. **Note: the `/api/user/delete` edit is 0d-ii Critical** (data deletion) — the one Critical-classified edit; walk it as such.
5. **R20a perimeter — match precedent, do NOT expand.** The `/api/mentor/*` practitioner tools sit OUTSIDE the distress perimeter (premeditatio/oikeiosis/passion-log/hupexairesis all do), and importing the substrate R20a gate would itself breach constraint 1. Match that (no distress check). **Surface — do not decide — the standing founder question** of whether the family should join the perimeter (Critical/AC5 if ever pursued). *Judgement note:* view-from-above explicitly invites grief/catastrophising input, so weigh whether this specific tool tips the perimeter call — but treat any perimeter change as its own Critical session, never a same-session add.
6. The **agent-instrument principles are OUT of scope** — #9/#13 have no agent half; they are wholly human-surface. Do not touch the enforce predicate, the reflect engine, or any flag.

## Procedure

1. **Reads** — the standing opener; plan §3(#9/#13)/§4/§5/§8 + §4 per-item guardrails; mentor D6; the survey's principle-9 + principle-13 sections (`inbox/Mentor answer to remaining principles question.rtf`); the freshest templates (`hupexairesis` route+page+layout+migration+boundary test; `premeditatio` for the enhance-vs-net-new contrast; `oikeiosis` as the leanest `/api/mentor` route). Confirm the frozen-capture-set hash is still pinned if the window is running.
2. **Design** — ground #9's shape + #13's integration in the mentor text; decide the route/table name; scope the fields + the (optional, fail-open) gate. Surface the design briefly before building.
3. **Build** — migration (new user-scoped table: uuid PK, `user_id` FK→auth.users ON DELETE CASCADE, per-verb RLS `auth.uid()=user_id` + service-role FOR ALL, `idx_<t>_user_created`, idempotent + reversible) → route (`requireAuth` + service-role client + `.eq('user_id', userId)`; `RATE_LIMITS.scoring`; `TEXT_LIMITS.medium`; optional fail-open gate; consider adding the revise/edit-in-place `PATCH` from the #7/#10 PR-3 pattern up front) → page (+ layout with `SupportFooter`) → welcome `MORE_TO_EXPLORE` link → data-rights (constraint 4) → the boundary test.
4. **Verify** — the boundary test (green), `tsc --noEmit` (0), `npm run build` (✓, new routes registered), the byte-identity git-guard (NONE). Then an **adversarial review** (a Workflow: dimensions → find → verify each; if it dies on token expiry/limit, complete first-hand per the §4 precedent). Fold any confirmed findings.
5. **Founder-walked deploy** — migration TEST→prod (+ §VERIFY) → push the code → live smoke. **Records + close** — a decision-log entry + a session close (lean form) + name the next window-safe successor.

## What this session does NOT do

It does not touch the instrument, the enforce predicate, or the reflect engine. It builds no agent half (there is none for #9/#13). It does not flip any enforce flag — **S11 stays DEFERRED**. It does not build the other window-safe tools; it does not do the D2 justice-arm narrowing (that is `code-elevated`, report-side, and precedes the return-with-record session).

## Rollback

`git revert` the tool's PR (standalone, additive human surface). The new table is `DROP TABLE`-able (founder-walked). `/api/reason` and the frozen capture set are untouched, so the observation record is unaffected.

## Forecast

Success = the view-from-above calibration tool (with the fate-acceptance reframe folded in) live and verified measurement-neutral (byte-identity + clean file boundary proven *before* shipping), the window still clean, the daily-practice surface deeper by one tool. **Queued behind it (window-safe, founder's tempo):** #8 morning-preparation (net-new); #6/#15 oikeiosis circle-extension + cosmopolitan check (enhance the live `/oikeiosis`); #14 sage-compass (net-new); #12 logos teaching module (net-new static page, read-only `stoic-brain.ts` import only). **Then, before the return-with-record session:** the D2 justice-arm narrowing. **The agent halves + the kathekon pair wait for the flip.** *Hold the window clean. Build as capacity allows. The assent will be examined then.*

End of prompt.
