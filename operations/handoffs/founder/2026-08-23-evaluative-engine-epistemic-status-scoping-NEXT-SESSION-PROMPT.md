# Next-Session Prompt — the Evaluative Engine Epistemic Status Scoping Session (scoping only; output = a FOR-MENTOR-REVIEW document)

> **✅ SPENT 2026-08-23** — the session opened with the gate verified discharged, produced the
> scoping document, ran + folded the three-reviewer adversarial pass, and — same day — the mentor's
> rulings returned and were adopted on the founder's "proceed"
> (`operations/agent-circles-2026-08/2026-08-23-mentor-rulings-evaluative-engine-epistemic-status-verbatim.md`;
> `D-MENTOR-RULINGS-EVALUATIVE-ENGINE-EPISTEMIC-STATUS-ADOPTED-EXECUTED-2026-08-23`). Do not
> re-paste.

> **✅ GATE DISCHARGED 2026-08-23, same day as drafting** — the Q-A1 ruling exists:
> `operations/agent-circles-2026-08/2026-08-23-mentor-rulings-atrf-sixteen-questions-verbatim.md`
> (Q-A1: one framework, two orthogonal axes; entry `{provenance, credence}` uniform in form with
> per-proposition-type honest constraints; *"This ruling un-gates the Evaluative Engine Epistemic
> Status Scoping Session"*). **This prompt may now be pasted**; Step 0.1 still runs as written and
> will find the ruling. **Two named inputs added by the same rulings:** (1) per **Q-A2**, *"credence
> assignment belongs to entry-type machinery outside [the ATRF's] scope — the Evaluative Engine
> Epistemic Status Scoping Session ... is the correct home for credence assignment questions"* —
> the credence half arrives here as owned subject matter, not merely context; (2) per **Q-A1**, the
> ruled entry structure and its per-type honest-constraint pattern (impressions / candidate ideas /
> blast-radius assessments / completion signals) is the model any engine-output status design
> composes with.

**⛔ HARD GATE — DO NOT OPEN UNTIL THE Q-A1 RULING EXISTS.** This session is gated, by the mentor's
2026-08-21 Q3 ruling (verbatim wins:
`operations/agent-circles-2026-08/2026-08-21-mentor-rulings-five-questions-examination-session-verbatim.md`),
on **the ATRF scoping session's ruling on the two-vocabulary question** — *"it cannot rule on
per-output epistemic status assignment before that."* That question is **Q-A1** of
`operations/agent-circles-2026-08/2026-08-23-ATRF-QUESTIONS-FOR-RULING.md`, issued 2026-08-23 and
**not yet ruled at this prompt's drafting**. The mentor's 2026-08-23 scope-confirmation response
explicitly rules no GS-ATRF question, so nothing as of drafting discharges this gate. **Step 0.1
verifies the gate; if the Q-A1 ruling is not in the repo, STOP and close honestly — do not proceed
on the founder's paste alone.** (The 2026-08-21 Q5 ruling for the parallel case is the model: a gate
is discharged by a recorded ruling, not by momentum.)

**Paste as the first message of a new session, in the `sagereasoning` repo root — after the gate is
discharged.**

**Stream:** founder.
**Tier:** `governance` — documents only. Standard risk under 0d-ii. No build, no schema, no flag, no
credential, no migration, no live op. AC7 not engaged. (If the session finds itself wanting to touch
code, stop — this session scopes, it does not build; any eventual implementation is separately
scoped, PR19-reviewed `code-critical` work on the measured surfaces.)
**Governing frame:** `/adopted/standing-protocol-cache.md`.
**Predecessor closes:** `operations/handoffs/founder/2026-08-23-ATRF-scoping-session-CLOSE.md`
(through its Addendum) and whatever is newest in `operations/handoffs/founder/` at open — **check,
do not assume**; multiple sessions were active in this working tree when this prompt was drafted.

**Session identity, fixed by ruling (Q3, 2026-08-21):** the session is named the **Evaluative Engine
Epistemic Status Scoping Session**. It is founder-elected, **not gated on any Q11 item** (the
evaluative engine is the foundational layer the IDEA loop runs on top of — routing it into a Q11
session was ruled *"a category error"*), and parallel to the standing-runner design session. Its
subject matter is **the evaluative engine's epistemic status layer** — the epistemic status of the
ENGINE's own outputs. It is NOT the ATRF scoping session: harness-side status (GS-ATRF-4, the
blast-radius vocabulary, §(c-bis)) belongs there and is not this session's to consume or re-open.

---

## Why this session matters

The engine already implements every element of an epistemic-status account — explicit unknown
values, observation anchoring, declared-uncertainty channels, withheld classifications, disclosed
conservative defaults, per-mechanism quality scores, verdict provenance, surface envelopes — but
**heterogeneously, in per-mechanism vocabularies at three grains** (per-field corroboration
findings; per-signal floors and bases; per-surface envelopes). Direction 3 of the engine-evolution
examination was found **WELL-GROUNDED as classification-and-disclosure precision**: the open design
question is whether one formal status per consequential output — a fourth, finer grain — is worth
its cost against that existing machinery, and what the two quiet confidence-exceeds-basis sites
should say. This session prepares those questions for ruling. **The output of THIS session is not a
ruling and not a final scope** — it is a scoping document the founder takes to the mentor for
feedback; the scope is finalised only after that feedback returns (the ATRF pattern, mentor-praised
2026-08-23).

---

## Step 0 — Gate verification, then re-ground in the CURRENT project state

1. **⛔ GATE CHECK FIRST.** Locate the mentor's ruling on Q-A1 (the two-vocabulary structural
   question) in the repo — expected as a verbatim record in `operations/agent-circles-2026-08/`
   answering `2026-08-23-ATRF-QUESTIONS-FOR-RULING.md`, plus its decision-log adoption entry. Read
   it in full. **If it does not exist, STOP: report the gate undischarged, close honestly, and mark
   this prompt still standing (not spent).** If it exists, restate its answer at open — one
   framework with two axes (and the complete entry structure, and whether uniform across proposition
   types) or two frameworks (and what governs their relationship) — because every question this
   session scopes sits downstream of it.
2. **Concurrent-session check:** run `ListAgents` before any file-edit intent; if peers are active,
   coordinate before writing and sequence any decision-log append explicitly (the 2026-08-22/23
   discipline).
3. **Check HEAD and the window since.** This prompt was drafted 2026-08-23 at HEAD `6dcbe09` with
   the ATRF-stream files still uncommitted. Run `git log` since whatever commit carried them and
   read the decision-log tail. In particular: whether the ATRF session's OTHER rulings (Q-A2…Q-F4)
   have returned and been executed — several bear on this session's material (Q-A1's
   uniformity-across-proposition-types answer; Q-C4's completion-signal statuses) — and whether
   anything has touched the engine files this session cites.
4. **Re-derive every mechanism fact from source.** Part B below is a **starting hypothesis, not a
   fact list** — every line anchor was verified 2026-08-23 and drifts (this stream has caught
   citation drift in every recent session). Re-verify each before it enters the scoping document;
   PR20 (both 2026-08-19 amendments) binds the document directly.
5. Confirm at open: tier (`governance`); hold-point (P0 0h active; the founder's 2026-08-22
   direction — all current tasks before any 0h assessment); model + effort stated; status
   vocabulary; weights BLOCKED; the Q1 hard constraint untouched; ADR-012 (the practice is a
   measurement instrument — a status layer discloses measurement limits, it does not enforce).

## Part A — Tier-2 reading (in this order; verbatim records win over every summary)

1. The Q-A1 ruling record (Step 0.1) — in full, plus any sibling ATRF rulings that landed with it.
2. `operations/agent-circles-2026-08/2026-08-21-mentor-rulings-five-questions-examination-session-verbatim.md`
   — Q2 (the two vocabularies, orthogonal axes) and Q3 (this session's charter: name, election,
   primary input, gate).
3. `operations/agent-circles-2026-08/2026-08-22-DESIGN-EXAMINATION-deterministic-engine-evolution-four-directions.md`
   — §0 (what "the engine" precisely is: Layer 1 inference sets the epistemic ceiling; Layer 2 is
   deterministic computation over it; the signature attests the computation, not the extraction's
   truth); **§3 FULL** (the primary input — §3.1 the two vocabularies; §3.2 the per-output
   inventory; §3.3 the precision-vs-structural-revision split); §5 (the D3 map row — the existing
   three-grain status machinery); §6.9 (the far-downstream trust-record question — named, not
   inherited); §7 (Direction 3's routing paragraph — what the scoped question would need to
   establish).
4. `operations/agent-circles-2026-08/2026-08-19-mentor-ruling-gsatrf4-epistemic-status-verbatim.md`
   — the ruled provenance vocabulary and its governing rule ("confidence of an explanation must
   never exceed its evidential basis"); read for the vocabulary this session inherits, not to
   re-open the harness-side routing.
5. `operations/agent-circles-2026-08/2026-08-23-ATRF-SCOPING-DOCUMENT-FOR-MENTOR-REVIEW.md` — §2.4
   and §10 only (how the vocabulary question was framed and finalised; the boundary: that session's
   inputs are not this one's to consume).
6. `adopted/adr/2026-06-24-sage-practice-measurement-instrument-reframe.md` (ADR-012) — the frame
   any disclosure design must keep.
7. Code, re-verified at open (anchors as of 2026-08-23): `website/src/lib/translation-sandwich/layer2-mechanisms.ts`
   — quiet site #1 (:1271-1274), quiet site #2 (:2903-2908 with :1538-1548 context), the disclosed
   ceilings (:1566-1569, :1695-1700), the corroboration conditional (:2808-2818);
   `website/src/lib/translation-sandwich/corroboration-check.ts:15-22` (the A2 structural residual);
   the R18 envelopes' `does_not_attest` surfaces (`llms.txt` / `agent-card.json` — surface-level
   disclosure today, field-level is the question).

## Part B — The inherited-inputs register (starting hypothesis; re-derive per Step 0.4)

The scoping document must disposition EVERY item — in scope, out of scope with named destination, or
deferred with named condition. Silent omission is the failure mode the register prevents.

1. **The Q-A1 ruling** — this session APPLIES it, never re-opens it. It determines whether a
   per-output status entry carries one structure (two axes) or two related frameworks, and whether
   the structure is uniform across proposition types. Every shape question below inherits its
   answer.
2. **The §3.2 per-output inventory (the ruled PRIMARY input).** Per-output provenance classes as
   implemented: verbatim `evidence` spans (observation); Layer-1 classifications
   (observation-anchored inference, with the `ambiguity_notes` declared-uncertainty channel); the
   two licensed structured judgements (bounded inference); the explicit refusal states
   (`sub_species: null`, `is_kathekon: null`, argued `indeterminate`, `single_snapshot` —
   unknown-expressed); the disclosed conservative defaults; Layer-2 computed fields (computation
   over inference — ceiling set by Layer 1); `proximity_floors.basis` and `stage_scores` (existing
   status machinery); the corroboration report (the one observation-anchored checking layer —
   conditional, not universal); Tier-1/2/3 refusals; the reserved `*_inferred` motivation values;
   the doctrinal-prior grades (assumption, **undisclosed as such**); the legacy `obligation_met`
   scan; the defaulted causal stage.
3. **The two quiet sites (the disclosure-wording question):** #1 — the kathekon zero-factor
   justification (*"No kathekon factors detected; action is contrary to appropriate action."*) — a
   substantive negative claim derived solely from absence of extracted factors, presented
   assertorically; #2 — `ruling_faculty_state` deliberation still fed by the "No circles engaged"
   filler proxy (the D4 fix was deliberately scoped to proximity only; already a named follow-up).
   The eventual rulings owe these two their labels — the wording, and where it rides.
4. **The three disclosed confidence-exceeds-basis routes** (lying-met; lying
   `examined_before_acting` — the LOCUS-2 ceiling, weights-tier gated; the A2 self-report-omission
   structural class) — inherited as DISCLOSED ceilings the status layer must represent honestly,
   never as things this session closes. Plus the general condition: inference fields ride inside a
   *signed* assessment whose signature attests the computation, not the extraction's truth —
   disclosed at surface level (R18 `does_not_attest`), not at field level.
5. **The core cost question (from examination §7):** which outputs count as consequential; whether
   one uniform field is worth its cost against the existing three-grain machinery; the disclosure
   wording for the two quiet sites.
6. **The §3.3 boundary, kept:** **status-as-disclosure is the finding** (additive fields, the
   record-and-floor pattern, no verdict changes); **status-as-gate is structural revision, outside
   the finding and unexamined** (the engine already gates on specific unknowns in bounded calibrated
   places; a general gate carries the ADR-010 §4 over-strictness class). The scoping document may
   name the gating variant as out of scope or propose it as its own future question — it must not
   fold it in.
7. **The measured-surface implementation constraint, carried without pre-deciding:**
   `layer1-extractor.ts` / `layer2-mechanisms.ts` are in the `/api/reason` and `/api/guardrail`
   import graphs — any eventual wiring is PR19-reviewed `code-critical` work under the byte-identity
   disciplines. This session is documents-only and licenses none of it.
8. **Boundary items to disposition explicitly:** the ATRF/harness side (GS-ATRF-4 assignment,
   blast-radius vocabulary, completion-signal statuses — the ATRF session's; if its Q-C4 ruling has
   returned, cite it as settled input, do not re-scope it); §6.9 (whether inquiry-discipline outputs
   feed the public trust record — far downstream, not inherited); the D4-scope follow-up behind
   quiet site #2 (a code follow-up with its own standing, not absorbed here); weights BLOCKED
   throughout (a status field must never become a training-reward surface).

## Part C — What this session produces

**One deliverable:**
`operations/agent-circles-2026-08/<session-date>-EVALUATIVE-ENGINE-EPISTEMIC-STATUS-SCOPING-DOCUMENT-FOR-MENTOR-REVIEW.md`

Requirements:
- **Status header:** `FOR MENTOR REVIEW — NOT FINAL. This scoping is not adopted until the mentor's
  feedback returns and the founder finalises it. No build, route, flag, credential, or schema is
  licensed by this document.`
- **PR20 throughout:** every present-tense mechanism fact carries a file:line or record citation
  verified in-session (not inherited from this prompt); unverifiable facts marked, not asserted.
- **Content:** (a) the proposed scope, structured around the register above with every item
  dispositioned; (b) the proposed question set for the mentor, each question self-contained; (c)
  proposed sequencing (which questions gate which — the Q-A1 application likely first); (d) named
  open questions the scoping could not settle; (e) boundary dispositions stated as proposals; (f) a
  cross-reference list.
- **Epistemic discipline:** per-item readiness assessed honestly (the anti-overcorrection ruling) —
  the inventory is done work (READY); the uniform-field cost question is a genuine open design
  question; the quiet-site wording is small and ready.
- An **independent adversarial review pass over the document's claims before handover** —
  proportionate (the ATRF session's three-reviewer pattern), not skipped.

**Close discipline:** lean decision-log entry (`governance`/Standard) + lean session close naming
the deliverable; commit only the session's own files, sequenced with any active peers. This prompt
is SPENT once the session opens **with the gate discharged** — if the gate check fails, it remains
standing.

## What does not move in this session

- No epistemic-status question is RESOLVED here — this session prepares them; the mentor rules.
- The engine's code: untouched (documents only; the byte-identity guard posture unchanged).
- The ATRF session's material, the standing-runner design session, the puzzle-taxonomy entry types,
  the Consciousness and Continuity Obligation: all untouched.
- The status-as-gate variant: not folded in (§3.3's boundary).
- The 0h call; weights BLOCKED; the Q1 hard constraint; the R20a perimeter; auth; `manifest.md`.

---

*End of prompt. Success condition: the gate verified discharged, then a FOR-MENTOR-REVIEW scoping
document grounded in the repo's state at open, with every inherited item dispositioned, every
mechanism fact freshly verified, the Q-A1 ruling applied rather than re-opened, and nothing scoped
into existence that the rulings have not licensed.*
