# Next-Session Prompt — ARC2 Session 1: carried work, auto-mode bundle

**Stream:** founder.
**Tier:** mixed Standard–Elevated under 0d-ii; no Critical surface in this session (no auth,
encryption, R20a, credential, or deployment-flag-activation touched). Lean + Elevated-additions
template form applies (per the standing cache) — not the full Critical templates.
**Governing frame:** `/adopted/standing-protocol-cache.md`.
**Predecessor context:** this arc opened from the 2026-08-01 Fable-5 regrounding audit
(`D-FABLE5-REGROUNDING-AUDIT-SESSIONS-2026-07-26-TO-30-2026-08-01`, decision-log line ~15856),
which surfaced six carried items that had been dropped from prior closes with no recorded
discharge. Scoped into a three-session arc during the runner-scoping session's follow-on work
(2026-08-10), running **in parallel with** — and independent of — the IDEA-loop bounded validation
run's own three-mode standing prompt
(`operations/handoffs/founder/2026-08-10-idea-loop-parallel-window-NEXT-SESSION-PROMPT.md`).
**This is ARC2 — a separate, unrelated thread.** Nothing in ARC2 touches any IDEA-loop surface
(the three flags, the `idea_loop_*` tables, the runner credential, or the `watching`/`fresh`
routes); the two arcs share a repo but not a single file.

---

## Why this session is shaped the way it is

Six carried items were identified; two need live founder action this AI cannot substitute for
(a credential revoke needing your admin JWT; browser-based auth smokes needing a real session).
The other four are genuinely executable by an AI agent session end-to-end — but they are not all
the same risk class, so they are **not** all bundled with the one item that is (a framework-wide
dependency upgrade). This session is the four same-risk-class items; the dependency upgrade and
the founder-only items are named in full below so nothing about them is lost, but they are **not**
this session's work.

**The full ARC2 shape, for reference at any point in the arc:**

| Session | Contents | Status |
|---|---|---|
| **ARC2 Session 1 (this prompt)** | PROTO-1 decision → `/limitations`+`/welcome` factual fix → brand-image identification + wiring → observability retention sweep (build) → process-adoption decisions | Next |
| **ARC2 Session 2** | Next.js version assessment + upgrade, isolated for focused regression verification | Queued — full spec in §"ARC2 Session 2" below |
| **ARC2 Session 3** | CRED-1 (revoke `ae2-smoke*` credentials) + the four AUTH post-deploy smokes, bundled — founder-performed, AI-guided, no code | Queued — full spec in §"ARC2 Session 3" below |

---

## Pre-conditions

1. Confirm no new mentor guidance supersedes anything below — this arc is process/hygiene work,
   not mentor-ruled architecture, so this is a quick check, not a corpus re-read.
2. Confirm the predecessor state: `git log --oneline -3` should show this session's own predecessor
   commits (the runner-scoping close + the `not_selected` fix) pushed, Vercel green.
3. If the session's Gate-1/Gate-2 hooks time out (the known 28s transient class, recurring across
   this whole arc's history) — proceed unframed, disclosed at open and close. Do not block on it.

---

## Part A — Open

Read in order:
1. `/adopted/standing-protocol-cache.md` (~3 min).
2. This prompt in full — it is self-contained; no other document is required reading.
3. `operations/decision-log.md` around line 15856 (`D-FABLE5-REGROUNDING-AUDIT-SESSIONS-2026-07-26-TO-30-2026-08-01`) — the source audit naming all six carried items, for context on why each exists.

Confirm at open: tier (mixed, no Critical surface); hold-point P0 0h; model selection — **no AC1 row
applies to steps 1/2/3/5 below** (no LLM call introduced); status vocabulary; signals.

---

## Part B — Procedure

### Step 0 — PROTO-1 decision (2 minutes, `AskUserQuestion`)

The inter-agent-handoff protocol's "resolve-before-P4" precondition was found silently bypassed
(the 2026-08-01 audit). It is documented at
`operations/agent-org-2026-07/P1-agent-roster-gap-analysis.md` and `operations/agent-org-2026-07/ops-calling-v1.md`
— read enough of both to state in one sentence what the protocol does before asking. Ask the
founder: **retire it** (remove the precondition, document why it no longer applies) or
**activate it properly** (wire the "resolve-before-P4" check for real). Record the answer in the
decision-log entry for this session; do not silently pick one.

### Step 1 — Fix the live factual misstatement on `/limitations` and `/welcome`

**Confirmed present, this session's own verification (2026-08-10):**
- `website/src/app/limitations/page.tsx:53` — *"Every evaluation produced by SageReasoning is
  generated by an AI language model (Anthropic's Claude)."*
- `website/src/app/welcome/page.tsx:138` — *"The reflections are generated by an AI model."*

**PR20 finding, carried into this step rather than resolved for you:** the 2026-08-01 audit cites
"the mentor's website-page feedback" on these two pages as its source, dated 2026-07-17 — **a
targeted repo-wide search this session found no transcribed verbatim record of that feedback
anywhere.** Search again yourself before writing anything (`grep -rn "misdescrib\|alt-3
architecture" operations/` is a starting point, not exhaustive) — if you find it, use it verbatim
and cite it. **If it is genuinely absent, do not fabricate a quote.** Use the closest thing that
*is* on the record: the already-adopted architectural correction from the alt-3 validation
(`operations/decision-log.md:158`, `D-RAG-MENTOR-ALT3-CRITICAL-PATH-APPROVED-2026-05-02` or its
neighbor) — *"a deterministic engine for the rule-like components of Stoic reasoning, with honest
soft-gating for the components that are not rule-like"* — **not** "fully deterministic," and
(per this whole repo's own governing architecture) **not** "generated by an AI language model"
either. The honest shape: Layer 1 (Sonnet) extracts structured features from your input; Layer 2
is a **pure, deterministic, non-LLM** computation over those features (the proximity rank, the
kathekon assessment, the signed verdict); Layer 3 (Sonnet, sometimes deferred) narrates it in
prose. `/limitations` currently collapses all three into "an AI language model produced this,"
which is the opposite of the architecture's actual selling point.

**Do:**
- Rewrite the `/limitations` §"AI-generated reasoning has limits" section to describe the
  three-layer shape honestly and briefly — the limitation that remains genuine (Layer 1's
  extraction and Layer 3's prose *are* LLM-generated and can be wrong; Layer 2's arithmetic is
  deterministic but only as good as what Layer 1 fed it) rather than the current blanket claim.
- Check whether `/welcome`'s "reflections are generated by an AI model" needs the same
  correction, or is actually accurate as written for the specific feature it describes (Sage
  Reflect's Q1–Q6 completions **are** substantially LLM-authored prose, unlike `/api/reason`'s
  verdict — read `website/src/lib/sage-reflect/engine.ts`'s own header before deciding; do not
  assume the two pages need an identical fix).
- Verify: `tsc --noEmit`, `npm run build`, both pages render (check via the Browser pane if a
  preview is running, or at minimum re-read the compiled output).
- Stage and commit; **do not push** — commit only, per the standing division of labour this whole
  arc has used (founder pushes and confirms Vercel green).

### Step 2 — Identify and wire the three new brand images

**Three new commissioned images exist, untracked in git, dated 2026-08-01:** `brand/Lituus.PNG`
(a curved staff/horn), `brand/open hand extended.PNG` (an open palm gesture), `brand/stone
basin.PNG` (a basin holding water, Greek key border). **This session's own inspection found none
of the three named or described in `brand/passion logos.rtf` or `brand/proximity cards and
colours.rtf`** — the existing 19-entry passion-symbol vocabulary in those files has no match for
any of the three. **Do not guess what they represent.** Ask the founder directly (a short,
specific `AskUserQuestion` — what each image is for: a passion sub-species not yet in the RTF, a
virtue, a Stoic exercise, something else) before touching any code. This is exactly the class of
judgement R21/R22 (adopted 2026-08-09, `D-WEBSITE-IMAGE-POLICY-AND-HUMAN-CREATOR-PROTECTION-ADOPTED-2026-08-09`)
were written to protect — a human commissioned specific meaning into these images, and an AI
inventing an association would misuse that work, not honour it.

**Once identified**, wire each per the established pattern (`website/public/images/`, the
`brand-display.ts` lookup table if it's a passion/virtue image, or the specific page(s) the
founder names) — mirror exactly how `millstone.PNG` was wired for `achos` (check
`website/src/lib/brand-display.ts` for that precedent before writing new code). Verify: `tsc`,
`npm run build`, and if the image renders somewhere checkable, confirm it visually.

### Step 3 — Build the observability retention sweep (C-1)

**Confirmed this session:** both `route_errors` and `throttle_events`
(`website/supabase-route-errors-migration.sql`, `website/supabase-throttle-events-migration.sql`)
already have a `retain_until` column **and an index on it** — the schema declares the retention
intent; only the sweep that actually enforces it is missing.

**Build one new route mirroring the exact `trust-core-retention-sweep` shape** (which already
sweeps three unrelated table-groups in one route via three independent purge calls — the direct
precedent for combining these two): a new `/api/cron/observability-retention-sweep`, handler-split
(route.ts thin GET wrapper; the real logic in `handler.ts`, injectable `purge` deps — copy
`trajectory-retention-sweep/handler.ts`'s exact shape), calling two new store functions in
`observability-store.ts` (`purgeExpiredRouteErrors`, `purgeExpiredThrottleEvents`, each a plain
`DELETE WHERE retain_until < now()`), gated behind a **new** flag
(`SUBSTRATE_OBSERVABILITY_SWEEP_ENABLED`, unset ⇒ honest 200 `{flag_enabled:false}`, zero DB work
— the standing dark-route pattern). CRON_SECRET-gated identically to the three existing sweeps.

**This session builds and tests it; does NOT activate it.** Flag activation + the `vercel.json`
cron entry is its own founder-walked Critical step (env-flag activating a new surface) — name it
as carried, do not do it here.

Verify: a new battery mirroring `trajectory-retention-sweep`'s own test file exactly (flag-off
byte-identity, flag-on purge-both-tables, cron-secret auth); `tsc`; `npm run build` (route
registered).

### Step 4 — Process-adoption decisions

Five items were recommended by the 2026-08-01 audit but never founder-approved as binding process
rules — **present each to the founder via `AskUserQuestion` individually** (adopt / adopt-modified
/ reject), do not silently adopt any of them:

1. **A mandatory reflect-harvest step** — the session's own Sage Reflect close-turn findings
   currently go nowhere (fire after the close is written). Proposed: an opener-check that reads
   the prior session's reflect output before starting new work.
2. **Widen PR19** (independent adversarial review, currently scoped to trust-core/predicate/fold/
   engine changes) to also cover auth/security/perimeter code.
3. **Mandatory model+effort commit trailers** — 10/21 commits since 2026-07-19 carry none.
4. **Memory-first triage** — check the memory index before diagnosing a recurring problem class.
5. **A retention-parity rule** — any new table declaring `retain_until` must ship its sweep in the
   same session, or the gap this whole step exists to close recurs.

**Only what the founder approves** gets written into `/adopted/standing-protocol-cache.md` (as a
new PR-numbered rule, following the exact amendment pattern PR17–PR20 already used — a dated
amendment note in the cache's own header, plus the cache-update-discipline decision-log entry) and,
if it touches project instructions, `/adopted/project-instructions-snapshot.md`. This is
`governance`, Standard risk, documentation-only.

---

## What this session does NOT do

- **Does not touch the Next.js version** — that is ARC2 Session 2, deliberately isolated (see below).
- **Does not run CRED-1 or any AUTH smoke** — those need your live admin JWT / browser session;
  ARC2 Session 3.
- **Does not activate the observability sweep flag** — build only; activation is its own
  founder-walked Critical step, named as carried, not taken here.
- **Does not touch anything in the IDEA-loop arc** — no flag, no `idea_loop_*` table, no route
  this arc's own three-mode standing prompt names as a guardrail.
- **Does not push to origin** — commits only; you push and confirm Vercel green for each.

---

## ARC2 Session 2 — full spec, carried forward

**Trigger:** after this session's commits are pushed and Vercel is confirmed green.

**Contents:** the Next.js version/security-exposure assessment. **Confirmed this session:**
`website/package.json` still pins `"next": "^14.2.0"`; the 2026-08-01 audit's own record states the
deployed version was `14.2.35` against a `15.5.18`/`16.2.6+` patched baseline, with 14.x **not**
in the 20-July patched set. That assessment is now over a week old — **re-verify the current CVE
exposure first-hand at session open** rather than trusting this paragraph, since Next.js
advisories move fast and the baseline may itself be stale by the time this session runs.

**Procedure:** (1) confirm current exposure against whatever the live patched baseline is at that
time; (2) if upgrade is warranted, bump the dependency, run the **full** test suite (not just the
touched-file batteries — a framework upgrade can regress anything), run `npm run build`, and
smoke-test a representative page or two if a preview is available; (3) isolate this to its own
commit, separate from anything else, so a regression is trivially bisectable; (4) the founder
reviews the diff, approves the risk classification (Elevated — "new external dependencies"), pushes,
and confirms Vercel green.

**Why it's isolated from Session 1:** a framework-wide dependency bump has app-wide regression
potential qualitatively different from four narrowly-scoped, independently-reviewable changes.
Bundling them risks a regression from the upgrade being harder to isolate from unrelated diffs, for
no real time saved.

### ARC2 Session 3 — full spec, carried forward

**Trigger:** whenever convenient — no dependency on Session 1 or 2 completing first; this can run
before, after, or interleaved with either, since it shares no surface with them. The only reason
it's sequenced last in this document is that it costs the least AI-session overhead, so bundling it
last minimizes total AI-facilitated sessions if the founder wants to do it in one sitting with the
others.

**Contents, bundled because both are quick founder-performed live checks with no build step
between them:**

1. **CRED-1** — `npx tsx --env-file=.env.development.local scripts/mint-credential.ts list | grep
   ae2-smoke` (adjust the env-file target to production per the standing mint-CLI discipline — a
   production admin JWT is required, memory `prod-mint-needs-prod-admin-jwt`), then `mint-credential.ts
   revoke practice --id <uuid>` for any still-active row found. Confirmed this session: no
   discharge record exists anywhere after the 2026-08-01 audit that raised it.
2. **The four AUTH post-deploy smokes** (the underlying fix has been live since 2026-07-25,
   never confirmed): (a) AUTH-1 negative smoke — while signed in, open `/auth/reset-password`
   directly, expect "No active reset link"; (b) the in-app retest — `/auth` → "Forgot your
   password?" → complete the flow (this doubles as the AUTH-1 positive path); (c) the `?redirect=`
   open-redirect check — `/auth?redirect=//example.com` or similar, expect same-origin-path
   validation to reject it; (d) the expired-link path — an old/already-used reset link, expect a
   clean failure, not a false "recorded and analyzed" or similar misleading success state.

**AI role in this session:** guide the exact commands and URLs, confirm each result against the
expected outcome as you report it back, write the decision-log entry closing out both items. No
code change, no push.

---

## Rollback path

Steps 1–3 each produce their own independently-revertible commit (`git revert`). Step 4 is
documentation-only, independently revertible. Nothing in this session touches a live production
surface — every change ships dark or as content, awaiting your push.

## Forecast

Success = four of the six carried items from the 2026-08-01 audit closed in one session (PROTO-1
decided, the live misstatement corrected, the three images correctly identified and wired, the
sweep built and tested), with the two founder-only items and the isolated dependency upgrade
explicitly named and carried rather than silently dropped again — closing the exact failure mode
(items dropped from carried lists with no recorded discharge) this arc exists to fix.

End of prompt.
