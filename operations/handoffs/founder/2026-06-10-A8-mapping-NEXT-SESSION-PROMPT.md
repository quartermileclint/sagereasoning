# Next-Session Prompt — A8 Mapping: substrate-migration + presentation map for the human tools

**Stream:** founder.
**Tier:** `governance` (mapping/design — no code this session).
**Governing frame:** `/adopted/standing-protocol-cache.md` (PR1–PR18; PR18 governs any production-state line touched).
**Predecessor session close:** `/operations/handoffs/founder/2026-06-10-prelaunch-S8b-close.md`.
**Predecessor decision-log entries:** `D-PRELAUNCH-S8B-RECONCILE-R18-RIDES-2026-06-10`, `D-REGISTRY-UPDATE-v1.6.0`, `D-PRELAUNCH-S8A-E2E-VERIFICATION-2026-06-10` (the migration election).
**Risk classification:** Standard under 0d-ii (design artefact only). Critical Change Protocol NOT engaged this session — but the map CLASSIFIES the migration sessions it produces, several of which will be Critical (AC5 perimeter routes; PR6).

## Why this session matters
The founder elected the substrate migration of the human tools pre-launch (S8a), and held the 0h exit in part on brand/presentation consistency (S8b blocker 2). A8 is the staging-plan vehicle (status: Scoped). This session produces the **per-endpoint migration map** — engine substitution AND presentation (W3 target rendering / W4 agent target-state contract) named together per endpoint so each route migrates ONCE. The map is what makes the migration arc a sequence of small, classified, PR1-disciplined sessions instead of a big bang.

## Pre-conditions
1. The S8b commit is pushed (close's Founder Verification block); founder spot-check of registry v1.6.0 done or scheduled (0h blocker 1).
2. Brand Guidelines v2 draft reviewed (`/drafts/2026-06-10-brand-guidelines-v2-draft.md`) — W3/W4 build to it (outward target; flame=Sage; Zeus=apprentice; placeholder list).
3. The lawyer email is out (wall-clock — does not gate this session).

## Part A — Open under the protocol
Read in order:
1. `/adopted/standing-protocol-cache.md` (~3 min)
2. The S8b close (authoritative state)
3. `/adopted/substrate-plugin-staging-plan.md` — A8 item + Stage-1 context
4. `/drafts/2026-06-10-brand-presentation-work-package.md` (W3/W4, confirmations folded) + the Brand Guidelines v2 draft
5. `/operations/decision-log.md` — last 3 entries

Confirm at open: tier; 0h status (HELD, three blockers); status vocabulary; PR18 in force.

## Part B — Spine
**Step 1 — Route census (code-read).** For each human-facing tool route — `/api/score`, `/api/score-decision`, `/api/score-document`, `/api/score-social`, `/api/score-conversation`, `/api/score-iterate`, `/api/score-scenario`, `/api/reflect`, the mentor surfaces — record: current engine path (runSageReason / raw messages.create / other), R20a perimeter membership + wiring state, response shape consumers (page components), metering hooks, registry row.
**Step 2 — Target architecture per route.** Substrate consumption pattern (per D21 migration plan + the A7 /api/reason precedent); Layer-3/prose needs; **presentation: W3 proximity-target render (outward; grey history; placeholder symbols) for the human pages; W4 target-state block + stable asset URLs where an agent contract exists**.
**Step 3 — Session sequence.** Order the migrations: PR1 single-endpoint proof first (recommend `/api/score` — the S8a-verified baseline with the founder's screenshot as the comparison artefact); each session classified per 0d-ii (perimeter routes Critical per AC5/PR6); score-conversation's distress wiring is its OWN Critical session in the sequence (S8a decision 2; 0h blocker 3).
**Step 4 — Verify.** The map reviewed route-by-route with the founder (business-document method); every route accounted for; every session in the sequence carries a risk class + rollback shape.
**Step 5 — Decision log (lean) + close.** Map adopted → A8 moves Scoped → Designed. Queue the first migration session's prompt.

## What is NOT in this session
No code. No flag changes. No perimeter changes. The map names the work; the sessions execute it.

## Rollback path
Design artefact only — no rollback needed; the map supersedes by revision.

## Forecast
Most likely: a one-session mapping that turns the migration + presentation arc into 6–10 ordered, classified sessions, with /api/score as the PR1 proof and the score-conversation Critical wiring slotted explicitly. The lawyer wall-clock runs in parallel; FPE-1/FPE-3 continue; the P1-input-rebuild prompt (review rec 3.2) follows the arc.

End of prompt. Opens on `main` (post-S8b commit). Trust the S8b close + decision log over any summary block (PR18).
