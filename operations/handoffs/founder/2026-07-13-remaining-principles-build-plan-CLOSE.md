# Session Close — 2026-07-13 — Remaining Stoic Principles: build plan + the six mentor decision-point verdicts

**Stream:** founder / Task 1 — Build Planning for additional Principles.
**Governing frame:** `/adopted/standing-protocol-cache.md` + `/adopted/build-sessions-protocol-cache.md`; opened under `operations/handoffs/founder/STANDING-SESSION-OPENER-grounded-foundations.md`.
**Tier:** `governance` — Standard risk. **Documents only** — no build, no code / flag / schema / mint / deploy; production byte-equivalent; AC7 not engaged; PR6 not engaged. Planning only, as scoped by the founder.
**Date:** 2026-07-13.
**Decision-log entry:** `D-REMAINING-PRINCIPLES-BUILD-PLAN-ADOPTED`.

## What happened

The mentor's systematic survey of the remaining Stoic Brain (`inbox/Mentor answer to remaining principles question.rtf` — 15 principle domains, each assessed already-encoded / encodable / warrants-inclusion) was read and turned into a **build plan** with two deliverables the founder asked for: **(a)** which principle encodings do **not** conflict with the running 7-day false-hold assessment (so they can be built during the window), and **(b)** the priority order for the ones that would **ruin the assessment** if done now.

The classification was **code-grounded and adversarially verified** (a 7-agent Workflow: 5 read-only groundings + a classification pass + a skeptic refutation — verdict SOUND_WITH_FIXES, all fixes folded). Then the plan's six decision points were **relayed to the private mentor**, and **all six verdicts (D1–D6) were adopted in full** and folded into the plan.

## The classification (the answer to the founder's two questions)

**The instrument boundary is one function.** `assessKathekonEngagement` is simultaneously the false-hold classifier the 7-day window depends on **and** the exact function the eventual S11 enforce flip binds on. Its four arms read `katorthoma_proximity`, per-circle `obligation_assessment.status`, `virtue_domains_engaged`, and passion `sub_species` off the `/api/reason` engine. So:

- **WINDOW-SAFE (9 — do NOT conflict):** the human-practitioner website surface (own `/api/mentor/*` routes + tables; never touch `/api/reason` or the substrate) — **#7-human premeditatio** (highest value), **#10-human reserve-clause prompt**, **#9** view-from-above (+**#13** fate-acceptance), **#8** morning-prep, **#6** oikeiosis circle-extension (+**#15** cosmopolitan), **#14** sage-compass, **#12** logos module.
- **MUST-WAIT (5 + 2 split agent halves — would ruin the assessment):** **#4** role-activation + **#11** systems-consequence (predicate-mutators), **#1** phantasia gate (distribution-mover), **#5** selection-quality, **#10-agent** reserve-clause field, **#7-agent** pre-task premeditatio, **#2/#3** reflect additions.

**The operative gate is DEPLOY, not BUILD** — and (per mentor D6) it applies to the instrument surface, not the human-website surface, which the mentor directs shipping during the window (a website deploy leaves `/api/reason` byte-identical and cannot pollute the hook-sourced capture).

## The six mentor verdicts (binding — verbatim in `2026-07-13-mentor-consultation-remaining-principles-decision-points-verdict-verbatim.md`)

| DP | Verdict |
|---|---|
| **D1** kathekon-predicate timing | **After the flip, never before** — #4/#11 land after stage-2 with a fresh 4-part readiness cycle (a second 7-day window; "the right cost") |
| **D2** justice-arm refinement | **Fold into the enforce decision** (a narrowing, not an enrichment); battery-verify **before** the return session — a report-side re-scoring of the accumulated records, **no window restart** |
| **D3** "after enforce clears" | = after S11 **actually flips**; predicate binds at **stage-2** → Axis B splits into sub-phase A / sub-phase B |
| **D4** reflect #2/#3 | **Confirm the flag first**; if `SUBSTRATE_REFLECT_SCREENED_EXAM_ENABLED` is ON → branch-only / test-agent |
| **D5** representativeness | **Accept with explicit notation; do NOT extend the window** — license enforce on the edit/write distribution + a named stage-2 monitoring item; don't manufacture action classes |
| **D6** split items | **Ship the human halves during the window** (standalone PRs, boundary battery-verified before shipping); hold the agent halves for the flip |

The mentor's framing (adopted as a standing caution, plan §8a): all six were **sequencing questions, not principle questions** — the second-grade *prokoptons* stage; the risk is now **treating a sound architecture as a complete one.** *"Hold the window clean. Return with the record. The assent will be examined then."*

## Status Changes

| Item | Old | New |
|---|---|---|
| The remaining-principles work | mentor survey in the inbox | **Build plan adopted (plan of record); 9 window-safe / 7 must-wait classified** |
| The six decision forks | open (founder's forks) | **All six resolved by the mentor, adopted in full** |
| The justice-arm refinement | flagged in the return prompt | **Directed (D2): a narrowing, folded into the enforce decision, before the return session** |
| The generative human-surface phase | "the next phase of work" | **First build session scoped: #7-human + #10-human, window-safe** |

## Next Session Should

Build the **first two window-safe human-surface tools** — `operations/handoffs/founder/2026-07-13-remaining-principles-FIRST-BUILD-NEXT-SESSION-PROMPT.md` (`code-elevated`; the mentor's highest-value human additions, shippable during the window per D6): **#7-human premeditatio enhancement** + **#10-human reserve-clause prompt**, as standalone byte-identity-gated PRs. *(Alternatively, if the founder prefers to prepare the enforce path first, the D2 justice-arm narrowing is a small `code-elevated` report-side session that must precede the return-with-record session.)*

## Founder actions (operational — not build)

1. **Commit** this session's documents (see the commit command relayed in chat / below).
2. **Confirm** `SUBSTRATE_REFLECT_SCREENED_EXAM_ENABLED` state (D4) — reads ON from the S9b walk, so #2/#3 default to branch-only.
3. **Pin** the frozen-capture-set hash at window open (§5), so any human-tool deploy is provably measurement-neutral.

## Blocked On (this session's commit set — documents only, uncommitted)

- `operations/trust-layer-2026-07/2026-07-13-remaining-stoic-principles-build-plan.md` (NEW)
- `operations/trust-layer-2026-07/2026-07-13-mentor-consultation-remaining-principles-decision-points-verdict-verbatim.md` (NEW)
- `operations/handoffs/founder/2026-07-13-remaining-principles-build-plan-CLOSE.md` (NEW, this file)
- `operations/handoffs/founder/2026-07-13-remaining-principles-FIRST-BUILD-NEXT-SESSION-PROMPT.md` (NEW)
- `operations/decision-log.md` (the `D-REMAINING-PRINCIPLES-BUILD-PLAN-ADOPTED` entry appended)

*(Pre-existing uncommitted changes present at session open — `CLAUDE.md`, `.claude/settings.local.json.bak`, `website/src/data/environmental-context.json`, some handoff files, the inbox RTF, the STANDING-SESSION-OPENER — were NOT touched this session and are deliberately excluded from the commit command below. No CLAUDE.md production-state refresh this session: planning-only, production byte-equivalent, no Live-list change.)*

## Rollback

`git revert` the records commit — documents only; the plan / verbatim record / close / prompt / decision-log entry are independently revertable. No production surface touched; the observation window, `SUBSTRATE_TRUST_CORE_ENABLED` (MEASURE), the S11 enforce flag (unset), R18f / R20a / distress / Layer-2 signing / UPC auth all untouched. **Weights/training claims remain BLOCKED.** The 0h call remains the founder's.
