# Next-session prompt — R8 follow-on: rulings adoption and the build-path elections

> **STATUS NOTE, 2026-08-30 (same day): branch B1 is ALREADY EXECUTED.** The founder relayed the
> mentor's ruling back into the R8 session itself; the ruling is captured verbatim
> (`operations/agent-circles-2026-08/2026-08-30-mentor-ruling-R8-producer-floor-semantics-verbatim.md`),
> annotated into the R8 design, and dispositioned into the register
> (`D-MENTOR-RULING-R8-PRODUCER-FLOOR-SEMANTICS-ADOPTED-EXECUTED-2026-08-30`). Headlines the next
> session inherits: **Q1a** founder-only v1 producer (ruled correct design, not a deficiency);
> **Q1b** the designed cycle-identity handoff on the dashboard adoption surface; **Q1c distinct
> identities required at mint** (runner ≠ executing agent, enforced, not convention); **Q2**
> Q-C2a's first question AMENDED to the producer's adoption-assent (the built handler/migration
> comments quote the old wording — a build item); **Q3** Option S buildable now, **M-vs-W
> deferred with locked framing** to the standing-runner track's next design-capable session
> (which opens with Option S's disagreement-rate data), and the R8-D7 backward-edge evaluation
> held AWAITING CONFIRMATION by that session. **The operative work below is therefore §B2's
> ruling-independent items and §C's elections — read §B1 as a record of what was done, not a
> task.**

**Paste this as the task after the standing session opener.** Tier: `governance` at open,
branching per §B — nothing in this prompt licenses a build, schema, flag, credential, or
activation; each build item below is its own founder election with its own prompt if elected.

**Predecessor:** the R8 standing-runner design session, complete and committed
(`operations/handoffs/founder/2026-08-30-standing-runner-design-R8-CLOSE.md`;
`D-STANDING-RUNNER-DESIGN-R8-COMPLETE-C11-EXPERIMENT-RUN-PR19-FOLDED-2026-08-30`). The mentor's
ruled Q7 sequence is complete at all five steps. The R8 design is a **proposal** — reviewed
(PR19, three independent dimensions, all findings folded) but not adopted; adoption of any part
of it is the founder's, and the two deepest items were deliberately routed to the mentor rather
than resolved in-design.

## A. Read at open (don't re-derive; every item below was independently reviewed)

1. `operations/agent-circles-2026-08/2026-08-30-standing-runner-design-R8.md` — in full. §4.0
   (the producer question), §5.3 (the M/W/S election), and §11b (the PR19 fold record) are the
   load-bearing sections; §11 is the follow-on list this session executes elections from.
2. `operations/agent-circles-2026-08/2026-08-30-c11-rerun-experiment-record.md` — the evidence
   record (9/10 vs 1/10 on identical text; the stage-assignment localization; Reading B
   demonstrated). Note its two inline corrections — both are part of the record, not blemishes
   on it.
3. `operations/agent-circles-2026-08/2026-08-30-MENTOR-QUESTIONS-R8-producer-and-floor-semantics-FOR-RULING.md`
   — the three-question brief (Q1 producer/handoff/identity-coincidence; Q2 the Q-C2a
   elector/attester tension; Q3 the M/W/S floor-semantics election), authored at R8's close
   under PR20.
4. The decision log's last entry + this prompt's predecessor close.

## B. Branch on the rulings' status at open

**B1 — the founder has relayed the brief and rulings are in hand:** capture the ruling verbatim
in-repo per the standing convention (verbatim wins over every summary, including your own
entry), apply the per-item dispositions to the R8 design as DATED ANNOTATIONS (never rewrite the
reviewed design silently — the 2026-08-29 F3-capture pattern: body untouched, binding
annotations win), update the named-input register's standing-runner rows to their post-ruling
state, and then surface the §C elections to the founder. If a ruling contradicts an R8 design
element, the ruling wins and the annotation says so plainly.

**B2 — rulings not yet in hand:** do NOT proceed to any build election that depends on them
(everything touching the completion-signal consumption's producer side, and any verdict-changing
R8-D7 variant). What is genuinely available without the rulings, each still a founder election
in-session: (i) the **contested-migration resolution walk** — founder-run `§PRE`/`§VERIFY`
against production for the ATRF-EE wave's migrations incl. `idea_loop_completion_signals`
(resolves the standing contested fact; read
`operations/agent-circles-2026-08/2026-08-23-atrf-ee-production-wave-FOUNDER-WALK.md` first and
treat its own completion-addendum as the contested claim under test, not as ground truth); (ii)
the **records-hygiene check** on the dogfood credential's usage meter (observed 7436/20000 vs
the recorded 5000/200 — one founder-run SQL read settles which record is stale); (iii) authoring
the build prompt for **R8-D6a (the verdict-repeatability instrument)** or **Option S** — both
are pure-measurement builds R8 judged safe ahead of the rulings, but AUTHORING a prompt is not
running it: each runs only under its own future session and election.

## C. The elections to put to the founder (whichever branch)

From the design's §11, in its recommended order: 1. the mentor brief (done if B1); 2. the
standing-runner build brief (`code-*` — gated on Q1's ruling for its producer-side items; its
ruling-independent items are listed in §11.2); 3. phase-1 activation of the completion-signal
write path (founder-walked `code-critical`; begins with the contested-migration resolution); 4.
R8-D6a as a standalone build + cadence election; 5. records hygiene. Present them as elections
with their gating stated — do not pre-elect, do not fold multiple elections into one on your own
judgement.

## D. Standing constraints (unchanged; do not soften)

- **Weights-BLOCKED** — both conditions unmet; no weighting function may be designed, sketched,
  or evaluated; R8's own first draft was caught reasoning-around once (§5.1, withdrawn) — do not
  reintroduce it.
- **Q1** — the loop proposes; it never executes. The producer question does not soften this; it
  exists because of it.
- **The Prerequisite Criterion** (manifest, binding) — engages on any proposal claiming
  practitioner-facing outputs; apply and show the application.
- **Q5b naming** — never the bare two-word layer term; "the context-injection layer" (live on
  `/api/reason`) vs "the per-consumer prose service" (not live).
- **PR19** — independent review before any design-consequential output of THIS session is
  treated as final (parallel `Agent` calls when the Workflow gate is unmet, disclosed as such).
- **PR20** — any further mentor brief names the mechanisms each ruling lands on;
  timestamp-check present-tense facts at drafting AND relay.
- **Concurrency convention** — `ListAgents` at open; `git status` twice; path-scoped commits;
  never `git add -A`; append shared records at the physical tail.
- **The O-C Gate-3 track** stays parallel, no ordering dependency either way.
- Nothing bears on the 0h call, which remains the founder's.

## E. What "done" looks like

Rulings captured verbatim and annotated into the design (B1), or the ruling-independent items
advanced with the dependency honestly held (B2); the elections surfaced with gating stated; a
PR19-reviewed decision-log entry appended at the tail; a lean close per the cache; a
next-session prompt if the elected path needs one. Nothing built, activated, or migrated except
what a founder election inside the session explicitly walks.

End of prompt.
