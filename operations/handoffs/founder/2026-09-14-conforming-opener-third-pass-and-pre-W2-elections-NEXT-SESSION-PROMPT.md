# SESSION PASTE — the conforming opener's third pass, and two founder elections prepared

**Paste this as the FIRST message of a FRESH session on the main checkout of
`/Users/clintonaitkenhead/Claude-work/PROJECTS/sagereasoning`. Not a worktree.**

Authored 2026-09-14 (from `date`, just after midnight AEST) by session `sagereasoning-b6 [efdaf2]` at
the founder's direction, after the Q-S2 four-question ruling was adopted and pushed
(`origin/main` = `c1c1804`, Vercel green per the founder).
**EVERY NUMBER IN THIS FILE IS A CLAIM TO RE-DERIVE, NOT A FACT TO QUOTE.**

---

## ⛔ STOP — read this fork before anything else

**THIS IS NOT W2's FIRST SITTING AND MUST NOT BE TREATED AS ONE.** W2's clock has not started.
Everything this session writes lands in the **CURRENT** window, whose intake is still open. That is
expected and correct — do not try to avoid it and do not try to cause it.

**If the founder intends to open W2's first sitting instead, DO NOT USE THIS PROMPT.** W2 opens under
`operations/handoffs/founder/W2-SESSION-OPENER-qs2-conforming-DRAFT.md`, pasted in place of the
standing opener, with the task stated beneath it. **The founder's act of opening is what makes a
sitting W2's first** — ruled 2026-09-13, *"The distinction is in the record, not in the prompt."*

**And the opener is not yet cleared for that use.** Task A below is what clears it. **If Task A has not
run, W2 should not open.**

---

## 0. Open under the standing opener, then this

Open under `operations/handoffs/founder/STANDING-SESSION-OPENER-grounded-foundations.md` — **Version
2026-09-13, founder-adopted.** Read its **Addendum first**, then the body. **Note that its carve-out
block and addendum §H now carry RULED annotations** added 2026-09-13; the ruling supersedes their
"not resolved" framing. Re-derive every number per its Part E.

Then read, in this order — **verbatim wins over every summary, including this prompt**:

1. `operations/trust-layer-2026-07/2026-09-13-mentor-ruling-qs2-opener-collision-four-questions-verbatim.md`
   — **the governing ruling for this session.** Q-A1, Q-A2, Q-A3, Q-B1, plus the process
   recommendation and the founder items. **Read its recording notes too** — one founder item in it
   rests on a stale premise and is flagged there.
2. `operations/handoffs/founder/2026-09-13-qs2-collision-and-pre-W2-items-CLOSE.md` — **§11, the
   addendum**, which records the second pass and corrects §3's count from one defect to five.
3. `operations/handoffs/founder/W2-SESSION-OPENER-qs2-conforming-DESIGN-NOTES.md` — **founder-facing;
   read it, do not paste it.** It carries both review passes, the five-instance lesson, **and the
   explicit line on where trimming was STOPPED.**
4. `operations/trust-layer-2026-07/2026-09-12-mentor-ruling-window-specification-QS1-QS2-QS3-verbatim.md`
   and `…2026-09-13-mentor-ruling-w2-window-work-and-QS2-discipline-verbatim.md` — the two rulings the
   governing one applies.
5. The decision log's **physical tail** (the two newest entries are this arc's); the S11 register's
   change-log tail.

**Tier: `governance` / documents for Tasks A and C. Task B is DESIGN ONLY — no code. AC7 NOT engaged.**

### Absolute constraints — any one breached means stop and write the close

- **No code, schema, flag, credential, migration, deploy, or push.** Path-scoped commits only.
  **Task B produces designs, not edits** — see its own stop condition.
- **No `GUARD_RE` file may be modified** — re-read the regex in
  `website/src/app/logos/__tests__/human-practitioner-boundary.test.ts`. No waiver exists.
- **No `manifest.md` edit.** D-4 and D-8 remain **reserved to the founder's own act** by ruling.
- **No R18 public surface.** D-3's recast wording needs the founder's signature, and by the Q-B1
  ruling its application waits for activation.
- **No mentor relay may be SENT.** Drafts only.
- **Do not open W2's first sitting.**
- **`option-s/` stays closed.** Reading the Option S close disqualifies you from the reserved
  `complete_series()` work — say so and do not open it.
- **Never write to `~/.sage-gate1/`.** Any report run is `--dry-run` ONLY.
- **Never stage another session's files.** `git status` whole before every commit. At this writing the
  tree carries a peer's `website/src/data/environmental-context.json` and two peer prompt files —
  **none of them yours.**
- **Do not open, read or list** `…/PROJECTS/idea-loop-rerun-2026-09/` (Session R's scratch project).

**Re-derive at open, run not quoted:** `git status` whole; `git fetch origin && git log --oneline
origin/main..HEAD`; the byte-identity guard battery **run**; the three SHA pins; the buffer size;
R20a from the arrays; extensions from the file; `ListAgents`.

---

## 1. TASK A (FIRST, and it gates W2) — the third implication pass on the conforming opener

**Why this exists.** The mentor ruled a second adversarial pass **mandatory** and specified its form:
*"the reviewer should be asked to find what the document implies, not only what it states."* That pass
ran and **found a HIGH** — the opener's close-pointer index cited a report **by exact subsection** and
the register by section, carrying **29 and 19** trigger-term occurrences respectively. Four further
instances of the same class were found. **All five were fixed — but the document changed at five points
AFTER the reviewer that assessed it.** That reviewer's own closing recommendation was to *"re-run this
same implication-focused review rather than a literal grep"* once the findings were addressed.

**So the document has never been reviewed in its current form. That is what this task fixes.**

**Do, in order:**

1. **Re-derive the baseline yourself.** `grep -ci "andreia\|variety\|courage"` the opener (expect 0 —
   but verify, do not trust). Then **audit every file it cites**: resolve each path and count trigger
   terms in it. **Two high-count citations are deliberate and must NOT be removed** — `CLAUDE.md` and
   `operations/decision-log.md`. The design notes §"Where the trimming was STOPPED" gives the ruled
   reason and the operative line. **If you find yourself about to trim them, re-read Q-A3 first.**
2. **Run TWO reviewers, and keep their contamination profiles different — this is the design, not a
   flourish.**
   - **Reviewer A — the implication hunt.** Give it the rulings and the ruled test (a bare per-domain
     count table is permitted; a purposive row is not; *"a row that says 'this domain is unmet and this
     window addresses it' communicates the criterion regardless of whether the word 'criterion'
     appears"*). Ask for **inference chains spelled out step by step**, and tell it explicitly to check
     **where the document sends its reader**, not only what the document says — that is where the last
     HIGH was.
   - **Reviewer B — the blind inference test.** Give it **ONLY the opener's path** and an instruction
     **not to grep or open any other repository file**. Ask what it infers is being watched for, and
     require it to **attribute every inference** to (a) the document, (b) background context it already
     holds, or (c) general reasoning. **Tell it that "I cannot determine what is being watched for" is
     a valid and useful answer and not a failure.** Note the known limit: a subagent receives the full
     `CLAUDE.md`, so it cannot be perfectly blind — the attribution requirement is what makes the
     result readable anyway.
   - Sonnet is adequate for both; the founder has standing permission for that.
3. **Verify every finding against source before acting on it.** The last pass's HIGH was real and the
   one before it was too, but a reviewer's claim is a claim.
4. **If findings are HIGH or MEDIUM: fix, then RE-RUN reviewer B on the fixed document.** The loop
   terminates when a pass finds nothing substantive on the document as it then stands. **Do not declare
   the document ready on the strength of a pass that examined an earlier version — that is the exact
   error this task exists to correct.**
5. **Record the outcome plainly.** If clean: say so, and state that the opener is ready for the founder
   to use for W2's first sitting. If not: say what remains and that W2 should not open until it is
   closed.

**Watch for the class this document has now produced five times: any meta-commentary about itself — its
provenance, its omissions, its adoption status, where to read more — has leaked or routed every single
time.** The standing lesson, recorded in the design notes: *a document whose purpose is controlled
context cannot carry its own meta-commentary.*

---

## 2. TASK B — prepare BOTH AC5 executable-check designs (design only; do not build)

**The mentor's item, verbatim:** *"pair whichever resolution is chosen with an executable check. A
written instruction has failed to arrest this drift three consecutive times. An executable check is the
only remedy that does not depend on a reader noticing."*

**⛔ THE CHECK CANNOT BE BUILT YET, AND THIS IS THE STOP CONDITION.** Its form depends on a founder
election that has not been made: §AC5 Option **A** (delete the enumeration) needs a check asserting the
section carries **no** route enumeration; Option **B** (keep it, relabel it honestly) needs a check
asserting any enumeration **matches the registry arrays exactly**. **Building the wrong one is worse
than building none.** Both options are set out at
`operations/handoffs/founder/2026-09-13-manifest-edits-FOR-FOUNDER-ACT.md` §EDIT 2.

**Do:** design **both** checks so the founder's election is immediately actionable — for each, the
assertion, where it lives, what it reads, how it fails, and **the mutation that must make it fail**.
Confirm first-hand that `website/src/lib/__tests__/r20a-invocation-guard.test.ts` does **not** match
`GUARD_RE` (it did not at the last check — re-verify). Note that this file's own docstring already
carries a precedent: a hand-maintained count there went stale **three times despite an emphatic
warning**, and is now enforced by assertion.

**Produce** `operations/count-discipline-2026-09/2026-09-14-ac5-executable-check-DESIGNS.md`.
**Write no test code into the repository's test files. Apply nothing.**

---

## 3. TASK C — prepare the Q-A2 bare-count-table election

**Q-A2 opened an option the last session did not take.** The ruling: *"a bare per-domain count table —
four domains, counts only, no link to the window's purpose — would be permitted instrument
disclosure."* The conforming opener currently carries **no** domain figures at all, which is **stricter
than the ruling requires**. It was left out on the judgement that a document which had just leaked at
five levels was not the place to add domain figures the same day.

**Do:** prepare the exact insert the founder would elect — a bare table, counts only, **no link to any
readiness part, no attribution of any gap, no mention of what any window is for**, with the figures
re-derived from the buffer at your own open and stated as-of. Give the exact placement. **State the
argument on both sides honestly**: it is permitted and it restores instrument fidelity a sitting may
genuinely want; against, it is the single highest-risk addition available to this document and the
ruling permits it without requiring it.

**Put it in the design notes as a new section, marked as an unelected option. Do not add it to the
opener.** If Task A's reviewers flag it as risky even in bare form, **say so and recommend against** —
the ruling permits, it does not direct.

---

## 4. What this session must NOT do

- **Not** open W2's first sitting or declare the opener ready on a stale pass.
- **Not** edit `manifest.md`, any R18 surface, or any `GUARD_RE` file. **Not** build either AC5 check.
- **Not** apply the D-3 recast wording — it needs the founder's signature **and**, by Q-B1, activation.
- **Not** revoke, mint or query any credential. **`sagereasoning:option-s@v1` is the founder's and
  needs a production admin JWT.**
- **Not** close peer sessions — the founder's, through the UI.
- **Not** commit the peer's `environmental-context.json` or either peer prompt file.

---

## 5. The founder's own acts, carried — remind them in the close

Ordered as the mentor put them:

1. **Revoke `sagereasoning:option-s@v1`.** *"The only open item with a live security surface… an open
   credential with unused quota is not a neutral state — it is an exposure with no remaining purpose."*
   **First, before W2 opens.**
2. **Close idle peers** (23 at the last count — re-derive). *"A stale peer waking inside W2 contributes
   records nobody decided to add."*
3. **The `manifest.md` sitting** — D-4's ruled ATRF item-3 wording and the D-8 AC5 fix, one sitting,
   from `2026-09-13-manifest-edits-FOR-FOUNDER-ACT.md`. **Elect A or B for AC5** — that election
   unblocks Task B's check.
4. **Sign the recast D-3 wording** (`…compliance-not-virtue-clause-SIGNOFF-PACKAGE.md` §8). Its
   application waits for activation by ruling.
5. **Then open W2's first sitting** under the conforming opener — **once Task A clears it.**

---

## 6. Close, records, commit

1. Close at `operations/handoffs/founder/2026-09-14-conforming-opener-third-pass-CLOSE.md` — what the
   reviewers found, whether the opener is cleared, what was prepared, the **tool-mode and record
   disclosure** (which buffer records this session added, by tool), guard + three pins at close.
2. Decision-log entry at the **physical tail**.
3. A register change-log row **only if** Task A changes whether W2 may open.
4. **Commit path-scoped:** `git add <your new files>`; `git commit -F <msgfile> -- <every path you
   touched>`; then `git show --stat HEAD` and confirm nothing beyond your paths landed. **`-F`, never
   `-m`.** **NEVER push** — the founder pushes.

## 7. If you hit a conflict

**Route it; do not resolve it.** A governed surface, a settled constraint, a `GUARD_RE` file, a decision
that is the founder's or the mentor's — stop, write the close, name it. **Two sessions running have now
had their most valuable act be a refusal**: one declined to write a prompt that would have breached a
ruling; the next stopped itself from trimming mandatory reading out of a document to chase a number.

**W2's clock has not started. D2 remains blocked. The S11 flip remains REFUSED. Weights remain BLOCKED.
The 0h call remains the founder's.**
