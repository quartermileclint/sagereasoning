# Next-Session Prompt — S11a: the extraction gate → the Arm-1 narrowing (conditional) → the trust-record cap

> **⚠ THIS SESSION MAY LEGITIMATELY NOT NARROW ANYTHING.** The mentor's supplementary ruling (**R12**) makes the Layer-1 extraction question a **conditional gate** on the narrowing: *"If the extraction is known to be reliable on the action classes in scope, narrow the arm. **If the extraction is uncertain, the extraction question is prior.**"* On the evidence in hand the extraction is **uncertain** (129/130 zero circles, 100% file writes; the predecessor could not separate *starved* from *mis-sited*). **Deferring the narrowing behind an extraction work-item is a SUCCESS outcome, not a failure.** The title says "narrowing" because that is the arc; the gate decides whether it happens here.
>
> **What binds regardless of the gate:** the **trust-record cap review** (R14 — *"do not close the narrowing session with the arm fixed and the cap unreviewed"*) and confirming the **§9 question is in the register** (R15).

**Stream:** founder (trust-layer).
**Tier:** `code-elevated` for the narrowing — **with a `code-critical` arm** (see §Tiers; the AI must confirm the tier at open, and the founder may reclassify upward at any time).
**Governing frame:** `/adopted/standing-protocol-cache.md`, opened under `STANDING-SESSION-OPENER-grounded-foundations.md`.
**BINDING SPECIFICATION (verbatim wins over every digest, including this prompt):** `operations/trust-layer-2026-07/2026-07-17-mentor-consultation-F2-exclusion-clause-verdict-verbatim.md` — **read §1 in full before any work.**
**Predecessor close:** `operations/handoffs/founder/2026-07-17-trust-layer-S11-F2-mentor-briefing-CLOSE.md`.
**Predecessor decision-log entries:** `D-TRUST-LAYER-S11-F2-MENTOR-BRIEFING-AUTHORED-HELD`, `D-TRUST-LAYER-S11-F2-MENTOR-RULING-EXCLUSION-CLAUSE-GOVERNS-ADOPTED`.
**Evidence of record (frozen):** `operations/trust-layer-2026-07/runs/2026-07-17/false-hold-record-FROZEN-2026-07-17.jsonl` — **130 records**, `2026-07-12T13:15:47Z → 2026-07-17T12:09:07Z`.

---

## Why this session exists

The mentor ruled on 2026-07-17: **the exclusion clause governs; Arm 1 requires narrowing; the readiness standard is not met; the flip does not proceed.** This session does the work the ruling names — and the mentor scoped it explicitly, all three parts, *"not deferred"*:

> *"The ruling is: the exclusion clause governs. The arm requires narrowing. The flip does not proceed. **The next session is the narrowing session, with the extraction question and the trust record consequence in scope.**"*

**This session does NOT flip anything, and does not re-measure part (3).** A re-measurement needs a new window (narrowed predicate + a representative distribution + a populated denominator) and is not in scope here.

---

## The ruling's operative findings (all binding; verbatim §1 governs)

1. **R1 — the exclusion clause governs.** A `dikaiosyne` tag resting solely on `is_kathekon === false`, with **no oikeiosis circle identified**, is **NOT** "a justice surface present". Ground: *"specific governs general — it names the exact engine emission, character for character."*
2. **R2 — the 2026-06-19 Scope premise does not carry forward.** *"Honouring the literal tag honours the letter of the specification while defeating its purpose."*
3. **R3 — the newer definition governs.** ADR-010 §4's `computeDikaiosyneFloor` (`circles >= 1 || hasNaturalRelationship`) over `computeVirtueDomains` (`circles >= 1 || is_kathekon !== null`). *"The floor's silence on 124 records is evidence, not absence of evidence."*
4. **R4 — the A2 concern is real, and the remedy is the extraction.** *"The omission class is a Layer 1 failure. The predicate is a measurement instrument. Fixing the instrument to compensate for the extraction failure produces a number that means neither thing clearly."*
5. **R9 — the instrument is not broken.** No fault finding against the S11 build. This is a narrowing, not a repair.

**The SUPPLEMENTARY ruling (§1b — read it; it revises this session's procedure):**

6. **R11 — the narrowing is SPECIFIED:** *"Arm 1 requires at least one identified circle."* **This closes the fork the predecessor left open** (≥1 circle vs §4's test). Do not re-litigate the content; only the *location* is open (Step 3).
7. **R12 — THE ORDER IS RULED. The extraction question is a CONDITIONAL GATE on the narrowing:** *"If the extraction is known to be reliable on the action classes in scope, narrow the arm. **If the extraction is uncertain, the extraction question is prior.**"* ⇒ **Step 2 runs first and may block Steps 3–4.**
8. **R13 — the A2 concern is answered, not dismissed:** *"The resolution is not to resist the narrowing. It is to be clear about what the narrowing does and does not fix… **If Layer 1 is not identifying affected parties when they are genuinely present, the narrowing produces a clean number on a starved input.**"*
9. **R14 — the cap is a CLOSING CONDITION:** *"Do not close the narrowing session with the arm fixed and the cap unreviewed."* Sequenced **after** the narrowing + re-run.
10. **R15 — the §9 question → the register. DONE 2026-07-17** (registered in the predecessor session rather than deferred here — strictly safer): `operations/trust-layer-2026-07/S11-FLIP-PREREQUISITES-REGISTER.md`, item **P1**. **This session must confirm it is there before closing** (the mentor's instruction was "before the session closes"); it need not resolve it.

---

## Pre-conditions

1. The **binding verdict §1** read in full. Then the briefing (`2026-07-17-F2-mentor-briefing.md`) for the mechanism + the evidence; then the finding of record — **read with its four corrections** (it overstates on four counts; the corrections are in the briefing §12 and the close).
2. **The clock is STOPPED and the observation freeze is LIFTED** (founder election, 2026-07-17). **⚠ The freeze lift is NOT a production licence** — see §Tiers. Confirm at open whether `GATE1_FALSE_HOLD_CAPTURE` has actually been unset in the founder-loop `settings.local.json` (it was the founder's carried step at the predecessor close); if it is still set, the buffer keeps growing — harmless, but say so.
3. **The frozen buffer is the evidence.** Do not re-derive from the live buffer; it has moved on.

## Part A — Open under the protocol

1. `/adopted/standing-protocol-cache.md` (~3 min) + `/adopted/build-sessions-protocol-cache.md` (this is a substrate build session)
2. **The binding verdict §1** — in full
3. `operations/trust-layer-2026-07/2026-07-17-F2-mentor-briefing.md` — the mechanism, the evidence, both steelmen
4. `/operations/decision-log.md` — last 2 entries
5. `/manifest.md` targeted: AC7, AC5, R18f, KG1

Confirm at open: **tier + the `code-critical` arm**; hold-point (P0 0h); model selection (cache Element 6); status vocabulary; signals + risk class; KG1 (DB writes).

---

## ⚠ Tiers — read before touching anything

The observation freeze is lifted. **The production constraint is not.** The trust core has been **LIVE in production under MEASURE since 2026-07-11** (`SUBSTRATE_TRUST_CORE_ENABLED=true`). So:

| Target | Live? | Tier |
|---|---|---|
| `kathekon-engagement.ts` (the predicate — **the narrowing**) | **No** — report-time only. The harness capture carries its own JS mirror and does **not** call it. | **`code-elevated`**, repo-only |
| `false-hold-observation-report.ts` (re-run over the frozen buffer) | No — offline | `code-standard`; **`--dry-run` only unless the founder elects an ingest** |
| **`derive-trust-events.ts`** (`deriveWorstJusticeOutcome` — **the trust-record cap's ground**) | **YES — LIVE.** It emits `justice-surface-unevaluated` on every R18f-verified accreditation write. | **`code-critical` + founder-walked (AC7)** |
| **`layer1-extractor.ts`** (the extraction **review**) | **YES — LIVE** on `/api/reason` | **review = read-only.** Any *change* is `code-critical` + founder-walked (AC7) and is its own step |
| The published cap on `sagereasoning:s9-loop@v1` | **YES — public, live** | remediation may need a **data** step (the founder's; the AI performs no Supabase op) |

**The single most important structural fact for this session:** `kathekon-engagement.ts`'s justice arm **delegates to `deriveWorstJusticeOutcome`** — deliberately, on the stated principle that *"'justice surface present' here means exactly what the engine means by it."* **The narrowing must decide whether to break that delegation.** Narrowing inside `deriveWorstJusticeOutcome` changes **live trust-event emission** (Critical). Narrowing in `kathekon-engagement.ts` alone leaves the live reducer emitting the same events — which is exactly what keeps the public cap latched (R7). **These are the same root and they pull in different directions. Surface the fork to the founder at open; do not pick it silently.**

---

## Part B — Procedure

> **⚠ THE ORDER IS RULED, NOT CHOSEN (R12, verbatim §1b).** *"Those are two separate problems and the narrowing session should address both, **in order**: first, does the extraction need work before the arm is narrowed, or can they proceed in parallel? **If the extraction is known to be reliable on the action classes in scope, narrow the arm. If the extraction is uncertain, the extraction question is prior.**"*
>
> **The extraction gate runs FIRST and may block the narrowing.** An earlier draft of this prompt had the narrowing at Step 3 and the extraction review at Step 4, in parallel — **that ordering is superseded.**

### Step 1 — Re-verify the ground (do not inherit it)
Re-confirm first-hand, from the **frozen** buffer + the source: the four-line mechanism (`computeVirtueDomains` `is_kathekon !== null` → tag; `selectImprovementPath` p4 → the hold; `deriveWorstJusticeOutcome` `statuses.length === 0` → `unevaluated`; Arm 1 = `justice !== null`); the kathekon factor-count table (0→`contrary`/`false`, 1→`marginal`/`null`, 2–3→`true`); and that `obligationStatuses: []` means **zero circles** (`oikeiosis` is a required `Layer2Assessment` field). **If any line does not reproduce, STOP and say so.**

### Step 2 — 🚦 THE EXTRACTION GATE (R12) — this decides whether the narrowing happens this session
**The question, in the mentor's terms:** *is Layer 1 extraction **known to be reliable** at identifying affected parties **on the action classes in scope**?*

Separate two hypotheses with **opposite remedies** — do not conflate them:
- **(a) STARVED** — the extraction should be finding parties and is not. ⇒ **the extraction question is prior**; the narrowing waits.
- **(b) MIS-SITED** — a tool-call payload (a filesystem path) genuinely has no party, no role, no purpose, so `contrary` is **arithmetic, not judgement**. **The project already made this argument once and acted on it** — it dropped Bash from the consult because a Bash payload carries no intent; `Edit`/`Write` payloads may carry no more. ⇒ the extraction is *reliable for what it is being shown*, and the narrowing may proceed.

**Evidence in hand (frozen buffer):** 130 records · **zero circles on 129** · `contrary` 128 · **`Edit` 66 / `Write` 64 — 100% file writes** · one depth · one proximity. Read `LAYER1_SYSTEM_PROMPT` re `relevant_circles` / `obligation_assessment` (read-only).

**⚠ The predecessor session could NOT separate (a) from (b) — it named this an open fork.** On that evidence the honest reading is **UNCERTAIN**, which by the mentor's own conditional routes to *"the extraction question is prior."* **Reach this judgement first-hand; do not inherit it — but do not paper over it either.** A useful discriminator: does the extraction find circles on action classes where a party is *unambiguously* present? If nothing in scope can demonstrate that, the answer is "uncertain," not "reliable."

**Put the gate to the founder (AskUserQuestion) with your judgement + the evidence:**
- **Reliable ⇒ narrow now** (proceed to Step 3).
- **Uncertain ⇒ the extraction question is prior** — the narrowing is **deferred to its own session behind an extraction work item**, and this session delivers the extraction diagnosis + the cap review (Step 6 still binds — R14 is not conditional on the narrowing landing; if the arm is not narrowed, the cap is reviewed on the current ground and the review says so).

**The warning to carry either way (R13, verbatim):** *"The narrowing makes Arm 1 accurate for the measurement instrument. It does not fix the extraction. **If Layer 1 is not identifying affected parties when they are genuinely present, the narrowing produces a clean number on a starved input.**"*

### Step 3 — Surface the remaining fork (AskUserQuestion) — **only the LOCATION is open**
**The narrowing's CONTENT is settled by R11 — do not re-litigate it:** *"Arm 1 requires at least one identified circle."* (The AI's earlier "≥1 circle vs §4's `circles >= 1 || hasNaturalRelationship`" fork is **CLOSED** — the mentor was specific and verbatim wins. The `hasNaturalRelationship` disjunct is a design detail, and on this distribution it is moot: 0 kathekon factors ⇒ no natural relationship, and that route is already covered by **Arm 3** via §4's floor to `reflexive`. Note it; don't reopen it.)

**What IS open — where the narrowing lives:**
- **`kathekon-engagement.ts` only** — `code-elevated`, repo-only. **But** the LIVE `deriveWorstJusticeOutcome` keeps emitting `justice-surface-unevaluated`, which is exactly what keeps the public cap latched (D1).
- **Also `deriveWorstJusticeOutcome`** — closes the cap at the root, **but it is LIVE** (trust core ON in production under MEASURE since 2026-07-11) ⇒ **`code-critical` + founder-walked (AC7)**.

**Same root, opposite directions. Surface it; do not pick silently.** Also elect: the report re-run `--dry-run` vs ingest.

### Step 4 — Narrow the arm — **only if Step 2's gate permits**
Arm 1 requires ≥1 identified circle. **Regression-pin both directions:** a `contrary` + zero-circle verdict must now read `false_positive`; a genuine circle-carrying justice surface (violated / indeterminate / met) must still read `correct_hold`. **Pin the A2 blindness explicitly** (R13) — a test that documents what the narrowed arm does with a zero-circle record, so the blindness the mentor *accepted with open eyes* is **visible in the battery**, never implicit.

### Step 5 — Re-run the report over the FROZEN buffer
Against `runs/2026-07-17/false-hold-record-FROZEN-2026-07-17.jsonl`. **Record the reading as evidence of what the narrowed arm sees — NOT as a readiness claim.** The standard is not met and **this window cannot meet it** (parts 1+2 fail; the denominator is unpopulated — register P5). **Do not let a non-zero rate here be read as progress toward the flip.**

### Step 6 — 🔒 THE TRUST-RECORD CAP — **a CLOSING CONDITION (R14), sequenced AFTER Steps 4–5**
Verbatim: *"Once the arm is narrowed and the buffer re-runs, the cap's ground should be re-evaluated **in the same session**. **Do not close the narrowing session with the arm fixed and the cap unreviewed.**"*

Establish the cap's **ground** — do not infer it (the predecessor verified the cap **exists** but did **not** query the ledger): read `agent_trust_events` for `sagereasoning:s9-loop@v1` and confirm the `justice-surface-unevaluated` events + their artifacts. Then disposition per the Step-3 election: code fix / data fix / both / disclose-and-carry. **The founder performs any Supabase op (PR17).**

**Honest bounds to carry:** the cap is the **safe** direction (trust reads lower, never higher), and there are **no external users** — this is an R18 **honesty** problem, not a safety one. But it is **live and public**, and the mentor calls it *"a live signal on a live surface."*

**This step binds even if Step 2 defers the narrowing.** If the arm was not narrowed, review the cap on its current ground and say so plainly.

### Step 7 — Adversarial review
Per the slice pattern (PR15 — Workflow fan-out). Dimensions worth naming: narrowing-faithfulness to the **exclusion clause**; **the A2 blindness made visible** (R13); the **extraction gate's judgement** — was "reliable" concluded because it was convenient?; flag-off/production byte-identity if any live path is touched; claims-vs-code; **and a "did we narrow it so it passes?" critic** — the trap the ruling itself warns against.

### Step 8 — Verify · Step 9 — Decision-log (lean) · Step 10 — Close (lean)
Batteries: `kathekon-engagement` · `false-hold-capture` · trust-core S1 · `emission-hooks` · S10 · logic-harness · negative-battery (RELEASE GATE) · `tsc` · `npm run build` if any route/page changes. Suggested id: `D-TRUST-LAYER-S11A-ARM-NARROWED`.

---

## Explicitly NOT in scope
- **The S11 flip.** ENFORCE remains S11, readiness-gated. Weights BLOCKED. **Nothing this session produces is progress toward it** — the standard is not met and this window cannot meet it.
- **The §9 input question** — **already REGISTERED** (P1) per R15; a **flip prerequisite**, its own design step. **Confirm it is in the register before closing; do not resolve it here.**
- **A part-(3) re-measurement / a new window** (register P6). Needs the narrowing landed + a representative distribution + a populated denominator (P5).
- **Any Layer-1 prompt CHANGE** — Critical + founder-walked, its own step. **The extraction *review* (Step 2) is read-only** and is in scope; the *fix* is not.

## Rollback path
The narrowing: `git revert` (repo-only if confined to `kathekon-engagement.ts`). Any live-path change (`derive-trust-events.ts`): per its own Critical rollback — specified in-session **before** it is made, founder-walked, AC7.

## Forecast
**Two legitimate outcomes, and the gate decides which:**
- **Gate says "reliable"** → the arm is narrowed to ≥1 circle, the A2 blindness is pinned visibly in the battery, the frozen buffer re-runs as evidence, and the cap is reviewed on the narrowed ground (R14).
- **Gate says "uncertain"** → **the narrowing does NOT happen this session** (R12: *"the extraction question is prior"*). The session delivers the extraction diagnosis — starved vs mis-sited, with evidence — the cap review on current ground (R14 still binds), and an extraction work-item prompt. **This is a success, not a failure.** Shipping a narrowed arm over an extraction nobody has characterised is precisely *"a clean number on a starved input."*

**Then:** the §9 input question (P1) → (only after) a re-measurement design (P6). **Update `S11-FLIP-PREREQUISITES-REGISTER.md` before closing** — mark P2/P3 per what actually happened. **The 0h call remains the founder's.**

End of prompt.
