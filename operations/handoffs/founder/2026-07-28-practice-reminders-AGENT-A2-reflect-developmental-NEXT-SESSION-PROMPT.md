# Next-Session Prompt — Practice Reminders, Agent Half, Phase A2: The Reflect Developmental Read-Back

**Stream:** founder (substrate / agent experience). This is a **substrate-build session** — read `/adopted/build-sessions-protocol-cache.md` at open alongside the standing cache.
**Tier:** `code-elevated` — dark, flag-gated, additive. **The A3 activation is NOT this session** (its own founder-walked `code-critical` 0c-ii; nothing here pre-approves it).
**Governing frame:** `/adopted/standing-protocol-cache.md`.
**Plan of record:** `operations/reminders-2026-07/2026-07-26-practice-reminders-AGENT-build-plan.md` — **§5 (Phase A2) is this session's spec.** Read it in full; this prompt supplements it with seam-level grounding from the A1 session, it does not replace it.
**Binding verdict records (read BOTH before writing any string):** `operations/reminders-2026-07/2026-07-27-step-M-mentor-verdicts-verbatim.md` AND `operations/reminders-2026-07/2026-07-28-mentor-verdicts-agent-suggestions-verbatim.md` — **the verbatim records win over every summary, including the plan and this prompt; where they overlap, the 2026-07-28 record is the later and more specific ruling.** A1 proved the discipline the hard way: the plan's own §4 B3 row was a spec overreach the first record names in terms, caught only by the independent review. **The 2026-07-28 record answers this prompt's two central design questions directly (Items 6 and 7 — see the BDs below) and settles the Item 8 closure principle.** Treat the plan's §5 as a summary to be checked against both records, not as ground truth.
**Predecessor close:** `operations/handoffs/founder/2026-07-28-practice-reminders-AGENT-A1-suggestion-composer-CLOSE.md` (A1 — built dark, reviewed, folded; commit "Build the agent suggestion composer, then fold an independent review (A1)", pushed, Vercel green).
**Risk classification:** Elevated under 0d-ii — a bounded read + additive, flag-gated response fields on the reflect completion; **no schema, no flag set, no auth change, no deploy, no mint; production byte-equivalent until the founder's push, and on push byte-identical because `SUBSTRATE_REFLECT_DEVELOPMENTAL_ENABLED` is unset everywhere (battery-asserted).** AC7/PR6 not engaged. **If the observation source turns out to require any schema change — STOP; that reclassifies the step.**

## Where the arc stands

1. **A1 is COMPLETE, including the second consultation's fold** (`D-PRACTICE-REMINDERS-AGENT-A1-SUGGESTION-COMPOSER-BUILT-REVIEW-FOLDED` + `D-PRACTICE-REMINDERS-AGENT-SUGGESTIONS-MENTOR-VERDICTS-ADOPTED-AND-FOLDED`, both 2026-07-28): the composer (`website/src/lib/substrate/practice-suggestion.ts`) serves at most one question-form suggestion inside the CI-13 `practice` block on `/api/reason` and the accreditation write 200, dark behind `SUBSTRATE_PRACTICE_SUGGESTION_ENABLED` (unset everywhere). The locked vocabulary is 18 basis codes / 7 practices / 2 vetted question forms (the acute-fear line carries the prohairesis question); nine build decisions BD-1a…BD-9 are recorded in its module header; its battery is 759/0 with 32/32 + 13/13 mutations killed.
2. **A2 (this session) is the stage-crossing trigger** — the reflect completion gains the developmental read-back by wiring the already-built-but-never-called `evaluateDevelopmentalFlags`, plus the one-suggestion attach at the `grade_changed` moment. **A3** (R18 docs + founder-walked Critical activation) follows and is where A1 and A2 both go live; their flags are separate so the surfaces can activate independently.
3. Step M is discharged and binding (`D-PRACTICE-REMINDERS-STEP-M-MENTOR-VERDICTS-ADOPTED`, 2026-07-27).

## The binding verdicts that shape A2 (re-read verbatim before building)

1. **The repetition threshold belongs to THIS trigger.** Mentor, on suggestion density: the in-session trigger responds to a single diagnosis, but *"if the same passion fires repeatedly across multiple sessions, that is stage-crossing data."* A2's pattern-based flag (consistent `deliberate` across sessions in one domain) is exactly this — the threshold that was WRONG for B5 in-session is RIGHT here. The B5/BD-2 honesty bar still applies to the evidence (see BD-A2-1).
2. **QUESTION form for any rendered suggestion line** — and per plan §5, **"same locked strings, same one-max rule"**: A2 authors NO new suggestion lines. If a genuinely new line seems needed, that is a fork for AskUserQuestion (new content would be un-vetted by Step M), not a build decision.
3. **"Tracked, not intervened"** — spec-7 constraint 3, mentor-fixed, already encoded in `evaluateDevelopmentalFlags`'s own note text. The read-back describes; it binds nothing and gates nothing.
4. **Protected silence** — no qualifying pattern ⇒ the field content is honest (empty list or absent field per the byte-identity design); a failed read ⇒ **fail-honest omit, never a fabricated flag** (plan §5's own words).

## Pre-conditions

1. `git log` shows BOTH the A1 commit ("Build the agent suggestion composer, then fold an independent review (A1)") AND the 2026-07-28 verdicts-fold commit ("Fold the second mentor consultation into the suggestion composer") at or behind HEAD, pushed. If not — STOP and say so.
2. If `operations/handoffs/founder/2026-07-27-practice-reminders-human-phase2-in-session-suggestions-CLOSE.md` or `website/src/data/environmental-context.json` still show as modified, they are NOT this session's — leave both alone.
3. Baselines at open (verified 2026-07-28 after the verdicts fold — re-run and confirm; drift is a finding, not something to absorb): practice-suggestion **759/0** · loop-fold **181/0** · trajectory-delta 73/0 · trajectory-overlay 36/0 · aah-store 120/0 · kathekon-engagement 105/0 · practice-cycle-hint 13/0 · practice-sequence 645/0 · **S4 417/0** (the engine A2 wires) · S10 106/0 · trust-core 98/0 · emission-hooks 15/0 · accreditation route 90/90 (needs `--env-file=.env.development.local`; prints its summary then HANGS on a keepalive — run redirected to a file in the background and kill after the summary, per the standing tsx memory) · `tsc` 0 · `npm run build` 0. **Reflect-side baselines from the records (NOT re-verified at A1 — confirm first-hand):** s9b-practice-completion 86/0 · reflect-service 28/0 · session-store 34/0 · request-helpers 17/0 · reflect-completion-schema-drift 9/0.

## Verified seam grounding (re-verify first-hand at open; line numbers drift)

- **The unwired engine function:** `evaluateDevelopmentalFlags`, `src/lib/substrate/trust-core/intervention-engine.ts:724` (verified 2026-07-28 — called only by its own tests). The S4 engine is pure with `import type`-only imports (zero runtime deps — no I/O/env/clock/DB); its developmental threshold (3 consecutive `deliberate`) is DERIVED/tunable per the S4 record, unlike the mentor-fixed A8 bound. Read its actual signature and the `SessionDomainObservation` shape first-hand before designing the feed.
- **The reflect completion read-back:** `reflect/response-builders.ts:164-207` per the plan's §2 grounding (2026-07-26 — re-verify path and lines): carries the profile read-back incl. `grade_changed`, no developmental field. Locate the exact attach point and what is already in scope there.
- **The A1 interface A2 builds on** (all in `practice-suggestion.ts`): `PracticeSuggestionSnapshot` (`assessment?`/`examinationOpen?`/`delta?`/`loopFold?`) → `composePracticeSuggestion` (PURE — no env read) → `practiceSuggestionFor` (**reads A1's flag** + the never-throws boundary). The locked `SUGGESTION_LINES`/basis vocabulary; `PRACTICE_SUGGESTION_FRAMING_NOTE`.
- **Import-purity pins that MUST stay green:** the composer never imports the intervention engine (practice-suggestion battery §9, mutation-verified) — so `evaluateDevelopmentalFlags` is wired in the REFLECT path (route/response-builders), never through the composer. The loop-fold battery §11 allow-lists every `loopFoldAnnotation` consumer by exact line — do not add one.
- **The `/api/reason` seam is locked VERBATIM** by the A1 battery (whitespace-normalised exact match). A2 has no business touching `/api/reason` or the accreditation seams at all; if a change there ever seems needed, stop and reconsider.
- **Reflect-surface standing facts:** the route is inside the R20a perimeter (`SUBSTRATE_REFLECT_R20A_ENABLED` live — untouched by A2); reflect metering is live (the S9b walk's loop-billing fixes); reflect-row erasure is wired (S9b); the `reflect-store owner-scoping` register item stands (no owner column — any read A2 adds must be scoped to the subject credential/agent, R17a-style, and must not widen exposure).

## Build (plan §5, restated as work items)

1. **The developmental read-back.** A bounded read of an EXISTING store for the subject credential (window + cap fixed and documented; fail-honest omit on any read error) assembles `SessionDomainObservation[]`; `evaluateDevelopmentalFlags` runs; the completion response gains an additive, flag-gated **`developmental_priorities: [{domain, note}]`** — the engine's own note text served to the reflecting agent. R4: results-level only (domain + note); engine internals stay closed.
2. **The `grade_changed` moment.** When the completion's profile read-back reports a grade change, the composer may attach its ONE suggestion suited to the new record — same locked strings, same one-max rule, honest silence when no basis qualifies (see BD-A2-2).
3. **Flag + battery.** Everything behind the NEW `SUBSTRATE_REFLECT_DEVELOPMENTAL_ENABLED` (unset everywhere): flag-off, the completion response is byte-identical AND the bounded read is **skipped entirely** (zero new DB work — pin it). Measure completion latency/metering before/after flag-on (the plan's named check). Battery with the A1 disciplines: serialized byte-identity against the REAL response builder where importable (else a verbatim-locked seam); every new served field asserted (the A1 review's `basis.signal`/`observed` lesson — no wire field with zero assertions); non-vacuous fixtures; boundary values in the firing direction; **mutation-test every load-bearing pin, asserting each mutation applied before trusting its verdict**.

## Named design decisions to resolve IN-SESSION and record as BDs

- **BD-A2-1 — the observation source: ANSWERED (2026-07-28 record, Item 6 — encode, don't re-decide).** The session unit is **the accreditation write** — "the agent's closest analog to the human practitioner's session close … externally triggered and formally recorded" (the completed reflection is "internal to the session rather than the session's formal close"). The threshold is a **plateau, not a streak**: "deliberate-level reasoning in at least three of the four most recent accreditation writes in a domain, with no single write showing reflexive-level reasoning in that domain." **If the store cannot honestly compute that, three-consecutive is an acceptable approximation — noted as a known limitation and flagged for revision** when richer evidence lands. What REMAINS this session's grounding work: verify first-hand which store honestly carries per-accreditation-write domain observations (`agent_trust_events` rows derive from accreditation writes — the natural candidate), keep the read bounded + credential-scoped + fail-honest, and **never fabricate session boundaries from timestamps** (the A1 BD-2 trap; `agent_assessment_history` rows are per-consult with no session identifier, verified field-by-field).
- **BD-A2-2 — the `grade_changed` read: ANSWERED (2026-07-28 record, Item 7 — encode, don't re-decide).** **No grade-keyed mapping** ("exactly the corridor the stages-as-conditions ruling rejected"). The grade change is the TRIGGER only; the suggestion is determined by **a fresh read of the FULL vetted signal mapping against the record in hand** — "a grade improvement might be driven by strength in one domain masking weakness in another. … The grade change opens the doorbell; the record determines what the doorbell says." Same locked strings, one-max, B7 silence when nothing qualifies. What REMAINS this session's grounding work: what of the record is honestly in hand at completion (the profile read-back derives from the accreditation record — e.g. `passions_persisting`, the same `PersistingPassion` type the composer's persisting bases read); if feeding an existing basis needs a new snapshot field, keep the composer pure and the codes/lines unchanged, recording the per-leg fidelity mapping (the A1 BD-4/BD-5 pattern). A narrow live basis set — or none — is B7 working, not a failure.
- **BD-A2-3 — the reflect seam helper.** `practiceSuggestionFor` reads **A1's** flag; calling it from the reflect path would couple this surface to A1's activation, defeating the plan's separate-flags election. Recommended: a second seam helper in `practice-suggestion.ts` reading `SUBSTRATE_REFLECT_DEVELOPMENTAL_ENABLED`, mirroring the first exactly (flag-read-at-seam + the never-throws boundary); the pure composer stays the single shared core. Record the choice; extend the §9 purity slice if the module grows.
- **BD-A2-4 — the threshold.** The engine's 3-consecutive-`deliberate` threshold is derived/tunable. Keep it as-is unless grounding shows a reason not to; any change is to the CALL SITE's documentation, never a silent edit to the shared engine (S4's battery is 417/0 and stays untouched).

If any of these turns out to be a genuine fork the verdicts do not bound, surface it via AskUserQuestion rather than electing silently.

## Boundaries (verify before close)

- **A1's surfaces byte-untouched:** `/api/reason` route, accreditation route + response-builders unchanged; practice-suggestion 515/0 and loop-fold 181/0 stay green (extending `practice-suggestion.ts` with the new seam helper is permitted; every existing pin must survive).
- The composer never imports the intervention engine (the pin stays green — the wiring lives in the reflect path).
- S10 public record unchanged · `/api/guardrail` + `/api/practice/discernment` untouched · ADR-013 §8 envelope unchanged · **weights-tier claims BLOCKED** (restate on-block and in the decision-log entry) · 0h unaffected · MEASURE throughout · the S4 engine file itself unmodified.
- **`stoic-brain.ts` frozen** (standing guard). **Known, founder-undispositioned:** the logos `human-practitioner-boundary` git byte-identity guard forbids ANY working-tree change under `src/lib/substrate/` — it went red-while-uncommitted at A1 and will again this session. Expected transient (green at HEAD, green on commit); record it in the close, do NOT weaken or scope it — that disposition is the founder's open item.
- **No schema** (STOP if the observation source demands one) · no new rate-limit category · no public-docs change while dark (R18 — A3's job).

## Adversarial review (PR19 — REQUIRED before "Verified")

Run the independent adversarial review **as a multi-agent Workflow — this sentence is the explicit opt-in for the Workflow tool this session.** Dimensions (≥5 finders, each finding put to an adversarial verifier): verdict-fidelity against the verbatim record AND the plan-vs-record check A1 proved necessary (does §5 itself overreach anywhere?); flag-off byte-identity + zero-DB-work at the reflect completion; observation-assembly honesty (BD-A2-1 — can the served "across N sessions" claim be produced by evidence that is not session-grained?); boundary/blast-radius (A1 seams, composer purity, S4 engine untouched, metering/latency unperturbed); test-adequacy (the A1 review's confirmed gap classes: unpinned seams, vacuous fixtures, unasserted wire fields, bypassable string checks, boundary values never exercised in the firing direction). If the Workflow dies on account limits, complete first-hand per the §4 precedent, disclose honestly, and queue the independent re-run.

## Close-out

Decision-log entry (`D-PRACTICE-REMINDERS-AGENT-A2-REFLECT-DEVELOPMENTAL-BUILT…`; BDs recorded with reasoning) · agent plan §5/§8 status update · session close with the Founder Verification block (GitHub Desktop flow: a staging command, then Summary + Description — quote mentor text only in files, and if any terminal commit is ever used, `-F` a file, never bare `-m` with nested quotes) · **commit this prompt file with the close** (it is untracked; the A1 lesson — a plan that references an uncommitted prompt leaves a dangling reference) · no AI-side commit; the founder commits by name.

## Rollback path

`git revert` the session commit — a bounded read + flag-gated response fields + tests; the flag is unset everywhere so nothing is live at any point. (A3's eventual rollback: unset flag(s) + redeploy, byte-identical, battery-asserted — recorded there.)

## Open items carried (not this session's work)

- **A3** (R18 docs + founder-walked Critical activation of A1+A2) — the deferred `loop_fold` R18 docs remain its natural neighbour, still not folded in.
- The B5 per-session-granularity decline signal (a delta change) · the fold-open closure class (needs the CI-4 marker persistence schema step; **the closure principle is pre-settled** — 2026-07-28 Item 8: the bar is the SAME decision re-examined, never same-class competence) · **the logos byte-identity guard disposition (founder: scope or retire)**.
- Plain-language clause copy for the non-philodoxia craving sub-species (orge/eros/pothos/philedonia/philoplousia currently share the vetted general-contingency line — the Item 5 known limitation; next consultation).
- R17 on `milestones` (Critical, founder-walked) · human Phases 0–1 independent-review re-runs · the journal UTC pace-gate mismatch · the day-55 evening-pole case · `/api/milestones` + `/api/baseline` on the `scoring` bucket · `oikeiosis_context` never written.
- The s9-loop harness ran mostly unframed at A1 (401/429) but produced genuine frames by that session's close — consistent with the documented transient fail-secure class; the credential-refresh follow-up stands only if 401s persist.

## Forecast

Success = an agent completing its reflect receives, flag-on, the engine's own developmental note — "consistent deliberate in ⟨domain⟩, a developmental priority, tracked not intervened" — grounded in evidence that is honestly session-grained, and at a grade change at most one question in the mentor's register; flag-off, the completion is byte-identical and does zero extra work. Behind it: A3 walks both surfaces live with founder-signed docs.

End of prompt.
