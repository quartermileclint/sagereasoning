# SESSION PASTE — reground the standing opener; re-derive the mentor's last open figure

**Paste this as the FIRST message of a FRESH session.**

**Do NOT open the standing opener as your grounding source this time — it is the thing being fixed.**
Read it, but treat every fact in it as suspect until re-derived. **EVERY NUMBER IN THIS FILE IS A
CLAIM TO RE-DERIVE, NOT A FACT TO QUOTE.** Authored Thu Sep 10 17:50:34 AEST 2026 by session
`8fe3a6ae-dcb1-4324-846d-631d51336dc4`.

---

## 0. Why this session exists, and why it is first in the queue

**The standing opener (Version 2026-09-08) is stale in three load-bearing ways, and the mentor has
called its regrounding overdue** — *"A session opening on false facts costs the first twenty minutes,
as this session found."* That cost was paid by the session that wrote this prompt.

The three false facts, each verified false first-hand:

| Opener says | Actually |
|---|---|
| Baseline **"2 of 5"** | **5 of 5** in the counting sense, mentor-confirmed |
| `classifyCaller` is **byte-unchanged**; nothing may be built against Option C′ | **Built and LIVE**, mentor-ruled and licensed; emitting `live_agent` |
| `caller_class` **measures null** — two values only | **Three values**; schema is now **`false-hold-record-v6`**, carrying `clientVersion`/`clientEntrypoint` |

Nothing downstream can be trusted while sessions open on those. **This session is the unblocker.**

**Read first, in this order, and treat these as the primary sources:**
1. `operations/handoffs/founder/2026-09-10-guard-log-buffer-reconciliation-CLOSE.md`
2. `operations/trust-layer-2026-07/2026-09-10-mentor-ruling-guard-reconciliation-three-questions-verbatim.md`
   — **canonical; verbatim wins over every summary including this prompt**
3. `operations/handoffs/founder/2026-09-10-post-condition3-build-verification-CLOSE.md`
4. `operations/trust-layer-2026-07/2026-09-10-post-build-outcome-mentor-response-verbatim.md`
5. `operations/handoffs/founder/2026-09-10-condition-3-version-pinning-design-CLOSE.md` (its ADDENDA)
6. The decision-log **physical tail** — the newest entries are at the END; a bare `grep '^## D-'`
   misses entries using the `## <date> — D-…` form

---

## 1. Task A — reground the standing opener (the reason this session is first)

**Convention:** the opener's own footer says it is *"Rewritten only at a grounding session, from
primary sources, carrying its as-of date."* The prior version was written by such a session,
first-hand and **without subagents**. Follow that.

**One ambiguity to respect rather than resolve:** the mentor called the regrounding *"a founder act"*
while also saying it *"is not a build act and does not require a ruling."* The safe reading — and the
one this prompt recommends — is that **a grounding session does the derivation and drafts the new
version; the founder adopts it.** Do not treat drafting as adoption, and say so in your close.

**Minimum that must be re-derived first-hand, not quoted:** buffer size + window population + the
per-UTC-day consult/guard split + baseline days; **both SHA pins**; the byte-identity guard **run**;
R20a route-level + substrate-gate counts **from the registry arrays**; agent-card extensions **from
the file**; the PR range **by enumeration**; `vercel.json` crons; `git status` whole; `ListAgents`.

**Facts that must land in the new version and are absent or wrong in 2026-09-08:**
- The three corrections in the table above.
- **D2 is NOT unblocked.** Counting completion is **necessary but not sufficient** (Q1 ruling).
  Whether the instrument-change disclosure suffices for the flip is **the 0h call, the founder's.**
- The Option C′ gate is **discharged**; the build is live; `Q-CALLER-BUILD` is no longer "NOT LICENSED".
- **TRAP-1** (`GUARD-OUTAGE` lines carry no `tool=` — match on token + `session=`) and **TRAP-3**
  (**"record N" in this project's records is 1-INDEXED**; the probe is 0-based **138**, the window is
  0-based **139 onward**) belong beside the existing `CONSULT`/`CONSULT-OUTAGE` prefix trap.
- The guard log token is **lossier than the buffer** — `GUARD-CAUTION` covers both
  `pause_for_review` and `proceed_with_caution`; for caution **grade**, the buffer is authoritative.
- **F-K is resolved in recommendation:** do **not** extend the window for more v6 consult days;
  tool-mode routing, if wanted, is *"a deliberate founder-visible setting, not an auto-mode outcome."*
- The `~/.sage-gate1/*-stdin.json` residue state (CLAUDE.md's S9 block remains uncorrected by design).

---

## 2. Task B — re-derive the mentor's last open carried item

**The *"51 guard records, 1 consult record, a full build session"* figure**, attributed to the
Cognitive OS session, is **the only one of the mentor's two carried items still open**. It has been
carried through three sessions and **re-derived by none**. The mentor's own note says a future session
*"should re-derive it rather than quote it from here."*

**Method that works** (per-session attribution, which no session has yet done): group buffer records
by their own `session` field — **never by an ad-hoc time filter** — and split guard vs consult by
`path == 'guard'`. Correlate to `gate1.log` by `session=` if you want a second source.

**Report what you find, including "the figure does not reproduce" if it does not.** Do not force
agreement, and do not quote the mentor's number as the answer.

---

## 3. Task C — surface, for founder/mentor decision only

**A figure inside the Q1 ruling is off by one.** The ruling designates the schema distribution as
**required pre-flip content** and states *"v3=47, v4=96, v5=135, v6=6"*. **`v4` is 97, not 96** —
provable by arithmetic: the buffer is append-only, so its first 423 rows are that state and sum to
**exactly 423** only with v4=97; the ruling's set sums to **422**. Provenance: the verification
session's **close had 97**; its **register row had 96**; the ruling adopted the register row.

**Do NOT edit the verbatim ruling** — it is canonical and not a session's to amend. The decision
(erratum? a note on the pre-flip report? nothing?) is the founder's and the mentor's.
**Re-verify the arithmetic yourself before relaying it.**

---

## 4. Hard constraints

- **The byte-identity guard is ARMED** (the window is running). **No `GUARD_RE` file may sit modified
  in the working tree**, and both SHA pins must stay clean. Nothing in Tasks A–C requires touching one.
- **Do NOT open `Q-PREFLIP-REPORTS`.** The ruling **adds required content** to it; it does **not**
  license building it. `false-hold-observation-report.ts` matches `GUARD_RE` ⇒ **founder waiver + its
  own session.**
- **Do NOT treat D2 as unblocked**, and do not extend the window to improve its composition.
- **Never "refresh" the buffer.** Append-only; never truncated.
- **Path-scoped commits, always** — `git commit -- <explicit paths>`. A bare `git commit` commits the
  **entire index**, including a peer's staged work. Verify with `git show --stat` / `git diff --cached
  --stat` that **nothing beyond your own paths** is present, not merely that yours are.
- **Disclose your tool mode.** A Bash-authored session contributes **guard records but zero consult
  records**. Choose on the task's merits; never *because of* the measurement effect.

## 5. What is in the tree that is not yours

At this prompt's authoring, several files from **other, closed sessions** sit uncommitted — the
cognitive-os arc, the condition-3 arc, a peer's `environmental-context.json` scan, and S10's working
notes. **None matches `GUARD_RE`. Never stage another session's files.**

## 6. If you hit a conflict

**Route it; do not resolve it.** Governed surface, settled constraint, or `GUARD_RE` file ⇒ stop and
flag. The founder is the decision authority on all governance questions.

**D2 REMAINS BLOCKED. The S11 flip remains REFUSED. Weights remain BLOCKED. The 0h call remains the
founder's.**
