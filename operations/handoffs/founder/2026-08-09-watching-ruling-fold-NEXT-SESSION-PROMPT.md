# Next-Session Prompt — Fold the mentor's ruling on `watching` into the scope document (third item of the ruled post-brief sequence)

> **SPENT — 2026-08-09, same day it was authored.** The mentor's ruling arrived in the same conversation that authored this prompt, so the fold executed there rather than in a fresh session (the `fresh` same-session precedent): ruling recorded verbatim (`operations/agent-circles-2026-08/2026-08-09-mentor-consultation-watching-scope-rulings-verbatim.md`), RULED annotations folded into the scope document, mirror updated, generation-step scope unblocked. Close: `operations/handoffs/founder/2026-08-09-watching-ruling-fold-CLOSE.md`. Decision: `D-WATCHING-SCOPE-RULED-2026-08-09`. **Do not paste this file into a new session. For the actual next session, use `operations/handoffs/founder/2026-08-09-generation-step-scoping-NEXT-SESSION-PROMPT.md`.** The text below is retained as the historical record.

**Stream:** founder.
**Tier:** `governance` / design (fold-in-a-ruling, same shape as the `fresh` ruling-fold) — Standard risk under 0d-ii. **No code, schema, flag, credential, or public-surface change. No build.**
**Governing frame:** `/adopted/standing-protocol-cache.md`.
**Predecessor close:** `operations/handoffs/founder/2026-08-09-watching-per-cycle-record-table-scoping-CLOSE.md`.
**Predecessor decision entries:** `D-WATCHING-PER-CYCLE-RECORD-TABLE-SCOPED-2026-08-09`.
**Risk classification:** Standard under 0d-ii. Critical Change Protocol NOT engaged.

## Hard pre-condition — check this FIRST, before anything else

**Does the founder have the mentor's ruling on the `watching` scope document in hand?**

- **If NO:** this session has nothing to do yet. Say so plainly, do not proceed to Part A/B below, and end the session. Per the Q11 sequence, the generation-step scope document does not open — and this fold cannot happen — until the ruling arrives. Do not draft, guess, or pre-empt what the ruling might say.
- **If YES:** proceed to Part A.

## Why this session matters

The `watching` scope document (`operations/agent-circles-2026-08/2026-08-09-watching-per-cycle-record-table-scope.md`) was offered for mentor relay at the close of the predecessor session, with three open questions: **QW-A** (the `dependency_unavailable`/fallback-counter distinction — carried from the `fresh` ruling, genuinely re-verified open — plus a newly-surfaced cycle-level semantics gap), **QW-B** (the write path's capability class), **QW-C** (the cycle-level timeout token's spelling). This session folds whatever the mentor rules into the scope document, in the same pattern the `fresh` session used the same day: proposal prose kept, marked RULED rather than deleted, each ruling cited to a verbatim record.

## Pre-conditions

1. The two records commits from the predecessor arc are pushed and Vercel is green (founder-confirmed at open) — `36e42bd` (watching scope + PR19 fold), `9678a76` (the mentor's separate Website Image Use Policy / Human Creator Protection Commitment instruction, Part One + Two), `893e71e` (the resulting cache-drift fix). None of these three touch the `watching` scope's content or the Q11 sequence's gating — confirm this is still true at open (re-read the `watching` scope document's current state; it should be unchanged since the predecessor close).
2. The mentor's ruling on the `watching` scope is in hand (the hard pre-condition above).
3. No new mentor guidance has superseded the Q11 sequence itself, or the `fresh`/brief rulings that precede it — confirm with the founder at open.

## Part A — Open under the protocol

Read in order:
1. `/adopted/standing-protocol-cache.md` (~3 min).
2. This session's predecessor close (above).
3. The `watching` scope document in full (`operations/agent-circles-2026-08/2026-08-09-watching-per-cycle-record-table-scope.md`) — its current, un-ruled state; the target of this session's edits.
4. The mentor's ruling, as relayed by the founder — record it verbatim FIRST (its own file, `operations/agent-circles-2026-08/2026-08-09-mentor-consultation-watching-scope-rulings-verbatim.md` or equivalent dated name), before folding anything into the scope document. Verbatim wins over every paraphrase, per the standing discipline.
5. The `fresh` ruling-fold precedent (`operations/agent-circles-2026-08/2026-08-09-fresh-novelty-endpoint-scope.md`'s RULED annotations, and its verbatim record) — the pattern to mirror: status header updated, inline RULED note under each affected section, `§3` open-questions list rewritten to show every question ruled, `§4` restates the sequence advance.

Confirm at open: tier; hold-point status (P0 0h); status vocabulary; signals + risk classification.

## Part B — Procedure

### Step 1 — Record the ruling verbatim
Own dated file, front matter states "verbatim wins," cites the scope document it responds to.

### Step 2 — Fold the ruling into the scope document
Inline RULED annotations under §2.3 (QW-A's territory), §2.2 (QW-B's territory — the write capability class proposal), §2.2 (QW-C — the timeout token), and §3 (rewrite the open-questions list to show all three ruled). If the ruling amends anything beyond the three named questions (as the `fresh` ruling did for the LOCUS-2 precision note), fold that too, citing the exact ruling text.

### Step 3 — Update the architecture-map mirror
`operations/architecture-map-2026-08/06-PLAIN-TEXT-MIRROR.md` — the `watching` item's status line, mirroring how `fresh`'s ruling was reflected there.

### Step 4 — Decide whether the generation-step scope document opens now
Per Q11: *"generation-step scope document does not open until watching is ruled."* If this session's fold completes cleanly (no new genuinely-open question the ruling itself surfaces — check for this the same way the predecessor session checked the `fresh` ruling's own carry-forward pointer, by re-verifying against the corpus, not trusting the pointer), the generation-step scope document is unblocked. **Author its own next-session prompt as this session's final deliverable** (do not scope the generation-step content itself in this session — that stays its own, later, session, per the Q11 boundary). If the mentor's ruling on `watching` itself names a new carry-forward (as `fresh`'s ruling did for `watching`), carry it forward explicitly and re-verify it against the corpus before trusting it, exactly as the predecessor session did.

### Step 5 — Adversarial review (PR19), if the fold is non-trivial
If the ruling only confirms the three proposals as-stated (no amendments), a lighter-touch review may suffice (disclose the reasoning for skipping the full 3-dimension workflow). If the ruling amends anything (as happened with `fresh`'s Q-C wiring detail), run the full independent review before offering the folded document as final, per PR19.

### Step 6 — Records
Decision-log entry (lean form, citing every RULED item to the verbatim record); session close; the generation-step scope document's next-session prompt (if Step 4 unblocks it) or an explicit statement of what remains blocking it (if not).

## What this session does NOT do

- Does not scope the generation-step content itself (heuristics, prompt structure, friction-detection threshold, `randomOffsetPercent`, the guardrail fail-closed handling) — that is the document Step 4 may unblock, not this session's own deliverable.
- Does not build anything — no code, schema, flag, or credential change.
- Does not touch the two registered defects, C1c-original, D4, the Stoa activation, W1–W3, B6, or the permission-layer items 14–17 (including the Human Creator Protection permission-layer constraint named 2026-08-09 — carried, not resolved, until those items themselves open).
- Does not re-open anything already ruled in the brief or in the `fresh`/`watching` scopes beyond what this specific ruling addresses.
- Does not proceed at all if the mentor's ruling is not yet in hand (the hard pre-condition).

## Rollback path

Documents only — `git revert` the records commit.

## Forecast

Success = the `watching` scope document fully RULED and folded, faithful to the verbatim record, with any genuinely new carry-forward re-verified rather than assumed; the generation-step scope document unblocked and its own next-session prompt authored, or the specific remaining blocker named if it is not yet unblocked.

End of prompt.
