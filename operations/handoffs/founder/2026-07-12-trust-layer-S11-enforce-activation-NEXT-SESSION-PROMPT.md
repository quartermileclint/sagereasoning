# Next-Session Prompt — Trust Layer S11: the ENFORCE activation (the logos gate)

**For the founder. Paste as the first message of a fresh session.** (Rename the date prefix to the actual session date.)

**Stream:** founder. **Tier:** `code-elevated` for any build → **`code-critical` at the flip** (binding enforcement on the reference harness; AC7 + PR6 + PR17 engage; the FULL Critical Change Protocol 0c-ii; the founder walks every flag/live step).
**Governing frame:** the two caches. **Design-of-record:** ADR-013 (§3 row 7 + §7 the gate state + §11 G6) + ADR-012 (the three-use ladder — this session activates the SECOND rung, logos-enforce; the weights rung stays BLOCKED) + the build plan §S11. **Predecessor close:** the S10 close (`operations/handoffs/founder/2026-07-12-trust-layer-S10-public-read-surface-CLOSE.md`).

## The gate, honestly stated (confirm at open)

ADR-012's logos-enforce **activation condition is DISCHARGED** (the corroboration check is Live on both surfaces since 2026-07-08; its both-directions battery cleared 0/0/0) — **but nothing pre-approves this flip.** S11 is its own founder-walked Critical decision, re-confirmed at flip time. If the founder does not affirm the gate at open, the session stops there and records the deferral (PR7).

## Pre-conditions (confirm at open)

1. **The S10 founder walk is DONE** (the S10 close §Founder Walk: `SUBSTRATE_TRUST_READ_SURFACE_ENABLED=true` + redeploy; live smokes green — the s9-loop record 200 with envelope, unknown-agent 404; the staged R18 docs applied + pushed; agent-card 17 extensions). If not, RUN IT FIRST.
2. Batteries at the S10 standing counts (S10 **106/0** · emission-hooks 15/0 · S9b 86/0 · S8 146/0 · S1 97 · S2 87 · S3 106 · S4 417 · S5 87 · S6 84 · S7 122 · hooks 104/0 + 230/0 · the reflect/erasure suites per the S10 close).
3. Read the S10 R18 sign-off memo (`operations/trust-layer-2026-07/2026-07-12-s10-r18-signoff-memo.md`) — its CARRIED items are this session's decision inputs — AND the standing register's S10 addendum (`operations/trust-layer-2026-07/2026-07-11-preactivation-safety-audit-report.md`, end of file), which now carries **`reflect-store owner-scoping`**: the item BLOCKS any external multi-tenant onboarding until the owner-scoping schema step (covering the export §2e rider AND the S9b delete leg together) lands; any onboarding review consults that register first.

## Scope (build plan §S11 + ADR-013 §11 G6)

Flip the intervention engine from log-and-continue to **binding on the reference harness**: guard-deny per the S4 decision table (any violated obligation → do-not-proceed; reflexive → do-not-proceed + escalate; habitual → pause bounded by A8's two-then-escalate; conflict → pause, never average); per-deployment oversight routing live; **R20c human-override supremacy stated as a binding term of the contract** (every recommendation stays humanOverridable; a deny is overridable by the human operator, never hardened against them). Plus the two G6 line-items (mentor-specified, binding): **(a)** an unclosed correction loop produces a do-not-proceed on the triggering action until the re-examination completes (the A8 bound — never an infinite loop); **(b)** the write boundary holds the trust-record update pending on an action whose correction loop is unclosed — update and action release together. Plus the S9b-built **calling-gate ENFORCE arm** (`GATE1_CALLING_GATE_MODE=enforce` — dark since S9b; its activation is part of this flip or explicitly deferred, founder's election).

## Elections at open (AskUserQuestion)

1. **Enforce scope:** full decision-table binding on the reference harness vs a staged subset first (e.g. violated-obligation + reflexive rows only, then the pause rows) — PR1 single-endpoint-proof instinct applies.
2. **The A2/PA-10 decrease-surface decision** (carried from S9b + the S10 memo): whether the A2 omission class + the client-gated decrease surfaces get an enforce-arm treatment or stay MEASURE-disclosed.
3. **PA-5 API-path hardening** (carried from S10): gate the direct `/api/practice/reflect` mint path on `verbatim_provenance` (or equivalent) so an unharnessed agent cannot accrue a reflect history at will — now that reflect modulation sits under an ENFORCE regime, the bound tightens.
4. **PA-10/PA-11 structural closure:** wire the S2 recency confidence tier into the fold (the mentor-A5-shaped replay closure) before/with the flip, or carry with disclosure.
5. **The G5 per-domain depth refinement** (aggregate-keyed today, conservative): refine now or carry.
6. **Calling-gate enforce arm:** flip with the engine or defer.

## Procedure

Reads (caches, S10 close, memo, ADR-013 §3/§7/§11, ADR-012) → the gate affirmation → elections → build dark (enforce wiring flag-gated; every new binding path battery-pinned incl. the A8 bound termination + the G6 hold-and-release pair + R20c overridability) → adversarial review (Workflow; refuters; §4 first-hand fallback; route to `model:'opus'` if the Fable pool exhausts) → **the founder-walked flip** (TEST-first election; flag(s) + redeploy + live-fire on a throwaway loop proving a deny binds and an override overrides; teardown) → records + the close. **Weights BLOCKED throughout; no public claim changes without its own R18 pass.**

## Rollback

Unset the enforce flag(s) + redeploy ⇒ MEASURE exactly as before (flag-off byte-identity battery-asserted); the harness keeps its pre-existing irreversible-action guard either way. `git revert` for any docs.

End of prompt.
