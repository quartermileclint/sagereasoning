# Next-Session Prompt — Scope C2 and C1c (item 4, standing binding sequence)

**Tier:** `governance`/`code-elevated` — a scope document, no code written, following the same pattern as the two prior sessions this week (`OikeiösisGap`/`GeneratedCandidate`, then the IDEA loop configuration shape + shared task-list storage). **Read first, in order:**

1. `operations/decision-log.md` — every `D-IDEA-LOOP-*` and `D-OIKEIOSIS-GAP-*` entry, most recent first, back through `D-IDEA-LOOP-PREBRIEF-RULINGS-C2-WIDENED-2026-08-05`. **Verbatim wins over this prompt's summary below.**
2. `operations/architecture-map-2026-08/06-PLAIN-TEXT-MIRROR.md` §"Sixth element" — items 2b and 3 (C2's own scope, C1c), the four notes just added to items 2a/9/10/11, and the current state of the binding-sequence prose (step three through the current close).
3. `operations/agent-circles-2026-08/2026-08-06-oikeiosis-gap-generated-candidate-type-scope.md` — the approved `OikeiösisGap`/`GeneratedCandidate` shapes, including the same-day `cycleOutcome`/`unavailableDependency` amendment. C2's generative-prompt field is written to feed the generation step these types describe, so this is required context, not background.
4. `operations/agent-circles-2026-08/2026-08-06-idea-loop-configuration-and-shared-task-list-scope.md` — for the pattern (recommend, flag genuinely open questions, bring to the mentor) and because item 2a's `sessionId`/`loopId` note is directly relevant if C2's orientation reading or C1c's trust-event type end up naming a session or loop identifier.

## What this session is

Per the ruled sequence: **C2 builds first, C1c second, both scoped together before either builds** (`D-IDEA-LOOP-PREBRIEF-RULINGS-C2-WIDENED-2026-08-05` — C2's orientation reading produces the signal C1c's trust-event type describes, so C1c's schema cannot be written first). Item 2b widened this to a **three-component scope document**, all three authored together in one session because they feed one IDEA-loop cycle and need compatible outputs:

1. **The orientation reading itself (C2, part i)** — the Layer-1 signal it reads, the threshold condition for firing, the Layer-3 prose framing, and the trust-event type C1c will write to record it. This is the "fifth-circle orientation reading" — detecting when reasoning stays confined to a narrower circle than the cosmopolis/fifth circle when a wider circle was genuinely available.
2. **The generative-prompt field (C2, part ii)** — a single plain-language sentence naming the gap the reading detected, never a prescribed action. This is the seed the IDEA loop's generation step consumes as its `OikeiösisGap.targetCircleMeaning` (or the input that produces it — confirm the exact connection point, don't assume).
3. **The novelty detection specification (C2, part iii)** — structural novelty only for this build: querying the existing history table for circle/virtue-domain-combination frequency, using the existing 3-occurrence evidence floor already established in `trajectory-delta.ts`. Scoped alongside the orientation reading because both feed the same cycle and need compatible output shapes.
4. **C1c (item 3)** — the trust-ledger event classes for a circle-4 (or, per the widened framing, more generally a circle-N) failure — i.e., the trust event C2's orientation reading writes when it fires. This is item 3's own numbered item but is scoped in this same session, per the ruling.

## What this session should NOT do

- Does not build any of the above — scope only, then bring the three-component document to the mentor.
- Does not touch D4 (the trust-ledger reducer divergence) — independent, its own track, runs in parallel.
- Does not touch the Stoa activation (independent, founder-walked whenever elected).
- Does not open the generation step's own scope document — that item is now unblocked (all three of its prerequisites closed 2026-08-06) but is not this session's task; C2+C1c is the standing next item regardless.

## A founder decision to make before or at session open

Two independent items are both available to sequence next: **C2+C1c** (the standing next item in the numbered sequence) and **the generation step's own scope document** (now unblocked, having waited on the IDEA loop configuration shape + shared task-list storage, both closed 2026-08-06). Per the architecture map's own notes: "Independent of steps four through twelve — runs in parallel, converges only at the generation step" (step thirteen's closing sentence) — so scoping the generation step does not need to wait for C2/C1c, and C2/C1c does not need to wait for the generation step. **This prompt defaults to C2+C1c** because it is the item explicitly numbered "four" in the standing binding sequence and has been "next" since 2026-08-05; if the founder prefers to open the generation step's own session instead (now that its three prerequisites are all closed), that is equally available and this prompt's context-reading list (items 1–4 above) still applies, with the heuristics document (`2026-08-05-idea-loop-generation-heuristics.md`) added as required reading instead of items 2b/3 of the architecture map.

## What "bring to the mentor" means here

Same established pattern as the two prior sessions this week: author the shape, name the connecting architectural surfaces at mechanism level (PR20), flag any genuine open question rather than resolving it silently, bring the whole three-component document to the mentor before C2 or C1c is built. Given the density of rulings issued this week, keep the document itself focused — cite, don't re-derive, anything already settled (the `OikeiösisGap`/`GeneratedCandidate` shapes, the examination-cost/null-cycle rulings, the `dependency_unavailable` addition, the external-only configuration and task-list rulings).
