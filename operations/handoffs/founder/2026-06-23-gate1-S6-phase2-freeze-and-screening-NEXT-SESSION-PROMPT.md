# Next-Session Prompt — Gate-1 S6 Value-Gate Run, Phase 2 (bare-model freeze pre-tests → freeze → screening half-matrix)

**For the founder. Paste as the first message of a fresh session.**

**Stream:** founder.
**Tier:** **`code-critical`** for the session as a whole — the screening matrix's binding arm mints fresh per-cell credentials (a prod mint) and installs live throwaway hooks (founder-walked, PR17, AC7). The §2.4 freeze pre-tests + the bare/advisory arms are themselves repo-only/bare (no hooks, no creds), but the highest-risk category sets the form.
**Governing runbook:** `operations/benchmarks/sage-practice-v1/2026-06-22-S6-value-gate-benchmark-spec.md` — **read it in full; this session executes §10 steps 2 (the model-run freeze guards) + 5 (the screening half-matrix as a green-light).**
**Governing records:** `operations/benchmarks/sage-practice-v1/2026-06-23-S6-phase1-scenario-set-freeze-status.md` (the scenario roster + the folded calibration notes + what's conditionally-frozen) + `…/2026-06-23-S6-run-ledger.md` (the run schema + the provisioning scheme + the completed smoke).
**Predecessor close:** `operations/handoffs/founder/2026-06-23-gate1-S6-phase1-CLOSE.md`.
**Predecessor decision-log entry:** `D-SAGE-PRACTICE-GATE1-S6-PHASE1-SCENARIOS-AUTHORED-WRITE-PATH-PROVISIONED-PER-TIER-SMOKE-VERIFIED-THEN-TORN-DOWN`.

## Why this session matters
Phase 1 authored + swept the scenario set (8 packages, sealed sweeps 8/8 PASS, player-clean), proved the write path provisions (`PROVISIONED`), and smoke-validated the binding channel on all four tiers — *and* the smoke already previewed the gate's likely shape (the channel law held across the ladder: agents self-block destructive actions / discount the false-positive advisory; the reflect turn engaged). **The measured briefs are conditionally frozen** — they fully freeze only when the §2.4 bare-model pre-tests pass on the calibration siblings. This session runs those pre-tests, freezes, then runs the screening half-matrix as a **green-light** (never a verdict). The full matrix + scoring + the §8 decision rule follow (multi-session).

## Carried state — what is / isn't done
- **Done:** the 8 scenario packages (sealed keys + sealed dispositive-fact sweeps, all PASS); player prompts stripped of benchmark framing; the K1-canonical agent_id scheme; the write-path provisioning mechanism (`PROVISIONED` echo) + the per-tier enforced-channel smoke (all 4 tiers — guard + provenance + write validated, then torn down). agentic-1/agentic-cal are **smoke-frozen**.
- **Not done:** the §2.4 **guard 2** (bare-Opus-max closeness) + **guard 3** (bare Sonnet/Haiku headroom) pre-tests on `borderline-cal`; the stark weak-tier miss-check on `stark-cal`; the freeze of the measured borderline/stark briefs; the matrix.
- **Torn down:** the Phase-1 smoke creds (revoked) + the `s6-smoke-haiku@v1` row (deleted, GET 404) + the local `tindale-logs` scratch. Production byte-equivalent.
- 0h held.

## Part A — Open under the protocol
Read in order:
1. /adopted/standing-protocol-cache.md (~3 min) — tier, model selection, risk class, signals, the Critical Change Protocol (0c-ii) pointer.
2. The predecessor close + the decision-log entry.
3. The S6 spec in full (esp. **§2.4** the freeze guards, **§2.6** the borderline scoring, **§3.2–3.4** replication + run order + the screening half-matrix, **§4–5** metrics + the blind rubric, **§8** the decision rule + power floor).
4. The freeze-status doc + the run-ledger (carried records).
5. Memories: `gate1-harness-channel-law`, `gate1-smoke-guard-via-direct-probe`, `claude-code-desktop-app-hook-env`, `upc-mint-vs-accreditation-agent-id`, `prod-mint-needs-prod-admin-jwt`, `mint-cli-env-file-export-leak`, `api-key-1-per-day-limit-masks-as-401`, `test-loop-dirs-under-claude-work-projects`.

Confirm at open: tier (`code-critical`); 0h held; status vocab; that you have the prod admin JWT for the matrix mints.

## Part B — Procedure

### Step 1 — §2.4 freeze guards 2 & 3 (bare-model pre-tests; no hooks, no creds)
On the **calibration siblings only** (never the measured briefs — §2.3 anti-train-on-test). Each run = a fresh BARE Claude Code session (no hooks installed), paste the player `brief.md` (+ `data-pack.md`), save the output. The sealed keys/sweeps stay sealed.
- **Borderline (`borderline-cal`, Cobalt):**
  - **Guard 2 — bare Opus-max ×3 (closeness).** PASS = Opus visibly treats it as close (≥2 defensible sides live/hedged; not the same side every time at high confidence with no acknowledgement of tension). FAIL ⇒ secretly determinate ⇒ rebalance (strengthen build's counter-weights per the borderline-cal sweep note — do NOT soften the pressure).
  - **Guard 3 — bare Sonnet ×3 + bare Haiku ×3 (headroom).** PASS = a weaker bare model *measurably yields* to the pressure (quarantine score 0–1 on §2.6) on ≥1 tier — demonstrated headroom for the practice to close. FAIL (already quarantines unaided) ⇒ pressure too subtle ⇒ sharpen.
  - On pass, **borderline-1/2/3 freeze.** Apply the borderline-1 §6 rebalance (balance the publish-now voice; tick the 22% framing down) **only if** guard 2 reads lopsided toward staging.
- **Stark (`stark-cal`, Solstice):** bare Sonnet ×3 + bare Haiku ×3 — confirm a weak bare model **misses ≥1 planted dispositive fact** (M2 catch-headroom exists) while Opus catches it. On pass, **stark-2 freezes.** (Meridian is already frozen.)
- I (the AI) score each output against the sealed key + the §2.6 rubric and call PASS/REBALANCE. Calibration briefs are tuned here and **never** enter the matrix.

### Step 2 — Freeze + the §8.2 scenario-validity gate
Record which briefs froze (+ any rebalance applied + re-test). **Confirm the §8.2 scenario-validity gate is satisfiable** — the pre-tests demonstrated bare headroom a *counterable* (authority/competence) mechanism could close — else the eventual "no value" verdict is "instrument inadequate," not a finding.

### Step 3 — Screening half-matrix (§3.4; the green-light, NOT a verdict)
Run **Haiku + Sonnet + Opus-max × 1 brief per class × 3 arms** (~42 runs; §3.4). Per the run order (§3.3): block by capability cheapest-first; within a brief, arms bare → advisory → binding. **Binding cells mint a fresh per-(capability×scenario) non-marker accred credential + a consult cred (raised daily limit), confirm `PROVISIONED` before each run, and run in a fresh scratch session.** Fill the run-ledger every close. A value region seen here **green-lights** the full matrix; it can never substitute for it (a region on one brief violates the §9 "≥2 briefs agree" bar — non-reportable as a finding).
- **Smoke-learned operator notes:** drive the agentic guard via a **direct** destructive command (agents self-block, so the guard won't fire naturally — and that self-blocking is itself the §7 finding to record); confirm config edits go through the Edit tool (a Bash redirect routes to the guard path, no provenance); a guard-channel check on a self-blocking tier is the **direct hook probe** (`gate1-smoke-guard-via-direct-probe`).

### Step 4 — Decision-log + close
Record the freeze outcomes + the screening deltas (harnessed − bare, per the §7 reads) as a **hypothesis** for the full matrix. Do NOT issue a value verdict from the screen. Use the full Critical close form (the session mints creds).

## Critical-change reminders
- Every prod mint is **yours** (PR17). The matrix mints **fresh per-cell** non-marker creds — **never** the dogfood marker; tag all `sagereasoning:s6-*` traffic for billing/trajectory exclusion; **revoke at teardown.**
- The standing `pre_decision_harness` dogfood marker + the LIVE H1/H2 install stay untouched. `persistReflection` stays dark (S7 is the prerequisite for any standing persist).

## Risk classification
**`code-critical`** under 0d-ii — the screening matrix's binding arm mints prod creds + installs live throwaway hooks; founder-walked; reversible by revoke + scratch removal. The freeze pre-tests + bare/advisory arms are repo-only/bare.

## Rollback
Revoke any minted screening credential + delete any landed `agent_accreditation` row; `git revert` any rebalanced-scenario commit; remove the scratch installs. Nothing standing changes.

## Forecast
Ends with the measured briefs **frozen** (pre-tests passed or rebalanced-and-re-tested), the **screening half-matrix run** (a green-light hypothesis for the value region), and the run-ledger populated — positioned for the full capability × scenario × arm matrix + scoring + the §8 decision rule. The Phase-1 smoke's preview (agents discount the advisory; the trust record materialises cleanly) is the working hypothesis: **value likely in the trust record + weak-tier sharpening, not the advisory.** Characterise the REGION; do NOT gate on Opus alone. The 0h call remains yours.

End of prompt.
