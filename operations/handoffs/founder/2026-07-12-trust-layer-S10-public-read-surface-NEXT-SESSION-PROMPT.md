# Next-Session Prompt — Trust Layer S10: the public trust-record read surface (native, interop-shaped)

**For the founder. Paste as the first message of a fresh session.** (Rename the date prefix to the actual session date.)

**Stream:** founder. **Tier:** `code-elevated` for the build → **`code-critical` at activation** (a NEW public read surface; R18 governance sign-off REQUIRED before anything publishes; AC5 re-check mandated by the S8 R20a recorded decision).
**Governing frame:** the two caches. **Design-of-record:** ADR-013 (§3 row on S10, §8 the honest-claims envelope — the surface must stay INSIDE it) + the build plan §S10. **Predecessor close:** `operations/handoffs/founder/2026-07-12-trust-layer-S9b-practice-completion-CLOSE.md`.

## Pre-conditions (confirm at open)
1. The S9b close commit is pushed AND **the S9b founder walk is DONE** (the close §Founder Walk: migration TEST→prod §VERIFY green; the two flags; the capability widening; the persist flip; both smokes). If the walk has not run, RUN IT FIRST — S10 reads the records the walk activates.
2. Batteries at the S9b standing counts (S9b 80/0; S8 146/0; hooks 104/0 + 230/0; the rest per the close).
3. **The `fix_before_s10` register GATES this session's R18 sign-off** — read it whole: PA-5, PA-6 (§8 envelope narrowing), PA-10 (now incl. the suppression-watch screen-caught exception), PA-11, the A7-dead-code note, F-1, plus the S9/S9b findings (F-CONF; the A2-as-decrease-surface note; the G5 per-domain refinement; `/api/user/export` reflect rows).

## Scope (build plan §S10)
A public GET (accreditation-style) serving per-domain trust levels + confidence + coverage + **the honest-claims envelope verbatim** (what a trust record attests / does NOT: the extraction-trust ceiling, the A2 self-report-omission class, the A9 case-3 uncatchable class, the PA-10 replay class, no future-behaviour prediction). Schemas VC-mappable/A2A-extension-shaped (design-for, publish nothing external). EU Art 12/14 framing in docs. R20a/AC5 re-check (agent-facing read; outside the human-distress perimeter — re-confirm per the S8 precedent). Discharge or explicitly carry every register item in the R18 sign-off memo. Founder elections at open: surface path + auth posture (public-unauthenticated like the accreditation GET vs credential-scoped); which register items block vs disclose.

## Procedure
Reads (caches, close, ADR-013 §3/§8, the register) → elections → build dark (flag-gated route + payload composer + battery) → adversarial review (Workflow; refuters; §4 fallback — route review agents to `model:'opus'` if the Fable pool exhausts) → the founder-walked activation (flag + R18 docs on the three surfaces) → records + the S11 prompt.

## Rollback
Flag unset ⇒ 503 (dark); `git revert`. **ENFORCE is S11.** Weights BLOCKED; the 0h call remains the founder's.

End of prompt.
