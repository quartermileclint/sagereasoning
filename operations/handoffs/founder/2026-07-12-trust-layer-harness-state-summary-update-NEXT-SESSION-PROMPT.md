# Next-Session Prompt — Update the Trust-Layer Harness state summary for the post-enforce end-state

**For the founder. Paste as the first message of a fresh session.** This session brings the founder-facing harness summary current (a lot has landed since it was written on 2026-07-11) and recasts its forward-looking sections around the capabilities and opportunities that open up **after the 7-day observation review completes and — assuming the S11 enforce flip works as intended — the trust layer begins to BIND, not just measure.**

**Stream:** founder. **Tier:** `governance` — documents only (updating one summary doc). Standard risk under 0d-ii; no code / flag / schema / mint / deploy; AC7 not engaged; production byte-equivalent.
**Governing frame:** the two caches. **Design-of-record:** ADR-013 (esp. §7/§11 — the readiness standard + the binding S11 shape) + ADR-012 (the three-use ladder; weights BLOCKED). **Binding verdict:** `operations/trust-layer-2026-07/2026-07-12-mentor-consultation-s11-enforce-gate-verdict-verbatim.md`.
**The document to update (in place):** `operations/trust-layer-2026-07/2026-07-11-trust-layer-harness-completed-state-summary.md`.

## Why this session matters

The summary is the clearest single founder-facing statement of what the trust layer *is* and *offers*. It was written while S1–S8 were built-dark and S9–S11 were planned. Since then: **S9 dogfooded LIVE, S9b LIVE, and the S10 public trust-record read surface is LIVE (all under MEASURE); S11 (enforce) was examined and DEFERRED on mentor counsel with a four-part readiness standard; and the false-hold observation instrument is now built and the 7-day clock is about to run.** The summary's "where the build stands today" section is stale, and its forward-looking sections don't yet reflect how close the enforcing end-state now is — nor the *specific, binding shape* the mentor set for it. This session makes the document both accurate about today and sharp about the post-enforce payoff, honestly conditioned.

## Part A — Open under the protocol; get current

Read in order:
1. `/adopted/standing-protocol-cache.md` + `/adopted/build-sessions-protocol-cache.md` (~5 min — tier, model, risk, signals; the AI-failure-mode table, esp. method/altitude-before-purpose).
2. `CLAUDE.md` — the **Live in production** list + the 2026-07-12 refreshes (S9 dogfood-install-live · S9b · S10 read-surface + walk · S11 deferral · the observation instrument). This is the current-state ground truth.
3. The recent closes, in order: `2026-07-11-...S9-dogfood-install...`, `2026-07-12-trust-layer-S9B-...CLOSE`, the S10 walk close, `2026-07-12-trust-layer-S11-enforce-gate-mentor-deferral-CLOSE.md`, `2026-07-12-trust-layer-S11-observation-instrument-CLOSE.md`.
4. The binding S11 verdict (verbatim) — for the exact enforce shape (Q1 readiness standard; Q2 staged; Q3 the G6(a) kathekon-engagement qualification; Q4/Q5 enforcement-claim bounds; Q6 calling-gate-with-engine; Q7 aggregate-keyed depth).
5. ADR-013 §7 + §11 (the amendments) + the build plan §S9b/§S10/§S11.
6. The document to update, in full.

Confirm at open: tier (`governance`); the current state (S1–S10 + S9b **Live under MEASURE**; the public read surface **Live**; S11 **deferred, readiness-gated**; the observation instrument **built, clock pending the founder's flag**); that **nothing here pre-approves the enforce flip** — this is a documentation update, and the post-enforce state it describes is **conditional** (assumes the 7-day review clears the four-part standard AND S11 works as intended).

## Part B — The update (revise the summary in place)

Keep the document's shape (its section structure, its `[LIVE]`/`[BUILT-DARK]`/`[PLANNED]` tag discipline, its honest-bounds framing) and its founder-facing register. Bump the date to 2026-07-12 and note the revision. Make these changes:

1. **§7 (build state) — bring it fully current.** S1–S8 are no longer "built-dark, planned S9–S11." The table should read: examination engine / corroboration check / guardrail / accreditation / data rights / retention — LIVE; **the trust core (S1–S7) + the reference harness (S8) + S9b (calling gate, screened reflection, Gate-2 elicitation, depth calibration) — LIVE under MEASURE in the founder's dogfood loop** (`sagereasoning:s9-loop@v1`); **the S10 public trust-record read surface — LIVE (MEASURE)**; **S11 ENFORCE — DEFERRED, readiness-gated** (the four-part standard + the staged/Q3-qualified binding shape); **the false-hold observation instrument — BUILT, the 7-day clock pending the founder's capture flag.** Add a new tag if useful (e.g. `[LIVE-MEASURE]`) to distinguish "running and recording" from "binding."

2. **Add the S9b capabilities** the current doc predates (§2 + §4): the SessionStart **calling gate** (a purposeless agent is routed to a calling session before its first task — the precondition for a non-circular examination); **screened reflection** (the forced close-turn + verbatim persist, examined out-of-band Q1–Q6 against the persist — the L4 principle applied to reflection); the **Gate-2 structured elicitation** on a consequential action (captured out-of-band, examined deterministically, calibrated to the *causal signature* never output content); **trust-calibrated depth** (reflexive ⇒ deep required); the three new trust events (`calling-completed`, `reflect-screened-honest`, `self-screen-absent`).

3. **§4 (the S11 end-state) — recast as the near, specific, BINDING end-state — honestly conditioned.** Most of §4 is now Live under MEASURE; what remains is the *binding*. State the **specific S11 shape** the mentor set (this is the payoff of the update): the flip is **staged** (do-not-proceed class first — a structural false-positive floor of zero on kathekon-free actions — then the pause rows + the G6 loop bound after a live calibration window); **G6(a) is QUALIFIED** — an unclosed correction loop binds do-not-proceed ONLY when the opening verdict engaged a kathekon factor (justice surface / violated obligation / proximity ≤ habitual / sub-species passion), while a "contrary; no kathekon factors detected" verdict is log-and-continue + a developmental flag (the false-positive class the observation instrument measures); the **calling-gate enforce arm flips with the engine**; **aggregate-keyed conservative depth binds at v1**; and **PA-5 / PA-10 / the A2 decrease-dodge become named bounds on the enforcement claim** (not hidden — the enforcement binds on what the examination can find, disclosed on the surface). Frame all of this as *what becomes available once the readiness standard is met and the flip runs* — conditional, not done.

4. **§6 (opportunities) — sharpen for the post-enforce world.** The strongest new opportunity: the trust layer moves from *measuring* reasoning quality to *binding* on it — an **enforcing** governance substrate (a do-not-proceed that actually holds, with R20c human-override supremacy), which is a materially stronger position than every "measures/attests" rail. Update the regulatory framing (Art 14 human oversight is now *demonstrable binding intervention + override*, not just logging); the graduated-autonomy signal becomes *actionable* (autonomy granted on an enforced, decaying, unforgeable record); and note the **readiness discipline itself as a credibility asset** — the layer refused to bind until a *measured* false-hold rate over the live distribution licensed it (the mentor's Q1), which is exactly the honesty the weights-tier claim will one day need. Keep §6.7 (weights BLOCKED) intact.

5. **Honesty throughout (R18, non-negotiable):** the post-enforce capabilities are **conditional** — the doc must say, plainly, that they assume (a) the 7-day observation review clears the four-part standard, and (b) the S11 flip works as intended, and that the flip remains a founder-walked Critical step with the assent re-confirmed at flip time (PR7). Keep the §8-derived honest-bounds paragraph (factual correctness / omitted harms / no self-report / no future behaviour / weights BLOCKED) and extend it with the enforcement-claim bounds. Do not let the recast read as though enforce is live.

## Part C — Verify + close

- Re-read the revised summary end-to-end against `CLAUDE.md`'s Live list + the S11 verbatim verdict: every `[LIVE]`/`[LIVE-MEASURE]`/`[PLANNED]` tag accurate; the enforce shape matches the verdict (staged, Q3-qualified, bounds named); no capability stated as binding-today; the conditionality explicit.
- Optionally, a light Explore/Workflow claims-vs-code pass on any capability sentence you're unsure survived S9b/S10 (e.g. the exact hook set, the read-surface envelope) — governance docs still earn a vacuous-pass check.
- Lean decision-log entry (`D-TRUST-LAYER-HARNESS-STATE-SUMMARY-UPDATED-POST-ENFORCE`) + a lean close. Documents only ⇒ `git revert` is the rollback.

## Forecast

Success = the summary reads true as of 2026-07-12 (S1–S10 + S9b Live under MEASURE, the read surface Live, S11 deferred/readiness-gated, the observation instrument built), and its forward-looking sections articulate — sharply and honestly conditioned — what the trust layer offers once it begins to *bind*: an enforcing, per-decision, unforgeable credential of reasoning quality that refused to bind until a measured false-hold rate licensed it. The document becomes the founder's ready statement of the post-enforce position, without over-claiming it as done. **ENFORCE remains S11, readiness-gated; the 0h call remains the founder's.**

End of prompt.
