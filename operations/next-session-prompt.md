# Next Session Prompt

Paste everything below the line into a new session.

---

Governing frame: /adopted/session-opening-protocol.md

I am opening a new session in the tech stream. The previous session (26 April 2026) completed ADR-PE-01 Session 6 (per-consumer 2A-recompute switch on `/api/mentor/private/reflect`, first per-consumer switch in §8's live-consumer rollout phase post-loader-Verified). Founder selected Framing A at session open with all defaults: reflect first per Q-Consumer; (i) skip persistence on empty input per Q-Empty-Recompute-Posture (preserves existing entry on bypass-with-empty-data); per_request preserved per Q-Cadence-Adjustment; (a) propagate `bypass_pattern_cache` flag to reflect per Q-Verification-Pattern (symmetric with the proof endpoint); keep on each consumer per Q-Bypass-Long-Term-Posture (O-PE-01-G partial direction landed). Critical Change Protocol (0c-ii) was executed in full pre-deploy with fourteen worst cases (A through N) accepted by founder approval ("I accept worst cases A–N. Go ahead."). Three-probe verification completed in-session via consolidated Console snippet on `/founder-hub` calling `/api/mentor/private/reflect` directly: R1 (cache hit, no bypass) returned `pattern_source: 'persisted'`, `interactions_source: null`, `interactions_count: null`, `bypass_used: false` — back-compat preserved; R2 (FORCE RECOMPUTE via `bypass_pattern_cache: true`) returned `pattern_source: 'recomputed'`, `interactions_source: 'live_loader'`, `interactions_count: 51`, `bypass_used: true` — the decisive verification that the loader was actually invoked on a live consumer, returned 51 real `mentor_interactions` rows, was synthesised through pattern-engine successfully, and the live-recomputed result was persisted; Re-R1 (cache hit on the post-R2 entry) confirmed round-trip end-to-end on the live consumer. TypeScript reached `tsc --noEmit` exit 0; the push completed via GitHub Desktop; Vercel reached Ready; smoke check on `/founder-hub` rendered normally. Session 6 reached Verified status. The session close is at `/operations/handoffs/tech/2026-04-26-ADR-PE-01-S6-reflect-recompute-close.md`. One decision-log entry was Adopted at the close: `D-PE-01-S6-REFLECT-RECOMPUTE-VERIFIED`. Seven decision-log entries (`D-PE-01-S1-1A-VERIFIED`, `D-PE-01-S2-2A-VERIFIED`, `D-PE-01-S3-3A-VERIFIED`, `D-PE-01-S35-3.5α-VERIFIED`, `D-PE-01-S4-4B-VERIFIED`, `D-PE-01-S5-LOADER-VERIFIED`, `D-PE-01-S6-REFLECT-RECOMPUTE-VERIFIED`) all currently carry "TBD per founder share" in the commit-hash slot. The §8 single-endpoint-proof sequence has its first per-consumer 2A-recompute switch landed: the live `mentor_interactions` loader is now Verified live on a real-user-facing consumer (reflect), with the `bypass_pattern_cache` flag as the deterministic verification mechanism (also a permanent operational tool on reflect — force-recompute on demand). Founder-hub remains on 2A-skip on absence pending its own per-consumer 2A-recompute switch — the natural next §8 rollout step. The founder's `pattern_analyses['private-mentor']` entry on the encrypted profile blob is now twice-overwritten with live-recomputed data (Session 5 Probe L4 + Session 6 Probe R2). The founder's `pattern_analyses['founder-mentor']` entry remains at the Session 5 L3-overwrite value (founder-hub was not modified this session). This session continues from there.

I use GitHub Desktop to push commits. If any code lands this session, give me explicit GitHub Desktop instructions (open GitHub Desktop → confirm Current Repository = `sagereasoning` → confirm Current Branch = `main` → review the Changes pane → fill Summary + Description → click Commit to main → click Push origin → wait for spinner) per commit, in the same pattern the prior tech-stream sessions used.

The verification probe pattern uses a Console snippet on a deployed page; the working auth-cookie discovery for this project is `decodeURIComponent(cookies['sb-access-token'])` reading the raw JWT directly from the cookie value (no JSON envelope, no chunked patterns). Use that snippet pattern from the start. This pattern has now been used four times successfully without rediscovery (Sessions 3.5, 4, 5, 6 — Session 6 being the consolidated three-probe snippet on reflect). PR8's third-recurrence rule firmly supports promotion to canonical at this session open (counter is 4 of 3 conservative). Recommendation: promote it at session open as a small standalone item (or batch with whatever framing is picked).

Before touching any code, complete the session opening protocol:

* Tier declaration: this is at minimum a documentation/cleanup session; it becomes a code/tech session if Framing A or Framing C-with-code is picked. Read tier 1, 2, 3, 6 every-session sources + KG governance scan; add 8, 9 (code state) if a framing requiring code is picked.
* Read the most recent handoff in `/operations/handoffs/tech/`: `2026-04-26-ADR-PE-01-S6-reflect-recompute-close.md`. Read end-to-end. The "Decisions Made", "Status Changes", "Open Questions", "Next Session Should", and "Process-Rule Citations" sections are the load-bearing parts.
* Read the adopted ADR in full: `/compliance/ADR-PE-01-pattern-analysis-storage.md`. Specifically §1.2 (loader (c) — Verified live on the proof endpoint per Session 5 and on reflect per Session 6), §1.4 (hub-scoping), §3, §6 (R17 footprint), §7 (Coordination with the loader — particularly §7.3 read cadence per consumer, newly relevant for the founder-hub switch), §8 (Single-endpoint proof sequence — Sessions 1, 2, 3, 3.5, 4, 5, 6 all Verified; founder-hub switch is the natural next §8 rollout step), §9 (Risks accepted), §10 (Rollback), and §12 (Open items O1–O5; O3 — `last_pattern_compute_at` plain column — becomes more relevant if cadence work or freshness queries surface during the founder-hub switch).
* Scan `/operations/knowledge-gaps.md` for any entry touching live-consumer integration with the loader (KG3 hub-label end-to-end contract — engages on every new reader/writer; KG7 JSONB shape — inherited via the loader's defensive parse on any new consumer; KG1 rule 2 awaited writes — engages if the founder-hub switch introduces a new conditional write surface; KG6 composition order — engages if any change alters prompt assembly).
* Confirm P0 hold-point status — should still be active. Pattern-engine integration sits inside the assessment set; the loader work completed Session 5; the first per-consumer switch (reflect) completed Session 6; the founder-hub switch would complete the live-consumer rollout phase.
* Confirm model selection (PR4). The likely next work items are pure DB-loader-routing work (no LLM added by switching founder-hub from 2A-skip to 2A-recompute) or cleanup work (no LLM at all). Confirm at session open. If any framing changes prompt composition, PR4 re-engages.

Step 1 — Pick the next framing.

The §8 single-endpoint-proof sequence has its first per-consumer 2A-recompute switch landed (reflect, Session 6). The remaining ADR-PE-01 work items vary widely in scope and risk. Three candidate framings — surface all three with brief reasoning. I pick.

* Framing A — Per-consumer 2A-recompute switch on founder-hub. Critical risk under PR6 + R17. The natural §8 next step: with the loader-on-a-live-consumer pattern Verified at Session 6 on reflect, the existing 2A-skip on absence at founder-hub can be switched to 2A-recompute on a per-consumer basis. Founder-hub is the third live consumer of the loader and adds the third `saveMentorProfile()` recompute writer. KG3 surface is wider than reflect's because founder-hub uses the canonical mapper (`mapRequestHubToContextHub(effectiveHubId)`) rather than a verbatim hardcode — but the mapper is already used by the existing observation/snapshot/signals readers in the route. The loader would be called for both `'private-mentor'` and `'founder-mentor'` labels depending on request shape (`hub_id: 'private-mentor'` → `'private-mentor'`; `hub_id: 'founder-hub'` → `'founder-mentor'` per the mapper). Sub-questions for the plan walk listed below.
* Framing B — Cleanup pass + Console-snippet promotion. Standard risk overall. Batch the small carry-forward items into a single session: (i) Console-snippet auth-cookie discovery promotion to canonical under PR8's third-recurrence rule (counter is 4 of 3 conservative — promotion is overdue); (ii) commit-hash recording across the seven `D-PE-01-*-VERIFIED` decision-log entries (founder shares from GitHub Desktop History tab; AI amends inline); (iii) O-PE-01-B blob-size measurement (one-off Supabase probe of `length(encrypted_profile)` against the 50 KB threshold from ADR §9); (iv) cosmetic `27 April 2026` → `26 April 2026` comment-date fix in `/website/src/app/api/mentor/private/reflect/route.ts` (carry-forward from Session 3 close, slightly grown after Session 6 added correctly-dated comment blocks alongside the incorrectly-dated Session 3 preamble); (v) cosmetic proof-endpoint top-of-file JSDoc bullet stale ("Uses a hand-constructed fixture profile (not the live profile store)" — inaccurate since 25 April 2026 and now triply inaccurate post-Session-5/6). Smaller scope, finishes the trailing items, frees the next architectural session from carry-forward overhead. Sub-questions listed below.
* Framing C — Step out of ADR-PE-01. The hold-point assessment is still active per P0 0h. Pattern-engine integration was inside the assessment set; with the integration now Verified live on the proof endpoint AND on reflect (one of the two major mentor-touching live consumers), the assessment can use the wired-and-loader-backed pattern data to test what works on real founder data. Or — alternative — step into a different priority entirely (P2 ethical safeguards is the next priority per project instructions; P2 starts with R20a vulnerable user detection and R17a bulk profiling prevention, both Critical and both pre-dating ADR-PE-01 in priority order). Sub-question for the plan walk depends on which alternative. AI signal: I'd push back gently on starting P2 work while ADR-PE-01 carry-forward items (commit-hash recording, blob-size measurement, Console-snippet promotion, founder-hub switch) remain open — Framing B handles four of those in one session, and Framing A completes the §8 rollout. But if you want P2, I'll execute it.

Do not assume which framing I am taking. Ask me directly: "Framing A, B, or C?" Wait for my answer.

Also surface, at the same time as the framing question, the appropriate plan-walk questions for each framing:

For Framing A (per-consumer 2A-recompute switch on founder-hub), surface these sub-questions and ask me to answer alongside the framing pick:

* Q-Empty-Recompute-Posture — what happens if the live loader returns `[]` (no rows in the 90-day window) for either label? Three options: (i) skip persistence (preserves any existing entry — same posture as Session 6 on reflect); (ii) persist the empty PatternAnalysis (cache pollution risk per ADR §6.3 worst case G language — same posture as the proof endpoint Session 5); (iii) keep 2A-skip semantics on the empty case specifically (recompute fires but doesn't replace cache when result is empty). Default recommendation: (i) skip persistence on empty input — continuity with Session 6's reflect posture and matches the conservative posture the founder picked at the loader's Q-Recompute-Read-Precedence.
* Q-Cadence-Adjustment — keep per_request, or switch to throttled (e.g., 6 hours) for the live consumer? Default recommendation: keep per_request — continuity with Sessions 1/2/3/3.5/4/5/6; revisit if cost / latency picture changes.
* Q-Verification-Pattern — propagate `bypass_pattern_cache` flag to founder-hub per Q-Bypass-Long-Term-Posture continuity? Three options: (a) propagate the flag (symmetric with proof endpoint and reflect; permanent operational value; the canonical pattern under O-PE-01-G's "keep on each consumer" partial direction); (b) verify by deleting the relevant `pattern_analyses[label]` entry on the founder's blob to force cache miss (one-off Supabase SQL); (c) verify by adding a brand-new test hub label. Default recommendation: (a) — propagate the bypass flag — continuity with Sessions 5 and 6; aligns with O-PE-01-G partial direction.
* Q-Hub-Label-Coverage — founder-hub serves both `'private-mentor'` and `'founder-mentor'` labels (via the canonical mapper). Verification probes need to exercise both labels. Default: run probes for both labels (R1/R2/Re-R1 for `'founder-mentor'` via `hub_id: 'founder-hub'` request body, then R1/R2/Re-R1 for `'private-mentor'` via `hub_id: 'private-mentor'` request body — six probes total).
* Q-Verification-Probe-Shape — founder-hub's response shape carries diagnostics inside `pipeline_meta` (per Session 4) rather than at the top level (the proof endpoint and reflect's pattern). The probe snippet must read `result.pipeline_meta.pattern_source` (and equivalents) for founder-hub probes. Confirm.
* Q-Bypass-Long-Term-Posture (O-PE-01-G) — concurrent with Q-Verification-Pattern: the partial direction from Session 6 was "keep on each consumer." Confirm continuation, and decide whether to also surface the long-term retire-vs-generalise question (now that the rollout is one switch from complete) or defer until rollout is complete.

For Framing B (cleanup pass + Console-snippet promotion), surface these sub-questions:

* Q-Cleanup-Scope — all five items, or narrower? Default recommendation: all five — they are independent and small, and the next architectural session benefits from a clean carry-forward.
* Q-Console-Snippet-Promotion-Format — promote to a new canonical-pattern entry in `/operations/knowledge-gaps.md` (under a new slot or a dedicated "Canonical Patterns" sub-section), or to a new file `/adopted/canonical-patterns.md`, or to the existing `/operations/verification-framework.md`? Default recommendation: add a new file `/adopted/canonical-patterns.md` — keeps the auth-cookie pattern findable separately from KGs and verification framework; separation of concerns.
* Q-Blob-Size-Probe-Pattern — the blob-size probe needs Supabase access. Two options: (i) a SQL query through Supabase's web UI (founder runs once, reports back); (ii) a temporary diagnostic API route. Default recommendation: (i) — simpler, no code change needed.
* Q-Commit-Hash-Recording-Mechanic — founder shares all seven hashes inline at session open in chat; AI amends seven entries in `/operations/decision-log.md` in a single edit pass. Confirm.
* Q-Cosmetic-Date-Fix-Risk-Classification — Standard risk under 0d-ii (cosmetic, comment-only, not behavioural, not safety-critical, not user-facing). Confirm.
* Q-JSDoc-Bullet-Fix-Risk-Classification — Standard risk under 0d-ii (cosmetic, comment-only, JSDoc accuracy). Confirm.

For Framing C (step out), surface this sub-question:

* Q-Alternative — which alternative? Hold-point assessment work (use wired-and-loader-backed pattern data to test what works on real founder data per P0 0h Assessments 1–5 — now with TWO live consumers running real data, not just the proof endpoint)? Or step into a different priority entirely (P2 ethical safeguards — R20a vulnerable user detection, R17a bulk profiling prevention, R17b application-level encryption wiring, R17c genuine deletion endpoint, R19c/d limitations page + mirror principle, R20b/d independence + relationship asymmetry guidance)? Or something else (e.g., O-PE-01-F MENTOR_CONTEXT_V2 decoupling decision, O-PE-01-G `bypass_pattern_cache` long-term retire-vs-generalise decision)? Founder names the alternative; AI then surfaces the plan-walk questions for that alternative.

Step 2 — Critical Change Protocol (0c-ii) requirements.

The Critical / Standard classification depends on which framing is picked.

Framing A (per-consumer 2A-recompute switch on founder-hub): Critical under PR6 (third `saveMentorProfile()` consumer added to the recompute path; encryption-pipeline blast radius engaged, plus the new live read of `mentor_interactions` on founder-hub for both labels). All five steps of the Critical Change Protocol must appear in the conversation before I deploy anything:

1. What is changing — plain language. No jargon. Name the specific files (`/website/src/app/api/founder/hub/route.ts`, possibly with the bypass flag added per Q-Verification-Pattern's (a)).
2. What could break — the specific worst case. Live-consumer regression on real founder-hub traffic across both labels. Empty-recompute cache pollution if Q-Empty-Recompute-Posture is (ii). KG3 hub-label drift on the new consumer's call site (wider surface than reflect because the canonical mapper is involved). Read amplification on real founder traffic across both labels. Worst case L equivalent: founder-hub probes write real founder-hub interaction rows.
3. What happens to existing sessions — does this affect users currently signed in? Existing pattern_analyses entries (live-recomputed by Session 5 Probes L3/L4 and Session 6 Probe R2) would be overwritten on next cache miss / bypass once the loader is wired in on founder-hub.
4. Rollback plan — exact GitHub Desktop steps. The founder-hub revert should leave the live-recomputed entries intact (they are still readable by the cache-hit branch which hasn't changed).
5. Verification step — what URL I visit, what I expect to see, what to do if the result is different. Likely: probes that exercise founder-hub with bypass=true for both labels, confirming `pattern_source: 'recomputed'` (cache miss → recompute from real data → save), then re-runs that confirm `pattern_source: 'persisted'` (cache hit on the freshly recomputed entries). Use the canonical `decodeURIComponent(cookies['sb-access-token'])` cookie pattern from the start. Read diagnostics from `result.pipeline_meta.*` (founder-hub shape) not `result.*` (reflect shape).

Framing B (cleanup pass + Console-snippet promotion): Standard risk for items (ii)-(v); item (iii) blob-size probe is read-only (Standard). Item (i) Console-snippet promotion is a documentation change (Standard). The full Critical Change Protocol does not apply, but PR2 verification immediate still applies for any code change, and any code change still goes through founder approval before deploy. Most of Framing B is documentation; only the JSDoc bullet fix and the comment-date fix touch code.

Framing C (step out): Risk classification depends on the alternative picked. P2 ethical safeguards work is Critical for several items (R17a, R17b, R17c, R20a) per the project instructions — full Critical Change Protocol applies for any of those. Hold-point assessment work is generally Standard or Elevated (not modifying production code). O-PE-01-F or O-PE-01-G decisions are documentation (Standard).

Cannot answer one of those? Stop and signal "I need your input" or "This is a limitation."

PR3 applies — pattern-engine call (when invoked via cache-miss recompute on a live consumer) is synchronous, no fire-and-forget. PR4 applies — confirmed at session open. KG1 rule 2 applies if any new conditional write or new write surface is added (Framing A's per-consumer switch on founder-hub adds a new `await saveMentorProfile()` call site on founder-hub — KG1 rule 2 engaged). KG3 applies — any new reader/writer of hub-scoped data must use the canonical mapper or its allowlist equivalent. KG6 applies — any change altering prompt assembly must respect the user-message zone for per-request data.

Step 3 — Acceptance criteria.

This session ends successfully if at least one of these is true:

* The chosen framing's first implementation step reaches Verified status: TypeScript clean (`npx tsc --noEmit` exit 0 in `/website` if code is involved); the chosen verification probe passes (per-framing probe shape, designed in the CCP step 5); I push the commit(s) via GitHub Desktop; Vercel reaches Ready (if applicable); I confirm the round-trip works.
* The session ends earlier with a clear "I'm done for now" signal from me. Stabilise and close. Do not propose additional fixes.
* The session reveals a blocker that requires a smaller scoping change before the chosen work can land. In that case, surface the blocker with the AI signal "I need your input" and let me decide whether to proceed in this session or close and reopen with a narrower scope.

Step 4 — Session close.

Per the protocol Part C, produce a handoff at session end in the required-minimum format plus the relevant extensions (Verification Method Used / Risk Classification Record / PR5 Knowledge-Gap Carry-Forward / Founder Verification / Decision Log Entries — Adopted / Process-Rule Citations / Orchestration Reminder). Save to `/operations/handoffs/tech/[YYYY-MM-DD]-[short-description]-close.md`. Name the protocol explicitly as the governing frame at close.

If the session involved code and I pushed via GitHub Desktop, the handoff's "Founder Verification" section names the pushed commit hashes and the live-probe / verification result.

If the session involved any framing-A founder-hub switch that completes the live-consumer integration of the loader on the second major mentor-touching consumer, the handoff should also note the new state of `pattern_analyses` entries on the founder's blob (which were last overwritten by Session 5 Probes L3/L4 for `'founder-mentor'` and Session 6 Probe R2 for `'private-mentor'`).

Carry-forward open items (do not address unless I bring them up):

* Decision-log entries to confirm at session open: `D-PE-01-S1-1A-VERIFIED`, `D-PE-01-S2-2A-VERIFIED`, `D-PE-01-S3-3A-VERIFIED`, `D-PE-01-S35-3.5α-VERIFIED`, `D-PE-01-S4-4B-VERIFIED`, `D-PE-01-S5-LOADER-VERIFIED`, and `D-PE-01-S6-REFLECT-RECOMPUTE-VERIFIED` are all Adopted as of the 2026-04-26 closes. Commit hashes for all seven pushes are placeholder ("TBD per founder share from GitHub Desktop History tab"); if I share the hashes at session open, AI amends all seven entries inline. Framing B item (ii) is the natural place to do this if not done at session open.
* Cosmetic comment-date carry-forward (slightly grown post-Session-6): `/website/src/app/api/mentor/private/reflect/route.ts` carries comment blocks dated "27 April 2026" in the Session 3 preamble (lines ~291) alongside correctly-dated "26 April 2026" comment blocks added by Session 6. Standard-risk one-line fix; can be batched with any future change to that file. Tiered as Efficiency & stewardship per PR9; absorbed into ongoing work. Framing A (founder-hub) does NOT touch reflect, so this would not naturally batch with Framing A. Framing B item (iv) is the explicit fix path.
* Cosmetic JSDoc bullet stale (carried from Session 5): `/website/src/app/api/mentor/ring/proof/route.ts` top-of-file bullet "Uses a hand-constructed fixture profile (not the live profile store)" became inaccurate at ADR-Ring-2-01 Session 1 (25 April 2026) and is doubly inaccurate post-Session-5 (the recompute branch now uses live data via the loader). Tiered as Efficiency & stewardship per PR9. Framing B item (v) is the explicit fix path.
* O-S5-A — `/private-mentor` chat thread persistence (pre-existing UX gap).
* O-S5-B — write-side verification of ADR-Ring-2-01 4b (deferred until natural triggering).
* O-S5-D — static fallback canonical-rewrite revisit.
* O-PE-01-A (read amplification) — every profile read decrypts the full blob even when pattern data is not needed. Now also concretely measurable on reflect's recompute branch (two new queries per cache-miss / bypass invocation: profile_id lookup + mentor_interactions read) in addition to the proof endpoint's recompute branch from Session 5. Will scale further under Framing A's per-consumer 2A-recompute switch on founder-hub (third recompute writer; both labels). Revisit if profile reads on any consumer become a measurable hot path.
* O-PE-01-B (blob-size monitoring) — versions now incremented by three writers (proof endpoint, reflect endpoint, founder-hub endpoint) plus the live-recomputed L3/L4/Re-L1 from Session 5 and R2/Re-R1 from Session 6. Trigger condition for revisit is blob > 50 KB or encrypt/decrypt p95 > 50 ms. Not blocking; a quiet measurement when convenient. Framing B item (iii) is the explicit measurement path.
* O-PE-01-C (optional `last_pattern_compute_at` plain column) — adding a plain timestamp column would let freshness queries skip decryption. More relevant if Framing A introduces freshness-driven cadence (e.g., throttled with a conditional via Q-Cadence-Adjustment).
* O-PE-01-D (write cadence) — resolved for Sessions 1, 2, 3, 3.5, 4, 5, 6 (per_request). Revisit in Framing A's plan walk if Q-Cadence-Adjustment surfaces concerns.
* O-PE-01-E (backfill of existing profiles) — partially resolved further for the founder's profile via Session 6 Probe R2 overwrite of `'private-mentor'`; `'founder-mentor'` still at Session 5 L3-overwrite value. Carries forward unchanged for any other practitioners (none currently).
* O-PE-01-F — pattern-read decoupling from `useProjection` / MENTOR_CONTEXT_V2 env var. Defer until the V2 feature flag retirement question lands on the table; not blocking.
* O-PE-01-G — `bypass_pattern_cache` long-term posture. Partial direction landed at Session 6 (keep on each consumer). Long-term retire-vs-generalise question still open until rollout is complete across all live consumers (one switch away — founder-hub).
* PR5 candidate for promotion — Console-snippet auth-cookie discovery (`decodeURIComponent(cookies['sb-access-token'])` reading raw JWT directly). Now used four times successfully without rediscovery (Sessions 3.5, 4, 5, 6). PR8's third-recurrence rule firmly supports promotion (counter is 4 of 3 conservative — promotion is overdue). Framing B item (i) is the explicit promotion path. Can also be done as a small standalone item at session open before any framing work begins.
* PR5 candidate (1 of 3) — two-phase staging within a single founder session, separated by a stop-point gate. Demonstrated successfully in the 2026-04-26 Sessions 3.5 + 4 combined session. Promotion trigger: two more occurrences. Counter unchanged at 1 of 3 from Session 6.
* PR5 candidate (1 of 3) — diagnostic-via-pipeline_meta spread-conditional pattern. First introduced at Session 4 in `/api/founder/hub/route.ts`. Counter unchanged at 1 of 3 from Session 6.
* PR5 candidate (1 of 3) — sub-decision-after-framing-acceptance pattern. Demonstrated at Sessions 3.5 + 4 close. Counter unchanged at 1 of 3 from Session 6.
* PR5 candidate (2 of 3) — bypass-flag-as-verification-mechanism. Used a second time at Session 6 (propagated from proof endpoint to reflect verbatim). Counter advanced from 1 of 3 to 2 of 3. Promotion trigger: one more occurrence (likely the founder-hub switch under Framing A) earns promotion under PR8.
* PR5 candidate (1 of 3, NEW from Session 6) — per-consumer 2A-recompute switching as a §8 rollout primitive. The pattern: take the loader proven on the proof endpoint and switch one live consumer at a time from 2A-skip to 2A-recompute, with each switch being its own PR1 single-endpoint proof. Promotion trigger: if founder-hub switches under the same pattern in a future session (and any subsequent consumer switches), the rollout primitive earns its place under PR8.

Do not write any code until I have picked a framing (Step 1) AND named the relevant scope/cadence/alternative sub-question(s) for that framing AND (if Framing A) named all six Q-Empty-Recompute-Posture / Q-Cadence-Adjustment / Q-Verification-Pattern / Q-Hub-Label-Coverage / Q-Verification-Probe-Shape / Q-Bypass-Long-Term-Posture sub-decisions AND the Critical Change Protocol (Step 2) is satisfied with my explicit "OK / go ahead" naming the listed risks (where Critical risk applies).

If I am only doing the plan walk and decide not to proceed to code in this session, that is a complete and successful session. Do not push for code.
