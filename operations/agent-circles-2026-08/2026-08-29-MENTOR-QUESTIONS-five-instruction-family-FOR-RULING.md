# Mentor questions — the five-instruction family in `inbox/` — FOR RULING

**Date:** 2026-08-29. **Status: RULED 2026-08-29 — all eleven questions answered in one round**
(verbatim canonical: `2026-08-29-mentor-ruling-five-instruction-family-verbatim.md`; the ruling
wins over this document; execution recorded at
`D-FIVE-INSTRUCTION-FAMILY-RULED-ADOPTED-EXECUTED-2026-08-29`). Original status line preserved
below for the record: FOR RULING — nothing below is adopted, executed, or built.
**Authored by:** the 2026-08-29 grounding session (Fable 5), at the founder's direction: *read the
five newest inbox files, assess them in full against the project purpose, engine, and harness
before acting, and draft detailed mentor questions first — there may be contradictions across the
five.* That direction is honoured literally: **none of the five files has been acted on** (verified
— zero decision-log references to any of them), none is adopted as binding, no build/scope/design
work has been done under any of them, and this document is the only output.

**PR20 statement.** Every present-tense mechanism fact below was timestamp-checked at drafting
(2026-08-29) against the decision log, the governing records, or the code at source — each carries
its citation. Facts a repo session cannot verify (live Vercel env values; whether a founder walk
happened without a record) are marked **unverified** rather than asserted. Per the amended PR20,
the stale present-tense facts *inside the five instructions themselves* are surfaced here as
findings for the mentor, not silently corrected and not acted on under either reading.

---

## §1 — The five files (newest first), and what each says

| # | File (inbox/, mtime) | What it is |
|---|---|---|
| F5 | `Instructions for structural causal apprehension.rtf` (2026-08-29 06:11) | The Spinoza *scientia intuitiva* synthesis (Perplexity/Grok/OpenAI reviewed by the mentor). Names the integrated four-part frame — cybernetics, neural operators, Spinoza, Stoic doctrine as structural priors — as **one frame**, the standing-runner design session's conceptual foundation, "presented together, not sequentially." Licenses purpose-level reasoning (the harness scaffolds the *prerequisite* — adequate ideas via examined assent — never the destination). Defers multi-scale operator design "before the bounded validation run has produced trajectory data." Carries the agent-side scientia-intuitiva question as **named orientation, not a design directive**. Licenses no build. |
| F3 | `neural control instructions.txt` (2026-08-27 13:46) | A standing orientation instruction ("read before responding to any build/scoping/design question"): project restatement, harness-as-structural-priors doctrine, a **settled-surface-names block** (practice-on/off, logos-on/off, idea-on/off, fresh, watching), a **GS-ATRF-1..4 open-questions block** ("do not pre-answer"), the Anandkumar-frame ruling (confirmations require no action; multi-scale/curriculum/formal-verification defer to the standing-runner session; the physics-vs-doctrine correction-signal distinction), and standing build rules (verify at source; name departures; per-flag gating; phased-not-carve-out; nothing licenses a build). Identified **by F4** as the second of the three-instruction set (F4: "the Anandkumar frame … — the second instruction"); F3 itself carries no ordinal. |
| F4 | `convergence instruction.rtf` (2026-08-27 13:40) | "The third of three instructions." The cybernetics × neural-operators convergence: intellectually sound, **not yet actionable** — a named standing-runner input alongside the adversarial-review findings, the Anandkumar frame, §5d, GS-CYB-1, GS-CYB-2, GS-ATRF-4. Raises **the update-rule question** (the operator mapping post-completion proximity delta → next cycle's generation bias) as GS-CYB-2's core content, "sequentially dependent on GS-ATRF-3," unanswerable "before the bounded validation run has produced real completion signal data." Five ordered checks for any convergence-touching question. Licenses no build. |
| F2 | `cyber adversarial instruction.txt` (2026-08-25 20:33) | The **seven-probe adversarial review instruction**: review the harness as a cybernetic control system (reinforcing loops; balancing-loop integrity; delays; stochastic stability; Meadows leverage points; second-order reflexivity; system archetypes), with a fixed output format (per-probe findings, a leverage-point summary, a stochastic stability assessment) and strict constraints (no pre-answering open questions; no build recommendations; no doctrine evaluation; named gaps over inferences; weights-BLOCKED not reasoned around). Carries its own "what you know about the harness" briefing table. |
| F1 | `cyber adversarial prereq.rtf` (2026-08-25 20:31) | The mentor's answer to *when* to run F2: **"not yet"** — run it "immediately after the §6 report is complete and before the standing-runner design session opens," its findings becoming a named input to that session. One exception: **Probe 6** (reflexivity) can run now. One preparatory step: instrument the watching table (virtue domain + oikeiosis circle of each elected candidate; guardrail pass/fail for **every** candidate in the generation set; cycle-over-cycle proximity delta) — "a scoping observation, not a build instruction." |

The already-executed `Mentor Cybernetics Instructions.rtf` (2026-08-24; adopted binding, all four
tasks executed — `D-CYBERNETICS-INSTRUCTION-EXECUTED-AND-FOUNDER-ELECTIONS-2026-08-24`) reads as
this family's first landed member; F4 calls itself "third of three" and F5 speaks of "the four
instructions you are carrying together." The enumeration is reconciled at Q6.

## §2 — Verified mechanism facts the questions rest on

Each fact numbered for citation from the questions. **[V]** = verified first-hand at drafting;
**[R]** = from the decision-log/record, not re-verifiable from a repo session; **[U]** = unverified.

1. **[R]** The bounded validation run **completed 2026-08-16** — stopped at 20 cycles by the
   mentor's own ruling (`D-MENTOR-RULING-IDEA-LOOP-STOP-AT-20-RECORDED-2026-08-16`), and the §6
   report was compiled from live production data and **accepted by the mentor in full** the same
   day (`D-MENTOR-RULING-R1-S6-REPORT-ACCEPTED-2026-08-16`). All five files postdate this by 9–13
   days, yet all five speak of the run as not yet having happened.
2. **[R]** The standing-runner design session's gate is **discharged** (ruled explicitly,
   `D-MENTOR-RULINGS-FIVE-QUESTIONS-EXAMINATION-ADOPTED-EXECUTED-2026-08-22` Q5: "the
   standing-runner gate is DISCHARGED … the session may open on founder election"). It has not
   opened. Its CLOSE is gated on the nine-candidate classification
   (`D-MENTOR-RULING-R1-S6-REPORT-ACCEPTED-2026-08-16`), and completing the ATRF row of the
   named-input register is owed before it opens (the register's own honest-scope note,
   `operations/primal-substrate-2026-08/00-PRIORITY-INDEX.md`).
3. **[R]** All four GS-ATRF questions were **resolved or fully routed** by the ATRF sixteen
   rulings (`D-MENTOR-RULINGS-ATRF-SIXTEEN-ADOPTED-EXECUTED-2026-08-23`): Q-A3 (GS-ATRF-4's
   `unknown` closes §(c-bis)); Q-B1/Q-B2 (GS-ATRF-2's shape + the bundled migration); Q-C1
   (GS-ATRF-3's return path — schema, endpoint, receiving write); Q-A1/Q-A2 (the one-framework/
   two-axes ruling). **GS-ATRF-4 is ruled and LIVE** in the production `project_context` row since
   2026-08-19 (`D-GSATRF4-EPISTEMIC-STATUS-LIVE-2026-08-19`).
4. **[R]** GS-ATRF-3's completion-signal endpoint is **built dark** (2026-08-23:
   `POST /api/practice/completion-signal` behind `SUBSTRATE_COMPLETION_SIGNAL_ENABLED`, new table,
   new capability) — but **no completion-signal data exists**: the endpoint postdates the run,
   nothing consumes a persisted signal yet (ruled Q-C2b work at the standing-runner session), and
   the run itself produced none. **[U]** whether its founder walk (migrations + optional
   activation) has happened — **no apply record exists** in the decision log; presumed unapplied.
5. **[V]** F1's instrumentation ask is **already substantially satisfied by the existing schema**:
   `idea_loop_candidates` persists per-candidate `guardrail_proximity`, `guardrail_domains`,
   `classified_domains`, and `cycle_outcome` incl. `rejected_by_guardrail` — "full per-candidate
   transparency including rejected_by_guardrail candidates with heuristic attribution"
   (`website/src/lib/substrate/idea-loop-watching-store.ts:13-15,156-172`; the §6 report's
   9-guardrail-rejected-of-120 count was derived from these live rows). `target_circle` and the
   blast-radius fields ride the **authored** ATRF/S4 migration as deliberately-optional insert
   keys — safe in either order against the migration
   (`idea-loop-watching-store.ts:175-198`). **A second genuine gap on the historical corpus:**
   per-candidate **oikeiosis-circle** data (F1's first instrumentation item) was never persisted
   for the 20 completed cycles and cannot be backfilled — the additive `target_circle` column,
   whenever applied, is NULL on them. The **cycle-over-cycle proximity delta** has no
   dedicated column but is derivable at read time from each cycle's winner-candidate
   `guardrail_proximity`. What genuinely does not exist in any form: completion-signal data
   (fact 4).
6. **[V]** `/api/reason` injects `getProjectContext('condensed')` into the Layer-1 extraction on
   every request (`website/src/app/api/reason/route.ts:1413-1418,1454`); **the live guardrail
   sandwich branch injects none** (`guardrail-sandwich.ts`: zero `projectContext` references;
   `guardrail/route.ts:389` fetches `'minimal'` on the route, outside the sandwich extraction).
   F2's briefing sentence "injects project context verbatim into **every** Layer-1 extraction
   call" is therefore true of `/api/reason` and false of the live gate.
7. **[R]** F2's briefing figure "4,159 characters" is stale: the production `project_context` row's
   `current_phase` measured **5,616** chars at v1.4.1
   (`D-PROJECT-CONTEXT-LIVE-ROW-UPDATED-ENCODING-INCIDENT-2026-08-24`, founder-run length query).
8. **[R]** F2's briefing facts on the provenance thread have moved: the honesty correction is **no
   longer pending — LIVE** on all three R18 surfaces plus the served envelope, production-verified
   2026-08-25 (`D-EXTRACTION-PROVENANCE-CORRECTION-LIVE-AND-LEDGER-PROMPT-AUTHORED-2026-08-25`);
   the signature-keyed ledger is **no longer unbuilt — slices 1–2 are LIVE record-only** since
   2026-08-26 (`D-PROVENANCE-LEDGER-SLICE2-ACTIVATION-LIVE-2026-08-26`), with the two-week C2
   readiness watch running; the `l1_supply` population is **zero active credentials** (both
   revoked 2026-08-25, founder-verified `active_with_l1_supply = 0`) and **zero supplied
   extractions across 3,200 recorded consults** (454 pre-stamp rows genuinely unknown). The
   emission-hooks enforcement gap itself is still real (enforcement is slice 5, gated).
9. **[V]** The name collision is **three-way, and partly among adopted surfaces** (the independent
   completeness review's headline correction to this document's first draft). (i) The
   `/practice-on`–`/practice-off` **skills** are the Gate-1 harness hooks toggle (the skill's own
   description: "Enable the SageReasoning practice hooks"; renamed from sage-on/off at Trust
   Layer S8 under ADR-012, whose **logos-mode = ENFORCE** vs practice-mode = MEASURE). (ii) The
   **2026-08-01 "practice-on/logos-on fifth-circle split" is ADOPTED AS BINDING**
   (`D-AGENT-CIRCLES-MENTOR-VERDICTS-ADOPTED-PLANS-AUTHORED-2026-08-01`; verbatim records
   `2026-08-01-mentor-consultation-agent-circles-{practice-on,logos-on}-verbatim.md`) — there
   "practice-on" names the orientation-reading/trust-event program half, **matching F3's
   definition**, and "logos-on" names the fifth-circle enforcement/documentation half. (iii) F3's
   block matches (ii) for practice-on but assigns **logos-on = "Layer 3 project context
   injection"** — matching neither (ii)'s logos-on nor ADR-012's logos-mode. Mechanically, the
   orientation/trust pipeline is gated by the founder-set Vercel flags
   `SUBSTRATE_ORIENTATION_READING_ENABLED` (+ `SUBSTRATE_AGENT_CIRCLES_ENABLED`) and
   `SUBSTRATE_TRUST_CORE_ENABLED`, not by the skills in (i). **[R]** The repo's **"Layer 3"** is
   the per-consumer prose service — **Verified, NOT Live** (`SUBSTRATE_LAYER3_ENABLED` unset; the
   route answers 503; S7 re-affirmed by Ruling Set D 2026-08-15) — while F3's "Layer 3" is the
   context-injection layer, which IS live on `/api/reason` (fact 6). See Q5.
10. **[R]** GS-CYB-1's weights-BLOCKED constraint stands in its amended **two-condition** form
    (2026-08-24 ruling; neither condition met; gaming-bar route (ii) ruled against as worded;
    route (i) reframed as its own session). F2 and F4 state this correctly.
11. **[R]** The Rajpal/Expert/Vasiliauskaite single-backward-edge citation in F2's briefing was
    recorded **unverified-at-relay** at the cybernetics execution
    (`D-CYBERNETICS-INSTRUCTION-EXECUTED-AND-FOUNDER-ELECTIONS-2026-08-24`) and has not since been
    independently verified.
12. **[R]** Probe 6's named "live inputs" have materially changed state since F1 was written: the
    honesty correction is live (fact 8), and the self-referential-attestation condition it wanted
    probed is partially remediated (the `attests[1]` scoping + the record-only ledger), while the
    within-record undetectability point survives until slice 5 enforces. Two existing records
    already cover parts of Probe 6's ground: the self-examination-moment investigation
    (P-A1/P-A2, 2026-08-15) and the project-reflections examination (2026-08-23).

## §3 — The central finding: one stale temporal frame across all five files

Every deferral in all five files is anchored on "before/after the bounded validation run has run"
— and the run completed, with its report accepted, **before the earliest of the five files
arrived** (fact 1). This is not five separate errors; it is one authoring baseline that predates
2026-08-10, carried into files dated 08-25 → 08-29. Two readings, with genuinely different
consequences:

- **Reading (i) — the conditions are discharged.** Each deferral collapses by its own trigger:
  the adversarial review is due *now* (F1's own rule — after the §6 report, before the
  standing-runner session), and every frame is available to the standing-runner session, which is
  itself already licensed (fact 2).
- **Reading (ii) — the substance of some deferrals survives their stale tense.** What several
  deferrals were actually waiting for was *data*, and parts of that data still do not exist: no
  completion-signal behaviour (fact 4), no runner history across sessions (h6's finding), 20
  cycles rather than a long trajectory. On this reading those deferrals re-anchor to the standing
  runner's own future operation rather than dissolving.

The questions below do not choose between the readings; they put the choice, per item, to the
mentor. Under the amended PR20 this session's obligation is exactly this surfacing.

## §4 — Contradiction and collision inventory

| # | Collision | Files | Severity |
|---|---|---|---|
| C-1 | All deferrals keyed to a run that has already completed and reported (fact 1) | F1–F5 | Governing — see Q1/Q2 |
| C-2 | "GS-ATRF-1..4 are open; do not pre-answer/resolve" vs. the executed sixteen rulings + the live GS-ATRF-4 + the dark-built GS-ATRF-3 endpoint (facts 3–4) | F3 (and F2's constraints) | High — obeying as written would un-know binding rulings; see Q4 |
| C-3 | Surface-names collision, **three-way** (fact 9): F3's practice-on matches the ADOPTED 2026-08-01 vocabulary, so the genuine collisions reduce to **logos-on** (three candidate referents) and **"Layer 3"** (two live referents), plus the skill-name overlap in (i) — and, intra-family, F2 coins non-settled names ("Stoic Risk Gate") while F3 commands "Use these exactly. Do not introduce synonyms" | F3, F2 | High — see Q5 |
| C-4 | F2's briefing carries moved facts AND two mechanism errors: provenance thread (fact 8), 4,159→5,616 chars (fact 7), "every Layer-1 extraction call" (fact 6), the unverified Rajpal citation (fact 11), "GS-ATRF-3 open / no confirmed mechanism" (facts 3–4); **plus** the component table lists "the ATRF's blast-radius and virtue-domain assessment" as a live comparator (designed-and-unbuilt — fact 5; violates F2's own do-not-exceed-known-structure constraint), and Probe 2's premise "The Stoic Risk Gate produces a binary pass/fail, not a graded error signal" is wrong at source — the live gate returns a graded `katorthoma_proximity` plus a three-way recommendation (proceed / pause_for_review / deny; observed first-hand in this very session's own GUARD-CAUTION frame), so the probe's "below its available resolution" conclusion is scripted on a false premise | F2, F1 | High if the review runs unbriefed; see Q3 |
| C-4b | F2 calls the proximity score "the harness's **primary error signal**"; F3's own doctrine says it is "a **point estimate, not a control signal**" (and GS-CYB-1 — whether it may serve as an error signal — is open and gated). Adopting both verbatim hands the reviewer a contested framing every probe inherits | F2 vs F3 | Medium — see Q3 |
| C-5 | F1's instrumentation prep assumed a future run; the run is over, and most of the asked-for instrumentation already exists or is authored (fact 5) | F1 | Medium — reshapes Q2's data-basis question |
| C-6 | F4: the update rule "cannot be answered before the run has produced real completion signal data" — the run produced none, and none exists (fact 4) | F4 | Medium — see Q8 |
| C-7 | Family enumeration: F4 "third of three" vs F5 "the four instructions you are carrying together" — note F5's "four" actually enumerates **frame components** (cybernetics, neural operators, Spinoza, Stoic doctrine — the last is no instruction file at all), a category slip rather than an inconsistent count | F4, F5 | Low — record hygiene; see Q6 |
| C-8 | F4 both says the update-rule question "is not yet named in the ATRF Integration register" and that it "is GS-CYB-2's core content" — GS-CYB-2 *is* a register row | F4 | Low — resolved by reading GS-CYB-2 as the home; noted for confirmation in Q8 |
| C-9 | F3's standing-rules block restates existing numbered discipline (PR20 verify-at-source; name-departures; the per-flag lesson; PR-adjacent phasing language) as an unnumbered parallel rule surface | F3 | Low — duplicated rule surfaces drift (the perimeter-count lesson); see Q10 |

**Convergences worth naming (not contradictions):** F2/F4 state GS-CYB-1's amended constraint
correctly (fact 10); F3's "the harness-deferral honesty claim depends on the 404 being settled…
a forward commitment, not a current fact" matches the built record exactly (the F-2 commitment is
future-tensed on the live surfaces; slice 3 unbuilt); F3's flag-gating rule is the Stoa ST3/ST4
lesson already encoded in memory and honoured at slice 2 (Q6 ruling); F5's
prerequisite-not-destination framing matches ADR-012's measurement-instrument reframe; and the
founder's own handling of these five files — assessment before adoption — is itself the "receive
the illumination without converting it into a build directive" discipline F3/F4 name.

---

## §5 — The questions

**Q1 — Temporal reconciliation (governs everything else).** All five files defer on a run that
completed and reported before they arrived (fact 1, C-1). Per deferral, which reading applies?
(a) **Condition-discharged** — the deferral collapses; the deferred thing is available/due now.
(b) **Re-anchored** — the deferral's substance was the *data*, not the calendar event, and
re-anchors to the standing runner's own operation (which does not yet exist).
(c) **Per-item split** — you name which items take (a) and which (b).
The items needing the answer: the adversarial review's run condition (Q2); the availability of the
Anandkumar/convergence/Spinoza frames to the standing-runner session — the session itself is
licensed by an **adopted** ruling (fact 2), but the frames' status as its named inputs is a
designation made **inside these unadopted files** (F3/F4/F5) plus, in the adopted record, only
GS-CYB-1/GS-CYB-2/§5d and the register's rows — so their input-status becomes binding only with
your ruling here, and we propose (a) on that basis rather than assert it; multi-scale operator
design (F3/F5 — we read as (b): still deferred, now on the standing runner's data); the update
rule (Q8); F5's long-horizon deferral (Q9).

**Q2 — The adversarial review: does it run now, and on what data basis?** F1's own trigger —
"immediately after the §6 report is complete and before the standing-runner design session opens"
— is satisfied **in letter** (facts 1–2); whether in substance is exactly Q1's choice for this
item, since F1's stated rationale for the timing was data availability ("You cannot measure mean
reversion in a process that has not generated a trajectory"), and whether 20 cycles is that
trajectory is yours to say. The honest evidence both ways: F1 itself pre-authorizes running with
an absence — if GS-ATRF-3 is unresolved, "the review names the missing balancing loop as a
confirmed finding rather than a latent one" — while its instrumentation prep was framed for a
future run that is already over. Verified data situation (fact 5): per-candidate guardrail
results, virtue domains, and cycle outcomes **are** persisted for all 20 cycles; the proximity
delta is derivable at read time; per-candidate **oikeiosis-circle data is absent for the
historical 20 cycles regardless of when the authored migration is applied** — a gap the review
must name per F2's own named-gap constraint; completion-signal data does not exist at all
(fact 4). Sub-questions:
- **Q2a.** Run the full seven-probe review now, on the 20-cycle corpus + the current code and
  records, with the missing data named as gaps per F2's own constraint ("a named gap is an honest
  finding")? Or a split — our proposed cut, contestable: structural probes (5, 6, 7, and Probe
  2's structural surfaces) now, data-hungry probes (1, 3, 4, and Probe 2's completion-signal
  surface) re-run or extended against the standing runner's first operating window later — or a
  split you specify? Or defer whole?
- **Q2b.** Does Probe 6's special "run now" license stand as a separate early run, or is it
  absorbed if the whole review runs now? (The license is **unexercised** — a repo-wide search finds
  no Probe-6 run anywhere in the record.) If it runs, it should inherit the existing records that
  already cover parts of its ground (fact 12) rather than re-derive — confirm.
- **Q2c.** F1's third instrumentation item (a persisted cycle-over-cycle proximity delta): is
  read-time derivation sufficient for the review, or do you want it persisted for the standing
  runner (a standing-runner-design item, not a now-build)?
- **Q2d.** The review is a substantial governance analysis of live surfaces. Confirm its tier
  (`governance`, documents-only, PR19-reviewed output) and that its findings land as the input
  **F1 designates** for the standing-runner session — a designation that binds only with F1's
  adoption, not an already-ruled fact.

**Q3 — Correcting F2's briefing before any run.** The review's "what you know about the harness"
carries facts that have moved (C-4; facts 3, 4, 6, 7, 8, 11). Two options:
(a) the relaying session applies a **dated briefing-correction addendum** — each correction citing
its decision-log source, the instruction text itself untouched (the 08-24 precedent: your ruling
amended tasks in place at relay); or (b) you re-issue the briefing. Whichever option, the addendum
must cover, beyond the moved facts (C-4): (i) the component table's "ATRF blast-radius and
virtue-domain assessment" comparator is **designed-and-unbuilt**, not live — by F2's own
constraint an inference stated as a finding is a false positive; (ii) Probe 2's "binary pass/fail"
premise corrected — the live gate emits a graded `katorthoma_proximity` and a three-way
recommendation, so the probe should ask what of that available resolution the LOOP consumes, not
assert the gate lacks it; (iii) the "primary error signal" framing aligned to F3's own doctrine
("a point estimate, not a control signal") and GS-CYB-1's open, gated status (C-4b) — the review
must not inherit as a premise the thing GS-CYB-1 holds open; (iv) the Rajpal single-backward-edge
claim treated as recorded-but-not-independently-verified (fact 11); (v) Probe 3/7's
project-context items scoped to `/api/reason` (fact 6). Which option — and do you want to see the
correction addendum before the review runs?

**Q4 — The GS-ATRF status block.** F3 lists GS-ATRF-1..4 as open and forbids resolving them; the
sixteen rulings resolved or routed all four, GS-ATRF-4 is live, and GS-ATRF-3's endpoint is built
dark (facts 3–4; C-2). F3 itself half-knows this — its GS-ATRF-4 entry refers to "**GS-ATRF-1's
ruled four-virtue proxy**" while its own list marks GS-ATRF-1 open, an internal inconsistency
that supports the residuals reading. We read the prohibition's surviving force as: do not
pre-answer **what remains open** (Q-C2b at the standing-runner session; the GS-ATRF-4 vocabulary direction; §5d; the
per-item residuals your rulings themselves routed) — and the ATRF rulings record, not F3's block,
is the authoritative status list. Confirm or correct.

**Q5 — The surface-names collision, three-way (fact 9; C-3).** The first draft of this document
framed this as "F3 vs the record"; independent review corrected it — **F3's practice-on
definition matches your own ADOPTED 2026-08-01 fifth-circle-split vocabulary**, so the genuine
collisions reduce to: **logos-on** (three candidate referents: the 2026-08-01 fifth-circle
enforcement/documentation half; ADR-012's logos-mode = ENFORCE; F3's "Layer 3 project context
injection") and **"Layer 3"** (the live context-injection layer vs the not-live per-consumer
prose service), plus the standing overlap between the practice-on *program* name (2026-08-01) and
the `/practice-on` harness-toggle *skill* name (ADR-012 S8). Under the C15/D3 precedent
(vocabularies coexist only when each is explicitly canonical within its own named domain):
- **Q5a.** Confirm the canonical referent of **logos-on/off** and whether F3's context-injection
  assignment is an error to correct at adoption or a third domain-scoped usage; and rule the
  practice-on program-name/skill-name overlap explicitly domain-scoped (or rename one).
- **Q5b.** Specifically for "Layer 3": the repo now has two live referents circulating (the
  context-architecture layer that IS injected on `/api/reason`, and the per-consumer prose service
  that is NOT live). Naming discipline for records going forward?
- **Q5c.** F3's "Layer 1, 2, and 3 verified live" — which referent did you intend? (Fact 9 states
  the live status of each referent.)

**Q6 — Family enumeration and capture (C-7).** Please confirm the canonical set and its order —
our reading: the 2026-08-24 Cybernetics Instructions (executed) as the landed first member; F2(+F1)
= the adversarial-review instruction and its timing rule; F3 = the Anandkumar/neural-control
instruction; F4 = the convergence instruction; F5 = the integrating Spinoza capstone that names the
four-part frame. On adoption, each will be captured verbatim in-repo per the 08-22 compliance
precedent (none currently has an in-repo record; all five sit untracked in `inbox/`). Confirm the
enumeration so the capture records carry the right names and relationships.

**Q7 — Sequencing into the standing-runner design session.** Proposed order, for confirmation or
amendment: (1) adopt **those of the five you rule adoptable**, as corrected under these rulings,
with verbatim capture (F1 in particular is a timing answer whose moment may have passed entirely
under Q1(a), leaving a record to capture rather than an instruction to adopt); (2) run the
adversarial review per Q2; (3) run the nine-candidate classification (ruled to gate the
standing-runner session's CLOSE — may it fold into the review's Probe-1/novelty work, or do you
want it separate?); (4) complete the ATRF row of the named-input register (owed before the session
opens); (5) open the standing-runner design session with the four frames presented together (F5's
instruction) plus the review's findings. Also: the O-C Gate-3 design session is independently
licensed — any ordering constraint between it and the standing-runner session, or genuinely
parallel tracks?

**Q8 — The update rule's data prerequisite (C-6, C-8).** F4 holds the update-rule question
unanswerable "before the bounded validation run has produced real completion signal data" — the
run produced none and none exists (fact 4). If the standing-runner session opens now, it must
design the completion-signal consumption and the update rule **without observed completion-signal
behaviour**. Which shape: (a) design-with-disclosed-absence (the Q-F1 defer-with-absence-disclosed
pattern — design the rule, name the evidence gap, phase activation on the standing runner's own
first observed signals); or (b) split the update rule out of the session's first pass entirely,
gated on the standing runner's first operating window? (We note GS-CYB-2 is the ruled home either
way — C-8 resolved by reading, flagged for confirmation.)

**Q9 — F5's long-horizon deferral, re-anchored.** "Do not propose architectures that claim to
implement [agent-side scientia intuitiva] before the bounded validation run has produced the data
that would make such a claim honest" — condition satisfied in letter (the run happened), not in
substance (20 cycles is not long-horizon trajectory data). Confirm the deferral re-anchors to the
standing runner's own longitudinal operation, with the question carried exactly as F5 frames it:
named orientation, never a design directive.

**Q10 — F3's standing-rules block vs. the numbered rule surface (C-9).** F3's rules substantially
restate PR20 (verify at source), the name-departures discipline, the per-flag lesson (honoured at
slice 2 under your own Q6 ruling), and the phased-not-carve-out principle (your Q3/addendum-2
rulings). Adopt the block as **pointers to the existing numbered rules and recorded lessons**
rather than as a parallel unnumbered rule surface? (Grounding: the perimeter-count lesson — an
instruction embedded in a drifting artifact does not arrest the drift; two rule surfaces saying
almost the same thing will diverge.)

**Q11 — F5's universal evaluation criterion: governance or orientation?** (Added on independent
review.) F5 states: "**Every design decision should be evaluated against this purpose.** A design
that produces outputs resembling wisdom without building the prerequisite is not serving the
purpose — it is bypassing it." That is a paradigm-level design test — the one genuinely
*normative* addition in the family, sitting inside files whose refrain is "nothing licenses a
build." On adoption, is this criterion **binding governance** (a standing evaluative rule
alongside e.g. weights-BLOCKED, engaged at design/scoping sessions), or **orientation prose**
(carried, cited, but not a rule)? If binding: where does it live (manifest section? the
cybernetic-control governing document?) and what engages it?

---

## §6 — What this session did NOT do

No file adopted or executed; no verbatim capture yet (queued on Q6); no adversarial review run
(queued on Q1/Q2/Q3); no build, route, flag, schema, credential, migration, or public-surface
change; the standing-runner session not opened; nothing here bears on the 0h call; **weights
BLOCKED unchanged**; the Q1 hard constraint (the loop proposes; it never executes) unchanged.

## §7 — Assessment notes (clearly labelled; not recommendations on blocked items)

- The five files are mutually coherent as a *conceptual* family and largely convergent with the
  project's own recorded discipline (see the convergences in §4); the contradictions are almost
  all **temporal** (one stale authoring baseline) and **nominal** (the names block), not
  doctrinal. Nothing in them presses against weights-BLOCKED, the Q1 constraint, or the
  measurement-instrument reframe — F2/F3/F4 restate all three correctly.
- The one place a reading could quietly unblock something: Q1(a) applied to the adversarial
  review makes a substantial new work item due immediately. We flag that this document's Q2
  therefore *asks* rather than schedules — the review runs only on your ruling and the founder's
  election.
- The instrumentation worry in F1 is partially moot: the schema already recorded per-candidate
  guardrail results, virtue domains, and cycle outcomes for all 20 cycles (fact 5). **Two genuine
  gaps remain on the historical corpus**: completion-signal data, and per-candidate
  oikeiosis-circle data — neither can be conjured by any timing choice; only the standing runner's
  own operation (with the migration applied) produces them going forward.

## §8 — Independent adversarial review of this document (run before relay)

Per the twice-named 08-24 pattern (a mentor-question draft that reasoned toward "the blocked thing
is not really blocked" and was rewritten only after adversarial review), three fresh-context
reviewers examined this document before relay; **every confirmed finding is folded above**:

- **Claims-vs-source** (verified every §2 fact, every load-bearing quote, every decision-log ID
  and line cite against primary sources): 1 WRONG (the F3 "self-identifies as second" attribution
  — corrected: F4 assigns the ordinal), 2 NITs (a quote splice; line-cite drift) — all folded;
  all other claims CONFIRMED, including the injection asymmetry (fact 6) traced to the sandwich
  branch's own boundary lines and the presumed-unapplied ATRF-EE walk independently corroborated.
- **Reasoning-toward-unblocking**: 2 HIGH (Q1 and Q2d treated designations made *inside the
  unadopted files* as already-ruled facts), 2 MEDIUM (Q2 asserted F1's trigger "satisfied now"
  ahead of Q1's own choice; the historical oikeiosis-circle data gap was waved off), 3 LOW — all
  folded; probed-clean: the weights-BLOCKED handling, the §3 two-readings frame, the Q3 precedent
  characterization.
- **Completeness** (built its own contradiction inventory from the five files + verified state
  before reading this document): found the 2026-08-01 adopted practice-on/logos-on vocabulary
  (re-grounding Q5), two F2 briefing mechanism errors (the unbuilt-comparator listing; the
  binary-gate premise), the F2-vs-F3 error-signal/point-estimate tension (C-4b), F5's normative
  criterion (now Q11), and F3's internal "ruled four-virtue proxy" inconsistency (now cited in
  Q4); judged C-7 a category slip rather than a count conflict (folded) and C-8/C-9 correctly
  weighted. Verdict: the question set covers the genuine ruling needs with those additions.

One reviewer died mid-pass on an account session limit and was resumed to completion after the
reset (the PR19-codified pattern). The 2026-08-01 finding was re-verified first-hand against
`D-AGENT-CIRCLES-MENTOR-VERDICTS-ADOPTED-PLANS-AUTHORED-2026-08-01` before folding; the graded
gate output was confirmed first-hand from this session's own GUARD-CAUTION frame.

*End. Verbatim capture of the five files follows adoption, per Q6.*
