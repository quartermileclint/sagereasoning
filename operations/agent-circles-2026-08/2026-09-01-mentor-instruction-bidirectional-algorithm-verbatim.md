# Mentor instruction — the bidirectional algorithm, two named candidates for the standing-runner design track — VERBATIM

**Date:** 2026-09-01. **Status:** relayed by the founder; **NOT a ruling.** The instruction states this
of itself, twice, in its own words: both connection points are named as *"a candidate ... not a
ruling"*, and *"the executing session does not build either of these now."*
**Verbatim wins over every summary, including this header.**

**Relay channel:** founder-to-session, direct message. No source document was supplied; this file is
the primary record. **PR20 note:** no present-tense mechanism fact is asserted by the instruction
itself — it proposes a candidate framing against two already-open questions, and does not claim any
code, schema, or production state. Nothing here needed source-verification before capture on that
account; the connection to the two named open questions (GS-ATRF-1, GS-CYB-1/the generation step) is
first-hand re-derived below, against `operations/primal-substrate-2026-08/00-PRIORITY-INDEX.md`, not
assumed from the instruction's own framing.

---

> the bidirectional algorithm has two specific connection points in the project's open questions.
>
> First, GS-ATRF-1's blast-radius proxy. The reverse algorithm suggests a more principled basis for
> the proxy: blast radius as a topological property of how far a candidate has moved from the
> generalisation of the practitioner's current state — measured by the number and kind of expanding
> moves required, not by task-level consequence assessment. This is consistent with the proxy being
> disclosed as an assessed classification. The executing session carries this framing into the
> standing-runner design session as a named candidate for GS-ATRF-1's resolution, not a ruling.
>
> Second, the generation step design. The reverse algorithm is a candidate architecture: anchor the
> practitioner's current state as the clean residual structure, insert generative fingers at
> virtue-domain and oikeiosis-circle branch points, apply expanding moves scored by the four-virtue
> evaluative layer, ground candidates in concrete actions, prune with a forward pass. The topological
> constraint — continuous connection to the core — is the natural analogue of the weights constraint
> and may address the GS-CYB-1 concern without requiring a numerical weighting function. This is
> carried into the standing-runner design session as a named candidate, not a ruling.
>
> The two independent conditions of the amended GS-CYB-1 must be examined before any weighting or
> scoring function is introduced into the generation step.
>
> The executing session does not build either of these now. They are named candidates for the
> standing-runner design session. The current sequencing — fresh endpoint scope, watching table
> scope, generation-step scope, first build gate, bounded validation run, standing-runner design —
> is unchanged.

---

## What this session did with it (capture only — no design work performed)

This session is `governance`, documents-only. It captured the instruction verbatim (above), traced
its two connection points against the current state of the named-input register, and added two
pointer rows so a future session opening the standing-runner track's receiving session inherits
them the way every other named input in that register does. **No design element was evaluated, no
proxy basis was chosen, no generation-step architecture was adopted, and GS-CYB-1's gate was not
touched** — the instruction's own closing sentence ("does not build either of these now") is honoured
by construction: nothing here is a design act.

### Connection 1 — GS-ATRF-1 (re-verified, not assumed)

`00-PRIORITY-INDEX.md`'s current GS-ATRF-1 entry (§ "GS-ATRF-1 (blast-radius four-virtue proxy) —
resolved") records the **ruled 2026-08-11 answer as untouched**, with its one open gap (the
`friction_detection` basis-lessness case, §(c-bis)) closed by GS-ATRF-4's `unknown` category — **not**
by any amendment to GS-ATRF-1's own basis. This instruction proposes something different in kind: not
a fix to the basis-lessness gap, but an alternative **underlying justification** for the proxy
itself — topological distance of required expanding moves, in place of task-level consequence
assessment — while leaving the ruled classification vocabulary and its disclosed-assessment posture
untouched (*"consistent with the proxy being disclosed as an assessed classification"*). It is
recorded as a **named candidate for GS-ATRF-1's resolution**, explicitly not reopening the 2026-08-11
ruling on its own.

### Connection 2 — the generation-step design, gated by GS-CYB-1

`00-PRIORITY-INDEX.md`'s GS-CYB-1 row (`agent-circles-2026-08/2026-08-24-agent-cybernetic-control-
architecture.md` §3) already **"carries the weights-BLOCKED standing constraint as a gate that
PRECEDES examination"** for exactly the surface this instruction's second connection point touches —
any weighting or scoring function in the generation step. The instruction does not ask that gate to
move; it proposes a candidate architecture (anchoring the practitioner's current state, generative
fingers at virtue-domain/oikeiosis-circle branch points, four-virtue-scored expanding moves, a
forward-pass prune) and observes that its **topological continuous-connection-to-core constraint**
may be the structural analogue the weights-BLOCKED gate is protecting against — a way to shape
candidate generation without a numerical weighting function. The instruction is explicit that
**GS-CYB-1's two independent conditions must still be examined before any weighting or scoring
function enters the generation step** — this candidate does not discharge that gate; it is offered as
a possible route that might make the gate's underlying concern moot for this one architecture,
which is itself a question for the standing-runner track to examine, not something settled here.

### Sequencing — unchanged, confirmed against the register

The instruction states the sequencing is unchanged: fresh endpoint scope → watching table scope →
generation-step scope → first build gate → bounded validation run → standing-runner design. This
matches `00-PRIORITY-INDEX.md`'s own recorded sequence (the same six-step chain, `06-PLAIN-TEXT-
MIRROR.md` item 12's numbered list) — no drift found, nothing corrected.

---

## Register rows added

Both routed to **"Standing-runner design"** in `00-PRIORITY-INDEX.md`'s named-input table — but note
R8 (the design session with that exact name) already ran 2026-08-30; per that table's own framing
note, *"the receiving session for what survives is the standing-runner track's next design-capable
session."* These two rows carry the same receiving-session label as every other row in that table
for consistency with the existing convention, and rely on that framing note (not a per-row
restatement) to make clear which session actually inherits them.

1. **GS-ATRF-1's blast-radius proxy — a candidate alternative basis** (topological distance of
   expanding moves, vs. task-level consequence assessment), named, not ruled.
2. **The generation-step architecture — a candidate design** (the reverse-algorithm shape: anchor,
   generative fingers, four-virtue-scored expanding moves, grounding, forward-pass prune), gated by
   GS-CYB-1's unmoved two-condition examination requirement, named, not ruled.
